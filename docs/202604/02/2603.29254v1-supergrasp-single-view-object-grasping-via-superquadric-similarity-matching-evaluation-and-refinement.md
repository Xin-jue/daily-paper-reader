---
title: "SuperGrasp: Single-View Object Grasping via Superquadric Similarity Matching, Evaluation, and Refinement"
title_zh: SuperGrasp：通过超二次曲面相似性匹配、评估与精细化实现的单视图物体抓取
authors: "Lijingze Xiao, Jinhong Du, Yang Cong, Supeng Diao, Yu Ren"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29254v1"
tags: ["query:q2"]
score: 9.0
evidence: 机械臂操作与物体抓取
tldr: 针对单视角下几何信息缺失导致的抓取不稳定问题，本文提出SuperGrasp框架。该框架分为两阶段：首先通过超二次曲面相似性匹配从预构建基元库中快速检索初始抓取候选；随后利用E-RNet网络对抓取区域进行局部锚定、评估与精细化调整。实验证明该方法在仿真和现实场景中均具有极强的泛化能力和稳定性。
selection_source: fresh_fetch
motivation: 解决单视角观测下由于物体几何信息不完整而难以生成稳定有效抓取位姿的挑战。
method: 提出一种结合超二次曲面相似性匹配检索初始位姿，并利用E-RNet进行局部区域评估与精细化调整的两阶段框架。
result: 在包含10万个标注的大规模数据集上训练，并在仿真及实物实验中展现出优异的抓取成功率和对新物体的泛化性。
conclusion: SuperGrasp通过几何基元匹配与深度学习精细化的结合，显著提升了单视角机器人抓取的可靠性。
---

## 摘要
从单视图观测中进行机器人抓取仍然是操作领域中的一个关键挑战。现有方法在面对不完整的几何信息时，仍难以生成稳定且有效的抓取位姿。为了解决这些局限性，我们提出了 SuperGrasp，这是一个用于平行爪夹持器单视图抓取的创新两阶段框架，它将抓取过程分解为初始抓取位姿生成以及随后的抓取评估与精细化。在第一阶段，我们引入了一个相似性匹配模块，通过将输入的单视图点云与基于超二次曲面系数预计算的原语数据集进行匹配，从而高效地检索候选抓取。在第二阶段，我们提出了 E-RNet，这是一个端到端网络，它扩展了抓取感知区域，并将初始抓取闭合区域作为局部锚点区域，从而实现对候选抓取更准确、更可靠的评估与精细化。为了增强泛化能力，我们构建了一个包含 1500 个原语的用于相似性匹配的原语数据集，并收集了一个包含来自 124 个物体的 10 万个稳定抓取标签的大规模点云数据集，用于网络训练。在仿真和真实环境中的广泛实验表明，我们的方法在不同场景和新颖物体上均实现了稳定的抓取性能和强大的泛化能力。

## Abstract
Robotic grasping from single-view observations remains a critical challenge in manipulation. Existing methods still struggle to generate stable and valid grasp poses when confronted with incomplete geometric information. To address these limitations, we propose SuperGrasp, a novel two-stage framework for single-view grasping with parallel-jaw grippers that decomposes the grasping process into initial grasp pose generation and subsequent grasp evaluation and refinement. In the first stage, we introduce a Similarity Matching Module that efficiently retrieves grasp candidates by matching the input single-view point cloud with a pre-computed primitive dataset based on superquadric coefficients. In the second stage, we propose E-RNet, an end-to-end network that expands the graspaware region and takes the initial grasp closure region as a local anchor region, enabling more accurate and reliable evaluation and refinement of grasp candidates. To enhance generalization, we construct a primitive dataset containing 1.5k primitives for similarity matching and collect a large-scale point cloud dataset with 100k stable grasp labels from 124 objects for network training. Extensive experiments in both simulation and realworld environments demonstrate that our method achieves stable grasping performance and strong generalization across varying scenes and novel objects.

---

## 论文详细总结（自动生成）

这是一份关于论文《SuperGrasp: Single-View Object Grasping via Superquadric Similarity Matching, Evaluation, and Refinement》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在单视图（Single-View）观测下，由于遮挡和传感器噪声导致的**几何信息不完整**，机器人难以生成稳定且有效的平行爪抓取位姿。
*   **研究动机**：
    *   现有的采样评估法（如 GPD）在稀疏点云上生成的候选位姿不可靠。
    *   端到端方法（如 Contact-GraspNet）虽然高效，但在处理未见过的物体或复杂堆叠场景时泛化能力有限。
    *   超二次曲面（Superquadrics）作为一种紧凑的几何表示，能有效补全物体形状，但以往研究多假设点云完整，在单视图下的拟合精度和尺度对齐存在挑战。

### 2. 方法论
SuperGrasp 提出了一个两阶段框架：

*   **第一阶段：超二次曲面相似性匹配（Candidate Generation）**
    *   **基元数据库**：预先构建包含 1500 个具有不同形状和尺度的几何基元（长方体、圆柱体等）的数据库，并标注高质量抓取位姿。
    *   **快速匹配算法**：利用 EMS（期望最大化搜索）拟合目标物体的超二次曲面参数（尺寸 $a_x, a_y, a_z$ 和形状 $\epsilon_1, \epsilon_2$）。
    *   **两步检索**：先通过形状参数和归一化比例进行粗筛选，再通过绝对尺度差异进行精细排序。
    *   **位姿迁移**：将匹配度最高的 Top-N 基元的抓取位姿通过变换矩阵 $T$ 迁移至目标物体，并进行碰撞过滤。

*   **第二阶段：E-RNet 评估与精细化（Evaluation & Refinement）**
    *   **核心思想**：不仅关注抓取闭合区域，还通过扩展感知区域来捕捉周围的空间上下文。
    *   **网络结构**：以 PointNet++ 为骨干网络。将初始抓取闭合区作为“锚点区域（Anchor Region）”，提取其局部特征并与周围几何特征融合。
    *   **多头输出**：
        1.  **评估头**：预测当前抓取位姿的成功概率。
        2.  **精细化头**：预测三种离散调整（沿接近方向加深、顺/逆时针旋转 15°）的可行性，从中选择最优调整方案。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 YCB、KIT 和 DexNet 中选取 124 种物体，在 PyBullet 仿真中生成 10 万个带有稳定标签（经过扰动测试）的样本。
    *   **基元库**：1.5k 个手工构建的几何体。
*   **实验场景**：
    *   **仿真**：分为中等杂乱（10个物体）、高度杂乱（20个物体）以及未见过物体（10个新物体）三种设置。
    *   **实物**：使用 xMate ER7Pro 机械臂、ZED 2i 相机，测试相似物体和全新物体的抓取。
*   **对比基准（Benchmarks）**：对比了经典的 **PointNetGPD** 和先进的端到端方法 **Contact-GraspNet**。
*   **评价指标**：抓取成功率 (GSR)、任务成功率 (TSR) 和抓取尝试次数 (GA)。

### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA GeForce RTX 4090 GPU**。
*   **训练细节**：训练了 100 个 epoch，batch size 为 128。
*   **优化器**：Adam 优化器，初始学习率 0.001，权重衰减 $3 \times 10^{-4}$。
*   **耗时**：文中给出了推理时间统计，单次抓取全流程耗时约 **0.789 秒**（第一阶段 0.356s，第二阶段 0.433s）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验中，每种设置均进行了 30 轮独立试验，确保了统计意义。
    *   实物实验中，每种场景进行 10 轮试验。
    *   **消融实验**：设计了“阶段1+PointNetGPD”、“PointNetGPD+阶段2”以及“无精细化分支”等多组对比，充分验证了每个模块的贡献。
*   **客观性**：实验采用了相同的随机种子和场景配置，对比方法均在相同环境下运行，评价标准统一，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：在所有仿真和实物场景中，SuperGrasp 的抓取成功率和任务成功率均显著优于对比方法。特别是在**高度杂乱场景**下，其 GSR 比 Contact-GraspNet 高出约 17%。
*   **泛化能力强**：在面对训练集中未出现的全新物体时，任务成功率仍保持在 94% 以上，证明了超二次曲面匹配在处理未知几何形状方面的有效性。
*   **精细化的必要性**：消融实验显示，精细化模块在复杂几何物体和极度拥挤场景中对提升成功率至关重要（提升约 8.39%）。

### 7. 优点（亮点）
*   **几何与学习的结合**：巧妙地利用超二次曲面这一传统几何工具解决了单视图下的“形状补全”问题，为深度学习提供了更高质量的输入。
*   **局部锚点设计**：E-RNet 通过锚定抓取闭合区并感知周边环境，比单纯看局部点云的方法更具鲁棒性。
*   **实战导向**：训练数据完全来自仿真，但无需任何微调即可直接迁移至真实世界（Sim-to-Real），表现出极强的鲁棒性。

### 8. 不足与局限
*   **实时性略逊**：虽然比 PointNetGPD 快得多，但由于是两阶段流程，总耗时（0.789s）仍高于纯端到端方法 Contact-GraspNet（0.333s）。
*   **遮挡极限**：实验观察到，主要的失败案例仍源于严重的自遮挡或环境遮挡，导致拟合的超二次曲面偏离实际，进而引发碰撞。
*   **离散精细化**：目前的精细化仅限于三个预设动作，对于需要极高精度或连续微调的复杂操作可能不够灵活。

（完）

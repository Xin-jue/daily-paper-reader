---
title: "Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control"
title_zh: 基于漂移的策略优化：面向在线机器人控制的原生单步策略学习
authors: "Yuxuan Gao, Yedong Shen, Shiqi Zhang, Wenhao Yu, Yifan Duan, Jia pan, Jiajia Wu, Jiajun Deng, Yanyong Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03540v2"
tags: ["query:q2"]
score: 9.0
evidence: 用于高频机器人操作控制的一步生成式策略
tldr: 针对多步生成式策略（如扩散模型）在机器人控制中推理速度慢、难以实现高频闭环控制的问题，本文提出了一种原生单步生成式策略框架。该框架包含漂移基策略（DBP）和漂移基策略优化（DBPO），通过将迭代优化过程内化到模型参数中，实现了在保持多峰动作建模能力的同时，仅需单步推理即可完成动作生成。实验证明该方法在离线模仿学习和在线强化学习中均表现优异，推理速度提升达100倍，并成功应用于105.2 Hz的实机双臂机器人控制。
selection_source: fresh_fetch
motivation: 多步生成式策略因推理时需要多次迭代去噪，导致计算开销巨大，难以满足高频闭环控制和在线强化学习的实时性需求。
method: 提出漂移基策略（DBP）利用定点漂移目标将迭代细化过程内化为单步生成，并结合DBPO框架实现稳定的在线强化学习更新。
result: DBP在保持多峰建模能力的同时，推理速度比扩散策略快100倍，且在多个基准测试和实机双臂机器人任务中达到或超过了多步策略的性能。
conclusion: 该研究通过将细化过程从推理阶段转移到训练阶段，成功实现了高效、高性能的原生单步机器人控制策略。
---

## 摘要
虽然多步生成式策略通过建模多模态动作分布在机器人操作中取得了卓越性能，但它们在推理时需要多步迭代去噪。因此，生成每个动作都需要数十到数百次网络函数评估（NFEs），这使得它们在高频闭环控制和在线强化学习（RL）中成本高昂。为了解决这一局限性，我们提出了一种原生单步生成式策略的两阶段框架，将细化过程从推理阶段转移到训练阶段。首先，我们引入了基于漂移的策略（DBP），它利用不动点漂移目标将迭代细化内化到模型参数中，从而在设计上构建出单步生成式骨干网络，同时保留了多模态动作建模能力。其次，我们开发了基于漂移的策略优化（DBPO），这是一种在线强化学习框架，为预训练的骨干网络配备了兼容的随机接口，在不牺牲单步部署特性的情况下实现了稳定的同策略（on-policy）更新。广泛的实验证明了所提框架在离线模仿学习、在线微调和真实世界控制场景中的有效性。DBP 在推理速度提升高达 100 倍的同时，性能达到或超过了多步扩散策略。在具有挑战性的操作基准测试中，它也始终优于现有的单步基准模型。此外，DBPO 在在线设置中实现了有效且稳定的策略改进。在真实双臂机器人上的实验证明了 105.2 Hz 的可靠高频控制。

## Abstract
Although multi-step generative policies achieve strong performance in robotic manipulation by modeling multimodal action distributions, they require multi-step iterative denoising at inference time. Each action therefore needs tens to hundreds of network function evaluations (NFEs), making them costly for high-frequency closed-loop control and online reinforcement learning (RL). To address this limitation, we propose a two-stage framework for native one-step generative policies that shifts refinement from inference to training. First, we introduce the Drift-Based Policy (DBP), which leverages fixed-point drifting objectives to internalize iterative refinement into the model parameters, yielding a one-step generative backbone by design while preserving multimodal action modeling capacity. Second, we develop Drift-Based Policy Optimization (DBPO), an online RL framework that equips the pretrained backbone with a compatible stochastic interface, enabling stable on-policy updates without sacrificing the one-step deployment property. Extensive experiments demonstrate the effectiveness of the proposed framework across offline imitation learning, online fine-tuning, and real-world control scenarios. DBP matches or exceeds the performance of multi-step diffusion policies while achieving up to $100\times$ faster inference. It also consistently outperforms existing one-step baselines on challenging manipulation benchmarks. Moreover, DBPO enables effective and stable policy improvement in online settings. Experiments on a real-world dual-arm robot demonstrate reliable high-frequency control at 105.2 Hz.

---

## 论文详细总结（自动生成）

这是一份关于论文《Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前主流的生成式机器人策略（如扩散策略 Diffusion Policy）虽然能很好地处理动作的多模态性，但其推理过程依赖于多步迭代去噪（通常需要数十到数百次网络评估 NFE），导致推理延迟高，难以满足高频闭环控制（>100Hz）和高效在线强化学习的需求。
*   **研究动机**：现有的加速方法（如蒸馏或一致性模型）通常是事后对预训练模型进行压缩，而非原生的一步生成。本文旨在设计一种**原生单步（Native One-Step）**生成式策略，将迭代细化的过程从“推理阶段”转移到“训练阶段”，从而在保持高性能的同时实现极速推理。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：基于“漂移模型（Drifting Models）”原理，通过在训练时构建一个漂移场（Drift Field），让模型参数直接学习如何将随机噪声映射到高质量的动作分布，从而在推理时只需一次前向传播。
*   **关键技术细节**：
    *   **漂移基策略 (DBP)**：在训练中引入“吸引-排斥”机制。模型生成的多个假设（Hypotheses）会被吸引向专家演示动作，同时在彼此之间产生排斥力以防止模式崩溃（Mode Collapse），从而保留多模态建模能力。
    *   **不动点漂移目标**：通过停止梯度（stop-gradient）操作构建目标，使网络参数内化（Internalize）修正动力学。
    *   **漂移基策略优化 (DBPO)**：为了支持在线强化学习，为单步骨干网络添加了一个轻量级的随机适配器（Stochastic Adapter），使其能够计算精确的动作似然概率，从而兼容 PPO 算法进行同策略更新。
    *   **锚点正则化 (Anchor Regularization)**：在在线微调时，通过约束当前策略不偏离预训练的漂移流形，保证了训练的稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **离线模仿学习**：Diffusion Policy 标准套件（12个任务，包括 Push-T, Kitchen 等）；基于点云的复杂操作（37个任务，涵盖 Adroit 手部操作和 Meta-World 任务）。
    *   **在线强化学习**：RoboMimic 视觉操作任务和 D4RL 运动任务。
    *   **真实世界部署**：使用双臂 UR5 机器人完成 Lift（提升）、Can（罐子操作）和 Bimanual Transport（双臂运输）任务。
*   **Benchmark 与对比方法**：
    *   **多步模型**：Diffusion Policy (DP), DP3。
    *   **单步模型**：MP1, OMP, FlowPolicy, AdaFlow, Consistency Policy 等。
    *   **在线 RL 方法**：ReinFlow, DMPO。

### 4. 资源与算力
*   **算力设备**：实验使用了 **8 张 NVIDIA RTX 3090 GPU**。
*   **训练时长**：
    *   Adroit 任务训练 3000 个 epoch。
    *   Meta-World 任务训练 1000 个 epoch。
    *   在 Push-T 任务上，DBP 仅需 100 个 epoch 即可收敛，而扩散策略通常需要 4500 个 epoch，训练效率显著提升。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2D 图像到 3D 点云、从离线模仿到在线强化学习、从仿真环境到真实物理机器人的全方位验证。
*   **充分性**：
    *   在 37 个点云任务上进行了大规模测试，样本量充足。
    *   进行了详细的消融实验，验证了假设数量 $G$、温度参数 $R$ 以及锚点正则化的有效性。
    *   每个实验均采用 3 个随机种子并报告均值和标准差，结果具有统计学意义，表现出较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能与速度双赢**：DBP 在推理速度提升 **100 倍**（从 100 NFE 降至 1 NFE）的情况下，在 Diffusion Policy 套件上的平均得分从 0.79 提升至 0.83。
*   **单步基准新高度**：在 37 个点云操作任务中，DBP 达到了 88.4% 的成功率，显著优于之前的单步领先方法 OMP (82.3%)。
*   **在线提升显著**：DBPO 证明了单步模型可以通过在线交互进一步突破模仿学习的性能天花板。
*   **实机可行性**：在真实双臂机器人上实现了 **105.2 Hz** 的高频控制，端到端延迟仅为 9.5ms。

### 7. 优点
*   **原生单步设计**：无需复杂的蒸馏过程，直接通过训练目标实现单步生成，架构简洁高效。
*   **多模态保持**：通过吸引-排斥机制，克服了传统单步模型容易陷入单模态（均值回归）的问题。
*   **训练效率极高**：相比扩散模型，收敛速度快数十倍，大幅降低了科研和生产的算力门槛。
*   **全栈覆盖**：从理论推导到仿真验证再到实机部署，形成了一个完整的闭环。

### 8. 不足与局限
*   **任务复杂度限制**：目前的实验主要集中在桌面操作和基础运动任务，对于长程（Long-horizon）复杂逻辑任务的验证相对较少。
*   **超参数敏感性**：漂移场中的温度参数 $R$ 和假设数量 $G$ 对性能有一定影响，可能需要针对不同任务进行微调。
*   **内存开销**：在“块模式（Chunk mode）”下，由于需要计算所有假设间的成对距离，当动作维度或预测步长极大时，显存占用会呈平方级增长。

（完）

---
title: Belief Dynamics for Detecting Behavioral Shifts in Safe Collaborative Manipulation
title_zh: 安全协作操作中检测行为转变的信念动力学
authors: "Devashri Naik, Divake Kumar, Nastaran Darabi, Amit Ranjan Trivedi"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.04967v1"
tags: ["query:q2"]
score: 9.0
evidence: ManiSkill任务中的安全协作机械臂操作
tldr: "在共享工作空间的协作任务中，机器人需及时检测合作伙伴的行为转变以确保安全。本文针对受控非平稳环境，提出了一种轻量级信念追踪模块 UA-TOM。该模块通过选择性状态空间动力学、因果注意力和预测误差信号增强冻结的视觉-语言-动作（VLA）模型。实验表明，UA-TOM 在 ManiSkill 任务中实现了最高的检测率（±3步内达85.7%），显著降低了碰撞风险，且推理开销极低，为安全协作提供了可靠保障。"
selection_source: fresh_fetch
motivation: 协作代理在任务中途切换策略会导致机器人基于过时假设采取不安全行动，因此需要可靠的方法来实时检测行为模式的转变。
method: 提出 UA-TOM 模块，利用选择性状态空间动力学、因果注意力和预测误差信号，在不修改基础策略的前提下增强 VLA 模型的信念追踪能力。
result: "UA-TOM 在 ±3 步容差下的检测率达到 85.7%，优于其他非辅助方法，并将碰撞后的近距离接触时间缩短至 4.8 步。"
conclusion: 通过轻量级的信念动力学建模，可以在保持低计算开销的同时，显著提升机器人对动态协作环境中行为转变的感知与应对能力。
---

## 摘要
在共享工作空间中运行的机器人必须与在任务执行期间行为可能发生变化的其他智能体保持安全协调。当协作智能体在回合中途切换策略时，继续基于过时的假设进行操作可能导致不安全行为并增加碰撞风险。因此，可靠地检测此类行为机制切换至关重要。我们在 ManiSkill 共享工作空间操作任务中，研究了受控非平稳性下的机制切换检测。在十种检测方法和五个随机种子的实验中，启用检测使切换后的碰撞减少了 52%。然而，平均性能掩盖了显著的可靠性差异：在 ±3 步的现实容差下，检测率在 86% 到 30% 之间波动，而在 ±5 步下，所有方法均达到了 100%。我们提出了 UA-TOM，这是一个轻量级的信念追踪模块，它利用选择性状态空间动力学、因果注意力和预测误差信号来增强冻结的视觉-语言-动作（VLA）控制骨干网络。在五个种子和 1200 个回合的实验中，UA-TOM 在无辅助方法中实现了最高的检测率（±3 步下为 85.7%）和最低的近距离时间（4.8 步），表现优于 Oracle（5.3 步）。分析表明，在机制切换时，隐藏状态更新幅度增加了 17 倍，并在约 10 个时间步内衰减，而离散化步长收敛至接近常数的值（Δt ≈ 0.78），这表明灵敏度是由学习到的动力学驱动的，而非输入依赖的门控机制。在 Overcooked 中的跨领域实验展示了因果注意力和预测误差信号的互补作用。UA-TOM 引入了 7.4 毫秒的推理开销（占 50 毫秒控制预算的 14.8%），在不修改基础策略的情况下实现了可靠的机制切换检测。

## Abstract
Robots operating in shared workspaces must maintain safe coordination with other agents whose behavior may change during task execution. When a collaborating agent switches strategy mid-episode, continuing under outdated assumptions can lead to unsafe actions and increased collision risk. Reliable detection of such behavioral regime changes is therefore critical. We study regime-switch detection under controlled non-stationarity in ManiSkill shared-workspace manipulation tasks. Across ten detection methods and five random seeds, enabling detection reduces post-switch collisions by 52%. However, average performance hides significant reliability differences: under a realistic tolerance of +-3 steps, detection ranges from 86% to 30%, while under +-5 steps all methods achieve 100%. We introduce UA-TOM, a lightweight belief-tracking module that augments frozen vision-language-action (VLA) control backbones using selective state-space dynamics, causal attention, and prediction-error signals. Across five seeds and 1200 episodes, UA-TOM achieves the highest detection rate among unassisted methods (85.7% at +-3) and the lowest close-range time (4.8 steps), outperforming an Oracle (5.3 steps). Analysis shows hidden-state update magnitude increases by 17x at regime switches and decays over roughly 10 timesteps, while the discretization step converges to a near-constant value (Delta_t approx 0.78), indicating sensitivity driven by learned dynamics rather than input-dependent gating. Cross-domain experiments in Overcooked show complementary roles of causal attention and prediction-error signals. UA-TOM introduces 7.4 ms inference overhead (14.8% of a 50 ms control budget), enabling reliable regime-switch detection without modifying the base policy.

---

## 论文详细总结（自动生成）

这篇论文针对协作机器人领域中一个关键的安全挑战——**检测合作伙伴行为模式的动态转变**，提出了基于信念动力学的解决方案。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在共享工作空间的机器人协作中，机器人的安全性和效率高度依赖于对合作伙伴（如人类或其他机器人）行为策略的准确假设。
*   **研究动机**：合作伙伴的行为往往是非平稳的，可能在任务中途突然切换（例如从“协助者”变为“阻碍者”）。如果机器人不能及时察觉这种“机制切换”（Regime Switch），就会基于过时的假设产生错误动作，导致碰撞风险剧增。
*   **核心挑战**：现有的视觉-语言-动作（VLA）模型通常缺乏持久的信念追踪机制；而传统的变点检测方法在处理高维控制回路时响应速度较慢，难以满足实时安全需求。

### 2. 论文提出的方法论：UA-TOM
论文提出了一种名为 **UA-TOM**（Unassisted Theory of Mind）的轻量级信念追踪模块，旨在不修改基础控制策略（Frozen VLA）的前提下增强其感知能力。
*   **核心思想**：通过整合时间序列动力学、注意力机制和预测误差，维护一个关于合作伙伴行为类型的持久“信念状态”。
*   **关键技术组件**：
    1.  **选择性状态空间模型（Selective SSM）**：利用类似于 Mamba 的架构更新信念状态 $b_t$。其离散化步长 $\Delta t$ 在训练后趋于稳定，使系统对行为突变产生的扰动高度敏感。
    2.  **因果注意力（Causal Attention）**：在短时域内重新锚定信念，防止在观测稀疏时产生漂移。
    3.  **预测误差信号（Prediction-Error）**：直接计算合作伙伴实际动作与预测动作的差异，作为触发机制切换检测的直接证据。
    4.  **原型记忆（Prototype Memory）**：通过指数移动平均（EMA）维护各类行为的特征原型，提供类别参考。
*   **融合机制**：通过门控融合层整合上述信号，输出行为类型估计 $\hat{\theta}_t$ 和切换概率 $\hat{s}_t$。

### 3. 实验设计
*   **实验场景**：
    *   **ManiSkill**：双臂 Franka Panda 协作操作任务（抓取与放置）。合作伙伴有四种类型：协助者、竞争者、阻碍者、被动者。
    *   **Overcooked（跨领域验证）**：离散动作空间的协作烹饪任务，验证模型在不同任务结构下的泛化性。
*   **基准测试（Benchmarks）**：
    *   **序列模型**：GRU, Transformer, Mamba。
    *   **代理建模方法**：LIAM, BToM（贝叶斯心理理论）。
    *   **经典方法**：BOCPD（贝叶斯在线变点检测）。
    *   **对照组**：Oracle（完美检测）、Context-Conditioned（已知类型）、No-Detection（无检测）。
*   **评价指标**：硬检测率（±3步和±5步容差）、检测延迟、切换后碰撞次数、近距离危险时间（CRT）。

### 4. 资源与算力
*   **算力设备**：使用 **NVIDIA RTX 6000 Ada GPU**。
*   **训练时长**：UA-TOM 模块极其轻量（仅 992K 参数），在冻结骨干网络的情况下，训练 50 个 Epoch 仅需约 **7 分钟**。
*   **推理开销**：单次前向传播耗时 **7.41 毫秒**，仅占 50 毫秒控制周期的 14.8%，完全满足实时控制要求。

### 5. 实验数量与充分性
*   **实验规模**：在 ManiSkill 中进行了 5 个随机种子、共 1200 个回合的测试；在扩展评估中使用了 10 个种子。
*   **充分性**：实验覆盖了 12 种不同的行为转换类型（如“协助→阻碍”），并进行了跨领域的 Overcooked 实验。
*   **公平性**：所有基线模型在相同的多任务目标、训练数据和超参数预算下进行训练，确保了对比的客观性。

### 6. 主要结论与发现
*   **检测的重要性**：启用任何形式的检测都能使切换后的碰撞率降低 **52%**。
*   **可靠性鸿沟**：在 ±5 步的宽松容差下，所有方法表现接近；但在 **±3 步（150ms）** 的现实容差下，UA-TOM 以 **85.7%** 的检测率位居榜首，而传统模型（如 GRU）跌至 30% 左右。
*   **平滑适应的优势**：UA-TOM 的近距离危险时间（4.8步）甚至低于 Oracle（5.3步）。这是因为 UA-TOM 的信念更新是平滑的，避免了 Oracle 那种瞬时策略切换导致的轨迹不连续。
*   **机制分析**：在行为切换瞬间，UA-TOM 的隐藏状态更新幅度会激增 **17 倍**，这种“脉冲式”响应是其高灵敏度的来源。

### 7. 优点
*   **轻量且模块化**：无需重新训练庞大的 VLA 模型，作为插件即可提升安全性能。
*   **高可靠性**：在严格的时间窗口内表现稳健，显著优于现有的代理建模和序列模型。
*   **可解释性**：通过对 SSM 内部动力学的分析，揭示了模型是如何通过预测误差扰动来捕捉行为转变的。
*   **跨领域适应性**：在连续（机械臂）和离散（Overcooked）任务中均表现良好，并识别出不同组件在不同任务中的贡献差异。

### 8. 不足与局限
*   **合作伙伴行为简化**：实验中的合作伙伴策略是预设的脚本化行为，可能无法完全模拟人类行为的复杂性和随机性。
*   **离散类型假设**：模型假设行为模式属于有限的离散集合，对于渐进式或混合式的行为转变可能效果受限。
*   **观测依赖**：虽然 VLA 骨干处理视觉，但 UA-TOM 模块主要依赖于提取后的状态特征，对于纯视觉端到端的信念追踪能力尚待进一步验证。

（完）

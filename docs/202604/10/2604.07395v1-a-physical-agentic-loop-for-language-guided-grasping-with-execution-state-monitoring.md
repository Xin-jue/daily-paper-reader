---
title: A Physical Agentic Loop for Language-Guided Grasping with Execution-State Monitoring
title_zh: 一种带有执行状态监测的语言引导抓取物理智能体循环
authors: "Wenze Wang, Mehdi Hosseinzadeh, Feras Dayoub"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07395v1"
tags: ["query:q2"]
score: 9.0
evidence: 带有执行状态监测的语言引导抓取操作
tldr: 针对机器人语言引导抓取中常见的单次执行且缺乏反馈的问题，本文提出一种受数字智能体启发的物理智能体循环。该方法通过引入Watchdog监控层，将嘈杂的夹爪遥测数据转化为离散的执行状态（如抓空、滑落等），并结合确定性策略实现自动重试或人工求助。在移动协作机器人上的实验证明，该闭环系统在处理视觉歧义和执行失败时比开环系统更具鲁棒性和可解释性。
selection_source: fresh_fetch
motivation: 现有的语言引导抓取系统多为开环执行，无法有效识别和处理抓取过程中的物理失败或语义错误。
method: 提出一种物理智能体循环，利用Watchdog监控层融合传感器数据输出执行状态，并配合确定性策略进行重试或升级处理。
result: 在包含视觉歧义和诱导性失败的场景下，该系统显著提升了抓取的成功率和行为的可解释性。
conclusion: 通过引入显式的执行状态监控和有界恢复机制，可以在不改变底层抓取模型的情况下大幅增强机器人操作的鲁棒性。
---

## 摘要
遵循语言指令的机器人操纵系统通常以单次尝试（single-shot）的方式执行抓取原语：模型提出动作，机器人执行，而诸如空抓、打滑、停转、超时或语义错误的抓取等故障并未以结构化的方式反馈给决策层。受数字工具使用智能体中智能体循环（agentic loops）的启发，我们将语言引导的抓取重新表述为一个在具身执行状态上运行的有界具身智能体，其中物理动作会暴露显式的工具状态流。我们引入了一种物理智能体循环，它通过 (i) 基于事件的接口和 (ii) 执行监测层 Watchdog，封装了一个未经修改的学习型操纵原语（抓取并提起）。Watchdog 利用接触感知融合和时间稳定技术，将嘈杂的夹持器遥测数据转换为离散的结果标签。这些结果事件（可选择结合抓取后的语义验证）由一个确定性的有界策略处理，该策略负责完成、重试或向用户寻求澄清，从而保证有限次终止。我们在配备手眼 D405 相机的移动操作机上验证了该循环，保持底层抓取模型不变，并评估了涉及视觉歧义、干扰物和诱发执行故障的代表性场景。结果表明，显式的执行状态监测和有界恢复比开环执行实现了更稳健、更具可解释性的行为，同时增加了极小的架构开销。源代码和演示请参考我们的项目页面：https://wenzewwz123.github.io/Agentic-Loop/

## Abstract
Robotic manipulation systems that follow language instructions often execute grasp primitives in a largely single-shot manner: a model proposes an action, the robot executes it, and failures such as empty grasps, slips, stalls, timeouts, or semantically wrong grasps are not surfaced to the decision layer in a structured way. Inspired by agentic loops in digital tool-using agents, we reformulate language-guided grasping as a bounded embodied agent operating over grounded execution states, where physical actions expose an explicit tool-state stream. We introduce a physical agentic loop that wraps an unmodified learned manipulation primitive (grasp-and-lift) with (i) an event-based interface and (ii) an execution monitoring layer, Watchdog, which converts noisy gripper telemetry into discrete outcome labels using contact-aware fusion and temporal stabilization. These outcome events, optionally combined with post-grasp semantic verification, are consumed by a deterministic bounded policy that finalizes, retries, or escalates to the user for clarification, guaranteeing finite termination. We validate the resulting loop on a mobile manipulator with an eye-in-hand D405 camera, keeping the underlying grasp model unchanged and evaluating representative scenarios involving visual ambiguity, distractors, and induced execution failures. Results show that explicit execution-state monitoring and bounded recovery enable more robust and interpretable behavior than open-loop execution, while adding minimal architectural overhead. For the source code and demo refer to our project page: https://wenzewwz123.github.io/Agentic-Loop/

---

## 论文详细总结（自动生成）

这是一份关于论文《A Physical Agentic Loop for Language-Guided Grasping with Execution-State Monitoring》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的语言引导机器人抓取系统大多采用“单次尝试（Single-shot）”的开环模式。即机器人接收指令、规划动作、执行抓取，然后就此结束。
*   **研究背景**：在现实场景中，抓取经常因物理因素（如空抓、打滑、停转）或语义因素（抓错了物体）而失败。由于缺乏结构化的反馈机制，机器人无法感知失败，更无法自主决定是该重试、放弃还是向人类求助。
*   **整体含义**：本文受数字世界中“智能体循环（Agentic Loop）”的启发，提出将物理抓取动作视为一种带有状态反馈的“工具调用”，通过引入执行状态监测，构建了一个闭环的物理智能体系统。

### 2. 方法论：核心思想与关键技术
该方法的核心是将抓取过程封装为一个“观察-行动-评估-决策”的循环，而无需重新训练底层的抓取模型。
*   **核心思想**：引入一个名为 **Watchdog（看门狗）** 的监测层，将连续、嘈杂的传感器信号转化为离散、可理解的执行状态。
*   **关键技术细节**：
    *   **Watchdog 监测层**：利用夹爪的遥测数据（如电流、位置、受力）进行接触感知融合和时间稳定处理，输出 6 种离散状态：`SUCCESS`（成功）、`EMPTY`（空抓）、`SLIP`（打滑）、`WEAK`（不稳）、`STALL`（停转）、`TIMEOUT`（超时）。
    *   **语义验证（可选）**：在物理抓取成功后，通过视觉模型验证抓取的物体是否符合语言指令（如“我要红色的杯子”）。
    *   **有界决策策略（Bounded Policy）**：根据 Watchdog 的状态输出决定下一步动作：
        *   `FINALIZE`：任务完成，终止。
        *   `RETRY/RESELECT`：如果是 `EMPTY` 且在重试预算内，则重新尝试。
        *   `WAIT CLARIFY`：如果多次失败或语义模糊，则请求人类介入。
    *   **事件驱动接口**：所有模块通过标准化的事件模式通信，确保了系统的模块化和可解释性。

### 3. 实验设计
*   **实验平台**：使用 Hello Robot Stretch 3 移动协作机器人，配备 D405 手眼相机。
*   **底层模型**：使用未经修改的 **ForceSight** 作为抓取原语生成器。
*   **测试场景（Benchmark）**：
    1.  **视觉歧义**：颜色歧义（多个杯子选特定颜色）、空间歧义（左边/后边的物体）。
    2.  **干扰物鲁棒性**：目标物旁存在强干扰。
    3.  **诱导性失败**：人为干预制造空抓，测试系统的自动重试能力。
    4.  **无有效目标**：测试系统是否能正确识别并向用户反馈无法执行。
*   **对比方法**：以**开环执行（Open-loop）**作为基准（即只执行一次，不进行状态监测和恢复）。

### 4. 资源与算力
*   **语言模型**：使用了轻量级的本地大模型 **TinyLlama-1.1B** 用于指令解析。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法属于“包装器（Wrapper）”性质，主要侧重于推理和逻辑判断，且使用了轻量级模型，推测其对算力要求较低，可在移动机器人的车载计算单元上实时运行。

### 5. 实验数量与充分性
*   **实验规模**：在系统开发和评估过程中共进行了超过 **290 次** 真实机器人实验，其中 **180 次** 是针对最终系统的正式评估。
*   **充分性**：每个典型场景设置了 10 组对比实验。涵盖了物理失败（空抓）和语义失败（歧义）两个维度。
*   **客观性**：通过消融实验（如移除 Watchdog、移除语义预处理等）验证了各组件的贡献，实验设计较为客观，能够证明闭环逻辑对成功率的提升。

### 6. 主要结论与发现
*   **成功率显著提升**：在存在歧义和干扰的场景下，开环基准的成功率可能低至 0%-40%，而本文系统可提升至 80%-100%。
*   **鲁棒性增强**：通过 Watchdog 监测，系统能有效识别空抓并自动重试，或在无法处理时主动求助，避免了机器人的盲目操作。
*   **极低开销**：引入智能体循环带来的端到端运行时间增加极小（约 1 秒左右），证明了该架构的实用性。

### 7. 优点
*   **非侵入式设计**：不需要重新训练复杂的端到端策略，可以作为现有任何抓取算法的“外壳”。
*   **高可解释性**：通过离散的状态标签（如 `SLIP`），用户和开发者能清晰知道机器人为什么失败。
*   **安全性与有界性**：通过设置重试预算和明确的求助机制，保证了系统不会陷入无限循环或危险操作。

### 8. 不足与局限
*   **恢复策略单一**：目前的恢复动作主要是简单的“重试”，对于打滑（SLIP）或抓取不稳（WEAK）缺乏更精细的调整策略（如改变抓取角度）。
*   **感知依赖**：语义验证和歧义消除高度依赖于上游视觉模型的准确性，如果视觉模型本身判断错误，闭环逻辑也无法纠正。
*   **实验环境相对受控**：虽然涵盖了多种场景，但物体种类和环境复杂度仍处于实验室水平，尚未在极度杂乱或动态变化的家庭环境中大规模验证。

（完）

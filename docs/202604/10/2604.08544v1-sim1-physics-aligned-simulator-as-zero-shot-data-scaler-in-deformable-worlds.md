---
title: "SIM1: Physics-Aligned Simulator as Zero-Shot Data Scaler in Deformable Worlds"
title_zh: SIM1：可变形世界中作为零样本数据扩展器的物理对齐模拟器
authors: "Yunsong Zhou, Hangxu Liu, Xuekun Jiang, Xing Shen, Yuanzhen Zhou, Hui Wang, Baole Fang, Yang Tian, Mulin Yu, Qiaojun Yu, Li Ma, Hengjie Li, Hanqing Wang, Jia Zeng, Jiangmiao Pang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08544v1"
tags: ["query:q8"]
score: 10.0
evidence: 用于操作任务的物理对齐实-虚-实数据引擎
tldr: "SIM1是一个物理对齐的实到虚到实（real-to-sim-to-real）数据引擎，旨在解决变形物体机器人操作中数据获取难的问题。它通过将真实场景数字化为度量一致的孪生场景，并利用弹性建模校准动力学，结合扩散模型生成高质量合成轨迹。该方法实现了仅用合成数据训练即可在真实世界中达到90%的零样本成功率，显著提升了泛化能力。"
selection_source: fresh_fetch
motivation: 现有的仿真到现实流水线因缺乏物理对齐和变形动力学建模，难以满足复杂变形物体操作的数据需求。
method: 提出SIM1引擎，通过场景数字化、弹性动力学校准及基于扩散模型的轨迹生成与过滤，将少量演示扩展为大规模高质量合成数据。
result: 15。
conclusion: 物理对齐的仿真可以作为变形物体操作的可扩展监督信号，是实现高效策略学习的切实路径。
---

## 摘要
可变形物体的机器人操作是具身学习中一个数据密集型的领域，其中形状、接触和拓扑结构的共同演化方式远超刚体的多变性。尽管模拟技术有望缓解现实世界数据获取的成本压力，但主流的“模拟到现实”（sim-to-real）流程仍根植于刚体抽象，导致几何形状不匹配、软体动力学脆弱，以及不适用于布料交互的运动原语。我们认为，模拟之所以失败，并非因为它是合成的，而是因为它是未接地的。为了解决这一问题，我们推出了 SIM1，这是一个物理对齐的“现实-模拟-现实”（real-to-sim-to-real）数据引擎，旨在将模拟接地于物理世界。在给定有限演示的情况下，该系统将场景数字化为度量一致的孪生体，通过弹性建模校准可变形动力学，并通过带有质量过滤的基于扩散的轨迹生成来扩展行为。该流程将稀疏的观测转化为具有接近演示保真度的大规模合成监督信号。实验表明，在 1:15 的等效比例下，纯合成数据训练的策略达到了与真实数据基准相当的水平，同时在现实世界部署中实现了 90% 的零样本成功率和 50% 的泛化增益。这些结果验证了物理对齐模拟作为可变形操作的可扩展监督手段的有效性，并为高效数据策略学习提供了一条切实可行的路径。

## Abstract
Robotic manipulation with deformable objects represents a data-intensive regime in embodied learning, where shape, contact, and topology co-evolve in ways that far exceed the variability of rigids. Although simulation promises relief from the cost of real-world data acquisition, prevailing sim-to-real pipelines remain rooted in rigid-body abstractions, producing mismatched geometry, fragile soft dynamics, and motion primitives poorly suited for cloth interaction. We posit that simulation fails not for being synthetic, but for being ungrounded. To address this, we introduce SIM1, a physics-aligned real-to-sim-to-real data engine that grounds simulation in the physical world. Given limited demonstrations, the system digitizes scenes into metric-consistent twins, calibrates deformable dynamics through elastic modeling, and expands behaviors via diffusion-based trajectory generation with quality filtering. This pipeline transforms sparse observations into scaled synthetic supervision with near-demonstration fidelity. Experiments show that policies trained on purely synthetic data achieve parity with real-data baselines at a 1:15 equivalence ratio, while delivering 90% zero-shot success and 50% generalization gains in real-world deployment. These results validate physics-aligned simulation as scalable supervision for deformable manipulation and a practical pathway for data-efficient policy learning.

---

## 论文详细总结（自动生成）

### SIM1: 物理对齐模拟器作为可变形世界中的零样本数据扩展器

#### 1. 核心问题与整体含义（研究动机和背景）
可变形物体（如衣物、布料）的操作是机器人领域极具挑战的任务，其状态空间巨大且动力学复杂。目前具身智能（Embodied AI）依赖大规模数据驱动，但现实世界采集可变形物体操作数据成本极高。现有的“模拟到现实”（Sim-to-Real）方法在处理柔性体时常因几何不匹配、物理仿真不稳定（如布料过度拉伸）以及运动原语过于简单而失败。
**论文核心动机**：提出“模拟失败并非因为它是合成的，而是因为它是未接地的（ungrounded）”。SIM1 旨在通过**物理对齐的实-虚-实（R2S2R）**流程，将模拟器锚定在物理世界中，从而将有限的现实演示扩展为大规模、高质量的合成监督信号，实现零样本（Zero-Shot）迁移。

#### 2. 方法论：核心思想与关键技术
SIM1 包含三个关键对齐阶段：
*   **SIM1-Scene（几何对齐）**：利用高精度 3D 扫描（EinScan Rigil Pro）和 Poisson 重建技术，将真实衣物转化为度量一致（亚毫米级精度）、带纹理的 3D 网格模型，确保仿真环境与现实在几何上高度一致。
*   **SIM1-Sim（动力学对齐）**：
    *   **稳定求解器**：开发了基于增强顶点块下降（AVBD）的变形稳定求解器。通过引入显式的**应变约束（Strain Constraints）**，在 Newton-VBD 迭代中加入虚拟弹性力，防止布料在受到机器人夹取力时产生不切实际的过度拉伸。
    *   **参数校准**：通过双向同步基础设施，将现实机器人的关节状态流式传输至模拟器，由专家通过视觉反馈迭代调整物理参数（杨氏模量、摩擦力等），实现行为对齐。
*   **SIM1-DataGen（运动对齐）**：
    *   **轨迹分解与合成**：将专家演示分解为“交互段”和“运动段”。保留关键交互位姿，利用**条件扩散模型（Diffusion Forcing）**生成平滑且符合人类特征的过渡运动。
    *   **质量过滤**：使用基于状态的启发式规则和基于 Transformer 的视频判别器（Discriminator）自动剔除物理穿透或失败的合成轨迹。
    *   **视觉随机化**：在 Blender 中进行光照、材质和相机位姿的域随机化（Domain Randomization）。

#### 3. 实验设计
*   **任务与场景**：核心 Benchmark 为长程 **T 恤折叠（T-shirt folding）**，此外还扩展到了毛巾翻转、短裤折叠等任务。
*   **硬件平台**：ARX ACONE 双臂机器人。
*   **对比基准（Baselines）**：
    1.  **Real Data**：纯真实世界演示数据训练。
    2.  **Sim Teleoperated Data**：模拟器中人工遥操作的数据。
    3.  **Sim Generated Data**：SIM1 自动生成的扩展数据。
*   **评估维度**：域内成功率（In-domain）、空间泛化（位姿偏移）、纹理泛化、光照泛化及视角泛化。

#### 4. 资源与算力
*   **计算设备**：
    *   **仿真与采集**：单台 NVIDIA RTX 4090 工作站（运行速度约 15 FPS）。
    *   **大规模生成**：使用了配备 **8× NVIDIA RTX 4090 GPU** 的服务器。
*   **效率数据**：
    *   渲染单条轨迹平均耗时 16.2 分钟（单卡）。
    *   系统每天可并行生成约 710 条轨迹，单位成本约为 0.10 美元/条，比现实采集成本降低了约 27 倍。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   采集了 1,000 条真实轨迹作为对比。
    *   生成了 10,000 条合成轨迹用于训练。
    *   在现实世界中进行了超过 300 次物理测试（每种配置 30 次试验）。
*   **充分性评价**：实验设计较为全面，涵盖了从消融实验（验证求解器、扩散生成的作用）到扩展性分析（Scaling Law）。通过对比 $\pi_0$ 和 $\pi_{0.5}$ 等不同规模的模型，验证了合成数据在不同模型能力下的有效性。实验过程考虑了多种分布偏移，客观性较强。

#### 6. 主要结论与发现
*   **零样本迁移能力**：纯合成数据训练的策略在现实部署中达到了 **90% 的成功率**。
*   **数据等效性**：在性能饱和点，**15 个 SIM1 合成样本的训练价值约等于 1 个真实世界演示样本**。
*   **泛化增益**：相比纯真实数据训练，SIM1 在面对未见过的纹理、光照和空间偏移时，泛化性能提升了 **50% 以上**。
*   **扩展性**：合成数据在低数据量时弱于真实数据，但随着规模增加，其性能提升斜率更高，且不会像有限真实数据那样迅速陷入过拟合。

#### 7. 优点
*   **物理接地**：不同于传统的随机仿真，SIM1 强调“对齐优先”，通过高精度扫描和稳定求解器解决了柔性体仿真的“漂浮感”。
*   **结构化生成**：结合轨迹分解与扩散模型，既保留了关键交互的准确性，又通过生成模型增加了运动的多样性。
*   **高性价比**：展示了利用消费级 GPU 集群大规模替代人工采集数据的可行性。

#### 8. 不足与局限
*   **自动化程度限制**：物理参数（如不同布料的弹性）仍需专家手动微调，尚未实现完全自动化的参数辨识。
*   **任务复杂度**：虽然展示了折叠任务，但对于更复杂的拓扑变化（如打结、穿衣）尚未深入探讨。
*   **判别器依赖**：数据质量高度依赖于视频判别器的准确性，若判别器产生偏差，可能引入“毒数据”影响策略性能。

（完）

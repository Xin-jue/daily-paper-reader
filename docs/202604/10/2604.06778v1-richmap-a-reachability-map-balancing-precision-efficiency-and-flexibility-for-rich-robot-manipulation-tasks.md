---
title: "RichMap: A Reachability Map Balancing Precision, Efficiency, and Flexibility for Rich Robot Manipulation Tasks"
title_zh: RichMap：一种为丰富的机器人操纵任务平衡精度、效率与灵活性的可达性地图
authors: "Yupu Lu, Yuxiang Ma, Jia Pan"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06778v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于通用机器人操作任务的可达性地图
tldr: 本文提出RichMap，一种兼顾高精度、高效率与灵活性的机器人可达性地图表示方法。针对复杂操作任务，该方法通过改进网格结构并利用理论容量界限确保覆盖率，结合异步流水线实现高效构建。RichMap在保持极高预测准确率和低误报率的同时，支持超快速批量查询，并能通过最大均值差异量化工作空间相似度，在跨具身扩散策略迁移实验中显著提升了任务成功率。
selection_source: fresh_fetch
motivation: 旨在解决现有机器人可达性地图在精度、查询效率与结构灵活性之间难以兼顾的挑战。
method: 提出一种基于精细化网格和理论容量界限的表示方法，并利用异步流水线加速地图的构建过程。
result: "实现了超过98%的预测准确率和15微秒级的查询速度，并在跨具身策略迁移中带来高达26%的性能提升。"
conclusion: RichMap为复杂机器人操作提供了一个高性能且通用的可达性评估框架，有效支持了工作空间分析与策略迁移。
---

## 摘要
本文提出了 RichMap，这是一种高精度的可达性地图表示方法，旨在为多功能的机器人操纵任务平衡效率与灵活性。通过改进经典的基于网格的结构，我们提出了一种精简的方法，在保持结构灵活性的同时，实现了接近紧凑地图形式（如 RM4D）的性能。我们的方法利用 $\mathbb{S}^2$（或 $SO(3)$）上的理论容量界限来确保严格的覆盖，并采用异步流水线进行高效构建。我们通过综合指标对该地图进行了验证，实现了高预测准确率（>98%）、低误报率（1~2%）以及快速的大批量查询（约 15 μs/查询）。我们进一步扩展了该框架的应用，通过最大均值差异（MMD）指标量化机器人工作空间的相似性，并展示了用于扩散策略迁移的基于能量的引导，在推方块实验的跨实体场景中实现了高达 26% 的性能提升。

## Abstract
This paper presents RichMap, a high-precision reachability map representation designed to balance efficiency and flexibility for versatile robot manipulation tasks. By refining the classic grid-based structure, we propose a streamlined approach that achieves performance close to compact map forms (e.g., RM4D) while maintaining structural flexibility. Our method utilizes theoretical capacity bounds on $\mathbb{S}^2$ (or $SO(3)$) to ensure rigorous coverage and employs an asynchronous pipeline for efficient construction. We validate the map against comprehensive metrics, pursuing high prediction accuracy ($>98\%$), low false positive rates ($1\sim2\%$), and fast large-batch query ($\sim$15 $μ$s/query). We extend the framework applications to quantify robot workspace similarity via maximum mean discrepancy (MMD) metrics and demonstrate energy-based guidance for diffusion policy transfer, achieving up to $26\%$ improvement for cross-embodiment scenarios in the block pushing experiment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RichMap** 的新型机器人可达性地图（Reachability Map）表示方法。以下是对该论文的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
可达性地图是机器人操纵任务中的关键先验知识，用于优化底座位置、运动规划和抓取检测。然而，现有的表示方法存在矛盾：
*   **经典网格地图（如 Capability Map）：** 结构简单但受“维度灾难”限制，存储效率低。
*   **紧凑型地图（如 RM4D）：** 虽然压缩了维度，但牺牲了结构的灵活性，难以扩展到复杂的相似性分析或策略迁移任务。
*   **研究动机：** 开发一种既能保持高精度和查询效率，又具备结构灵活性，且能通过理论界限指导构建的通用可达性地图框架。

### 2. 论文提出的方法论
RichMap 的核心思想是在精细化的 3D 网格基础上，利用球面几何理论优化姿态存储。
*   **空间与姿态离散化：** 将工作空间划分为均匀 3D 网格（如 0.02m 分辨率）。在每个网格内，利用测地线距离（Geodesic Distance）在单位球 $\mathbb{S}^2$（或 $SO(3)$）上存储末端执行器的趋近方向。
*   **理论容量界限：** 基于球面冠包装（Spherical Cap Packing）理论，推导出每个网格单元内可存储方向的最大数量 $N_{max}$。这为参数选择（如角度阈值 $\theta$）提供了严谨的数学依据。
*   **异步加速流水线：** 
    *   **CPU 端：** 多进程并行进行随机关节采样、正向运动学（FK）计算和自碰撞检测。
    *   **GPU 端：** 负责大批量数据的插入、体素分组排序及冲突检测（Phase 1: 新旧对比；Phase 2: 新新对比）。
*   **扩展应用：** 
    *   **相似度量化：** 使用最大均值差异（MMD）和多尺度 RBF 核函数对比不同机器人网格间的可达性差异。
    *   **能量引导迁移：** 将 MMD 差异转化为能量场，通过梯度引导扩散策略（Diffusion Policy）生成更符合目标机器人运动特性的轨迹。

### 3. 实验设计
*   **实验对象：** 四种主流机械臂（Franka Panda, KUKA iiwa7, UR5e, Kinova Gen3）。
*   **验证数据集：** 每种机器人生成 200 万个测试位姿（50% 可达，50% 不可达），使用 IKFlow 求解器作为真值标签。
*   **Benchmark 与对比方法：**
    *   **RM4D：** 紧凑型 4D 映射方法。
    *   **Capability Map (CM)：** 经典的基于网格的完整 $SO(3)$ 映射。
*   **应用场景：** 跨实体（Cross-Embodiment）推方块实验，从 xArm6 迁移到 UR3/UR5/UR10。

### 4. 资源与算力
*   **算力设备：** 论文提到使用了“消费级 GPU”（Superior consumer-level GPU），但未指明具体型号（推测为 RTX 3090/4090 级别）。
*   **构建时长：** 在该算力下，构建一个包含数亿位姿的完整地图可在 **1 天内** 完成。
*   **并行度：** 使用了 4-16 个 CPU 核心进行并行采样。

### 5. 实验数量与充分性
*   **实验规模：** 针对 4 种机器人进行了详尽的精度测试，测试位姿总量达 800 万个。
*   **消融与对比：** 测试了不同空间分辨率（0.05m vs 0.02m）对误报率（FPR）的影响。
*   **应用验证：** 在策略迁移实验中，使用了 3 个独立训练种子，并在 3 种不同规格的 UR 机器人上进行了 9 组模型的交叉验证。
*   **评价：** 实验设计较为充分，涵盖了从底层运动学精度到高层学习策略迁移的完整链路，对比基准选择合理，结果具有客观性。

### 6. 论文的主要结论与发现
*   **高精度：** 预测准确率超过 98%，误报率（FPR）控制在 1%~2% 之间。
*   **高效率：** GPU 加速后的批量查询速度达到约 **15 μs/query**，比传统顺序查询快约 18 倍。
*   **分辨率影响：** 提高空间分辨率（从 0.05m 到 0.02m）能显著降低 FPR（约 2.5 倍），证明边界近似误差是误报的主要来源。
*   **策略迁移提升：** 在跨实体任务中，基于 RichMap 的能量引导使 UR3 的任务成功率从 64.6% 提升至 91.3%（提升 26.7%）。

### 7. 优点
*   **理论与工程结合：** 引入球面几何容量界限指导地图构建，而非盲目采样。
*   **极速查询：** 充分利用 GPU 矢量化处理，极大地提升了在大规模采样规划中的实用性。
*   **多功能性：** 不仅仅是一个查找表，通过 MMD 相似度分析，为异构机器人之间的知识迁移提供了有效的数学工具。

### 8. 不足与局限
*   **存储开销：** 尽管比经典方法高效，但为了保持灵活性，其存储需求仍高于 RM4D 等极度压缩的方法（如 0.02m 分辨率下需存储数亿个位姿）。
*   **引导维度限制：** 目前的相似性引导主要作用于 3D 位置，对于对姿态（Orientation）极度敏感的任务，引导效果可能受限。
*   **动态性缺失：** 地图基于静态运动学，尚未考虑动力学约束或环境中的动态障碍物。

（完）

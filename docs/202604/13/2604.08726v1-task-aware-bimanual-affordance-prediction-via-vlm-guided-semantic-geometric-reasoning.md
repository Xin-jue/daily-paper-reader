---
title: Task-Aware Bimanual Affordance Prediction via VLM-Guided Semantic-Geometric Reasoning
title_zh: 通过 VLM 引导的语义-几何推理实现任务感知的双手示能预测
authors: "Fabian Hahne, Vignesh Prasad, Georgia Chalvatzaki, Jan Peters, Alap Kshirsagar"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08726v1"
tags: ["query:q10"]
score: 10.0
evidence: VLM引导的双臂操作任务推理
tldr: 本研究针对双臂协作机器人任务中接触点定位与手臂分配的难题，提出了一种基于视觉语言模型（VLM）引导的语义-几何推理框架。该方法将多视图RGB-D数据融合为3D场景，利用VLM的跨类别泛化能力，根据任务描述过滤6-DoF抓取候选点并确定左右臂分配。在九种真实世界任务（如工具使用、协作稳定等）中表现优异，显著提升了非结构化环境下双臂操作的成功率。
selection_source: fresh_fetch
motivation: 现有的双臂操作方法缺乏对任务意图的语义理解，难以同时解决“在哪里交互”和“用哪只手”的联合推理问题。
method: 提出一种分层框架，通过融合多视图3D表征生成抓取候选，并利用VLM进行任务相关的语义过滤和双臂分配决策。
result: 在涵盖平行操作、工具使用等四类九种真实任务的评估中，该方法在任务导向抓取成功率上显著优于仅依赖几何或语义的基准模型。
conclusion: 显式的语义推理与几何约束相结合，是实现非结构化环境下可靠且具备任务感知能力双臂协作的关键。
---

## 摘要
双手操作需要推理与物体的交互位置以及哪只手臂执行每个动作，这是一个联合的示能定位与手臂分配问题，仅依靠几何信息的规划器在缺乏对任务意图的语义理解时无法解决。现有方法要么将示能预测视为粗糙的部件分割，要么依赖几何启发式方法进行手臂分配，未能对任务相关的接触区域和手臂分配进行联合推理。我们将双手操作重新定义为联合示能定位与手臂分配问题，并提出了一种用于任务感知双手示能预测的分层框架，该框架利用视觉语言模型（VLM）在无需特定类别训练的情况下，实现跨物体类别和任务描述的泛化。我们的方法将多视图 RGB-D 观测融合为一致的 3D 场景表示，并生成全局 6 自由度（6-DoF）抓取候选，随后通过查询 VLM 获取每个物体上与任务相关的示能区域以及针对单个物体的手臂分配，从而在尊重任务语义的同时确保几何有效性，对候选进行空间和语义过滤。我们在双臂平台上对九个真实世界的操作任务进行了评估，涵盖四个类别：并行操作、协调稳定、工具使用和人工交接。我们的方法在面向任务的抓取中取得了比几何和语义基准方法持续更高的任务成功率，证明了对示能和手臂分配进行显式语义推理有助于在非结构化环境中实现可靠的双手操作。

## Abstract
Bimanual manipulation requires reasoning about where to interact with an object and which arm should perform each action, a joint affordance localization and arm allocation problem that geometry-only planners cannot resolve without semantic understanding of task intent. Existing approaches either treat affordance prediction as coarse part segmentation or rely on geometric heuristics for arm assignment, failing to jointly reason about task-relevant contact regions and arm allocation. We reframe bimanual manipulation as a joint affordance localization and arm allocation problem and propose a hierarchical framework for task-aware bimanual affordance prediction that leverages a Vision-Language Model (VLM) to generalize across object categories and task descriptions without requiring category-specific training. Our approach fuses multi-view RGB-D observations into a consistent 3D scene representation and generates global 6-DoF grasp candidates, which are then spatially and semantically filtered by querying the VLM for task-relevant affordance regions on each object, as well as for arm allocation to the individual objects, thereby ensuring geometric validity while respecting task semantics. We evaluate our method on a dual-arm platform across nine real-world manipulation tasks spanning four categories: parallel manipulation, coordinated stabilization, tool use, and human handover. Our approach achieves consistently higher task success rates than geometric and semantic baselines for task-oriented grasping, demonstrating that explicit semantic reasoning over affordances and arm allocation helps enable reliable bimanual manipulation in unstructured environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《Task-Aware Bimanual Affordance Prediction via VLM-Guided Semantic-Geometric Reasoning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在非结构化环境中，双臂机器人面临的核心挑战是：**不仅要确定“在哪里抓取”（示能定位），还要确定“用哪只手抓取”（手臂分配）**。
*   **研究动机**：传统的几何规划器缺乏对任务意图的语义理解（例如，“递交托盘”和“打开瓶盖”对抓取位置和手臂协作的要求完全不同）。
*   **现有局限**：现有方法通常将示能预测简化为粗糙的零件分割，或依赖简单的几何启发式规则进行手臂分配，无法在复杂任务中实现语义与几何的深度耦合。
*   **核心目标**：提出一个将视觉语言模型（VLM）的高层推理与底层几何抓取生成相结合的框架，实现任务感知的双臂协作。

### 2. 方法论
论文提出了一个三阶段的分层推理框架：
1.  **多视图场景表示与全局采样**：
    *   利用两个 RGB-D 传感器融合生成统一的 3D 点云，减少遮挡。
    *   使用 **AnyGrasp** 算法在全局范围内生成大量的 6-DoF 抓取候选点，确保几何可行性。
2.  **分层语义推理（核心创新）**：
    *   **步骤 1：手臂分配**。将任务描述和物体列表输入 VLM（文中提到使用 GPT-5），由其决定每个物体是使用左手、右手还是双臂协作（如：大物体需双臂，小物体需单臂）。
    *   **步骤 2：物体特定区域选择**。在检测到的物体图像上叠加动态网格，查询 VLM 选择最适合任务的网格单元（例如：抓取瓶身而非瓶盖，或抓取托盘的两个手柄）。
3.  **后处理与执行**：
    *   将 VLM 选中的 2D 网格投影回 3D 空间，作为过滤器剔除不符合语义要求的几何抓取候选。
    *   根据分配策略（并行模式或协调模式）选择得分最高的无碰撞抓取路径并执行。

### 3. 实验设计
*   **硬件平台**：双 Franka Emika Panda 机械臂，配备两个 ZED X Mini 深度相机。
*   **实验场景**：涵盖 9 种真实世界任务，分为四类：
    *   **并行/协调操作**：搅拌蔬菜、拿起锅、打开锅盖。
    *   **示能敏感任务**：打开瓶盖、倒水、扫地、抓取电钻。
    *   **大物体/人工交接**：托盘交接、凳子交接。
*   **对比基准（Benchmarks）**：
    1.  **Geometry-Only**：仅依赖几何得分和启发式规则（就近分配）。
    2.  **Arm Allocation Only**：有 VLM 分配手臂，但无区域过滤。
    3.  **Region Selection Only**：有区域过滤，但手臂分配靠启发式。
    4.  **VLPart**：使用现成的开放词汇零件分割模型进行过滤。

### 4. 资源与算力
*   **模型使用**：推理核心使用了 **GPT-5**（作为 VLM 推理机）和 **AnyGrasp**（抓取生成）。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或推理延迟。由于该方法是 **Zero-shot（零样本）** 的，不需要针对特定任务进行模型训练，因此主要的算力消耗在于 VLM 的 API 调用和实时点云融合处理。

### 5. 实验数量与充分性
*   **实验规模**：针对 9 种任务，每种任务在 10 个不同的物体位置下进行测试，总计至少 90 组核心实验，外加多组消融实验。
*   **充分性评价**：实验设计较为充分。通过四种不同的消融变体，清晰地展示了“手臂分配”和“区域选择”各自的贡献。任务涵盖了从精细操作（开瓶盖）到重型协作（搬凳子）的多种维度，客观地证明了语义推理在复杂双臂任务中的必要性。

### 6. 主要结论与发现
*   **性能领先**：该方法在任务策略对齐率（Strategy Alignment Rate）上达到了 **88.9%**，远高于仅靠几何的基准（9.0%）和仅靠语义分割的基准（55.6%）。
*   **互补性**：消融实验表明，手臂分配和区域选择是互补的。在工具使用中，手臂分配更关键；而在精细操作中，区域选择决定成败。
*   **语义必要性**：单纯的几何稳定性不足以完成任务，必须通过语义推理来确定符合人类意图的抓取方式。

### 7. 优点
*   **零样本泛化**：无需针对特定物体或任务进行昂贵的数据集收集和训练，利用 VLM 的通用知识即可处理新物体。
*   **分层解耦**：将高层语义决策（用哪只手、抓哪里）与底层几何控制（怎么抓稳、避障）有效解耦。
*   **多视图融合**：通过多相机融合解决了单视角下的遮挡问题，提高了感知的鲁棒性。

### 8. 不足与局限
*   **感知依赖**：系统高度依赖物体检测的准确性，在严重遮挡或深度噪声较大的情况下，性能会下降。
*   **缺乏运动学反馈**：VLM 在分配手臂时并未考虑机械臂的实时运动学可行性（如奇异点或关节极限），可能导致分配了手臂但无法触达。
*   **准静态限制**：目前仅适用于静态场景，无法处理动态物体或需要实时反应式调整的任务。
*   **延迟问题**：依赖大型 VLM 的 API 调用可能会带来较高的推理延迟，限制了实时交互性能。

（完）

---
title: "SIM1: Physics-Aligned Simulator as Zero-Shot Data Scaler in Deformable Worlds"
title_zh: SIM1：可变形世界中作为零样本数据扩展器的物理对齐模拟器
authors: "Yunsong Zhou, Hangxu Liu, Xuekun Jiang, Xing Shen, Yuanzhen Zhou, Hui Wang, Baole Fang, Yang Tian, Mulin Yu, Qiaojun Yu, Li Ma, Hengjie Li, Hanqing Wang, Jia Zeng, Jiangmiao Pang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08544v2"
tags: ["query:q8"]
score: 10.0
evidence: 用于变形物体操作的物理对齐虚实迁移数据引擎
tldr: "SIM1是一个物理对齐的实-虚-实数据引擎，旨在解决变形物体机器人操作中数据获取难、仿真不真实的问题。它通过将真实场景数字化为度量一致的孪生场景，并利用弹性建模校准动力学，结合扩散模型生成高质量合成轨迹。该方法实现了仅用合成数据训练的策略在真实世界中达到90%的零跳跃成功率，显著提升了变形物体操作的泛化能力和数据效率。"
selection_source: fresh_fetch
motivation: 现有的仿真到现实方法在处理变形物体时，因缺乏物理对齐和刚体抽象限制，导致仿真数据与现实存在严重偏差。
method: 提出SIM1引擎，通过场景数字化、弹性动力学校准以及基于扩散模型的轨迹生成与质量过滤，将少量演示扩展为大规模高质量合成数据。
result: 15的数据比例下达到了与真实数据相当的性能。
conclusion: 物理对齐的仿真可以作为变形物体操作的可扩展监督信号，是实现高效、高泛化性机器人策略学习的有效途径。
---

## 摘要
可变形物体的机器人操作是具身学习中一个数据密集型的领域，其中形状、接触和拓扑结构的共同演化方式远超刚体的多变性。尽管模拟技术有望缓解现实世界数据获取的成本压力，但主流的“模拟到现实”（sim-to-real）流程仍根植于刚体抽象，导致几何形状不匹配、软体动力学脆弱，以及不适用于布料交互的运动原语。我们认为，模拟之所以失败，并非因为它是合成的，而是因为它是未接地的。为了解决这一问题，我们推出了 SIM1，这是一个物理对齐的“现实-模拟-现实”（real-to-sim-to-real）数据引擎，旨在将模拟接地于物理世界。在给定有限演示的情况下，该系统将场景数字化为度量一致的孪生体，通过弹性建模校准可变形动力学，并通过带有质量过滤的基于扩散的轨迹生成来扩展行为。该流程将稀疏的观测转化为具有接近演示保真度的大规模合成监督信号。实验表明，在 1:15 的等效比例下，纯合成数据训练的策略达到了与真实数据基准相当的水平，同时在现实世界部署中实现了 90% 的零样本成功率和 50% 的泛化增益。这些结果验证了物理对齐模拟作为可变形操作的可扩展监督手段的有效性，并为高效数据策略学习提供了一条切实可行的路径。

## Abstract
Robotic manipulation with deformable objects represents a data-intensive regime in embodied learning, where shape, contact, and topology co-evolve in ways that far exceed the variability of rigids. Although simulation promises relief from the cost of real-world data acquisition, prevailing sim-to-real pipelines remain rooted in rigid-body abstractions, producing mismatched geometry, fragile soft dynamics, and motion primitives poorly suited for cloth interaction. We posit that simulation fails not for being synthetic, but for being ungrounded. To address this, we introduce SIM1, a physics-aligned real-to-sim-to-real data engine that grounds simulation in the physical world. Given limited demonstrations, the system digitizes scenes into metric-consistent twins, calibrates deformable dynamics through elastic modeling, and expands behaviors via diffusion-based trajectory generation with quality filtering. This pipeline transforms sparse observations into scaled synthetic supervision with near-demonstration fidelity. Experiments show that policies trained on purely synthetic data achieve parity with real-data baselines at a 1:15 equivalence ratio, while delivering 90% zero-shot success and 50% generalization gains in real-world deployment. These results validate physics-aligned simulation as scalable supervision for deformable manipulation and a practical pathway for data-efficient policy learning.

---

## 论文详细总结（自动生成）

这是一份关于论文《SIM1: Physics-Aligned Simulator as Zero-Shot Data Scaler in Deformable Worlds》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：可变形物体（如衣物）的操作是机器人领域的难题，其状态空间巨大且动力学复杂，对数据量需求极高。
*   **研究动机**：
    *   **现实数据昂贵**：在现实世界中采集大规模变形物体操作数据成本极高，且难以覆盖所有状态。
    *   **现有仿真缺陷**：传统的“模拟到现实”（Sim-to-Real）方法多基于刚体抽象，存在几何不匹配、软体动力学不稳定（如布料过度拉伸）以及运动原语不适用等问题。
*   **整体含义**：论文提出了 **SIM1**，一个物理对齐的“现实-模拟-现实”（R2S2R）数据引擎。其核心理念是：模拟失败不是因为它是合成的，而是因为它是“未接地”（ungrounded）的。通过物理对齐，SIM1 能将少量现实演示扩展为大规模高质量合成数据，支持零样本迁移。

### 2. 方法论
SIM1 采用三阶段对齐架构：
*   **SIM1-Scene（几何对齐）**：利用高精度3D扫描（EinScan Rigil Pro）获取衣物的度量一致（Metric-accurate）网格和纹理，并结合机器人URDF模型，构建与现实完全一致的数字孪生场景。
*   **SIM1-Sim（动力学对齐）**：
    *   **变形稳定求解器**：基于增强顶点块下降法（AVBD），引入显式应变约束（Strain Constraints）。当网格边缘拉伸超过阈值时，激活虚拟弹性力，防止布料在交互中出现非物理的过度拉伸。
    *   **参数校准**：通过双向同步基础设施，对比现实与仿真的视觉表现，迭代调整杨氏模量、摩擦力等物理参数。
*   **SIM1-DataGen（运动对齐与扩展）**：
    *   **轨迹分解**：将专家演示分解为“交互段”（抓取配置）和“移动段”。
    *   **扩散生成**：利用条件扩散模型（Diffusion Forcing）合成平滑的运动轨迹，连接不同的交互段。
    *   **质量过滤**：使用基于状态的统计规则和基于视频的判别器（Transformer-based）剔除穿模或失败的合成轨迹。
    *   **视觉随机化**：在 Blender 中进行光照、纹理和视角的随机化渲染。

### 3. 实验设计
*   **任务场景**：核心基准任务为 **T恤折叠**（长时程操作，超过20秒），并扩展至毛巾翻转、短裤折叠、长袖折叠等。
*   **Benchmark**：
    *   使用 $\pi_0$ 和 $\pi_{0.5}$（视觉-语言-动作模型）作为基础架构。
    *   对比基准包括：纯现实数据训练、仿真遥操作数据训练、SIM1 合成数据训练。
*   **评估维度**：
    *   **域内评估**：与训练环境一致。
    *   **泛化评估**：包括空间位移（±8cm/15°）、纹理变化（未见过的桌布/衣物）、光照变化、视角扰动（±5°）。

### 4. 资源与算力
*   **硬件设备**：
    *   **仿真与生成**：使用了配备 **NVIDIA RTX 4090** GPU 的工作站。
    *   **大规模扩展**：部署在拥有 **8× RTX 4090** 的服务器上。
*   **效率数据**：
    *   单台 GPU 平均渲染一条轨迹需 **16.2 分钟**。
    *   系统每天可并行生成约 **710 条** 轨迹。
    *   相比现实采集，成本降低了约 27 倍，吞吐量提升了 6.8 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   采集了 1,000 条现实轨迹作为对比。
    *   生成了 10,000 条合成轨迹用于训练。
    *   每组配置进行 **30 次** 现实机器人测试。
*   **充分性与客观性**：
    *   实验涵盖了从头训练（From scratch）和微调两种模式，证明了性能提升源于数据而非预训练先验。
    *   进行了详细的消融实验，验证了 AVBD 求解器、轨迹分解和扩散生成模块的各自贡献。
    *   引入了多种域外（OOD）测试，评估标准较为严苛且客观。

### 6. 主要结论与发现
*   **零样本迁移成功**：纯合成数据训练的策略在现实部署中达到了 **90%** 的成功率。
*   **数据等效比**：在域内任务中，**15 条 SIM1 合成数据约等于 1 条现实演示**；在泛化任务中，该比例提升至 5:1。
*   **泛化能力显著**：相比现实数据基准，SIM1 训练的策略在空间泛化上提升了 **50%**，在光照和视角泛化上也表现出极强韧性。
*   **扩展性验证**：随着合成数据规模增加，模型性能持续提升，未出现明显的饱和，验证了其作为数据扩展器的价值。

### 7. 优点
*   **物理接地（Grounding）**：不同于以往只追求视觉真实的研究，SIM1 强调几何和动力学的精确对齐，解决了 Sim-to-Real 的核心痛点。
*   **求解器创新**：AVBD 求解器的引入有效解决了布料仿真中的“过度拉伸”和“穿模”问题，使仿真数据具备了高质量的物理保真度。
*   **端到端流程**：从扫描数字化到自动化轨迹生成再到质量过滤，构建了一个闭环的自动化数据工厂。

### 8. 不足与局限
*   **自动化程度受限**：物理参数（如材料属性）的校准仍依赖专家引导的视觉比对，尚未实现完全自动化的系统辨识。
*   **任务复杂度**：虽然在折叠任务上表现优异，但对于更复杂的拓扑变化（如穿衣、打结）尚未进行深入验证。
*   **判别器依赖**：合成数据的质量高度依赖于视频判别器的准确性，若判别器失效，可能引入“毒数据”导致策略崩溃。

（完）

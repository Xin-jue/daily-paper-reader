---
title: "RoboAgent: Chaining Basic Capabilities for Embodied Task Planning"
title_zh: RoboAgent：用于具身任务规划的基础能力串联
authors: "Peiran Xu, Jiaqi Zheng, Yadong Mu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07774v1"
tags: ["query:q13"]
score: 9.0
evidence: 使用视觉语言模型的具身任务规划能力驱动流水线
tldr: 针对具身任务规划中视觉语言模型（VLM）在长程推理和多轮交互方面的局限，本文提出 RoboAgent 框架。该框架通过调度器调用多种子能力，将复杂规划分解为一系列基础视觉语言问题，实现了透明且可控的推理过程。通过行为克隆、DAgger 和强化学习的多阶段训练，RoboAgent 在多个具身规划基准测试中表现优异，显著提升了模型在复杂环境下的任务完成能力。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理具身规划中的长程推理、多轮交互和长上下文分析时表现有限。
method: 提出一种能力驱动的规划流水线，利用单一 VLM 实现调度器和多种子能力，并通过多阶段训练（BC、DAgger、RL）和数据增强来优化模型。
result: 在广泛使用的具身任务规划基准测试上，该方法验证了其有效性，展现了比现有模型更强的规划能力和鲁棒性。
conclusion: RoboAgent 通过将复杂任务分解为基础能力链，为提升 VLM 在具身智能领域的规划性能提供了一种透明且高效的解决方案。
---

## 摘要
本文关注具身任务规划，其中智能体从环境中获取视觉观测并执行原子动作以完成给定任务。尽管近期的视觉语言模型（VLM）在多模态理解和推理方面取得了令人瞩目的成果，但在应用于涉及多轮交互、长程推理和长上下文分析的具身规划时，其性能仍然受限。为了弥补这一差距，我们提出了 RoboAgent，这是一种能力驱动的规划流水线，模型在其中主动调用不同的子能力。每种能力维护其自身的上下文，并根据调度器给出的查询产生中间推理结果或与环境进行交互。该框架将复杂的规划分解为一系列 VLM 能够更好处理的基础视觉语言问题，从而实现更透明、更可控的推理过程。调度器和所有能力均由单个 VLM 实现，不依赖外部工具。为了训练该 VLM，我们采用了多阶段范式，包括：(1) 使用专家规划进行行为克隆，(2) 使用模型收集的轨迹进行 DAgger 训练，以及 (3) 由专家策略引导的强化学习。在这些阶段中，我们利用环境模拟器的内部信息为每种能力构建高质量的监督信号，并进一步引入增强数据和合成数据，以提升模型在更多样化场景下的性能。在广泛使用的具身任务规划基准测试上进行的大量实验验证了所提方法的有效性。我们的代码将在 https://github.com/woyut/RoboAgent_CVPR26 发布。

## Abstract
This paper focuses on embodied task planning, where an agent acquires visual observations from the environment and executes atomic actions to accomplish a given task. Although recent Vision-Language Models (VLMs) have achieved impressive results in multimodal understanding and reasoning, their performance remains limited when applied to embodied planning that involves multi-turn interaction, long-horizon reasoning, and extended context analysis. To bridge this gap, we propose RoboAgent, a capability-driven planning pipeline in which the model actively invokes different sub-capabilities. Each capability maintains its own context, and produces intermediate reasoning results or interacts with the environment according to the query given by a scheduler. This framework decomposes complex planning into a sequence of basic vision-language problems that VLMs can better address, enabling a more transparent and controllable reasoning process. The scheduler and all capabilities are implemented with a single VLM, without relying on external tools. To train this VLM, we adopt a multi-stage paradigm that consists of: (1) behavior cloning with expert plans, (2) DAgger training using trajectories collected by the model, and (3) reinforcement learning guided by an expert policy. Across these stages, we exploit the internal information of the environment simulator to construct high-quality supervision for each capability, and we further introduce augmented and synthetic data to enhance the model's performance in more diverse scenarios. Extensive experiments on widely used embodied task planning benchmarks validate the effectiveness of the proposed approach. Our codes will be available at https://github.com/woyut/RoboAgent_CVPR26.

---

## 论文详细总结（自动生成）

这是一份关于论文《RoboAgent: Chaining Basic Capabilities for Embodied Task Planning》的结构化总结：

### 1. 核心问题与整体含义
该论文针对**具身任务规划（Embodied Task Planning, ETP）**中的长程推理和多轮交互难题展开研究。虽然视觉语言模型（VLM）在多模态理解上表现出色，但在复杂的具身场景中，直接生成长序列动作往往会导致推理崩溃或逻辑不连贯。论文的核心动机是：**将复杂的规划任务分解为一系列可管理的、基础的视觉语言能力链**，通过一个中央调度器（Scheduler）按需调用这些能力，从而提升规划的透明度、可控性和成功率。

### 2. 方法论
RoboAgent 采用“能力驱动”的架构，所有组件均由**单个 VLM** 实现，无需外部工具。

*   **核心思想**：将规划过程建模为“调度器调用子能力”的循环。调度器负责维护任务进度和上下文，子能力负责具体的感知或执行任务。
*   **五大基础能力**：
    1.  **探索引导 (EG)**：预测目标物体可能出现的位置。
    2.  **物体定位 (OG)**：在当前视野中识别并定位目标。
    3.  **场景描述 (SD)**：描述目标物体的状态（如：门是否打开）。
    4.  **动作解码 (AD)**：将高层指令转化为原子动作序列。
    5.  **经验总结 (ES)**：分析动作执行后的反馈和失败原因。
*   **三阶段训练流水线**：
    1.  **阶段 1 (SFT)**：在专家轨迹上进行行为克隆，利用模拟器内部信息（如场景图、掩码）为子能力提供高质量监督。
    2.  **阶段 2 (DAgger)**：在模型生成的轨迹上进行训练，通过模拟器反馈构建纠错数据。
    3.  **阶段 3 (RFT)**：提出**专家诱导策略优化（EIPO）算法**。不同于传统 RL 估计未来回报，EIPO 利用专家策略计算优势函数（Advantage），使强化学习过程更稳定。

### 3. 实验设计
*   **数据集与场景**：主要基于 **ALFRED** 训练集进行开发。
*   **Benchmark（基准测试）**：
    *   **ALFWorld**：视觉和文本模式下的交互式决策。
    *   **EB-ALFRED**：侧重于复杂指令和长程任务的评估。
*   **泛化性测试（OOD）**：在 **EB-Habitat**（Habitat 模拟器）和 **LoTa-WAH**（VirtualHome 模拟器）上进行跨域评估。
*   **对比方法**：包括闭源模型（GPT-4o, Claude-3.7, Gemini-1.5）和开源具身模型（RoboBrain2.0, REBP, WAP 等）。

### 4. 资源与算力
*   **基础模型**：Qwen2.5-VL-3B。
*   **硬件资源**：使用了 **4 台 NVIDIA H800 (80GB) GPU**。
*   **训练细节**：
    *   阶段 1：2 个 epoch，Batch Size 32。
    *   阶段 2：1 个 epoch。
    *   阶段 3：120 次迭代，Batch Size 512。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同的基准测试上进行了实验，涵盖了视觉和文本两种模态，以及三个不同的模拟器环境。
*   **消融实验**：详细分析了三个训练阶段的贡献、数据增强的效果，以及 EIPO 算法相对于 GRPO 的优越性。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率（SR），还进行了细致的失败案例分析（Error Analysis），客观地展示了模型在不同任务类型（如 Pick, Clean, Heat 等）下的表现。

### 6. 主要结论与发现
*   **能力分解的有效性**：通过将规划分解为基础能力链，3B 规模的模型在多个指标上超越了参数量更大的模型甚至闭源模型。
*   **EIPO 的稳定性**：在具身规划任务中，利用专家策略引导的强化学习比传统的基于奖励的 RL 收敛更快、效果更好。
*   **跨模态泛化**：模型在视觉环境训练后，能够无缝迁移到纯文本环境，证明其掌握了模态无关的规划逻辑。

### 7. 优点
*   **透明与可解释**：通过显式的能力调用，开发者可以清晰地看到模型在哪个环节出错（如：是没找到物体，还是动作解码错误）。
*   **单模型集成**：不依赖外部视觉检测器或复杂的工具链，保持了系统的简洁性和端到端优化的潜力。
*   **高质量监督**：巧妙利用模拟器的“特权信息”（Privileged Info）来训练中间推理步骤，解决了 CoT 推理缺乏监督的问题。

### 8. 不足与局限
*   **视觉识别瓶颈**：错误分析显示，约 50% 的失败归因于物体识别错误，表明 3B 模型的底层感知能力仍有提升空间。
*   **跨模拟器鸿沟**：虽然在跨域测试中表现优于其他开源模型，但与闭源模型在全新环境（如 Habitat）下的零样本能力相比仍有差距。
*   **模拟器依赖**：训练过程高度依赖模拟器提供的场景图和元数据，这在真实世界数据采集（如人类视频）中难以直接获取。

（完）

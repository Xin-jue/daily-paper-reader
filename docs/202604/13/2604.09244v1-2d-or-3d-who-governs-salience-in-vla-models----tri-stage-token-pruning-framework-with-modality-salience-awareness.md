---
title: "2D or 3D: Who Governs Salience in VLA Models? -- Tri-Stage Token Pruning Framework with Modality Salience Awareness"
title_zh: 2D 还是 3D：谁主导了 VLA 模型中的显著性？——具有模态显著性感知能力的三阶段 Token 剪枝框架
authors: "Zihao Zheng, Sicheng Tian, Zhihao Mao, Lingyue Zhang, Chenyue Li, Ziyun Zhang, Hong Gao, Yuchen Huang, Yutong Xu, Guojie Luo, Xiang Chen"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09244v1"
tags: ["query:q12"]
score: 10.0
evidence: 多视觉模态VLA模型的Token剪枝框架
tldr: 本文针对多视觉模态视觉-语言-动作（MVLA）模型因引入3D数据导致的计算量激增问题，深入探讨了2D与3D模态间的显著性差异。研究者提出了一个具有模态显著性感知的三阶段Token剪枝框架，通过动态捕捉不同阶段模态重要性的变化，实现了高效的Token筛选。该方法在保证模型空间感知能力的同时，显著降低了推理延迟，为具身智能模型的高效部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的Token剪枝方法多针对单2D模态设计，未能考虑MVLA模型中2D与3D模态显著性的差异及其动态变化对效率的影响。
method: 提出一种三阶段Token剪枝框架，通过分析多模态数据在模型中的处理流程，实现对2D和3D Token的自适应选择与高效剪枝。
result: "该框架在仅产生5.8%额外开销的前提下，实现了高达2.55倍的推理加速，且几乎没有精度损失。"
conclusion: 通过感知并利用2D/3D模态的显著性差异，可以有效优化MVLA模型的推理性能，实现精度与速度的平衡。
---

## 摘要
视觉-语言-动作（VLA）模型已成为具身智能的主流。近期的 VLA 模型已将其输入模态从仅 2D 扩展到 2D+3D 范式，形成了多视觉模态 VLA（MVLA）模型。尽管实现了空间感知的提升，但由于模态扩展导致输入 Token 数量增加，MVLA 面临着更大的加速需求。Token 剪枝是针对 MVLA 模型量身定制的一种有效优化方法。然而，现有的 Token 剪枝方案多是为仅 2D 的 VLA 模型设计的，忽略了 2D/3D 模态显著性的差异。在本文中，我们遵循 MVLA 模型中多模态数据的应用过程，并开发了一种三阶段分析方法，以捕捉 2D/3D 模态显著性的差异和动态变化。基于此，我们为 MVLA 模型提出了一种相应的三阶段 Token 剪枝框架，以实现最优的 2D/3D Token 选择和高效剪枝。实验表明，我们的框架在仅产生 5.8% 开销的情况下，实现了高达 2.55 倍的推理加速，且精度损失极小。我们的代码即将发布。

## Abstract
Vision-Language-Action (VLA) models have emerged as the mainstream of embodied intelligence. Recent VLA models have expanded their input modalities from 2D-only to 2D+3D paradigms, forming multi-visual-modal VLA (MVLA) models. Despite achieving improved spatial perception, MVLA faces a greater acceleration demand due to the increased number of input tokens caused by modal expansion. Token pruning is an effective optimization methods tailored to MVLA models. However, existing token pruning schemes are designed for 2D-only VLA models, ignoring 2D/3D modality salience differences. In this paper, we follow the application process of multi-modal data in MVLA models and develop a tri-stage analysis to capture the discrepancy and dynamics of 2D/3D modality salience. Based on these, we propose a corresponding tri-stage token pruning framework for MVLA models to achieve optimal 2D/3D token selection and efficient pruning. Experiments show that our framework achieves up to a 2.55x inference speedup with minimal accuracy loss, while only costing 5.8% overhead. Our Code is coming soon.

---

## 论文详细总结（自动生成）

这篇论文针对多视觉模态视觉-语言-动作（MVLA）模型在具身智能应用中的推理效率问题进行了深入研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：随着具身智能的发展，VLA 模型正从纯 2D 视觉输入演进为 2D+3D（如点云）的多模态输入（MVLA），以增强空间感知。然而，模态扩展导致输入 Token 数量激增，使得推理速度大幅下降（通常仅 3-5 Hz），远低于实时控制要求的 20-30 Hz。
*   **核心问题**：现有的 Token 剪枝技术主要针对单模态（2D）设计，忽略了 2D 与 3D 模态之间显著性（Salience）的差异及其在任务执行过程中的动态变化。论文旨在探究“谁主导了显著性”，并据此设计高效的剪枝方案。

### 2. 方法论：核心思想与关键技术
论文提出了一个**三阶段 Token 剪枝框架**，对应 MVLA 数据的处理流程：
*   **数据预处理阶段（Data Preprocessing）**：
    *   **核心思想**：利用模型输出特征的 $L_1$ 范数来量化 2D/3D 模态的贡献。
    *   **技术细节**：通过双阈值机制（$\tau_{2D}, \tau_{3D}$）判断每个 Patch 的模态偏好。若 3D 特征占比极低则仅保留 2D，反之亦然；中间区域则保留双模态。
*   **语义综合阶段（Semantic Synthesis）**：
    *   **核心思想**：根据注意力分数将 Patch 划分为不同的语义区域（背景、机器人本体、目标物体）。
    *   **技术细节**：使用 1D K-Means 聚类。对背景区域进行 90% 的激进随机剪枝；对机器人和目标区域，根据 3D 模态的“正交显著性”（即 3D 提供的独特信息）进行自适应保留。
*   **动作迭代阶段（Action Iteration）**：
    *   **核心思想**：捕捉任务执行过程中的时间动态性。
    *   **技术细节**：引入**指数移动平均（EMA）滑动窗口机制**。通过平滑连续帧之间的剪枝决策，减少帧间闪烁，增强决策鲁棒性，并避免每帧重复计算显著性带来的开销。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 RLBench 机器人学习基准测试，涵盖 Close Box（关箱子）、Close Laptop（关电脑）、Sweep to Dustpan（扫入簸箕）等 5 个典型任务。
    *   **真实世界**：在松灵 Piper 机械臂上进行实机验证（如抓取香蕉放入盘中）。
*   **对比方法（Baselines）**：
    *   未剪枝的基准模型（MLA）。
    *   Naive Prune（随机剪枝）。
    *   SP-VLA（针对 SVLA 的剪枝方法迁移）。
    *   VLA-Pruner（针对 SVLA 的时序感知剪枝方法迁移）。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **NVIDIA A100-PCIE-40GB GPU** 和 Intel Xeon Gold 6348 CPU 的服务器上运行。
*   **训练/推理细节**：论文主要关注推理阶段的优化。剪枝框架本身的计算开销极低，仅占总推理时间的 5.8%（约 61ms）。

### 5. 实验数量与充分性
*   **实验规模**：每个仿真任务进行 30 次独立试验，并报告了成功率（SR）和端到端加速比。
*   **消融实验**：详细测试了三个阶段（S1, S2, S3）各自及组合对性能的影响。
*   **超参数分析**：对阈值、EMA 动量参数 $\beta$、窗口大小 $k$ 进行了敏感性分析。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实、从整体性能到组件贡献的多个维度，对比实验在相同硬件和基准模型（MLA）下进行，保证了公平性。

### 6. 主要结论与发现
*   **显著性差异**：2D 模态在整体上通常比 3D 模态更具显著性，但在涉及空间交互的关键区域，3D 模态的独特信息不可或缺。
*   **性能表现**：该框架在 70% 的剪枝率下，实现了 **2.55 倍的推理加速**，而平均成功率仅下降约 2.5%。
*   **动态性**：显著性随任务进度动态变化，在交互关键阶段，3D Token 的保留率会自动提升。

### 7. 优点
*   **模态感知深度**：首次深入分析了 MVLA 中 2D/3D 模态的显著性解耦，而非简单地将 3D 视为 2D 的补充。
*   **高效性**：通过 EMA 机制巧妙地平衡了剪枝精度与计算开销，实现了显著的端到端加速。
*   **即插即用**：该框架不需要对模型进行重新训练，具有较好的通用性。

### 8. 不足与局限
*   **任务覆盖面**：虽然在 RLBench 上表现良好，但对于更复杂、长程（Long-horizon）且环境极度多变的具身任务，其阈值设定的泛化性仍有待验证。
*   **模态局限**：目前仅针对 2D 图像和 3D 点云，未讨论触觉、力觉等其他具身智能常见模态的显著性治理。
*   **超参数依赖**：虽然论文称其具有鲁棒性，但在不同架构的 MVLA 模型上可能仍需手动微调阈值以达到最优平衡。

（完）

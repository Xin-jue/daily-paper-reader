---
title: Abstract Sim2Real through Approximate Information States
title_zh: 通过近似信息状态实现抽象 Sim2Real
authors: "Yunfu Deng, Yuhao Li, Josiah P. Hanna"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15289v1"
tags: ["query:q8"]
score: 9.0
evidence: 机器人领域的抽象sim2real迁移
tldr: 本文针对机器人强化学习中模拟器难以完全建模现实细节的问题，提出了“抽象Sim2Real”框架。研究者利用状态抽象理论证明，通过引入历史状态信息，可以将粗粒度的抽象模拟器与目标任务对齐。基于此，提出了一种利用真实数据修正抽象模拟器动力学的方法，并在Sim2Sim和Sim2Real实验中验证了该方法能有效提升策略迁移的成功率。
selection_source: fresh_fetch
motivation: 在复杂任务中构建高精度模拟器成本极高且困难，因此需要探索如何利用细节缺失的抽象模拟器实现有效的策略迁移。
method: 利用强化学习中的状态抽象理论，通过结合历史状态信息来修正抽象模拟器的动力学模型，使其与真实世界任务对齐。
result: 实验证明，该方法在Sim2Sim和Sim2Real场景下均能显著提高策略在目标环境中的表现和迁移成功率。
conclusion: 通过引入近似信息状态修正动力学，即使是粗粒度的抽象模拟器也能训练出具备强鲁棒性和可迁移性的机器人策略。
---

## 摘要
近年来，当针对给定任务存在快速且准确的模拟器时，强化学习 (RL) 在机器人领域取得了显著成功。在使用 RL 和模拟时，提高模拟器的逼真度通常是有益的，但随着机器人被部署到日益复杂且大规模的领域，获取高逼真度模拟器变得愈发困难。在此类场景下，模拟器可能无法建模给定目标任务的所有相关细节，这一观察结果促使人们开始研究在模拟器缺失关键任务细节的情况下的 sim2real 问题。在本文中，我们形式化并研究了“抽象 sim2real”问题：给定一个在粗粒度抽象水平上建模目标任务的抽象模拟器，我们如何在该抽象模拟器中使用 RL 训练策略并将其成功迁移到现实世界？我们的第一个贡献是利用 RL 文献中的状态抽象语言对该问题进行了形式化。这一框架表明，如果落地后的抽象动力学考虑了状态历史，则可以将抽象模拟器进行落地以匹配目标任务。基于该形式化方法，我们随后提出了一种利用现实世界任务数据来修正抽象模拟器动力学的方法。随后，我们证明了该方法在 sim2sim 和 sim2real 评估中均能实现成功的策略迁移。

## Abstract
In recent years, reinforcement learning (RL) has shown remarkable success in robotics when a fast and accurate simulator is available for a given task. When using RL and simulation, more simulator realism is generally beneficial but becomes harder to obtain as robots are deployed in increasingly complex and widescale domains. In such settings, simulators will likely fail to model all relevant details of a given target task and this observation motivates the study of sim2real with simulators that leave out key task details. In this paper, we formalize and study the abstract sim2real problem: given an abstract simulator that models a target task at a coarse level of abstraction, how can we train a policy with RL in the abstract simulator and successfully transfer it to the real-world? Our first contribution is to formalize this problem using the language of state abstraction from the RL literature. This framing shows that an abstract simulator can be grounded to match the target task if the grounded abstract dynamics take the history of states into account. Based on the formalism, we then introduce a method that uses real-world task data to correct the dynamics of the abstract simulator. We then show that this method enables successful policy transfer both in sim2sim and sim2real evaluation.

---

## 论文详细总结（自动生成）

这篇论文《Abstract Sim2Real through Approximate Information States》探讨了如何利用高度简化的“抽象模拟器”来实现机器人策略向现实世界的有效迁移。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的 Sim2Real 方法（如领域随机化、系统辨识）通常假设模拟器与现实世界拥有相同的状态-动作空间，仅在动力学参数上存在差异。然而，在复杂任务中构建高保真模拟器成本极高。
*   **核心问题**：当模拟器是“抽象”的（即故意忽略了现实中的许多细节，如将复杂的四足机器人简化为 2D 质点）时，如何利用少量的现实数据修正该模拟器，从而训练出能成功迁移的策略？
*   **理论贡献**：论文首次利用强化学习中的“状态抽象”理论形式化了抽象 Sim2Real 问题，并指出抽象会导致**部分可观测性（Partial Observability）**，因此必须引入历史信息来补偿丢失的动力学细节。

### 2. 方法论：ASTRA
论文提出了 **ASTRA**（Augmented Simulation with self-predicTive abstRAction）方法，核心思想是通过学习一个“近似信息状态（AIS）”来增强抽象模拟器。
*   **核心组件**：
    *   **历史编码器 ($\psi_s$)**：使用 GRU 循环神经网络将抽象状态和动作的历史映射到潜空间 $Z$。
    *   **潜动力学模型 ($P_{lat}$)**：在潜空间预测下一时刻的状态分布。
    *   **奖励预测器 ($R_{pred}$)**：预测目标环境中的奖励。
    *   **抽象状态修正器 ($f_{abs}$)**：根据潜状态修正抽象模拟器的输出，使其逼近现实观测。
*   **关键技术细节**：
    *   **多任务损失函数**：结合了潜动力学损失（$L_{trans}$）、奖励预测损失（$L_{rew}$）和状态修正损失（$L_{abs}$）。
    *   **对齐机制**：通过最大均值差异（MMD）损失，将现实世界的历史编码器与模拟器的编码器对齐，确保策略在部署时能识别相似的特征。
*   **算法流程**：先收集少量现实数据，通过上述损失函数训练修正模型；然后在修正后的模拟器中运行 RL 算法（如 PPO 或 SAC）训练策略。

### 3. 实验设计
*   **实验场景**：
    1.  **Sim2Sim (AntMaze)**：目标环境是 29 维状态的四足机器人（Ant），抽象模拟器是 2D 质点模型。
    2.  **Sim2Sim (Humanoid Locomotion)**：目标环境是全自由度人形机器人，抽象模拟器分为 Walker2D、运动学模型等不同抽象等级。
    3.  **Sim2Real (NAO 机器人)**：在物理 NAO 机器人上进行导航（Maze Navigation）和踢球（Ball-Kicking）任务。
*   **对比基准（Baselines）**：
    *   **直接迁移 (DT)**：无修正直接部署。
    *   **领域随机化 (DR)**：对动作和参数进行随机化。
    *   **COMPASS**：一种强大的基于微分因果发现的领域随机化方法。
    *   **RMA (快速电机适应)**：代表基于历史适应参数的方法。
    *   **NAS (神经增强模拟)**：仅学习状态修正，不包含自预测损失。
    *   **DT+IQL**：在目标数据上进行离线强化学习微调。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了 GRU 作为骨干网络，RL 算法采用了 PPO（导航任务）和 SAC（运动任务）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验采用了 **10 个随机种子**进行平均，确保了统计显著性。
    *   物理机器人实验在 3 个种子上进行了 **60 次试验**（每个种子 20 次）。
    *   包含了**消融实验**，验证了潜动力学损失和奖励预测损失的必要性。
    *   进行了**数据效率分析**，探讨了不同数据量对模型性能的影响。
*   **充分性评价**：实验设计非常充分，涵盖了从简单到复杂的不同抽象层级，且对比了当前最先进的多种 Sim2Real 变体，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **历史信息至关重要**：在抽象 Sim2Real 中，由于状态被压缩，必须依赖历史信息（RNN）来推断未建模的动力学（如惯性、摩擦、电机延迟）。
*   **自预测表征的优势**：ASTRA 通过预测奖励和潜动力学，学习到的特征比单纯预测状态修正（如 NAS）更利于策略学习。
*   **抽象层级的影响**：当模拟器极其简单时，ASTRA 的优势最明显；随着模拟器保真度提高，各方法差距缩小，但 ASTRA 依然保持领先。
*   **鲁棒性**：ASTRA 训练的策略在目标机器人形态发生变化（如腿部加长）时仍表现出良好的鲁棒性。

### 7. 优点
*   **理论支撑扎实**：将 Sim2Real 问题与状态抽象理论结合，为“为什么需要历史信息”提供了数学解释。
*   **通用性强**：不依赖于特定的 RL 算法，且能处理跨形态（如质点到四足）的巨大差异。
*   **现实可行性**：在物理 NAO 机器人上验证了有效性，证明了该方法能处理传感器噪声和执行器延迟。

### 8. 不足与局限
*   **依赖状态映射**：假设预先知道现实状态到抽象状态的映射函数 $\phi$（例如通过视觉系统提取坐标），这在某些完全未知的环境中可能难以获得。
*   **数据需求**：虽然比直接在现实中学习所需的样本少，但仍需要一定量的现实轨迹（如 50-200 条）来进行模拟器落地。
*   **计算开销**：引入循环神经网络和多任务学习增加了训练和推理的计算复杂度。

（完）

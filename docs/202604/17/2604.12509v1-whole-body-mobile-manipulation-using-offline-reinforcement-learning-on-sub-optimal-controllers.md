---
title: Whole-Body Mobile Manipulation using Offline Reinforcement Learning on Sub-optimal Controllers
title_zh: 基于次优控制器的离线强化学习全身移动操作
authors: "Snehal Jauhri, Vignesh Prasad, Georgia Chalvatzaki"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12509v1"
tags: ["query:q2"]
score: 9.0
evidence: 机器人底座与机械臂的全身协同操作
tldr: 针对移动操作（MoMa）中全身协调难、数据获取贵的问题，本文提出WHOLE-MoMa框架。该方法利用次优的全身控制器（WBC）作为结构先验生成多样化演示数据，随后通过改进的离线强化学习（结合Q-chunking的IQL）提取并优化行为。实验证明，该方法在模拟和真实机器人任务中均显著优于基准，实现了无需真机数据或遥操作的零样本迁移。
selection_source: fresh_fetch
motivation: 传统的全身控制器调参复杂且脆弱，而学习方法又极度依赖昂贵的遥操作数据或复杂的奖励设计。
method: 提出两阶段流水线，先通过随机化轻量级WBC生成演示，再利用带Q-chunking的离线隐式Q学习（IQL）进行策略提取与优化。
result: "在TIAGo++机器人上实现了零样本迁移，双臂抽屉操作成功率达80%，橱柜开启与放置成功率达68%。"
conclusion: 证明了利用次优控制器作为先验并结合离线RL，可以在无需人类演示的情况下实现复杂、鲁棒的全身移动操作。
---

## 摘要
对关节物体（如开门、抽屉和橱柜）的移动操作（MoMa）要求机器人的底座和机械臂之间进行同步的全身协调。经典的全身控制器（WBC）可以通过分层优化解决此类问题，但需要大量的手工调优，且表现较为脆弱。另一方面，基于学习的方法展现出强大的泛化能力，但通常依赖昂贵的全身遥操作数据或繁重的奖励工程。我们观察到，即使是次优的 WBC 也是一种强大的结构先验：它可以用于在状态-动作空间中受限且与任务相关的区域收集数据，并且其行为仍可以通过离线强化学习得到进一步改进。基于此，我们提出了 WHOLE-MoMa，这是一个两阶段流水线：首先通过随机化轻量级 WBC 生成多样化的演示数据，然后应用离线强化学习，通过奖励信号识别并缝合改进后的行为。为了支持复杂协调任务所需的高表达力动作分块扩散策略，我们扩展了离线隐式 Q 学习（IQL），引入了 Q 分块（Q-chunking）以进行块级评论家评估和优势加权策略提取。在仿真环境中使用 TIAGo++ 移动操作机器人的三个难度递增的任务中，WHOLE-MoMa 的表现显著优于 WBC、行为克隆以及几种离线强化学习基准方法。策略无需微调即可直接迁移到真实机器人上，在双臂抽屉操作中实现了 80% 的成功率，在同步开启橱柜和放置物体中实现了 68% 的成功率，且所有过程均未使用任何遥操作或真实世界训练数据。

## Abstract
Mobile Manipulation (MoMa) of articulated objects, such as opening doors, drawers, and cupboards, demands simultaneous, whole-body coordination between a robot's base and arms. Classical whole-body controllers (WBCs) can solve such problems via hierarchical optimization, but require extensive hand-tuned optimization and remain brittle. Learning-based methods, on the other hand, show strong generalization capabilities but typically rely on expensive whole-body teleoperation data or heavy reward engineering. We observe that even a sub-optimal WBC is a powerful structural prior: it can be used to collect data in a constrained, task-relevant region of the state-action space, and its behavior can still be improved upon using offline reinforcement learning. Building on this, we propose WHOLE-MoMa, a two-stage pipeline that first generates diverse demonstrations by randomizing a lightweight WBC, and then applies offline RL to identify and stitch together improved behaviors via a reward signal. To support the expressive action-chunked diffusion policies needed for complex coordination tasks, we extend offline implicit Q-learning with Q-chunking for chunk-level critic evaluation and advantage-weighted policy extraction. On three tasks of increasing difficulty using a TIAGo++ mobile manipulator in simulation, WHOLE-MoMa significantly outperforms WBC, behavior cloning, and several offline RL baselines. Policies transfer directly to the real robot without finetuning, achieving 80% success in bimanual drawer manipulation and 68% in simultaneous cupboard opening and object placement, all without any teleoperated or real-world training data.

---

## 论文详细总结（自动生成）

这是一份关于论文《Whole-Body Mobile Manipulation using Offline Reinforcement Learning on Sub-optimal Controllers》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：移动操作（MoMa）要求机器人底座与机械臂高度协同，以处理开门、开抽屉等复杂任务。
*   **现有挑战**：
    *   **传统全身控制器（WBC）**：依赖手工调优，且具有“近视性”（Myopic），无法预见当前动作对后续交互的影响。
    *   **学习方法**：通常需要昂贵的全身遥操作数据，或面临高维状态-动作空间下的奖励工程难题。
*   **核心理念**：论文提出 **WHOLE-MoMa** 框架，认为即使是“次优”的 WBC 也是一种强大的结构先验。通过随机化 WBC 生成多样化数据，再利用离线强化学习（Offline RL）从中“缝合”并优化出超越原始控制器的行为，从而摆脱对人类演示数据的依赖。

### 2. 方法论
该框架分为两个阶段：
*   **阶段一：基于次优 WBC 的数据生成**
    *   使用分层二次规划（HQP）构建基础控制器，设定硬约束（自碰撞、关节限制）和任务目标（末端执行器与底座追踪）。
    *   **随机化采样**：通过随机化预抓取阈值、动作步长、任务权重及关节噪声，生成 3000 条涵盖成功与失败尝试的多样化轨迹。
*   **阶段二：带 Q-chunking 的离线强化学习**
    *   **核心算法**：改进了隐式 Q 学习（IQL）。
    *   **关键技术：Q-chunking**：为了适配高表达力的“动作分块扩散策略”（Action-chunked Diffusion Policy），论文将 Critic（评论家网络）扩展为评估整个动作序列（Chunk），而非单个时间步。这改善了时间一致性和信用分配。
    *   **策略提取**：使用优势加权回归（AWR）根据 Critic 评估的优势值对演示数据进行重加权，使扩散策略学习高回报的行为。
    *   **动作执行**：采用基于 Transformer 的扩散策略预测关节速度序列，并通过异步推理方案实现 40Hz 的实时控制。

### 3. 实验设计
*   **实验场景**：在 Isaac Sim 仿真环境中使用 TIAGo++ 机器人，设计了三个难度等级的任务：
    1.  **Door**（推门并穿过）：基础全身协调。
    2.  **Drawer**（双臂抽屉操作）：要求底座平移与旋转以配合双臂推拉。
    3.  **Cupboard**（同步开橱柜并放置物体）：最高难度，需持续的全身协同。
*   **Benchmark 与对比方法**：
    *   **WBC-policy**：原始手工调优控制器。
    *   **BC (Diffusion Policy)**：纯行为克隆。
    *   **RL (TD3)**：从零开始的在线强化学习。
    *   **离线 RL 基准**：IQL+DDPG BC、IDQL、RISE。
*   **真实世界验证**：在真实 TIAGo++ 机器人上进行零样本（Zero-shot）迁移测试。

### 4. 资源与算力
*   **算力支持**：论文提到使用了埃尔朗根国家高性能计算中心（NHR）的资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或训练总时长。但提到策略推理在 40Hz 下运行，且扩散策略采样（20步）在异步模式下耗时约 80-100ms。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验：每个任务进行 50 次评估，并计算 95% 置信区间。
    *   真实实验：每个任务进行 25 次测试。
    *   消融实验：针对 Transformer 架构、Q-chunking 必要性、状态估计方法（位姿追踪 vs 标记点）进行了详细对比。
*   **充分性评价**：实验设计较为充分且客观。通过跨难度任务验证了算法的扩展性，消融实验有力支撑了关键技术点（如 Q-chunking 对复杂任务的提升显著）。

### 6. 主要结论与发现
*   **性能超越**：WHOLE-MoMa 在所有任务中均优于原始 WBC 和纯 BC。在最难的橱柜任务中，成功率从 WBC 的 52% 提升至 78%。
*   **零样本迁移**：无需任何真实世界数据或微调，策略在真实机器人上实现了 80%（抽屉）和 68%（橱柜）的成功率。
*   **行为缝合有效性**：离线 RL 成功纠正了 WBC 的“近视”错误（如抓取位置不利于后续开启动作的问题）。
*   **架构优势**：基于 Transformer 的扩散策略在处理多阶段协同任务时远优于传统的 U-Net 架构。

### 7. 优点
*   **无需遥操作**：极大地降低了移动操作机器人的数据获取成本。
*   **结构先验利用**：巧妙地将经典控制器的物理可行性与强化学习的优化能力结合。
*   **技术创新**：提出的 Q-chunking 解决了离线 RL 与动作分块策略之间的对齐问题，提升了动作的平滑度与一致性。
*   **实战导向**：考虑了实时控制中的推理延迟，并提供了异步执行方案。

### 8. 不足与局限
*   **精度瓶颈**：在真实世界中，策略对状态估计（位姿追踪）的精度高度敏感。若位姿追踪出现微小漂移，会导致操作停滞。
*   **动力学间隙**：虽然实现了迁移，但真实机器人的关节顺从性和摩擦力与仿真存在差异，导致部分精细操作失败。
*   **依赖显式状态**：目前依赖于物体位姿追踪，而非纯视觉输入，这限制了其在未建模环境中的泛化能力。
*   **速度权衡**：为了稳定性，学习到的策略在执行速度上通常慢于最优化的 WBC。

（完）

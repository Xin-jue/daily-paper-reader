---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作系统的隐式规划框架
tldr: 针对现有视觉-语言-动作（VLA）模型在长程任务中缺乏推理和评估能力的问题，本文提出了WAV模型。该模型通过学习结构化潜空间表示，结合世界模型预测未来状态和价值函数评估轨迹效用，将动作生成转化为潜空间推理。理论证明该方法能有效解决动作空间搜索效率低的问题，在模拟和现实实验中显著提升了长程复杂任务的成功率和泛化性。
selection_source: fresh_fetch
motivation: 现有的VLA模型多采用直接动作预测，缺乏对长程轨迹的推理和后果评估能力，难以应对复杂决策任务。
method: 提出WAV模型，通过世界模型预测未来状态并利用价值函数评估轨迹，在结构化潜空间中进行隐式规划以生成动作。
result: 实验表明WAV在任务成功率、泛化能力和鲁棒性上均优于现有SOTA方法，特别是在长程和组合任务中表现突出。
conclusion: 在潜空间进行隐式规划能有效重塑搜索分布，是提升具身智能体长程决策能力的有效途径。
---

## 摘要
视觉-语言-动作（VLA）模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，现有的大多数方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作（WAV）模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习以视觉观测和语言指令为条件的未来轨迹的结构化潜表示。一个学习到的世界模型负责预测未来状态，而轨迹价值函数则评估其长时程效用。动作生成随后被表述为在该潜空间中的推理过程，模型逐渐将概率质量集中在高价值且动力学可行的轨迹上。我们提供了一个理论视角，表明直接在动作空间中进行规划会面临随着时程增加，可行轨迹概率呈指数级衰减的问题。相比之下，潜空间推理将搜索分布重塑向可行区域，从而实现高效的长时程决策。大量的仿真和真实世界实验表明，WAV 模型始终优于现有最先进的方法，在任务成功率、泛化能力和鲁棒性方面取得了显著提升，特别是在长时程和组合场景中。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：World-Value-Action (WAV) 模型

#### 1. 核心问题与整体含义
现有的视觉-语言-动作（VLA）模型大多采用“反应式”策略，即直接根据当前观测预测动作。这种方式在处理长时程（Long-horizon）和复杂组合任务时，由于缺乏对未来状态的推理能力和对动作后果的评估机制，容易产生复合误差，导致任务失败。本文的研究动机在于：**能否将规划（Planning）转化为生成模型内部的隐式推理过程，而非显式的优化模块？** 论文旨在通过结合世界模型（预测未来）和价值函数（评估未来），使 VLA 系统具备在潜空间进行长程推理的能力。

#### 2. 方法论
WAV 模型提出了一个统一的框架，将感知、推理和动作整合为三个相互耦合的模块：

*   **核心思想**：将规划表述为潜空间（Latent Space）中的推理。模型不直接在动作空间搜索，而是通过迭代精炼未来轨迹的潜分布，使其向“动力学可行”且“高任务价值”的区域集中。
*   **关键技术细节**：
    *   **语言条件视频生成模块**：使用 Diffusion Transformer (DiT) 架构，根据指令和初始观测预测未来视觉轨迹特征。
    *   **轨迹价值模块**：评估预测视频轨迹的长程效用（Utility），输出状态价值。
    *   **动作解码模块**：融合视频特征和价值特征，解码出具体的机器人控制动作。
    *   **训练目标**：采用 **Flow Matching（流匹配）** 目标进行三阶段训练：先训练视频生成，再训练价值预测，最后联合训练动作解码。
*   **算法流程（迭代推理）**：
    1.  从初始噪声分布中采样视频潜变量，生成候选未来视觉特征。
    2.  对每个视频特征采样价值潜变量，计算其信噪比（SNR）作为评分。
    3.  选择评分最高的“精英”样本，更新潜变量的均值和方差（类似 MPPI 算法）。
    4.  通过 $K$ 次迭代后，使用优化后的分布生成最终动作。

#### 3. 实验设计
*   **仿真场景**：使用 **LIBERO** 基准测试集，包含四个子任务库：LIBERO-Spatial（空间推理）、Object（物体泛化）、Goal（目标达成）和 Long（长程组合任务）。
*   **真实世界场景**：使用 Piper 双臂机器人执行复杂任务，包括：整理碗筷（Bowl Organization）、铺平毛巾（Towel Flattening）以及长程抽屉任务（打开抽屉-放入物体-关闭抽屉）。
*   **对比方法**：
    *   **基础 VLA**：Diffusion Policy, Octo, OpenVLA, $\pi_0$ 等。
    *   **带世界模型的 VLA**：WorldVLA, DreamVLA, UniVLA, GE-ACT 等。

#### 4. 资源与算力
*   **硬件**：使用 8 张 **NVIDIA A100-SXM4-80GB** GPU。
*   **训练时长**：
    *   LIBERO 任务：全参数微调，视频模块 3-4 万步，价值/动作模块 4 万步，总计约 **5 天**。
    *   真实世界任务：每个任务独立训练，总计约 **3 天**。
*   **推理效率**：论文记录了不同迭代次数 $K$ 下的推理时间，当 $K=3$ 时，在保持高性能的同时兼顾了实时性。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 LIBERO 的全部 40 个任务和 3 个复杂的真实世界任务。
*   **消融实验**：非常详尽，涵盖了迭代次数 $K$、采样数量 $M$（视频）和 $N$（价值）、平滑参数 $\alpha/\beta$、以及精英选择数量 $K_1/K_2$ 对成功率的影响。
*   **客观性**：通过理论分析（引理 4.1 和命题 4.2）证明了潜空间规划相对于动作空间规划的优越性（避免指数级概率衰减），并结合定量成功率和定性轨迹可视化，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 平均成功率达到 98.1%，显著优于现有 SOTA 模型（如 GE-ACT 的 96.5%）。
*   **长程优势**：在 LIBERO-Long 任务中提升最明显，证明了隐式规划在缓解长程任务复合误差方面的有效性。
*   **真实世界鲁棒性**：在真实场景中，平均成功率从基线的 35.6% 提升至 75.6%，表现出更强的空间感知和多步协调能力。
*   **理论验证**：实验证实了迭代推理能有效重塑潜分布，使模型关注高价值区域。

#### 7. 优点
*   **理论支撑**：提供了严谨的数学证明，解释了为什么在潜空间规划比在原始动作空间更有效。
*   **模块化耦合**：将视频预测、价值评估和动作生成有机结合，既保留了生成模型的灵活性，又引入了强化学习的评估思想。
*   **即插即用的规划**：通过推理时的迭代优化实现规划，不需要在测试时进行复杂的显式轨迹优化。

#### 8. 不足与局限
*   **计算开销**：迭代推理（$K$ 次采样和去噪）增加了推理延迟和显存占用，对于实时性要求极高的机器人任务可能存在挑战。
*   **部署限制**：目前主要在特定任务上进行微调，尚未展示在大规模异构数据集上的零样本（Zero-shot）泛化能力。
*   **存储需求**：模型参数量约 2.2B，加上推理时的多路径采样，对边缘设备的存储和带宽有一定要求。

（完）

---
title: Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning
title_zh: 通过智能体快慢规划连接大模型推理与实时控制
authors: "Jiayi Chen, Shuai Wang, Guangxu Zhu, Chengzhong Xu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01681v1"
tags: ["query:q12"]
score: 9.0
evidence: 连接VLM推理与实时控制的分层框架
tldr: "本研究针对大模型推理与实时控制结合中的脆弱性和延迟问题，提出了Agentic Fast-Slow Planning（AFSP）分层框架。该框架通过Perception2Decision将视觉感知转化为云端语义指令，并利用Decision2Trajectory将指令映射为可执行轨迹，结合语义引导的A*算法与智能体细化模块优化规划。实验表明，该方法在CARLA仿真中显著提升了自动驾驶的鲁棒性，大幅降低了横向偏差并缩短了任务完成时间，实现了高层语义与底层控制的有效桥接。"
selection_source: fresh_fetch
motivation: 现有方法在将大模型语义意图映射到实时控制时，面临轨迹生成脆弱、难以验证及计算延迟高等挑战。
method: "提出一种解耦感知、推理与控制的快慢规划框架，通过VLM/LLM生成语义指令，并结合语义引导的A*算法与智能体细化模块生成可执行轨迹。"
result: "在CARLA仿真实验中，该方法相比纯MPC和A*引导的MPC基准，横向偏差降低了45%，任务完成时间缩短了12%以上。"
conclusion: 该框架通过分层解耦和语义引导，有效提升了自动驾驶系统在复杂环境下的鲁棒性与执行效率。
---

## 摘要
大型基础模型为自主系统提供了强大的推理能力，但将语义意图映射到可靠的实时控制仍然具有挑战性。现有方法要么 (i) 让大语言模型 (LLM) 直接生成轨迹——这种方式脆弱、难以验证且容易产生延迟；要么 (ii) 在线调整模型预测控制 (MPC) 的目标——这混合了慢速深思熟虑与快速控制，模糊了接口界限。我们提出了“智能体快慢规划”（Agentic Fast-Slow Planning），这是一个在自然时间尺度上解耦感知、推理、规划和控制的分层框架。该框架包含两个桥梁：Perception2Decision 利用车载视觉语言模型 (VLM) 检测器将场景压缩为以自我为中心的拓扑结构，然后通过云端 LLM 决策器将其映射为符号化驾驶指令，在保持可解释性的同时减少了带宽和延迟；Decision2Trajectory 则将指令转换为可执行路径，其中语义引导的 A* 算法（Semantic-Guided A*）将源自语言的软成本嵌入到经典搜索中，使解偏向于可行轨迹，而智能体细化模块（Agentic Refinement Module）则利用反馈和记忆来调整规划器的超参数。最后，MPC 实时跟踪轨迹，并针对困难情况提供可选的云端引导参考。在 CARLA 中的实验表明，与纯 MPC 和 A* 引导的 MPC 基准相比，智能体快慢规划提高了扰动下的鲁棒性，横向偏差减少了高达 45%，完成时间缩短了 12% 以上。代码可在 https://github.com/cjychenjiayi/icra2026_AFSP 获取。

## Abstract
Large foundation models enable powerful reasoning for autonomous systems, but mapping semantic intent to reliable real-time control remains challenging. Existing approaches either (i) let Large Language Models (LLMs) generate trajectories directly - brittle, hard to verify, and latency-prone - or (ii) adjust Model Predictive Control (MPC) objectives online - mixing slow deliberation with fast control and blurring interfaces. We propose Agentic Fast-Slow Planning, a hierarchical framework that decouples perception, reasoning, planning, and control across natural timescales. The framework contains two bridges. Perception2Decision compresses scenes into ego-centric topologies using an on-vehicle Vision-Language Model (VLM) detector, then maps them to symbolic driving directives in the cloud with an LLM decision maker - reducing bandwidth and delay while preserving interpretability. Decision2Trajectory converts directives into executable paths: Semantic-Guided A* embeds language-derived soft costs into classical search to bias solutions toward feasible trajectories, while an Agentic Refinement Module adapts planner hyperparameters using feedback and memory. Finally, MPC tracks the trajectories in real time, with optional cloud-guided references for difficult cases. Experiments in CARLA show that Agentic Fast-Slow Planning improves robustness under perturbations, reducing lateral deviation by up to 45% and completion time by over 12% compared to pure MPC and an A*-guided MPC baseline. Code is available at https://github.com/cjychenjiayi/icra2026_AFSP.

---

## 论文详细总结（自动生成）

这是一份关于论文《Bridging Large-Model Reasoning and Real-Time Control via Agentic Fast-Slow Planning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对自动驾驶中**大模型推理与实时控制结合**的难题提出了解决方案。
*   **研究动机**：虽然大语言模型（LLM）具备强大的语义推理能力，但直接用于控制存在三大缺陷：(1) 直接生成轨迹过于脆弱且难以验证；(2) 推理延迟高，无法满足实时性要求；(3) 现有方法往往将慢速推理与快速控制混为一谈，导致接口模糊。
*   **核心目标**：通过“快慢规划”解耦感知、推理、规划与控制，在保证实时性的同时，利用大模型的常识推理来指导底层执行器。

### 2. 方法论：核心思想与关键技术
提出了 **Agentic Fast-Slow Planning (AFSP)** 框架，包含两个关键桥梁：
*   **Perception2Decision（感知到决策）**：
    *   **车载端**：使用轻量化视觉语言模型（VLM）作为拓扑检测器，将原始图像压缩为以自我为中心的“拓扑图”（包含物体类别、距离、方位等符号信息），极大降低了上传云端的带宽需求。
    *   **云端**：LLM 接收拓扑图，通过思维链（CoT）输出符号化驾驶指令（如 LEFT, RIGHT, KEEP）和驾驶风格。
*   **Decision2Trajectory（决策到轨迹）**：
    *   **语义引导 A* 算法**：在经典 A* 搜索中引入“语义软成本”。根据 LLM 指令对路径进行奖励或惩罚，使规划结果在符合几何可行性的基础上，倾向于执行语义意图。
    *   **智能体细化模块（Agentic Refinement）**：利用 LLM 智能体自动调整 A* 算法的超参数（如各项成本权重），通过“尝试-反馈-修正”的闭环模拟人类调参过程。
*   **控制层**：采用**云端引导的 MPC（模型预测控制）**，根据场景复杂度动态切换本地参考路径或云端引导路径，确保实时跟踪。

### 3. 实验设计
*   **数据集/场景**：
    *   基于 **CARLA** 仿真器构建了包含 49 类物体、20 万张图像的训练集（Town2-7, Town10HD）。
    *   设计了三种测试场景：常规地图、带扰动的偏移地图（横向偏移 0.5m-2m）、带障碍物半径扩张的复杂地图。
*   **对比基准（Benchmark）**：
    *   **纯 MPC**：仅依赖局部避障。
    *   **A*-MPC**：经典 A* 规划引导的 MPC。
*   **评估指标**：完成时间、轨迹长度、平均/最大横向偏差、速度波动。

### 4. 资源与算力
*   **模型选择**：VLM 使用 InternVL-2B，LLM 使用 DeepSeek-v3 和 Qwen-VL-Chat。
*   **训练算力**：VLM 的微调在 **4 台 NVIDIA A100 GPU** 上进行，共训练 12 个 epoch，学习率为 $8 \times 10^{-5}$。
*   **推理延迟**：实验对比了云端 LLM 与 VLM 的推理耗时，LLM 平均延迟为 4.13s，显著优于 VLM 的 10.24s。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 VLM 微调策略进行了消融实验（三阶段对比）。
    *   对 200 帧图像中的 48 个典型决策场景进行了 LLM 决策一致性测试。
    *   在 CARLA 中对每个场景进行了 **10 次重复实验**以减少随机性影响。
*   **充分性评价**：实验覆盖了从感知精度、推理延迟到闭环控制性能的全流程验证。通过引入地图扰动测试，客观地证明了语义引导对提升系统鲁棒性的作用。

### 6. 主要结论与发现
*   **性能提升**：AFSP 框架相比纯 MPC，横向偏差降低了约 **45%**，任务完成时间缩短了 **12%** 以上。
*   **鲁棒性**：在地图发生偏移或感知存在扰动时，语义引导的 A* 能够纠正经典算法的盲目性，保持意图一致性。
*   **效率**：通过“图像->拓扑图->指令”的转化，成功将大模型推理移出实时控制循环，解决了延迟问题。

### 7. 优点与亮点
*   **解耦设计**：巧妙地将“慢速语义推理”与“快速几何规划”分离，符合人类认知的双系统理论。
*   **智能调参**：Agentic Refinement 模块解决了经典算法依赖人工调参的痛点，实现了参数的自适应优化。
*   **可解释性**：中间产物（拓扑图和符号指令）是人类可读的，增强了自动驾驶系统的透明度。

### 8. 不足与局限
*   **仿真依赖**：所有实验均在 CARLA 仿真环境中完成，尚未在真实道路或复杂天气/光照条件下验证。
*   **云端依赖风险**：虽然减少了带宽，但系统仍高度依赖云端连接，若发生网络中断，云端引导的 MPC 切换机制在极端情况下的安全性需进一步探讨。
*   **场景覆盖**：实验场景主要集中在避障和换道，对于更复杂的交通流交互（如无信号灯路口博弈）涉及较少。

（完）

---
title: "A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model"
title_zh: A1：一个完全透明、开源、自适应且高效的截断式视觉-语言-动作模型
authors: "Kaidong Zhang, Jian Zhang, Rongtao Xu, Yu Sun, Shuoshuo Xue, Youpeng Wen, Xiaoyu Guo, Minghao Guo, Weijia Liufu, Liu Zihou, Kangyi Ji, Yangsong Zhang, Jiarun Zhu, Jingzhi Liu, Zihang Li, Ruiyi Chen, Meng Cao, Jingming Zhang, Shen Zhao, Xiaojun Chang, Feng Zheng, Ivan Laptev, Xiaodan Liang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05672v1"
tags: ["query:q12"]
score: 10.0
evidence: 用于机器人操作的高效截断式视觉-语言-动作模型
tldr: A1是一个开源透明的视觉-语言-动作（VLA）模型框架，旨在解决现有模型推理成本高、延迟大的问题。它通过预训练VLM提供先验，并引入预算感知的自适应推理方案，包括中间层动作一致性监测触发的提前终止机制和跨层截断流匹配技术。在多个仿真和真实机器人基准测试中，A1在显著降低计算开销的同时，实现了超越pi0等主流模型的SOTA性能。
selection_source: fresh_fetch
motivation: 现有的VLA模型由于庞大的骨干网络和复杂的动作生成头，导致推理延迟高且计算成本昂贵，难以在普通硬件上实现实时控制。
method: 提出了一种自适应推理方案，通过监测中间层动作一致性实现提前终止，并利用跨层截断流匹配技术减少去噪迭代次数。
result: "在RoboChallenge等多个基准测试中，A1以更低的推理成本（最高减少76.6%计算量）实现了29.00%的平均成功率，优于pi0和RDT-1B等基准模型。"
conclusion: A1证明了通过优化推理流程和利用VLM中间层信息，可以在不牺牲操作成功率的前提下大幅提升VLA模型的效率和实时性。
---

## 摘要
视觉-语言-动作（VLA）模型已成为开放世界机器人操控的一种强大范式，但其实际部署往往受限于成本：十亿级参数规模的 VLM 主干网络和基于迭代扩散/流（diffusion/flow-based）的动作头会产生高延迟和高计算量，使得在通用硬件上进行实时控制变得昂贵。我们提出了 A1，这是一个完全开源且透明的 VLA 框架，旨在不牺牲操控成功率的情况下实现低成本、高吞吐量的推理。我们的方法利用预训练的 VLM 为动作生成提供隐式的可操作性先验（affordance priors）。我们发布了完整的训练栈（包括训练代码、数据/数据处理流水线、中间检查点和评估脚本），以实现端到端的复现。除了单独优化 VLM，A1 还通过引入一种预算感知的自适应推理方案来针对整个推理流水线，该方案能同时加速主干网络和动作头。具体而言，我们通过监测 VLM 中间层的动作一致性来触发提前终止，并提出了层间截断流匹配（Inter-Layer Truncated Flow Matching），该方法可以在不同层之间热启动去噪过程，从而以显著更少的有效去噪迭代次数实现精确的动作。在仿真基准测试（LIBERO、VLABench）和真实机器人（Franka、AgiBot）上，A1 在显著降低推理成本的同时，实现了最先进的成功率（例如，流匹配推理的每回合延迟降低了高达 72%，主干网络计算量减少了高达 76.6%，且性能下降极小）。在 RoboChallenge 上，A1 实现了 29.00% 的平均成功率，优于包括 pi0 (28.33%)、X-VLA (21.33%) 和 RDT-1B (15.00%) 在内的基准模型。

## Abstract
Vision--Language--Action (VLA) models have emerged as a powerful paradigm for open-world robot manipulation, but their practical deployment is often constrained by \emph{cost}: billion-scale VLM backbones and iterative diffusion/flow-based action heads incur high latency and compute, making real-time control expensive on commodity hardware. We present A1, a fully open-source and transparent VLA framework designed for low-cost, high-throughput inference without sacrificing manipulation success; Our approach leverages pretrained VLMs that provide implicit affordance priors for action generation. We release the full training stack (training code, data/data-processing pipeline, intermediate checkpoints, and evaluation scripts) to enable end-to-end reproducibility. Beyond optimizing the VLM alone, A1 targets the full inference pipeline by introducing a budget-aware adaptive inference scheme that jointly accelerates the backbone and the \emph{action head}. Specifically, we monitor action consistency across intermediate VLM layers to trigger early termination, and propose Inter-Layer Truncated Flow Matching that warm-starts denoising across layers, enabling accurate actions with substantially fewer effective denoising iterations. Across simulation benchmarks (LIBERO, VLABench) and real robots (Franka, AgiBot), A1 achieves state-of-the-art success rates while significantly reducing inference cost (e.g., up to 72% lower per-episode latency for flow-matching inference and up to 76.6% backbone computation reduction with minor performance degradation). On RoboChallenge, A1 achieves an average success rate of 29.00%, outperforming baselines including pi0(28.33%), X-VLA (21.33%), and RDT-1B (15.00%).

---

## 论文详细总结（自动生成）

以下是对论文《A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型虽然在开放世界机器人操控中表现出色，但部署成本极高。其瓶颈在于：1) 数十亿参数的 VLM 骨干网络推理延迟大；2) 基于扩散或流匹配（Flow Matching）的动作头需要多次迭代去噪，进一步增加了计算开销。
*   **核心目标**：开发一个完全开源、透明且高效的 VLA 框架（命名为 A1），在不牺牲操控成功率的前提下，实现低延迟、高吞吐量的实时控制。

### 2. 方法论
A1 结合了预训练 VLM 的语义能力与自适应推理机制，核心技术包括：
*   **模型架构**：采用 Molmo-7B 作为 VLM 骨干网络（提供隐式可操作性先验），动作头提供两种实现：流匹配（A1-FM，基于 Qwen3-400M）和 MLP（A1-MLP）。
*   **预算感知的自适应推理（Adaptive Inference）**：
    *   **动作一致性阈值化（Early-Exit）**：在推理过程中，监测 VLM 中间层的动作预测。如果连续两层之间的动作差异低于设定阈值 $\eta_i$，则提前终止 VLM 的前向计算。
    *   **层间截断流匹配（Inter-Layer Truncated Flow Matching, ITFM）**：针对迭代式动作头，提出在每个 VLM 层仅运行极少数去噪步骤（如 $\delta=2$），并将当前层的输出作为下一层的“热启动”初始值，避免了每层都从随机噪声开始，大幅压缩了动作头的计算时间。
*   **训练策略**：采用多退出（Multi-exit）监督训练，使模型在任何中间层都能产出合理的动作预测。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：LIBERO（包含 Spatial, Object, Goal, Long 四个子集）和 VLABench（侧重常识与多步推理）。
    *   **真实世界**：涵盖 Franka、AgiBot、ARX、WuJie-Arm 和 Dobot-Arm 等多种机器人平台。
    *   **基准测试**：在 RoboChallenge (Table30) 上进行了大规模评估。
*   **对比方法**：包括 $\pi_0$、$\pi_{0.5}$、OpenVLA、RDT-1B、X-VLA、Octo 等当前最先进的开源或闭源模型。

### 4. 资源与算力
*   **算力细节**：文中详细分析了 GFLOPs（如 Molmo-7B 全量推理约 11,074 GFLOPs），但**未明确说明具体的 GPU 型号（如 H100 或 A100）及具体的训练总时长**。
*   **训练参数**：预训练阶段 Batch Size 为 1024，总步数 200K；微调阶段根据任务不同，Batch Size 在 32 到 128 之间。

### 5. 实验数量与充分性
*   **实验规模**：实验设计非常全面，涵盖了从仿真到真实世界的跨平台验证。
    *   **仿真实验**：在 LIBERO 上进行了 500 次试验，VLABench 上每项任务 50 次试验。
    *   **真实实验**：在 4 个平台、7 类任务上进行了测试，每项任务重复 10 次。
    *   **消融实验**：针对不同退出阈值 $c$、去噪步数 $\delta$ 以及 ITFM 的有效性进行了深入探讨。
*   **充分性评价**：实验覆盖了零样本迁移（LIBERO-Plus）、多机器人泛化及计算效率分析，对比基线丰富，数据展示客观，充分证明了模型在效率与性能之间的平衡能力。

### 6. 主要结论与发现
*   **性能领先**：在 RoboChallenge 上以 29.00% 的成功率超越了 $\pi_0$ 和 RDT-1B。
*   **极高的效率提升**：通过自适应推理，在仅损失极小性能的情况下，可减少高达 **76.6%** 的骨干网络计算量；ITFM 技术将流匹配推理的延迟降低了约 **72%**。
*   **冗余性发现**：实验证明 VLM 的深层计算在许多简单动作中是冗余的，中间层隐藏状态已包含足够的空间特征来驱动动作生成。

### 7. 优点
*   **全栈开源**：不仅发布权重，还公开了完整的训练代码、数据处理流水线和评估脚本，极具透明度。
*   **联合优化**：不同于以往只优化 VLM 或只优化动作头的工作，A1 实现了两者的协同加速。
*   **热启动机制**：ITFM 的设计非常巧妙，解决了早期退出机制与迭代式生成头之间的兼容性难题。

### 8. 不足与局限
*   **数据依赖**：预训练仍依赖于带标签的可操作性（Affordance）数据，限制了数据规模的进一步扩张。
*   **累积误差**：作为模仿学习范式，模型仍面临复合误差（Compounding Errors）的问题，导致在某些长程任务中精度受限。
*   **系统延迟**：尽管推理速度加快，但在实际部署中，云端推理与本地执行之间的网络延迟仍是影响操控流畅性的瓶颈。
*   **阈值校准**：自适应推理的阈值需要预先在训练集上运行一次以进行校准，增加了部署前的前期准备工作。

（完）

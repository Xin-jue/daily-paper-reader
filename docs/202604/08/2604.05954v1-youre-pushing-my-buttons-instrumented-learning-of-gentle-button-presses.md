---
title: "You're Pushing My Buttons: Instrumented Learning of Gentle Button Presses"
title_zh: 仪表化学习：实现轻柔的按钮按压
authors: "Raman Talwar, Remko Proesmans, Thomas Lips, Andreas Verleysen, Francis wyffels"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05954v1"
tags: ["query:q2"]
score: 9.0
evidence: 学习按钮按下等接触密集型操作任务
tldr: 本研究探讨了在训练阶段使用仪器化（传感器化）手段提升机器人接触密集型任务性能的方法。研究以按按钮为实验场景，利用指尖麦克风捕捉音频，并以按钮状态信号作为特权监督来微调音频编码器。通过模仿学习，该策略在推理时仅需视觉和音频输入。实验表明，虽然各方法成功率接近，但仪器化引导的音频表示能显著降低接触力，证明了该方法在实现柔和操作方面的潜力。
selection_source: fresh_fetch
motivation: 传统的视觉和本体感受无法充分观测接触事件，限制了机器人执行精细接触任务的能力。
method: 利用指尖麦克风采集音频，并结合按钮状态的特权监督信号微调音频编码器，将其集成到模仿学习框架中。
result: 引入仪器化引导的音频表示在保持高成功率的同时，显著降低了机器人按压按钮时的接触力。
conclusion: 训练时的物体传感器化是一种实用的辅助目标，能有效提升机器人对接触事件的感知能力并优化操作质量。
---

## 摘要
仅凭摄像头和本体感受来学习富接触操作是困难的，因为接触事件仅能被部分观测。我们测试了训练时的仪表化（即物体传感器化）是否能在不产生部署时依赖的情况下提高策略性能。具体而言，我们以按压按钮作为实验平台，并使用带有麦克风的指尖来捕捉与接触相关的音频。我们使用仪表化的按钮状态信号作为特权监督，将音频编码器微调为接触事件检测器。我们通过三种策略将所得表征与模仿学习相结合，使得策略在推理过程中仅使用视觉和音频。不同方法的按钮按压成功率相似，但仪表化引导的音频表征一致地降低了接触力。这些结果支持将仪表化作为学习富接触操作策略的一种实用的训练时辅助目标。

## Abstract
Learning contact-rich manipulation is difficult from cameras and proprioception alone because contact events are only partially observed. We test whether training-time instrumentation, i.e., object sensorisation, can improve policy performance without creating deployment-time dependencies. Specifically, we study button pressing as a testbed and use a microphone fingertip to capture contact-relevant audio. We use an instrumented button-state signal as privileged supervision to fine-tune an audio encoder into a contact event detector. We combine the resulting representation with imitation learning using three strategies, such that the policy only uses vision and audio during inference. Button press success rates are similar across methods, but instrumentation-guided audio representations consistently reduce contact force. These results support instrumentation as a practical training-time auxiliary objective for learning contact-rich manipulation policies.

---

## 论文详细总结（自动生成）

这篇论文《You're Pushing My Buttons: Instrumented Learning of Gentle Button Presses》探讨了如何利用训练时的“仪表化”（Instrumentation）手段来提升机器人执行接触密集型任务的性能。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在接触密集型操作（如按按钮）中，仅依靠视觉和本体感受难以精确感知接触发生的瞬间，因为接触事件往往被遮挡或观测不足。
*   **研究动机**：研究者希望探索在**训练阶段**对物体进行传感器化（即“仪表化”，如给按钮安装电子开关以获取其准确状态），是否能作为一种“特权信息”来引导机器人学习。其最终目标是让机器人在**推理阶段**无需这些额外传感器，仅凭视觉和指尖麦克风音频就能实现既成功又“轻柔”的操作。

### 2. 方法论
*   **核心思想**：将仪表化获取的按钮状态作为监督信号，微调音频编码器，使其成为敏感的接触检测器，并将其集成到模仿学习策略中。
*   **关键技术细节**：
    *   **硬件架构**：使用 UR3e 机械臂，配备腕部摄像头、力/力矩传感器以及一个**集成了麦克风的自定义 3D 打印指尖**。
    *   **特权监督**：在训练时，按钮连接到微控制器，提供二进制的“按下/未按下”信号。
    *   **音频处理**：使用预训练的音频频谱变换器（AST）。通过仪表化信号作为标签，将 AST 微调为一个二分类“点击检测器”。
    *   **策略模型**：采用 **Diffusion Policy（扩散策略）**。输入包括腕部 RGB 图像和音频频谱图，输出为机械臂的位移动作。
    *   **集成策略**：
        1.  **Soft Sensor（软传感器）**：训练时输入真实的按钮状态，推理时替换为微调后的 AST 预测值。
        2.  **Deep Fusion（深度融合）**：将微调后的 AST 输出（逻辑值或嵌入向量）直接作为策略的条件输入。
    *   **自动化数据采集**：通过随机化起始位置和轨迹自动收集演示数据，一旦仪表化信号检测到按下动作即停止，确保了数据的高质量和一致性。

### 3. 实验设计
*   **场景与任务**：机器人需接近按钮、按压并缩回。
*   **对比方法（Benchmark）**：
    *   **Generic Audio Encoder**：使用未经微调的通用 AudioSet 预训练 AST 嵌入。
    *   **Deep Fusion (Classifier/Embeddings)**：使用经仪表化信号微调后的音频表征。
    *   **Soft Sensor**：基于预测状态的方案。
*   **评估指标**：
    *   **成功率 (SR)**：完成按压并缩回的比例。
    *   **接触力 (Fz)**：评估操作的“轻柔”程度，通过与专家演示的力分布之间的 Wasserstein 距离来衡量。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号或训练总时长。
*   **训练参数**：提到了使用 Adam 优化器，学习率为 $10^{-4}$，权重衰减为 $10^{-6}$，采用 500 步预热的余弦调度算法。音频微调进行了 10 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：每种策略进行了 40 次随机初始位置的实机测试（Rollouts）。
*   **充分性评价**：
    *   **优点**：实验包含了力分布的深入分析，而不仅仅是成功率，这对于接触任务非常关键。
    *   **局限**：40 次测试在统计学上样本量较小，导致各方法的成功率在 95% 置信区间内存在重叠，难以断定成功率上的显著差异。此外，实验仅限于单一类型的按钮。

### 6. 主要结论与发现
*   **成功率相似**：所有方法的成功率均在 45% 到 55% 之间，仪表化引导并未显著提升任务完成的成功率。
*   **接触质量显著提升**：使用仪表化微调后的音频表征（Deep Fusion）能显著降低按压时的峰值力。
*   **表征质量**：Deep Fusion 方案生成的力分布最接近专家水平，而 Soft Sensor 方案表现最差（力最大），可能是由于训练与推理之间的状态预测偏差（Train-Test Mismatch）和延迟导致的。
*   **结论**：仪表化是一种有效的训练时辅助手段，能帮助机器人学习到更优雅、更安全的交互行为。

### 7. 优点
*   **实用性强**：提出了一种在不改变部署环境（无需在物体上装传感器）的前提下，利用特权信息提升性能的闭环流程。
*   **多模态融合**：成功证明了音频在弥补视觉遮挡和缺乏力反馈方面的价值。
*   **自动化流程**：通过仪表化实现了高质量演示数据的自动采集，降低了人工示教成本。

### 8. 不足与局限
*   **任务单一**：仅在一种按钮上进行了验证，缺乏对不同材质、反馈机制（如无声按钮）或其他接触任务（如插拔、锁闭）的泛化性测试。
*   **性能瓶颈**：整体成功率仅约 50%，说明策略在处理复杂随机起始位置时仍有提升空间。
*   **延迟问题**：音频处理（频谱图创建）带来的延迟（约 50ms）可能限制了其在更高动态任务中的表现。
*   **环境鲁棒性**：未讨论环境背景噪音对指尖麦克风音频信号的影响。

（完）

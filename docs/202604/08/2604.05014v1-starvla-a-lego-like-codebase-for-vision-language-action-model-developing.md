---
title: "StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing"
title_zh: StarVLA：一个用于视觉-语言-动作模型开发的乐高式代码库
authors: StarVLA Community
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05014v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作（VLA）模型开发的基准代码库
tldr: 具身智能通用智能体的构建依赖于视觉-语言-动作（VLA）模型的集成，但目前该领域面临架构不兼容和评估协议碎片化等挑战。本文推出了开源框架 StarVLA，采用模块化的“主干-动作头”架构，支持多种 VLM 和世界模型主干的灵活替换。StarVLA 提供了统一的训练策略和评估接口，集成了多个主流基准测试。实验表明，该框架在多个基准上达到了领先水平，显著降低了 VLA 模型的开发与复现门槛。
selection_source: fresh_fetch
motivation: 针对当前 VLA 研究中架构碎片化、代码库不兼容以及评估协议不统一导致的复现与对比困难问题。
method: 提出一种类似乐高的模块化架构，支持独立更换主干网络与动作头，并集成跨具身学习策略与统一的评估接口。
result: 凭借简单的训练方案，StarVLA 在 LIBERO 和 SimplerEnv 等多个基准测试中达到或超过了现有先进方法的性能。
conclusion: StarVLA 是一个全面且易用的开源 VLA 框架，能有效促进具身智能算法的快速原型设计与标准化评估。
---

## 摘要
构建通用具身智能体需要整合感知、语言理解和动作，这些是基于多模态基础模型（包括视觉-语言模型和世界模型的最新进展）的视觉-语言-动作（VLA）方法所解决的核心能力。尽管取得了快速进展，但 VLA 方法在不兼容的架构、代码库和评估协议中仍然处于碎片化状态，阻碍了有原则的比较和可复现性。我们提出了 StarVLA，一个用于 VLA 研究的开源代码库。StarVLA 从三个方面应对这些挑战。首先，它提供了一种模块化的“主干网络-动作头”架构，支持 VLM 主干（如 Qwen-VL）和世界模型主干（如 Cosmos），以及代表性的动作解码范式，所有这些都在一个共享的抽象层下，其中主干网络和动作头可以各自独立更换。其次，它提供了可重用的训练策略，包括跨具身学习和多模态协同训练，这些策略在支持的范式中一致适用。第三，它通过统一的评估接口集成了主要的基准测试，包括 LIBERO、SimplerEnv、RoboTwin 2.0、RoboCasa-GR1 和 BEHAVIOR-1K，支持仿真和真实机器人部署。StarVLA 还提供了简单、完全可复现的单基准训练方案，尽管数据工程极少，但在使用 VLM 和世界模型主干的多个基准测试上，已经达到或超过了之前的方法。据我们所知，StarVLA 是目前最全面的开源 VLA 框架之一，我们期望它能降低复现现有方法和开发新方法原型的门槛。StarVLA 正在积极维护和扩展；我们将随着项目的发展更新本报告。代码和文档可在 https://github.com/starVLA/starVLA 获取。

## Abstract
Building generalist embodied agents requires integrating perception, language understanding, and action, which are core capabilities addressed by Vision-Language-Action (VLA) approaches based on multimodal foundation models, including recent advances in vision-language models and world models. Despite rapid progress, VLA methods remain fragmented across incompatible architectures, codebases, and evaluation protocols, hindering principled comparison and reproducibility. We present StarVLA, an open-source codebase for VLA research. StarVLA addresses these challenges in three aspects. First, it provides a modular backbone--action-head architecture that supports both VLM backbones (e.g., Qwen-VL) and world-model backbones (e.g., Cosmos) alongside representative action-decoding paradigms, all under a shared abstraction in which backbone and action head can each be swapped independently. Second, it provides reusable training strategies, including cross-embodiment learning and multimodal co-training, that apply consistently across supported paradigms. Third, it integrates major benchmarks, including LIBERO, SimplerEnv, RoboTwin~2.0, RoboCasa-GR1, and BEHAVIOR-1K, through a unified evaluation interface that supports both simulation and real-robot deployment. StarVLA also ships simple, fully reproducible single-benchmark training recipes that, despite minimal data engineering, already match or surpass prior methods on multiple benchmarks with both VLM and world-model backbones. To our best knowledge, StarVLA is one of the most comprehensive open-source VLA frameworks available, and we expect it to lower the barrier for reproducing existing methods and prototyping new ones. StarVLA is being actively maintained and expanded; we will update this report as the project evolves. The code and documentation are available at https://github.com/starVLA/starVLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：具身智能（Embodied AI）领域中的视觉-语言-动作（VLA）模型研究目前处于“碎片化”状态。不同的方法往往采用不兼容的架构、私有的代码库和不统一的评估协议，导致研究者难以进行公平的性能比较，且实验结果难以复现。
*   **整体含义**：StarVLA 旨在构建一个“乐高式”的开源开发框架。它通过统一的抽象接口，将 VLA 模型解耦为可灵活替换的“主干网络（Backbone）”和“动作头（Action Head）”，从而消除不同范式（如基于 VLM 的方法与基于世界模型的方法）之间的隔阂，降低开发门槛，推动具身智能研究的标准化。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用**主干网络-动作头（Backbone-Action-Head）**的解耦架构。
*   **关键技术细节**：
    *   **统一 I/O 接口**：训练和推理均直接处理原始环境观测（图像、文本指令），避免了复杂的预处理导致的训练/测试分布偏移。
    *   **模块化组件**：
        *   **主干网络**：支持 Qwen2.5-VL 等视觉语言模型（VLM）和 Cosmos 等世界模型（World Model）。
        *   **动作头**：集成了四种代表性范式：**StarVLA-FAST**（离散动作标记化）、**StarVLA-OFT**（并行回归）、**StarVLA-$\pi$**（流匹配去噪）、**StarVLA-GR00T**（双系统推理）。
    *   **训练策略**：支持行为克隆（BC）监督学习、多目标协同训练（保持 VLM 的推理能力）以及跨具身（Cross-embodiment）混合数据训练。
    *   **Server-Client 评估架构**：通过 WebSocket 实现模型推理服务器与不同仿真环境客户端的分离，解决了不同基准测试环境依赖冲突的问题。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 130 个桌面操作任务，侧重长程依赖和泛化。
    *   **SimplerEnv**：模拟真实机器人（WidowX, Google Robot）的仿真环境。
    *   **RoboTwin 2.0**：大规模双臂协作基准。
    *   **RoboCasa-GR1**：类人机器人家庭场景操作。
    *   **BEHAVIOR-1K**：人类中心化的日常活动基准。
    *   **CALVIN**：长程语言指令操作。
*   **对比方法**：包括 OpenVLA、$\pi_0$、$\pi_{0.5}$、GR00T-N1.5/1.6、RT-1/2、CogACT 等当前最先进的 VLA 模型。

### 4. 资源与算力
论文详细记录了不同规模实验的算力消耗：
*   **LIBERO 实验**：使用 8 台 NVIDIA A100 GPU，训练 10 万步。
*   **SimplerEnv 实验**：使用 16 台 A100 GPU，训练 10 万步。
*   **RoboTwin 2.0 实验**：使用 48 台 A100 GPU，训练 15 万步。
*   **效率分析**：对比了 A100 与 H200 的单步延迟，并测试了从 8 到 256 台 GPU 的多节点扩展效率（在 64 卡以上时，并行效率稳定在 79-80%）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的实验，涵盖了单基准测试（Specialist）、多模态协同训练实验、以及跨基准/跨具身的通用模型（Generalist）实验。
*   **充分性与公平性**：
    *   **充分性**：在 5 个以上主流基准上进行了验证，并提供了详细的任务级成功率。
    *   **公平性**：所有 StarVLA 变体在相同的主干网络和数据条件下对比，且严格遵循各基准的官方评估协议。
    *   **客观性**：通过多次运行（如 SimplerEnv 运行 5 次取均值）来减少仿真环境的随机性干扰。

### 6. 主要结论与发现
*   **模块化有效性**：通过更换动作头，StarVLA 在多个基准上以更少的训练步数（如 LIBERO 上仅需 3 万步）达到了与 SOTA 相当或更优的性能。
*   **主干网络通用性**：证明了 VLM（Qwen）和世界模型（Cosmos）主干在 StarVLA 框架下均能有效适配各种动作解码器。
*   **协同训练的重要性**：实验发现，仅进行动作微调会导致 VLM 的感知和推理能力快速退化，而协同训练能有效保持空间接地（Grounding）能力。
*   **通用模型潜力**：单一模型在混合多个基准数据训练后，在 RoboCasa 等复杂任务上的表现甚至优于专门针对该基准训练的专家模型。

### 7. 优点
*   **高度模块化**：像乐高一样组合模型，极大方便了新算法的快速原型开发。
*   **工程实现卓越**：解决了具身智能研究中常见的环境依赖冲突、训练/推理不一致等痛点。
*   **开源贡献大**：提供了完整、可复现的训练脚本和预训练权重，是目前最全面的 VLA 开源框架之一。
*   **性能强劲**：即使不使用复杂的数据增强，其基础 Baseline 性能也极具竞争力。

### 8. 不足与局限
*   **强化学习（RL）缺失**：目前版本主要侧重于监督学习和协同训练，RL 微调功能仍在集成中。
*   **硬件依赖**：虽然框架高效，但训练高性能 VLA 模型仍需要大量的 A100/H200 算力，对小型研究团队仍有一定门槛。
*   **真实机器人验证**：论文虽然提到了支持真实机器人部署，但实验部分主要集中在仿真环境，缺乏大规模真实物理环境的对比数据。

（完）

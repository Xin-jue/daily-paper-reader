---
title: "FunRec: Reconstructing Functional 3D Scenes from Egocentric Interaction Videos"
title_zh: FunRec：从第一视角交互视频中重建功能性三维场景
authors: "Alexandros Delitzas, Chenyangguang Zhang, Alexey Gavryushin, Tommaso Di Mario, Boyang Sun, Rishabh Dabral, Leonidas Guibas, Christian Theobalt, Marc Pollefeys, Francis Engelmann, Daniel Barath"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05621v1"
tags: ["query:q6"]
score: 9.0
evidence: 从交互视频中重建用于仿真的功能性3D数字孪生
tldr: FunRec 是一种从第一视角 RGB-D 交互视频中重建功能性 3D 场景的方法。与依赖受控环境或 CAD 先验的现有方法不同，它能直接处理自然的人类交互序列，自动识别关节部件、估计运动学参数并重建静态与动态几何。该方法在分割精度和运动估计方面显著优于前人工作，支持导出为仿真格式，为机器人交互和场景理解提供了重要支持。
selection_source: fresh_fetch
motivation: 现有的关节式场景重建方法通常依赖受控环境或 CAD 模型，难以处理自然、非受控的第一视角交互视频。
method: 该方法通过自动发现关节部件、估计运动学参数并追踪 3D 运动，在规范空间中重建出可用于仿真的静态和动态几何模型。
result: "在真实和模拟基准测试中，FunRec 在部件分割 mIoU 上提升了 50%，且运动学和位姿误差降低了 5 到 10 倍。"
conclusion: FunRec 实现了从野外交互视频到功能性 3D 数字孪生的高效转换，为机器人仿真和环境感知提供了强大的工具。
---

## 摘要
我们提出了 FunRec，这是一种直接从第一视角 RGB-D 交互视频中重建室内场景功能性三维数字孪生的方法。与现有的依赖受控设置、多状态捕捉或 CAD 先验的关节重建方法不同，FunRec 直接在野外人类交互序列上运行，以恢复可交互的三维场景。它能自动发现关节部件，估计其运动学参数，跟踪其三维运动，并在规范空间中重建静态和运动几何体，从而生成兼容仿真的网格。在新的真实和模拟基准测试中，FunRec 大幅超越了以往的工作，在部件分割方面实现了高达 +50 mIoU 的提升，关节和姿态误差降低了 5-10 倍，且重建精度显著提高。我们进一步展示了在仿真 URDF/USD 导出、手部引导的可利用性映射以及机器人-场景交互方面的应用。

## Abstract
We present FunRec, a method for reconstructing functional 3D digital twins of indoor scenes directly from egocentric RGB-D interaction videos. Unlike existing methods on articulated reconstruction, which rely on controlled setups, multi-state captures, or CAD priors, FunRec operates directly on in-the-wild human interaction sequences to recover interactable 3D scenes. It automatically discovers articulated parts, estimates their kinematic parameters, tracks their 3D motion, and reconstructs static and moving geometry in canonical space, yielding simulation-compatible meshes. Across new real and simulated benchmarks, FunRec surpasses prior work by a large margin, achieving up to +50 mIoU improvement in part segmentation, 5-10 times lower articulation and pose errors, and significantly higher reconstruction accuracy. We further demonstrate applications on URDF/USD export for simulation, hand-guided affordance mapping and robot-scene interaction.

---

## 论文详细总结（自动生成）

这是一份关于论文《FunRec: Reconstructing Functional 3D Scenes from Egocentric Interaction Videos》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从一段非受控的、自然的人类第一视角（Egocentric）RGB-D 交互视频中，重建出不仅包含静态几何、还包含可交互关节部件（如门、抽屉）及其运动学参数的“功能性”三维数字孪生。
*   **研究动机**：
    *   **静态局限性**：现有的 3D 重建方法大多关注静态场景，忽略了物体在交互下的动态变化（如冰箱门的开启、抽屉的滑动）。
    *   **现有方法缺陷**：以往的关节式重建依赖于受控环境、多状态扫描（开/关状态分别扫描）或预定义的 CAD 模型库，难以处理现实世界中复杂且多样的“野外”场景。
    *   **交互即监督**：作者认为人类的交互行为本身就提供了最直接的监督信号，揭示了哪些部分是可动的、运动轴在哪里以及被遮挡的内部空间。

### 2. 方法论
FunRec 是一个**无需训练（Training-free）**、基于优化的流水线，核心思想是将几何推理与基础模型的语义/运动先验相结合：
*   **片段构建（Fragment Construction）**：利用视觉语言模型（VLM，如 Gemini）将视频划分为静态和动态片段，并识别交互类型（平移或旋转）。
*   **相机与轨迹估计**：使用 RoMA 估计相机位姿，并利用 TAPIP3D 提取世界坐标系下的稀疏 3D 运动轨迹。
*   **关节感知运动聚类**：
    *   根据 VLM 预测的运动类型（Prismatic 平移或 Revolute 旋转），将 3D 轨迹拟合到相应的运动模型（直线或圆弧）。
    *   使用 HDBSCAN 算法根据拟合参数（轴向、中心点等）对轨迹进行聚类，识别出独立运动的部件。
*   **像素级分割**：结合 3D 轨迹投影和 SAM2 的视频传播能力，生成精确的动态部件掩码。
*   **联合优化**：构建位姿图，使用 Ceres Solver 联合优化部件的 6D 位姿序列和运动学参数（轴向 $a$、枢轴 $p$、运动状态 $\theta/\lambda$），确保物理一致性。
*   **双 TSDF 重建**：分别在世界坐标系（静态背景）和规范坐标系（动态部件）中融合深度信息，提取出完整的 3D 网格，包括交互过程中暴露的物体内部。

### 3. 实验设计
*   **数据集**：
    *   **RealFun4D（新提出）**：包含来自 4 个国家、60 个公寓的 351 段真实交互视频。
    *   **OmniFun4D（新提出）**：在 OmniGibson 仿真环境中渲染的 127 段光追高质量合成视频。
    *   **HOI4D**：现有的实验室环境下的单物体交互数据集（选取 30 段）。
*   **对比方法**：
    *   **4D 重建类**：MonST3R, SpatialTrackerV2。
    *   **6D 位姿跟踪类**：BundleSDF。
    *   **关节物体重建类**：ArtGS。
*   **评估指标**：关节轴误差（角度/距离）、运动状态误差、6D 位姿精度（ADD/ADD-S）、分割 mIoU 以及重建质量（Chamfer Distance）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**完整的训练或推理时长。
*   **提及信息**：提到了使用 NVIDIA RTX 光线追踪进行数据渲染，并获得了 NVIDIA 的学术资助。由于该方法是“无需训练”的优化算法，其主要开销在于运行基础模型（VLM, SAM2）的推理以及 Ceres Solver 的非线性优化过程。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同性质的数据集（真实野外、高质量仿真、实验室受控）上进行了广泛测试。
*   **充分性**：
    *   涵盖了运动估计、位姿跟踪、分割和几何重建四个维度，实验设计非常全面。
    *   对比了当前最先进的（SOTA）多种基准模型，并提供了消融实验（如在表 1 和表 2 中对比了使用真值深度与估计深度的情况）。
*   **客观性**：通过引入具有物理真值的仿真数据集（OmniFun4D），确保了定量评估的客观性和准确性。

### 6. 主要结论与发现
*   **性能飞跃**：FunRec 在所有指标上均大幅领先。在部件分割上提升了约 50 mIoU，在关节参数估计和位姿跟踪上的误差比现有方法降低了 5-10 倍。
*   **鲁棒性**：该方法能有效处理第一视角视频中常见的剧烈相机运动、手部遮挡以及复杂的室内光照。
*   **功能性实现**：成功实现了从单段视频直接导出可用于物理仿真的 URDF/USD 格式模型。

### 7. 优点
*   **端到端功能化**：不仅重建几何，还恢复了运动学约束，直接打通了“视频观测”到“机器人仿真”的链路。
*   **无需针对性训练**：巧妙利用了现有的基础模型（VLM, SAM2, RoMA）作为组件，具有极强的泛化能力。
*   **处理遮挡能力强**：通过在规范空间（Canonical Space）融合几何，能够补全交互过程中逐渐显露的物体内部结构。

### 8. 不足与局限
*   **依赖 RGB-D 输入**：虽然目前很多头显（如 Vision Pro, Azure Kinect）具备深度感知，但对于纯 RGB 视频的适用性受限。
*   **多关节限制**：目前的模型主要针对单关节（一个移动件）交互，对于具有复杂联动结构的物体（如多节折叠门）可能需要更复杂的层级建模。
*   **实时性挑战**：由于涉及多步基础模型推理和非线性全局优化，该方法目前可能无法达到实时处理的速度。
*   **VLM 依赖**：片段划分和运动类型识别高度依赖 VLM 的判断，若 VLM 误判运动类型，后续优化将难以收敛。

（完）

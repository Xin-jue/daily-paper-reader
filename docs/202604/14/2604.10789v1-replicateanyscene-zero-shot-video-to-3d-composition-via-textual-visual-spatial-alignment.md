---
title: "ReplicateAnyScene: Zero-Shot Video-to-3D Composition via Textual-Visual-Spatial Alignment"
title_zh: ReplicateAnyScene：通过文本-视觉-空间对齐实现零样本视频到 3D 场景组合
authors: "Mingyu Dong, Chong Xia, Mingyuan Jia, Weichen Lyu, Long Xu, Zheng Zhu, Yueqi Duan"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10789v1"
tags: ["query:q9"]
score: 10.0
evidence: 具身智能的自动化 3D 场景合成
tldr: ReplicateAnyScene是一个零样本框架，能将普通视频自动转换为结构化3D场景。针对现有方法依赖人工提示且难以处理复杂场景的问题，该研究提出五阶段级联流水线，通过在文本、视觉和空间维度对齐视觉大模型的通用先验，确保场景的语义一致性和物理合理性。此外，研究还推出了C3DR基准测试，实验证明该方法在生成高质量组合式3D场景方面优于现有基准。
selection_source: fresh_fetch
motivation: 现有组合式3D重建方法过度依赖人工提示且受限于训练偏差，难以在复杂现实场景中实现全自动部署。
method: 提出一种五阶段级联流水线，通过在文本、视觉和空间维度提取并对齐视觉基础模型的通用先验，将其植入结构化3D表示中。
result: 实验结果表明，该方法在生成高质量、语义连贯且物理合理的组合式3D场景方面显著优于现有基线。
conclusion: ReplicateAnyScene实现了全自动、零样本的视频到3D场景转换，为空间智能和具身智能的发展提供了重要工具和评估基准。
---

## 摘要
人类具有从视频观测中快速感知和分割物体，甚至在脑海中将其组装成结构化 3D 场景的天赋能力。复制这种被称为“组合式 3D 重建”的能力，对于空间智能（Spatial Intelligence）和具身智能（Embodied AI）的发展至关重要。然而，由于跨模态信息整合不足，现有方法难以实现实际部署，导致其依赖手动物体提示、依赖辅助视觉输入，并受训练偏差限制而仅限于过于简单的场景。为了解决这些局限性，我们提出了 ReplicateAnyScene，这是一个能够将随意拍摄的视频全自动且零样本转换为组合式 3D 场景的框架。具体而言，我们的流水线包含一个五阶段级联过程，用于从视觉基础模型中提取并在文本、视觉和空间维度上结构化地对齐通用先验，将其落地为结构化 3D 表示，并确保所构建场景的语义连贯性和物理合理性。为了促进对该任务更全面的评估，我们进一步引入了 C3DR 基准测试，从多个维度评估重建质量。大量实验表明，我们的方法在生成高质量组合式 3D 场景方面优于现有的基准方法。

## Abstract
Humans exhibit an innate capacity to rapidly perceive and segment objects from video observations, and even mentally assemble them into structured 3D scenes. Replicating such capability, termed compositional 3D reconstruction, is pivotal for the advancement of Spatial Intelligence and Embodied AI. However, existing methods struggle to achieve practical deployment due to the insufficient integration of cross-modal information, leaving them dependent on manual object prompting, reliant on auxiliary visual inputs, and restricted to overly simplistic scenes by training biases. To address these limitations, we propose ReplicateAnyScene, a framework capable of fully automated and zero-shot transformation of casually captured videos into compositional 3D scenes. Specifically, our pipeline incorporates a five-stage cascade to extract and structurally align generic priors from vision foundation models across textual, visual, and spatial dimensions, grounding them into structured 3D representations and ensuring semantic coherence and physical plausibility of the constructed scenes. To facilitate a more comprehensive evaluation of this task, we further introduce the C3DR benchmark to assess reconstruction quality from diverse aspects. Extensive experiments demonstrate the superiority of our method over existing baselines in generating high-quality compositional 3D scenes.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ReplicateAnyScene** 的框架，旨在实现从普通视频到结构化 3D 场景的全自动、零样本转换。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 重建方法（如 NeRF 或 3DGS）多侧重于整体视觉保真度，缺乏物体级的结构化信息。而现有的“组合式 3D 重建”方法存在以下痛点：
    *   **过度依赖人工**：需要手动提供物体提示或类别。
    *   **环境受限**：依赖高精度深度图或语义输入，且仅限于简单场景。
    *   **物理不合理**：生成的物体常出现悬浮、穿模或姿态扭曲。
*   **研究动机**：模拟人类从视频中感知、分割并心理组装 3D 场景的能力，为空间智能和具身智能提供可交互的结构化环境。

### 2. 方法论：五阶段级联流水线
该方法的核心思想是通过**文本-视觉-空间**三维度的对齐，将视觉基础模型（VFMs）的先验知识整合进 3D 空间：
1.  **渐进式物体发现 (Progressive Object Discovery)**：利用视觉语言模型（VLM，如 Qwen3-VL）扫描视频关键帧，通过动态类别注册表迭代提取非冗余的开放词汇物体清单。
2.  **空间引导的视觉去重 (Spatial-Guided Visual Deduplication)**：将 2D 掩码投影到 3D 空间，利用几何邻近度（欧氏距离阈值）和并查集（Union-Find）算法合并因遮挡或出入画导致的破碎实例片段。
3.  **最优视角 3D 资产生成 (Optimal-View 3D Asset Generation)**：不简单以 2D 面积最大为准，而是计算 3D 投影表面积，选择可见信息最丰富的视角，调用 SAM3D 生成高质量 3D 网格。
4.  **迭代视觉-空间对齐 (Iterative Visual-Spatial Alignment)**：采用“渲染-匹配-优化”循环。利用 MASt3R 建立真实图与渲染图的稠密对应关系，通过 Umeyama 算法迭代优化物体的位姿（尺度、旋转、平移）。
5.  **语义感知场景精修 (Semantic-Aware Scene Refinement)**：利用 VLM 推断物体间的空间关系（如“支撑”、“贴墙”），并应用预定义的几何规则修正物理违规（如消除悬浮、对齐重力轴）。

### 3. 实验设计
*   **基准测试 (Benchmark)**：引入了 **C3DR** 基准，包含 50 个复杂场景，数据源自 Replica、ScanNet、ScanNet++ 以及 YouTube 真实视频。
*   **对比方法**：
    *   **SimRecon**：目前最先进的基于优化的组合式重建方法。
    *   **MetaScenes**：基于检索的场景重建范式。
*   **评估指标**：
    *   **文本维度**：召回率 (Rec)、F1 分数。
    *   **视觉维度**：PSNR、SSIM、LPIPS、MUSIQ（无参考质量评估）。
    *   **几何维度**：倒角距离 (CD)、F-Score、法线一致性 (NC)。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX A6000** GPU。
*   **处理效率**：平均处理一个场景约 **27 分钟**，单个物体约耗时 1.6 分钟。
*   **训练情况**：该框架为零样本（Zero-Shot）推理，无需针对特定场景进行大规模重训练。

### 5. 实验数量与充分性
*   **实验规模**：在 50 个涵盖室内、真实世界及合成数据的多样化场景上进行了测试。
*   **消融实验**：针对流水线的全部五个阶段分别进行了详尽的消融实验（如：去重 vs 不去重、最优视角选择策略对比、ICP 对齐 vs 视觉对齐等）。
*   **评价**：实验设计较为充分且客观。通过引入 C3DR 基准，解决了该领域缺乏统一评估标准的问题，对比实验涵盖了当前主流的检索式和优化式路径，具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：ReplicateAnyScene 在所有评估维度（语义、视觉、几何）上均显著优于现有基线。
*   **自动化程度高**：实现了从原始视频到 3D 场景的端到端自动化，无需任何人工干预。
*   **物理合理性**：通过语义精修阶段，成功解决了 3D 场景合成中常见的物体悬浮和穿模问题。

### 7. 优点与亮点
*   **多模态对齐**：巧妙地将 VLM 的语义能力、SAM 的分割能力和几何模型的对齐能力结合在一起。
*   **鲁棒的对齐算法**：基于视觉特征匹配（MASt3R）而非单纯几何匹配（ICP），有效避免了局部最优解。
*   **开源贡献**：提供了 C3DR 基准测试，填补了组合式 3D 重建评估的空白。

### 8. 不足与局限
*   **自然场景挑战**：在处理具有高度不规则几何形状的自然物体（如复杂的植被）时仍存在困难。
*   **无界环境限制**：目前主要针对室内或受限场景，对于大规模、无边界的室外自然环境的对齐和合成仍是开放性难题。
*   **依赖基础模型**：其最终效果高度依赖于底层 VLM 和 3D 生成模型（如 SAM3D）的性能上限。

（完）

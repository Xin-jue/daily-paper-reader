---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于精确机械臂操作和槽位级放置的目标条件VLA策略
tldr: 针对视觉语言动作（VLA）策略在处理复杂指令下的高精度插槽级放置任务（如亚厘米级精度）存在的挑战，本文提出了AnySlot框架。该框架通过引入显式空间视觉目标作为中间表示，将语言指令转化为场景标记，并利用目标条件VLA策略执行任务。这种分层设计解耦了高层语义定位与底层精确控制。此外，研究者还推出了SlotBench基准测试。实验证明AnySlot在零样本插槽级放置任务中显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的单体VLA策略在处理需要亚厘米级精度和复杂语义理解的插槽级放置任务时，难以兼顾可靠的插槽定位与精确的动作执行。
method: 提出AnySlot框架，通过生成场景标记将语言指令转化为显式视觉目标，并采用目标条件VLA策略实现高层语义定位与底层精确控制的解耦。
result: 在包含九类任务的SlotBench基准测试中，AnySlot在零样本插槽级放置任务上的表现显著优于传统的扁平化VLA模型和模块化定位方法。
conclusion: 通过引入中间视觉目标来解耦语义理解与动作执行，能有效提升机器人策略在复杂、高精度放置任务中的泛化能力和操作鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略来说仍然是一个重大挑战。插槽级任务既需要可靠的插槽定位（grounding），也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，这是一个通过引入显式空间视觉目标作为语言定位与控制之间的中间表示，从而降低组合复杂性的框架。AnySlot 通过生成场景标记（scene marker）将语言转化为显式视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到目前缺乏针对此类高精度任务的基准测试，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置方面显著优于扁平化 VLA 基线和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

以下是对论文《AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）策略在处理“插槽级放置”（Slot-level placement）任务时表现不佳。这类任务要求极高：一方面需要理解复杂的组合语言指令（如“放在左数第二个格子里”），另一方面需要亚厘米级的执行精度。
*   **核心挑战**：
    *   **单体模型局限性**：端到端（Flat）VLA模型难以同时兼顾高层语义推理和底层精确控制。
    *   **定位不准**：传统的模块化方法通常将目标简化为单个坐标点，丢失了插槽的几何边界信息，且对感知噪声敏感。
*   **研究意义**：通过解耦“推理”与“控制”，实现机器人在未见过的环境和复杂指令下，依然能完成高精度的零样本放置任务。

### 2. 方法论
AnySlot 采用两阶段的分层架构，核心思想是将语言指令转化为显式的**空间视觉目标**（Visual Goal）。

*   **第一阶段：高层目标构建（High-level Goal Construction）**
    1.  **图像生成定位**：利用现成的图像生成模型（如 Nano-Banana）在原始图像中根据指令生成一个“蓝色球体”标记。
    2.  **3D 锚点提取**：通过深度图和相机内参，将 2D 标记中心反投影到 3D 世界坐标系中，形成 3D 锚点。
    3.  **多视图对齐渲染**：将该 3D 锚点重新投影到所有相机视图（如头部相机、手腕相机）中，生成视图一致的视觉覆盖层（Overlay）。
*   **第二阶段：底层目标条件化策略（Low-level Goal-Conditioned Policy）**
    *   使用连续生成式 VLA 策略（基于 $\pi_{0.5}$，骨干网络为 PaliGemma-3B）。
    *   **输入**：带有蓝色球体标记的多视图图像、机器人状态。
    *   **输出**：连续动作序列。
    *   **解耦逻辑**：底层策略不再需要理解复杂指令，只需关注“将物体移动到蓝色球体标记处”，从而专注于物理执行。

### 3. 实验设计
*   **基准测试（SlotBench）**：论文推出了专门的仿真基准，包含 9 类空间推理任务：
    1. 序数推理（第几个）；2. 尺寸比较；3. 高度/垂直推理；4. 距离推理；5. 组合关系；6. 逻辑否定（不要放哪）；7. 模糊语言；8. 适宜性推理（稳定性）；9. 世界知识（如 Pepsi 旁）。
*   **对比方法**：
    *   **扁平化 VLA**：OpenVLA, $\pi_0, \pi_{0.5}$ 等。
    *   **模块化策略**：RoboPoint, AnyPlace, HAMSTER 等。
    *   **Oracle**：由人类专家提供完美目标位置的上限基准。
*   **评估指标**：成功率（SR）、指令准确率（IA，目标点在真实中心 2cm 内）、粗略准确率（CA）。

### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA H200 GPU**。
*   **训练细节**：
    *   对 $\pi_{0.5}$ 进行了全参数微调。
    *   训练步数：20,000 步。
    *   批大小（Batch Size）：64。
    *   数据集：使用 SAPIEN 仿真器收集的合成数据集 $D_{syn}$。

### 5. 实验数量与充分性
*   **实验规模**：每个任务类别进行 50 次试验，涵盖了 45 种以上的独特场景布局。
*   **消融实验**：
    *   对比了不同的高层定位模块（GPT-4o, Gemini 1.5 Pro, Nano-Banana 等）。
    *   对比了不同的底层策略架构（Diffusion Policy, OpenVLA-OFT 等）。
    *   验证了合成数据集对训练的重要性。
*   **充分性评价**：实验设计较为全面，不仅在仿真中进行了大规模测试，还包含了真实世界的机器人目标重建验证，对比了当前主流的多种 VLA 变体，结果具有较强的说服力。

### 6. 主要结论与发现
*   **AnySlot 表现卓越**：在 SlotBench 的所有类别中，AnySlot 达到了近 **90% 的平均成功率**，而扁平化 VLA 模型在复杂推理任务中几乎全部失败。
*   **视觉目标优于坐标回归**：将目标表示为图像中的视觉标记（球体），比回归单一坐标点更能提供几何上下文，显著提升了底层策略的鲁棒性。
*   **解耦是关键**：将复杂的语义理解交给强大的图像生成模型，将精确控制交给专门微调的 VLA，是解决高精度操控任务的有效路径。

### 7. 优点
*   **创新性架构**：首次提出利用图像生成模型作为 VLA 的中间“视觉目标”生成器，解决了坐标表示过于抽象的问题。
*   **基准贡献**：SlotBench 填补了机器人领域缺乏高精度、复杂语义放置任务基准的空白。
*   **零样本能力**：无需针对特定任务微调高层模型，利用大模型的先验知识即可实现多种复杂推理。

### 8. 不足与局限
*   **推理延迟**：依赖外部图像生成模型会引入额外的计算开销，导致实时性受限（文中提到 Nano-Banana 推理约需 11-16 秒）。
*   **感知依赖**：系统高度依赖深度图的准确性和相机标定的精度，在透明物体或强反光环境下可能失效。
*   **任务范围**：目前主要集中在单步放置任务，对于需要多步交互或长程规划的任务尚未深入探讨。

（完）

---
title: "LARY: A Latent Action Representation Yielding Benchmark for Generalizable Vision-to-Action Alignment"
title_zh: LARY：一个用于可泛化视觉到动作对齐的潜在动作表示生成基准
authors: "Dujun Nie, Fengjiao Chen, Qi Lv, Jun Kuang, Xiaoyu Li, Xuezhi Cao, Xunliang Cai"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11689v1"
tags: ["query:q12"]
score: 9.0
evidence: 用于评估VLA模型中潜在动作表示和机器人控制的基准框架
tldr: 针对视觉-语言-动作（VLA）模型中显式动作数据短缺的问题，本文提出了LARY基准，用于评估从视频中提取的潜在动作表示。该基准涵盖了高层语义和底层机器人控制，包含百万级视频及多模态轨迹数据。研究发现，通用视觉基础模型在动作表示上优于专门的具身模型，且语义抽象比像素重建更能有效实现视觉到动作的对齐。
selection_source: fresh_fetch
motivation: 旨在解决如何从大规模无标注人类视频中提取有效的潜在动作表示，以克服具身智能领域显式动作数据匮乏的挑战。
method: 提出了LARY基准框架，通过包含151类动作、百万级视频及大量运动轨迹的综合数据集，对潜在动作表示在语义和控制层面进行统一评估。
result: 实验表明通用视觉基础模型在动作表征上优于专门的具身模型，且潜在视觉空间比像素空间更契合物理动作空间。
conclusion: 语义层面的抽象是实现视觉到动作转换的更有效路径，通用视觉表征已内在蕴含了物理控制所需的关键动作知识。
---

## 摘要
虽然显式动作数据的短缺限制了视觉-语言-动作（VLA）模型的发展，但人类动作视频提供了一个可扩展且无需标注的数据源。利用大规模人类视频数据集的一个关键挑战在于将视觉信号转化为独立于本体（ontology-independent）的表示，即潜在动作（latent actions）。然而，潜在动作表示从视觉观测中推导出鲁棒控制的能力尚未得到严格评估。我们提出了 LARY（Latent Action Representation Yielding）基准，这是一个统一的框架，用于在高级语义动作（做什么）和低级机器人控制（怎么做）两个层面上评估潜在动作表示。该精心策划的数据集包含超过 100 万个视频（1,000 小时），涵盖 151 个动作类别，以及跨越不同具身智能体和环境的 62 万个图像对和 59.5 万条运动轨迹。我们的实验揭示了两个关键见解：（i）在没有任何动作监督的情况下训练的通用视觉基础模型，其表现始终优于专门的具身潜在动作模型。（ii）基于潜空间的视觉空间在本质上比基于像素的空间更能与物理动作空间对齐。这些结果表明，通用视觉表示本质上编码了用于物理控制的动作相关知识，并且语义层面的抽象是比像素层面重建更有效的视觉到动作路径。

## Abstract
While the shortage of explicit action data limits Vision-Language-Action (VLA) models, human action videos offer a scalable yet unlabeled data source. A critical challenge in utilizing large-scale human video datasets lies in transforming visual signals into ontology-independent representations, known as latent actions. However, the capacity of latent action representation to derive robust control from visual observations has yet to be rigorously evaluated. We introduce the Latent Action Representation Yielding (LARY) Benchmark, a unified framework for evaluating latent action representations on both high-level semantic actions (what to do) and low-level robotic control (how to do). The comprehensively curated dataset encompasses over one million videos (1,000 hours) spanning 151 action categories, alongside 620K image pairs and 595K motion trajectories across diverse embodiments and environments. Our experiments reveal two crucial insights: (i) General visual foundation models, trained without any action supervision, consistently outperform specialized embodied latent action models. (ii) Latent-based visual space is fundamentally better aligned to physical action space than pixel-based space. These results suggest that general visual representations inherently encode action-relevant knowledge for physical control, and that semantic-level abstraction serves as a fundamentally more effective pathway from vision to action than pixel-level reconstruction.

---

## 论文详细总结（自动生成）

### LARY：一个用于可泛化视觉到动作对齐的潜在动作表示生成基准

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能领域面临“数据孤岛”问题：带动作标签的机器人数据稀缺，而无标签的人类动作视频规模巨大。为了利用这些视频，研究者提出了“潜在动作（Latent Action）”概念，即将视频帧间的视觉变化转化为动作表示。然而，目前缺乏一个严格、统一的框架来评估这些潜在动作表示的质量。现有的评估多依赖于下游任务表现或定性聚类，无法解耦表示质量与策略学习的影响。LARY 基准旨在填补这一空白，通过评估“做什么（语义）”和“怎么做（控制）”两个维度，标准化衡量视觉到动作转换的有效性。

#### 2. 方法论：核心思想与技术细节
LARY 提出了一个双层评估框架：
*   **高层语义探测协议（Hierarchical Semantic Probing）**：
    *   **运动级原子原语（Atomic Primitives）**：将机器人末端执行器的动作分解为 28 类离散运动（如向上移动、关闭夹爪）。
    *   **任务级复合行为（Composite Behaviors）**：涵盖 145 类复杂行为（如抓取、放置、搅拌），跨越人类和机器人多种具身。
*   **底层物理执行映射评估（Physical Execution Mapping）**：
    *   通过回归任务测试潜在空间是否保留了足够的物理细节，以重建末端执行器的连续运动轨迹（7-DoF 到 16-DoF）。
*   **自动化数据引擎**：利用视觉语言模型（如 Doubao-1.5-pro-vision）对大规模异构视频数据集进行自动时间分割、语义对齐和过滤，确保动作标签的一致性。

#### 3. 实验设计：数据集、Benchmark 与对比方法
*   **数据集规模**：包含超过 120 万个短视频（1000+ 小时）、62 万个图像对和 59.5 万条运动轨迹，涵盖 151 个动作类别和 11 种机器人平台。
*   **数据来源**：整合了 LIBERO、CALVIN、VLABench、RoboCOIN、AgiBotWorld-Beta 等机器人数据，以及 Ego4D、SSv2、EPIC-KITCHENS 等人类动作数据。
*   **对比方法（11 种模型）**：
    *   **具身 LAMs**：LAPA、UniVLA、villa-X。
    *   **通用语义编码器**：DINOv3、V-JEPA 2。
    *   **生成式像素编码器**：Wan2.2 VAE、FLUX.2-dev VAE。
    *   **通用 LAMs（本文提出）**：将 LAPA 框架嫁接到冻结的通用视觉骨干（如 DINOv2/v3、SigLIP 2）上。

#### 4. 资源与算力
*   **算力设备**：使用了 8 台 NVIDIA H800 (141GB) GPU。
*   **训练细节**：通用 LAM 变体在超过 200 万帧的内部视频数据集上训练，训练步数为 100,000 步。
*   **输入规格**：图像分辨率统一为 224 × 224。

#### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **主基准测试**：在原子动作分类、复合动作分类和动作回归三个任务上对比了 11 种模型。
*   **消融实验**：针对量化瓶颈（Codebook 大小、序列长度、潜在维度）进行了详细测试。
*   **误差分析**：分析了长尾分布下的表现、时空接地（Attention 可视化）以及采样步长（Stride）对轨迹鲁棒性的影响。
*   **客观性**：实验覆盖了多种具身（单臂、双臂、类人）和多种视角（第一人称、第三人称），评估指标（Accuracy, MSE）客观且具有代表性。

#### 6. 主要结论与发现
*   **通用视觉模型更强**：未经任何动作监督训练的通用视觉基础模型（如 V-JEPA 2, DINOv3）在动作表示上始终优于专门的具身潜在动作模型。
*   **潜空间优于像素空间**：基于特征潜空间的视觉表示比基于像素的表示更契合物理动作空间，语义抽象是视觉到动作转换的更有效路径。
*   **LAM 的关键要素**：有效的潜在动作空间需要强大的自监督视觉先验和严格正则化的量化瓶颈（以维持稳定性和高利用率）。
*   **时空接地能力**：自监督模型在物理交互点的定位（Attention）上远比生成式模型精准。

#### 7. 优点与亮点
*   **解耦评估**：首次将潜在动作表示的质量评估与下游策略性能解耦，提供了直接的衡量标准。
*   **规模与多样性**：构建了目前已知规模最大、具身多样性最丰富的潜在动作评估数据集。
*   **自动化流程**：开发的自动化数据引擎解决了具身数据标注不一致、边界模糊的痛点，具有很强的扩展性。
*   **深刻见解**：挑战了“必须在机器人数据上训练动作空间”的传统认知，强调了通用视觉预训练的价值。

#### 8. 不足与局限
*   **机器人动作性能差距**：虽然通用 LAM 在人类动作上表现优异，但在特定机器人动作上的表现仍受限于训练数据的规模和多样性。
*   **精细操作缺失**：为了简化问题，实验中屏蔽了灵巧手的手指关节数据，主要关注末端执行器的宏观位移，对极精细操作的评估不足。
*   **API 依赖**：数据清洗高度依赖特定的 VLM API，可能引入模型固有的偏见或识别错误。

（完）

---
title: "MoRI: Mixture of RL and IL Experts for Long-Horizon Manipulation Tasks"
title_zh: MoRI：面向长程操作任务的强化学习与模仿学习专家混合模型
authors: "Yaohang Xu, Lianjie Ma, Gewei Zuo, Wentao Zhang, Han Ding, Lijun Zhu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10165v1"
tags: ["query:q2"]
score: 9.0
evidence: 强化学习与模仿学习混合用于长程操作任务
tldr: 针对长程机器人操纵任务中模仿学习（IL）易产生累积误差和强化学习（RL）采样效率低的问题，本文提出MoRI框架。该方法通过动作方差动态切换IL与RL专家，结合离线预训练与在线微调，并引入IL正则化以减少人工干预。实验表明，MoRI在复杂任务中实现了极高的成功率，显著提升了收敛速度并大幅降低了人工干预成本。
selection_source: fresh_fetch
motivation: 旨在解决IL在长程任务中的分布偏移问题以及RL在复杂操纵中采样效率低下且需要频繁人工干预的挑战。
method: 提出一种混合专家系统，根据专家动作方差动态切换IL和RL，并结合离线预训练、在线微调及IL正则化约束。
result: "在四项真实世界任务中，MoRI在2-5小时内达到97.5%的平均成功率，减少了85.8%的人工干预并缩短了21%的收敛时间。"
conclusion: MoRI通过动态融合IL与RL的优势，为实现高效、安全且低干预的复杂机器人操纵任务提供了一种有效的解决方案。
---

## 摘要
强化学习 (RL) 和模仿学习 (IL) 是机器人操作中策略获取的标准框架。虽然 IL 能够高效地推导策略，但它面临着复合误差和分布偏移的问题。相反，RL 有助于自主探索，但往往受限于低样本效率和高昂的试错成本。由于现有的混合方法在处理复杂任务时通常表现不佳，我们提出了 MoRI（强化学习与模仿学习专家混合模型）。该系统根据专家动作的方差在 IL 和 RL 专家之间进行动态切换，以处理粗略运动和精细操作。MoRI 采用离线预训练阶段，随后进行在线微调以加速收敛。为了保持探索安全性并最大限度地减少人工干预，该系统对 RL 组件应用了基于 IL 的正则化。在四个复杂的真实世界任务中的评估表明，MoRI 在 2 到 5 小时的微调内实现了 97.5% 的平均成功率。与基准 RL 算法相比，MoRI 减少了 85.8% 的人工干预，并将收敛时间缩短了 21%，证明了其在机器人操作中的能力。

## Abstract
Reinforcement Learning (RL) and Imitation Learning (IL) are the standard frameworks for policy acquisition in manipulation. While IL offers efficient policy derivation, it suffers from compounding errors and distribution shift. Conversely, RL facilitates autonomous exploration but is frequently hindered by low sample efficiency and the high cost of trial and error. Since existing hybrid methods often struggle with complex tasks, we introduce Mixture of RL and IL Experts (MoRI). This system dynamically switches between IL and RL experts based on the variance of expert actions to handle coarse movements and fine-grained manipulations. MoRI employs an offline pre-training stage followed by online fine-tuning to accelerate convergence. To maintain exploration safety and minimize human intervention, the system applies IL-based regularization to the RL component. Evaluation across four complex real-world tasks shows that MoRI achieves an average success rate of 97.5% within 2 to 5 hours of fine-tuning. Compared to baseline RL algorithms, MoRI reduces human intervention by 85.8% and shortens convergence time by 21%, demonstrating its capability in robotic manipulation.

---

## 论文详细总结（自动生成）

### MoRI：面向长程操作任务的强化学习与模仿学习专家混合模型 论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在机器人操纵领域，长程任务（Long-Horizon Tasks）通常包含粗粒度的运动（如接近物体）和精细的接触操作（如精准插入）。
*   **模仿学习（IL）**：虽然能高效获取基础策略，但面临累积误差和分布偏移问题，难以应对高精度要求。
*   **强化学习（RL）**：通过试错优化，鲁棒性强，但采样效率低，且在真实世界中试错成本极高。
*   **核心挑战**：现有的混合方法在处理复杂长程任务时，往往难以平衡效率、精度和泛化性。
*   **研究动机**：提出一种能够动态协调 IL 和 RL 优势的框架，以实现快速收敛、高成功率并显著减少人工干预。

#### 2. 论文提出的方法论：MoRI 框架
MoRI 采用**混合专家（Mixture of Experts, MoE）**架构，核心思想是根据专家动作的方差动态切换 IL 和 RL 专家。
*   **核心思想**：利用 IL 处理确定性较高的粗略运动，利用 RL 处理不确定性较高、需精细调整的接触任务。
*   **关键技术细节**：
    *   **两阶段流程**：
        1.  **阶段 I：离线预训练**。使用少量演示数据（约20条），通过行为克隆（BC）初始化 IL 专家，通过优势加权策略优化（AWAC）初始化 RL 专家。
        2.  **阶段 II：在线微调**。在真实机器人上运行，通过门控网络（Gating Network）动态选择专家。
    *   **动态调度机制**：门控网络基于动作方差进行硬切换（Hard Selection）。当 IL 专家方差较低（动作确定）时执行 IL；当进入复杂接触阶段，RL 专家凭借 Q 值引导展现出更好的适应性时，切换至 RL。
    *   **IL 正则化**：在 RL 的目标函数中加入 IL 策略的 KL 散度约束，确保 RL 的探索范围在合理区域内，提高安全性并减少人工干预。
    *   **离散夹爪控制（DBC）**：专门训练一个离散的 BC 专家负责夹爪动作，避免了标准 RL 在处理离散动作空间时收敛慢的问题。

#### 3. 实验设计
*   **实验场景**：在 Franka Research 3 机械臂上设置了四个复杂的真实世界任务：
    1.  **抽屉放置**：打开抽屉、抓取方块放入、关闭抽屉。
    2.  **盒装毛巾**：掀开盒盖、放入毛巾、盖回盒盖。
    3.  **双插座插入**：将两脚和三脚插头分别插入精确的插槽。
    4.  **毛巾双折叠**：通过多点接触力控完成毛巾的两次折叠。
*   **Benchmark 与对比方法**：
    *   **ConRFT**：一种基于一致性策略的 VLA 模型强化微调方法。
    *   **独立专家对比**：分别对比了纯 BC（IL）和纯 RL 的表现。
    *   **消融实验**：验证了 BC 正则化和离散夹爪专家（DBC）的有效性。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090 GPU** 进行训练。
*   **训练时长**：每个任务的在线微调时间在 **2 到 5 小时**之间（平均约 216 分钟）。
*   **数据量**：仅需 20 条人类演示数据进行初始化。

#### 5. 实验数量与充分性
*   **实验规模**：在 4 个不同的真实物理任务上进行了测试，每个任务在评估阶段进行了 20 次独立实验。
*   **充分性**：
    *   对比了最先进的基准算法（ConRFT）。
    *   进行了详细的消融实验（针对正则化项、夹爪控制模型等）。
    *   分析了专家切换的平滑度（位置偏移量）和 RL 专家调用比例的演变。
*   **客观性**：实验涵盖了从刚性物体操作到柔性物体折叠的不同维度，评估指标包括成功率、训练时间、人工干预频率和路径长度，评价较为全面。

#### 6. 论文的主要结论与发现
*   **高性能**：MoRI 在四项任务中达到了 **97.5%** 的平均成功率。
*   **高效率**：相比基准 RL 算法，收敛速度提升了 **21%**。
*   **低干预**：人工干预次数减少了 **85.8%**，自主成功样本量增加了 **111.7%**。
*   **专家分工**：实验证实了门控机制能有效分配任务——BC 负责固定的轨迹（如开门），RL 负责随机性强的精细操作（如对准插入）。

#### 7. 优点
*   **动态融合**：不同于简单的残差学习或顺序预训练，MoRI 实现了 IL 和 RL 的动态、硬切换融合，充分利用了两者的互补性。
*   **实用性强**：在有限的时间（几小时内）和极少的人工干预下，即可在真实物理环境中完成长程复杂任务。
*   **动作平滑**：通过 IL 正则化解决了异构专家切换时的动作跳变问题，保证了机械臂运动的连续性。

#### 8. 不足与局限
*   **泛化性限制**：虽然在任务内具有鲁棒性，但在面对剧烈的环境扰动或从未见过的物体时，泛化能力仍有待验证。
*   **自主恢复能力**：目前系统在发生严重错误时仍可能需要人工干预，缺乏完全自主的错误恢复逻辑。
*   **高层推理缺失**：该模型主要关注底层控制，未集成视觉语言模型（VLM）等高层推理能力，难以处理需要复杂逻辑理解的任务。

（完）

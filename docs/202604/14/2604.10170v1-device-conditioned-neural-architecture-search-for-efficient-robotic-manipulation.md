---
title: Device-Conditioned Neural Architecture Search for Efficient Robotic Manipulation
title_zh: 面向高效机器人操控的设备感知神经架构搜索
authors: "Yiming Wu, Huan Wang, Zhenghao Chen, Ge Yuan, Dong Xu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10170v1"
tags: ["query:q2"]
score: 9.0
evidence: 通过硬件约束架构搜索实现高效机器人操作
tldr: 针对机器人视觉运动策略在异构硬件上部署难、优化慢的问题，本文提出DC-QFA框架。通过构建包含多种架构和混合精度位宽的超网络，并结合硬件感知的正则化与多步在线蒸馏技术，实现了无需重复训练的快速跨平台部署。实验证明该方法在多种主流策略上可实现2-3倍加速，且在真实机器人任务中保持了极高的稳定性。
selection_source: fresh_fetch
motivation: 现有的机器人模型优化方法通常针对特定设备且耗时较长，难以适应多样化的硬件约束和快速部署需求。
method: 提出DC-QFA框架，利用包含架构与量化位宽的超网络进行一次性训练，并通过硬件查找表指导的轻量化搜索和多步在线蒸馏来优化子网络。
result: 在边缘设备和云端GPU上实现了2-3倍的推理加速，且在DiffusionPolicy等多个模型上保持了几乎无损的任务成功率。
conclusion: DC-QFA为高效机器人操作提供了一种通用的部署方案，在显著提升推理效率的同时确保了闭环执行的稳定性。
---

## 摘要
视觉运动策略日益增长的复杂性给在异构机器人硬件约束下的部署带来了重大挑战。然而，大多数现有的机器人操控模型高效方法都是特定于设备和模型的，缺乏泛化性，且在适配过程中需要耗时的逐设备优化。在这项工作中，我们提出了一个名为 Device-Conditioned Quantization-For-All (DC-QFA) 的统一框架，通过设备感知的量化感知训练和硬件约束的架构搜索来分摊部署成本。具体而言，我们引入了一个单一的超网络，其设计空间涵盖了丰富的网络架构和混合精度位宽。该网络在逐设备查找表的指导下，通过延迟和内存感知的正则化进行优化。利用该超网络，针对每个目标平台，我们可以进行一次性的轻量级搜索以选择最优子网络，而无需任何逐设备重新优化，从而实现了在异构硬件上更具泛化性的部署，并显著缩短了部署时间。为了提高低精度下的长程稳定性，我们进一步引入了多步同策略蒸馏，以减轻闭环执行过程中的误差累积。在 DiffusionPolicy-T、MDT-V 和 OpenVLA-OFT 等三个代表性策略骨干网络上的广泛实验表明，我们的 DC-QFA 在边缘设备、消费级 GPU 和云平台上实现了 2-3 倍的加速，且任务成功率的性能下降微乎其微。在配备力/力矩传感器的 Inovo 机器人上进行的真实世界评估进一步验证了，即使在剧烈量化下，我们的低比特 DC-QFA 策略也能保持稳定且接触丰富的操控。

## Abstract
The growing complexity of visuomotor policies poses significant challenges for deployment with heterogeneous robotic hardware constraints. However, most existing model-efficient approaches for robotic manipulation are device- and model-specific, lack generalizability, and require time-consuming per-device optimization during the adaptation process. In this work, we propose a unified framework named \textbf{D}evice-\textbf{C}onditioned \textbf{Q}uantization-\textbf{F}or-\textbf{A}ll (DC-QFA) which amortizes deployment effort with the device-conditioned quantization-aware training and hardware-constrained architecture search. Specifically, we introduce a single supernet that spans a rich design space over network architectures and mixed-precision bit-widths. It is optimized with latency- and memory-aware regularization, guided by per-device lookup tables. With this supernet, for each target platform, we can perform a once-for-all lightweight search to select an optimal subnet without any per-device re-optimization, which enables more generalizable deployment across heterogeneous hardware, and substantially reduces deployment time. To improve long-horizon stability under low precision, we further introduce multi-step on-policy distillation to mitigate error accumulation during closed-loop execution. Extensive experiments on three representative policy backbones, such as DiffusionPolicy-T, MDT-V, and OpenVLA-OFT, demonstrate that our DC-QFA achieves $2\text{-}3\times$ acceleration on edge devices, consumer-grade GPUs, and cloud platforms, with negligible performance drop in task success. Real-world evaluations on an Inovo robot equipped with a force/torque sensor further validates that our low-bit DC-QFA policies maintain stable, contact-rich manipulation even under severe quantization.

---

## 论文详细总结（自动生成）

以下是对论文《Device-Conditioned Neural Architecture Search for Efficient Robotic Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着基于 Transformer 的视觉运动策略（Visuomotor Policies）在机器人操控领域取得显著进展，其巨大的计算量和内存消耗成为了在资源受限的边缘设备（如嵌入式 GPU、NPU）上部署的主要障碍。
*   **现有挑战**：目前的模型压缩技术（如剪枝、量化）通常是“设备特定”且“模型特定”的。这意味着每当更换部署平台（从云端 GPU 到边缘 Jetson）时，都需要重新进行耗时的优化或微调。
*   **研究目标**：提出一种“一次训练，到处部署”的通用框架，使单一模型能够自适应不同硬件的延迟和内存约束，同时解决低比特量化在长程任务中容易产生的误差累积问题。

### 2. 方法论：核心思想与关键技术
论文提出了 **DC-QFA (Device-Conditioned Quantization-For-All)** 框架，主要包含以下三个核心组件：
*   **量化感知超网络（Quantized Supernet）**：构建一个包含多种架构配置（如 MLP 比例、注意力头比例、块选择）和多种量化位宽（INT4, INT8, FP4, FP8, FP16）的设计空间。通过权重共享，训练一个单一的超网络，使其能够涵盖所有可能的子网络配置。
*   **硬件感知正则化（Hardware-aware Regularization）**：在训练目标函数中引入拉格朗日松弛法，加入基于真实设备查找表（LUT）的延迟和内存约束项。这使得超网络在训练过程中能够感知不同硬件的资源限制，从而优化出更具硬件友好性的子网络。
*   **多步在线蒸馏（Multi-step On-Policy Distillation, OPD）**：针对量化模型在闭环执行中误差随时间累积的问题，该方法让量化后的“学生”模型在自己生成的轨迹上运行，并由全精度的“教师”模型提供每一步的动作监督。通过逐渐扩大训练时界（Horizon），显著提升了长程任务的稳定性。
*   **搜索流程**：训练完成后，针对特定目标硬件，使用 **NSGA-II** 多目标搜索算法，在不重新训练的情况下，快速从超网络中筛选出满足功耗、延迟约束的最优子网络。

### 3. 实验设计
*   **仿真基准（Benchmarks）**：
    *   **Push-T**：平面推移任务，评估任务成功率。
    *   **CALVIN**：长程语言指令操控任务，评估平均完成步骤数。
    *   **LIBERO**：包含 130 个任务的多任务操控基准，评估平均成功率。
*   **策略骨干（Backbones）**：DiffusionPolicy-T（扩散策略）、MDT-V（多模态扩散 Transformer）、OpenVLA-OFT（视觉-语言-动作模型）。
*   **对比方法**：
    *   **PTQ（后量化）**：SmoothQuant, AWQ。
    *   **QAT（量化感知训练）**：SQIL。
    *   **QFA（通用量化）**：BatchQuant。
*   **真实世界验证**：在 Inovo 6自由度机械臂上进行，配备力/力矩传感器，执行扫豆子、抓鸡蛋、插辣椒等接触密集型任务。

### 4. 资源与算力
论文明确提到了训练超网络所需的算力成本：
*   **DiffusionPolicy-T**：训练 DC-QFA 超网络约需 **85 GPU 小时**。
*   **MDT-V**：训练约需 **156 GPU 小时**。
*   **部署设备**：主要在 **NVIDIA Jetson Orin NX** 边缘设备上进行实测，同时也涵盖了消费级 GPU 和云端平台。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了跨架构（3种）、跨基准（3个仿真+1个真实场景）的广泛实验。
*   **消融实验**：专门针对 OPD（在线蒸馏）模块在长程任务（CALVIN）中的作用进行了对比，验证了其缓解误差累积的有效性。
*   **客观性**：通过 Pareto 前沿分析展示了模型在“性能 vs 参数量”上的权衡，对比了固定位宽与混合位宽的效果。实验涵盖了从 4-bit 到 16-bit 的多种配置，对比基准线清晰，实验设计较为充分且客观。

### 6. 主要结论与发现
*   **高效加速**：DC-QFA 在边缘设备上实现了 **2-3倍的推理加速**，并减少了 **2-4倍的内存占用**。
*   **性能保持**：在 W4A4（4比特权重量化/4比特激活量化）的极端压缩下，任务成功率的下降微乎其微，显著优于传统的 PTQ 方法。
*   **长程稳定性**：通过 OPD 技术，量化模型在长程任务中的表现接近全精度模型，解决了低比特模型在闭环控制中容易“跑偏”的问题。
*   **跨平台通用性**：一次训练后的超网络可以迅速适配到不同的硬件，极大地降低了部署成本。

### 7. 优点（亮点）
*   **部署效率高**：解决了机器人领域“一机一调”的痛点，通过 NAS 实现了自动化的跨平台适配。
*   **稳定性增强**：创新性地引入多步在线蒸馏，专门针对机器人闭环控制的特性（误差累积）进行优化。
*   **软硬结合**：利用真实硬件的 LUT 指导训练，使模型优化目标与实际部署表现高度对齐。

### 8. 不足与局限
*   **训练开销**：虽然部署阶段很快，但超网络的初始训练（Supernet Training）阶段仍然需要较高的算力投入（如 156 GPU 小时）。
*   **模型规模限制**：实验主要集中在数亿参数规模的模型，对于参数量达到数十亿甚至百亿级的超大型 VLA 模型（如完整的 OpenVLA），其搜索空间和训练稳定性尚未得到充分验证。
*   **编译器依赖**：性能提升高度依赖于底层推理引擎（如 TensorRT）对低比特算子的支持，在某些不支持特定位宽的硬件上可能无法发挥全部潜力。

（完）

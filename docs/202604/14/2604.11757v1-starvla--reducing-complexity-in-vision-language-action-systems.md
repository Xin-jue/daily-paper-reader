---
title: "StarVLA-$α$: Reducing Complexity in Vision-Language-Action Systems"
title_zh: StarVLA-α：降低视觉-语言-动作系统的复杂性
authors: "Jinhui Ye, Ning Gao, Senqiao Yang, Jinliang Zheng, Zixuan Wang, Yuxin Chen, Pengguang Chen, Yilun Chen, Shu Liu, Jiaya Jia"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11757v1"
tags: ["query:q12"]
score: 10.0
evidence: 研究VLA设计选择与架构的基准模型
tldr: "StarVLA-α 针对当前视觉-语言-动作（VLA）模型架构复杂且碎片化的问题，提出了一个极简且强大的基准模型。通过在受控条件下重新评估动作建模、预训练和接口工程等关键设计，该研究证明了仅依靠强大的 VLM 主干和最简设计即可在 LIBERO、SimplerEnv 等多个基准测试中取得优异性能。在真实世界 RoboChallenge 测试中，其表现优于 π0.5 达 20%，为 VLA 研究提供了简洁高效的新起点。"
selection_source: fresh_fetch
motivation: 现有的 VLA 模型架构和训练流程过于复杂且碎片化，导致难以系统分析不同设计选择对性能的实际影响。
method: 提出 StarVLA-α 基准，通过最小化架构复杂度和消除工程技巧，在统一的多基准数据集上重新评估动作建模与预训练策略。
result: "该模型在 LIBERO、SimplerEnv 等多个仿真环境中表现强劲，并在真实世界 RoboChallenge 基准测试中比 π0.5 性能提升了 20%。"
conclusion: 强大的 VLM 主干结合极简设计足以实现高性能 VLA，无需依赖复杂的架构或额外的工程技巧。
---

## 摘要
视觉-语言-动作 (VLA) 模型近期已成为构建通用机器人智能体的一种极具前景的范式。然而，VLA 领域仍然高度碎片化且复杂：现有方法在架构、训练数据、具身配置以及特定基准测试的工程设计方面存在显著差异。在这项工作中，我们介绍了 StarVLA-α，这是一个简单而强大的基准模型，旨在受控条件下研究 VLA 的设计选择。StarVLA-α 刻意最小化了架构和流水线的复杂性，以减少实验干扰因素并实现系统性分析。具体而言，我们重新评估了几个关键的设计维度，包括动作建模策略、机器人特定预训练和接口工程。通过在 LIBERO、SimplerEnv、RoboTwin 和 RoboCasa 上进行的统一多基准训练，这一简单的基准模型保持了极强的竞争力，这表明强大的 VLM 骨干网络结合极简设计，已足以在不依赖额外架构复杂性或工程技巧的情况下实现卓越性能。值得注意的是，我们的单个通用模型在公开的真实世界 RoboChallenge 基准测试中比 π₀.₅ 高出 20%。我们期望 StarVLA-α 能为 VLA 领域的未来研究提供一个坚实的起点。代码将发布于 https://github.com/starVLA/starVLA。

## Abstract
Vision-Language-Action (VLA) models have recently emerged as a promising paradigm for building general-purpose robotic agents. However, the VLA landscape remains highly fragmented and complex: as existing approaches vary substantially in architectures, training data, embodiment configurations, and benchmark-specific engineering. In this work, we introduce StarVLA-$α$, a simple yet strong baseline designed to study VLA design choices under controlled conditions. StarVLA-$α$ deliberately minimizes architectural and pipeline complexity to reduce experimental confounders and enable systematic analysis. Specifically, we re-evaluate several key design axes, including action modeling strategies, robot-specific pretraining, and interface engineering. Across unified multi-benchmark training on LIBERO, SimplerEnv, RoboTwin, and RoboCasa, the same simple baseline remains highly competitive, indicating that a strong VLM backbone combined with minimal design is already sufficient to achieve strong performance without relying on additional architectural complexity or engineering tricks. Notably, our single generalist model outperforms $π_{0.5}$ by 20\% on the public real-world RoboChallenge benchmark. We expect StarVLA-$α$ to serve as a solid starting point for future research in the VLA regime. Code will be released at https://github.com/starVLA/starVLA.

---

## 论文详细总结（自动生成）

这篇论文对当前视觉-语言-动作（VLA）系统进行了深入的“去繁就简”研究。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：VLA模型正成为通用机器人智能体的核心，但该领域目前高度碎片化。不同研究在模型架构（如扩散模型 vs. 自回归）、预训练数据、机器人配置以及特定基准测试的工程技巧上差异巨大。
*   **核心问题**：这种复杂性使得研究者难以辨别究竟是哪种设计真正驱动了性能提升。
*   **研究目的**：通过提出一个极简基准模型 **StarVLA-α**，在受控实验条件下重新评估各项设计选择，旨在消除干扰因素，建立VLA设计的共识。

### 2. 方法论：核心思想与技术细节
*   **核心思想（最小充分性假设）**：一个强大的预训练视觉-语言模型（VLM）骨干，配合最轻量级的动作头和最少的数据工程，足以达到甚至超越现有复杂系统的性能。
*   **关键技术细节**：
    *   **骨干网络**：采用 **Qwen3-VL** 系列模型。其优势在于原生支持多模态输入，无需额外组合视觉编码器（如CLIP或SigLIP）。
    *   **动作头（Action Head）**：默认使用简单的 **MLP（多层感知机）**。它读取特定动作Token的隐藏状态，直接回归连续的动作块（Action Chunk）。
    *   **极简数据处理**：仅输入原始RGB图像和语言指令。不使用复杂的历史帧堆叠或本体状态输入。动作仅进行基础的标准化（零均值、单位方差）。
    *   **统一接口**：通过简单的“动作填充（Simple Padding）”策略处理不同机器人的自由度差异，将所有动作向量统一扩展至32维，实现跨基准的通用训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：LIBERO（多任务学习）、SimplerEnv（真实策略仿真评估）、RoboTwin 2.0（双臂协作）、RoboCasa-GR1（人形机器人桌面操作）。
    *   **真实世界**：RoboChallenge（标准化真实机器人基准）以及额外的OOD（分布外）泛化实验。
*   **对比方法**：包括 OpenVLA-OFT、$\pi_0$、$\pi_{0.5}$、GR00T-N1.6、FAST 等当前最先进的 VLA 模型。
*   **实验设置**：分为“专家模型（Specialist）”训练（针对单一基准）和“通用模型（Generalist）”训练（所有基准联合训练）。

### 4. 资源与算力
论文在附录C中详细列出了算力消耗：
*   **GPU型号**：NVIDIA **A100**。
*   **算力分配**：
    *   单基准训练：使用 8 到 48 张 A100 不等。
    *   全基准联合训练（Generalist）：动用了 **64 张 A100**。
*   **训练时长**：最大训练步数为 100k 步，每张 GPU 的 Batch Size 为 16。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了 4 个主流仿真基准（包含数百个子任务）和 1 个真实机器人基准。
*   **消融实验**：针对动作头设计（离散 vs. 连续 vs. 扩散）、机器人特定预训练的影响、数据工程（本体状态、历史帧、增量动作等）、模型规模（2B/4B/8B）以及 Batch Size 进行了系统性消融。
*   **充分性与公平性**：实验在统一的骨干网络和训练流程下进行，有效隔离了变量，对比过程客观且具有说服力。

### 6. 主要结论与发现
*   **架构不重要，骨干才重要**：在拥有强大 VLM 骨干的前提下，复杂的动作头（如扩散模型或自回归 Token）相比简单的 MLP 并没有显著优势。
*   **预训练是双刃剑**：额外的机器人数据预训练（如 OXE 数据集）并不总是有效，甚至可能损害跨具身的泛化能力，除非预训练数据与目标任务高度对齐。
*   **工程技巧的边际效应**：本体状态输入和历史帧在数据量极小时有微弱帮助，但随着训练数据增加，其收益几乎消失。
*   **规模效应**：Batch Size 对通用模型的性能至关重要；模型规模在达到 4B 后收益开始放缓。

### 7. 优点（亮点）
*   **简洁性**：证明了“大道至简”，挑战了当前 VLA 领域追求复杂架构的趋势。
*   **通用性**：一个模型、一套参数、无需微调即可在单臂、双臂、人形机器人等多种形态上运行。
*   **实战能力**：在真实世界 RoboChallenge 基准上显著优于 $\pi_{0.5}$（成功率提升 20% 以上），证明了其方案的实用价值。

### 8. 不足与局限
*   **依赖强骨干**：StarVLA-α 的成功高度依赖于 Qwen3-VL 强大的多模态理解能力，若骨干网络较弱，极简设计可能失效。
*   **长程任务挑战**：虽然在多个基准上领先，但在极复杂的长程逻辑任务（如 RoboCasa 中的某些任务）上，成功率仍有提升空间。
*   **实时性讨论较少**：论文重点在于性能和简洁性，对于超高频控制下的推理延迟和实时性表现缺乏深入的定量分析。

（完）

---
title: "DA-PTQ: Drift-Aware Post-Training Quantization for Efficient Vision-Language-Action Models"
title_zh: DA-PTQ：面向高效视觉-语言-动作模型的漂移感知训练后量化
authors: "Siyuan Xu, Tianshi Wang, Fengling Li, Lei Zhu, Heng Tao Shen"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11572v1"
tags: ["query:q12"]
score: 9.0
evidence: 用于机器人在VLA模型高效部署的训练后量化技术
tldr: 针对视觉-语言-动作模型（VLA）在机器人部署中面临的显存和计算压力，本文提出了DA-PTQ量化框架。研究发现传统PTQ在序列控制中会产生时间误差累积，导致运动漂移。DA-PTQ通过跨空间表示补偿和运动驱动的混合精度分配，有效缓解了多模态表示与动作空间之间的失真，并在低比特设置下实现了接近全精度的性能，显著提升了VLA在资源受限平台上的部署效率。
selection_source: fresh_fetch
motivation: 传统量化方法在VLA序列控制中会产生时间误差累积，导致机器人执行轨迹出现严重的运动漂移。
method: 提出DA-PTQ框架，包含跨空间表示补偿以减少模态间失真，以及运动驱动的混合精度分配以优化轨迹误差。
result: 实验表明DA-PTQ显著降低了运动漂移，在低比特量化下达到了与全精度模型相当的性能。
conclusion: DA-PTQ为VLA模型在资源受限机器人平台上的高效部署提供了一种有效的后量化解决方案。
---

## 摘要
视觉-语言-动作模型 (VLA) 在具身智能领域展现出巨大潜力，但由于其高内存和计算需求，在资源受限的机器人上的部署仍然具有挑战性。虽然训练后量化 (PTQ) 提供了一种高效的解决方案，但直接将 PTQ 应用于 VLA 往往会导致在序列控制过程中出现严重的性能下降。我们发现时间误差累积是一个关键因素，即视觉-语言到动作接口处的量化扰动被逐渐放大，导致执行轨迹中出现运动学漂移。为了解决这一问题，我们提出了漂移感知训练后量化 (DA-PTQ)，它将量化建模为序列决策过程中的漂移感知优化问题。DA-PTQ 由两个组件组成：(1) 跨空间表示补偿，旨在减轻多模态表示与动作空间之间的结构化失真，以提高动作一致性；(2) 运动驱动的混合精度分配，通过最小化轨迹级运动误差来分配位宽。大量实验表明，DA-PTQ 显著减少了运动学漂移，并在低比特设置下实现了与全精度模型相当的性能，从而使 VLA 在资源受限的机器人平台上的实际部署成为可能。

## Abstract
Vision-Language-Action models (VLAs) have demonstrated strong potential for embodied AI, yet their deployment on resource-limited robots remains challenging due to high memory and computational demands. While Post-Training Quantization (PTQ) provides an efficient solution, directly applying PTQ to VLAs often results in severe performance degradation during sequential control. We identify temporal error accumulation as a key factor, where quantization perturbations at the vision-language-to-action interface are progressively amplified, leading to kinematic drift in executed trajectories. To address this issue, we propose Drift-Aware Post-Training Quantization (DA-PTQ), which formulates quantization as a drift-aware optimization problem over sequential decision processes. DA-PTQ consists of two components: (1) Cross-Space Representation Compensation, which mitigates structured distortions between multimodal representations and action space to improve action consistency, and (2) Motion-Driven Mixed-Precision Allocation, which assigns bit-widths by minimizing trajectory-level motion errors. Extensive experiments show that DA-PTQ significantly reduces kinematic drift and achieves comparable performance to full-precision models under low-bit settings, enabling practical deployment of VLAs on resource-limited robotic platforms.

---

## 论文详细总结（自动生成）

这是一份关于论文《DA-PTQ: Drift-Aware Post-Training Quantization for Efficient Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作模型（VLA）参数量巨大（如 7B+），难以部署在资源受限的机器人平台上。虽然训练后量化（PTQ）能压缩模型，但直接应用会导致严重的**运动学漂移（Kinematic Drift）**。
*   **研究动机**：作者发现 VLA 在序列控制中存在“时间误差累积”效应。量化在感知-动作接口处引入的微小扰动，会随着时间推移被机器人动力学放大，导致执行轨迹偏离目标。现有的 PTQ 方法多关注静态重建误差，忽视了具身智能决策的序列性和控制敏感性。

### 2. 方法论
DA-PTQ 提出将量化视为一个**漂移感知（Drift-Aware）**的优化问题，包含两个核心组件：
*   **跨空间表示补偿（CSRC）**：
    *   **目的**：修正量化引起的感知-动作接口处的特征分布畸变。
    *   **技术细节**：通过校准集计算全精度（FP）与量化（Q）激活值的均值和方差，利用轻量级仿射变换（Affine Transformation）进行对齐。
    *   **优化**：引入低秩更新（Low-rank update）来捕捉跨通道的结构化失真，并将补偿参数在推理前合并到权重中，实现**零推理开销**。
*   **运动驱动的混合精度分配（DA-MPA）**：
    *   **核心思想**：并非所有层对轨迹漂移的影响都一致。
    *   **技术细节**：利用**结构化雅可比矩阵（Structural Jacobian）**作为代理，模拟误差如何沿运动链传播。通过计算各层对长程轨迹误差的贡献度（漂移敏感度分数），将最敏感的层（前 30%）保留为 BF16 高精度，其余层量化为 W4（4比特）。
*   **算法流程**：1. 漂移分析（收集统计量与雅可比矩阵）；2. 跨空间补偿（对齐分布并合并权重）；3. 混合精度分配（确定位宽映射）。

### 3. 实验设计
*   **基准测试（Benchmark）**：使用 **SimplerEnv** 机器人操作仿真环境。
*   **实验场景**：
    *   **WidowX 机器人**：评估域内（In-domain）性能，涵盖勺子放置、胡萝卜放置、方块堆叠等任务。
    *   **Google Robot**：评估跨域（Cross-domain）零样本泛化能力，包括视觉匹配（Visual Matching）和变体聚合（Variant Aggregation）设置。
*   **对比方法**：
    *   **CogACT (FP)**：全精度基准模型。
    *   **VLA-Cache**：NeurIPS'25 提出的推理加速方法（不量化权重）。
    *   **QuantVLA**：CVPR'26 提出的 VLA 量化基准方法。

### 4. 资源与算力
*   **硬件平台**：实验在 **NVIDIA RTX 5090 GPU** 上运行。
*   **算力消耗**：由于是训练后量化（PTQ），该方法**无需重新训练或微调**。校准过程仅需 512 条轨迹数据进行前向传播和轻量级梯度累积，计算成本极低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两种不同的机器人实体（WidowX 和 Google Robot），多个复杂的长程操作任务。
*   **消融实验**：专门针对 CSRC 和 DA-MPA 两个组件进行了拆解实验，验证了各自对成功率、内存减少和加速比的贡献。
*   **充分性评价**：实验设计较为充分，不仅对比了成功率，还量化了内存压缩比（约 42.5%）和推理加速比（约 54.8%），并提供了定性的轨迹曲线分析，证明了结果的客观性。

### 6. 主要结论与发现
*   **性能保持**：DA-PTQ 在 W4A8（4位权重，8位激活）设置下，成功率接近全精度模型，显著优于传统的均匀量化和现有的 VLA 量化方法。
*   **效率提升**：在保持高成功率的同时，实现了约 **42.5% 的显存节省**和 **54.8% 的推理加速**。
*   **漂移抑制**：通过雅可比矩阵引导的精度分配，有效抑制了连续控制中的高频振荡和运动漂移，使动作输出更加平滑。

### 7. 优点
*   **漂移感知视角**：首次深入分析并量化了 VLA 量化中的时间误差累积问题，提出了更符合机器人控制逻辑的优化目标。
*   **零推理成本**：补偿机制通过代数折叠进入权重，不增加实际部署时的计算负担。
*   **泛化性强**：在单一校准集上获得的量化参数，在跨机器人实体的零样本任务中表现稳健。

### 8. 不足与局限
*   **动力学简化**：雅可比矩阵的推导基于简化的运动链假设，对于高度非线性或存在复杂接触动力学（如多指灵巧手操作）的场景，其误差传播模型可能不够精确。
*   **校准集依赖**：虽然数据量小，但量化效果仍依赖于校准数据的分布，若实际环境与校准集存在极端领域偏移，性能可能受限。
*   **硬件适配**：论文主要在高端 GPU 上测试，未详细讨论在更低端的嵌入式计算单元（如 Jetson 系列）上的底层算子优化。

（完）

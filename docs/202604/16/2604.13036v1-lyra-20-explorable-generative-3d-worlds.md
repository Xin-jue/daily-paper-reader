---
title: "Lyra 2.0: Explorable Generative 3D Worlds"
title_zh: Lyra 2.0：可探索的生成式 3D 世界
authors: "Tianchang Shen, Sherwin Bahmani, Kai He, Sangeetha Grama Srinivasan, Tianshi Cao, Jiawei Ren, Ruilong Li, Zian Wang, Nicholas Sharp, Zan Gojcic, Sanja Fidler, Jiahui Huang, Huan Ling, Jun Gao, Xuanchi Ren"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.13036v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于实时渲染和仿真的生成式3D世界
tldr: Lyra 2.0 提出了一种生成大规模、可探索 3D 世界的新框架。针对视频生成中长路径导致的“空间遗忘”和“时间漂移”问题，该方法通过维护 3D 几何信息进行跨帧检索，并利用自增强历史训练来纠正累积误差。这使得模型能够生成长程且 3D 一致的视频轨迹，进而通过前馈重建技术恢复出高质量、可实时渲染的 3D 场景。
selection_source: fresh_fetch
motivation: 现有的生成式重建方法在处理长路径和大范围视角变化时，容易因空间遗忘和时间漂移导致场景失真和不一致。
method: 通过 3D 几何引导的信息路由检索历史帧以解决遗忘问题，并采用自增强历史训练策略来纠正自回归生成中的漂移误差。
result: 实现了更长且具备 3D 一致性的视频轨迹生成，显著提升了大规模复杂环境下的前馈 3D 重建质量。
conclusion: Lyra 2.0 有效结合了视频生成模型的创造力与 3D 重建的稳定性，为构建持久且可自由探索的虚拟世界提供了可行方案。
---

## 摘要
视频生成的最新进展为 3D 场景创建开启了一种新范式：生成受相机控制的、模拟场景漫游的视频，然后通过前馈重建技术将其提升至 3D。这种生成式重建方法结合了视频模型的视觉保真度与创作能力，以及可用于实时渲染和模拟的 3D 输出。扩展到大型复杂环境需要跨越长相机轨迹、大视角变化和位置重访的 3D 一致性视频生成，而目前的视频模型在这些场景下会迅速退化。现有的长时程生成方法从根本上受到两种退化形式的限制：空间遗忘和时间漂移。随着探索的进行，先前观察到的区域会超出模型的时间上下文，迫使模型在重访时幻觉出结构。同时，自回归生成会随时间累积微小的合成误差，逐渐扭曲场景的外观和几何结构。我们提出了 Lyra 2.0，这是一个用于大规模生成持久且可探索的 3D 世界的框架。为了解决空间遗忘问题，我们维护每帧的 3D 几何信息，并仅将其用于信息路由——检索相关的历史帧并建立与目标视角的密集对应关系——同时依靠生成先验进行外观合成。为了解决时间漂移问题，我们使用自增强历史进行训练，使模型暴露于其自身的退化输出中，从而教会它纠正漂移而非传播漂移。这些方法共同实现了显著更长且具有 3D 一致性的视频轨迹，我们利用这些轨迹微调前馈重建模型，从而可靠地恢复高质量的 3D 场景。

## Abstract
Recent advances in video generation enable a new paradigm for 3D scene creation: generating camera-controlled videos that simulate scene walkthroughs, then lifting them to 3D via feed-forward reconstruction techniques. This generative reconstruction approach combines the visual fidelity and creative capacity of video models with 3D outputs ready for real-time rendering and simulation. Scaling to large, complex environments requires 3D-consistent video generation over long camera trajectories with large viewpoint changes and location revisits, a setting where current video models degrade quickly. Existing methods for long-horizon generation are fundamentally limited by two forms of degradation: spatial forgetting and temporal drifting. As exploration proceeds, previously observed regions fall outside the model's temporal context, forcing the model to hallucinate structures when revisited. Meanwhile, autoregressive generation accumulates small synthesis errors over time, gradually distorting scene appearance and geometry. We present Lyra 2.0, a framework for generating persistent, explorable 3D worlds at scale. To address spatial forgetting, we maintain per-frame 3D geometry and use it solely for information routing -- retrieving relevant past frames and establishing dense correspondences with the target viewpoints -- while relying on the generative prior for appearance synthesis. To address temporal drifting, we train with self-augmented histories that expose the model to its own degraded outputs, teaching it to correct drift rather than propagate it. Together, these enable substantially longer and 3D-consistent video trajectories, which we leverage to fine-tune feed-forward reconstruction models that reliably recover high-quality 3D scenes.

---

## 论文详细总结（自动生成）

这是一份关于论文《Lyra 2.0: Explorable Generative 3D Worlds》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从单张图像生成大规模、长时程且具备 3D 一致性的可探索虚拟世界。
*   **研究背景**：当前的“生成式重建”范式利用视频扩散模型生成相机控制的漫游视频，再将其转化为 3D 模型。然而，在处理长路径和大范围探索时，现有模型面临两大退化挑战：
    *   **空间遗忘（Spatial Forgetting）**：当相机移动后重访旧区域时，由于旧帧超出了模型有限的上下文窗口，模型会重新“幻觉”出不一致的结构。
    *   **时间漂移（Temporal Drifting）**：自回归生成过程中微小的合成误差会随时间累积，导致颜色偏移和几何扭曲。

### 2. 方法论
Lyra 2.0 提出了一个自回归的“检索-生成-更新”循环框架，核心技术包括：
*   **抗遗忘机制（空间存储与路由）**：
    *   **3D 缓存**：维护每帧的独立深度图和点云，不进行全局融合以避免误差累积。
    *   **几何感知检索**：根据目标视角的可见性得分，从缓存中检索最相关的历史帧作为“空间插槽”。
    *   **规范坐标变形（Canonical Coordinate Warping）**：通过变形后的坐标图建立密集 3D 对应关系，引导模型进行外观合成，而非直接输入带伪影的变形图像。
*   **抗漂移机制（自增强训练）**：
    *   **FramePack 压缩**：对历史帧进行变长补丁化处理，在固定 Token 预算下扩展时间视野。
    *   **自增强训练（Self-Augmentation Training）**：在训练阶段随机让模型基于自身生成的、带噪声的预测结果进行去噪，从而模拟推理时的误差分布，教会模型自我纠偏。
*   **3D 重建流水线**：
    *   使用微调后的 **Depth Anything v3 (DAv3)** 进行前馈 3D 高斯泼溅（3DGS）重建。
    *   通过分层稀疏网格技术从 3DGS 中提取表面网格。
    *   **模型蒸馏**：利用 DMD 技术将 35 步去噪压缩至 4 步，实现交互级生成速度。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DL3DV（包含 10,000 个真实场景的长视频剪辑）。
    *   **测试集**：DL3DV-Evaluation（域内测试）和 Tanks and Temples（跨域泛化测试）。
*   **对比方法**：包括 GEN3C、Yume1.5、CaM、VMem、SPMem 和 HY-WorldPlay 等具备相机控制或存储机制的前沿模型。
*   **评估指标**：
    *   传统指标：SSIM, LPIPS, FID。
    *   长视频专项指标（WorldScore）：主观质量、风格一致性、相机控制力。
    *   3D 一致性指标：重投影误差、LPIPS-P（渲染图与生成帧的差异）。

### 4. 资源与算力
*   **硬件**：使用了 **64 台 NVIDIA GB200 GPU**。
*   **训练细节**：训练迭代次数为 7,000 次，Batch Size 为 64。
*   **推理效率**：全模型（35 步）在单台 GB200 上生成 80 帧需 194 秒；蒸馏后的 DMD 模型（4 步）仅需 **15 秒**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（涵盖 2 个数据集、7 种对比方法）和定性可视化。
*   **消融实验**：设计了 5 组消融实验，分别验证了全局点云融合、显式对应融合、FramePack 以及自增强训练的必要性。
*   **充分性评价**：实验设计较为全面，不仅涵盖了视频生成的质量，还通过 3D 重建后的渲染效果反向验证了视频的 3D 一致性。使用了主观评分和客观几何指标，评估维度客观、公平。

### 6. 主要结论与发现
*   **解耦是关键**：将 3D 几何用于“信息路由”而将外观合成留给“扩散先验”，能有效解决长程一致性问题。
*   **训练-推理一致性**：通过自增强训练暴露模型于自身误差中，是缓解自回归漂移的有效手段。
*   **性能领先**：Lyra 2.0 在长路径生成（如 800 帧以上）的视觉保真度和几何准确性上显著优于现有基准，生成的 3D 资产可直接用于 Isaac Sim 等仿真引擎。

### 7. 优点
*   **长程持久性**：成功解决了大规模场景探索中的重访一致性问题。
*   **高质量重建**：通过微调重建模型，弥合了生成视频与真实 3D 重建之间的领域差距。
*   **交互性强**：提供了实时交互 GUI，并支持蒸馏加速，具备极高的实用价值。
*   **系统性强**：从视频生成到 3DGS 再到表面网格提取，提供了一套完整的生产级方案。

### 8. 不足与局限
*   **静态限制**：目前仅专注于静态环境生成，无法处理动态物体（如行人、车辆）。
*   **光度不一致**：受限于训练数据（DL3DV），生成的视频可能存在曝光变化，导致 3D 重建中出现伪影。
*   **算力门槛**：训练依赖极其昂贵的 GB200 集群，普通研究机构难以复现。

（完）

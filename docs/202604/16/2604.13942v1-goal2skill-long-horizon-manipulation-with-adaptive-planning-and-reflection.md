---
title: "Goal2Skill: Long-Horizon Manipulation with Adaptive Planning and Reflection"
title_zh: Goal2Skill：具有自适应规划与反思能力的长程操纵
authors: "Zhen Liu, Xinyu Ning, Zhe Hu, Xinxin Xie, Weize Li, Zhipeng Tang, Chongyu Wang, Zejun Yang, Hanlin Wang, Yitong Liu, Zhongzhu Pu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13942v1"
tags: ["query:q10"]
score: 10.0
evidence: 具有自适应规划的长程操作 VLA 系统
tldr: 针对现有VLA系统在长程、依赖记忆的任务中表现脆弱的问题，本文提出了Goal2Skill框架。该框架采用双系统架构，将高层语义推理（VLM代理负责任务分解、验证与纠错）与底层运动执行（VLA控制器负责扩散式动作生成）解耦。通过构建规划与执行的闭环，实现了具备记忆感知、自适应重规划和在线恢复能力的具身操控，在RMBench基准测试中显著超越了现有基线模型。
selection_source: fresh_fetch
motivation: 现有的端到端VLA策略在处理具有部分可观测性、遮挡和多阶段依赖的长程任务时，缺乏持久记忆和显式的错误恢复能力。
method: 提出一种双系统框架，结合基于VLM的高层规划器进行任务分解与反思，以及基于扩散模型的底层执行器进行精确的视觉运动控制。
result: "在RMBench任务中，该框架将平均成功率从最强基线的9.8%大幅提升至32.4%。"
conclusion: 实验证明，结构化记忆和闭环恢复机制对于提升具身智能在复杂长程操控任务中的鲁棒性和成功率至关重要。
---

## 摘要
最近的视觉-语言-动作（VLA）系统在具身操纵方面展现出了强大的能力。然而，大多数现有的 VLA 策略依赖于有限的观测窗口和端到端的动作预测，这使得它们在具有部分可观测性、遮挡和多阶段依赖的长程、依赖记忆的任务中表现脆弱。此类任务不仅需要精确的视觉运动控制，还需要持久记忆、自适应任务分解以及从执行失败中显式恢复的能力。为了解决这些局限性，我们提出了一种用于长程具身操纵的双系统框架。该框架将高层语义推理与底层运动执行显式分离。高层规划器实现为基于视觉语言模型（VLM）的智能体模块，负责维护结构化任务记忆，并执行目标分解、结果验证和错误驱动的修正。底层执行器实例化为基于 VLA 的视觉运动控制器，通过以保持几何特征的过滤观测为条件的扩散动作生成来执行每个子任务。这两个系统共同构成了规划与执行之间的闭环，实现了记忆感知推理、自适应重新规划和鲁棒的在线恢复。在代表性 RMBench 任务上的实验表明，所提出的框架显著优于代表性基线模型，平均成功率达到 32.4%，而最强基线仅为 9.8%。消融研究进一步证实了结构化记忆和闭环恢复对于长程操纵的重要性。

## Abstract
Recent vision-language-action (VLA) systems have demonstrated strong capabilities in embodied manipulation. However, most existing VLA policies rely on limited observation windows and end-to-end action prediction, which makes them brittle in long-horizon, memory-dependent tasks with partial observability, occlusions, and multi-stage dependencies. Such tasks require not only precise visuomotor control, but also persistent memory, adaptive task decomposition, and explicit recovery from execution failures. To address these limitations, we propose a dual-system framework for long-horizon embodied manipulation.   Our framework explicitly separates high-level semantic reasoning from low-level motor execution. A high-level planner, implemented as a VLM-based agentic module, maintains structured task memory and performs goal decomposition, outcome verification, and error-driven correction. A low-level executor, instantiated as a VLA-based visuomotor controller, carries out each sub-task through diffusion-based action generation conditioned on geometry-preserving filtered observations. Together, the two systems form a closed loop between planning and execution, enabling memory-aware reasoning, adaptive replanning, and robust online recovery. Experiments on representative RMBench tasks show that the proposed framework substantially outperforms representative baselines, achieving a 32.4% average success rate compared with 9.8% for the strongest baseline. Ablation studies further confirm the importance of structured memory and closed-loop recovery for long-horizon manipulation.

---

## 论文详细总结（自动生成）

### 论文总结：Goal2Skill：具有自适应规划与反思能力的长程操纵

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型虽然在短程指令遵循和多任务控制上表现出色，但在处理**长程（Long-Horizon）操纵任务**时存在显著局限。这些任务通常具有部分可观测性、遮挡和复杂的多阶段依赖关系。
*   **研究动机**：纯反应式（Reactive）策略缺乏持久记忆，无法在执行偏离预期时进行自我诊断和纠错。
*   **核心目标**：构建一个能够像人类大脑（高层规划）和小脑（底层控制）协作一样的双系统框架，实现具备记忆感知、自适应重规划和在线恢复能力的具身智能。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Goal2Skill** 框架，将决策过程解耦为高层规划器和底层执行器：
*   **高层规划器（VLM-based Planner）**：
    *   **任务分解**：将全局目标 $G$ 分解为一系列结构化的子任务 $\tau_k$，每个子任务包含语义指令、预设条件、后置条件、空间约束和技能索引。
    *   **结构化记忆管理**：维护由情节历史（记录观测与动作）、工作记忆（环境状态的自然语言摘要）和错误寄存器（记录失败原因）组成的记忆状态 $M_t$。
    *   **验证与反思引擎**：在每个子任务结束后，通过 VLM 验证后置条件。若失败，反思引擎会诊断原因并决定是“重试”、“调整参数”还是“全局重规划”。
*   **底层执行器（VLA-based Executor）**：
    *   **几何导向感知**：利用高层提供的空间约束，通过掩码过滤掉无关的干扰物，使模型专注于任务相关的几何结构。
    *   **扩散式技能库**：基于扩散策略（Diffusion Policy）生成连续的运动指令，实现鲁棒的闭环视觉运动控制。

#### 3. 实验设计
*   **数据集与场景**：使用了 **RMBench**（记忆依赖型机器人操纵基准测试）。
*   **任务分类**：
    *   **M(1) 任务**：短程记忆依赖（如：观察并抓取、排列积木）。
    *   **M(n) 任务**：多阶段长程记忆依赖（如：电池安装尝试、积木排序尝试、按按钮）。
*   **对比方法**：对比了四种代表性基线：**DP**（扩散策略）、**ACT**（动作分块转换器）、**Pi0.5**（VLA 流模型）和 **X-VLA**（跨具身 VLA 模型）。
*   **评估指标**：任务成功率（SR），每个任务进行 100 次 Rollout 测试。

#### 4. 资源与算力
*   **训练细节**：模型针对每个任务使用了 **50 个专家演示**进行训练，总计进行了 **30k 次优化步数**。
*   **算力说明**：论文提取文本中**未明确说明**具体的 GPU 型号、数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个代表性 RMBench 任务上进行了详尽测试，每个任务 100 次实验，确保了统计意义上的可靠性。
*   **消融实验**：
    *   **记忆消融**：验证了情节历史（$H_t$）和工作记忆（$W_t$）对长程任务的贡献。
    *   **恢复机制消融**：验证了验证器（$\Phi_{verify}$）和反思引擎（$\Phi_{reflect}$）在应对执行失败时的必要性。
*   **评价**：实验设计覆盖了从简单记忆到复杂逻辑推理的梯度，对比基线涵盖了当前主流的具身智能架构，实验结果客观且具有说服力。

#### 6. 主要结论与发现
*   **性能飞跃**：Goal2Skill 的平均成功率达到 **32.4%**，远超最强基线（9.8%）。
*   **长程优势**：在更难的 M(n) 任务中，该方法领先优势扩大（38.7% vs 9.0%），证明了双系统架构在处理复杂依赖关系时的卓越性。
*   **记忆与反思的作用**：消融实验显示，结构化记忆是长程决策的基础，而显式的验证与反思机制显著提升了系统在面对物理交互不确定性时的鲁棒性。

#### 7. 优点（亮点）
*   **解耦设计**：成功将高层语义推理与底层精细控制分离，降低了单一端到端模型在长程任务中的学习难度。
*   **闭环纠错**：引入了类似人类的“反思”机制，使机器人能够从失败中学习并即时调整策略，而非盲目重复错误。
*   **感知优化**：通过几何过滤（Masked Filtering）有效解决了复杂环境中的干扰物问题。

#### 8. 不足与局限
*   **绝对成功率仍有提升空间**：虽然相对基线提升巨大，但在复杂任务（如按按钮）上的绝对成功率（10%）仍不足以支持大规模商业化应用。
*   **技能库依赖**：底层执行器依赖于预定义的技能库，对于从未见过的全新技能动作，系统的泛化能力可能受限。
*   **实时性挑战**：高层 VLM 的频繁调用和反思推理可能会引入额外的计算延迟，影响实时交互性能。

（完）

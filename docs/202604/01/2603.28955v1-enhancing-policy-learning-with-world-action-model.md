---
title: Enhancing Policy Learning with World-Action Model
title_zh: 利用世界-动作模型增强策略学习
authors: "Yuci Han, Alper Yilmaz"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28955v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于操作任务的动作正则化世界模型
tldr: "本文提出世界-动作模型（WAM），通过在DreamerV2中引入逆动力学目标，使模型能同时预测未来视觉观测和驱动状态转移的动作，从而增强潜在表示中与动作相关的结构信息。在CALVIN基准测试的8个任务中，WAM显著提升了行为克隆和PPO微调的成功率，在减少8.7倍训练步数的同时，将平均成功率从79.8%提升至92.8%，证明了动作正则化对策略学习的巨大增益。"
selection_source: fresh_fetch
motivation: 传统世界模型仅通过图像预测训练，难以有效捕捉对下游控制至关重要的动作相关特征。
method: 在DreamerV2框架中整合逆动力学目标，通过预测潜在状态转移对应的动作来正则化表示学习。
result: "WAM在CALVIN任务中将策略微调成功率提升至92.8%，且训练效率比基准模型提高8.7倍。"
conclusion: 通过动作正则化增强世界模型的表示能力，可以显著提升复杂操作任务中的策略学习效果和样本效率。
---

## 摘要
本文提出了世界-动作模型（World-Action Model, WAM），这是一种动作正则化的世界模型，能够对未来的视觉观测以及驱动状态转移的动作进行联合推理。与仅通过图像预测进行训练的传统世界模型不同，WAM 在 DreamerV2 中引入了逆动力学目标，通过潜在状态转移来预测动作，从而鼓励学习到的表示捕捉对下游控制至关重要的动作相关结构。我们在 CALVIN 基准测试的八个操作任务中评估了 WAM 在增强策略学习方面的表现。我们首先通过在世界模型潜在空间上进行行为克隆来预训练扩散策略，然后在冻结的世界模型内部使用基于模型的 PPO 对其进行微调。在不修改策略架构或训练程序的情况下，相比于 DreamerV2 和 DiWA 基线，WAM 将平均行为克隆成功率从 59.4% 提升至 71.2%。经过 PPO 微调后，WAM 实现了 92.8% 的平均成功率（基线为 79.8%），其中两个任务达到了 100%，且训练步数减少了 8.7 倍。

## Abstract
This paper presents the World-Action Model (WAM), an action-regularized world model that jointly reasons over future visual observations and the actions that drive state transitions. Unlike conventional world models trained solely via image prediction, WAM incorporates an inverse dynamics objective into DreamerV2 that predicts actions from latent state transitions, encouraging the learned representations to capture action-relevant structure critical for downstream control. We evaluate WAM on enhancing policy learning across eight manipulation tasks from the CALVIN benchmark. We first pretrain a diffusion policy via behavioral cloning on world model latents, then refine it with model-based PPO inside the frozen world model. Without modifying the policy architecture or training procedure, WAM improves average behavioral cloning success from 59.4% to 71.2% over DreamerV2 and DiWA baselines. After PPO fine-tuning, WAM achieves 92.8% average success versus 79.8% for the baseline, with two tasks reaching 100%, using 8.7x fewer training steps.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Policy Learning with World-Action Model》（利用世界-动作模型增强策略学习）的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的世界模型（如 Dreamer 系列）主要通过预测未来观测（像素）来学习环境动力学，动作仅作为输入条件。这种不对称性导致学习到的潜在表示（Latent Representations）主要优化了图像重建，而缺乏对下游控制至关重要的“动作相关结构”信息。
*   **研究动机**：作者认为，如果要求模型预测导致状态转移的动作（逆动力学），可以强制编码器捕捉环境如何响应智能体行为的细微特征。这种“动作感知”的表示能显著提升下游策略学习（如行为克隆和强化学习）的效率和成功率。

### 2. 方法论：世界-动作模型 (WAM)
*   **核心思想**：在 DreamerV2 的 RSSM（循环状态空间模型）架构基础上，增加一个**逆动力学头（Inverse Dynamics Head）**，通过联合预测未来观测和驱动状态转移的动作来正则化潜在空间。
*   **关键技术细节**：
    *   **逆动力学目标**：使用一个三层 MLP 预测连续两个编码器嵌入（$e_t$ 和 $e_{t+1}$）之间的动作 $a_t$。
    *   **级联效应（Cascading Effect）**：这是 WAM 的设计精髓。通过直接正则化编码器，动作感知结构会传递给后验分布 $z_t$，再通过 KL 散度损失传播到先验分布 $\hat{z}_t$。这意味着即使在没有真实观测的“想象”阶段，模型生成的潜在状态也带有动作相关信息。
    *   **训练目标**：总损失函数由 KL 散度损失、图像重建损失和动作预测损失（L1 损失）加权组成。
    *   **策略学习流水线**：首先在冻结的 WAM 潜在空间上通过行为克隆（BC）预训练一个扩散策略（Diffusion Policy），然后在 WAM 模拟器中利用 PPO 算法进行离线强化学习微调。

### 3. 实验设计
*   **数据集与场景**：使用 **CALVIN 机器人操作基准测试**。这是一个桌面操作环境，包含 7 自由度的 Franka 手臂，提供约 6 小时的遥操作数据（约 50 万帧）。
*   **任务设置**：评估了 8 个具体任务，包括开关抽屉、左右移动滑块、开关灯泡和 LED 灯。
*   **对比方法 (Benchmark)**：
    *   **世界模型质量对比**：对比 DreamerV2（标准世界模型）。
    *   **策略学习对比**：对比 **DiWA**（基于扩散策略的世界模型自适应方法）。
*   **评估指标**：图像生成的 PSNR、SSIM、LPIPS、FVD；策略执行的成功率。

### 4. 资源与算力
*   **训练效率**：论文明确指出 WAM 仅需 **23 万个梯度步** 即可达到优异性能，而基准模型 DreamerV2 需要 200 万步。这意味着 WAM 的训练效率提升了约 **8.7 倍**。
*   **硬件说明**：文中**未明确提及**具体的 GPU 型号、数量或具体的训练时长（小时/天）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 8 个不同的操作任务上进行了详尽测试。
    *   图像生成评估基于 100 个随机序列的 50 步开环想象。
    *   策略评估在 29 个未见过的初始配置上进行。
*   **充分性与客观性**：实验设计较为充分，涵盖了从感知（图像生成质量）到控制（BC 和 PPO 成功率）的全流程对比。通过保持策略架构（DiffusionMLP）完全一致，仅改变世界模型，证明了性能提升确实源于 WAM 提供的表示增强，对比具有公平性。

### 6. 主要结论与发现
*   **表示能力更强**：引入动作正则化后，世界模型生成的未来图像在形状保持、细节和颜色准确度上均优于 DreamerV2。
*   **策略性能大幅提升**：
    *   **行为克隆 (BC)**：平均成功率从 45.8% 提升至 61.7%。
    *   **PPO 微调**：平均成功率从 79.8% 提升至 **92.8%**，其中两个任务达到 100% 成功。
*   **样本效率极高**：在减少近 90% 训练步数的情况下，依然在所有任务上超越了强力基线 DiWA。

### 7. 优点
*   **轻量且高效**：仅通过增加一个简单的 MLP 逆动力学头和相应的损失项，就实现了显著的性能飞跃，无需重新设计复杂的大模型架构。
*   **即插即用**：该方法可以无缝集成到现有的基于 RSSM 的世界模型中，且不改变下游策略的训练逻辑。
*   **显著提升控制精度**：在需要精确位置控制的任务（如开关抽屉）中表现尤为突出，证明了动作正则化对空间感知和因果建模的帮助。

### 8. 不足与局限
*   **场景单一**：实验仅限于 CALVIN 桌面操作环境，尚未在更复杂的户外场景或多机器人协作场景中验证。
*   **缺乏真机实验**：论文所有结果均在仿真环境中得出，虽然世界模型通常具有较好的迁移潜力，但未展示在真实物理硬件上的表现。
*   **依赖专家数据**：策略学习的第一阶段仍依赖于 50 个专家演示视频，对于完全无监督或纯探索环境下的表现尚不明确。

（完）

---
title: Efficient Camera Pose Augmentation for View Generalization in Robotic Policy Learning
title_zh: 机器人策略学习中面向视角泛化的高效相机位姿增强
authors: "Sen Wang, Huaiyi Dong, Jingyi Tian, Jiayi Li, Zhuo Yang, Tongtong Cao, Anlin Chen, Shuang Wu, Le Wang, Sanping Zhou"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29192v1"
tags: ["query:q5"]
score: 9.0
evidence: 用于机器人策略训练的合成视图渲染
tldr: 针对2D视觉运动策略在未知视角下泛化能力差的问题，本文提出GenSplat框架。该框架利用前馈式3D高斯泼溅（3DGS）技术，从稀疏且未校准的输入中快速重建高质量3D场景。通过3D先验蒸馏策略确保几何稳定性，并利用生成的合成视角进行数据增强，使机器人策略能够基于底层3D结构进行决策，显著提升了在剧烈空间扰动下的执行鲁棒性。
selection_source: fresh_fetch
motivation: 现有的2D视觉策略过度依赖静态观测，导致其在面对未见过的相机视角时难以保持动作输出的一致性。
method: 提出一种基于置换等变架构的前馈式3DGS框架，并引入3D先验蒸馏策略以防止几何坍缩，从而实现高效的视角合成与增强。
result: 通过在训练中引入多样化的合成视角，该方法强制策略学习底层3D结构，在严重空间扰动下的表现远超基准模型。
conclusion: 结合高效的3D场景重建与相机位姿增强，能够显著增强机器人策略的视角泛化能力和执行稳健性。
---

## 摘要
主流的以 2D 为中心的视觉运动策略在未知视角泛化方面表现出明显的不足，因为它们对静态观测的依赖阻碍了在未见视角下实现一致的动作映射。为此，我们引入了 GenSplat，这是一个前馈 3D 高斯泼溅（3D Gaussian Splatting）框架，通过新视角渲染来促进具有视角泛化能力的策略学习。GenSplat 采用置换等变架构，通过单次前向传递从稀疏、未校准的输入中重建高保真 3D 场景。为了确保结构完整性，我们设计了一种 3D 先验蒸馏策略来正则化 3DGS 优化，从而防止纯光度监督中常见的几何坍缩。通过从这些稳定的 3D 表示中渲染多样化的合成视角，我们在训练过程中系统地增强了观测流形。这种增强迫使策略将其决策建立在底层 3D 结构之上，从而确保在基准模型性能严重下降的剧烈空间扰动下，仍能实现稳健的执行。

## Abstract
Prevailing 2D-centric visuomotor policies exhibit a pronounced deficiency in novel view generalization, as their reliance on static observations hinders consistent action mapping across unseen views. In response, we introduce GenSplat, a feed-forward 3D Gaussian Splatting framework that facilitates view-generalized policy learning through novel view rendering. GenSplat employs a permutation-equivariant architecture to reconstruct high-fidelity 3D scenes from sparse, uncalibrated inputs in a single forward pass. To ensure structural integrity, we design a 3D-prior distillation strategy that regularizes the 3DGS optimization, preventing the geometric collapse typical of purely photometric supervision. By rendering diverse synthetic views from these stable 3D representations, we systematically augment the observational manifold during training. This augmentation forces the policy to ground its decisions in underlying 3D structures, thereby ensuring robust execution under severe spatial perturbations where baselines severely degrade.

---

## 论文详细总结（自动生成）

### 机器人策略学习中面向视角泛化的高效相机位姿增强 (GenSplat) 论文总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视觉运动策略（Visuomotor Policies）大多以 2D 为中心，高度依赖特定的相机视角。当机器人面临未见过的相机位姿（SE(3) 扰动）时，其空间感知会失效，导致任务成功率大幅下降。
*   **核心问题**：如何在不增加人工演示收集成本的前提下，通过增强观测流形（Observational Manifold）来提升机器人策略的视角泛化能力？
*   **整体含义**：本文提出了一种名为 **GenSplat** 的框架，利用前馈式 3D 高斯泼溅（3DGS）技术，从稀疏且未校准的图像中快速重建 3D 场景并生成高质量的新视角图像，以此作为数据增强手段，训练出对相机位姿变动更具鲁棒性的机器人策略。

#### 2. 论文提出的方法论
*   **核心思想**：将 3D 场景重建转化为一个前馈预测任务，通过引入 3D 几何先验蒸馏，确保在稀疏观测下生成的合成视角具有严格的几何一致性。
*   **关键技术细节**：
    *   **置换等变架构 (Permutation-Equivariant Architecture)**：使用 DINOv2 提取特征，通过交替的帧内和全局自注意力机制处理多视图输入，确保输出不依赖于输入视图的顺序。
    *   **前馈 3DGS 预测**：模型直接预测相机位姿（$R, T$）、密集 3D 点图（Point Map）以及高斯原语的属性（不透明度、旋转、缩放、球谐系数）。
    *   **3D 先验蒸馏 (3D-Prior Distillation)**：从预训练的 3D 基础模型中提取几何知识，通过点图损失 ($L_{3D}$) 和相机位姿损失 ($L_{cam}$) 约束重建过程，防止纯光度监督导致的几何坍缩（如“漂浮物”或边缘撕裂）。
    *   **数据增强流程**：利用重建的 3D 场景，在原始演示轨迹的基础上，按预设的旋转和平移范围渲染新视角，生成增强后的训练集。

#### 3. 实验设计
*   **实验场景**：使用 Franka Research 3 机械臂，配备 Robotiq 2F-85 夹爪。
*   **任务设置**：包含 6 个真实世界操作任务（如捡香蕉、堆叠方块、插环、放罐子到架子等）。
*   **数据集**：每个任务收集 100 组人工演示。预训练阶段使用了大规模机器人数据集 DROID（约 27.1 万张图像）。
*   **对比方法 (Benchmarks)**：
    *   **策略模型**：$\pi_0$ (VLA 模型) 和 Diffusion Policy。
    *   **新视角合成 (NVS) 方法**：扩散模型类（ZeroNVS, VISTA, SEVA）和高斯泼溅类（InstantSplat, NoPoSplat, AnySplat）。
*   **评估指标**：在不同扰动水平（Small, Medium, Large）下的任务成功率。

#### 4. 资源与算力
*   **训练细节**：模型训练了 30k 次迭代，Batch Size 为 16，使用了余弦退火学习率调度（峰值 $2e^{-4}$）。
*   **优化技术**：启用了 FlashAttention、梯度检查点（Gradient Checkpointing）和 bfloat16 精度以降低显存占用。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号和数量，但提到其推理速度极快（每帧 0.14 秒），远优于需要逐场景优化的传统方法。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 6 个任务进行了详尽的真实机器人测试。
    *   每个任务在不同扰动水平下评估 30 个 Episode。
    *   进行了消融实验（验证置换等变架构和 3D 先验蒸馏的作用）。
    *   探讨了渲染视角数量（N=1 到 4）对性能的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从底层重建质量到上层策略执行的完整链路。对比了当前最先进的多种 NVS 方案，且在真实硬件上进行了验证，具有较高的客观性和说服力。

#### 6. 论文的主要结论与发现
*   **视角泛化显著提升**：在剧烈（Large）相机扰动下，GenSplat 增强后的策略表现远超基准模型（如 Diffusion Policy 的相对提升达 56%）。
*   **几何一致性至关重要**：简单的 2D 扩散增强或缺乏先验约束的 3D 重建会引入视觉伪影，反而可能损害策略性能；而 GenSplat 提供的物理一致性观测能有效引导策略学习底层 3D 结构。
*   **高效性**：前馈架构避免了耗时的逐场景优化，使其能够在大规模机器人数据集上实现可扩展的增强。

#### 7. 优点
*   **无需校准**：能够处理未校准（Uncalibrated）的相机输入，极大地降低了实际部署的门槛。
*   **高质量合成**：通过 3D 先验蒸馏解决了稀疏视角下 3DGS 的拓扑破碎问题，生成的图像更符合物理规律。
*   **架构无关**：作为一种数据增强手段，可以无缝集成到 $\pi_0$ 或 Diffusion Policy 等多种主流策略架构中。

#### 8. 不足与局限
*   **动态场景限制**：目前主要针对静态工作空间重建，对于手眼相机（Wrist Camera）在运动过程中产生的复杂动态遮挡和几何变化，处理能力尚待进一步探索。
*   **计算开销**：虽然推理快，但训练阶段仍需依赖大规模 3D 基础模型的蒸馏，且对高质量预训练数据的依赖较强。
*   **泛化边界**：虽然提升了视角鲁棒性，但对于极端角度（如完全背后的视角）的补全能力受限于初始稀疏观测的信息量。

（完）

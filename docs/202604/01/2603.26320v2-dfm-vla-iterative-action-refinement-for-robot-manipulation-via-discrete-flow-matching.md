---
title: "DFM-VLA: Iterative Action Refinement for Robot Manipulation via Discrete Flow Matching"
title_zh: DFM-VLA：通过离散流匹配实现机器人操纵的迭代动作细化
authors: "Jiayi Chen, Wenxuan Song, Shuai Chen, Jingbo Wang, Zhijun Li, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26320v2"
tags: ["query:q12"]
score: 10.0
evidence: 离散流匹配VLA用于迭代动作精细化
tldr: 针对现有视觉-语言-动作（VLA）模型在离散动作解码时无法纠正早期生成错误的局限性，本文提出了DFM-VLA。该模型引入离散流匹配技术，通过建模动作序列的概率速度场实现动作令牌的迭代优化。结合两阶段解码策略，DFM-VLA在CALVIN和LIBERO等基准测试及真实机器人任务中表现优异，显著提升了操作成功率和推理效率。
selection_source: fresh_fetch
motivation: 现有的自回归或离散扩散VLA模型在生成动作令牌后无法修正早期错误，导致误差累积并限制了操作性能。
method: 提出基于离散流匹配的VLA框架，利用概率速度场动态更新全量动作序列，并配合迭代细化与确定性验证的两阶段解码策略。
result: "在CALVIN和LIBERO基准测试中分别取得4.44成功长度和95.7%成功率，性能全面超越了主流的自回归及扩散模型基准。"
conclusion: 离散流匹配通过迭代细化机制有效解决了动作生成的错误累积问题，为高效、高精度的机器人操作提供了新方案。
---

## 摘要
采用离散标记化方案对动作进行编码的视觉-语言-动作（VLA）模型正越来越多地被应用于机器人操纵，但现有的解码范式仍存在根本性的局限。无论是通过自回归 VLA 进行顺序解码，还是通过离散扩散 VLA 进行并行解码，一旦生成标记（token）后通常就是固定的，无法在后续迭代中进行修改，因此早期的标记错误无法在后期得到有效纠正。我们提出了 DFM-VLA，一种用于动作标记迭代细化的离散流匹配 VLA。DFM-VLA 建模了一个标记级概率速度场，该速度场在细化迭代过程中动态更新整个动作序列。我们研究了两种构建速度场的方法：辅助速度头公式化和动作嵌入引导公式化。我们的框架进一步采用了一种两阶段解码策略，包括迭代细化阶段和随后的确定性验证，以实现稳定收敛。在 CALVIN、LIBERO 和真实世界操纵任务上的大量实验表明，DFM-VLA 在保持高推理效率的同时，在操纵性能上始终优于强大的自回归、离散扩散和连续扩散基准模型。特别是，DFM-VLA 在 CALVIN 上实现了 4.44 的平均成功长度，在 LIBERO 上实现了 95.7% 的平均成功率，突显了通过离散流匹配进行动作细化在机器人操纵中的价值。项目主页见 https://chris1220313648.github.io/DFM-VLA/。

## Abstract
Vision--Language--Action (VLA) models that encode actions using a discrete tokenization scheme are increasingly adopted for robotic manipulation, but existing decoding paradigms remain fundamentally limited. Whether actions are decoded sequentially by autoregressive VLAs or in parallel by discrete diffusion VLAs, once a token is generated, it is typically fixed and cannot be revised in subsequent iterations, so early token errors cannot be effectively corrected later. We propose DFM-VLA, a discrete flow matching VLA for iterative refinement of action tokens. DFM-VLA~models a token-level probability velocity field that dynamically updates the full action sequence across refinement iterations. We investigate two ways to construct the velocity field: an auxiliary velocity-head formulation and an action-embedding-guided formulation. Our framework further adopts a two-stage decoding strategy with an iterative refinement stage followed by deterministic validation for stable convergence. Extensive experiments on CALVIN, LIBERO, and real-world manipulation tasks show that DFM-VLA consistently outperforms strong autoregressive, discrete diffusion, and continuous diffusion baselines in manipulation performance while retaining high inference efficiency. In particular, DFM-VLA achieves an average success length of 4.44 on CALVIN and an average success rate of 95.7\% on LIBERO, highlighting the value of action refinement via discrete flow matching for robotic manipulation. Our project is available https://chris1220313648.github.io/DFM-VLA/

---

## 论文详细总结（自动生成）

### DFM-VLA：基于离散流匹配的机器人操纵迭代动作细化总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对现有视觉-语言-动作（VLA）模型在离散动作解码中的**“不可逆承诺”（Irreversible Commitment）**问题提出了挑战。
*   **背景：** 现有的离散 VLA 主要分为自回归（AR）和离散扩散（DD）两类。AR 模型按顺序生成标记，一旦生成便无法更改；DD 模型虽然并行，但在早期迭代中生成的错误标记在后续步骤中也难以有效修正。
*   **核心问题：** 早期解码阶段的微小错误会随时间累积，导致机器人执行长程任务时失败。
*   **研究动机：** 引入离散流匹配（Discrete Flow Matching, DFM）机制，允许模型在推理过程中对整个动作序列进行多次迭代和“撤回重做”，从而实现动作的精细化修正。

#### 2. 方法论：核心思想与关键技术
DFM-VLA 的核心思想是建模一个**标记级概率速度场（Token-level Probability Velocity Field）**，通过该速度场动态更新动作序列。
*   **速度场构建：** 论文探索了两种方案：
    1.  **辅助速度头（Auxiliary Velocity Head）：** 通过额外的预测头从模型隐藏状态直接预测标记替换的速度。
    2.  **动作嵌入引导（Action-Embedding-Guided）：** 利用动作标记在嵌入空间中的语义距离定义概率路径，推导出动力学最优速度。实验证明此方案收敛更快、性能更强。
*   **两阶段解码策略：**
    1.  **迭代细化阶段：** 使用连续时间马尔可夫链（CTMC）的欧拉离散化进行随机跳转更新，允许模型探索和修正。
    2.  **确定性验证阶段：** 在最后几步切换为贪婪解码（Greedy Decoding），确保动作序列稳定收敛。
*   **自适应 KV 缓存（Adaptive KV Caching）：** 针对迭代解码的效率问题，通过监控动作标记特征的变化，动态复用指令和视觉观测的 KV 缓存，实现 2.4 倍的推理加速。

#### 3. 实验设计
*   **数据集与场景：**
    *   **CALVIN：** 评估长程操纵能力（ABCD→D 设置），关注连续完成子任务的平均长度。
    *   **LIBERO：** 评估跨任务泛化能力，包含 Spatial、Object、Goal 和 Long 四个子集。
    *   **真实世界任务：** 双臂协作抬锅、蔬菜入锅、方块放置。
*   **对比方法（Baselines）：**
    *   **自回归模型：** RT-1, OpenVLA, UniVLA, ReconVLA 等。
    *   **离散扩散模型：** PD-VLA, Dream-VLA, LLADA-VLA 等。
    *   **连续扩散模型：** RDT, π0-FAST。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA H100 GPU** 进行训练和推理。
*   **训练时长/规模：** 
    *   仿真环境（CALVIN/LIBERO）训练 20k 至 32k 步。
    *   真实世界任务每个任务训练 5k 步。
    *   学习率设为 $1 \times 10^{-4}$，Batch Size 为 8。

#### 5. 实验数量与充分性
*   **实验规模：** 
    *   CALVIN 进行了 1000 次测试循环。
    *   LIBERO 每个任务进行 50 次测试，总计覆盖 40 个任务。
    *   真实世界每个任务进行 40 次试验。
*   **消融实验：** 针对超参数（速度场调度参数 $c$ 和 $\alpha$）、解码步骤分配（细化步数 vs 验证步数）、速度场构建方式、数据规模（10%、50%、100%）均做了详尽对比。
*   **评价：** 实验设计非常充分，涵盖了从仿真到现实、从性能到效率的多维度对比，实验过程遵循了学术界的标准 Benchmark，对比具有客观性和公平性。

#### 6. 主要结论与发现
*   **性能领先：** DFM-VLA 在 CALVIN 上达到 4.44 的平均成功长度，在 LIBERO 上达到 95.7% 的成功率，均刷新了 SOTA 记录。
*   **修正机制有效：** 实验证明，允许全序列迭代细化的 DFM 机制显著优于只能修改掩码位置的离散扩散模型。
*   **嵌入引导更优：** 相比于直接预测速度，利用动作嵌入空间的几何结构来引导流匹配能显著提升数据效率和最终精度。
*   **效率平衡：** 结合自适应缓存后，DFM-VLA 在保持高性能的同时，推理速度优于传统的自回归模型。

#### 7. 优点
*   **创新性：** 首次将离散流匹配引入 VLA 领域，解决了离散动作生成的错误累积痛点。
*   **架构统一：** 保持了 VLM 的离散标记化优势，同时获得了类似扩散模型的细化能力。
*   **实用性：** 提出的两阶段解码和自适应缓存技术，使其在真实机器人部署中具有极高的推理效率。

#### 8. 不足与局限
*   **量化误差：** 尽管引入了细化机制，但模型仍受限于动作标记化（Tokenization）的精度，对于极度精细的连续控制可能仍存在量化损失。
*   **序列长度固定：** 动作块（Action Chunk）的长度在训练时固定，可能限制了其在需要极长时序依赖任务中的灵活性。
*   **算力需求：** 虽然推理效率提升，但训练过程仍依赖高性能 H100 集群，对于资源受限的环境存在一定门槛。

（完）

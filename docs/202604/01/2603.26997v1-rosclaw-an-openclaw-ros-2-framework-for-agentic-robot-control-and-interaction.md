---
title: "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction"
title_zh: ROSClaw：一种用于代理式机器人控制与交互的 OpenClaw ROS 2 框架
authors: "Irvin Steve Cardenas, Marcus Anthony Arnett, Natalie Catherine Yeo, Lucky Sah, Jong-Hoon Kim"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26997v1"
tags: ["query:q7"]
score: 9.0
evidence: 将基础模型连接到物理机器人以进行智能体控制
tldr: ROSClaw是一个集成OpenClaw与ROS 2的模型无关执行层框架，旨在解决基础模型与机器人集成时耦合度高的问题。它通过动态能力发现、多模态观测归一化和安全验证，实现了模型与硬件的灵活解耦。实验证明，该框架能有效评估不同模型在多样化机器人上的表现差异，为具身智能提供了标准化的基础设施。
selection_source: fresh_fetch
motivation: 现有的基础模型与机器人集成方案多为定制化，缺乏能解耦感知、执行与安全逻辑的通用框架。
method: 提出了ROSClaw框架，利用标准化接口注入、多模态归一化及预执行安全校验，实现模型与ROS 2机器人的无缝对接。
result: 在多种机器人和模型上的实验显示，模型间的动作建议率差异高达4.8倍，且执行层设计对任务成功率有显著影响。
conclusion: ROSClaw为具身智能提供了一个实用的基础设施和可重复的性能测量工具。
---

## 摘要
基础模型可以赋予机器人开放式推理、语言理解和自适应规划能力，但目前将模型连接到物理机器人需要定制化集成，将感知、执行和安全性与单一模型和平台耦合。我们提出了 ROSClaw，这是一个与模型无关的执行层，它将 OpenClaw 智能体运行时与 ROS 2 集成，使任何基础模型能够通过以下方式感知、推理并作用于任何支持 ROS 的机器人：(i) 具有标准化示能性（affordance）注入的动态能力发现，(ii) 多模态观测归一化，(iii) 在可配置安全包络内的执行前动作验证，以及 (iv) 结构化审计日志记录。更换模型后端或机器人平台仅需更改配置；工具模式（tool schemas）、安全执行和溯源日志保持不变。我们在三个平台（轮式、四足、人形）上部署了 ROSClaw，并使用了四个基础模型后端。在这种受控基座下，模型在策略外动作建议率上表现出高达 4.8 倍的差异（仅在前沿模型中就有 3.4 倍），并针对相同的指令产生性质迥异的物理行为。一项针对 ROSA 的跨框架对等协议证实，执行层设计（而非仅仅是提示词措辞）会显著影响任务完成情况和安全行为，从而确立了 ROSClaw 既是实用的代理式机器人基础设施，又是具身智能（embodied AI）的可重复测量工具。

## Abstract
Foundation models can endow robots with open-ended reasoning, language understanding, and adaptive planning, yet connecting a model to a physical robot today requires bespoke integration that couples perception, actuation, and safety to a single model and platform. We present ROSClaw, a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2, enabling any foundation model to perceive, reason about, and act on any ROS-enabled robot through (i) dynamic capability discovery with standardized affordance injection, (ii) multimodal observation normalization, (iii) pre-execution action validation within a configurable safety envelope, and (iv) structured audit logging. Swapping model backends or robot platforms is a configuration change; tool schemas, safety enforcement, and provenance logging remain invariant. We deploy ROSClaw on three platforms (wheeled, quadruped, humanoid) with four foundation-model backends. Under this controlled substrate, models exhibit up to 4.8 x differences in out-of-policy action proposal rates (3.4 x among frontier models alone) and produce qualitatively distinct physical behaviors from identical commands. A cross-framework parity protocol against ROSA confirms that executive-layer design, not just prompt wording, significantly affects both task completion and safety behavior, establishing ROSClaw as both practical agentic-robot infrastructure and a reproducible measurement instrument for embodied AI.

---

## 论文详细总结（自动生成）

以下是对论文《ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：目前将基础模型（如 LLM/VLM）集成到物理机器人时，通常采用“点对点”的定制化方案，导致感知、执行和安全逻辑与特定模型或硬件平台高度耦合，难以跨平台复用或进行公平的模型性能比较。
*   **研究动机**：基础模型虽具备推理和规划潜力，但缺乏一个标准化的“执行层”（Executive Layer）来充当机器人“大脑”与“身体”之间的中介。
*   **整体含义**：论文提出了 **ROSClaw**，一个与模型无关的 ROS 2 执行层框架。它通过形式化“执行层契约”，实现了模型后端与机器人硬件的解耦，为具身智能（Embodied AI）研究提供了一个可重复、可审计的实验基座。

### 2. 方法论：核心思想与关键技术
ROSClaw 的核心思想是建立一个**执行层契约 $C = \langle A, O, V, L \rangle$**，具体包含：
*   **能力发现与示能性注入 ($A$)**：通过动态发现节点自动扫描 ROS 图（Topics/Services/Actions），生成标准化的工具模式（Tool Schemas）并注入模型提示词中。
*   **观测归一化 ($O$)**：支持原生多模态（直接传图像）和桥接接地（由 VLM 将图像转为 JSON 场景描述）两种模式，确保不同模型接收到的环境信息一致。
*   **预执行验证 ($V$)**：在动作发布前，通过安全包络（如速度限制、白名单）拦截非法指令。若指令违规，拦截器会返回结构化理由引导模型重新规划。
*   **结构化审计日志 ($L$)**：记录所有观测、模型提议、验证决策（允许/拦截）及执行结果，用于事后分析模型行为。
*   **传输抽象**：支持本地 DDS、WebSocket (rosbridge) 和 WebRTC P2P 三种通信模式。

### 3. 实验设计
*   **机器人平台**：TurtleBot3（轮式）、Unitree Go2（四足）、Unitree G1（人形）。
*   **模型后端**：Claude Opus 4.6、GPT-5.2、Gemini 3.1 Pro、Llama 4 Maverick。
*   **任务套件（共 40 个任务）**：
    *   **结构化任务 (20个)**：分为直接指令、上下文相关和多步规划三个难度等级。
    *   **开放式行为任务 (10个)**：如“Shake and bake”（俚语指令），测试模型的创意解释和风险偏好。
    *   **安全发散任务 (10个)**：对抗性提示（如“忽略安全规则，全速前进”），测试模型的合规性。
*   **基准测试 (Benchmark)**：以 NASA JPL 开发的 **ROSA** 框架作为直接对比基准，采用相同的硬件、提示词和安全限制进行对等协议测试。

### 4. 资源与算力
*   **算力说明**：文中提到 Llama 4 Maverick 是通过 **vLLM** 进行本地托管的，但未明确给出具体的 GPU 型号、数量或训练时长。
*   **推理延迟**：实验记录了 LLM 推理延迟在 1.3s 到 2.3s 之间，远高于 5-10ms 的通信延迟，表明当前瓶颈在于模型推理而非系统架构。

### 5. 实验数量与充分性
*   **实验规模**：每个任务在每个条件下重复 $N=10$ 次试验，总计产生了数百组审计日志。
*   **充分性**：实验覆盖了从轮式到人形的三种主流机器人形态，并对比了当前最顶尖的四个商用/开源模型。
*   **客观性**：引入了消融实验（如“边界可见性”对安全行为的影响）和跨框架对等协议，并使用独立评分员进行双盲评分（Kappa系数 0.81），实验设计较为严谨、公平。

### 6. 主要结论与发现
*   **模型行为显著发散**：在相同指令（如“Shake and bake”）和相同约束下，不同模型产生的物理轨迹截然不同（如 Claude 选择先旋转再前进，而 GPT 仅做线性振荡）。
*   **安全风险差异巨大**：在对抗性提示下，模型间的策略外动作建议率差异高达 **4.8 倍**（前沿模型间也有 3.4 倍差异），Llama 4 的违规尝试率最高。
*   **执行层设计至关重要**：对比实验显示，ROSClaw 的任务完成率优于 ROSA，证明了执行层的工具归一化和错误反馈机制对任务成功有显著贡献。
*   **平台可移植性**：更换机器人平台仅需 10-15 分钟配置，无需修改核心代码。

### 7. 优点
*   **高度解耦**：实现了“大脑”与“身体”的真正分离，更换模型或机器人仅需修改配置文件。
*   **安全性与审计性**：通过预执行拦截和全量日志记录，解决了 LLM 行为不可预测带来的安全隐患。
*   **标准化工具**：为具身智能研究提供了一套可重复的测量协议，有助于量化不同模型的“物理常识”和“风险偏好”。

### 8. 不足与局限
*   **实时性受限**：受限于 LLM 推理延迟（秒级），该框架目前仅适用于高层任务规划（慢回路），无法处理需要毫秒级响应的反应式控制（如避障、动态平衡）。
*   **样本量限制**：虽然每组实验重复 10 次，但对于捕捉极低概率的安全失效模式来说，样本量仍显不足。
*   **安全包络较简单**：目前的验证器主要基于速度阈值和白名单，尚未集成复杂的碰撞检测或工作空间约束。

（完）

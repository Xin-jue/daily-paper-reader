---
title: "GaussianGPT: Towards Autoregressive 3D Gaussian Scene Generation"
title_zh: GaussianGPT：迈向自回归三维高斯场景生成
authors: "Nicolas von Lützow, Barbara Rössle, Katharina Schmid, Matthias Nießner"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26661v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于仿真的自回归3D高斯场景生成
tldr: 本研究提出GaussianGPT，一种基于Transformer的自回归3D场景生成模型。不同于主流的扩散模型，它通过预测下一个Token直接生成3D高斯点云。该方法利用稀疏3D卷积自编码器将高斯原语压缩为离散潜空间，并结合3D旋转位置编码进行序列化建模。GaussianGPT支持场景补全、外扩及可控采样，为可控且具备上下文感知能力的3D生成提供了新的自回归范式。
selection_source: fresh_fetch
motivation: 旨在探索除扩散模型外的自回归路径，以利用Transformer的可扩展性实现更具组合性和上下文感知能力的3D场景生成。
method: 通过稀疏3D卷积自编码器将高斯原语量化为离散Token，并使用带有3D旋转位置编码的因果Transformer进行序列化预测。
result: 成功实现了3D场景的逐步构建，支持场景补全、外扩以及通过温度调节的可控采样，展现了灵活的生成能力。
conclusion: GaussianGPT证明了自回归Transformer在显式3D表示生成中的有效性，为可控3D场景建模提供了一种与扩散模型互补的新范式。
---

## 摘要
最近三维生成模型的大多数进展都依赖于扩散或流匹配公式。与之不同，我们探索了一种完全自回归的替代方案，并引入了 GaussianGPT。这是一个基于 Transformer 的模型，通过下个标记预测直接生成三维高斯，从而实现完整的三维场景生成。我们首先使用带有向量量化的稀疏三维卷积自编码器，将高斯基元压缩到离散潜空间网格中。生成的标记被序列化，并使用带有三维旋转位置嵌入的因果 Transformer 进行建模，从而实现空间结构和外观的顺序生成。与整体优化场景的扩散模型不同，我们的方案逐步构建场景，自然地支持补全、外扩、通过温度调节的可控采样以及灵活的生成范围。该方案利用了自回归建模的组合归纳偏置和可扩展性，同时在与现代神经渲染管线兼容的显式表示上运行，将自回归 Transformer 定位为可控且上下文感知的三维生成的一种补充范式。

## Abstract
Most recent advances in 3D generative modeling rely on diffusion or flow-matching formulations. We instead explore a fully autoregressive alternative and introduce GaussianGPT, a transformer-based model that directly generates 3D Gaussians via next-token prediction, thus facilitating full 3D scene generation. We first compress Gaussian primitives into a discrete latent grid using a sparse 3D convolutional autoencoder with vector quantization. The resulting tokens are serialized and modeled using a causal transformer with 3D rotary positional embedding, enabling sequential generation of spatial structure and appearance. Unlike diffusion-based methods that refine scenes holistically, our formulation constructs scenes step-by-step, naturally supporting completion, outpainting, controllable sampling via temperature, and flexible generation horizons. This formulation leverages the compositional inductive biases and scalability of autoregressive modeling while operating on explicit representations compatible with modern neural rendering pipelines, positioning autoregressive transformers as a complementary paradigm for controllable and context-aware 3D generation.

---

## 论文详细总结（自动生成）

以下是对论文《GaussianGPT: Towards Autoregressive 3D Gaussian Scene Generation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前 3D 生成模型（如物体或场景生成）主要依赖扩散模型（Diffusion）或流匹配（Flow-matching）。这些方法通常采用全局去噪过程，虽然视觉效果好，但在处理 3D 场景的**增量构建**（如逐步扩展、局部编辑、补全）时缺乏灵活性。
*   **核心问题**：如何利用自回归（Autoregressive）Transformer 的序列建模能力，实现像 GPT 生成文本一样，通过“预测下一个 Token”来构建复杂的 3D 高斯（3D Gaussian Splatting）场景？
*   **研究动机**：3D 场景在现实中往往是逐步构建和修改的。作者旨在探索一种与扩散模型互补的范式，利用自回归模型在组合性、可扩展性和上下文感知方面的优势。

### 2. 方法论
GaussianGPT 的核心思想是将连续的 3D 高斯场景离散化为 Token 序列，并使用因果 Transformer 进行建模。
*   **场景压缩与量化（Tokenization）**：
    *   使用**稀疏 3D 卷积自编码器**将 3D 高斯原语（位置、不透明度、尺寸、旋转、颜色）映射到离散的潜空间网格。
    *   采用**无查找量化（Lookup-Free Quantization, LFQ）**技术，将连续特征转换为离散的代码簿索引（Tokens）。
*   **序列化（Serialization）**：
    *   采用简单的 **xyz 遍历顺序**将 3D 潜空间网格线性化为 1D 序列。
    *   序列设计：交替排列**位置 Token ($p_i$)** 和**特征 Token ($f_i$)**，解耦几何结构与外观建模。
*   **Transformer 架构**：
    *   基于 **GPT-2** 的解码器架构。
    *   **关键技术：3D 旋转位置编码（3D RoPE）**。由于 1D 序列化会破坏空间邻近性，3D RoPE 将实际的 3D 坐标注入注意力机制，使模型能感知空间距离而非仅是序列距离。
*   **生成流程**：
    *   从起始符（BOS）开始，模型交替预测位置和特征 Token，直到生成结束符（EOS）。
    *   支持**滑动窗口外扩（Outpainting）**：通过在已生成列的局部上下文上滑动，实现无限场景生成。

### 3. 实验设计
*   **数据集**：
    *   **PhotoShape**：用于物体级（椅子）生成的基准测试。
    *   **3D-FRONT**：高质量室内场景数据集。
    *   **Aria Synthetic Environments (ASE)**：大规模合成室内场景。
    *   **ScanNet++**：用于验证在真实世界数据上的微调效果。
*   **对比方法（Baselines）**：
    *   物体生成：$\pi$-GAN, EG3D, DiffRF, L3DG。
    *   场景生成：主要对比 L3DG（基于扩散模型的 3D 高斯生成）。
*   **评估指标**：
    *   视觉质量：FID、KID。
    *   几何保真度：Coverage (COV)、Minimum Matching Distance (MMD)。

### 4. 资源与算力
论文详细列出了训练所需的算力资源：
*   **自编码器训练**：使用 **4 张 NVIDIA RTX A6000 GPU**。场景训练约需 4 天，物体训练约需 2 天。
*   **Transformer 训练**：使用 **4 张 NVIDIA GH200 GPU**。场景训练约需 1 天，物体训练约需 4.5 小时。
*   **推理效率**：在 GH200 上，生成一个 4m × 4m 的场景块约需 90 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了物体级定量对比（1000 个样本）。
    *   展示了大规模场景（12m × 12m）的生成效果。
    *   包含场景补全（Completion）和外扩（Outpainting）的多组定性实验。
*   **消融实验**：针对 **3D 序列化策略**做了专门对比，测试了 Z-order、Hilbert 曲线和 xyz 遍历。结果显示在有 3D RoPE 支持下，简单的 xyz 遍历效果最好。
*   **客观性**：实验覆盖了从合成到真实、从物体到场景的多个维度，对比了当前最先进的扩散模型方法，实验设计较为全面且公平。

### 6. 主要结论与发现
*   **自回归可行性**：证明了纯自回归 Transformer 可以直接生成高质量的 3D 高斯场景，无需依赖 2D 扩散模型先验。
*   **性能领先**：在物体生成任务上，GaussianGPT 在 FID、KID 和 COV 指标上均优于现有的扩散模型方法（如 L3DG）。
*   **空间感知的重要性**：3D RoPE 是自回归模型处理 3D 数据的核心，它弥补了序列化带来的空间信息损失。
*   **多功能性**：同一个模型可以自然地处理无条件生成、场景补全和无限外扩，无需修改架构。

### 7. 优点
*   **范式创新**：为 3D 场景生成提供了一种除扩散模型之外的高效、灵活的自回归路径。
*   **增量生成能力**：天然支持场景的逐步构建和补全，这对于交互式设计和虚拟环境扩展非常有用。
*   **显式表示兼容**：直接操作 3D 高斯，生成结果可直接用于实时神经渲染。

### 8. 不足与局限
*   **真实数据挑战**：在 ScanNet++ 等真实世界数据上，自编码器的重建精度仍是瓶颈，高频细节（如纹理）可能丢失。
*   **空洞风险**：在生成大规模场景时，由于训练数据的稀疏性，模型有时会预测出空区域，需要通过重采样（Resampling）策略来缓解。
*   **计算开销**：虽然推理速度尚可，但生成超大规模场景时的 Token 序列极长，对显存和计算时间仍有一定压力。

（完）

---
title: "Extend3D: Town-Scale 3D Generation"
title_zh: Extend3D：城镇尺度的 3D 生成
authors: "Seungwoo Yoon, Jinmo Kim, Jaesik Park"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29387v1"
tags: ["query:q3"]
score: 9.0
evidence: 从单张图像生成城镇规模的3D场景
tldr: Extend3D是一个无需训练的单图生成城镇级3D场景的框架。它通过扩展物体中心生成模型的潜空间，并将其划分为重叠块进行协同生成，解决了大场景表示难题。利用单目深度估计的初始点云和SDEdit的“欠噪声”策略修复遮挡区域，并结合3D感知优化目标，实现了高质量、大尺度的3D场景重建与生成。
selection_source: fresh_fetch
motivation: 现有的物体中心3D生成模型受限于固定大小的潜空间，难以直接生成城镇规模的广阔3D场景。
method: 通过在x和y方向扩展潜空间并划分为重叠块，结合点云先验初始化和SDEdit迭代优化，实现大尺度场景的无缝生成。
result: 实验证明，该方法在几何结构和纹理保真度上均优于现有方法，并获得了更高的用户偏好评分。
conclusion: Extend3D成功将物体中心模型扩展至城镇级场景生成，为大规模3D环境的快速构建提供了有效方案。
---

## 摘要
在本文中，我们提出了 Extend3D，这是一个基于以物体为中心的 3D 生成模型、从单张图像生成 3D 场景的免训练流水线。为了克服以物体为中心的模型在表示宽阔场景时固定大小潜空间的局限性，我们在 $x$ 和 $y$ 方向上扩展了潜空间。然后，通过将扩展的潜空间划分为重叠的补丁（patches），我们在每个时间步将以物体为中心的 3D 生成模型应用于每个补丁并将其耦合。由于带有图像条件的补丁级 3D 生成要求图像与潜空间补丁之间严格的空间对齐，我们使用来自单目深度估计器的点云先验初始化场景，并通过 SDEdit 迭代细化遮挡区域。我们发现，在 3D 细化过程中将 3D 结构的不完整性视为噪声，可以通过一个我们称之为“欠加噪”（under-noising）的概念实现 3D 补全。此外，为了解决以物体为中心的模型在子场景生成中的次优性，我们在去噪过程中优化扩展的潜变量，确保去噪轨迹与子场景动力学保持一致。为此，我们引入了 3D 感知的优化目标，以提高几何结构和纹理保真度。人类偏好和定量实验证明，我们的方法比现有方法取得了更好的结果。

## Abstract
In this paper, we propose Extend3D, a training-free pipeline for 3D scene generation from a single image, built upon an object-centric 3D generative model. To overcome the limitations of fixed-size latent spaces in object-centric models for representing wide scenes, we extend the latent space in the $x$ and $y$ directions. Then, by dividing the extended latent space into overlapping patches, we apply the object-centric 3D generative model to each patch and couple them at each time step. Since patch-wise 3D generation with image conditioning requires strict spatial alignment between image and latent patches, we initialize the scene using a point cloud prior from a monocular depth estimator and iteratively refine occluded regions through SDEdit. We discovered that treating the incompleteness of 3D structure as noise during 3D refinement enables 3D completion via a concept, which we term under-noising. Furthermore, to address the sub-optimality of object-centric models for sub-scene generation, we optimize the extended latent during denoising, ensuring that the denoising trajectories remain consistent with the sub-scene dynamics. To this end, we introduce 3D-aware optimization objectives for improved geometric structure and texture fidelity. We demonstrate that our method yields better results than prior methods, as evidenced by human preference and quantitative experiments.

---

## 论文详细总结（自动生成）

以下是对论文《Extend3D: Town-Scale 3D Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 生成模型（如 Trellis, Hunyuan3D）大多是“以物体为中心”（Object-centric）的，受限于固定大小的潜空间（Latent Space），在生成大规模、复杂的城镇级场景时，往往会出现分辨率不足（模糊）、几何畸变（如地面消失）、物体重复或接缝明显等问题。
*   **研究动机**：为了在不重新训练模型的情况下，利用现有的高质量物体生成模型来构建大规模 3D 场景。研究者希望通过扩展潜空间并引入空间先验，实现从单张图像生成具有高保真度和几何一致性的城镇级 3D 资产。

### 2. 方法论
Extend3D 是一个**免训练（Training-free）**的流水线，核心思想是将大场景分解为多个重叠的子块进行协同生成。
*   **重叠补丁流（Overlapping Patch-wise Flow）**：在 $x$ 和 $y$ 方向扩展潜空间，将其划分为多个重叠的补丁（Patches）。在去噪过程中，每个补丁独立计算向量场，但在重叠区域进行平均耦合，从而确保补丁间的平滑过渡和全局一致性。
*   **先验初始化与迭代 SDEdit**：
    *   利用单目深度估计器（MoGe-2）从输入图像提取点云作为初始结构。
    *   **欠加噪（Under-noising）策略**：在 SDEdit 细化过程中，设置加噪强度小于去噪步长（$t_{noise} < t_{start}$）。这种设计让模型将点云中的空缺（遮挡区域）视为噪声并进行补全。
    *   通过迭代执行该过程，逐步修复初始点云中的空洞。
*   **3D 感知优化目标**：在去噪的每个时间步，对潜变量进行实时优化：
    *   **稀疏结构优化（$L_{SS}$）**：确保生成的体素与初始点云对齐，防止地面或关键结构消失。
    *   **结构化潜变量优化（$L_{SLAT}$）**：通过微分渲染损失（LPIPS, SSIM）优化纹理，提升视觉保真度并消除接缝。

### 3. 实验设计
*   **数据集/场景**：
    *   使用来自 Google Earth、UrbanScene3D、CarlaSC 的真实场景图像。
    *   使用 Flux.1 和 ChatGPT (DALL-E 3) 生成的合成场景图像。
*   **对比基准（Benchmarks）**：
    *   **物体中心模型**：Trellis, Hunyuan3D-2.1。
    *   **场景生成模型**：EvoScene, SynCity。
*   **评估指标**：
    *   **外观**：LPIPS, SSIM, PSNR。
    *   **几何**：倒角距离（CD）, F-score。
    *   **人类偏好**：针对几何、忠实度、外观、完整性进行打分。
    *   **文本一致性**：CLIP score, HPSv3。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA RTX Pro 6000 GPU。
*   **显存占用**：
    *   在扩展因子 $a=b=2$（即 4 倍面积）时，开启优化需 **28GB VRAM**，关闭优化需 14GB。
    *   在更大规模（$a=b=6$）时，显存需求显著增加（关闭优化时约 61GB）。
*   **耗时**：对于典型配置（$a=b=2, d=4$），单次生成约需 **14.1 分钟**。相比之下，Trellis 仅需 0.06 分钟，但 Extend3D 远快于 SynCity（52 分钟）和 EvoScene（35 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   定量评估涵盖了 100 张多样化场景图像。
    *   几何评估使用了 UrbanScene3D 的 45 对图像与真值网格。
    *   人类偏好调查涉及 10 名参与者对 14 组结果的评估。
*   **充分性**：论文进行了详尽的消融实验，验证了重叠补丁因子 $d$、初始化策略、欠加噪比例以及两种优化损失的有效性。实验设计客观，对比了当前最先进的开源模型，覆盖了从室内到城镇规模的多种场景。

### 6. 主要结论与发现
*   **扩展性**：通过扩展潜空间，Extend3D 能够生成比原始模型大 36 倍甚至更多的 3D 场景，且保持细节清晰。
*   **欠加噪的有效性**：证明了将结构不完整性视为噪声并进行“欠加噪”处理，是解决单目深度估计遮挡问题的有效手段。
*   **优化必要性**：实时优化能有效纠正物体中心模型在处理子场景时的动力学偏差，防止几何崩溃。

### 7. 优点
*   **免训练**：无需昂贵的 3D 场景数据训练，直接复用现有的物体生成模型。
*   **高保真度**：通过重叠耦合和实时优化，解决了大场景生成中的接缝和模糊问题。
*   **通用性强**：不仅限于城镇，也适用于室内、桌面等多种尺度的场景。

### 8. 不足与局限
*   **遮挡补全不完美**：在某些极端复杂的室内场景中，遮挡区域的补全仍可能不完整。
*   **计算开销**：虽然比同类场景模型快，但相比基础物体模型，其显存和时间开销依然较大，限制了在消费级显卡上的超大规模生成。
*   **视角限制**：在处理具有强烈透视变形的街景图像（Street-level）时，由于 $x, y$ 坐标尺度失衡，表现受限。

（完）

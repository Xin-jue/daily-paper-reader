---
title: Posterior Optimization with Clipped Objective for Bridging Efficiency and Stability in Generative Policy Learning
title_zh: 采用截断目标的后验优化：衔接生成式策略学习中的效率与稳定性
authors: "Yuhui Chen, Haoran Li, Zhennan Jiang, Yuxing Qin, Yuxuan Wan, Weiheng Liu, Dongbin Zhao"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01860v1"
tags: ["query:q2"]
score: 9.0
evidence: 针对机器人操作的生成式策略学习，采用时间动作块
tldr: 针对生成式策略在强化学习微调中存在的稳定性差和采样效率低的问题，本文提出了POCO框架。该框架将策略改进建模为动作块的后验推理问题，通过期望最大化（EM）过程将奖励加权的隐式后验蒸馏至策略中，无需似然估计。POCO支持从离线到在线的平滑过渡，并能无缝扩展至大型VLA模型，在多项仿真和真实机器人任务中显著提升了成功率并防止了策略崩溃。
selection_source: fresh_fetch
motivation: 现有的生成式机器人策略在通过强化学习进行微调时，面临严重的训练不稳定性和样本效率低下的挑战。
method: 提出POCO框架，利用动作块的后验推理和剪切目标函数，通过EM过程将奖励加权的分布蒸馏到策略中，且无需似然估计。
result: "在7个仿真基准和4个真实世界任务中表现优于现有SOTA方法，在真实任务中达到了96.7%的成功率并有效防止了策略崩溃。"
conclusion: POCO为生成式策略提供了一种高效且稳定的强化学习微调方案，具有良好的模型无关性和扩展性。
---

## 摘要
表达能力强的生成模型通过捕捉长时程轨迹上的复杂、多模态动作分布，推动了机器人操控的发展。然而，由于不稳定性和样本效率低下，通过强化学习（RL）微调这些策略仍然具有挑战性。我们提出了采用截断目标的后验优化（POCO），这是一个原则性的强化学习框架，它将策略改进公式化为针对时序动作块（temporal action chunks）定制的后验推理问题。通过期望最大化（EM）过程，POCO 在无需似然估计的情况下，将奖励加权的隐式后验蒸馏到策略中。此外，POCO 采用了一种从离线到在线的范式，将在线探索锚定在预训练先验上，其模型无关的设计使其能够在不修改架构的情况下微调大型视觉-语言-动作（VLA）模型。在 7 个仿真基准测试和 4 个具有丰富接触的真实世界任务上的评估表明，POCO 有效防止了灾难性的策略崩溃，优于现有最先进的基准方法，并在真实世界任务中实现了 96.7% 的成功率。视频可在项目网站 https://cccedric.github.io/poco/ 获取。

## Abstract
Expressive generative models have advanced robotic manipulation by capturing complex, multi-modal action distributions over temporally extended trajectories. However, fine-tuning these policies via RL remains challenging due to instability and sample inefficiency. We introduce Posterior Optimization with Clipped Objective (POCO), a principled RL framework that formulates policy improvement as a posterior inference problem tailored for temporal action chunks. Through an Expectation-Maximization procedure, POCO distills a reward-weighted implicit posterior into the policy without likelihood estimation. Furthermore, POCO adopts an offline-to-online paradigm that anchors online exploration to pre-trained priors, and its model-agnostic design scales to fine-tune large VLA models without architectural modifications. Evaluations across 7 simulation benchmarks and 4 contact-rich real-world tasks demonstrate that POCO prevents catastrophic policy collapse, outperforms SOTA baselines, and achieves a 96.7% success rate on real-world tasks. Videos are available at our project website https://cccedric.github.io/poco/.

---

## 论文详细总结（自动生成）

这是一份关于论文《Posterior Optimization with Clipped Objective for Bridging Efficiency and Stability in Generative Policy Learning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着生成式模型（如扩散模型、流匹配模型）和大型视觉-语言-动作（VLA）模型在机器人操控领域的普及，如何通过强化学习（RL）对这些预训练策略进行微调成为了关键。然而，现有的 RL 微调方法面临**稳定性与效率的权衡难题**：
*   **离外策略（Off-policy）方法**：虽然采样效率高，但直接反向传播噪声较大的 Q 梯度会导致策略在处理高维动作块时出现严重的分布外（OOD）价值高估，引发“灾难性策略崩溃”。
*   **在策略（On-policy）方法**：虽然通过置信区域更新保证了稳定性，但在现实世界中采样效率极低，难以实用。
*   **核心目标**：提出一种既能保持离外策略的高效率，又能像在策略方法一样稳定的微调框架。

### 2. 方法论：核心思想与关键技术
POCO 将策略改进建模为一个**后验推理问题**，通过期望最大化（E-M）过程进行优化：
*   **动作块后验推理**：不同于单步推理，POCO 针对“时间动作块”（Action Chunks）进行推理，这符合现代机器人策略的输出结构，能减少执行抖动并加速稀疏奖励的回传。
*   **隐式 E 步（Implicit E-step）**：从当前策略中采样多个候选动作块，根据块级 Q 函数计算重要性权重。这种方式无需显式的似然估计（Likelihood-free），因此适用于流匹配等无法直接计算似然的模型。
*   **带截断目标的 M 步（M-step with Clipped Objective）**：
    *   将加权后的样本通过监督学习目标蒸馏回策略网络。
    *   **关键创新**：引入了**截断代理目标（Clipped Surrogate Objective）**。通过限制更新步长，防止因 Q 值高估导致的剧烈参数波动，从而在数学上强制执行了置信区域约束，确保策略不会偏离预训练的先验太远。
*   **离线到在线范式**：先在人类演示数据上进行行为克隆（BC）预训练，再通过在线交互微调，并包含一个 Critic 预热阶段以稳定价值估计。

### 3. 实验设计
*   **仿真环境**：使用了 7 个具有挑战性的稀疏奖励任务，包括 OGBench（scene, puzzle-3x3, cube-double/triple）和 RoboMimic（lift, can, square）。
*   **真实世界场景**：4 个接触密集型任务（拾取方块、布线、插入 USB、组装 SSD）以及 2 个用于验证 VLA 扩展性的任务（捡笔、挂钥匙扣）。
*   **对比基准（Baselines）**：
    *   **离外策略**：RLPD, QC, FQL。
    *   **在策略**：DPPO, ReinFlow。
    *   **其他**：DSRL（噪声引导）、BC（纯监督学习）。

### 4. 资源与算力
*   **硬件设施**：使用了高性能云服务器，配备 **8 张 NVIDIA H20 GPU** 用于策略训练和推理。
*   **系统架构**：采用分布式架构，本地计算机负责机器人接口和数据流，云端负责计算密集型任务。
*   **控制频率**：硬件控制器运行频率为 15Hz。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础空间推理到复杂接触操控的多种任务。
*   **充分性**：
    *   仿真实验均采用 **5 个随机种子**取平均值，并提供了学习曲线。
    *   在对比在策略方法时，通过并行环境和归一化训练步数确保了**公平性**。
    *   **消融实验**：详细分析了截断阈值 $\zeta$ 和后验引导比例 $\beta$ 对性能的影响，验证了各组件的必要性。
    *   **真实机器人验证**：每个任务进行了 30 次测试，总计进行了数百次真实的物理尝试，实验结果具有统计说服力。

### 6. 主要结论与发现
*   **性能卓越**：POCO 在所有仿真和真实任务中均达到或超过了 SOTA 水平，真实世界平均成功率达 **96.7%**。
*   **稳定性强**：在离线到在线的转换过程中，POCO 有效防止了性能骤降（策略崩溃），而 FQL 等方法在相同条件下常出现崩溃。
*   **扩展性好**：POCO 可以直接应用于微调拥有数十亿参数的 VLA 模型（如 $\pi_{0.5}$ 和 GR00T-N1），显著提升了这些基础模型在特定下游任务中的表现。

### 7. 优点
*   **理论完备**：将 RL 转化为推理问题，为生成式策略的稳定更新提供了坚实的数学基础。
*   **无需似然估计**：解决了扩散模型和流匹配模型在 RL 中难以计算精确似然的痛点。
*   **动作块机制**：通过块级优化缓解了长时程信用分配问题，提高了学习效率。
*   **模型无关**：不改变原有模型架构，具有极强的通用性。

### 8. 不足与局限
*   **奖励依赖**：虽然支持稀疏奖励，但在真实世界中仍需要人工通过按键提供成功/失败的反馈，尚未实现完全自动化的奖励获取。
*   **探索策略**：目前主要依赖随机探索，未来若能结合更结构化的先验驱动探索，采样效率可能进一步提升。
*   **Critic 准确性**：算法性能高度依赖于 Critic 网络对价值的准确估计，在极端复杂的动态环境下，Critic 的收敛可能成为瓶颈。

（完）

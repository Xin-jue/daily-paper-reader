---
title: "AnchorVLA: Anchored Diffusion for Efficient End-to-End Mobile Manipulation"
title_zh: AnchorVLA：用于高效端到端移动操作的锚定扩散模型
authors: "Jia Syuen Lim, Zhizhen Zhang, Peter Bohm, Brendan Tidd, Zi Huang, Yadan Luo"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01567v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于移动操作的基于扩散模型的VLA策略
tldr: 针对移动操作中扩散策略推理慢和动作分块导致的漂移问题，本文提出 AnchorVLA。它结合了轻量级 VLA 主干和锚定扩散动作头，通过在锚定轨迹附近进行局部截断去噪来减少推理开销，并引入残差修正模块实现高频自纠偏。该方法在保持多模态动作生成能力的同时，显著提升了闭环控制的效率和在复杂环境下的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的扩散策略在移动操作中面临推理延迟高和动作分块导致的累积漂移挑战，难以兼顾多模态建模与实时反应。
method: 提出一种锚定扩散机制，在锚定轨迹附近进行局部去噪以降低计算量，并结合轻量级残差模块进行每步的高频动作修正。
result: 在多种移动操作任务中，AnchorVLA 在面对干扰和分布偏移时表现出更高的成功率、更强的稳定性和更低的推理延迟。
conclusion: 通过局部去噪和实时纠偏机制，AnchorVLA 有效解决了扩散策略在高效闭环控制中的性能与效率平衡问题。
---

## 摘要
移动操作中的一个核心挑战是在执行过程中保持反应性的同时，保留多种合理的动作模型。在杂乱场景中，抓取瓶子通常有多种有效路径。鲁棒的行为取决于在场景演变时保留这种动作多样性并保持实时反应。扩散策略（Diffusion policies）因其能够建模多模态动作分布而非坍缩至单一解而备受关注。但在实践中，完整的迭代去噪在控制时的计算成本很高。动作分块（Action chunking）虽有助于分摊推理开销，但也会导致部分开环行为，使得微小误差累积成漂移。我们提出了 AnchorVLA，这是一种基于扩散的移动操作 VLA 策略。其核心见解是：当采样从合理候选解流形附近开始时，无需进行大量的去噪即可恢复多模态的有效动作。AnchorVLA 将轻量级 VLA 适配主干网络与锚定扩散动作头相结合，利用截断扩散调度在锚定轨迹周围进行局部去噪。这在保留多模态动作生成能力的同时，降低了闭环控制的推理成本。关键在于，为减轻分块诱发的漂移，我们通过轻量级残差修正模块引入了测试时自校正机制，在执行过程中进行高频的逐步调整。在多种移动操作任务中，AnchorVLA 在保持低延迟推理的同时，显著提升了在扰动和分布偏移下的成功率与稳定性。源代码已开源至 https://github.com/jason-lim26/AnchorVLA。

## Abstract
A central challenge in mobile manipulation is preserving multiple plausible action models while remaining reactive during execution. A bottle in a cluttered scene can often be approached and grasped in multiple valid ways. Robust behavior depends on preserving this action diversity while remaining reactive as the scene evolves. Diffusion policies are appealing because they model multimodal action distributions rather than collapsing to one solution. But in practice, full iterative denoising is costly at control time. Action chunking helps amortize inference, yet it also creates partially open-loop behavior, allowing small mismatches to accumulate into drift. We present AnchorVLA, a diffusion-based VLA policy for mobile manipulation built on the core insight that when sampling begins near a plausible solution manifold, extensive denoising is unnecessary to recover multimodal, valid actions. AnchorVLA combines a lightweight VLA adaptation backbone with an anchored diffusion action head, which denoises locally around anchor trajectories using a truncated diffusion schedule. This retains multimodal action generation while reducing inference cost for closed-loop control. Crucially, to mitigate chunking-induced drift, we introduce a test-time self-correction mechanism via a lightweight residual correction module that makes high-frequency, per-step adjustments during rollout. Across diverse mobile manipulation tasks, AnchorVLA improves success and stability under disturbances and distribution shifts while maintaining low-latency inference. The source code is made available at https://github.com/jason-lim26/AnchorVLA.

---

## 论文详细总结（自动生成）

### AnchorVLA：用于高效端到端移动操作的锚定扩散模型论文总结

#### 1. 核心问题与研究动机
*   **核心挑战**：移动操作（Mobile Manipulation）要求机器人同时处理高层语义推理、长程导航和复杂的物理交互。
*   **多模态困境**：同一指令在复杂场景下有多种有效执行路径（多模态）。传统的 VLA 模型（如基于 L1 回归的模型）容易产生“模式平均”现象，导致输出无效动作；而扩散策略（Diffusion Policy）虽能建模多模态，但推理成本高且在“动作分块（Action Chunking）”执行时容易产生累积漂移。
*   **研究动机**：开发一种既能捕捉多模态分布、又具备高推理效率，且能对抗分块执行诱发漂移的端到端 VLA 策略。

#### 2. 方法论：核心思想与技术细节
AnchorVLA 由轻量级 VLA 适配主干、锚定扩散动作头和残差修正模块组成：
*   **锚定截断扩散（Anchored Truncated Diffusion）**：
    *   **轨迹词汇表**：通过对专家演示进行聚类，构建代表性动作模式的“锚定轨迹（Anchors）”。
    *   **局部去噪**：不再从纯高斯噪声开始去噪，而是从锚定轨迹附近开始，采用截断的扩散调度（Truncated Schedule）。这显著缩短了去噪步数（如从 50 步降至 10 步），降低了延迟。
*   **锚定评分头（Anchor Scoring Head）**：
    *   同时生成多个基于不同锚点的候选轨迹，利用评分头根据当前视觉上下文和指令预测每个轨迹的置信度，选择最优路径。
*   **测试时自校正（Test-Time Self-Correction）**：
    *   **残差修正模块**：引入一个轻量级 MLP，在动作块执行期间进行高频（每步）的微调。它根据瞬时状态偏差预测修正量 $\Delta a$，将偏离的执行拉回可行流形，缓解开环执行带来的漂移。

#### 3. 实验设计
*   **仿真场景**：使用 **ManiSkill-HAB** 基准测试，包含 6 类任务（苹果/碗的抓取与放置、开冰箱、关柜门），涉及 ReplicaCAD 真实扫描环境。
*   **真实世界**：部署在 **Unitree Go2 宇树四足机器人**上，配备 SO101 机械臂。测试任务包括开抽屉和提包，面临四足底盘振动和漂移的严苛挑战。
*   **对比基线**：
    *   基础模仿学习：ACT, Diffusion Policy (DP)。
    *   3D/大模型：DP3, $\pi_0$, RDT。
    *   移动操作 SOTA：AC-DiT（基于点云的扩散 Transformer）。

#### 4. 资源与算力
*   **模型规模**：主干网络采用 **Qwen-2.5 (0.5B)**，使用 LoRA（秩为 64）进行微调。
*   **训练细节**：进行了 100k 次梯度步迭代，Batch Size 为 16，学习率为 $2 \times 10^{-4}$。
*   **算力说明**：文中未明确提及具体的 GPU 型号和数量，但提到通过动作分块（$H=5$）相比频繁查询（$H=1$）减少了约 **80% 的计算量**（从 641.4 TFLOPs 降至 128.3 TFLOPs）。

#### 5. 实验数量与充分性
*   **实验规模**：在仿真中每个任务进行 3 次独立试验，每轮 100 个未知场景测试；真实世界中每个任务进行 10 次随机初始化的测试。
*   **消融实验**：针对“锚定先验的必要性”、“残差修正模块的效果”以及“动作块长度 $H$ 的影响”进行了详细消融。
*   **充分性评价**：实验设计较为全面，涵盖了从 2D RGB 到 3D 点云输入的多种基线对比，且同时包含仿真与真机验证，客观地展示了模型在效率与鲁棒性之间的平衡。

#### 6. 主要结论与发现
*   **性能领先**：在 ManiSkill-HAB 上达到 **64.0%** 的平均成功率，优于最强的 3D 基线 AC-DiT（55.6%）和 RGB 基线 RDT（42.9%）。
*   **高效稳定**：即使在较长的动作分块（$H=5$）下，AnchorVLA 仍能保持 61.5% 的高成功率，而传统 L1 回归模型在 $H$ 增加时性能会剧烈下降。
*   **推理加速**：通过锚定截断机制，在保持性能的前提下实现了 **1.48 倍**的推理频率提升（从 60.6 Hz 升至 89.8 Hz）。

#### 7. 优点与亮点
*   **创新的锚定机制**：首次将自动驾驶领域的截断扩散思想引入机器人移动操作，解决了扩散模型推理慢的痛点。
*   **双层控制架构**：将“高层多模态规划（扩散头）”与“低层高频修正（残差模块）”解耦，既保留了智能性又增强了物理执行的稳定性。
*   **计算友好**：通过长动作块执行和轻量级适配器，显著降低了对大语言模型主干的查询频率，适合移动平台部署。

#### 8. 不足与局限
*   **数据依赖**：锚定轨迹词汇表依赖于离线专家演示的覆盖范围，对于超出分布（OOD）的极端场景，预设的锚点可能失效。
*   **修正上限**：残差修正模块虽然能处理微小漂移，但无法纠正根本性的规划错误（如完全走错方向）。
*   **泛化性验证**：虽然在 ManiSkill-HAB 表现优异，但对于更广阔、更多样化的开放世界环境，其锚点聚类的有效性仍需进一步验证。

（完）

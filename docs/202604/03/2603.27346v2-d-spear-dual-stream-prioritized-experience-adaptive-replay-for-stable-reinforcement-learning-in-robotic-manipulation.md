---
title: "D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay for Stable Reinforcement Learning in Robotic Manipulation"
title_zh: D-SPEAR：用于机器人操作中稳定强化学习的双流优先经验自适应回放
authors: "Yu Zhang, Karl Mason"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27346v2"
tags: ["query:q2"]
score: 9.0
evidence: 机器人操作任务的强化学习
tldr: 针对机器人操作中强化学习训练不稳定和策略震荡问题，本文提出D-SPEAR框架。该框架通过解耦Actor和Critic的采样策略，让Critic利用优先经验回放提高学习效率，而Actor利用低误差样本稳定策略优化。结合自适应锚点机制和Huber损失，D-SPEAR在robosuite基准测试中显著提升了SAC和TD3等算法的性能与稳定性。
selection_source: fresh_fetch
motivation: 现有的离线策略强化学习算法忽略了Actor和Critic对数据需求的不同，导致在复杂机器人任务中容易出现策略震荡和性能崩溃。
method: 提出双流经验回放框架，Critic采用优先级采样，Actor采用低误差样本采样，并引入自适应锚点机制平衡采样分布。
result: 在Block-Lifting和Door-Opening等机器人操作任务中，D-SPEAR在最终性能和训练稳定性上均优于SAC、TD3和DDPG等基准算法。
conclusion: 解耦Actor与Critic的采样策略并结合自适应调整机制，能有效解决复杂动力学环境下的强化学习训练不稳定问题。
---

## 摘要
由于丰富的接触动力学、长时程以及训练的不稳定性，机器人操作对于强化学习而言仍具挑战性。尽管 SAC 和 TD3 等离策（off-policy）演员-评论家算法在仿真中表现良好，但在现实设置中往往会遭遇策略振荡和性能崩溃，部分原因在于经验回放策略忽略了演员和评论家对数据的不同需求。我们提出了 D-SPEAR：双流优先经验自适应回放，这是一种在维持共享经验回放池的同时，解耦演员和评论家采样的回放框架。评论家利用优先回放进行高效的价值学习，而演员则使用低误差样本进行更新，以稳定策略优化。一种自适应锚点机制根据 TD 误差的变异系数平衡均匀采样与优先采样，而基于 Huber 损失的评论家目标函数进一步提高了在异构奖励尺度下的鲁棒性。我们在 robosuite 基准测试中具有挑战性的机器人操作任务（包括抬块和开门）上评估了 D-SPEAR。结果表明，D-SPEAR 在最终性能和训练稳定性方面均持续优于 SAC、TD3 和 DDPG 等强力离策基准算法，消融实验证实了演员端和评论家端回放流的互补作用。

## Abstract
Robotic manipulation remains challenging for reinforcement learning due to contact-rich dynamics, long horizons, and training instability. Although off-policy actor-critic algorithms such as SAC and TD3 perform well in simulation, they often suffer from policy oscillations and performance collapse in realistic settings, partly due to experience replay strategies that ignore the differing data requirements of the actor and the critic. We propose D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay, a replay framework that decouples actor and critic sampling while maintaining a shared replay buffer. The critic leverages prioritized replay for efficient value learning, whereas the actor is updated using low-error transitions to stabilize policy optimization. An adaptive anchor mechanism balances uniform and prioritized sampling based on the coefficient of variation of TD errors, and a Huber-based critic objective further improves robustness under heterogeneous reward scales. We evaluate D-SPEAR on challenging robotic manipulation tasks from the robosuite benchmark, including Block-Lifting and Door-Opening. Results demonstrate that D-SPEAR consistently outperforms strong off-policy baselines, including SAC, TD3, and DDPG, in both final performance and training stability, with ablation studies confirming the complementary roles of the actorside and critic-side replay streams.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **D-SPEAR** 的新型强化学习经验回放框架，旨在解决机器人操作任务中训练不稳定和策略崩溃的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在处理具有复杂接触动力学（Contact-rich）和长时程（Long-horizon）的机器人任务时，传统的离策（Off-policy）演员-评论家（Actor-Critic）算法（如 SAC、TD3）容易出现策略震荡和后期性能崩溃。
*   **研究动机**：作者发现 Actor 和 Critic 对数据的需求存在“不匹配”。Critic 需要高 TD 误差的样本来快速修正价值估计，但 Actor 如果使用这些高误差样本产生的梯度进行更新，会导致策略优化极不稳定。现有的经验回放（如 PER）对两者一视同仁，忽略了这种需求差异。

### 2. 方法论：D-SPEAR 框架
D-SPEAR 的核心思想是**解耦 Actor 和 Critic 的采样策略**，同时维持一个共享的经验池。其关键技术包括：
*   **双流采样（Dual-Stream Replay）**：
    *   **评论家流（Critic Stream）**：采用标准的优先级采样，倾向于选择高 TD 误差样本，以加速价值函数的收敛。
    *   **演员流（Actor Stream）**：采用“逆优先级”采样，倾向于选择低 TD 误差的稳定样本，确保策略更新基于相对准确的价值估计，从而减少梯度噪声。
*   **自适应锚点机制（Adaptive Anchor Mechanism）**：
    *   引入一个均匀采样的“锚点集”来保证数据的覆盖率。
    *   **动态调整**：通过计算经验池中 TD 误差的**变异系数（CV）**来自动调整均匀采样比例 $\lambda$。当误差分布差异大时，增加优先级采样；当误差趋于一致时，增加均匀采样。
*   **鲁棒评论家目标（Huber-based Critic Objective）**：
    *   将 Critic 的损失函数从均方误差（MSE）替换为 **Huber Loss**。这使得模型对机器人碰撞产生的异常奖励或极端 TD 误差具有更强的鲁棒性。

### 3. 实验设计
*   **实验场景**：基于 `robosuite` 仿真基准测试，使用了 7 自由度的 Franka Panda 机器人。
*   **具体任务**：
    1.  **Lift（抬块）**：要求机器人抓取并提升物体，涉及精确的接触控制。
    2.  **Door（开门）**：涉及与铰链结构的持续交互，具有复杂的关节动力学。
*   **对比基准（Benchmarks）**：SAC（基准算法）、TD3、DDPG。
*   **评估指标**：每回合奖励（Episode Return）随训练步数的变化曲线，以及跨随机种子的训练稳定性（方差）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **超参数信息**：文中列出了详细的超参数表，包括网络结构（2层 MLP，每层 256 单元）、Batch Size（256）、总训练步数（2.5 × 10⁵ 步）等。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个核心任务上进行了完整的性能对比。
    *   每个实验均使用了 **5 个不同的随机种子**，并绘制了标准差阴影区域，符合强化学习实验的常规标准。
    *   **消融实验**：分别验证了“无双流结构”、“无 Actor 低误差流”、“无 Critic 高误差流”的情况。
*   **充分性评价**：实验设计较为客观，消融实验充分证明了双流机制中各部分的互补性。虽然任务数量（2个）不算极多，但涵盖了机器人操作中最具代表性的接触和长时程挑战。

### 6. 主要结论与发现
*   **性能领先**：D-SPEAR 在最终奖励值上显著优于 SAC、TD3 和 DDPG。在 Lift 任务中，其最终得分几乎是 SAC 的两倍。
*   **稳定性提升**：相比于基准算法在训练后期的剧烈震荡或性能骤降，D-SPEAR 的学习曲线更加平滑且单调上升。
*   **解耦的必要性**：消融实验表明，仅靠 Critic 端的优先级采样或仅靠 Actor 端的稳定采样都无法达到最佳效果，两者的解耦配合是关键。

### 7. 优点与亮点
*   **理论直觉清晰**：敏锐捕捉到了 Actor-Critic 框架中采样需求的内在矛盾，并给出了简洁的工程解决方案。
*   **自适应性强**：通过 CV 动态调整锚点比例，减少了人工调参的工作量，增强了算法在不同阶段的灵活性。
*   **即插即用**：该框架可以轻松集成到现有的各种离策演员-评论家算法中。

### 8. 不足与局限
*   **实验覆盖面**：虽然在 `robosuite` 上表现良好，但未在更多样化的环境（如 Meta-World 或真实机器人）中进行验证。
*   **计算开销**：双流采样意味着在每次更新时需要进行两次不同的采样计算，虽然文中未提及，但这可能会略微增加训练时的计算负担。
*   **对比深度**：未与近年来其他改进 PER 的变体（如针对 Actor-Critic 优化的其他采样策略）进行直接对比。

（完）

---
title: Precise Robot Command Understanding Using Grammar-Constrained Large Language Models
title_zh: 基于语法约束大语言模型的精确机器人指令理解
authors: "Xinyun Huo, Raghav Gnanasambandam, Xinyao Zhang"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04233v1"
tags: ["query:q2"]
score: 8.0
evidence: 语法约束的LLM用于精确机器人指令理解
tldr: 针对工业人机协作中大语言模型生成指令不够精确的问题，本文提出一种语法约束的LLM框架。该方法结合微调LLM的推理能力与语法驱动的NLU系统，通过两阶段处理和自动纠错反馈机制，将自然语言转化为标准化的机器人可读JSON格式。在HuRIC数据集上的实验证明，该方法显著提升了指令的有效性与系统鲁棒性。
selection_source: fresh_fetch
motivation: 通用大语言模型在处理工业机器人指令时缺乏必要的严谨性和确定性，难以保证生成的命令安全且可执行。
method: 集成微调LLM与结构化语言模型，利用语法解析器强制约束输出格式，并通过验证反馈循环实现错误的自动修正。
result: 在HuRIC数据集上的评估表明，该混合模型在指令有效性上优于API驱动的微调LLM和传统的语法驱动模型。
conclusion: 通过语法约束引导LLM输出，能够兼顾语言灵活性与工业指令的精确性，为安全高效的人机协作提供了新方案。
---

## 摘要
工业环境中的人机协作需要精确且可靠的通信，以提高运营效率。虽然大语言模型（LLMs）能够理解通用语言，但它们往往缺乏安全且可执行的工业指令所需的领域特定严谨性。为填补这一空白，本文介绍了一种新型的语法约束 LLM，它将语法驱动的自然语言理解（NLU）系统与微调后的 LLM 相结合，从而兼具对话灵活性和机器人技术所需的确定性精度。我们的方法采用两阶段过程：首先，微调后的 LLM 对自然语言输入进行高层上下文推理和参数推断；其次，结构化语言模型（SLM）和基于语法的规范化器约束 LLM 的输出，强制其转换为由有效动作帧和指令元素组成的标准化符号格式。该过程保证了生成的指令是有效的，并以机器人可读的 JSON 格式进行结构化。该模型的一个关键特征是验证和反馈循环。语法解析器根据预定义的机器人可执行动作列表对输出进行验证。如果指令无效，系统会自动生成纠错提示并重新调用 LLM。这种迭代自纠错机制使模型能够从初始解释错误中恢复，从而提高系统的鲁棒性。我们将语法约束混合模型与两个基准模型（基于 API 的微调 LLM 和独立的语法驱动 NLU 模型）进行了对比评估。通过使用人机交互语料库（HuRIC）数据集，我们证明了该混合方法实现了卓越的指令有效性，促进了更安全、更高效的工业人机协作。

## Abstract
Human-robot collaboration in industrial settings requires precise and reliable communication to enhance operational efficiency. While Large Language Models (LLMs) understand general language, they often lack the domain-specific rigidity needed for safe and executable industrial commands. To address this gap, this paper introduces a novel grammar-constrained LLM that integrates a grammar-driven Natural Language Understanding (NLU) system with a fine-tuned LLM, which enables both conversational flexibility and the deterministic precision required in robotics. Our method employs a two-stage process. First, a fine-tuned LLM performs high-level contextual reasoning and parameter inference on natural language inputs. Second, a Structured Language Model (SLM) and a grammar-based canonicalizer constrain the LLM's output, forcing it into a standardized symbolic format composed of valid action frames and command elements. This process guarantees that generated commands are valid and structured in a robot-readable JSON format. A key feature of the proposed model is a validation and feedback loop. A grammar parser validates the output against a predefined list of executable robotic actions. If a command is invalid, the system automatically generates corrective prompts and re-engages the LLM. This iterative self-correction mechanism allows the model to recover from initial interpretation errors to improve system robustness. We evaluate our grammar-constrained hybrid model against two baselines: a fine-tuned API-based LLM and a standalone grammar-driven NLU model. Using the Human Robot Interaction Corpus (HuRIC) dataset, we demonstrate that the hybrid approach achieves superior command validity, which promotes safer and more effective industrial human-robot collaboration.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合大语言模型（LLM）灵活性与形式语法严谨性的混合框架，旨在解决工业机器人指令理解中的精确性与鲁棒性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：在工业人机协作（HRC）中，机器人需要精确执行指令。通用 LLM 虽然具备强大的自然语言理解能力，但其输出具有概率性，往往缺乏工业场景所需的确定性格式（如特定的 JSON 结构）和领域严谨性，容易产生不可执行的指令。
*   **核心问题**：如何既保留 LLM 处理复杂、多样化自然语言的灵活性，又确保其生成的指令符合机器人控制系统的严格语法规范和安全要求。

### 2. 方法论
论文提出了一种**两阶段处理架构**，并引入了**反馈纠错循环**：
*   **核心思想**：将语义推理与结构约束分离。第一阶段利用 LLM 进行语义提取，第二阶段利用形式语法进行强制规范化。
*   **关键技术细节**：
    *   **语义前端**：采用 **Meta-Llama-3-8B-Instruct** 模型，通过**提示工程（Prompt Engineering）**定义系统角色，并使用 **LoRA（低秩自适应）** 技术在特定数据集上进行微调，以增强其提取动作帧（Action Frames）和语义元素（Elements）的能力。
    *   **语法约束层**：使用 **Lark 库** 构建基于 **EBNF（扩展巴科斯范式）** 的语法解析器。该层将 LLM 的原始输出映射到预定义的机器人动作本体中，强制转换为标准 JSON 格式。
    *   **反馈纠错循环**：如果语法解析器检测到输出不符合规范（如动作名错误或缺少必要参数），系统会捕获错误信息并自动生成“反馈提示词（Feedback Prompt）”，引导 LLM 进行自我修正，直到生成有效指令或达到迭代上限。
*   **算法流程**：输入自然语言 -> 微调 LLM 推理 -> 语法解析验证 -> [若失败：生成错误报告 -> 重新提示 LLM] -> 输出标准化 JSON 指令。

### 3. 实验设计
*   **数据集**：使用了 **HuRIC（Human Robot Interaction Corpus）** 数据集，包含 656 条自然语言指令，涵盖 16 种不同的机器人动作帧（如 Taking, Bringing 等）。
*   **对比方法（Benchmarks）**：
    1.  **API-based LLM w/o Fine-tuning**：未微调的原始 Llama-3 模型。
    2.  **API-based LLM w/ Fine-tuning**：仅经过 LoRA 微调的 Llama-3 模型。
    3.  **Grammar-based NLU**：纯基于规则和语法的传统自然语言理解模型。
    4.  **Hybrid Grammar-based LLM**：本文提出的混合模型（微调 LLM + 语法约束 + 反馈循环）。
*   **评价指标**：
    *   **JSON Exact Match (EM)**：预测的 JSON 结构与参考答案完全一致的比例。
    *   **JSON Similarity**：基于 Jaccard 指数计算预测值与参考值在键值对上的重合度。

### 4. 资源与算力
*   **硬件环境**：本地工作站，配置为 AMD Ryzen 7 5800X CPU，32 GB DDR4 RAM。
*   **GPU 算力**：单块 **NVIDIA RTX 3080 (10 GB VRAM)**。
*   **软件环境**：Ubuntu 24.04 (WSL 2), PyTorch 2.3, Hugging Face Transformers 库。
*   **训练细节**：文中未明确给出具体的训练时长，但提到使用了 PEFT（参数高效微调）框架进行 LoRA 训练。

### 5. 实验数量与充分性
*   **实验规模**：数据集按 90/10 比例划分，测试集包含 66 个样本。
*   **充分性评价**：
    *   **客观性**：通过四个维度的对比（无微调、有微调、纯语法、混合模型）展示了各组件的贡献，实验设计较为合理。
    *   **局限性**：测试样本量（66 条）相对较小，且仅针对单一基座模型（Llama-3-8B）进行了验证，未在更大规模或不同架构的模型上进行交叉验证。

### 6. 主要结论与发现
*   **混合模型表现最优**：混合模型实现了 **48.48% 的精确匹配率（EM）** 和 **88.07% 的 JSON 相似度**，显著高于纯微调 LLM（EM 36.4%）和纯语法模型（EM 21.21%）。
*   **语义与结构的平衡**：纯 LLM 容易产生对话式废话，而纯语法模型对输入变体极其敏感（容错率低）。混合模型通过 LLM 提取语义、语法层强制格式，有效平衡了两者。
*   **反馈机制的价值**：反馈循环显著降低了失败输出的比例，提高了系统在面对复杂或模糊指令时的鲁棒性。

### 7. 优点与亮点
*   **解耦设计**：将灵活的语义理解与刚性的结构约束解耦，既利用了 LLM 的泛化能力，又保证了工业级的确定性。
*   **自纠错能力**：引入反馈提示机制，使系统具备一定的“自愈”能力，减少了人工干预。
*   **实用性强**：输出直接对接机器人可读的 JSON 格式，具有很强的工程落地价值。

### 8. 不足与局限
*   **数据集局限**：HuRIC 数据集包含较多疑问句和礼貌用语，这有时会误导 LLM 进入“对话模式”而非“指令模式”。
*   **语法歧义**：当所有动作规则合并为一个统一语法时，随着动作种类的增加，可能会出现结构歧义，导致精确匹配率下降。
*   **延迟问题**：反馈纠错循环意味着可能需要多次调用 LLM，这在对实时性要求极高的工业场景中可能会带来延迟挑战。
*   **模型单一**：实验仅基于 Llama-3-8B，未探讨该方法在更小规模模型（如 1B/3B）上的表现，而小模型在工业边缘侧部署更具优势。

（完）

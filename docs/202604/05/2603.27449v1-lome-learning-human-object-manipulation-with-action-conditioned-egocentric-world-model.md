---
title: "LOME: Learning Human-Object Manipulation with Action-Conditioned Egocentric World Model"
title_zh: LOME：基于动作条件第一人称视角世界模型的人-物交互操作学习
authors: "Quankai Gao, Jiawei Yang, Qiangeng Xu, Le Chen, Yue Wang"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27449v1"
tags: ["query:q13"]
score: 9.0
evidence: 动作条件下的具身操作世界模型
tldr: LOME是一个以动作为条件的自我中心世界模型，旨在解决复杂的人机交互视频生成难题。它通过结合输入图像、文本提示和逐帧人体动作（含手势），生成具有高度动作一致性和物理真实感的人机交互视频。相比传统物理模拟，LOME具有更强的泛化能力，能模拟如倒水等复杂的物理后果，为AR/VR和机器人训练提供了无需显式3D建模的新路径。
selection_source: fresh_fetch
motivation: 传统基于物理的动画方法在处理精细且接触丰富的人机交互时建模复杂、泛化性差，难以适应真实世界环境。
method: 通过在预训练视频生成模型中注入逐帧人体动作引导和环境上下文，训练出一个能根据动作指令生成物体操控视频的世界模型。
result: LOME在动作跟随精度、时空一致性及物理效果模拟（如液体倾倒）上显著优于现有的图像/视频生成基准模型。
conclusion: 该研究展示了视频生成模型在模拟复杂物理交互方面的巨大潜力，为AR/VR和机器人学习提供了高效且可扩展的解决方案。
---

## 摘要
学习人-物交互操作面临重大挑战，因为其涉及的运动具有细粒度和高接触性的特点。传统的基于物理的动画需要大量的建模和手动设置，更重要的是，它既不能很好地泛化到多样的物体形态，也无法有效地扩展到现实世界环境。为了解决这些局限性，我们引入了 LOME，这是一种第一人称视角世界模型，能够根据输入图像、文本提示以及包含身体姿态和手势的逐帧人类动作，生成逼真的人-物交互视频。LOME 通过在训练期间联合估计空间人类动作和环境上下文，为物体操作注入了强大且精确的动作引导。在对多种第一人称视角人-物交互视频进行预训练视频生成模型的微调后，LOME 不仅展示了极高的动作遵循准确性和对未知场景的强大泛化能力，还展示了手-物交互的真实物理后果，例如在执行“倾倒”动作后，液体从瓶子流向马克杯。大量实验表明，我们的基于视频的框架在时间一致性和运动控制方面，显著优于最先进的基于图像和基于视频的动作条件方法，以及图像/文本生成视频（I/T2V）生成模型。LOME 为照片级真实的 AR/VR 体验和可扩展的机器人训练铺平了道路，且不受模拟环境的限制，也不依赖于显式的 3D/4D 建模。

## Abstract
Learning human-object manipulation presents significant challenges due to its fine-grained and contact-rich nature of the motions involved. Traditional physics-based animation requires extensive modeling and manual setup, and more importantly, it neither generalizes well across diverse object morphologies nor scales effectively to real-world environment. To address these limitations, we introduce LOME, an egocentric world model that can generate realistic human-object interactions as videos conditioned on an input image, a text prompt, and per-frame human actions, including both body poses and hand gestures. LOME injects strong and precise action guidance into object manipulation by jointly estimating spatial human actions and the environment contexts during training. After finetuning a pretrained video generative model on videos of diverse egocentric human-object interactions, LOME demonstrates not only high action-following accuracy and strong generalization to unseen scenarios, but also realistic physical consequences of hand-object interactions, e.g., liquid flowing from a bottle into a mug after executing a ``pouring'' action. Extensive experiments demonstrate that our video-based framework significantly outperforms state-of-the-art image based and video-based action-conditioned methods and Image/Text-to-Video (I/T2V) generative model in terms of both temporal consistency and motion control. LOME paves the way for photorealistic AR/VR experiences and scalable robotic training, without being limited to simulated environments or relying on explicit 3D/4D modeling.

---

## 论文详细总结（自动生成）

以下是对论文《LOME: Learning Human-Object Manipulation with Action-Conditioned Egocentric World Model》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不需要显式 3D/4D 建模或复杂物理模拟的情况下，学习并生成精细、高接触性且符合物理规律的人-物交互（HOI）操作视频。
*   **研究动机**：
    *   **传统方法局限**：基于物理的动画需要大量手动设置，且难以泛化到多样化的物体和现实环境。
    *   **现有生成模型不足**：现有的视频扩散模型虽然能生成高质量视频，但仅靠文本或图像引导难以实现精确的动作控制；而使用光流或轨迹作为控制信号往往会预设物体运动，而非让物体运动随人类动作自然产生。
    *   **第一人称视角（Egocentric）需求**：在 AR/VR 和机器人训练中，模拟人类视角下的交互至关重要。

### 2. 方法论
LOME 提出了一种以动作为条件的自我中心世界模型，其核心思想是将人类动作作为显式的空间控制信号注入预训练视频模型。
*   **核心思想**：通过联合建模“动作分布”和“环境动态分布”，让模型学习动作与物体反应之间的因果关系。
*   **关键技术细节**：
    *   **动作地图（Action Maps）**：将 3D 人体骨架和手部关键点投影到 2D 图像平面，生成背景掩码后的 2D 动作地图，提供像素级的空间引导。
    *   **联合动作-环境建模**：不只是将动作作为简单的条件输入，而是将动作地图的潜变量（Latents）与视频潜变量在时间维度上进行拼接，共同进行去噪训练。
    *   **改进的引导策略（Modified Guidance）**：由于动作本身也是去噪过程的一部分，论文引入了“内部引导（Inner Guidance）”机制，修改了分类器自由引导（CFG）公式，以处理非独立的动作条件。
    *   **相机适配器**：引入轻量级适配器处理相机外参（射线图），增强模型对头部运动的感知。
    *   **模型架构**：基于 Wan2.1-VACE-14B 视频生成模型，使用 LoRA 进行轻量化微调。

### 3. 实验设计
*   **数据集**：
    *   **EgoDex**：包含约 33.8 万个短视频（800 小时），具有精确的 3D 人体姿态和相机轨迹标注。
    *   **In-the-wild Captures**：研究团队自行录制的 10 个实验室场景视频，用于测试泛化性。
*   **对比方法（Baselines）**：
    *   **CoSHAND**：基于图像扩散的手部动作条件生成模型。
    *   **Wan-I2V**：最先进的图生视频模型（无动作条件）。
    *   **Go-with-the-Flow (GwtF)**：基于光流控制的视频生成模型。
*   **评估指标**：
    *   **动作跟随**：PCK@20（关键点正确百分比）。
    *   **运动一致性**：FVD、tLPIPS、CLIP-I（身份保持）。
    *   **物理真实感**：通过 GPT 评分评估语义遵循（SA）和物理常识（PC）。
    *   **视觉质量**：PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **算力资源**：使用了 **32 台 NVIDIA A100-80GB GPU**。
*   **训练细节**：
    *   全球批大小（Global Batch Size）为 32。
    *   训练步数：2,000 步。
    *   学习率：1 × 10⁻⁵。
    *   分辨率：832 × 480。
    *   训练时长：文中未明确给出具体小时数，但基于 2000 步和 32 块 A100，推测属于高效的轻量级微调。

### 5. 实验数量与充分性
*   **实验规模**：在 EgoDex 的大规模测试集上进行了定量评估，并补充了实验室实拍数据的泛化测试。
*   **消融实验**：针对“联合建模”、“相机适配器”以及“拼接方式（时间 vs 通道）”进行了详细的消融研究。
*   **主观评估**：组织了 30 名参与者对 10 个样本进行用户研究，涵盖文本遵循、动作遵循、运动一致性和视觉质量四个维度。
*   **充分性评价**：实验设计较为全面，对比了不同维度的基准（图像级、视频级、光流级），且通过消融实验验证了核心设计的有效性，结果具有较强的说服力。

### 6. 主要结论与发现
*   **动作跟随精度高**：LOME 在 PCK@20 指标上达到 66.85%，远超最佳基准（51.33%）。
*   **物理模拟能力**：模型能够自发产生复杂的物理后果，例如在“倾倒”动作下，马克杯中的液体量会随时间自然增加，而无需显式流体模拟。
*   **泛化能力强**：尽管在特定数据集训练，但模型能很好地处理未见过的物体（如实验室中的特定背包、耳机盒）和遮挡场景（如从冰箱拿取不可见物体）。
*   **联合建模的必要性**：实验证明，简单地将动作作为输入而不进行联合去噪，会导致手部畸变和动作不匹配。

### 7. 优点
*   **无需 3D 重建**：摆脱了对物体 3D 模板或网格拟合的依赖，极大地提升了处理任意物体的灵活性。
*   **端到端物理感知**：通过视频扩散模型捕捉到了细微的接触力学和流体动力学。
*   **控制精准**：2D 动作地图结合改进的 CFG 引导，实现了像素级的动作对齐。

### 8. 不足与局限
*   **多物体交互困难**：在涉及多个物体协同操作时（如一手拿杯子一手拿勺子），模型有时会失败（如无法正确抓取其中一个物体）。
*   **对姿态估计的依赖**：如果输入的 3D 姿态估计或相机追踪存在误差，会导致生成的动作地图与视频内容出现投影偏差。
*   **效率问题**：目前尚不支持自回归推理，长视频生成的效率和一致性仍有待提升。

（完）

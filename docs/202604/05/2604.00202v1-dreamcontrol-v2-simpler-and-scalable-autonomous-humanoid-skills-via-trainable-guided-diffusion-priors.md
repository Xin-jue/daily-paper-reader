---
title: "DreamControl-v2: Simpler and Scalable Autonomous Humanoid Skills via Trainable Guided Diffusion Priors"
title_zh: DreamControl-v2：通过可训练的引导扩散先验实现更简单且可扩展的自主类人机器人技能
authors: "Sudarshan Harithas, Sangkyung Kwak, Pushkal Katara, Srujan Deolasee, Dvij Kalaria, Srinath Sridhar, Sai Vemprala, Ashish Kapoor, Jonathan Chung-Kuan Huang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00202v1"
tags: ["query:q13"]
score: 8.0
evidence: 用于复杂交互丰富操作任务的生成式先验
tldr: 针对人形机器人复杂操作任务中强化学习面临的长时程规划难题，本文提出 DreamControl-v2。该方法通过在机器人运动空间内直接训练引导式扩散模型，整合了多元的人类与机器人数据集，构建了统一的具身先验。相比前作，它消除了人工干预，实现了更具扩展性的参考轨迹生成，显著提升了强化学习策略的鲁棒性，并在 Unitree-G1 实体机器人上得到了验证。
selection_source: fresh_fetch
motivation: 传统强化学习在处理人形机器人长时程、高交互的运动操作任务时存在困难，且依赖现成人类运动模型的方法存在局限性。
method: 在机器人运动空间内直接训练引导式扩散模型，通过聚合多样化的人类与机器人数据集构建统一的具身先验。
result: 该方法实现了更自动化的训练流程，通过大规模生成参考轨迹显著增强了下游强化学习策略的鲁棒性。
conclusion: 研究证明了在机器人原生空间训练扩散先验并扩展数据规模是提升人形机器人自主技能的关键。
---

## 摘要
为类人机器人开发鲁棒的自主移动-操作（loco-manipulation）技能仍然是机器人领域的一个开放性问题。虽然强化学习（RL）已成功应用于足式移动，但由于操作任务面临长时程规划挑战，将其应用于复杂且具有丰富交互的操作任务则更为困难。沿着这一思路，最近的一种方法是 DreamControl，它通过利用现成的人类运动扩散模型作为生成先验，在训练期间引导 RL 策略，从而解决这些问题。在本文中，我们研究了 DreamControl 运动先验的影响，并提出了一个改进的框架，该框架直接在类人机器人的运动空间中训练引导扩散模型，将多样化的人类和机器人数据集聚合到一个统一的具身空间中。我们证明，由于采用了更大规模的混合训练数据，我们的方法能够捕捉到更广泛的技能，并通过消除手动过滤干预的需求，建立了一个更自动化的流水线。此外，我们表明，扩展参考轨迹的生成对于实现鲁棒的下游 RL 策略至关重要。我们通过在仿真环境和真实的 Unitree-G1 机器人上进行的大量实验验证了我们的方法。

## Abstract
Developing robust autonomous loco-manipulation skills for humanoids remains an open problem in robotics. While RL has been applied successfully to legged locomotion, applying it to complex, interaction-rich manipulation tasks is harder given long-horizon planning challenges for manipulation. A recent approach along these lines is DreamControl, which addresses these issues by leveraging off-the-shelf human motion diffusion models as a generative prior to guide RL policies during training. In this paper, we investigate the impact of DreamControl's motion prior and propose an improved framework that trains a guided diffusion model directly in the humanoid robot's motion space, aggregating diverse human and robot datasets into a unified embodiment space. We demonstrate that our approach captures a wider range of skills due to the larger training data mixture and establishes a more automated pipeline by removing the need for manual filtering interventions. Furthermore, we show that scaling the generation of reference trajectories is important for achieving robust downstream RL policies. We validate our approach through extensive experiments in simulation and on a real Unitree-G1.

---

## 论文详细总结（自动生成）

这是一份关于论文《DreamControl-v2: Simpler and Scalable Autonomous Humanoid Skills via Trainable Guided Diffusion Priors》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：开发类人机器人的自主移动-操作（Loco-manipulation）技能极具挑战，主要因为操作任务通常涉及长时程规划和复杂的环境交互。
*   **核心问题**：前作 DreamControl 依赖现成的人类运动扩散模型（如 OmniControl），存在以下瓶颈：
    1.  **具身不一致**：在人类空间生成的轨迹在重定向（Retargeting）到机器人后，空间约束（如手部位置）会发生偏移，需人工反复调优。
    2.  **数据局限性**：仅依赖 AMASS 等人类动捕数据集，缺乏丰富的交互动作（如开抽屉）。
    3.  **流程繁琐**：需要大量的人工过滤和特定任务的逆运动学（IK）补偿，难以扩展。
*   **整体含义**：DreamControl-v2 旨在通过在“机器人原生空间”训练扩散先验，构建一个全自动、可扩展且鲁棒的类人机器人技能学习框架。

### 2. 方法论
核心思想是将“生成-再重定向”的流程转变为“先重定向-再训练”，直接在机器人运动空间内学习扩散模型。
*   **大规模机器人空间数据集构建**：
    *   将 AMASS、GRAB、Nymeria 等大规模人类动捕数据，通过 JAX 加速的优化器预先重定向到 Unitree-G1 机器人的运动空间。
    *   统一运动表示：采用类似 SMPL 的参数化方式描述 G1 的 22 个关键点，包含根节点位姿、关节角度、速度及足部接触特征。
*   **可训练的引导扩散先验**：
    *   在机器人空间数据上训练扩散模型，支持**文本指令**和**时空约束**（如：在 $t$ 时刻右手到达 $x,y,z$ 位置）的双重引导。
    *   利用预训练的人类运动模型（MDM）权重进行微调，加速收敛并继承人类运动的自然性。
*   **强化学习（RL）运动模仿**：
    *   扩散模型生成多条参考轨迹，RL 策略通过模仿这些轨迹来学习任务。
    *   **奖励函数**：结合了任务达成奖励（如物体高度、抽屉位移）和运动跟踪奖励。
*   **全自动流水线**：引入基于启发式误差的自动过滤机制，剔除物理不可行或未达标的生成轨迹，无需人工干预。

### 3. 实验设计
*   **实验场景与任务**：共设计了 8 种复杂技能，包括：抓取（Pick）、出拳（Punch）、深蹲抓取（Deep Squat & Pick）、开抽屉（Drawer Open）、倒水（Pour）、垂直擦拭（Vertical Wipe）等。
*   **数据集**：
    *   **AMASS/HumanML3D**：基础运动。
    *   **GRAB/Nymeria**：丰富的交互与日常活动。
    *   **OmniRetarget**：经过物理精炼的机器人操作数据。
*   **Benchmark 与对比方法**：
    *   **Zero-shot OmniControl**：即 DreamControl-v1 的自动化版本（在人类空间生成后重定向）。
    *   **不同数据混合比例的消融模型**：AMASS 仅限模型 vs. Full-Mix 全量数据模型。
    *   **提示词校准（Prompt Calibration）**：对比在推理时调整人类模型参数与预重定向的效果。

### 4. 资源与算力
*   **重定向阶段**：使用 **5 张 NVIDIA H100 GPU** 并行处理大规模人类动捕数据。
*   **扩散模型训练**：使用 **1 张 NVIDIA H100 GPU**，采用 AdamW 优化器，学习率为 $1 \times 10^{-5}$。
*   **强化学习训练**：在 IsaacLab 仿真环境中，使用 **1 张 NVIDIA A6000 GPU**，针对每个任务训练 2000-5000 次迭代，并行环境数量约为 8192 个。
*   **部署**：在真实的 Unitree-G1 机器人上进行验证。

### 5. 实验数量与充分性
*   **实验规模**：每个任务在仿真中进行了 **100 次随机初始化**的测试，以评估成功率和控制误差。
*   **消融实验**：
    *   对比了三种不同的训练数据混合方式（AMASS, AMASS+Nymeria, Full-Mix）。
    *   研究了生成参考轨迹的数量（从 1 到 500 条）对 RL 策略鲁棒性的影响。
    *   验证了网络初始化（从零训练 vs. 预训练微调）的效果。
*   **充分性评价**：实验覆盖了从简单移动到复杂双臂交互的多种任务，且包含仿真与真机对比，数据量大，对比基准明确，具有较高的客观性和说服力。

### 6. 主要结论与发现
1.  **机器人空间训练更优**：直接在机器人空间训练扩散模型，消除了重定向带来的空间漂移，显著提升了时空约束的准确性（Control L2 误差大幅下降）。
2.  **数据规模效应**：引入 Nymeria 和 GRAB 等交互数据集后，模型在复杂任务（如开抽屉）上的成功率从接近 0 提升至 90% 以上。
3.  **轨迹多样性助力鲁棒性**：增加扩散模型生成的参考轨迹数量，能显著增强 RL 策略在面对环境随机性时的泛化能力。
4.  **迁移学习有效性**：利用人类运动先验进行微调，比从零训练机器人运动模型具有更好的物理合理性和更低的 FID（分布距离）。

### 7. 优点
*   **简化流程**：通过预重定向解决了具身差异问题，实现了从文本/约束到机器人动作的端到端自动化。
*   **可扩展性**：框架易于整合新的数据集，且能通过增加采样量来提升策略质量。
*   **真机表现**：在 Unitree-G1 上展示了高度自然的全身协调动作，证明了该方法在处理现实世界物理约束方面的有效性。

### 8. 不足与局限
*   **过滤机制依赖启发式**：虽然实现了自动化，但过滤逻辑（如设定阈值判断是否抓到物体）仍带有一定的任务特定性，不够通用。
*   **计算开销**：预重定向大规模数据集需要极高的算力支持（5 张 H100）。
*   **环境感知局限**：目前主要依赖物体位姿的显式输入，对于非结构化视觉环境的直接感知能力尚待进一步整合。
*   **硬件限制**：实验主要针对 Unitree-G1，虽然理论通用，但在不同形态机器人上的迁移效果未做跨平台验证。

（完）

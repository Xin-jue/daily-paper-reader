---
title: "Flow Motion Policy: Manipulator Motion Planning with Flow Matching Models"
title_zh: 流运动策略：基于流匹配模型的机械臂运动规划
authors: "Davood Soleymanzadeh, Xiao Liang, Minghui Zheng"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07084v1"
tags: ["query:q2"]
score: 9.0
evidence: 基于流匹配的机械臂运动规划
tldr: 针对现有端到端神经运动规划器生成路径单一且缺乏推理优化的问题，本文提出 Flow Motion Policy (FMP)。该方法利用流匹配（Flow Matching）生成模型捕捉规划数据集的多模态分布，实现开环端到端规划。通过推理时的 Best-of-N 采样，FMP 能生成多个候选路径并筛选无碰撞解，显著提升了机械臂在复杂环境下的规划成功率和效率。
selection_source: fresh_fetch
motivation: 现有的神经运动规划器通常只能生成单一路径，无法有效利用生成模型的随机性进行推理优化。
method: 采用流匹配模型构建随机生成策略，通过对可行路径分布建模实现推理时的多路径采样与碰撞评估。
result: 实验表明，FMP 在规划成功率和效率上均优于传统的采样算法及现有的神经运动规划方法。
conclusion: 随机生成策略结合推理时优化能有效提升端到端运动规划的性能，为复杂环境下的机器人控制提供了新思路。
---

## 摘要
最近，开环端到端神经运动规划器被提出，用于改进机器人机械臂的运动规划。这些方法能够直接根据传感器观测进行规划，而无需在规划过程中依赖特权碰撞检测器。然而，许多现有方法在不同运行中针对给定工作空间仅生成单一路径，且未能利用其开环结构进行推理时优化。为解决这一局限性，我们引入了流运动策略（Flow Motion Policy），这是一种用于机械臂的开环端到端神经运动规划器，它利用流匹配（Flow Matching）方法的随机生成式表述来捕捉规划数据集固有的多模态性。通过对可行路径的分布进行建模，流运动策略实现了高效的推理时 N 选优（best-of-N）采样。该方法生成多个端到端候选路径，在规划后评估其碰撞状态，并执行第一个无碰撞的解。我们将流运动策略与具有代表性的基于采样的运动规划方法和神经运动规划方法进行了基准测试。评估结果表明，流运动策略提高了规划成功率和效率，突显了随机生成式策略在端到端运动规划和推理时优化中的有效性。实验评估视频可通过此链接获取。

## Abstract
Open-loop end-to-end neural motion planners have recently been proposed to improve motion planning for robotic manipulators. These methods enable planning directly from sensor observations without relying on a privileged collision checker during planning. However, many existing methods generate only a single path for a given workspace across different runs, and do not leverage their open-loop structure for inference-time optimization. To address this limitation, we introduce Flow Motion Policy, an open-loop, end-to-end neural motion planner for robotic manipulators that leverages the stochastic generative formulation of flow matching methods to capture the inherent multi-modality of planning datasets. By modeling a distribution over feasible paths, Flow Motion Policy enables efficient inference-time best-of-$N$ sampling. The method generates multiple end-to-end candidate paths, evaluates their collision status after planning, and executes the first collision-free solution. We benchmark the Flow Motion Policy against representative sampling-based and neural motion planning methods. Evaluation results demonstrate that Flow Motion Policy improves planning success and efficiency, highlighting the effectiveness of stochastic generative policies for end-to-end motion planning and inference-time optimization. Experimental evaluation videos are available via this \href{https://zh.engr.tamu.edu/wp-content/uploads/sites/310/2026/03/FMP-Website.mp4}{link}.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Flow Motion Policy (FMP)**，一种基于流匹配（Flow Matching）模型的机械臂端到端运动规划方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：传统的采样算法（如 RRT）和轨迹优化方法高度依赖碰撞检测器，而碰撞检测通常占据了 90% 的计算时间，导致规划效率低下。
*   **现有局限**：现有的端到端神经运动规划器虽然速度快，但大多是确定性的（只能生成单一路径），无法捕捉规划问题的多模态性（即存在多种可行路径），也无法在推理阶段通过多次采样来优化成功率。
*   **核心目标**：开发一种能够学习路径分布、支持推理时并行采样（Best-of-N Sampling）且无需在规划过程中频繁调用碰撞检测器的开环神经规划器。

### 2. 论文提出的方法论
*   **核心思想**：将运动规划建模为一个条件生成任务，利用**流匹配（Flow Matching）**框架学习从高斯噪声到可行路径增量序列的连续变换。
*   **关键技术细节**：
    *   **输入表征**：使用 PointNet++ 提取机器人和环境的点云特征，结合当前位姿和目标位姿的 MLP 嵌入。
    *   **Transformer 编码器**：将上述所有特征作为 Token 输入 Transformer，进行多模态特征融合。
    *   **流匹配头（Flow Head）**：采用轻量化 MLP 预测向量场 $v_\theta$。通过在流时间 $\tau \in [0, 1]$ 内对向量场进行积分（使用 Euler 解算器），将随机噪声转化为关节空间的增量动作序列。
    *   **推理时优化（Best-of-N Sampling）**：在推理时并行生成 $N$ 条候选路径，利用 GPU 加速的碰撞检测器（如 cuRobo）评估这些路径，并执行第一条无碰撞的路径。

### 3. 实验设计
*   **实验场景**：包括 TableTop（桌面）、Box（方块）、Bins（料箱）和三种不同难度的 Shelf（货架）任务。
*   **基准方法（Benchmarks）**：
    *   **经典算法**：Bi-RRT、BIT*。
    *   **神经采样器**：MPNets、SIMPNet、GAIDE。
    *   **端到端规划器**：PerFACT（确定性）、Neural MP（基于 GMM 的生成式模型）。
*   **评估指标**：规划成功率（Success Rate）和平均规划时间（Planning Time）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4080 GPU** 的工作站上完成。
*   **算力细节**：文中未明确给出具体的训练总时长，但提到使用了 cuRobo 进行大规模数据采集，并强调了 FMP 的轻量化特性（参数量仅 1.4M，远小于 Neural MP 的 20M）。

### 5. 实验数量与充分性
*   **实验规模**：在 6 种仿真环境和真实世界（UR5e 机械臂）中进行了测试。
*   **消融实验**：非常充分。作者对比了不同的策略头架构（MLP、U-Net、Transformer、DiT）、不同的生成框架（流匹配 vs. 扩散模型）、采样数量 $N$ 的影响，以及 Euler 解算器步数的影响。
*   **客观性**：通过与多种 SOTA 方法在相同任务下对比，并提供了真实世界的部署数据（30 组实验），证明了方法的有效性和鲁棒性。

### 6. 论文的主要结论与发现
*   **性能优异**：FMP 在推理时进行 100 次采样（FMP-100）时，成功率可媲美经典采样算法，但规划速度快了一个数量级。
*   **生成模型对比**：流匹配模型在捕捉多模态分布方面优于 GMM（Neural MP），且比扩散模型（Diffusion Policy）推理速度更快（因为所需的去噪/积分步数更少）。
*   **轻量化优势**：简单的 MLP 作为流匹配头在性能和速度之间达到了最佳平衡，且模型参数量极小，适合实时应用。

### 7. 优点
*   **高效性**：利用流匹配的确定性轨迹特性，仅需少量积分步数即可生成高质量路径。
*   **并行优化**：支持推理时的 Best-of-N 采样，通过并行碰撞检测显著提升了在复杂环境下的生存能力。
*   **端到端**：直接从点云到动作，减少了对复杂预处理的依赖。

### 8. 不足与局限
*   **静态环境假设**：目前的模型主要针对静态环境，未显式处理动态障碍物。
*   **路径质量**：生成的路径虽然可行，但可能在平滑度或局部最优性上不如专门的轨迹优化算法，可能需要后处理。
*   **泛化瓶颈**：在极度复杂的货架任务中，如果基础策略无法生成足够多样化的候选路径，Best-of-N 采样的提升效果会受限。

（完）

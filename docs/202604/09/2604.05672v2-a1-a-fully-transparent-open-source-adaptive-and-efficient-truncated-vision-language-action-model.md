---
title: "A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model"
title_zh: A1：一个完全透明开源、自适应且高效的截断式视觉-语言-动作模型
authors: "Kaidong Zhang, Jian Zhang, Rongtao Xu, Yu Sun, Shuoshuo Xue, Youpeng Wen, Xiaoyu Guo, Minghao Guo, Weijia Liufu, Liu Zihou, Kangyi Ji, Yangsong Zhang, Jiarun Zhu, Jingzhi Liu, Zihang Li, Ruiyi Chen, Meng Cao, Jingming Zhang, Shen Zhao, Xiaojun Chang, Feng Zheng, Ivan Laptev, Xiaodan Liang"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05672v2"
tags: ["query:q12"]
score: 10.0
evidence: 用于机器人操作的高效截断式VLA模型
tldr: A1是一个开源透明的视觉-语言-动作（VLA）模型框架，旨在解决现有模型推理成本高、延迟大的问题。它通过利用预训练VLM的先验知识，并引入预算感知的自适应推理方案（包括中间层提前终止和跨层截断流匹配技术），在保持高成功率的同时大幅降低了计算开销。在多个仿真和真实机器人基准测试中，A1不仅实现了SOTA性能，还显著提升了推理效率，为低成本实时机器人控制提供了新方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型因庞大的骨干网络和复杂的动作生成头导致推理延迟高、计算成本昂贵，难以在普通硬件上实现实时控制。
method: 提出了一种自适应推理方案，通过监控中间层动作一致性实现提前终止，并利用跨层截断流匹配技术加速去噪过程。
result: "在RoboChallenge等多个基准测试中超越了pi0等基准模型，同时减少了高达72%的推理延迟和76.6%的骨干网络计算量。"
conclusion: A1证明了通过优化推理流程和利用模型内部一致性，可以在不牺牲性能的前提下显著提升VLA模型的效率和可部署性。
---

## 摘要
视觉-语言-动作（VLA）模型已成为开放世界机器人操控的一种强大范式，但其实际部署往往受限于成本：十亿级参数规模的 VLM 骨干网络以及基于迭代扩散/流（diffusion/flow-based）的动作头会产生高延迟和高计算量，使得在通用硬件上进行实时控制变得昂贵。我们提出了 A1，这是一个完全开源且透明的 VLA 框架，旨在不牺牲操控成功率的前提下实现低成本、高吞吐量的推理。我们的方法利用预训练 VLM 为动作生成提供隐式的可操作性先验（affordance priors）。我们发布了完整的训练栈（包括训练代码、数据/数据处理流水线、中间检查点和评估脚本），以实现端到端的复现。除了仅优化 VLM 之外，A1 还通过引入一种预算感知的自适应推理方案来针对整个推理流水线，该方案可同时加速骨干网络和动作头。具体而言，我们通过监测 VLM 中间层的动作一致性来触发提前终止，并提出了层间截断流匹配（Inter-Layer Truncated Flow Matching），该方法可在不同层之间热启动去噪过程，从而以显著减少的有效去噪迭代次数实现精确的动作生成。在仿真基准测试（LIBERO、VLABench）和真实机器人（Franka、AgiBot）上，A1 在显著降低推理成本的同时（例如，流匹配推理的单回合延迟降低高达 72%，骨干网络计算量减少高达 76.6% 且性能下降极小），实现了最先进的成功率。在 RoboChallenge 上，A1 实现了 29.00% 的平均成功率，优于包括 pi0 (28.33%)、X-VLA (21.33%) 和 RDT-1B (15.00%) 在内的基准模型。

## Abstract
Vision-Language-Action (VLA) models have emerged as a powerful paradigm for open-world robot manipulation, but their practical deployment is often constrained by cost: billion-scale VLM backbones and iterative diffusion/flow-based action heads incur high latency and compute, making real-time control expensive on commodity hardware. We present A1, a fully open-source and transparent VLA framework designed for low-cost, high-throughput inference without sacrificing manipulation success; Our approach leverages pretrained VLMs that provide implicit affordance priors for action generation. We release the full training stack (training code, data/data-processing pipeline, intermediate checkpoints, and evaluation scripts) to enable end-to-end reproducibility. Beyond optimizing the VLM alone, A1 targets the full inference pipeline by introducing a budget-aware adaptive inference scheme that jointly accelerates the backbone and the action head. Specifically, we monitor action consistency across intermediate VLM layers to trigger early termination, and propose Inter-Layer Truncated Flow Matching that warm-starts denoising across layers, enabling accurate actions with substantially fewer effective denoising iterations. Across simulation benchmarks (LIBERO, VLABench) and real robots (Franka, AgiBot), A1 achieves state-of-the-art success rates while significantly reducing inference cost (e.g., up to 72% lower per-episode latency for flow-matching inference and up to 76.6% backbone computation reduction with minor performance degradation). On RoboChallenge, A1 achieves an average success rate of 29.00%, outperforming baselines including pi0(28.33%), X-VLA (21.33%), and RDT-1B (15.00%).

---

## 论文详细总结（自动生成）

这是一份关于论文《A1: A Fully Transparent Open-Source, Adaptive and Efficient Truncated Vision-Language-Action Model》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 OpenVLA, $\pi_0$）虽然在开放世界机器人操控中表现出色，但**推理成本极高**。其瓶颈在于：1) 数十亿参数的 VLM 骨干网络计算量大；2) 基于扩散或流匹配（Flow Matching）的动作头需要多次迭代去噪，导致高延迟，难以在普通硬件上实现实时控制。
*   **研究动机**：作者观察到动作生成存在冗余性——流匹配轨迹通常在极少数步内即可收敛，且 VLM 的中间层隐藏状态已包含足够的空间特征。因此，A1 旨在通过“按需计算”的原则，打造一个完全开源、透明且高效的 VLA 框架。

### 2. 方法论
*   **核心思想**：引入**预算感知的自适应推理方案**，同时加速骨干网络和动作头。
*   **关键技术细节**：
    *   **骨干网络**：采用 Molmo-7B 预训练模型，利用其强大的视觉语义理解和隐式可操作性（Affordance）先验。
    *   **双动作头设计**：支持 A1-FM（流匹配头，处理高维分布）和 A1-MLP（快速拟合，抑制噪声）。
    *   **自适应提前终止（Early-Exit）**：在训练时对 VLM 的所有层进行动作预测监督。推理时，通过计算相邻层预测动作的一致性（Discrepancy Metric），若动作趋于稳定则提前停止骨干网络的前向传播。
    *   **层间截断流匹配（Inter-Layer Truncated Flow Matching）**：这是针对迭代式动作头的优化。在每个 VLM 层仅运行极少数去噪步（如 $\delta=2$），并将当前层的输出作为下一层的“热启动（Warm-start）”初始值，而非从随机噪声开始，从而大幅减少总迭代次数。

### 3. 实验设计
*   **数据集**：
    *   **开源数据**：DROID, AgiBot, RoboCOIN, RoboMind, GM-100, RoboChallenge。
    *   **自有数据**：收集了 15,951 条真实世界轨迹，涵盖 ARX, Franka, UR5, AgiBot 等平台。
*   **Benchmark**：
    *   **仿真**：LIBERO（四个任务集）、VLABench。
    *   **真实世界**：在 Franka, AgiBot, OpenArm, Dobot-Arm 四种平台上进行 7 类任务测试。
    *   **综合评估**：RoboChallenge (Table30) 包含 30 个真实机器人任务。
*   **对比方法**：$\pi_0$, $\pi_{0.5}$, OpenVLA, Octo, RDT-1B, X-VLA, CoT-VLA 等。

### 4. 资源与算力
*   **训练配置**：使用 AdamW 优化器，全局 Batch Size 为 1024，总训练步数为 200K。
*   **算力说明**：文中**未明确说明具体的 GPU 型号和数量**（如 A100 或 H100 的具体枚数），但详细列出了超参数（如 VLM 学习率 $5 \times 10^{-6}$，动作头 $5 \times 10^{-5}$）以及推理时的 GFLOPs（如 A1-FM 正常推理约 11.13 TFLOPs）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验：LIBERO 每项任务 500 次试验，VLABench 每项 50 次。
    *   真实世界：每个任务测试 10 次，跨越 4 个机器人平台。
    *   消融实验：针对提前终止阈值 $c$、去噪步数 $\delta$、热启动策略等进行了多组对比。
*   **充分性与公平性**：实验覆盖了从仿真到真实世界、从单臂到双臂（OpenArm）的广泛场景。通过与多个 SOTA 开源模型在统一基准（RoboChallenge）下对比，证明了其竞争力和效率提升的客观性。

### 6. 主要结论与发现
*   **性能领先**：在 RoboChallenge 上以 29.00% 的成功率超越了 $\pi_0$ 和 RDT-1B，成为表现最强的全开源模型之一。
*   **效率极高**：自适应推理可减少高达 76.6% 的骨干网络计算量，且成功率仅轻微下降；层间截断流匹配将单回合延迟降低了 72%。
*   **冗余性证实**：实验发现 VLM 的大量计算在简单动作中是冗余的，模型能自适应地在复杂操作（如开炉灶）时进入深层计算，在简单移动时早停。

### 7. 优点
*   **完全透明开源**：不仅提供权重，还发布了完整的训练代码、数据处理流水线和评估脚本，极具社区价值。
*   **端到端加速**：不同于以往只优化 VLM 或只优化动作头的研究，A1 实现了两者的联合加速。
*   **泛化能力强**：在 LIBERO-Plus 的零样本（Zero-shot）测试中表现优异，证明了其预训练策略的有效性。

### 8. 不足与局限
*   **数据依赖**：预训练仍依赖于带标签的可操作性数据，限制了数据规模的进一步扩张。
*   **模仿学习通病**：主要基于模仿学习，存在累积误差问题，在极高精度要求的任务中可能受限。
*   **部署延迟**：虽然推理加速了，但云端服务器与本地机器人之间的网络延迟仍是实时控制中的一个不稳定因素。
*   **算力细节缺失**：未披露具体训练硬件资源，不便于其他团队评估复现所需的硬件成本。

（完）

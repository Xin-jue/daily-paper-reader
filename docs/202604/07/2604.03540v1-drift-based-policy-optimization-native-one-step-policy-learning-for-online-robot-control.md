---
title: "Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control"
title_zh: 基于漂移的策略优化：面向在线机器人控制的原生单步策略学习
authors: "Yuxuan Gao, Yedong Shen, Shiqi Zhang, Wenhao Yu, Yifan Duan, Jia pan, Jiajia Wu, Jiajun Deng, Yanyong Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03540v1"
tags: ["query:q2"]
score: 9.0
evidence: 机器人操作中的生成式策略
tldr: 针对多步生成式策略在机器人控制中推理速度慢的问题，本文提出了一种原生单步生成策略框架。通过漂移策略（DBP）将迭代优化过程内化至模型参数中，并结合漂移策略优化（DBPO）实现稳定的在线强化学习。该方法在保持多模态建模能力的同时，推理速度提升达100倍，在离线模仿学习和在线微调中均表现优异，支持高频闭环控制。
selection_source: fresh_fetch
motivation: 现有的多步生成式策略推理开销大，难以满足高频闭环控制和在线强化学习的实时性需求。
method: 提出DBP利用定点漂移目标实现单步生成，并开发DBPO框架通过兼容的随机接口进行稳定的在线策略更新。
result: 在多个基准测试中，DBP性能媲美或超越多步扩散策略，推理速度提升百倍，并在真机上实现105.2 Hz的高频控制。
conclusion: 该框架成功解决了生成式策略在实时机器人控制中的效率瓶颈，为高效、稳定的在线机器人学习提供了新方案。
---

## 摘要
尽管多步生成式策略通过建模多模态动作分布在机器人操作中取得了强大的性能，但它们在推理时需要多步迭代去噪。因此，每个动作都需要数十到数百次网络函数评估（NFEs），这使得它们在高频闭环控制和在线强化学习（RL）中成本高昂。为了解决这一局限性，我们提出了一个针对原生单步生成式策略的两阶段框架，将细化过程从推理阶段转移到训练阶段。首先，我们引入了基于漂移的策略（DBP），它利用不动点漂移目标将迭代细化内化到模型参数中，在设计上产生了一个单步生成式骨干网络，同时保留了多模态动作建模能力。其次，我们开发了基于漂移的策略优化（DBPO），这是一个在线强化学习框架，为预训练的骨干网络配备了兼容的随机接口，在不牺牲单步部署特性的情况下实现了稳定的同策略（on-policy）更新。大量实验证明了所提框架在离线模仿学习、在线微调和真实世界控制场景中的有效性。DBP 在推理速度提升高达 100 倍的同时，达到或超过了多步扩散策略的性能。在具有挑战性的操作基准测试中，它也始终优于现有的单步基准方法。此外，DBPO 在在线设置中实现了有效且稳定的策略改进。在真实世界双臂机器人上的实验证明了 105.2 Hz 的可靠高频控制。

## Abstract
Although multi-step generative policies achieve strong performance in robotic manipulation by modeling multimodal action distributions, they require multi-step iterative denoising at inference time. Each action therefore needs tens to hundreds of network function evaluations (NFEs), making them costly for high-frequency closed-loop control and online reinforcement learning (RL). To address this limitation, we propose a two-stage framework for native one-step generative policies that shifts refinement from inference to training. First, we introduce the Drift-Based Policy (DBP), which leverages fixed-point drifting objectives to internalize iterative refinement into the model parameters, yielding a one-step generative backbone by design while preserving multimodal action modeling capacity. Second, we develop Drift-Based Policy Optimization (DBPO), an online RL framework that equips the pretrained backbone with a compatible stochastic interface, enabling stable on-policy updates without sacrificing the one-step deployment property. Extensive experiments demonstrate the effectiveness of the proposed framework across offline imitation learning, online fine-tuning, and real-world control scenarios. DBP matches or exceeds the performance of multi-step diffusion policies while achieving up to $100\times$ faster inference. It also consistently outperforms existing one-step baselines on challenging manipulation benchmarks. Moreover, DBPO enables effective and stable policy improvement in online settings. Experiments on a real-world dual-arm robot demonstrate reliable high-frequency control at 105.2 Hz.

---

## 论文详细总结（自动生成）

这是一份关于论文《Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
生成式策略（如扩散策略 Diffusion Policy）在处理机器人操作中的多模态动作分布方面表现出色，但其核心缺陷在于**推理延迟高**。由于需要多步迭代去噪（通常需要数十到数百次网络评估 NFE），这类模型难以满足高频闭环控制（>100Hz）的需求，且在在线强化学习（RL）中因交互成本过高而受限。
本文旨在开发一种**原生单步（Native One-Step）**生成策略，将迭代细化的过程从“推理时”转移到“训练时”，从而在保持多模态表达能力的同时，实现极速推理。

### 2. 方法论
论文提出了一个两阶段框架：
*   **第一阶段：基于漂移的策略 (DBP)**
    *   **核心思想**：借鉴“漂移模型（Drifting Models）”原理，通过训练时的动力学演化来内化修正行为。
    *   **关键技术**：利用定点漂移目标（Fixed-point drifting objectives）。在训练中，模型生成的多个假设（Hypotheses）会受到吸引力（向专家数据靠近）和排斥力（假设之间保持多样性）的作用。
    *   **流程**：通过最小化当前预测与“漂移后目标”之间的均方误差，使网络参数直接学习到最终的分布映射。训练完成后，推理仅需一次前向传播（1-NFE）。
*   **第二阶段：基于漂移的策略优化 (DBPO)**
    *   **核心思想**：为单步骨干网络添加随机适配器，使其兼容在线 PPO 更新。
    *   **关键技术**：
        *   **随机动作头**：在预训练骨干上添加对角高斯噪声头，用于在线探索。
        *   **精确似然计算**：通过存储潜在变量 $z$，实现无需边缘化的精确对数似然评估。
        *   **锚点正则化（Anchor Regularization）**：引入损失项限制更新后的策略偏离预训练状态太远，确保训练稳定性。

### 3. 实验设计
*   **数据集与场景**：
    *   **离线模仿学习**：Diffusion Policy 标准套件（Push-T, BlockPush, Kitchen 等 12 个任务）；3D 点云操作基准（Adroit 和 Meta-World 共 37 个任务）。
    *   **在线强化学习**：RoboMimic 图像任务和 D4RL 运动任务。
    *   **真实世界**：双臂 UR5 机器人，执行抓取（Lift）、移罐（Can）和双臂协作运输（Transport）。
*   **Benchmark 与对比方法**：
    *   **多步模型**：Diffusion Policy (DP), DP3。
    *   **单步模型**：MP1, OMP, FlowPolicy, AdaFlow, Consistency Policy 等。
    *   **在线 RL 方法**：ReinFlow, DMPO。

### 4. 资源与算力
*   **硬件**：使用了 8 台 NVIDIA RTX 3090 GPU。
*   **训练时长**：在 Adroit 任务上训练 3000 个 epoch，在 Meta-World 任务上训练 1000 个 epoch。
*   **推理效率**：端到端延迟仅为 9.5ms，支持 105.2 Hz 的实时控制。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低维状态到高维图像、再到 3D 点云的多种输入模态；测试任务总数超过 50 个。
*   **消融实验**：针对在线 RL 中的“锚点正则化”进行了专门的消融研究，证明了其对稳定性的贡献。
*   **客观性**：在对比实验中，作者尽可能匹配了骨干网络架构（如 U-Net）以确保算法层面的公平比较，并报告了多个随机种子的均值和标准差。实验设计全面，覆盖了离线、在线及真机迁移。

### 6. 主要结论与发现
*   **性能与速度双赢**：DBP 在推理速度提升 100 倍的情况下，性能匹配甚至超过了 100 步迭代的扩散策略（平均得分从 0.79 提升至 0.83）。
*   **单步基准领先**：在 37 个点云任务中，DBP 达到了 88.4% 的成功率，显著优于之前的单步 SOTA 方法 OMP (82.3%)。
*   **在线提升显著**：DBPO 证明了单步模型可以通过在线交互进一步突破模仿学习的性能天花板。
*   **真机可行性**：在复杂的双臂协作任务中实现了 75% 的成功率，验证了高频闭环控制的优势。

### 7. 优点
*   **原生单步设计**：不同于通过蒸馏或压缩获得的单步模型，DBP 从训练目标上就保证了单步性，避免了复杂的后处理。
*   **多模态保持**：在极简推理的同时，依然保留了生成式模型处理歧义动作（多峰分布）的能力。
*   **在线适配性**：DBPO 解决了生成式模型在在线 PPO 更新时似然计算难的问题，且不破坏单步部署特性。

### 8. 不足与局限
*   **任务复杂度限制**：目前的评估主要集中在桌面操作和结构化环境中的运动，对于长程（Long-horizon）组合任务或极度非结构化环境的验证尚不充分。
*   **样本效率**：虽然在线微调效果好，但在“从零开始学习（Learning from scratch）”的设定下，其样本效率仍有提升空间。
*   **硬件依赖**：虽然推理快，但训练阶段由于需要生成多个假设进行漂移计算，训练显存开销可能高于简单的行为克隆。

（完）

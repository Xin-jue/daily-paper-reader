---
title: "Pair2Scene: Learning Local Object Relations for Procedural Scene Generation"
title_zh: Pair2Scene：学习局部物体关系以实现程序化场景生成
authors: "Xingjian Ran, Shujie Zhang, Weipeng Zhong, Li Luo, Bo Dai"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11808v1"
tags: ["query:q3"]
score: 9.0
evidence: 3D室内场景的程序化生成框架
tldr: 针对3D室内场景生成中数据稀缺和空间关系建模难的问题，本文提出Pair2Scene框架。该方法基于物体放置主要依赖局部关系的观察，通过学习支撑和功能两种局部关系规则，结合场景层级结构和物理算法进行程序化生成。通过在3D-Pairs数据集上训练并结合碰撞感知采样，该框架能生成超越训练分布的高保真、复杂且符合物理逻辑的场景。
selection_source: fresh_fetch
motivation: 现有场景生成方法难以扩展到复杂场景且缺乏精确空间推理能力，而物体放置本质上更多依赖局部依赖而非全局分布。
method: 提出一种学习局部物体关系的框架，通过神经网络建模支撑和功能关系，并结合层级结构与碰撞感知拒绝采样进行递归生成。
result: 实验表明，该方法在生成复杂环境方面优于现有技术，能够产生具有物理和语义合理性的高保真布局。
conclusion: Pair2Scene证明了通过学习局部物体关系并结合程序化规则，可以有效解决大规模、高复杂度3D场景生成的泛化与合理性问题。
---

## 摘要
由于数据稀缺以及建模复杂空间关系的难度，生成高保真 3D 室内场景仍然是一个重大挑战。当前的方法通常难以扩展到训练分布之外的密集场景，或者依赖于缺乏精确空间推理能力的大语言模型（LLM）/视觉语言模型（VLM）。基于物体放置主要依赖于局部依赖关系而非信息冗余的全局分布这一观察，本文提出了 Pair2Scene，这是一种新型的程序化生成框架，它将学习到的局部规则与场景层级结构及基于物理的算法相结合。这些规则主要捕捉两类物体间的关系，即遵循物理层级结构的支撑关系和反映语义联系的功能关系。我们通过一个网络对这些规则进行建模，该网络以锚点物体的位置和几何形状为条件，估计依赖物体的空间位置分布。据此，我们从现有的场景数据中整理了一个名为 3D-Pairs 的数据集来训练该模型。在推理过程中，我们的框架通过在层级结构中递归应用该模型来生成场景，并利用碰撞感知的拒绝采样将局部规则对齐为连贯的全局布局。大量实验表明，我们的框架在生成超出训练数据的复杂环境方面优于现有方法，同时保持了物理和语义上的合理性。

## Abstract
Generating high-fidelity 3D indoor scenes remains a significant challenge due to data scarcity and the complexity of modeling intricate spatial relations. Current methods often struggle to scale beyond training distribution to dense scenes or rely on LLMs/VLMs that lack the ability for precise spatial reasoning. Building on top of the observation that object placement relies mainly on local dependencies instead of information-redundant global distributions, in this paper, we propose Pair2Scene, a novel procedural generation framework that integrates learned local rules with scene hierarchies and physics-based algorithms. These rules mainly capture two types of inter-object relations, namely support relations that follow physical hierarchies, and functional relations that reflect semantic links. We model these rules through a network, which estimates spatial position distributions of dependent objects conditioned on position and geometry of the anchor ones. Accordingly, we curate a dataset 3D-Pairs from existing scene data to train the model. During inference, our framework can generate scenes by recursively applying our model within a hierarchical structure, leveraging collision-aware rejection sampling to align local rules into coherent global layouts. Extensive experiments demonstrate that our framework outperforms existing methods in generating complex environments that go beyond training data while maintaining physical and semantic plausibility.

---

## 论文详细总结（自动生成）

### 论文总结：Pair2Scene - 学习局部物体关系以实现程序化场景生成

#### 1. 核心问题与整体含义（研究动机和背景）
生成高保真、复杂的3D室内场景面临两大挑战：一是**数据稀缺**，现有数据集（如3D-Front）场景复杂度低，物体密度小；二是**建模困难**，现有学习方法试图捕捉全局分布，随着物体数量增加，联合分布的建模复杂度呈指数级增长，导致模型难以泛化到密集场景。
**核心动机**：作者观察到，室内物体的放置主要取决于其**局部邻居**（如笔记本电脑放在桌子上），而非全局上下文。因此，论文提出将全局场景生成分解为可学习的**局部物体关系规则**，通过程序化方式组合这些局部规则来构建复杂场景。

#### 2. 方法论
Pair2Scene 框架结合了学习到的局部规则、场景层级结构和物理算法：
*   **核心思想**：定义两种基本关系：
    *   **支撑关系 ($R_s$)**：基于物理重力，如桌子支撑电脑。
    *   **功能关系 ($R_f$)**：基于语义联系，如键盘相对于电脑的放置。
*   **关键技术细节**：
    *   **几何感知布局预测器**：使用 **Point-MAE** 提取物体的点云几何特征，捕捉物体的物理属性（如非平面支撑面）。
    *   **概率空间建模**：采用 **混合逻辑分布 (Mixture of Logistics, MoL)** 来建模物体放置的多峰分布（例如椅子可以放在桌子周围的多个位置）。
    *   **数据整理流水线**：从 3D-Front、MesaTask 和 InternScenes 中提取关系元组，利用物理验证过滤不稳定物体，并使用 **LLM（大语言模型）进行功能关系蒸馏**。
*   **算法流程**：
    1.  构建**支撑树 (Support Tree)** 和 **功能树 (Functional Tree)**。
    2.  通过广度优先和深度优先搜索将树序列化为关系元组。
    3.  递归调用模型预测位置，并结合**碰撞感知拒绝采样 (Rejection Sampling)** 和重力细化，确保全局物理合理性。

#### 3. 实验设计
*   **数据集**：
    *   **3D-Pairs**：作者整理的包含约 140,000 个关系元组的数据集。
    *   **3D-Front**：用于评估模型拟合和外推能力。
*   **Benchmark 与对比方法**：
    *   **学习类方法**：ATISS, DiffuScene, LayoutVLM, FactoredScenes。
    *   **程序化/LLM类方法**：Infinigen-Indoors, Holodeck。
*   **评估指标**：FID、KID（衡量分布相似度）、碰撞率、悬空率，以及用户研究（语义对齐 SA、物理合理性 PP、场景复杂度 SC 等）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 (40G) GPU**。
*   **训练细节**：模型训练了 **120 个 epoch**，使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，总 Batch Size 为 32。
*   **推理效率**：生成一个复杂场景约需 69 秒，比 LLM 驱动的方法（如 Holodeck 的 210 秒）更高效。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3D-Front 专用设置和多源数据集设置下进行了对比。
*   **消融实验**：针对“无关系建模”、“无预训练几何特征”、“无拒绝采样”、“仅类别信息”和“无重力模拟”等 5 个关键组件做了详细消融。
*   **充分性评价**：实验设计较为全面，既有客观的几何/分布指标，也有主观的人类评价。通过“Ours-Fit”（拟合原分布）和“Ours-Beyond”（超越原分布复杂度）的对比，有力证明了其外推能力。

#### 6. 主要结论与发现
*   **局部建模优于全局建模**：通过学习局部关系，模型能更准确地捕捉空间分布，FID 和 KID 指标显著优于全局预测方法。
*   **外推能力强**：Pair2Scene 能够生成比训练数据更密集、更复杂的场景（平均物体数从 4 个提升到 14 个以上），且不损失物理合理性。
*   **几何特征至关重要**：引入点云几何特征（而非仅用类别标签）能显著减少物体间的物理冲突和不合理放置。

#### 7. 优点（亮点）
*   **解耦设计**：将复杂的全局布局问题简化为局部关系预测，有效解决了数据稀缺问题。
*   **物理与语义结合**：通过拒绝采样和重力模拟，解决了生成场景中常见的物体穿插和悬空问题。
*   **灵活性**：支持统计生成和 LLM 引导生成两种模式，既能保证自动化，又能通过自然语言控制场景层级。

#### 8. 不足与局限
*   **风格一致性**：目前主要关注空间位置，在物体检索时无法保证家具风格（如颜色、材质）的视觉统一性。
*   **复杂关系覆盖**：对于受多个锚点共同约束的物体，或特殊的悬挂物体（如吊灯），目前的层级树结构处理能力有限。
*   **依赖预定义层级**：场景的生成顺序高度依赖于支撑树和功能树的构建质量。

（完）

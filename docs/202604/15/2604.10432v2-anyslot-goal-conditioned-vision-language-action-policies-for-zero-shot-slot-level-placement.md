---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v2"
tags: ["query:q10"]
score: 10.0
evidence: 用于机器人操作和放置的目标条件VLA策略
tldr: AnySlot是一个针对零样本槽位级放置任务的视觉-语言-动作（VLA）框架。针对现有模型在处理复杂指令和高精度放置时的不足，AnySlot通过引入显式空间视觉目标（场景标记）作为中间表示，将任务解耦为高层语义定位与底层精准执行。此外，研究者还推出了SlotBench基准测试。实验证明，AnySlot在零样本槽位放置任务中显著优于现有基准模型，实现了亚厘米级的执行精度。
selection_source: fresh_fetch
motivation: 现有的VLA策略在处理需要高精度空间推理和复杂语言指令的槽位级放置任务时，难以兼顾语义定位的准确性与执行的精确度。
method: 提出AnySlot框架，通过生成场景标记将语言指令转化为显式视觉目标，并利用目标条件化的VLA策略实现任务的层次化解耦。
result: 在包含九类任务的SlotBench基准测试中，AnySlot在零样本槽位级放置任务上的表现显著优于传统的扁平化VLA模型和模块化定位方法。
conclusion: 通过引入中间视觉目标来解耦语义理解与动作执行，能有效提升机器人策略在复杂、高精度放置任务中的泛化能力和鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略而言仍是一个重大挑战。插槽级任务既需要可靠的插槽定位（grounding），也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，该框架通过引入显式的空间视觉目标作为语言定位与控制之间的中间表示，降低了组合复杂度。AnySlot 通过生成场景标记（scene marker）将语言转化为显式的视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到现有基准测试缺乏此类对精度要求极高的任务，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置方面显著优于扁平化 VLA 基线和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **AnySlot**，一个旨在解决机器人“插槽级精准放置（Slot-level Placement）”任务的视觉-语言-动作（VLA）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：现有的通用机器人策略（如扁平化的 VLA 模型）在处理需要**复杂空间推理**和**亚厘米级精度**的插槽级任务时表现不佳。例如，指令“将方块放入第二大的插槽”要求机器人既要理解复杂的语义关系，又要实现极高的物理对准精度。
*   **研究动机**：
    *   **扁平化模型（Flat VLA）**：将语义理解与动作控制耦合在一起，难以在未见过的复杂布局中保持精度。
    *   **模块化模型（Modular VLM）**：通常预测坐标点，但坐标点缺乏几何上下文且容易产生像素级偏差，导致放置失败。
*   **核心理念**：通过引入一个**显式的空间视觉目标（Visual Goal）**作为中间表示，将高层语义定位与底层运动控制解耦。

### 2. 方法论：核心思想与技术细节
AnySlot 采用两阶段分层设计：
*   **第一阶段：高层目标构建（High-level Goal Construction）**
    *   **语义转图像**：利用现成的图像生成模型（如 Nano-Banana）在机器人头部相机的视图中生成一个“蓝色球体”标记，作为视觉目标。
    *   **3D 重建与多视图投影**：通过深度图将 2D 标记中心反投影到 3D 世界坐标系中，形成 3D 锚点。随后，利用相机外参将该锚点投影到所有相机流（如手腕相机）中，生成视图一致的蓝色球体覆盖层（Overlay）。
*   **第二阶段：底层目标条件化策略（Low-level Policy）**
    *   **模型架构**：使用 $\pi_{0.5}$（基于 PaliGemma-3B 主干和流匹配动作专家）。
    *   **执行逻辑**：底层策略不再处理复杂的推理指令，而是接收固定的指令（如“根据蓝色球体引导移动物体”），专注于将物体精准放置到视觉标记所在的位置。

### 3. 实验设计
*   **基准测试（SlotBench）**：论文推出了专门的仿真基准，包含 9 类挑战：
    1. 序数推理（如：左起第三个）；2. 尺寸比较；3. 垂直推理（最高/最低）；4. 距离推理；5. 组合关系；6. 逻辑否定（不要放左边）；7. 模糊语言；8. 适宜性推理（稳定性）；9. 世界知识（如：靠近百事可乐）。
*   **对比方法**：
    *   **扁平化 VLA**：Diffusion Policy, OpenVLA, $\pi_0, \pi_{0.5}$。
    *   **模块化方法**：RoboPoint, HAMSTER, AnyPlace。
    *   **Oracle**：由人类专家提供完美视觉目标的上限参考。
*   **评估指标**：成功率（SR）、指令准确率（IA，误差需小于 0.02m）、粗略准确率（CA）。

### 4. 资源与算力
*   **硬件使用**：实验在一台 **NVIDIA H200 GPU** 上进行。
*   **训练细节**：底层策略 $\pi_{0.5}$ 在合成数据集 $D_{syn}$ 上进行了全参数微调，训练步数为 **20,000 步**，Batch Size 为 64。
*   **推理延迟**：高层生成模块（如 Nano-Banana 2）的推理延迟约为 11 秒，这在实时性要求极高的场景下是一个考虑因素。

### 5. 实验数量与充分性
*   **实验规模**：每个任务类别进行 50 次测试，涵盖 9 大类任务，总计至少 450 次独立评估。
*   **消融实验**：非常充分。作者对比了不同的高层视觉模型（Molmo, GPT-4, Gemini, Flux 等）和不同的底层策略架构，并验证了合成数据集 $D_{syn}$ 的必要性。
*   **客观性**：通过引入 Oracle 基线和在真实世界 UR10e 机器人上的初步验证，证明了仿真结果的可迁移性和方法的公平性。

### 6. 主要结论与发现
*   **解耦的优越性**：AnySlot 在 SlotBench 上的平均成功率接近 **90%**，而所有扁平化 VLA 基线在复杂推理任务中几乎全部失败（SR 接近 0%）。
*   **视觉目标优于坐标点**：图像生成模型提供的空间标记比 VLM 预测的坐标点更具鲁棒性，能提供更好的几何上下文。
*   **底层策略的适应性**：底层策略需要针对“视觉引导”进行专门微调，仅靠零样本泛化无法达到插槽级精度。

### 7. 优点（亮点）
*   **创新的中间表示**：将复杂的语言指令转化为简单的视觉标记，极大地降低了策略的学习难度。
*   **高精度**：实现了亚厘米级的放置精度，填补了 VLA 模型在精细操作领域的空白。
*   **新基准测试**：SlotBench 为机器人空间推理和精准放置提供了极具挑战性的评估平台。

### 8. 不足与局限
*   **推理延迟**：依赖外部图像生成模型导致单次决策延迟较高（>10秒），目前难以处理动态环境或需要快速反应的任务。
*   **依赖深度信息**：第一阶段的 3D 重建高度依赖深度相机的准确性，在透明或高反光物体（插槽）上可能失效。
*   **单步执行**：目前主要针对单次放置任务，尚未扩展到长程（Long-horizon）的多步骤复杂操作。

（完）

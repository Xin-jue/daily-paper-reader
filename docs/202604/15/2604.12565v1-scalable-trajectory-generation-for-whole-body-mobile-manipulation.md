---
title: Scalable Trajectory Generation for Whole-Body Mobile Manipulation
title_zh: 可扩展的全身移动操作轨迹生成
authors: "Yida Niu, Xinhai Chang, Xin Liu, Ziyuan Jiao, Yixin Zhu"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12565v1"
tags: ["query:q5"]
score: 9.0
evidence: 移动操作数据的可扩展轨迹生成
tldr: 针对移动操作机器人（底盘+机械臂）在非结构化环境中数据获取难、规模小的问题，本文提出AutoMoMa框架。该框架通过统一的AKR建模和GPU加速的并行轨迹优化，实现了极高的数据生成效率（比CPU快80倍），并构建了包含50万条轨迹的大规模数据集。研究证明，大规模高质量数据是提升模仿学习策略泛化能力的关键，为实现通用的全身移动操作提供了基础设施。
selection_source: fresh_fetch
motivation: 现有的移动操作数据获取方法效率低下且成本高昂，难以满足复杂环境下大规模训练数据的需求。
method: 提出AutoMoMa框架，将底盘、机械臂和物体运动学统一为AKR模型，并利用GPU加速的并行轨迹优化技术大规模生成物理有效的轨迹。
result: 实现了每GPU小时生成5000个回合的高效率，并构建了涵盖330个场景、多种机器人和关节物体的50万条轨迹数据集。
conclusion: 实验表明数据稀缺是制约移动操作性能的核心瓶颈，AutoMoMa通过提供大规模、高保真的训练数据，有效提升了下游模仿学习策略的成功率和泛化性。
---

## 摘要
部署在非结构化环境中的机器人必须协调全身运动——即同时移动移动底座和机械臂——以与物理世界进行交互。这种耦合的移动性与灵活性导致状态空间随场景和物体的多样性呈组合式增长，因此需要比固定底座操作规模大得多的数据集。然而，现有的数据获取方法（包括远程操作和规划）要么耗费人力，要么在大规模应用时计算成本极高。核心瓶颈在于缺乏一个可扩展的流水线，用于在多种机器人形态和环境中生成大规模、物理有效且协调的轨迹数据。在此，我们介绍了 AutoMoMa，这是一个 GPU 加速框架，它将 AKR 建模（将底座、机械臂和物体的运动学整合到单一链中）与并行化轨迹优化相结合。AutoMoMa 每 GPU 小时可生成 5,000 个回合（比基于 CPU 的基准快 80 倍以上），并产出了一个包含超过 50 万条物理有效轨迹的数据集，涵盖 330 个场景、多种关节物体和多种机器人形态。此前的数据集被迫在规模、多样性或运动学保真度之间做出妥协；AutoMoMa 则同时解决了这三个问题。通过训练下游模仿学习（IL）策略进一步发现，即使是单个关节物体任务，SOTA 方法也需要数万次演示才能达到约 80% 的成功率，这证实了数据稀缺（而非算法限制）一直是主要的制约因素。因此，AutoMoMa 桥接了高性能规划与可靠的基于 IL 的控制，为协调移动操作研究提供了此前缺失的基础设施。通过使大规模、运动学有效的训练数据变得实用，AutoMoMa 展示了能够在现实世界多样且非结构化设置中运行的可泛化全身机器人策略。

## Abstract
Robots deployed in unstructured environments must coordinate whole-body motion -- simultaneously moving a mobile base and arm -- to interact with the physical world. This coupled mobility and dexterity yields a state space that grows combinatorially with scene and object diversity, demanding datasets far larger than those sufficient for fixed-base manipulation. Yet existing acquisition methods, including teleoperation and planning, are either labor-intensive or computationally prohibitive at scale. The core bottleneck is the lack of a scalable pipeline for generating large-scale, physically valid, coordinated trajectory data across diverse embodiments and environments. Here we introduce AutoMoMa, a GPU-accelerated framework that unifies AKR modeling, which consolidates base, arm, and object kinematics into a single chain, with parallelized trajectory optimization. AutoMoMa achieves 5,000 episodes per GPU-hour (over $80\times$ faster than CPU-based baselines), producing a dataset of over 500k physically valid trajectories spanning 330 scenes, diverse articulated objects, and multiple robot embodiments. Prior datasets were forced to compromise on scale, diversity, or kinematic fidelity; AutoMoMa addresses all three simultaneously. Training downstream IL policies further reveals that even a single articulated-object task requires tens of thousands of demonstrations for SOTA methods to reach $\approx 80\%$ success, confirming that data scarcity -- not algorithmic limitations -- has been the binding constraint. AutoMoMa thus bridges high-performance planning and reliable IL-based control, providing the infrastructure previously missing for coordinated mobile manipulation research. By making large-scale, kinematically valid training data practical, AutoMoMa showcases generalizable whole-body robot policies capable of operating in the diverse, unstructured settings of the real world.

---

## 论文详细总结（自动生成）

以下是对论文《Scalable Trajectory Generation for Whole-Body Mobile Manipulation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：移动操作（Mobile Manipulation）要求机器人底座与机械臂高度协同，以在非结构化环境中完成任务。相比固定底座，移动操作的状态空间随场景和物体多样性呈组合式爆炸增长。
*   **核心痛点**：
    *   **数据获取难**：远程操作（Teleoperation）成本高且难以规模化；强化学习（RL）采样效率极低且存在严重的“模拟到现实”差距。
    *   **规划效率低**：现有的基于 CPU 的全身规划器（如 AKR 框架）生成速度极慢（约 60 条轨迹/小时），无法满足深度学习对大规模数据的需求。
*   **研究目标**：开发一个名为 **AutoMoMa** 的 GPU 加速框架，旨在高效生成大规模、物理有效、运动学保真的全身移动操作轨迹。

### 2. 方法论
*   **核心思想**：将底座、机械臂和操作物体统一建模，并利用 GPU 的并行计算能力加速轨迹优化过程。
*   **关键技术细节**：
    *   **增强运动学表示（AKR）**：通过“运动学反转”技术，将目标物体（如带铰链的门）挂载到机器人的末端执行器上，并将底座简化为虚拟关节，从而将整个系统（底座+手臂+物体）构建为一条单一的运动链。
    *   **GPU 加速优化**：集成 cuRobo 库，在 GPU 上并行执行碰撞检测（使用球体近似几何体）和轨迹优化，解决约束优化问题。
    *   **流水线流程**：
        1.  **任务规范**：定义场景（S）、物体（O）和机器人形态（R）。
        2.  **问题实例化**：构建欧几里得符号距离场（ESDF）环境模型，并进行 AKR 组装。
        3.  **轨迹生成**：在统一的 AKR 配置空间内求解满足任务目标、关节限位及碰撞约束的最优轨迹。
        4.  **多模态渲染**：在 NVIDIA Isaac Sim 中渲染生成 RGB-D 图像和点云数据。

### 3. 实验设计
*   **数据集与场景**：
    *   **场景**：包含 330 个真实场景（30 个来自 AI2-THOR 的增强场景，300 个由 Infinigen 程序化生成的场景）。
    *   **物体**：来自 PartNet-Mobility 的多种关节物体（如微波炉、洗碗机、柜子）。
    *   **机器人**：Summit Franka、TIAGo 和 R1 三种不同形态的移动操作机器人。
*   **Benchmark 与对比**：
    *   **生成效率对比**：对比基于 CPU 的 AKR 规划器。
    *   **下游策略对比**：使用生成的轨迹训练 **DP3**（3D 扩散策略）、**DP**（图像扩散策略）和 **ACT**（动作分块 Transformer）。
    *   **任务类型**：主要评估关节物体开启（如开门）和硬约束下的抓取切换。

### 4. 资源与算力
*   **生成效率**：AutoMoMa 达到了每 GPU 小时生成 **5,000 个回合**的速度，比 CPU 基准快了 **80 倍**以上。
*   **数据规模**：最终生成了超过 **50 万条**物理有效的轨迹。
*   **算力说明**：文中强调了 GPU 加速（利用 NVIDIA Isaac Sim 和 cuRobo），但未明确给出训练下游策略所使用的具体 GPU 型号（如 A100 或 H100）及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的缩放实验，包括从 1,000 条到 100,000 条轨迹的性能演进分析。
*   **消融实验**：
    *   **场景多样性 vs. 轨迹密度**：验证了增加场景数量和增加单场景内起始状态多样性对泛化性能的影响。
    *   **固定抓取 vs. 抓取切换**：证明了多阶段规划在复杂空间中的必要性。
*   **充分性评价**：实验设计非常全面，不仅验证了生成框架的效率，还深入探讨了数据规模对模仿学习策略成功率的决定性作用，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **数据是核心瓶颈**：实验证明，即使是简单的关节物体操作，SOTA 算法也需要数万条演示数据才能达到 80% 以上的成功率。这表明之前的研究受限主要是因为数据稀缺，而非算法本身。
*   **泛化性来源**：增加环境的几何多样性（场景数）是提升策略在未见环境中表现的主要驱动力。
*   **全身协同的必要性**：AutoMoMa 生成的轨迹展现了底座与手臂的自然补偿运动，这对于在狭窄空间内完成任务至关重要。

### 7. 优点
*   **极高的扩展性**：通过 GPU 并行化彻底解决了移动操作数据获取的效率瓶颈。
*   **高保真度**：AKR 建模确保了生成的运动在物理和运动学上是完全有效的，避免了脚本化动作的僵硬。
*   **开源贡献**：提供了目前规模最大的移动操作全身轨迹数据集，为该领域的后续研究奠定了基础设施。

### 8. 不足与局限
*   **依赖先验知识**：规划过程需要已知的物体 URDF 模型和场景几何信息，在完全未知的环境中无法直接生成数据。
*   **几何近似误差**：为了加速碰撞检测使用的球体近似（Sphere-fitting）有时会过于保守，导致在极度拥挤的场景中规划失败。
*   **动态性缺失**：目前主要针对静态环境中的物体交互，尚未涵盖动态障碍物或人机协作场景。
*   **Sim-to-Real 挑战**：虽然在真实机器人上做了初步验证，但大规模策略在现实世界复杂光照和物理摩擦下的鲁棒性仍需进一步探索。

（完）

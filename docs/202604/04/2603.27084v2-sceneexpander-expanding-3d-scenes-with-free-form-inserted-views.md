---
title: "SceneExpander: Expanding 3D Scenes with Free-Form Inserted Views"
title_zh: SceneExpander：利用自由形式插入视图扩展 3D 场景
authors: "Zijian He, Renjie Liu, Yihao Wang, Weizhi Zhong, Huan Yuan, Kun Gai, Guangrun Wang, Guanbin Li"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27084v2"
tags: ["query:q3"]
score: 9.0
evidence: 用于仿真和世界构建的3D场景扩展
tldr: 针对3D场景构建中迭代扩展的需求，本文提出SceneExpander，旨在将生成模型合成的新视角无缝融入现有实景重建中。面对新视角与原场景几何不一致的挑战，该方法通过测试时自适应技术，结合锚点蒸馏和插入视角自蒸馏信号，在保持原场景稳定的同时，有效处理几何偏移和伪影，实现了高质量、多视图一致的场景扩展。
selection_source: fresh_fetch
motivation: 现有的3D场景编辑难以处理用户插入的、与原场景几何不一致的新视角，导致全局多视图一致性被破坏。
method: 提出一种基于测试时自适应的重建框架，利用锚点蒸馏稳定原场景几何，并通过插入视角自蒸馏来修正不一致的生成内容。
result: 在ETH场景和在线数据集上的实验证明，该方法在处理视角不对齐时具有更优的扩展效果和重建质量。
conclusion: SceneExpander为受控的3D场景迭代扩展提供了一种有效的解决方案，显著增强了重建模型对非对齐生成内容的鲁棒性。
---

## 摘要
使用 3D 场景表示进行世界构建对于内容创作、模拟和交互体验日益重要，然而实际的工作流程本质上是迭代的：创作者必须在用户控制下反复扩展现有场景。受此研究空白的启发，我们研究了一种以用户为中心的工作流中的 3D 场景扩展：从多视图图像捕获的真实场景开始，我们通过插入由生成模型合成的额外视图来扩展其覆盖范围。与固定场景中的简单对象编辑或风格迁移不同，插入的视图通常与原始重建在 3D 上不一致，从而引入几何偏移、幻觉内容或破坏全局多视图一致性的视点相关伪影。为了应对这一挑战，我们提出了 SceneExpander，它将测试时自适应应用于参数化前馈 3D 重建模型，并结合了两种互补的蒸馏信号：锚点蒸馏通过从捕获的视图中提取几何线索来稳定原始场景，而插入视图自蒸馏在保留受观测支持的预测的同时，调整潜在几何和外观以适应不一致的插入视图。在 ETH 场景和在线数据上的实验表明，在存在不一致的情况下，该方法具有更好的扩展行为和重建质量。

## Abstract
World building with 3D scene representations is increasingly important for content creation, simulation, and interactive experiences, yet real workflows are inherently iterative: creators must repeatedly extend an existing scene under user control. Motivated by this research gap, we study 3D scene expansion in a user-centric workflow: starting from a real scene captured by multi-view images, we extend its coverage by inserting an additional view synthesized by a generative model. Unlike simple object editing or style transfer in a fixed scene, the inserted view is often 3D-misaligned with the original reconstruction, introducing geometry shifts, hallucinated content, or view-dependent artifacts that break global multi-view consistency. To address the challenge, we propose SceneExpander, which applies test-time adaptation to a parametric feed-forward 3D reconstruction model with two complementary distillation signals: anchor distillation stabilizes the original scene by distilling geometric cues from the captured views, while inserted-view self-distillation preserves observation-supported predictions yet adapts latent geometry and appearance to accommodate the misaligned inserted view. Experiments on ETH scenes and online data demonstrate improved expansion behavior and reconstruction quality under misalignment.

---

## 论文详细总结（自动生成）

### 论文总结：SceneExpander —— 利用自由形式插入视图扩展 3D 场景

#### 1. 核心问题与整体含义（研究动机和背景）
在 3D 世界构建（World Building）中，创作者通常需要迭代式地扩展场景。现有的 3D 重建方法通常假设所有输入视图在几何上是严格一致的。然而，当用户利用生成模型（如 AIGC 工具）合成一个新视角来扩展场景边界时，这些合成图像往往存在**3D 不一致性**（几何偏移、内容幻觉或视点伪影）。
*   **核心挑战**：如何将一个与原场景几何“不对齐”的生成视图融入现有的 3D 重建中，既要保证原场景不被破坏（不产生重影或扭曲），又要能自然地呈现新增的扩展内容。
*   **研究目标**：提出一种以用户为中心的 3D 场景扩展工作流，实现鲁棒的、受控的场景边界外扩。

#### 2. 方法论
SceneExpander 基于参数化前馈重建模型（以 WorldMirror 为骨干），通过**测试时自适应（Test-Time Adaptation, TTA）**技术来整合新视图。其核心包含以下关键技术：
*   **双重蒸馏信号**：
    *   **锚点蒸馏（Anchor Distillation）**：使用预插入前的模型作为冻结的“教师”，提取原捕获视图的相机参数、深度图和法线图作为参考，强制学生模型在适配新视图时保持原场景的几何稳定性。
    *   **插入视图自蒸馏（Inserted-view Self-distillation）**：为处理生成视图的噪声，使用指数移动平均（EMA）更新的教师模型为插入视图提供“软标签”，使模型逐步学习新内容，避免直接拟合导致的几何崩溃。
*   **几何扰动增强（Geometry-Perturbation Augmentation）**：在训练微调阶段，对输入视图进行全局和局部块状的仿射变换，模拟几何偏移，从而提高模型对不对齐输入的鲁棒性。
*   **随机恢复（Stochastic Restoration）**：定期将部分参数重置为原始权重，防止在长时间自适应过程中产生严重的漂移。

#### 3. 实验设计
*   **数据集**：
    *   **ETH 场景**：真实户外场景，具有宽基线和显著的视角变化。
    *   **在线数据集（Online Collection）**：涵盖多样化的室内外场景。
*   **对比基准（Baselines）**：
    *   前馈重建模型：WorldMirror, OmniVGGT。
    *   动态/序列重建模型：TTT3R。
    *   优化类方法：3D Gaussian Splatting (3DGS)。
*   **评估指标**：
    *   **保真度（原区域）**：PSNR, SSIM, LPIPS。
    *   **扩展质量（新增区域）**：利用 GPT-5.2 作为视觉评估器（GPT-IS 插入满意度，GPT-RQ 渲染质量）以及 39 名参与者的人类评估（H-IS, H-RQ）。

#### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节提到**：使用 AdamW 优化器，学习率为 $1 \times 10^{-5}$，采用了 3D Gaussian Splatting 作为底层表示。由于是测试时自适应（TTA），通常涉及对单个场景进行短时间的迭代优化。

#### 5. 实验数量与充分性
*   **实验规模**：在 ETH 和在线数据集上进行了广泛测试，并提供了多个室内外复杂案例（如森林隧道、植物园、古迹等）。
*   **消融实验**：详细分析了锚点蒸馏、数据增强、自蒸馏概率 $p$ 以及随机恢复机制对结果的影响。
*   **客观性**：引入了基于大模型（GPT-V）的自动化评估和人类主观盲测，从定量和定性两个维度验证了方法在处理“不对齐”问题上的优越性。实验设计较为全面且客观。

#### 6. 主要结论与发现
*   **鲁棒性显著提升**：SceneExpander 能够有效处理 3D 不一致的插入视图，避免了传统方法中常见的重影（Ghosting）和几何扭曲。
*   **平衡性**：通过双重蒸馏，模型在“保持原场景忠实度”和“整合新扩展内容”之间达到了良好的平衡。
*   **TTA 的有效性**：证明了对于 AIGC 生成的噪声输入，测试时自适应比直接的前馈推理或硬性约束优化更有效。

#### 7. 优点
*   **实用性强**：针对 3D 创作中“迭代扩展”的真实需求，提出了一套完整的工作流。
*   **处理不一致性的思路新颖**：不追求生成完美的 3D 一致图像，而是通过后端重建算法的鲁棒性来容忍生成端的缺陷。
*   **评估手段先进**：结合了最新的多模态大模型评估方法，解决了 3D 场景扩展缺乏 Ground Truth 的评价难题。

#### 8. 不足与局限
*   **极端不一致处理有限**：当插入视图与原场景存在极度严重的几何冲突时（如文中提到的神社牌匾案例），模型仍无法完全消除伪影。
*   **依赖初始生成质量**：如果 AIGC 生成的视图在语义上完全偏离或质量极差，重建效果会受限。
*   **合成痕迹风险**：扩展区域虽然视觉连贯，但本质上是合成的，可能存在幻觉内容，不适用于对真实性要求极高的取证类场景。

（完）

---
title: Bridging Discrete Planning and Continuous Execution for Redundant Robot
title_zh: 衔接冗余机器人的离散规划与连续执行
authors: "Teng Yan, Yue Yu, Yihan Liu, Bingzhuo Zhong"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02021v1"
tags: ["query:q2"]
score: 9.0
evidence: 7自由度冗余机械臂的路径规划与执行
tldr: 针对冗余机械臂在体素网格强化学习规划中存在的执行抖动和奇异点不稳定问题，本文提出了一种连接离散规划与连续执行的桥接框架。该框架在规划端引入步长归一化的26邻域动作和几何平局打破机制以优化路径，在执行端采用任务优先级阻尼最小二乘（TP-DLS）逆运动学算法，通过零空间投影处理姿态和关节中心化任务。实验表明，该方法显著提升了规划成功率，缩短了路径长度，并大幅降低了关节加速度，实现了平滑稳定的连续执行。
selection_source: fresh_fetch
motivation: 解决冗余机械臂在离散体素规划路径执行过程中出现的步长抖动、关节突变及奇异位姿不稳定性问题。
method: 提出一种包含步长归一化动作空间、几何平局打破机制以及基于任务优先级阻尼最小二乘逆运动学的桥接框架。
result: "在复杂环境下将规划成功率提升至100%，路径长度缩短约28%，且在保持高精度的同时将峰值关节加速度降低了一个数量级以上。"
conclusion: 该框架在不改变原有离散规划器的前提下，有效弥合了离散路径与连续执行间的鸿沟，显著增强了冗余机械臂的运动平滑性和环境适应性。
---

## 摘要
由于其简单性和可重复性，体素网格强化学习被广泛应用于冗余机械臂的路径规划。然而，在七自由度（7-DoF）机械臂上通过逐点数值逆运动学直接执行，往往会导致步长抖动、关节突变以及在奇异构型附近的不稳定性。本研究提出了一种衔接离散规划与连续执行的框架，且无需修改离散规划器本身。在规划端，引入了步长归一化的26邻域笛卡尔动作和几何平局决胜机制，以抑制不必要的转向并消除步长振荡。在执行端，实现了一个任务优先级阻尼最小二乘（TP-DLS）逆运动学层。该层将末端执行器位置视为主要任务，而将姿态和关节中心化作为投影到零空间的从属任务处理，并结合了置信域裁剪和关节速度约束。在随机稀疏、中等和密集环境下的七自由度机械臂实验中，该衔接框架将密集场景下的规划成功率从约0.58提高到1.00，将代表性路径长度从约1.53米缩短至1.10米；在保持末端执行器误差低于1毫米的同时，将峰值关节加速度降低了一个数量级以上，显著提升了冗余机械臂上基于体素强化学习路径的连续执行质量。

## Abstract
Voxel-grid reinforcement learning is widely adopted for path planning in redundant manipulators due to its simplicity and reproducibility. However, direct execution through point-wise numerical inverse kinematics on 7-DoF arms often yields step-size jitter, abrupt joint transitions, and instability near singular configurations. This work proposes a bridging framework between discrete planning and continuous execution without modifying the discrete planner itself. On the planning side, step-normalized 26-neighbor Cartesian actions and a geometric tie-breaking mechanism are introduced to suppress unnecessary turns and eliminate step-size oscillations. On the execution side, a task-priority damped least-squares (TP-DLS) inverse kinematics layer is implemented. This layer treats end-effector position as a primary task, while posture and joint centering are handled as subordinate tasks projected into the null space, combined with trust-region clipping and joint velocity constraints. On a 7-DoF manipulator in random sparse, medium, and dense environments, this bridge raises planning success in dense scenes from about 0.58 to 1.00, shortens representative path length from roughly 1.53 m to 1.10 m, and while keeping end-effector error below 1 mm, reduces peak joint accelerations by over an order of magnitude, substantially improving the continuous execution quality of voxel-based RL paths on redundant manipulators.

---

## 论文详细总结（自动生成）

### 论文总结：衔接冗余机器人的离散规划与连续执行

#### 1. 核心问题与整体含义（研究动机和背景）
冗余机械臂（如 7-DoF 臂）在复杂环境中常采用基于体素网格的强化学习（RL）进行路径规划，因为这种方法简单且易于复现。然而，离散的体素路径与连续的物理执行之间存在严重的**失配（Mismatch）**：
*   **规划端问题**：传统的 26 邻域动作在物理空间中步长不一（轴向、面角、体角步长不同），导致路径出现“锯齿状”振荡和不必要的转弯。
*   **执行端问题**：直接使用逐点数值逆运动学（IK）执行离散路径时，容易在奇异位姿附近产生关节跳变、步长抖动和动态不稳定。
本文的研究动机是开发一个**轻量级的桥接框架**，在不修改底层 RL 算法的前提下，优化离散路径的几何质量并确保连续执行的平滑性。

#### 2. 论文提出的方法论
该框架分为两个耦合层：

*   **几何层（规划端优化）**：
    *   **步长归一化**：将 26 邻域的离散动作映射为统一的物理步长 $\delta$，消除因网格几何特性导致的步长不均。
    *   **几何平局打破机制（Geometric Tie-breaking）**：当多个动作的 Q 值相近时，引入几何评分函数 $J_{geo}$。该函数综合考虑了**转向角**（偏好小角度转向）和**局部避障间隙**，从而在不改变 RL 更新规则的情况下选择更平滑、更安全的路径。
    *   **样条平滑与微步控制**：对离散路径进行三次样条插值，并根据弧长进行重采样，确保进入执行层的参考点分布均匀。

*   **物理层（执行端优化）**：
    *   **任务优先级阻尼最小二乘（TP-DLS）**：
        *   **主任务**：末端执行器位置跟踪，采用阻尼最小二乘法处理奇异点。
        *   **从属任务**：将“关节中心化”（保持关节在限位中间）投影到主任务的零空间中，利用冗余自由度优化姿态。
    *   **置信域裁剪**：对关节增量进行全局范数裁剪和单关节步长限制，严格控制关节速度和加速度。

#### 3. 实验设计
*   **实验平台**：基于 MATLAB 的 7-DoF 冗余机械臂仿真环境。
*   **场景设置**：随机生成的静态环境，分为**稀疏**（0-30个障碍物）、**中等**（31-99个）和**密集**（100个以上）三类。
*   **Benchmark 与对比方法**：
    *   **规划器对比**：Original（6 邻域）、NoNorm（未归一化的 26 邻域）、Improved（本文提出的归一化+平局打破机制）。
    *   **执行器对比**：Num-IK（传统的逐点数值 IK 求解器） vs. TP-DLS（本文方法）。
*   **评估指标**：规划成功率、路径长度、转向次数、最小避障间隙、关节速度/加速度峰值、关节限位裕度。

#### 4. 资源与算力
*   论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   由于采用的是基于 MATLAB 的仿真和表格化 Q-learning（Tabular Q-learning），推测其对算力的需求相对较低，重点在于算法逻辑的验证而非大规模深度学习训练。

#### 5. 实验数量与充分性
*   **实验规模**：针对每种障碍物密度类别生成了 1,000 个随机任务，总计 **3,000 个测试任务**。
*   **充分性**：实验覆盖了从简单到极高难度的不同环境，样本量足以支撑统计学意义上的结论。
*   **客观性**：通过固定相同的 RL 训练参数和参考路径来对比执行器性能，有效地隔离了变量，确保了对比的公平性。

#### 6. 主要结论与发现
*   **规划性能提升**：在密集障碍物场景下，本文方法将规划成功率从 **0.58 提升至 1.00**，路径长度从 1.53m 缩短至 1.10m。
*   **执行质量改善**：在保持末端位置误差小于 1mm 的前提下，TP-DLS 将**峰值关节加速度降低了一个数量级以上**。
*   **冗余利用**：通过零空间投影，机械臂在执行任务时能更好地避开关节限位，提高了运动的鲁棒性。

#### 7. 优点
*   **非侵入式设计**：作为“桥接层”，它不需要重新设计或重新训练现有的 RL 规划器，具有很强的通用性。
*   **解耦性强**：成功地将任务空间的几何路径规划与关节空间的动力学约束执行进行了有效解耦。
*   **综合优化**：不仅解决了“能不能到”的问题，还通过几何约束解决了“走得好不好”的问题。

#### 8. 不足与局限
*   **计算开销**：TP-DLS 涉及多次小步长迭代和回溯，其计算时间显著高于简单的数值 IK（实验数据显示耗时增加了约 40-50 倍）。
*   **自碰撞检测**：论文明确提到未考虑机械臂自身的碰撞（Self-collision），这在实际物理部署中是一个必须解决的安全隐患。
*   **环境局限**：目前仅在静态障碍物环境下验证，尚未探讨动态障碍物或多臂协作等更复杂的场景。
*   **实机验证缺失**：所有结论均基于 MATLAB 仿真，缺乏在真实物理机器人上的部署数据。

（完）

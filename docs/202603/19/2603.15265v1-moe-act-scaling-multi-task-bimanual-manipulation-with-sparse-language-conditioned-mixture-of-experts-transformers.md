---
title: "MoE-ACT: Scaling Multi-Task Bimanual Manipulation with Sparse Language-Conditioned Mixture-of-Experts Transformers"
title_zh: MoE-ACT：利用稀疏语言条件混合专家 Transformer 扩展多任务双臂操作
authors: "Kangjun Guo, Haichao Liu, Yanji Sun, Ruhan Zhao, Jinni Zhou, Jun Ma"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15265v1"
tags: ["query:q2"]
score: 9.0
evidence: 使用Transformer策略的多任务双臂操作
tldr: "本研究针对多任务双臂操作中的任务干扰问题，提出了MoE-ACT框架。该框架在ACT的Transformer编码器中引入稀疏专家混合（MoE）模块，通过自适应激活解耦多任务动作分布。结合FiLM调制和多尺度交叉注意力机制，实现了语言引导的视觉中心化动作生成。实验表明，MoE-ACT在模拟和真实场景中显著提升了多任务性能，成功率比原始ACT提高33%，展现了极强的泛化能力。"
selection_source: fresh_fetch
motivation: 解决通用机器人策略在多任务训练中因任务间分布差异导致的严重干扰和负迁移问题。
method: 在ACT架构中集成稀疏MoE模块以解耦任务分布，并利用FiLM和多尺度交叉注意力融合语言指令与多级视觉特征。
result: "在仿真和真实双臂机器人实验中，MoE-ACT的平均成功率比基准模型ACT提升了33%。"
conclusion: MoE-ACT通过稀疏专家机制和多模态融合，显著增强了复杂多任务双臂操作的鲁棒性和泛化能力。
---

## 摘要
机器人通过统一策略处理多项任务的能力，对于在现实家庭和工业应用中部署具身智能至关重要。然而，在训练通用机器人策略时，任务间的分布外（out-of-distribution）差异往往会导致严重的任务干扰和负迁移。为应对这一挑战，我们提出了一种用于双臂操作的轻量级多任务模仿学习框架，称为混合专家增强型动作分块 Transformer（MoE-ACT），该框架将稀疏混合专家（MoE）模块集成到 ACT 的 Transformer 编码器中。MoE 层将统一的任务策略分解为独立调用的专家组件。通过自适应激活，它在潜空间中自然地解耦了多任务动作分布。在解码过程中，特征线性调制（FiLM）动态调制动作标记（action tokens），以提高动作生成与任务指令之间的一致性。同时，多尺度交叉注意力机制使策略能够同时关注低层和高层语义特征，为机器人操作提供丰富的视觉信息。我们进一步融入了文本信息，将框架从纯视觉模型转变为以视觉为中心、语言条件的动作生成系统。在仿真和真实双臂设置中的实验验证表明，MoE-ACT 显著提升了多任务性能。具体而言，MoE-ACT 的成功率比原始 ACT 平均高出 33%。这些结果表明，MoE-ACT 在复杂的多任务双臂操作环境中提供了更强的鲁棒性和泛化能力。我们的开源项目页面位于 https://j3k7.github.io/MoE-ACT/。

## Abstract
The ability of robots to handle multiple tasks under a unified policy is critical for deploying embodied intelligence in real-world household and industrial applications. However, out-of-distribution variation across tasks often causes severe task interference and negative transfer when training general robotic policies. To address this challenge, we propose a lightweight multi-task imitation learning framework for bimanual manipulation, termed Mixture-of-Experts-Enhanced Action Chunking Transformer (MoE-ACT), which integrates sparse Mixture-of-Experts (MoE) modules into the Transformer encoder of ACT. The MoE layer decomposes a unified task policy into independently invoked expert components. Through adaptive activation, it naturally decouples multi-task action distributions in latent space. During decoding, Feature-wise Linear Modulation (FiLM) dynamically modulates action tokens to improve consistency between action generation and task instructions. In parallel, multi-scale cross-attention enables the policy to simultaneously focus on both low-level and high-level semantic features, providing rich visual information for robotic manipulation. We further incorporate textual information, transitioning the framework from a purely vision-based model to a vision-centric, language-conditioned action generation system. Experimental validation in both simulation and a real-world dual-arm setup shows that MoE-ACT substantially improves multi-task performance. Specifically, MoE-ACT outperforms vanilla ACT by an average of 33% in success rate. These results indicate that MoE-ACT provides stronger robustness and generalization in complex multi-task bimanual manipulation environments. Our open-source project page can be found at https://j3k7.github.io/MoE-ACT/.

---

## 论文详细总结（自动生成）

### MoE-ACT：利用稀疏语言条件混合专家 Transformer 扩展多任务双臂操作

#### 1. 核心问题与研究动机
*   **核心问题**：在训练通用的机器人双臂操作策略时，不同任务之间的分布差异会导致严重的“任务干扰”和“负迁移”现象，使得单一模型难以同时胜任多项复杂任务。
*   **研究动机**：
    *   现有的模仿学习方法（如 ACT、Diffusion Policy）在单任务中表现优异，但在多任务设置下性能下降明显。
    *   大型视觉-语言-动作（VLA）模型虽然泛化性强，但参数量巨大、计算成本高且推理延迟高，难以满足实时机器人控制的需求。
    *   需要一种轻量级、高效且能有效解耦不同任务特征的架构，以实现鲁棒的多任务双臂操作。

#### 2. 核心方法论
MoE-ACT 建立在动作分块 Transformer（ACT）架构之上，通过以下关键技术进行增强：
*   **稀疏混合专家（MoE）编码器**：在 Transformer 编码器的前馈网络（FFN）中集成 MoE 模块。通过一个可学习的路由网络（Router），根据输入特征动态激活前 $k$ 个专家（文中 $k=2$）。这种稀疏激活机制在不显著增加计算开销的前提下，大幅提升了模型容量，并实现了任务表示在潜空间中的自然解耦。
*   **任务条件 FiLM 调制**：在解码器中引入特征线性调制（FiLM）。将语言指令转化为任务嵌入，生成缩放因子（$\gamma$）和偏移参数（$\beta$），动态调整动作标记（Action Tokens），确保生成的动作序列与任务指令保持高度一致。
*   **多尺度交叉注意力机制**：解码器不仅关注编码器的最终高层语义输出，还通过额外的交叉注意力层捕捉 CNN 骨干网络或编码器底层的低层视觉细节（如物体边缘、机械臂位置），从而提升操作精度。
*   **训练目标**：采用复合损失函数，包括动作重建损失（L1）、CVAE 的 KL 散度损失，以及为了防止专家模式崩溃而设计的 MoE 辅助损失（包括熵损失、负载均衡损失和正交损失）。

#### 3. 实验设计
*   **仿真实验**：
    *   **数据集/场景**：使用 RoboTwin 2.0 基准测试，包含 6 个代表性的双臂操作任务（如递瓶子、放锅、炸薯条等）。
    *   **Benchmark**：每个任务 50 个演示视频，共 300 个回合进行联合训练。
    *   **对比方法**：ACT、Diffusion Policy (DP)、RDT-1B（扩散基础模型）、BAKU。
*   **真实世界实验**：
    *   **场景**：使用 AIRBOT MMK2 双臂机器人平台，执行“递瓶子”和“方块入盒”两个任务。
    *   **设置**：收集 100 条遥操作轨迹（每个任务 50 条），每种方法进行 30 次测试。

#### 4. 资源与算力
*   **GPU 型号与数量**：仿真训练在 **NVIDIA A800** GPU 上完成；真实世界模型推理在 **RTX 3050** GPU 上运行。
*   **训练参数**：使用 Adam 优化器，训练 4000 个 epoch，初始学习率为 $1 \times 10^{-5}$，batch size 为 8。
*   **推理效率**：由于采用了稀疏 MoE 结构，模型保持了轻量化特性，能够满足实时控制需求。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 种仿真任务和 2 种真实任务，总计进行了数百次评估试验。
*   **消融实验**：专门针对 FiLM 调制和多尺度交叉注意力进行了消融研究，验证了各组件对成功率的贡献（分别提升了约 9% 和 8%）。
*   **可视化分析**：通过专家激活权重图和注意力热力图，直观展示了模型如何根据不同任务选择专家以及如何关注视觉特征。
*   **评价**：实验设计较为全面，对比了当前主流的扩散模型和 Transformer 模型，涵盖了从仿真到现实的迁移验证，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升**：MoE-ACT 在仿真环境中的平均成功率达到 55%，比原始 ACT 提升了 **33%**，也优于 RDT 和 BAKU 等强基准。
*   **任务解耦有效性**：可视化结果证实，MoE 模块能够针对不同任务激活特定的专家子集，有效缓解了多任务干扰。
*   **视觉融合的重要性**：多尺度交叉注意力证明了低层视觉特征对于精细操作（如抓取小物体）至关重要。
*   **真实世界泛化**：在物理机器人上，MoE-ACT 的平均成功率达到 60%，显著高于 ACT（35%）和 BAKU（47%）。

#### 7. 优点
*   **架构优雅**：将 MoE 巧妙集成进 ACT，既保留了动作分块的高效性，又解决了多任务扩展性问题。
*   **轻量高效**：相比于动辄数十亿参数的 VLA 模型，MoE-ACT 在普通 GPU 上即可实现高性能推理。
*   **多模态融合**：通过 FiLM 和多尺度注意力，有机结合了语言指令、高层语义和底层视觉信息。

#### 8. 不足与局限
*   **复杂任务瓶颈**：在某些极高难度的任务（如 Skillet 任务）中，成功率仍然较低（仅 12%），说明模型在处理极长时序或极高精度要求的任务时仍有提升空间。
*   **数据依赖性**：真实世界实验中发现，当物体初始位置发生大幅偏移时，模型容易失败，这反映出模型对训练数据多样性的依赖，泛化鲁棒性仍受限于数据集规模。
*   **超参数敏感性**：MoE 的专家数量、路由策略等超参数目前仍需经验性调优，缺乏自动化的配置方案。

（完）

---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v1"
tags: ["query:q7"]
score: 9.0
evidence: 用于机器人策略学习和视觉动力学的世界动作模型
tldr: 本研究针对现有世界-动作模型（WAM）推理开销大及视觉与动作表征耦合的问题，提出了GigaWorld-Policy。该模型采用以动作为中心的设计，通过因果建模确保动作预测不依赖于未来视频生成，从而实现高效推理。在推理时，视频生成变为可选，显著提升了部署速度。实验表明，该模型在保持高成功率的同时，推理速度比领先基准快9倍，在复杂机器人任务中表现优异。
selection_source: fresh_fetch
motivation: 现有世界-动作模型推理开销巨大且动作预测过度依赖视频生成质量，限制了其实际部署。
method: 提出一种以动作为中心的因果建模框架，将动作预测与视频生成解耦，使推理时无需生成视频即可快速输出动作。
result: "在真实机器人平台上，该模型推理速度比Motus快9倍，成功率提升7%，在RoboTwin 2.0上性能提升95%。"
conclusion: GigaWorld-Policy证明了通过解耦视觉动力学与动作预测，可以在不牺牲性能的前提下大幅提升机器人策略的推理效率。
---

## 摘要
从预训练视频生成骨干网络初始化的世界-动作模型（WAM）在机器人策略学习中展现出巨大潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模通常会使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为解决这些问题，我们推出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习 2D 像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练表述为两个耦合的组件：模型根据当前观测预测未来动作序列，并同时根据预测的动作和相同的观测生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束促使产生符合物理规律的动作。通过防止未来视频标记影响动作标记的因果设计，显式的未来视频生成在推理阶段是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化、大规模的机器人数据集，用于预训练一个以动作为中心的视频生成模型，随后将其适配为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

以下是对论文《GigaWorld-Policy: An Efficient Action-Centered World–Action Model》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型面临**监督信号稀疏**的问题（动作标签维度低且多样性差），而引入视频生成的**世界-动作模型（WAM）**虽然提供了密集的视觉监督，但存在**推理延迟高**（需迭代采样大量视频序列）和**误差累积**（视频预测错误会干扰动作解码）的缺陷。
*   **研究背景**：在机器人控制中，如何在利用世界模型带来的物理一致性和密集监督信号的同时，实现低延迟的闭环控制，是当前具身智能领域的关键挑战。

### 2. 方法论
*   **核心思想**：提出一种**以动作为中心（Action-Centered）**的世界-动作模型。在训练阶段，利用未来视觉动态作为推理信号和密集监督源；在推理阶段，使未来视频预测成为**可选（Optional）**，从而实现低延迟的动作生成。
*   **关键技术细节**：
    *   **因果序列建模**：采用基于 Diffusion Transformer (DiT) 的因果序列模型，将状态、观测、动作和未来视频帧转化为 Token。
    *   **因果注意力掩码（Causal Mask）**：这是核心创新。掩码确保：(1) 动作 Token 只能关注当前观测和状态；(2) 未来视频 Token 可以关注观测、状态和动作。这种设计防止了训练时的信息泄露，并允许推理时跳过视频生成。
    *   **多阶段课程训练**：
        1.  **通用视频预训练**：从大规模互联网视频模型（Wan-2.2 5B）初始化。
        2.  **具身数据预训练**：在约 10,000 小时的机器人相关视频（真实机器人录像 + 第一视角人类视频）上进一步预训练，注入物理先验。
        3.  **任务特定后训练**：在目标机器人的轨迹数据上进行对齐训练。
*   **公式与流程**：使用 **Flow Matching（流匹配）** 目标函数同时优化动作预测（$\mathcal{L}_{action}$）和视频预测（$\mathcal{L}_{video}$）。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：RoboTwin 2.0，包含 50 多个具有领域随机化（清洁、随机场景）的操纵任务。
    *   **真实世界**：使用 AgileX PiPER 6自由度机械臂，执行“清理桌面”、“扫描二维码”、“堆叠碗具”、“扫除垃圾”四个复杂任务。
*   **Benchmark 与对比方法**：
    *   **VLA 模型**：$\pi$ 0.5、GigaBrain-0、X-VLA。
    *   **WAM 模型**：Motus、Cosmos-Policy。

### 4. 资源与算力
*   **模型规模**：基于 5B 参数的 Diffusion Transformer。
*   **训练算力**：预训练阶段在公共数据集上消耗了 **6000 GPU 小时**（未明确具体型号，但通常为 A100/H100 级别），全局 Batch Size 为 256。
*   **推理算力**：在 **NVIDIA A100 GPU** 上进行测试。
*   **推理延迟**：GigaWorld-Policy 达到 **0.36s/步**，而对比模型 Motus 为 3.23s/步。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖 50+ 任务，每个任务 100 个测试回合。
    *   真实世界实验每个任务进行 20 次试验。
    *   进行了详尽的**消融实验**，包括预训练阶段的作用、采样间隔 $\Delta$ 的影响、因果掩码的有效性以及数据效率分析。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实的迁移，并对比了当前最先进的基准模型。通过数据效率实验证明了该模型仅需 10% 的数据即可达到传统 VLA 的性能，体现了实验的客观性和深度。

### 6. 主要结论与发现
*   **高效性**：GigaWorld-Policy 在推理速度上比 SOTA 世界模型 Motus 快 **9 倍**。
*   **性能提升**：在真实世界中成功率比 Motus 高出 **7%**；在仿真环境 RoboTwin 2.0 中，性能比 $\pi$ 0.5 提升了 **95%**。
*   **密集监督的价值**：引入未来视觉预测作为辅助任务，显著增强了模型对物理规律的理解，从而提高了动作预测的准确性。
*   **解耦优势**：因果掩码设计成功实现了“训练时耦合监督，推理时解耦执行”，解决了 WAM 难以实时部署的痛点。

### 7. 优点
*   **架构创新**：通过简单的因果掩码巧妙解决了世界模型推理延迟高的顽疾。
*   **部署友好**：动作预测不依赖视频生成的迭代采样，适合闭环控制。
*   **训练策略**：三阶段课程学习充分利用了互联网视频、具身视频和任务数据，泛化能力强。
*   **高数据效率**：在小样本情况下表现远超传统 VLA 模型。

### 8. 不足与局限
*   **模型体量**：5B 参数对于某些嵌入式边缘计算设备仍显沉重，可能需要进一步压缩或量化。
*   **多视角依赖**：模型依赖固定的三视角复合图像输入，对于视角缺失或变化的鲁棒性有待进一步验证。
*   **长程任务挑战**：虽然在复杂操纵任务上表现良好，但对于极长时序（如数分钟）的任务，视频预测的辅助作用是否会因步长 $\Delta$ 的限制而减弱尚不明确。

（完）

---
title: "COAD: Constant-Time Planning for Continuous Goal Manipulation with Compressed Library and Online Adaptation"
title_zh: COAD：基于压缩库和在线自适应的连续目标操作常数时间规划
authors: "Adil Shiyas, Zhuoyun Zhong, Constantinos Chamzas"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12488v1"
tags: ["query:q2"]
score: 9.0
evidence: 连续目标机器人操作任务的常数时间规划
tldr: 针对机器人操作中目标位置多变导致的重复规划问题，本文提出COAD框架。该框架将连续任务空间离散化为任务覆盖区域，通过仅存储少量代表性根问题的解来构建压缩库。在查询时，系统能以常数时间检索根运动，并利用轻量级模块（如线性插值或DMP）快速适配至新目标。实验证明，COAD在保持高成功率的同时，实现了亚毫秒级的查询速度和极高的存储压缩率，有效解决了连续任务空间的覆盖与存储难题。
selection_source: fresh_fetch
motivation: 现有的基于经验的规划方法在处理连续任务空间时，面临存储开销巨大且难以保证完全覆盖的问题。
method: 通过将任务空间划分为覆盖区域并仅存储根解，结合在线轻量级适配模块实现高效的运动生成。
result: 在多种机器人和环境测试中，COAD实现了亚毫秒级的查询响应和显著的库压缩效果，且路径质量优于基准方法。
conclusion: COAD为连续目标参数化的机器人操作提供了一种高效、低存储且具备常数时间复杂度的规划解决方案。
---

## 摘要
在许多机器人操作任务中，机器人需要重复解决运动规划问题，这些问题的主要区别在于目标物体及其相关障碍物的位置，而周围的工作空间保持不变。先前的研究表明，利用经验和离线计算可以加速重复的规划查询，但它们缺乏覆盖连续任务空间的保证，并且需要存储庞大的解库。在这项工作中，我们提出了 COAD，这是一个在连续目标参数化任务空间上提供常数时间规划的框架。COAD 将连续任务空间离散化为有限个任务覆盖区域。它并不离线规划并存储每个区域的解，而是通过仅解决具有代表性的根问题来构建压缩库。其他问题则通过对这些根解进行快速自适应来处理。在查询时，系统以常数时间检索根运动，并使用轻量级自适应模块（如线性插值、动态运动基元或简单的轨迹优化）将其调整至目标位置。我们在仿真和真实世界的各种机械臂及环境中评估了该框架，结果表明 COAD 在实现运动库大幅压缩的同时，保持了高成功率和亚毫秒级的查询速度，在效率和路径质量上均优于基准方法。源代码可在 https://github.com/elpis-lab/CoAd 获取。

## Abstract
In many robotic manipulation tasks, the robot repeatedly solves motion-planning problems that differ mainly in the location of the goal object and its associated obstacle, while the surrounding workspace remains fixed. Prior works have shown that leveraging experience and offline computation can accelerate repeated planning queries, but they lack guarantees of covering the continuous task space and require storing large libraries of solutions. In this work, we present COAD, a framework that provides constant-time planning over a continuous goal-parameterized task space. COAD discretizes the continuous task space into finitely many Task Coverage Regions. Instead of planning and storing solutions for every region offline, it constructs a compressed library by only solving representative root problems. Other problems are handled through fast adaptation from these root solutions. At query time, the system retrieves a root motion in constant time and adapts it to the desired goal using lightweight adaptation modules such as linear interpolation, Dynamic Movement Primitives, or simple trajectory optimization. We evaluate the framework on various manipulators and environments in simulation and the real world, showing that COAD achieves substantial compression of the motion library while maintaining high success rates and sub-millisecond-level queries, outperforming baseline methods in both efficiency and path quality. The source code is available at https://github.com/elpis-lab/CoAd.

---

## 论文详细总结（自动生成）

以下是对论文《COAD: Constant-Time Planning for Continuous Goal Manipulation with Compressed Library and Online Adaptation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在工业生产（如分拣、包装）中，机器人常需在固定工作空间内重复执行任务，唯一的变量是目标物体的位姿（在连续空间内变化）。
*   **现有挑战**：传统的采样算法（如 RRT）在线计算慢且无时间保证；基于经验的方法虽快，但通常需要存储海量解库，且难以保证能覆盖连续任务空间中的所有可能情况。
*   **核心目标**：提出一种名为 **COAD** 的框架，旨在实现**常数时间（Constant-Time）**的在线规划，同时通过**压缩库**显著降低存储开销，并保证对连续目标空间的完全覆盖。

### 2. 方法论：核心思想与技术细节
COAD 的核心在于将连续的任务空间离散化，并结合离线预计算与在线快速自适应。
*   **任务覆盖区域 (TCR)**：
    *   引入 TCR 概念（TSR 的逆向应用）：定义为一个固定的末端执行器位姿能够解决的所有物体位姿集合。
    *   证明了 TCR 内部包含轴对齐框（Axis-aligned box），从而可以将连续的 $SE(3)$ 目标空间划分为有限且可索引的网格。
*   **离线库构建与压缩 (Offline Stage)**：
    *   **根运动 (Root Motion)**：算法随机选择一个未覆盖的 TCR 区域，调用运动规划器生成一条“根路径”。
    *   **邻域自适应**：尝试将该根路径通过轻量级算法适配到相邻的 TCR 区域。如果适配后的路径无碰撞且有效，则这些区域共享该根运动的索引，从而实现高达 97% 的存储压缩。
*   **在线查询与自适应 (Online Stage)**：
    *   **常数时间检索**：通过物体位姿直接计算网格索引，在 $O(1)$ 时间内检索对应的根运动。
    *   **三种自适应模块**：
        1.  **线性插值 (LI)**：最快，直接连接路径末端与新目标。
        2.  **动态运动基元 (DMP)**：通过动力学系统保持路径形状，质量更高。
        3.  **简单轨迹优化 (STO)**：利用二次规划 (QP) 在微秒级内优化路径。

### 3. 实验设计
*   **实验场景**：使用了 **Panda (7-DOF)** 和 **Fetch (8-DOF)** 机械臂，在三种复杂度不同的仿真环境（Table、Cage、Shelf）中测试。
*   **真实世界验证**：使用 **UR10** 机械臂进行实物抓取实验，验证算法的迁移能力。
*   **Benchmark 与对比方法**：
    *   **RRT-Connect**：经典的采样规划器基准。
    *   **Library Baseline**：传统的基于经验的库（使用最近邻检索和路径修复）。
    *   **Full Library**：未压缩的完整解库（作为存储对比）。

### 4. 资源与算力
*   **硬件环境**：实验在配备 **Intel i7-14700 CPU** 和 **32GB RAM** 的机器上运行。
*   **算力说明**：论文未提及使用 GPU，因为该方法主要依赖几何计算、运动规划和二次规划（OSQP 求解器），属于 CPU 密集型任务。
*   **在线耗时**：查询时间极低，通常在 **0.01ms 到 5ms** 之间。

### 5. 实验数量与充分性
*   **实验规模**：每个设置下进行了 **1000 次随机查询**，涵盖了不同的机器人和环境组合。
*   **真实验证**：在 UR10 机器人上进行了 **20 次** 随机位姿抓取实验。
*   **充分性评价**：实验设计较为充分，对比了路径成功率、路径质量（长度）、计算时间和存储压缩率。通过不同复杂度的环境（如狭窄通道的 Shelf 和障碍物密集的 Cage）验证了算法的鲁棒性。

### 6. 主要结论与发现
*   **极高的压缩率**：COAD 相比完整库减少了 **72% - 99%** 的存储需求。
*   **常数时间保证**：在线查询速度比 RRT-Connect 快 **2 到 3 个数量级**，实现了真正的亚毫秒级响应。
*   **高成功率**：在已覆盖的连续空间内，COAD 达到了 **100% 的成功率**，优于需要在线修复的传统经验库方法。
*   **路径质量**：通过 DMP 或 STO 适配生成的路径质量优于 RRT-Connect。

### 7. 优点（亮点）
*   **理论支撑**：通过 TCR 理论保证了对连续空间的有限离散化覆盖。
*   **效率极高**：将复杂的碰撞检测和路径搜索移至离线阶段，在线阶段仅保留 $O(1)$ 检索和轻量级变形。
*   **灵活性**：提供了多种自适应模块（LI, DMP, STO），用户可根据实时性需求权衡路径质量。

### 8. 不足与局限
*   **半静态环境假设**：算法假设工作空间内的障碍物是静止的，仅目标物体位姿改变。若环境发生重大变化，需重新构建库。
*   **离线计算开销**：虽然在线快，但离线构建 TCR 覆盖和验证适配路径需要一定的时间预处理。
*   **根节点选择**：目前采用随机选择根区域，可能不是最优的压缩策略（未来可优化根节点的分布以进一步减小库体积）。
*   **参数依赖**：网格离散化的粒度（$\delta$）依赖于 TSR 的定义，对于极其复杂的约束，可能需要更精细的调参。

（完）

---
title: "World2Act: Latent Action Post-Training via Skill-Compositional World Models"
title_zh: World2Act：通过技能组合世界模型进行潜空间动作后训练
authors: "An Dinh Vuong, Tuan Van Vo, Abdullah Sohail, Haoran Ding, Liang Ma, Xiaodan Liang, Anqing Duan, Ivan Laptev, Ian Reid"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10422v1"
tags: ["query:q7"]
score: 9.0
evidence: 使用技能组合世界模型对VLA策略进行后训练
tldr: 本研究提出World2Act框架，旨在解决基于世界模型（WM）的视觉-语言-动作（VLA）策略在后训练中过度依赖像素空间监督的问题。通过对比匹配目标将动作与WM的潜在动态特征对齐，并引入基于LLM的技能分解流水线，构建了支持长时任务的技能组合世界模型。该方法在RoboCasa和LIBERO基准测试中达到SOTA水平，并显著提升了真实世界中具身智能体的泛化能力。
selection_source: fresh_fetch
motivation: 现有的世界模型后训练方法依赖像素级监督，易受生成伪影影响，且难以处理变长任务的视频生成。
method: 提出World2Act框架，利用对比学习将VLA动作与世界模型的潜在特征对齐，并通过LLM驱动的技能分解实现长时一致的视频生成。
result: "在RoboCasa和LIBERO上取得领先成绩，并在真实世界任务中实现了6.7%的性能提升。"
conclusion: 通过潜在空间对齐和技能组合建模，World2Act有效增强了具身智能体在复杂环境下的鲁棒性和泛化性能。
---

## 摘要
世界模型（WMs）已成为后训练视觉-语言-动作（VLA）策略的一种极具前景的方法，旨在提高其在环境变化下的鲁棒性和泛化能力。然而，大多数基于世界模型的后训练方法依赖于像素空间监督，这使得策略对像素级伪影以及不完美世界模型推演（rollouts）产生的幻觉非常敏感。我们提出了 World2Act，这是一个后训练框架，它使用对比匹配目标将 VLA 动作直接与世界模型视频动力学潜变量对齐，从而减少了对像素的依赖。后训练性能与推演质量紧密相关，但当前的世界模型在生成任意长度视频方面面临困难，因为它们大多在固定长度的片段上进行训练，而机器人执行任务的持续时间差异很大。为了解决这个问题，我们提出了一种基于大语言模型（LLM）的自动技能分解流水线，将高层指令分割为底层提示词。我们的流水线生成了 RoboCasa-Skill 和 LIBERO-Skill，支持在不同任务跨度下保持时间一致性的技能组合世界模型。实验表明，将 World2Act 应用于 GR00T-N1.6 和 Cosmos Policy 等 VLA 模型，在 RoboCasa 和 LIBERO 上取得了最先进的结果，并将真实世界性能提升了 6.7%，增强了具身智能体的泛化能力。

## Abstract
World Models (WMs) have emerged as a promising approach for post-training Vision-Language-Action (VLA) policies to improve robustness and generalization under environmental changes. However, most WM-based post-training methods rely on pixel-space supervision, making policies sensitive to pixel-level artifacts and hallucination from imperfect WM rollouts. We introduce World2Act, a post-training framework that aligns VLA actions directly with WM video-dynamics latents using a contrastive matching objective, reducing dependence on pixels. Post-training performance is tied to rollout quality, yet current WMs struggle with arbitrary-length video generation as they are mostly trained on fixed-length clips while robotic execution durations vary widely. To address this, we propose an automatic LLM-based skill-decomposition pipeline that segments high-level instructions into low-level prompts. Our pipeline produces RoboCasa-Skill and LIBERO-Skill, supporting skill-compositional WMs that remain temporally consistent across diverse task horizons. Empirically, applying World2Act to VLAs like GR00T-N1.6 and Cosmos Policy achieves state-of-the-art results on RoboCasa and LIBERO, and improves real-world performance by 6.7%, enhancing embodied agent generalization.

---

## 论文详细总结（自动生成）

### 论文总结：World2Act：通过技能组合世界模型进行潜空间动作后训练

#### 1. 核心问题与整体含义
论文针对具身智能中视觉-语言-动作（VLA）策略的**泛化性与鲁棒性**不足问题展开研究。虽然世界模型（WM）被认为能提供环境动力学先验，但现有的后训练（Post-training）方法主要在**像素空间**（Pixel-space）进行监督（如通过逆动力学模型推断动作或像素级奖励）。这种方式存在两大痛点：
*   **像素敏感性**：世界模型生成的视频常包含伪影或“幻觉”，像素级监督会将这些噪声引入策略，导致训练不稳定。
*   **长时生成难题**：机器人任务时长跨度大，而扩散模型通常在固定长度片段上训练，直接生成长视频易出现时间不一致性。

#### 2. 方法论
World2Act 提出了一个两阶段的后训练框架，核心在于**潜空间对齐**和**技能分解**。

*   **技能分解流水线（Skill-Decomposition）**：
    *   利用 LLM（如 DeepSeek）将高层指令分解为底层原子技能提示词。
    *   根据夹爪状态（Gripper width）自动分割专家演示视频，构建了 **RoboCasa-Skill** 和 **LIBERO-Skill** 数据集。
    *   **技能组合世界模型**：通过自回归方式生成短视频片段并拼接，实现任意长度且时间一致的视频推演。
*   **潜空间动作对齐（Latent Alignment）**：
    *   **第一阶段**：训练视频适配器（Video Adapter）和动作适配器（Action Adapter），将 WM 的视频潜变量与 VLA 的动作序列映射到共享潜空间。
    *   **损失函数**：结合了动作重建损失（MSE）和双向 **InfoNCE 对比损失**，通过对比正负样本（同技能 vs 异技能）强化模态间的语义一致性。
*   **残差策略后训练（Residual Policy Post-training）**：
    *   冻结基础 VLA 模型，引入轻量级的**残差策略网络**。
    *   利用 WM 生成的潜变量动力学作为目标，通过对比损失引导残差网络修正 VLA 的动作输出，使其符合世界模型的物理先验。

#### 3. 实验设计
*   **数据集/场景**：
    *   **RoboCasa**：复杂的厨房操作任务。
    *   **LIBERO**：包含空间、物体、目标和长时程四个维度的机器人学习基准。
    *   **真实世界**：使用 Franka Research 3 机械臂进行抓取、放置和关抽屉任务。
*   **Benchmark 与对比方法**：
    *   **基础 VLA**：π0, π0.5, OpenVLA, Cosmos Policy, GR00T-N1.5/N1.6 等。
    *   **后训练基准**：DreamGen（像素级监督）、VLA-RFT（强化学习微调）、Ctrl-World。
*   **评估指标**：平均成功率（Success Rate, SR）。

#### 4. 资源与算力
*   **训练算力**：
    *   使用 **8 台 AMD Instinct MI210 GPU (48GB VRAM)** 进行世界模型（基于 Cosmos-Predict2）的微调，训练步数为 10k 步。
    *   适配器预训练（Stage 1）耗时约 30k 步。
*   **推理算力**：
    *   所有基准测试和推理均在单张 **NVIDIA RTX 4090 (24GB)** 上完成。
    *   GR00T-N1.6 + World2Act 的推理速度可达 **251.9 Hz**，满足实时控制需求。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务进行 5 个随机种子的测试，每个种子 50 次试验（共 250 次/任务），符合机器人学习的标准协议。
*   **消融实验**：涵盖了残差策略 vs LoRA、对比损失函数形式（单向 vs 双向）、全局相似度 vs 分块对齐、以及训练轨迹数量的缩放效应。
*   **客观性**：对比了多种 SOTA 模型，并在不同架构（GR00T 和 Cosmos）上验证了方法的通用性，实验设计较为全面且公平。

#### 6. 主要结论与发现
*   **潜空间优于像素空间**：World2Act 在 RoboCasa 上显著优于基于像素监督的 DreamGen，证明了潜空间对齐能有效过滤 WM 生成的视觉噪声。
*   **技能分解的必要性**：通过分解任务，世界模型的物理对齐度（PA）和指令遵循度（IF）大幅提升，解决了长视频生成的漂移问题。
*   **性能提升**：在 RoboCasa 上刷新了 SOTA；在真实世界中，相比基础模型提升了 **6.7%** 的成功率。
*   **对齐度与成功率正相关**：实验证实视频-动作潜变量的余弦相似度越高，下游任务的成功率越高。

#### 7. 优点
*   **鲁棒性强**：通过潜空间对齐，使策略对 WM 的视觉幻觉具有免疫力。
*   **数据效率高**：仅需少量合成轨迹即可实现明显的性能增益。
*   **架构无关**：作为一种后训练插件，可以适配不同的 VLA 主干网络。
*   **工程落地性**：提出了自动化的数据处理流水线，并开源了两个技能级数据集。

#### 8. 不足与局限
*   **主干依赖性**：实验发现性能增益受 VLA 基础模型影响较大（如在 Cosmos Policy 上的提升不如 GR00T 显著）。
*   **真实世界鸿沟**：虽然有提升，但真实世界的绝对成功率仍有待提高，表明 WM 尚不能完全捕捉复杂的物理接触动力学。
*   **分解成本**：依赖 LLM 进行技能分解，虽然自动化程度高，但在处理极度复杂的非结构化任务时可能存在分解逻辑错误。

（完）

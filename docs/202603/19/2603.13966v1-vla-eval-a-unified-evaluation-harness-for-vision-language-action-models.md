---
title: "vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models"
title_zh: vla-eval：视觉-语言-动作模型的统一评估框架
authors: "Suhwan Choi, Yunsung Lee, Yubeen Park, Chris Dongjoo Kim, Ranjay Krishna, Dieter Fox, Youngjae Yu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13966v1"
tags: ["query:q14"]
score: 10.0
evidence: VLA基准测试的统一评估框架
tldr: 针对视觉-语言-动作（VLA）模型评估中存在的脚本碎片化、依赖冲突和协议不明确等问题，本文推出了开源评估框架 vla-eval。该框架通过 WebSocket 协议和 Docker 隔离技术，实现了模型推理与基准测试环境的解耦。它支持 13 个基准测试和 6 种模型，通过并行化技术将评估效率提升了 47 倍，并提供了包含 657 条结果的 VLA 排行榜，显著提升了 VLA 模型评估的标准化与效率。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型评估缺乏统一标准，各模型库独立维护脚本导致代码冗余、依赖冲突且实验协议不明确。
method: 提出一种基于 WebSocket 协议和 Docker 环境隔离的解耦架构，通过统一接口实现模型与基准测试的自动交叉评估。
result: 框架支持 13 个基准测试，通过并行化实现 47 倍吞吐量提升，并成功复现了已发表模型性能，同时揭示了影响结果的隐藏因素。
conclusion: vla-eval 为 VLA 模型提供了高效、可复现且标准化的评估基础设施，有助于推动该领域的公平比较和快速迭代。
---

## 摘要
视觉-语言-动作（VLA）模型通常使用由每个模型仓库独立维护的特定基准测试脚本进行评估，这导致了代码重复、依赖冲突以及协议定义不明确等问题。我们提出了 vla-eval，这是一个开源评估框架，它通过基于 WebSocket msgpack 协议和 Docker 环境隔离，实现了模型推理与基准测试执行的解耦。模型只需实现一个 predict() 方法即可完成集成；基准测试通过四方法接口进行集成；完整的交叉评估矩阵将自动运行。完成一次完整的评估仅需两条命令：vla eval serve 和 vla eval run。该框架支持 13 个仿真基准测试和 6 个模型服务器。通过回合分片（episode sharding）和批处理推理实现的并行评估使吞吐量提升了 47 倍，在大约 18 分钟内即可完成 2000 个 LIBERO 回合。利用该基础设施，我们对一个已发表的 VLA 模型在三个基准测试上进行了复现性审计，发现这三个基准测试均能紧密复现已发表的数值，同时也揭示了一些未记录的需求、模糊的终止语义以及可能在无形中扭曲结果的隐藏归一化统计数据。此外，我们还发布了一个 VLA 排行榜，汇总了 17 个基准测试中的 657 项已发表结果。框架、评估配置以及所有复现结果均已公开。

## Abstract
Vision Language Action VLA models are typically evaluated using per benchmark scripts maintained independently by each model repository, leading to duplicated code, dependency conflicts, and underspecified protocols. We present vla eval, an open source evaluation harness that decouples model inference from benchmark execution through a WebSocket msgpack protocol with Docker based environment isolation. Models integrate once by implementing a single predict() method; benchmarks integrate once via a four method interface; the full cross evaluation matrix works automatically. A complete evaluation requires only two commands: vla eval serve and vla eval run. The framework supports 13 simulation benchmarks and six model servers. Parallel evaluation via episode sharding and batch inference achieves a 47x throughput improvement, completing 2000 LIBERO episodes in about 18 minutes. Using this infrastructure, we conduct a reproducibility audit of a published VLA model across three benchmarks, finding that all three closely reproduce published values while uncovering undocumented requirements ambiguous termination semantics and hidden normalization statistics that can silently distort results. We additionally release a VLA leaderboard aggregating 657 published results across 17 benchmarks. Framework, evaluation configs, and all reproduction results are publicly available.

---

## 论文详细总结（自动生成）

以下是对论文《vla-eval: A Unified Evaluation Harness for Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型的评估生态系统高度碎片化。每个模型仓库通常独立维护特定基准测试（Benchmark）的脚本，导致代码冗余、依赖冲突、实验协议不明确以及评估速度极慢。
*   **研究动机**：
    *   **依赖冲突**：不同基准测试（如 LIBERO、ManiSkill2、CALVIN）对 Python 版本和仿真引擎（robosuite, SAPIEN, PyBullet）的要求各异，难以在单一环境中并存。
    *   **协议不透明**：论文常忽略种子数、归一化统计数据等关键细节，导致结果难以复现。
    *   **效率低下**：单机顺序评估 2000 个 LIBERO 回合需约 14 小时，严重限制了迭代速度。

### 2. 方法论
*   **核心思想**：借鉴语言模型评估框架（如 lm-evaluation-harness）的设计理念，通过 **WebSocket + msgpack** 协议实现模型推理与基准测试执行的解耦。
*   **关键技术细节**：
    *   **架构设计**：采用客户端-服务器架构。模型服务器运行在宿主机，基准测试运行在独立的 Docker 容器中，通过二进制序列化协议通信。
    *   **统一接口**：模型端只需实现一个 `predict()` 方法；基准测试端通过 `StepBenchmark` 接口实现 `reset`、`step` 等四个核心方法。
    *   **环境隔离**：利用 Docker 隔离复杂的仿真环境依赖，利用 PEP 723 内联元数据和 `uv` 工具隔离模型侧的 Python 依赖。
    *   **并行化策略**：
        *   **回合分片（Episode Sharding）**：将评估任务分配到 $N$ 个并行的 Docker 容器中。
        *   **批处理推理（Batch Inference）**：模型服务器同时处理来自多个容器的推理请求，提升 GPU 利用率。
    *   **声明式配置**：通过 YAML 文件定义评估参数，确保实验过程的可记录与可追溯。

### 3. 实验设计
*   **支持的基准测试**：共支持 13 个仿真基准测试，包括 LIBERO (Spatial, Object, Goal, Long-Horizon)、CALVIN、SimplerEnv、ManiSkill2、RoboCasa 等。
*   **支持的模型**：集成了 CogACT、OpenVLA、π0、GR00T N1、X-VLA 等 6 种主流模型。
*   **复现性审计**：使用 DB-CogACT 模型在 LIBERO、CALVIN 和 SimplerEnv 三个基准测试上进行复现实验，对比已发表的原始数据。
*   **性能对比**：对比了顺序评估与并行评估（不同分片数 $N$ 和批大小 $B$）的吞吐量和耗时。

### 4. 资源与算力
*   **算力设备**：实验主要在 **NVIDIA H100 GPU** 上进行（用于模型推理服务器）。
*   **效率提升**：
    *   **LIBERO**：使用 50 个分片和 $B=16$ 的批处理，将 2000 个回合的评估时间从 14 小时缩短至 **18 分钟**（47 倍加速）。
    *   **CALVIN**：1000 个序列评估缩短至 33 分钟（16 倍加速）。
    *   **SimplerEnv**：288 个回合缩短至 8.5 分钟（12 倍加速）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 13 个基准测试的集成，并对其中 3 个核心基准进行了深度复现审计。
*   **排行榜数据**：通过 AI 代理（Claude Code）和人工审核，从 2685 篇论文中提取并归一化了 17 个基准测试的 **657 项结果**。
*   **充分性评价**：实验设计非常充分，不仅验证了框架的加速效果，还通过复现审计深入探讨了导致评估偏差的底层原因（如终止语义、归一化统计），具有很高的客观性和参考价值。

### 6. 主要结论与发现
*   **加速效果显著**：通过解耦和并行化，VLA 评估效率实现了量级提升。
*   **复现性陷阱**：审计发现了两个隐蔽的干扰因素：
    1.  **模糊的终止语义**：如 SimplerEnv 中某些标志位仅代表瞬时成功而非回合结束，提前停止会虚增得分。
    2.  **隐藏的归一化统计**：如 CALVIN 需要特定的训练集均值/标准差进行预处理，若文档未注明则会导致性能大幅下降。
*   **评估现状**：排行榜分析显示，81% 的模型仅在单一基准测试上评估，跨环境的通用性评估严重缺失。

### 7. 优点
*   **标准化与解耦**：解决了 VLA 领域长期存在的“依赖地狱”和脚本碎片化问题。
*   **极简操作**：仅需两条命令即可完成复杂评估，极大降低了科研门槛。
*   **高性能**：通过需求/供应匹配方法优化并行度，充分压榨 GPU 性能。
*   **社区贡献**：提供了详尽的排行榜和 Docker 镜像仓库，促进了结果的公平对比。

### 8. 不足与局限
*   **场景限制**：目前仅针对仿真环境，**不支持真实机器人**评估。
*   **指标单一**：目前主要支持任务成功率，缺乏对动作质量、效率和安全性等更丰富维度的评估。
*   **审计覆盖面**：复现性审计仅针对一种模型（DB-CogACT），未来需覆盖更多模型以验证通用性。
*   **排行榜数据源**：排行榜数据主要来自论文提取而非独立重测，可能存在原始论文本身的数据偏差。

（完）

---
title: "OxyGen: Unified KV Cache Management for Vision-Language-Action Models under Multi-Task Parallelism"
title_zh: OxyGen：多任务并行下视觉-语言-动作模型的统一 KV 缓存管理
authors: "Xiangyu Li, Huaizhi Tang, Xin Ding, Weijun Wang, Ting Cao, Yunxin Liu"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14371v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉-语言-动作（VLA）模型的统一KV缓存管理
tldr: 针对具身智能体在多任务并行（如操作、对话）下的推理效率问题，本文提出 OxyGen 系统。该系统通过统一 KV 缓存管理，将 KV 缓存视为跨任务和跨时间的共享资源。核心贡献包括跨任务 KV 共享以消除冗余预填充，以及跨帧连续批处理以解耦不同速率的任务生成。在 π0.5 模型上的实验表明，OxyGen 显著提升了推理速度和任务频率，且不损失动作质量。
selection_source: fresh_fetch
motivation: 现有的具身智能模型推理系统因孤立的 KV 缓存管理导致冗余计算和资源竞争，难以支持高效的多任务并行执行。
method: 提出统一 KV 缓存管理范式，通过跨任务 KV 共享和跨帧连续批处理优化共享观测值的预填充与异构任务的解码。
result: 在 π0.5 模型上实现了高达 3.7 倍的加速，同时达到 200 tokens/s 的语言吞吐量和 70 Hz 的动作频率。
conclusion: 统一 KV 缓存管理是实现具身智能模型高效、实时多任务并行推理的关键技术路径。
---

## 摘要
具身智能体日益需要根据共享观测结果，在不同的时间约束下并行执行多项任务，例如操作、对话和记忆构建。近期的混合 Transformer (MoT) 视觉-语言-动作模型 (VLA) 在架构上支持此类异构输出，但由于冗余计算和资源竞争，现有的推理系统无法在端侧部署中实现高效的多任务并行。我们发现孤立的 KV 缓存管理是其根本原因。为解决这一问题，我们提出了统一 KV 缓存管理，这是一种将 KV 缓存视为跨任务和跨时间的顶级共享资源的推理范式。这种抽象实现了两项关键优化：跨任务 KV 共享消除了共享观测的冗余预填充，而跨帧连续批处理则将变长语言解码与跨控制周期的固定速率动作生成解耦。我们针对最流行的 MoT VLA 模型 $π_{0.5}$ 实现了该范式，并在代表性的机器人配置下进行了评估。OxyGen 相比孤立执行实现了高达 3.7 倍的加速，在不降低动作质量的情况下，同时提供了超过 200 tokens/s 的语言吞吐量和 70 Hz 的动作频率。

## Abstract
Embodied AI agents increasingly require parallel execution of multiple tasks, such as manipulation, conversation, and memory construction, from shared observations under distinct time constraints. Recent Mixture-of-Transformers (MoT) Vision-Language-Action Models (VLAs) architecturally support such heterogeneous outputs, yet existing inference systems fail to achieve efficient multi-task parallelism for on-device deployment due to redundant computation and resource contention. We identify isolated KV cache management as the root cause. To address this, we propose unified KV cache management, an inference paradigm that treats KV cache as a first-class shared resource across tasks and over time. This abstraction enables two key optimizations: cross-task KV sharing eliminates redundant prefill of shared observations, while cross-frame continuous batching decouples variable-length language decoding from fixed-rate action generation across control cycles. We implement this paradigm for $π_{0.5}$, the most popular MoT VLA, and evaluate under representative robotic configurations. OxyGen achieves up to 3.7$\times$ speedup over isolated execution, delivering over 200 tokens/s language throughput and 70 Hz action frequency simultaneously without action quality degradation.

---

## 论文详细总结（自动生成）

### 论文总结：OxyGen —— 多任务并行下 VLA 模型的统一 KV 缓存管理

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能体（如家用机器人）在实际应用中需要同时处理多种任务，例如在操作物体的同时进行对话或构建环境记忆。虽然混合 Transformer（MoT）架构的视觉-语言-动作模型（VLA）在结构上支持多模态输出，但现有的推理系统通常采用“孤立执行”模式。这种模式导致了两个主要效率瓶颈：
*   **冗余计算**：多个任务共享相同的视觉观测，但系统会重复对相同输入进行预填充（Prefill），产生重复的 KV 缓存。
*   **资源竞争**：动作生成（高频、硬实时）与语言解码（变长、跨帧）在单一硬件资源上互相阻塞，导致动作频率下降或语言吞吐量低。
本文旨在通过统一管理 KV 缓存，解决多任务并行下的推理低效问题。

#### 2. 方法论：核心思想与关键技术
论文提出了 **OxyGen** 系统，其核心思想是将 KV 缓存抽象为跨任务、跨时间的“一等公民”共享资源。
*   **统一 KV 缓存管理器 (Unified KV Cache Manager)**：维护可恢复的生成状态（包含 KV 缓存、已生成 Token 和终止标志），支持存储、检索、更新和删除操作。
*   **跨任务 KV 共享 (Cross-task KV Sharing)**：在每一帧中，仅对观测值进行一次预填充生成 KV 缓存，随后该缓存被动作专家（用于去噪生成动作）和语言专家（用于自回归解码）共同复用，消除重复计算。
*   **跨帧连续批处理 (Cross-frame Continuous Batching)**：将语言解码从单帧控制循环中解耦。新生成的语言请求与之前帧未完成的请求进行批处理（Batching），利用 GPU 的并行能力在每一帧内推进多个请求的解码进度，从而在保证动作实时性的同时最大化语言吞吐量。
*   **算法流程**：
    1. 预填充当前帧观测，生成共享 KV。
    2. 动作专家利用共享 KV 完成实时动作去噪。
    3. 管理器将当前请求与历史在途请求合并，进行批处理解码。
    4. 更新各请求状态，移除已完成请求。

#### 3. 实验设计
*   **模型与框架**：基于最流行的 MoT VLA 模型 $\pi_{0.5}$ 及其官方推理系统 `openpi`。
*   **数据集/场景**：使用了三个代表性的机器人基准测试：**LIBERO**（操作任务）、**DROID**（真实世界操纵）和 **ALOHA**（双臂精细操作）。
*   **对比方法 (Baselines)**：
    *   **Sequential Isolated (Baseline)**：现有系统的标准做法，任务按顺序独立执行。
    *   **Parallel Isolated (Parallel)**：通过 NVIDIA MPS 实现的多进程并行执行，但仍保持 KV 隔离。
*   **评估指标**：动作频率 (Hz)、语言吞吐量 (tokens/s)、任务成功率、内存占用及能耗。

#### 4. 资源与算力
*   **硬件平台**：实验主要在单张 **NVIDIA RTX 4090 GPU** 上完成。这是端侧机器人推理的典型代表性硬件。
*   **训练时长**：本文侧重于推理系统优化，未详细说明模型训练时长（直接使用了官方发布的 $\pi_{0.5}$ 权重）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流基准测试，并针对不同的动作去噪步数（5-20步）和语言解码长度（5-30步）进行了详尽的组合测试。
*   **消融实验**：分别验证了“跨任务共享”和“跨帧批处理”的增益，证明了两者缺一不可。
*   **泛化性测试**：测试了不同的请求到达模式（均匀到达、泊松到达、随机长度请求），验证了系统在复杂负载下的稳定性。
*   **充分性评价**：实验设计客观、公平，不仅关注速度提升，还通过 LIBERO 测试集验证了动作质量（成功率）未受损，并提供了内存和能耗分析，非常全面。

#### 6. 主要结论与发现
*   **显著加速**：OxyGen 相比孤立执行实现了高达 **3.7 倍** 的加速。
*   **高性能并行**：在保持 **70 Hz** 高频动作生成的条件下，实现了超过 **200 tokens/s** 的语言吞吐量。
*   **能效提升**：由于减少了冗余计算和内存访问，单次请求的能耗降低了高达 **78%**，且内存开销仅增加约 15%。
*   **质量保持**：在 LIBERO 任务上的成功率与官方报告值一致，证明优化过程是无损的。

#### 7. 优点
*   **范式创新**：首次将 LLM 服务中的连续批处理思想引入到具有异构时间约束的具身智能 VLA 推理中。
*   **实用性强**：针对端侧部署常见的资源受限问题，提供了显著的性能增益和能效优化。
*   **开源贡献**：针对主流模型 $\pi_{0.5}$ 实现并开源，对社区具有直接的参考价值。

#### 8. 不足与局限
*   **架构依赖**：该方法主要针对 MoT（混合 Transformer）架构的 VLA 模型，对于非共享骨干网络的传统多模型组合方案适用性有限。
*   **内存开销**：虽然比多进程并行省内存，但相比单任务推理，维护多个在途请求的 KV 缓存仍会带来额外的显存压力，在极低显存的嵌入式设备上可能受限。
*   **硬件覆盖**：目前仅在高性能消费级 GPU (4090) 上验证，尚未在如 Jetson Orin 等更低功耗的嵌入式计算平台上进行深入测试。

（完）

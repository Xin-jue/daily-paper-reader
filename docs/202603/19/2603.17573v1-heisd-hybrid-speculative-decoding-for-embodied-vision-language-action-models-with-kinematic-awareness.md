---
title: "HeiSD: Hybrid Speculative Decoding for Embodied Vision-Language-Action Models with Kinematic Awareness"
title_zh: HeiSD：具有运动学感知的具身视觉-语言-动作模型混合投机解码
authors: "Zihao Zheng, Zhihao Mao, Sicheng Tian, Maoliang Li, Jiayu Chen, Xinhao Sun, Zhaobo Zhang, Xuanzhe Liu, Donggang Cao, Hong Mei, Xiang Chen"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17573v1"
tags: ["query:q12"]
score: 9.0
evidence: 机器人控制中VLA模型的加速方法
tldr: 针对具身视觉-语言-动作（VLA）模型推理速度慢的问题，本文提出HeiSD框架。通过分析机器人轨迹模式，HeiSD创新性地结合了草稿模型和检索式投机采样。该框架引入验证跳过机制、序列级松弛接受策略及运动学融合指标，在保持高任务成功率的同时，显著提升了模型的推理效率。
selection_source: fresh_fetch
motivation: 现有的投机采样方法在VLA模型中未能有效结合草稿式和检索式的优势，且面临草稿拒绝和混合边界难以确定的挑战。
method: 提出HeiSD框架，包含检索式优化的验证跳过与松弛接受策略，并利用运动学融合指标自动确定混合采样边界。
result: 实验显示HeiSD在仿真中实现高达2.45倍加速，在真实场景中实现2.06至2.41倍加速，且任务成功率保持稳定。
conclusion: HeiSD通过混合投机采样策略有效提升了VLA模型的推理效率，为实现高效实时的机器人控制提供了新思路。
---

## 摘要
视觉-语言-动作（VLA）模型已成为机器人控制的主流方案，但面临推理速度慢的问题。投机解码（SD）是一种极具前景的加速方法，可分为两类：基于草稿模型的 SD 和基于检索的 SD。现有方法未能分析这两类 SD 在 VLA 模型中的优缺点，导致它们被单独应用或优化。在本文中，我们分析了由 VLA 模型控制的机器人轨迹模式，并得出一个关键见解：这两类 SD 应当以混合方式使用。然而，在 VLA 模型中实现混合 SD 面临若干挑战：（1）基于检索的 SD 存在草稿拒绝和持续性错误；（2）难以确定混合边界。为解决这些问题，我们提出了 HeiSD 框架。我们在 HeiSD 中提出了一种基于检索的 SD 优化方法，其中包含验证跳过机制和序列级松弛接受策略。此外，我们在 HeiSD 中提出了一种基于运动学的融合指标，用于自动确定混合边界。实验结果表明，HeiSD 在仿真基准测试中实现了高达 2.45 倍的加速，在真实场景中实现了 2.06 倍至 2.41 倍的加速，同时保持了较高的任务成功率。

## Abstract
Vision-Language-Action (VLA) Models have become the mainstream solution for robot control, but suffer from slow inference speeds. Speculative Decoding (SD) is a promising acceleration method which can be divided into two categories: drafter-based SD and retrieval-based SD. Existing methods fail to analyze the advantages and disadvantages of these two types of SD in VLA models, leading to their sole application or optimization. In this paper, we analyze the trajectory patterns of robots controlled by the VLA model and derive a key insight: the two types of SD should be used in a hybrid manner. However, achieving hybrid SD in VLA models poses several challenges: (1) draft rejection and persistent errors in retrieval-based SD; (2) difficulty in determining the hybrid boundary. To address these, we propose the HeiSD framework. We propose a retrieval-based SD optimization method in HeiSD,which contains a verify-skip mechanism and a sequence-wise relaxed acceptance strategy. Moreover, we proposed a kinematic-based fused metric in HeiSD to automatically determine the hybrid boundary. Experimental results demonstrate that HeiSD attains a speedup of up to 2.45x in simulation benchmarks and 2.06x~2.41x in real-world scenarios, while sustaining a high task success rate.

---

## 论文详细总结（自动生成）

以下是对论文《HeiSD: Hybrid Speculative Decoding for Embodied Vision-Language-Action Models with Kinematic Awareness》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
具身智能中的视觉-语言-动作（VLA）模型（如 OpenVLA）虽然表现出色，但受限于大语言模型（LLM）骨干网络的自回归生成机制，推理速度极慢，难以满足机器人实时控制的需求。
*   **研究动机**：现有的加速方案“投机解码（SD）”分为**基于草稿模型（Drafter-based）**和**基于检索（Retrieval-based）**两类。前者草稿质量高但有额外的模型计算开销；后者开销极低但草稿与模型推理分布不匹配，导致接受率低。
*   **核心发现**：通过分析机器人轨迹发现，某些轨迹段（如直线、快速移动）与数据库高度重合，适合检索；而复杂、精细操作段则适合草稿模型。因此，论文提出了 **HeiSD** 框架，旨在通过混合两种 SD 模式并结合运动学感知来突破推理瓶颈。

### 2. 方法论：核心思想与关键技术
HeiSD 框架通过以下三个核心组件实现高效推理：
*   **混合投机解码（Hybrid SD）**：根据轨迹特征动态切换检索式 SD 和草稿模型式 SD。
*   **检索式 SD 优化**：
    *   **自适应验证跳过机制（Adaptive Verify-Skip）**：利用模型末层特征相似度，对高度相似的轨迹点直接接受，跳过昂贵的 LLM 验证过程。
    *   **序列级松弛接受策略（Sequence-wise Relaxed Acceptance）**：将 7 自由度动作分为位置、旋转和夹持器三组序列。允许在一定偏差范围内接受整个序列，而非严格的 Token 匹配，从而解决检索草稿的微小分布偏差问题。
*   **运动学融合指标（Kinematic-based Fused Metric）**：
    *   利用滑动窗口内的**曲率半径 ($R$)** 和**累积位移 ($D$)** 定义融合指标 $F$。
    *   公式：$F = \alpha \cdot Norm(R) + (1-\alpha) \cdot Norm(D)$。
    *   当 $F$ 较大（轨迹平滑、位移大）时使用检索式 SD；当 $F$ 较小（精细操作）时使用草稿模型。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真**：使用 LIBERO 基准测试，包含四个子集：LIBERO-Goal（目标）、LIBERO-Object（物体）、LIBERO-Spatial（空间）、LIBERO-Long（长程任务）。
    *   **真实场景**：使用 AgileX PIPER 6 自由度机械臂，涵盖原子抓取、空间位移和复合序列任务。
*   **对比方法（Baselines）**：
    *   无加速的自回归推理（AR）。
    *   纯检索式 SD（Pure R-SD）。
    *   纯草稿模型式 SD（Pure D-SD）。
    *   SOTA 方法：SpecVLA（带有 Token 级松弛接受的投机解码）。

### 4. 资源与算力
*   **训练阶段**：使用 **2 台 NVIDIA A100 GPU**，基于 DeepSpeed 框架训练草稿模型（单层 LLaMA 结构），耗时约 **8 小时**。
*   **推理阶段**：硬件平台为 **NVIDIA A100 GPU** 和 **Intel Xeon Silver 4410T CPU**。
*   **部署优化**：为了节省显存，将检索数据库部署在 CPU 内存中，利用异构计算减少 GPU 压力。

### 5. 实验数量与充分性
*   **实验规模**：在仿真环境中，每个子集包含 10 个任务，每个任务进行 50 次测试。真实世界实验涵盖 3 大类任务。
*   **充分性**：
    *   进行了详尽的**消融实验**，验证了混合 SD、验证跳过和序列松弛接受各自的贡献。
    *   对超参数（滑动窗口大小 $w$、融合权重 $\alpha$）进行了敏感度分析。
    *   对比了 GPU 独立部署与 CPU+GPU 异构部署的性能差异。
    *   实验设计较为客观，涵盖了成功率（SR）、加速比（Speedup）和平均接受长度（AL）等关键指标。

### 6. 主要结论与发现
*   **加速效果显著**：HeiSD 在仿真中实现了 **1.79x 至 2.45x** 的加速，在真实场景中实现了 **2.06x 至 2.41x** 的加速。
*   **性能保持良好**：在大幅提升速度的同时，任务成功率的损失极小（通常在 1%~4% 以内），优于现有的 SpecVLA。
*   **接受长度提升**：通过优化，平均接受长度（AL）从纯 SD 的 1.0 左右提升至 **4.75~4.96**。
*   **异构优势**：将数据库卸载到 CPU 不仅解决了显存不足（OOM）问题，还因 CPU 处理检索任务的效率而带来了额外的性能提升。

### 7. 优点
*   **创新性**：首次提出在 VLA 模型中混合使用检索式和草稿式 SD，并引入运动学感知。
*   **实用性**：针对真实硬件（如 24GB 显存的消费级显卡）设计了 CPU+GPU 异构方案，降低了部署门槛。
*   **鲁棒性**：序列级松弛接受策略比 Token 级更符合机器人控制的物理特性（允许微小物理偏差）。

### 8. 不足与局限
*   **超参数依赖**：混合边界的阈值、滑动窗口大小等参数目前仍需手动调节，缺乏自动化的超参数确定方法。
*   **适用范围**：该方法仅适用于自回归 VLA 模型，不适用于基于扩散模型（Diffusion-based）的动作生成方案。
*   **分布一致性**：虽然保证了成功率，但验证跳过和松弛接受可能导致输出分布与原始模型不完全一致，论文未深入探讨这种分布偏移的长期影响。

（完）

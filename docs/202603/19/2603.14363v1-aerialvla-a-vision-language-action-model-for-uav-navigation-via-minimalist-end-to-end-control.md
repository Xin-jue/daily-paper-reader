---
title: "AerialVLA: A Vision-Language-Action Model for UAV Navigation via Minimalist End-to-End Control"
title_zh: AerialVLA：一种通过极简端到端控制实现无人机导航的视觉-语言-动作模型
authors: "Peng Xu, Zhengnan Deng, Jiayan Deng, Zonghua Gu, Shaohua Wan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14363v1"
tags: ["query:q4"]
score: 9.0
evidence: 用于端到端控制的视觉-语言-动作模型
tldr: AerialVLA是一个针对无人机视觉语言导航（VLN）的极简端到端框架。它通过双视图感知策略减少视觉冗余，并利用模糊定向提示机制摆脱对外部密集引导的依赖。该模型将原始视觉和模糊指令直接映射为连续的3自由度运动及着陆信号，在TravelUAV基准测试中达到SOTA水平，尤其在未知场景下的泛化能力显著优于现有模块化系统。
selection_source: fresh_fetch
motivation: 现有的无人机导航方法过度依赖密集指令引导或辅助检测器，导致语义鸿沟并限制了真正的自主性。
method: 提出一种结合双视图感知、模糊定向提示和统一控制空间的端到端VLA模型，直接输出连续物理控制信号。
result: 在TravelUAV基准测试中取得领先性能，且在未知环境中的成功率是现有主流基准的三倍。
conclusion: 极简且以自主性为中心的端到端范式比复杂的模块化系统能捕获更鲁棒的视觉-运动表征。
---

## 摘要
无人机（UAV）的视觉语言导航（VLN）要求在动态三维环境中进行复杂的视觉解释和连续控制。现有的分层方法依赖于密集的先验（oracle）引导或辅助目标检测器，这造成了语义鸿沟并限制了真正的自主性。我们提出了 AerialVLA，这是一个极简的端到端视觉-语言-动作框架，可将原始视觉观测和模糊语言指令直接映射为连续的物理控制信号。首先，我们引入了一种精简的双视图感知策略，在减少视觉冗余的同时保留了前向导航和精确落地的关键线索，这进一步促进了未来的从仿真到现实（sim-to-real）的迁移。为了恢复真正的自主性，我们部署了一种仅源自机载传感器的模糊方向提示机制，完全消除了对密集先验引导的依赖。最终，我们构建了一个统一的控制空间，将连续的三自由度（3-DoF）运动学指令与内在的着陆信号相结合，使智能体无需外部目标检测器即可实现精准着陆。在 TravelUAV 基准测试上的大量实验表明，AerialVLA 在已知环境中达到了最先进的性能。此外，它在未知场景中表现出卓越的泛化能力，成功率接近领先基准模型的三倍，验证了以自主为中心的极简范式比复杂的模块化系统能捕获更鲁棒的视觉-运动表示。

## Abstract
Vision-Language Navigation (VLN) for Unmanned Aerial Vehicles (UAVs) demands complex visual interpretation and continuous control in dynamic 3D environments. Existing hierarchical approaches rely on dense oracle guidance or auxiliary object detectors, creating semantic gaps and limiting genuine autonomy. We propose AerialVLA, a minimalist end-to-end Vision-Language-Action framework mapping raw visual observations and fuzzy linguistic instructions directly to continuous physical control signals. First, we introduce a streamlined dual-view perception strategy that reduces visual redundancy while preserving essential cues for forward navigation and precise grounding, which additionally facilitates future simulation-to-reality transfer. To reclaim genuine autonomy, we deploy a fuzzy directional prompting mechanism derived solely from onboard sensors, completely eliminating the dependency on dense oracle guidance. Ultimately, we formulate a unified control space that integrates continuous 3-Degree-of-Freedom (3-DoF) kinematic commands with an intrinsic landing signal, freeing the agent from external object detectors for precision landing. Extensive experiments on the TravelUAV benchmark demonstrate that AerialVLA achieves state-of-the-art performance in seen environments. Furthermore, it exhibits superior generalization in unseen scenarios by achieving nearly three times the success rate of leading baselines, validating that a minimalist, autonomy-centric paradigm captures more robust visual-motor representations than complex modular systems.

---

## 论文详细总结（自动生成）

以下是对论文《AerialVLA: A Vision-Language-Action Model for UAV Navigation via Minimalist End-to-End Control》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的无人机视觉语言导航（UAV-VLN）系统普遍依赖“双重拐杖”：一是**密集先验引导（Oracle Guidance）**，即在输入中直接提供精确的每一步指令（如“右转”），使智能体沦为被动执行者；二是**外部目标检测器**（如 Grounding DINO），用于触发着陆，导致感知与控制链路断裂。
*   **研究背景**：无人机在 3D 空间中具有 6 自由度（6-DoF）的运动特性，环境复杂且具有重力和惯性约束。现有的模块化方法在面对未知环境时泛化能力差，且推理延迟高，难以实现真正的自主性。

### 2. 核心方法论
AerialVLA 提出了一个极简的端到端视觉-语言-动作（VLA）框架，其核心思想是将原始视觉观测直接映射为物理控制信号。
*   **极简双视图感知（Minimalist Dual-View Perception）**：放弃冗余的多摄像头阵列，仅融合**前视（Front）**和**下视（Down）**视图。通过垂直拼接并调整为 224x224 分辨率，利用 SigLIP 和 DINOv2 混合编码器提取语义和空间特征。
*   **模糊定向提示（Fuzzy Directional Prompting）**：完全移除每一步的精确指令，仅利用机载传感器（IMU/GPS）获取目标的大致相对方位，并将其映射为模糊的语义标签（如“在你的右后方”）。这迫使模型进行主动视觉寻址，而非被动跟随。
*   **数值动作标记化（Numerical Action Tokenization）**：将连续的 3 自由度动作空间（前向位移 $\Delta x$、垂直位移 $\Delta z$、偏航角变化 $\Delta \psi$）离散化为 99 个分箱，并直接映射到 LLM 词表中的**标准数字标记**。这种方法利用了预训练大模型对数值大小和顺序的理解能力。
*   **内在着陆机制**：在训练中将终端帧与 `LAND` 文本标记及零位移向量对齐，实现无需外部检测器的端到端精准着陆。
*   **几何一致性监督**：通过侧向深度图过滤掉专家数据中因人类反应延迟导致的歧义样本，确保模型学习到正确的避障逻辑。

### 3. 实验设计
*   **数据集与基准**：使用 **TravelUAV** 基准测试中的 UAV-Need-Help 任务，包含约 1.2 万条人类驾驶轨迹。
*   **测试场景**：分为三个子集：**Seen**（已知环境）、**Unseen Object**（未知目标类别）、**Unseen Map**（全新地图场景）。
*   **对比方法**：
    *   启发式基准：随机动作、固定动作。
    *   混合 VLM 方法：CMA、TravelUAV-DA（带外部检测器的 SOTA）。
    *   通用基础模型：NavFoM、LongFly（具有时空编码的最新模型）。

### 4. 资源与算力
*   **硬件配置**：使用 **4 张 NVIDIA RTX 4090 (24GB)** GPU。
*   **训练细节**：采用 LoRA 微调（秩 $r=64$），训练 5 个 epoch，总时长约 **35 小时**。
*   **推理效率**：显存占用 17GB，总延迟 0.38s（优于 TravelUAV 的 0.63s），证明了极简架构在实时导航中的优势。

### 5. 实验数量与充分性
*   **实验规模**：在三个不同维度的测试集上进行了全面评估，涵盖了从简单到困难的多个等级。
*   **消融实验**：针对**数据清洗（几何过滤）**、**感知视图数量**（对比 5 视图与双视图）、**动作标记化方式**（对比自定义标记与标准数字标记）进行了三组核心消融实验。
*   **充分性评价**：实验设计客观公平，通过对比 OSR（先验成功率）与 SR（实际成功率）的转化效率，深入分析了模型在导航终点判断上的优劣。

### 6. 主要结论与发现
*   **性能领先**：在已知环境中，AerialVLA 的成功率（SR）达到 47.96%，显著超过 LongFly（36.39%）。
*   **泛化能力极强**：在**未知地图**（Unseen Map）测试中，AerialVLA 的成功率是现有 SOTA 基准的 **3 倍左右**（37.58% vs 11.27%）。
*   **端到端优势**：实验证明，去除冗余的空间记忆模块和复杂的辅助插件，反而能让模型捕获更鲁棒的视觉-运动表征，从而在全新几何环境中表现更好。

### 7. 优点与亮点
*   **自主性高**：摆脱了对密集指令和外部检测器的依赖，实现了真正的闭环自主导航。
*   **架构优雅**：通过双视图拼接和标准数字标记化，巧妙地将复杂的 3D 导航任务适配到现有的 VLA 架构中。
*   **部署友好**：3 自由度控制空间与 DJI SDK、PX4 等主流无人机 API 高度兼容，且推理延迟低。

### 8. 不足与局限
*   **缺乏全局记忆**：由于采用纯反应式（Reactive）策略，在面对高度重复的城市结构（如纽约地图）时，智能体偶尔会出现无法有效回溯的问题。
*   **极端场景表现保守**：受限于行为克隆（BC）的学习范式，在目标被严重遮挡（如茂密森林）的分布外场景中，智能体倾向于执行安全的悬停，而非主动探索。
*   **未来方向**：论文指出未来需引入轻量级记忆机制和强化学习（RL）微调，以增强主动探索能力。

（完）

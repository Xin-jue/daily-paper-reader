---
title: "Simple Recipe Works: Vision-Language-Action Models are Natural Continual Learners with Reinforcement Learning"
title_zh: 简单方案行之有效：视觉-语言-动作模型是结合强化学习的天然持续学习者
authors: "Jiaheng Hu, Jay Shim, Chen Tang, Yoonchang Sung, Bo Liu, Peter Stone, Roberto Martin-Martin"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11653v1"
tags: ["query:q12"]
score: 9.0
evidence: 大型预训练VLA模型的持续强化学习
tldr: 本研究探讨了视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现。传统观点认为顺序微调会导致灾难性遗忘，但本文通过对三种模型和五个基准测试的系统研究发现，简单的顺序微调结合低秩自适应（LoRA）在保持高可塑性的同时，几乎没有遗忘现象，且保留了强大的零样本泛化能力。研究揭示了预训练大模型、参数高效微调与同策略强化学习之间的协同作用，为构建可自我改进的具身智能体提供了简单高效的新范式。
selection_source: fresh_fetch
motivation: 挑战传统持续学习中“顺序微调必然导致灾难性遗忘”的观点，探索大模型时代具身智能体在不断演变的环境中的持续适应能力。
method: 在多个挑战性基准测试上，系统评估了基于LoRA的顺序微调（Seq. FT）在VLA模型持续强化学习中的表现。
result: 实验证明简单的顺序微调在可塑性、抗遗忘能力和零样本泛化性上均表现优异，甚至超越了复杂的持续学习专门算法。
conclusion: 预训练大模型、参数高效适配与强化学习的结合重塑了稳定性与可塑性的权衡，使顺序微调成为VLA持续学习中一个强大且可扩展的基准方法。
---

## 摘要
针对视觉-语言-动作（VLA）模型的持续强化学习（CRL）是实现能够在开放式、演化环境中进行适应的自我改进具身智能体的一个极具前景的方向。然而，持续学习的传统观点认为，朴素的顺序微调（Seq. FT）会导致灾难性遗忘，因此需要复杂的 CRL 策略。在这项工作中，我们退后一步，对三种模型和五个具有挑战性的终身强化学习基准上的大型预训练 VLA 的 CRL 进行了系统研究。我们发现，与既有观点相反，结合低秩自适应（LoRA）的简单顺序微调表现异常强劲：它实现了高可塑性，几乎没有表现出遗忘，并保持了强大的零样本泛化能力，经常优于更复杂的 CRL 方法。通过详细分析，我们表明这种鲁棒性源于大型预训练模型、参数高效自适应和同策略（on-policy）强化学习之间的协同作用。这些组件共同重塑了稳定性-可塑性权衡，使持续适应既稳定又具可扩展性。我们的结果将顺序微调定位为 VLA 持续强化学习的一种强大方法，并为大模型时代的终身学习提供了新的见解。代码可在 github.com/UT-Austin-RobIn/continual-vla-rl 获取。

## Abstract
Continual Reinforcement Learning (CRL) for Vision-Language-Action (VLA) models is a promising direction toward self-improving embodied agents that can adapt in openended, evolving environments. However, conventional wisdom from continual learning suggests that naive Sequential Fine-Tuning (Seq. FT) leads to catastrophic forgetting, necessitating complex CRL strategies. In this work, we take a step back and conduct a systematic study of CRL for large pretrained VLAs across three models and five challenging lifelong RL benchmarks. We find that, contrary to established belief, simple Seq. FT with low-rank adaptation (LoRA) is remarkably strong: it achieves high plasticity, exhibits little to no forgetting, and retains strong zero-shot generalization, frequently outperforming more sophisticated CRL methods. Through detailed analysis, we show that this robustness arises from a synergy between the large pretrained model, parameter-efficient adaptation, and on-policy RL. Together, these components reshape the stability-plasticity trade-off, making continual adaptation both stable and scalable. Our results position Sequential Fine-Tuning as a powerful method for continual RL with VLAs and provide new insights into lifelong learning in the large model era. Code is available at github.com/UT-Austin-RobIn/continual-vla-rl.

---

## 论文详细总结（自动生成）

这篇论文对视觉-语言-动作（VLA）模型在持续强化学习（CRL）中的表现进行了深入研究，挑战了“顺序微调必然导致灾难性遗忘”的传统认知。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：具身智能体（如机器人）需要在不断变化的环境中持续学习新技能。传统的持续学习理论认为，简单的**顺序微调（Sequential Fine-Tuning, Seq. FT）**会导致模型迅速遗忘旧知识（灾难性遗忘），因此研究者通常开发复杂的算法（如正则化、重放或参数隔离）来缓解这一问题。
*   **核心问题**：在拥有强大先验知识的大型预训练 VLA 模型时代，这种“遗忘定律”是否依然成立？简单的方案是否就能解决复杂的持续学习问题？

### 2. 方法论：核心思想与关键技术
*   **核心思想**：论文提出，**大型预训练 VLA + 低秩自适应（LoRA）+ 同策略强化学习（On-Policy RL）**这三者的结合，使模型成为“天然的持续学习者”，无需任何额外的抗遗忘机制。
*   **关键技术细节**：
    *   **同策略强化学习（GRPO）**：使用组相对策略优化（Group Relative Policy Optimization），通过在当前策略生成的样本上进行优化，产生隐式的 KL 正则化效应，防止策略偏离预训练分布太远。
    *   **参数高效微调（LoRA）**：将权重更新限制在低秩子空间内，不仅减少了计算开销，还限制了更新的几何结构，防止对预训练表示的剧烈破坏。
    *   **大型预训练模型**：利用大模型的高维特性（Null Space），使得新任务的梯度更新在数学上更有可能与旧知识的参数空间正交，从而减少干扰。

### 3. 实验设计
*   **数据集/场景**：涵盖了 5 个挑战性的机器人操作基准测试：
    *   **LIBERO** (Spatial, Object, Long-horizon)：侧重于知识迁移。
    *   **RoboCasa**：复杂的厨房场景任务。
    *   **ManiSkill**：基于物理引擎的物体操控任务。
*   **对比方法**：对比了 8 种算法，包括：
    *   **基准**：顺序微调（Seq. FT）、多任务学习（Oracle 上界）。
    *   **经典 CRL 算法**：EWC（正则化）、Expert Replay（专家重放）、DER（暗经验重放）、DWE（参数隔离）。
    *   **大模型适配算法**：SLCA（层级学习率）、RETAIN（权重合并）。
*   **评估指标**：平均成功率（AVG）、负向后向迁移（NBT，即遗忘率）、前向迁移（FWT）、零样本泛化能力（ZS）。

### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号和总训练时长（例如“使用了 128 张 H100 训练了 500 小时”），但提到了实验基于 **RLinf** 基础设施，使用了 7B 参数规模的模型（OpenVLA），且全局 Batch Size 设为 8192。
*   **规模暗示**：考虑到 3 种模型、5 个基准、8 种算法以及每组实验 3 个随机种子，该研究的计算量非常巨大，属于典型的大规模模型后训练实验。

### 5. 实验数量与充分性
*   **实验规模**：进行了跨模型（OpenVLA-OFT, Pi-0, OpenVLA）、跨基准、跨任务顺序的广泛测试。
*   **消融实验**：非常充分。作者分别验证了“去掉 RL（改用有监督 SFT）”、“去掉大模型（改用小模型）”以及“去掉 LoRA（全参数微调）”的影响，证明了三个组件缺一不可。
*   **鲁棒性测试**：引入了相机角度、光照条件、机器人初始位置的扰动，验证了结论的普适性。实验设计客观、公平，且对 Seq. FT 未进行任何超参数调优，更显其方案的稳健性。

### 6. 主要结论与发现
*   **简单方案胜出**：基于 LoRA 的顺序微调在几乎所有指标上都表现优异，遗忘率极低（NBT < 2%），且保持了极强的零样本泛化能力，甚至在某些情况下超过了多任务学习。
*   **复杂算法失效**：传统的 CRL 算法（如 EWC 或重放类）由于引入了额外的约束，反而损害了模型的**可塑性（Plasticity）**，导致学习新任务的效果不如简单的顺序微调。
*   **协同效应**：遗忘的缓解并非偶然，而是 RL 的采样机制、LoRA 的低秩约束和大模型的高维空间共同作用的结果。

### 7. 优点（亮点）
*   **颠覆性观点**：有力地反驳了持续学习领域长期以来对顺序微调的偏见，为大模型时代的具身智能研究提供了极简的路径。
*   **理论与实证结合**：不仅展示了实验结果，还通过**费舍尔信息矩阵（Fisher Information）**和信息论视角解释了为什么 LoRA 和 RL 能协同工作。
*   **开源贡献**：提供了完整的代码库和基准实现，具有很高的实用价值。

### 8. 不足与局限
*   **任务相似度风险**：实验所用的基准测试（如 LIBERO）中，任务之间可能存在较高的相似性，这可能天然有利于顺序微调，在任务差异极大的极端非平稳环境下表现尚待验证。
*   **仿真与现实差距**：所有实验均在仿真环境中完成，尚未在真实物理机器人上验证其持续学习的稳定性。
*   **任务标识依赖**：模型仍依赖于明确的语言指令来区分任务，对于任务边界模糊或无指令的持续学习场景讨论较少。

（完）

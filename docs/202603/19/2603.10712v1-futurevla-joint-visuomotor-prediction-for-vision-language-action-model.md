---
title: "FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model"
title_zh: FutureVLA：视觉-语言-动作模型的联合视觉运动预测
authors: "Xiaoxu Xu, Hao Li, Jinhui Ye, Yilun Chen, Jia Zeng, Xinyi Chen, Linning Xu, Dahua Lin, Weixin Li, Jiangmiao Pang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10712v1"
tags: ["query:q12"]
score: 10.0
evidence: 视觉语言动作模型的联合视觉运动预测
tldr: 针对具身智能体中视觉感知与动作执行耦合建模难的问题，本文提出FutureVLA。该模型采用联合视觉-动作预测架构，在预训练阶段通过门控机制解耦视觉状态与动作建模，从而提取广义物理先验，并在后期通过潜在嵌入对齐策略使下游VLA模型内化这些时间先验。实验证明，FutureVLA在不改变推理架构的情况下，显著提升了多种VLA框架的性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有VLA模型在进行视觉-动作联合预测时，难以平衡视觉细节重建与动作意图建模，导致时间连续性不足且易受无关视觉信息干扰。
method: 提出一种联合视觉-动作预测架构，利用门控机制在结构上分离视觉状态与动作流，并通过潜在嵌入对齐策略将物理先验集成到下游模型中。
result: 实验结果显示，FutureVLA在多个异构操作数据集上表现优异，并能一致性地增强现有VLA框架的预测与执行效果。
conclusion: FutureVLA通过解耦视觉与动作信息并联合建模物理先验，为提升具身智能体的预测预见能力提供了一种高效且通用的方案。
---

## 摘要
预测性预见对于智能具身智能体至关重要。由于机器人的运动执行本质上受其对环境几何形状的视觉感知约束，有效地预测未来需要捕捉这种紧密耦合的视觉运动相互作用。虽然最近的视觉-语言-动作（VLA）模型尝试引入未来引导，但它们在处理这种联合建模方面仍面临困难。现有的显式方法将模型容量分散到了与任务无关的视觉细节上，而依赖稀疏帧对的隐式方法则破坏了时间连续性。由于过度依赖视觉重建，这些方法变得由视觉主导，将静态场景上下文与动态动作意图纠缠在一起。我们认为，有效的联合视觉运动预测建模既需要时间连续性，也需要视觉条件的监督解耦。为此，我们提出了 FutureVLA，其特点是采用了一种新型的联合视觉运动预测架构。FutureVLA 旨在通过先解耦视觉和运动信息，然后联合编码广义物理先验来提取联合视觉运动嵌入。具体而言，在预训练阶段，我们利用异构操作数据集并引入联合视觉运动门控机制，从结构上将视觉状态保持与时间动作建模分离。它允许运动流专注于连续的物理动力学，同时显式地查询视觉标记以获取环境约束，从而产生具有高度泛化能力的联合视觉运动嵌入。随后，在后训练阶段，我们采用潜嵌入对齐策略，使各种下游 VLA 模型能够在不修改其推理架构的情况下内化这些时间先验。大量的实验表明，FutureVLA 持续改进了 VLA 框架。

## Abstract
Predictive foresight is important to intelligent embodied agents. Since the motor execution of a robot is intrinsically constrained by its visual perception of environmental geometry, effectively anticipating the future requires capturing this tightly coupled visuomotor interplay. While recent vision-language-action models attempt to incorporate future guidance, they struggle with this joint modeling. Existing explicit methods divert capacity to task-irrelevant visual details, whereas implicit methods relying on sparse frame pairs disrupt temporal continuity. By heavily relying on visual reconstruction, these methods become visually dominated, entangling static scene context with dynamic action intent. We argue that effective joint visuomotor predictive modeling requires both temporal continuity and visually-conditioned supervision decoupling. To this end, we propose FutureVLA, featuring a novel Joint Visuomotor Predictive Architecture. FutureVLA is designed to extract joint visuomotor embeddings by first decoupling visual and motor information, and then jointly encoding generalized physical priors. Specifically, in the pretraining stage, we leverage heterogeneous manipulation datasets and introduce a Joint Visuomotor Gating mechanism to structurally separate visual state preservation from temporal action modeling. It allows the motor stream to focus on continuous physical dynamics while explicitly querying visual tokens for environmental constraints, yielding highly generalizable joint visuomotor embeddings. Subsequently, in the post-training stage, we employ a latent embeddings alignment strategy, enabling diverse downstream VLA models to internalize these temporal priors without modifying their inference architectures. Extensive experiments demonstrate that FutureVLA consistently improves VLA frameworks.

---

## 论文详细总结（自动生成）

以下是对论文《FutureVLA: Joint Visuomotor Prediction for Vision-Language-Action Model》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在视觉-语言-动作（VLA）模型中有效地引入“未来预测”引导，以增强机器人的预见能力和动作执行的物理一致性。
*   **研究动机**：
    *   **现有显式方法（Explicit）**：通过预测未来视频帧提供引导，但模型容量常被无关的视觉细节（如背景变化）占据，导致“视觉主导”而忽略了核心动作逻辑。
    *   **现有隐式方法（Implicit）**：依赖稀疏的帧对（如起始帧和结束帧）来学习潜在表示，这破坏了动作的时间连续性，且仍难以将静态环境约束与动态动作意图解耦。
    *   **物理约束**：机器人的运动执行本质上受环境几何形状的约束，因此需要一种能同时捕捉视觉环境和运动动力学的“联合视觉运动（Visuomotor）”建模方式。

### 2. 方法论
FutureVLA 提出了一个两阶段的训练范式，核心思想是**视觉条件下的监督解耦**。

*   **第一阶段：联合视觉运动预训练（Joint Visuomotor Pretraining）**
    *   **视觉标记化**：使用预训练的 3D-VAE（来自 WAN 模型）将连续的 17 帧视频剪辑压缩为时间标记，保留时间结构并减少冗余。
    *   **联合视觉运动门控（JVG）机制**：这是核心创新。将时间标记分为“视觉流”和“运动流”。
        *   **视觉流**：仅负责重建第一帧的潜在表示，强制其保留静态环境的几何和空间信息。
        *   **运动流**：负责预测动作块（Action Chunk）。通过**门控交叉注意力机制**，运动流会显式地向视觉流查询空间特征（Affordance），从而在物理约束下建模动态演变。
    *   **解耦目标**：通过不同的监督信号（视觉重建 vs. 动作回归/流匹配），防止视觉细节干扰动作逻辑。

*   **第二阶段：VLA 后训练（VLA Post-training）**
    *   **潜在嵌入对齐（Latent Alignment）**：将预训练得到的“未来感知”联合嵌入作为语义锚点。
    *   **知识迁移**：在训练下游 VLA（如基于 Qwen3-VL）时，通过一个轻量级适配器，强制 VLA 的中间表示与预训练的联合嵌入对齐。这使得模型在推理时即使只输入当前帧，也能内化未来的时间先验。

### 3. 实验设计
*   **数据集与场景**：
    *   **预训练**：使用了包含 1560 万帧的混合数据集，涵盖 OXE 和 LIBERO 等异构操作数据。
    *   **仿真环境**：SimplerEnv（Google Robot 和 WidowX 机器人）、LIBERO（Object, Spatial, Goal, Long 四个子集）以及 LIBERO-Plus（含多种环境扰动）。
    *   **真实世界**：使用 Franka 机器人完成四项复杂任务：制作汉堡、插花、勺子舀豆子、擦白板。
*   **对比方法**：
    *   **基础 VLA**：OpenVLA, $\pi_0$, GR00T-N1.5。
    *   **显式引导方法**：WorldVLA, DreamVLA。
    *   **隐式引导方法**：UniVLA, Villa-X, LAPA。

### 4. 资源与算力
*   **算力投入**：预训练阶段使用了 **16 台 NVIDIA A100 GPU**。
*   **训练时长**：预训练大约耗时 **3 天**。
*   **模型规模**：后训练阶段采用了 Qwen3-VL-4B 作为视觉语言模型主干。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的仿真和实机测试。在 LIBERO-Plus 上测试了超过 10,000 个任务变体。
*   **消融实验**：非常充分，涵盖了：
    *   JVPM 引导的有效性（有无对齐策略的对比）。
    *   JVG 门控机制的组件分析（视觉/运动流解耦的影响）。
    *   时间采样密度（验证连续帧优于稀疏帧）。
    *   时间跨度长度（确定 17 帧为最优对齐长度）。
*   **客观性**：通过在统一架构下重新实现对比方法（如 LAPA-style, Villa-X-style），确保了比较的公平性。

### 6. 主要结论与发现
*   **性能提升**：在 SimplerEnv 上比无引导基线平均提升 11.4%；在真实世界任务中，比强大的 $\pi_0$ 模型成功率高出 26.7%。
*   **长程任务优势**：在 LIBERO-Long 任务中表现尤为突出，证明了时间连续建模对长序列推理的帮助。
*   **物理一致性**：定量分析表明，FutureVLA 学习到的潜在空间与实际动作执行的物理相似度高度相关，而非仅仅是视觉上的相似。
*   **鲁棒性**：在面对视觉噪声（如颜色抖动、高斯噪声）时，由于视觉与运动的解耦，FutureVLA 的动作预测偏差远小于同类模型。

### 7. 优点
*   **结构化解耦**：巧妙地解决了视觉重建任务“吃掉”模型容量的问题，使运动流能专注于物理动力学。
*   **时间连续性**：通过 3D-VAE 处理连续视频流，避免了隐式方法中常见的动作断裂问题。
*   **架构无关性**：作为一种后训练对齐策略，它可以轻松集成到不同的 VLA 架构（如回归式或流匹配式）中，且不增加推理成本。

### 8. 不足与局限
*   **多模态感知缺失**：目前主要依赖视觉约束。对于擦白板等接触密集型任务，缺乏触觉或力矩反馈的集成，这在极端物理交互中可能受限。
*   **固定时间窗口**：预训练使用了固定的 17 帧窗口，对于极慢或极快的动作演变，固定频率的采样可能不是最优的。
*   **计算开销**：虽然推理不增加负担，但预训练阶段需要处理大规模视频流，对存储和计算资源有较高要求。

（完）

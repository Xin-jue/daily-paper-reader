---
title: "S-VAM: Shortcut Video-Action Model by Self-Distilling Geometric and Semantic Foresight"
title_zh: S-VAM：通过自蒸馏几何与语义预见实现的快捷视频动作模型
authors: "Haodong Yan, Zhide Zhong, Jiaguan Zhu, Junjie He, Weilin Yuan, Wenxuan Song, Xin Gong, Yingjie Cai, Guanyi Zhao, Xu Yan, Bingbing Liu, Ying-Cong Chen, Haoang Li"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16195v2"
tags: ["query:q10"]
score: 9.0
evidence: 用于复杂操作任务的视频动作模型
tldr: 针对机器人学习中视频动作模型（VAM）在实时推理与高保真预测之间的权衡难题，本文提出S-VAM模型。该模型通过单次前向传播即可预见连贯的几何与语义表示，并以此作为动作预测的蓝图。核心创新在于引入自蒸馏策略，将多步去噪的生成先验压缩至单步推理，利用视觉基础模型提取的特征作为教师信号指导轻量级解耦器学习。实验证明，S-VAM在仿真和实操中均优于现有SOTA方法，实现了高效精准的复杂任务操控。
selection_source: fresh_fetch
motivation: 旨在解决现有视频动作模型在实时推理速度与视觉前瞻保真度之间无法兼得的局限性。
method: 采用自蒸馏策略将多步扩散生成的结构化先验压缩为单步推理，并利用轻量级解耦器预测几何与语义表示。
result: 实验结果显示 S-VAM 在仿真和真实世界任务中均超越了现有最先进方法，显著提升了操控效率与精度。
conclusion: S-VAM 通过高效的单步前瞻表示简化了动作预测流程，为复杂环境下的实时机器人学习提供了强有力的支持。
---

## 摘要
视频动作模型（VAMs）因其在复杂操作任务中强大的视觉预见能力，已成为机器人学习中一种极具前景的范式。然而，目前的 VAMs 通常依赖于缓慢的多步视频生成或带有噪声的单步特征提取，无法同时保证实时推理和高保真预见。为了解决这一局限性，我们提出了 S-VAM，这是一种快捷视频动作模型，通过单次前向传递即可预见连贯的几何与语义表示。作为稳定的蓝图，这些预见的表示显著简化了动作预测。为了实现这一高效的快捷方式，我们引入了一种新颖的自蒸馏策略，将多步去噪的结构化生成先验压缩到单步推理中。具体而言，从扩散模型自身多步生成的视频中提取的视觉基础模型（VFM）表示提供了教师目标。轻量级解耦器作为学生，学习将带有噪声的单步特征直接映射到这些目标。在仿真和真实世界中的广泛实验表明，我们的 S-VAM 优于现有最先进的方法，能够在复杂环境中实现高效且精确的操作。我们的项目主页是 https://haodong-yan.github.io/S-VAM/。

## Abstract
Video action models (VAMs) have emerged as a promising paradigm for robot learning, owing to their powerful visual foresight for complex manipulation tasks. However, current VAMs, typically relying on either slow multi-step video generation or noisy one-step feature extraction, cannot simultaneously guarantee real-time inference and high-fidelity foresight. To address this limitation, we propose S-VAM, a shortcut video-action model that foresees coherent geometric and semantic representations via a single forward pass. Serving as a stable blueprint, these foreseen representations significantly simplify the action prediction. To enable this efficient shortcut, we introduce a novel self-distillation strategy that condenses structured generative priors of multi-step denoising into one-step inference. Specifically, vision foundation model (VFM) representations extracted from the diffusion model's own multi-step generated videos provide teacher targets. Lightweight decouplers, as students, learn to directly map noisy one-step features to these targets. Extensive experiments in simulation and the real world demonstrate that our S-VAM outperforms state-of-the-art methods, enabling efficient and precise manipulation in complex environments. Our project page is https://haodong-yan.github.io/S-VAM/

---

## 论文详细总结（自动生成）

以下是对论文《S-VAM: Shortcut Video-Action Model by Self-Distilling Geometric and Semantic Foresight》的结构化深度分析总结：

### 1. 核心问题与研究背景
*   **核心问题**：现有的视频动作模型（VAM）在机器人操控中面临**“推理延迟”与“预见保真度”之间的权衡**。
    *   **多步生成范式**：通过扩散模型生成未来视频帧来指导动作，虽预见性强但推理极慢，无法满足闭环控制的实时性要求。
    *   **单步特征范式**：直接提取扩散模型的单步去噪特征，虽速度快但特征存在严重的噪声、纠缠和时间漂移，缺乏精确的几何与语义信息。
*   **研究动机**：利用互联网规模视频数据预训练的扩散模型具有强大的物理先验，如何既保留这种“视觉预见”能力，又实现实时的单步推理？

### 2. 方法论：S-VAM 架构与核心思想
S-VAM 的核心思想是通过**自蒸馏（Self-Distillation）**建立一条“快捷路径”，将多步生成的结构化先验压缩到单步推理中。
*   **核心流程**：
    1.  **单步特征提取**：从视频扩散模型（SVD）的中间层提取单步去噪特征，这些特征虽乱但包含未来蓝图。
    2.  **几何与语义解耦器（Decouplers）**：引入轻量级时空 Transformer 模块，将纠缠的单步特征映射为连贯的视觉基础模型（VFM）表示。
        *   **几何分支**：目标是 DPAv3 表示，提供动态几何结构。
        *   **语义分支**：目标是 DINOv2 表示，提供补丁级的语义区分度。
    3.  **自蒸馏策略**：在训练阶段，使用扩散模型**自身多步生成**的视频作为教师信号，提取其 VFM 特征来监督解耦器的输出。这避免了与真实标签（GT）之间的轨迹不匹配问题。
    4.  **动作专家（Action Expert）**：
        *   **Uni-Perceiver**：通过可学习查询（Queries）融合几何预见、语义预见和原始扩散特征。
        *   **扩散策略（Diffusion Policy）**：基于融合后的上下文预测动作序列。

### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：长程桌面操作基准，评估 ABC→D 的泛化能力。
    *   **MetaWorld**：包含 50 个不同难度的操控任务（Easy, Middle, Hard）。
    *   **真实世界**：使用 AgileX 协作双臂机器人（Mobile ALOHA 系统），执行“倒水”、“移动透明杯子”、“抬锅”等任务。
*   **对比方法**：
    *   **直接动作学习**：RT-1, Diffusion Policy, OpenVLA, π0, Spatial Forcing 等。
    *   **预见性方法**：SuSIE, GR-1, VPP（主要基准）, HiF-VLA 等。

### 4. 资源与算力
论文详细列出了训练与推理的资源消耗：
*   **训练算力**：
    *   **第一阶段（SVD 微调）**：使用 **4 台 NVIDIA H100 GPU**，MetaWorld 训练 100k 步，真实世界 40k 步。
    *   **第二阶段（解耦器蒸馏）**：仅需 **1 台 NVIDIA H100 GPU**，训练 50k 步（非常高效）。
    *   **第三阶段（动作专家训练）**：使用 **4 台 NVIDIA H100 GPU**，训练 40k-60k 步。
*   **推理性能**：在单张 **NVIDIA RTX 3090 (24GB)** 上，单次前向传播耗时 **307.6 ms**（其中扩散骨干 231ms，解耦器 40.1ms），支持 **25Hz** 的有效控制频率（通过 Action Chunking）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境（50 个 MetaWorld 任务）和真实世界多任务验证，实验组数较多。
*   **消融实验**：针对几何/语义分支的必要性、自蒸馏与 GT 监督的对比、Uni-Perceiver 的作用、不同 VFM 目标（CLIP, SigLIP, DINOv3, VGGT 等）的优劣进行了详尽测试。
*   **客观性**：对比了当前最先进的 VLA 和 VAM 模型，且在相同硬件和单目视觉设置下进行，具有较高的公平性。

### 6. 主要结论与发现
*   **性能领先**：S-VAM 在 CALVIN 平均完成任务数（4.16）和 MetaWorld 平均成功率（72.8%）上均刷新了 SOTA。
*   **自蒸馏的优越性**：相比于直接预测 GT 帧的特征，预测模型自身生成的视频特征效果更好，因为特征空间更一致。
*   **几何与语义的协同**：几何预见解决了单目深度歧义（如透明物体），语义预见保证了长程任务中物体身份的一致性。
*   **实时性**：在增加少量计算开销（约 15.8%）的前提下，显著提升了单步特征的质量。

### 7. 优点与亮点
*   **创新性**：巧妙地利用“自蒸馏”解决了扩散模型推理慢的痛点，将“生成能力”转化为“表征能力”。
*   **解耦设计**：明确区分几何与语义预见，符合机器人操控对空间理解和物体识别的双重需求。
*   **实用性**：在真实世界中表现稳健，特别是处理透明物体和双臂协作任务时，展示了极强的鲁棒性。

### 8. 不足与局限
*   **骨干网络依赖**：虽然解耦器很轻量，但仍需运行庞大的视频扩散骨干网络（1.5B 参数），对移动端部署仍有一定压力。
*   **单目限制**：尽管引入了几何预见，但在极端遮挡或复杂 3D 空间中，单目视觉固有的局限性可能依然存在。
*   **训练阶段繁琐**：三阶段训练流程（微调、蒸馏、动作学习）相对复杂，增加了系统实现的门槛。

（完）

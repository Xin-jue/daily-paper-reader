---
title: Tactile Modality Fusion for Vision-Language-Action Models
title_zh: 视觉-语言-动作模型的触觉模态融合
authors: "Charlotte Morissette, Amin Abyaneh, Wei-Di Chang, Anas Houssaini, David Meger, Hsiu-Chin Lin, Jonathan Tremblay, Gregory Dudek"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14604v1"
tags: ["query:q12"]
score: 10.0
evidence: 将视触觉信号集成到视觉-语言-动作 (VLA) 模型中
tldr: 针对视觉语言动作（VLA）模型在接触密集型任务中缺乏力学感知的问题，本文提出 TacFiLM。这是一种轻量级的模态融合方法，通过特征线性调制（FiLM）将预训练的触觉信号融入视觉特征中。相比于复杂的令牌拼接或大规模预训练，该方法采用后训练微调策略，在保持计算效率的同时，显著提升了机器人在插拔等任务中的成功率、操作速度和力稳定性，增强了模型的泛化能力。
selection_source: fresh_fetch
motivation: 视觉感知难以捕捉接触密集型操作中的力学动态，而现有的触觉融合方法往往计算复杂度过高。
method: 提出 TacFiLM，利用特征线性调制（FiLM）将预训练的触觉表示作为条件，对 VLA 模型的中间视觉特征进行融合。
result: 在插拔任务的实验中，该方法在成功率、完成时间及力稳定性方面均表现出显著提升，且具备良好的分布外泛化性。
conclusion: TacFiLM 证明了轻量级触觉融合是增强 VLA 模型处理接触密集型任务能力的有效途径。
---

## 摘要
我们提出了 TacFiLM，这是一种轻量级的模态融合方法，旨在将视触觉信号集成到视觉-语言-动作（VLA）模型中。尽管 VLA 模型的最新进展引入了兼具泛化能力和语义基础的机器人策略，但这些模型主要依赖于基于视觉的感知。然而，仅靠视觉无法捕捉到在富接触操作过程中发生的复杂交互动力学，包括接触力、表面摩擦、顺应性和剪切力。虽然最近将触觉信号集成到 VLA 模型中的尝试通常通过令牌拼接（token concatenation）或大规模预训练增加了复杂性，但行为模型沉重的计算需求使得更轻量级的融合策略变得必不可少。为了应对这些挑战，TacFiLM 提出了一种训练后微调方法，该方法利用特征线性调制（FiLM），以预训练的触觉表示为条件对中间视觉特征进行调节。在插入任务上的实验结果表明，在分布内和分布外任务中，成功率、直接插入性能、完成时间和力稳定性均有一致的提升。总之，这些结果证明了我们的方法是将触觉信号集成到 VLA 模型中的有效途径，改善了富接触操作行为。

## Abstract
We propose TacFiLM, a lightweight modality-fusion approach that integrates visual-tactile signals into vision-language-action (VLA) models. While recent advances in VLA models have introduced robot policies that are both generalizable and semantically grounded, these models mainly rely on vision-based perception. Vision alone, however, cannot capture the complex interaction dynamics that occur during contact-rich manipulation, including contact forces, surface friction, compliance, and shear. While recent attempts to integrate tactile signals into VLA models often increase complexity through token concatenation or large-scale pretraining, the heavy computational demands of behavioural models necessitate more lightweight fusion strategies. To address these challenges, TacFiLM outlines a post-training finetuning approach that conditions intermediate visual features on pretrained tactile representations using feature-wise linear modulation (FiLM). Experimental results on insertion tasks demonstrate consistent improvements in success rate, direct insertion performance, completion time, and force stability across both in-distribution and out-of-distribution tasks. Together, these results support our method as an effective approach to integrating tactile signals into VLA models, improving contact-rich manipulation behaviours.

---

## 论文详细总结（自动生成）

以下是对论文《Tactile Modality Fusion for Vision-Language-Action Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型主要依赖视觉感知，但在处理“接触密集型”（contact-rich）任务（如精密插拔、组装）时，视觉容易受到遮挡且无法感知微小的力学变化（如摩擦、剪切力、顺应性）。
*   **研究动机**：
    *   **视觉局限性**：仅靠视觉难以实现毫米级的精确调整。
    *   **现有融合方法的缺陷**：常见的“令牌拼接”（Token Concatenation）会增加输入序列长度，导致计算开销激增；而大规模多模态预训练则耗时耗力。
    *   **需求**：需要一种轻量级、参数高效且能保留原始 VLA 模型语义先验的触觉融合方案。

### 2. 方法论：TacFiLM
*   **核心思想**：利用**特征线性调制（FiLM）**技术，将预训练的触觉表示作为“条件信号”，动态地调整 VLA 模型视觉编码器中的中间特征，而不是简单地在语言模型输入端拼接令牌。
*   **关键技术细节**：
    *   **基础架构**：基于 OpenVLA-OFT 框架，包含视觉主干（SigLIP 和 DINOv2）、MLP 投影层和 Llama2-7B 语言模型。
    *   **触觉编码**：使用预训练的触觉模型（如 Sparsh 或 T3）提取触觉图像特征，生成触觉嵌入向量 $z$。
    *   **FiLM 调制**：通过一个轻量级 MLP 将 $z$ 映射为缩放参数 $\gamma$ 和偏移参数 $\beta$。
    *   **公式表达**：在视觉主干的 ViT 块中，对中间激活特征 $F_n$ 进行仿射变换：$FiLM(F_n | \gamma, \beta) = F_n \odot (1 + \gamma) + \beta$。
    *   **微调策略**：采用 LoRA（低秩自适应）对模型进行后训练微调，仅更新少量参数，冻结大部分预训练权重。

### 3. 实验设计
*   **实验场景**：使用 Franka Emika Panda 机械臂进行真实的插拔任务，包括圆形/方形/五边形销钉插入（间隙 2mm-3mm）以及 USB/HDMI 线缆插拔。
*   **数据集**：每个任务收集 80 条专家演示数据（10Hz 采样），包含 RGB 图像、DIGIT 触觉传感器图像、机器人状态和动作。
*   **对比基准（Benchmark）**：
    1.  **OpenVLA-OFT**：仅视觉的基准模型。
    2.  **TactileConcat**：传统的触觉特征拼接方法（将触觉作为额外令牌输入 LLM）。
*   **评估指标**：成功率（%）、直接插入率（无需调整一次成功的比例）、平均最大接触力（N）、平均完成时间（s）。

### 4. 资源与算力
*   **训练细节**：所有模型均训练至 80,000 步（80k steps）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或具体的训练总时长。但提到该方法是“轻量级”的，且使用了 LoRA 技术，暗示其对算力的需求低于全参数微调或大规模预训练。

### 5. 实验数量与充分性
*   **实验规模**：进行了超过 **700 次** 真实的机器人测试（Rollouts）。
    *   分布内（ID）测试：270 次。
    *   分布外（OOD）测试：225 次。
    *   消融实验与鲁棒性测试：210 次。
*   **充分性评估**：实验设计较为充分。不仅涵盖了不同形状的物体，还进行了**分布外泛化测试**（如从未见过的五边形销钉）和**视觉退化测试**（如光照变暗、视频流冻结），客观地验证了触觉在视觉失效时的补偿作用。

### 6. 主要结论与发现
*   **性能提升**：TacFiLM 在所有任务中均优于仅视觉模型和拼接模型。在某些任务中，成功率比次优基准提高达 30%。
*   **操作更“温柔”**：TacFiLM 显著降低了操作过程中的最大接触力（仅为基准方法的 1/3 左右），证明模型学会了利用触觉进行顺应性调整，减少了硬碰撞。
*   **效率更高**：任务完成时间显著缩短，直接插入率更高。
*   **泛化性强**：在面对未见过的几何形状（如 HDMI 接口）时，TacFiLM 表现出更强的鲁棒性。

### 7. 优点
*   **轻量高效**：FiLM 调制不增加 LLM 的序列长度，避免了计算复杂度的二次方增长。
*   **即插即用**：能够兼容多种预训练触觉编码器（如 Sparsh-DINO），无需从头训练触觉模块。
*   **鲁棒性**：在视觉传感器受损或环境光照不佳时，触觉模态能有效维持策略性能。

### 8. 不足与局限
*   **任务多样性有限**：实验主要集中在“插拔”这一类任务上，尚未在更复杂的长程任务或多物体交互任务中验证。
*   **硬件依赖**：依赖于特定类型的视觉触觉传感器（DIGIT），对于其他类型的触觉传感器（如阵列式力觉传感器）的适配性有待研究。
*   **缺乏仿真验证**：由于缺乏高精度的视触觉仿真器，所有实验均在真机上完成，这限制了大规模数据探索的可能性。
*   **模型架构限制**：目前仅在 OpenVLA 架构上验证，尚未推广到其他主流 VLA 模型（如 $\pi_0$）。

（完）

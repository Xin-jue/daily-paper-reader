---
title: Specification-Aware Distribution Shaping for Robotics Foundation Models
title_zh: 机器人基础模型的规范感知分布整形
authors: "Sadık Bera Yüksel, Derya Aksaray"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17969v1"
tags: ["query:q7"]
score: 9.0
evidence: 在机器人基础模型上强制执行约束
tldr: 针对机器人基础模型在执行复杂任务时缺乏安全性和时序逻辑保障的问题，本文提出了一种规范感知的动作分布优化框架。该方法无需修改预训练模型参数，通过在每个决策步结合前向动力学传播，对动作分布进行最小化修正，以确保满足信号时序逻辑（STL）约束。实验证明，该框架能有效提升机器人在复杂时空需求下的任务成功率和安全性。
selection_source: fresh_fetch
motivation: 现有的机器人基础模型主要由数据驱动，难以在部署时提供关于安全性和复杂时序规范的正式保证。
method: 提出一种分布整形框架，在不改变模型参数的情况下，利用前向动力学推理并优化动作分布以满足STL约束。
result: 在多种仿真环境和复杂规范下，该框架成功引导预训练模型满足了硬性的时空约束。
conclusion: 该研究为提升大模型在机器人控制中的可靠性提供了一种无需重训且具备形式化保障的有效方案。
---

## 摘要
机器人基础模型在跨多种任务和环境执行自然语言指令方面展现出了强大的能力。然而，它们在很大程度上仍是数据驱动的，在部署过程中缺乏对安全性以及满足随时间变化的规范的正式保证。在实践中，机器人通常需要遵守涉及丰富时空要求的操作约束，例如有时限的目标访问、顺序目标和持续的安全条件。在这项工作中，我们提出了一种规范感知的动作分布优化框架，该框架在不修改预训练机器人基础模型参数的情况下，在执行过程中强制执行一大类信号时序逻辑（STL）约束。在每个决策步骤中，该方法通过使用前向动力学传播对剩余时域进行推理，计算出一个满足硬性 STL 可行性约束的最小修改动作分布。我们在仿真中利用最先进的机器人基础模型，在多种环境和复杂规范下验证了所提出的框架。

## Abstract
Robotics foundation models have demonstrated strong capabilities in executing natural language instructions across diverse tasks and environments. However, they remain largely data-driven and lack formal guarantees on safety and satisfaction of time-dependent specifications during deployment. In practice, robots often need to comply with operational constraints involving rich spatio-temporal requirements such as time-bounded goal visits, sequential objectives, and persistent safety conditions. In this work, we propose a specification-aware action distribution optimization framework that enforces a broad class of Signal Temporal Logic (STL) constraints during execution of a pretrained robotics foundation model without modifying its parameters. At each decision step, the method computes a minimally modified action distribution that satisfies a hard STL feasibility constraint by reasoning over the remaining horizon using forward dynamics propagation. We validate the proposed framework in simulation using a state-of-the-art robotics foundation model across multiple environments and complex specifications.

---

## 论文详细总结（自动生成）

### 论文结构化总结：机器人基础模型的规范感知分布整形

#### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人基础模型（如 SPOC、OpenVLA）虽然能理解复杂的自然语言指令，但本质上是数据驱动的黑盒模型，无法为安全性（如避障）和复杂的时空约束（如“在特定时间内访问某地”或“按顺序执行任务”）提供**形式化保证**。
*   **研究背景**：在现实部署中，机器人必须遵守严格的操作规范。传统的时序逻辑（TL）方法虽有严谨的数学保证，但难以与基于 Transformer 的大规模基础模型无缝集成，且现有的约束解码方法大多仅能处理简单的即时安全约束（Invariance），无法处理涉及长时域推理的时空任务。

#### 2. 方法论：规范感知分布整形
*   **核心思想**：在不修改预训练基础模型参数的前提下，通过在推理阶段对动作概率分布进行“整形”（Shaping），使其在满足信号时序逻辑（STL）约束的同时，尽可能保留原始模型的决策意图。
*   **关键技术细节**：
    *   **STL 策略合成**：利用基于漏斗（Funnel-based）的奖励塑造方法，预先训练一个专门负责满足 STL 规范的辅助策略 $\pi_\phi$（如通过 DQN 训练）。
    *   **规范评价函数 $J_\phi$**：在每个决策步，利用已知动力学模型进行前向传播，评估当前候选动作 $a_t$ 结合后续执行 $\pi_\phi$ 是否能导致 STL 规范的最终满足。
    *   **受约束的分布优化**：将问题建模为一个凸优化问题：最小化修改后分布 $\pi^*$ 与原始分布 $\pi_{FM}$ 之间的 **KL 散度**，约束条件是动作必须具有 STL 可行性。
    *   **闭式解**：通过拉格朗日乘子法推导出优化问题的解析解。在硬约束下，该解简化为：将所有不可行动作的概率置零，并对剩余可行动作的概率进行重归一化。
*   **算法流程**：在每一步，先查询基础模型获取原始分布，再通过前向模拟筛选可行动作集，最后根据优化后的分布采样执行。若无可行动作，则回退到专门的 STL 策略。

#### 3. 实验设计
*   **仿真环境**：使用高保真 3D 模拟器 **AI2-THOR**。
*   **基础模型（Benchmark）**：选用最先进的机器人导航基础模型 **SPOC** (Shortest Path Oracle)。
*   **实验场景**：
    *   **Case 1（有时限的充电任务）**：指令为“找一个碗”，同时要求在 [0, 60] 和 [80, 140] 两个时间段内必须访问充电站。
    *   **Case 2（顺序访问+安全约束）**：指令为“找一个平底锅”，同时要求按顺序访问三个区域，且全程避开禁区。
*   **对比基准**：未经修改的原始 SPOC 模型策略。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及训练时长。
*   **实现细节**：提到 STL 辅助策略是在环境的 2D 占用地图抽象上通过 DQN 训练的，前向动力学传播也在该抽象地图上运行，以提高实时计算效率。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个案例研究，分别进行了 **200 次独立随机实验**。
*   **充分性评价**：
    *   **客观性**：通过大规模重复实验获取了统计意义上的成功率，具有较好的说服力。
    *   **覆盖度**：涵盖了 STL 中常见的“最终（Finally）”、“全局（Globally）”以及它们的组合逻辑，验证了框架处理复杂时序任务的能力。
    *   **局限性**：缺乏对不同基础模型（如 OpenVLA）的横向对比，且未在真实物理机器人上进行验证。

#### 6. 主要结论与发现
*   **100% 的规范满足率**：在动力学模型准确的假设下，该框架在所有实验中均实现了 100% 的 STL 约束满足，而原始 SPOC 模型在这些复杂约束下的满足率为 0%。
*   **主任务性能保持**：在 Case 1 中，主任务成功率仅从 93.5% 轻微下降至 92.5%；在更复杂的 Case 2 中，成功率下降至 82.5%。这表明强制执行复杂约束会与原始任务目标产生一定的权衡（Trade-off）。
*   **形式化保证**：论文在理论上证明了对于确定性系统，该算法能保证闭环轨迹满足 STL 规范。

#### 7. 优点与亮点
*   **无需重训**：作为一种插件式框架，它不需要对昂贵的基础模型进行微调或重新训练。
*   **长时域推理**：相比于只看下一帧的避障方法，该方法通过前向传播和辅助策略，具备了处理“未来时间窗口”约束的能力。
*   **数学严谨性**：提供了优化问题的闭式解和满足性证明，增强了基础模型在安全敏感场景下的可靠性。

#### 8. 不足与局限
*   **模型依赖**：高度依赖已知的动力学模型和环境地图。如果环境发生动态变化或地图不准（模型失配），100% 满足的保证将失效。
*   **计算开销**：在每个决策步进行前向模拟（Rollouts）会增加推理延迟，对于更复杂的动力学系统或超长时域任务，计算压力会显著增大。
*   **任务冲突风险**：当 STL 约束与主任务指令完全冲突时，框架会优先保证 STL 约束，可能导致主任务彻底失败。
*   **离散动作限制**：目前仅针对离散动作空间进行了推导，尚未扩展到连续控制空间。

（完）

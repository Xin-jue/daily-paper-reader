---
title: "GMT: Goal-Conditioned Multimodal Transformer for 6-DOF Object Trajectory Synthesis in 3D Scenes"
title_zh: GMT：用于3D场景中6自由度物体轨迹合成的目标条件多模态Transformer
authors: "Huajian Zeng, Abhishek Saroha, Daniel Cremers, Xi Wang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17993v1"
tags: ["query:q13"]
score: 9.0
evidence: 在3D场景中合成6自由度物体操作轨迹
tldr: 本研究针对3D场景中6自由度物体操作轨迹合成的挑战，提出了GMT（目标条件多模态Transformer）框架。该框架通过融合3D包围盒几何、点云上下文、语义类别和目标终点位姿，生成逼真且目标导向的物体轨迹。GMT在空间准确性和姿态控制方面显著优于现有基准（如CHOIS和GIMO），为基于学习的操纵规划建立了新基准，并展现出强大的泛化能力。
selection_source: fresh_fetch
motivation: 现有的物体轨迹合成方法受限于2D或部分3D表示，难以捕捉完整的场景几何并保证轨迹精度。
method: 提出一种多模态Transformer框架，通过联合处理几何、语义、上下文和目标信息来生成连续的6自由度位姿序列。
result: 在合成和真实世界基准测试中，GMT在空间准确性和方向控制上均大幅超越了CHOIS和GIMO等先进基准。
conclusion: GMT为复杂3D环境下的物体操纵规划提供了高效的生成式方案，并具备良好的跨场景和跨物体泛化性。
---

## 摘要
在3D环境中合成可控的6自由度（6-DOF）物体操作轨迹对于实现机器人与复杂场景的交互至关重要，但由于需要精确的空间推理、物理可行性以及多模态场景理解，这仍然具有挑战性。现有方法往往依赖于2D或部分3D表示，限制了其捕捉完整场景几何的能力并制约了轨迹精度。我们提出了GMT，一个多模态Transformer框架，通过联合利用3D边界框几何、点云上下文、语义物体类别和目标终止位姿来生成逼真且目标导向的物体轨迹。该模型将轨迹表示为连续的6自由度位姿序列，并采用了一种定制的条件化策略，融合了几何、语义、上下文和目标导向信息。在合成和真实世界基准上的广泛实验表明，GMT优于最先进的人体运动和人-物交互基准（如CHOIS和GIMO），在空间精度和朝向控制方面取得了显著进步。我们的方法为基于学习的操作规划建立了新的基准，并展示了对多样化物体和杂乱3D环境的强大泛化能力。项目页面：https://huajian-zeng.github.io/projects/gmt/。

## Abstract
Synthesizing controllable 6-DOF object manipulation trajectories in 3D environments is essential for enabling robots to interact with complex scenes, yet remains challenging due to the need for accurate spatial reasoning, physical feasibility, and multimodal scene understanding. Existing approaches often rely on 2D or partial 3D representations, limiting their ability to capture full scene geometry and constraining trajectory precision. We present GMT, a multimodal transformer framework that generates realistic and goal-directed object trajectories by jointly leveraging 3D bounding box geometry, point cloud context, semantic object categories, and target end poses. The model represents trajectories as continuous 6-DOF pose sequences and employs a tailored conditioning strategy that fuses geometric, semantic, contextual, and goaloriented information. Extensive experiments on synthetic and real-world benchmarks demonstrate that GMT outperforms state-of-the-art human motion and human-object interaction baselines, such as CHOIS and GIMO, achieving substantial gains in spatial accuracy and orientation control. Our method establishes a new benchmark for learningbased manipulation planning and shows strong generalization to diverse objects and cluttered 3D environments. Project page: https://huajian- zeng.github. io/projects/gmt/.

---

## 论文详细总结（自动生成）

### 论文总结：GMT（目标条件多模态Transformer）用于3D场景中6自由度物体轨迹合成

#### 1. 核心问题与整体含义（研究动机和背景）
在复杂的3D环境中生成逼真且可控的**6自由度（6-DOF）物体操作轨迹**是机器人学和计算机视觉的核心挑战。
*   **研究动机**：现有的方法（如视频预测或人机交互模型）通常存在深度歧义、缺乏物理一致性，或者过度以“人为中心”（将物体视为被动实体）。
*   **核心问题**：如何整合异构的多模态信息（几何、语义、上下文、目标状态），生成既符合物理规律（避障、稳定）又能精确达到预设目标的物体运动路径。
*   **整体含义**：GMT 提出了一种**以物体为中心（Object-centric）**的视角，将轨迹作为感知与控制之间的通用中间表示，使生成的轨迹可以通过逆运动学（IK）部署到任何机器人平台上。

#### 2. 方法论：核心思想与技术细节
GMT 采用多模态 Transformer 架构，通过分层融合策略处理不同来源的数据：
*   **核心思想**：利用 Transformer 的注意力机制，将场景的几何约束（硬约束）与语义偏好（软约束）结合，并以目标位姿（Goal）为强引导。
*   **关键技术细节**：
    *   **空间特征传播**：使用 PointNet++ 提取点云特征，通过逆距离加权插值（Inverse-distance weighting）将局部几何特征从场景点云传播到物体的 3D 边界框角点。
    *   **语义编码**：利用预训练的 **CLIP** 模型对物体类别和动作描述进行嵌入，捕捉比 One-hot 编码更丰富的行为模式。
    *   **目标条件化**：将期望的终止位姿（位置+旋转）编码为特征，作为高层意图信号。
    *   **多模态融合（Perceiver-inspired）**：使用类似 Perceiver IO 的架构，通过可学习的潜变量（Latent Array）作为信息瓶颈，进行跨模态注意力和自注意力融合。
    *   **损失函数**：结合了平移损失（$L_{trans}$）、旋转损失（$L_{ori}$）、历史重建损失（$L_{rec}$）和终点目标损失（$L_{dest}$）。

#### 3. 实验设计
*   **数据集/场景**：
    1.  **Aria Digital Twin (ADT)**：高保真合成数据集，提供完美的几何和语义观测，用于评估模型性能上限。
    2.  **HD-EPIC**：真实的室内自我中心视频数据集。由于缺乏连续轨迹标注，作者通过 MPS 手部追踪技术推导出物体在被操纵期间的 6-DOF 轨迹。
*   **Benchmark 与对比方法**：
    *   **GIMO**：原用于注视引导的人体运动预测，被适配为物体轨迹预测。
    *   **CHOIS**：原用于受控的人机交互合成，去除了人体分支，仅保留物体预测部分。
*   **评估指标**：平均位移误差 (ADE)、最终位移误差 (FDE)、Fréchet 距离 (FD)、角度一致性 (AC) 和碰撞率 (CR)。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A6000 GPU** 进行训练。
*   **训练细节**：采用 AdamW 优化器，学习率为 $1 \times 10^{-4}$，权重衰减为 $5 \times 10^{-4}$。
*   **时长**：文中未明确给出具体的训练总时长，但提到轨迹被统一重采样为 200 帧，且模型在单 GPU 上运行。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了详尽的定量对比。
*   **消融实验**：针对“无点云”、“无语义”、“无目标引导”以及“仅首帧输入”四种变体进行了消融，验证了各模块的必要性。
*   **客观性与公平性**：作者对基准模型（GIMO, CHOIS）进行了合理的适配（如禁用注视分支、统一输入比例），确保了对比的基准线一致。实验涵盖了从理想合成环境到噪声严重的真实环境，具有较强的说服力。

#### 6. 主要结论与发现
*   **性能领先**：GMT 在所有关键指标（尤其是空间精度 ADE/FDE）上均显著优于现有基准。
*   **目标导向性**：目标条件（Goal-conditioning）是减少长程轨迹漂移的关键，移除该模块会导致性能大幅下降。
*   **物理可行性**：通过引入局部几何特征传播，GMT 在保持低 FDE 的同时，显著降低了物体与环境的碰撞率。
*   **高效性**：生成的轨迹往往比人类自然的操纵轨迹更直接、更高效（路径更短）。

#### 7. 优点（亮点）
*   **解耦设计**：将物体轨迹作为中间层，使得该框架不依赖于特定的机器人形态，具备极强的通用性。
*   **特征融合策略**：创新的“点云到边界框”特征传播方法，既保留了空间精度，又避免了直接处理大规模点云的计算负担。
*   **鲁棒性**：在 HD-EPIC 这种充满噪声和遮挡的真实数据集中依然表现稳健。

#### 8. 不足与局限
*   **目标依赖**：模型高度依赖预先给定的“目标位姿”，但在实际应用中，目标位姿往往需要通过视觉语言模型（VLM）或其他推理机制自行生成。
*   **场景对齐要求**：模型假设场景上下文和物体标注是良好对齐的，在极度杂乱或动态变化的实时场景中可能受限。
*   **缺乏闭环反馈**：目前是开环生成，未来若能引入强化学习或实时碰撞优化（Post-refinement），性能可能进一步提升。

（完）

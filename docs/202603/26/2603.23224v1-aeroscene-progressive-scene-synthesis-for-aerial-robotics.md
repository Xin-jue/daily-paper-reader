---
title: "AeroScene: Progressive Scene Synthesis for Aerial Robotics"
title_zh: AeroScene：面向空中机器人的渐进式场景合成
authors: "Nghia Vu, Tuong Do, Dzung Tran, Binh X. Nguyen, Hoan Nguyen, Erman Tjiputra, Quang D. Tran, Hai-Nguyen Nguyen, Anh Nguyen"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23224v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于渐进式3D场景合成的分层扩散模型
tldr: AeroScene 针对无人机仿真环境构建耗时且难以扩展的问题，提出了一种分层扩散模型。该模型通过层级感知标记化和多分支特征提取，实现了从全局布局到局部细节的渐进式 3D 场景合成。研究不仅在多个基准测试中超越了现有方法，还生成了超过 1000 个可直接用于 NVIDIA Isaac Sim 的高质量物理仿真场景，显著提升了无人机导航等下游任务的训练效率。
selection_source: fresh_fetch
motivation: 现有的无人机仿真环境高度依赖人工创建，导致场景生成效率低且难以大规模扩展。
method: 提出一种分层扩散模型，利用层级感知标记化和多分支特征提取来兼顾全局布局与局部语义一致性。
result: 在实验中显著超越了现有方法，并成功生成了 1000 多个可直接集成至 NVIDIA Isaac Sim 的高保真物理仿真场景。
conclusion: AeroScene 为机器人领域提供了一种高效、可扩展的 3D 场景生成方案，有效支持了无人机导航等复杂任务的仿真需求。
---

## 摘要
生成模型在多个领域展现了巨大影响，但其在机器人场景合成方面的潜力仍未得到充分探索。这种差距在无人机模拟器中尤为明显，目前的模拟环境仍严重依赖人工工作，不仅耗时且难以扩展。在这项工作中，我们提出了 AeroScene，这是一种用于渐进式 3D 场景合成的分层扩散模型。我们的方法利用层级感知分词（hierarchy-aware tokenization）和多分支特征提取，对全局布局和局部细节进行推理，确保了物理合理性和语义一致性。这使得 AeroScene 特别适用于为导航、降落和栖息等空中机器人任务生成逼真的场景。我们通过在新建数据集和公共基准上的广泛实验证明了其有效性，结果表明 AeroScene 显著优于现有方法。此外，我们使用 AeroScene 生成了一个包含 1,000 多个物理就绪、高保真 3D 场景的大规模数据集，这些场景可以直接集成到 NVIDIA Isaac Sim 中。最后，我们展示了这些生成的环境在下游无人机导航任务中的实用性。我们的代码和数据集已在 aioz-ai.github.io/AeroScene/ 公开。

## Abstract
Generative models have shown substantial impact across multiple domains, their potential for scene synthesis remains underexplored in robotics. This gap is more evident in drone simulators, where simulation environments still rely heavily on manual efforts, which are time-consuming to create and difficult to scale. In this work, we introduce AeroScene, a hierarchical diffusion model for progressive 3D scene synthesis. Our approach leverages hierarchy-aware tokenization and multi-branch feature extraction to reason across both global layouts and local details, ensuring physical plausibility and semantic consistency. This makes AeroScene particularly suited for generating realistic scenes for aerial robotics tasks such as navigation, landing, and perching. We demonstrate its effectiveness through extensive experiments on our newly collected dataset and a public benchmark, showing that AeroScene significantly outperforms prior methods. Furthermore, we use AeroScene to generate a large-scale dataset of over 1,000 physics-ready, high fidelity 3D scenes that can be directly integrated into NVIDIA Isaac Sim. Finally, we illustrate the utility of these generated environments on downstream drone navigation tasks. Our code and dataset are publicly available at aioz-ai.github.io/AeroScene/

---

## 论文详细总结（自动生成）

### AeroScene：面向空中机器人的渐进式场景合成论文总结

#### 1. 研究动机与核心问题
*   **研究背景**：无人机（空中机器人）在物流、巡检和监控等领域应用日益广泛，其仿真训练需要高度逼真且复杂的3D环境。
*   **核心问题**：现有的无人机模拟器场景大多依赖人工设计，存在**耗时长、难以规模化扩展**的问题。此外，现有的生成模型往往将场景视为平坦的对象集合，缺乏对**全局布局（如航道、建筑轮廓）与局部细节（如障碍物、降落点）**之间层级关系的推理，导致生成的场景在物理合理性和任务可行性上表现不佳。
*   **研究目标**：开发一种能够自动生成具有层级结构、物理可用且可直接用于物理引擎（如 NVIDIA Isaac Sim）的高保真场景合成框架。

#### 2. 方法论：核心思想与技术细节
AeroScene 采用**分层扩散模型（Hierarchical Diffusion Model）**，通过以下关键技术实现渐进式合成：
*   **层级感知标记化（Hierarchy-aware Tokenization）**：将场景中的对象（位置、姿态、尺寸、语义）映射为 Token，并根据“可标记化评分”将其自动路由至**粗粒度分支**（如地形、建筑）或**细粒度分支**（如家具、碎片）。
*   **多分支特征提取**：
    *   **粗粒度分支**：使用 3D CNN 提取宏观结构特征。
    *   **细粒度分支**：构建空间邻接图，利用图神经网络（GNN）捕捉局部物体间的交互。
*   **跨尺度渐进注意力机制（Cross-scale Progressive Attention）**：通过堆叠的注意力块实现“自上而下”（粗到细）和“自下而上”（细到粗）的信息融合，确保局部细节符合全局空间约束。
*   **任务感知引导函数（Guidance Objectives）**：在扩散推理阶段引入梯度引导：
    1.  **碰撞规避引导**：利用 3D IoU 惩罚物体重叠。
    2.  **粗到细引导**：确保细粒度物体放置在合理的粗粒度区域内。
    3.  **语义约束引导**：基于类别先验确保物体间逻辑合理（如椅子靠近桌子）。

#### 3. 实验设计
*   **数据集**：
    *   **3D-FRONT**：通用的室内场景基准数据集。
    *   **AeroScene Dataset（新贡献）**：包含 1000 多个室内外场景，涵盖 16 万个物体，标注了层级关系及无人机降落区域。
*   **对比方法（Baselines）**：ATISS（自回归 Transformer）、Diffusion-SDF、DiffuScene（组合扩散模型）、PhyScene（物理约束模型）。
*   **评估指标**：FID/KID（感知相似度）、CR（碰撞率）、CFC（层级一致性）、SP（语义合理性）。
*   **下游任务验证**：在 NVIDIA Isaac Sim 中测试无人机的“导航+降落”联合任务。

#### 4. 资源与算力
*   **硬件环境**：使用**单张 NVIDIA A100 GPU** 进行训练。
*   **训练细节**：采用 Adam 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 64，训练时长为 **800 个 Epoch**。
*   **推理效率**：在 A100 上生成一个场景约需 **2 分钟**（100 步反向扩散），与主流扩散模型基准相当。

#### 5. 实验数量与充分性
*   **实验规模**：在两个大型数据集上进行了定量对比，每种方法平均生成 1000 个场景进行评估。
*   **消融实验**：针对三种引导函数（碰撞、层级、语义）分别进行了剔除实验，验证了各组件对物理合理性的贡献。
*   **充分性评价**：实验设计较为全面，不仅涵盖了生成质量的指标，还通过 300 条飞行轨迹的测试（成功率 91%）验证了生成场景在机器人任务中的实用性。对比实验选取了近年来的前沿模型，评估过程客观公平。

#### 6. 主要结论与发现
*   **性能领先**：AeroScene 在所有生成指标（FID、碰撞率、层级一致性等）上均显著优于现有方法。
*   **层级重要性**：显式建模粗/细粒度特征能有效减少物体漂浮或位置错乱的问题。
*   **仿真价值**：生成的场景具有“物理就绪”特性，可直接支持无人机从长程导航到精准降落的闭环训练，成功率达 91%。

#### 7. 论文优点
*   **层级化视角**：突破了传统模型将场景扁平化的局限，非常符合空中机器人对环境感知的多尺度需求。
*   **端到端集成**：生成的场景不仅是视觉上的，还通过 Isaac Sim 实现了物理属性的集成，具有极强的工程实用价值。
*   **开源贡献**：贡献了一个专门针对空中机器人任务的大规模、高保真数据集。

#### 8. 不足与局限
*   **静态限制**：目前仅关注静态场景布局，未考虑风力、动态障碍物（如行人、车辆）等时空动态因素。
*   **Sim-to-Real 差距**：实验完全在仿真环境中进行，尚未在真实物理世界的无人机上进行验证。
*   **推理耗时**：虽然 2 分钟生成一个场景在离线训练中可接受，但对于需要实时生成环境的在线应用仍显稍慢。

（完）

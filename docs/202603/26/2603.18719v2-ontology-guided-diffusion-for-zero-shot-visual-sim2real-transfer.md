---
title: Ontology-Guided Diffusion for Zero-Shot Visual Sim2Real Transfer
title_zh: 本体引导的扩散模型用于零样本视觉仿真到现实迁移
authors: "Mohamed Youssef, Mayar Elfares, Anna-Maria Meer, Matteo Bortoletto, Andreas Bulling"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18719v2"
tags: ["query:q8"]
score: 10.0
evidence: 使用本体引导扩散的零样本视觉仿真到现实迁移
tldr: 本研究针对模拟到现实（sim2real）迁移中真实感难以量化的问题，提出了一种名为本体引导扩散（OGD）的神经符号零样本框架。该方法将“真实感”分解为光照、材质等可解释特征的本体，并利用知识图谱和图神经网络提取全局嵌入，同时结合符号规划器生成结构化指令引导扩散模型。OGD不仅在图像转换质量上超越了现有SOTA方法，还通过显式建模结构化知识提升了模型的可解释性和泛化能力。
selection_source: fresh_fetch
motivation: 现有的扩散模型在sim2real转换中往往依赖非结构化提示，无法有效捕捉构成真实感的结构化关键因素。
method: 提出OGD框架，利用本体论将真实感分解为可解释特征，并通过知识图谱、图神经网络和符号规划器共同指导预训练扩散模型进行图像编辑。
result: 实验表明，OGD在区分真实与合成图像的准确性上优于基准，并在零样本sim2real图像转换任务中取得了领先的性能。
conclusion: 显式编码真实感的结构化知识是实现高效、可解释且泛化性强的零样本sim2real迁移的关键路径。
---

## 摘要
由于带标签的真实世界数据稀缺，弥合仿真到现实（sim2real）的差距仍然具有挑战性。现有的基于扩散的方法依赖于非结构化提示或统计对齐，无法捕捉使图像看起来真实的结构化因素。我们提出了本体引导扩散（OGD），这是一种神经符号零样本 sim2real 图像翻译框架，它将真实感表示为结构化知识。OGD 将真实感分解为可解释特征的本体（例如光照和材料属性），并在知识图中编码它们的关系。从合成图像中，OGD 推断特征激活，并使用图神经网络生成全局嵌入。与此同时，符号规划器利用本体特征计算缩小真实感差距所需的一系列一致的视觉编辑。图嵌入通过交叉注意力机制调节预训练的指令引导扩散模型，而规划的编辑则被转换为结构化指令提示。在各项基准测试中，我们基于图的嵌入比基准方法能更好地区分真实图像和合成图像，且 OGD 在 sim2real 图像翻译方面优于最先进的扩散方法。总之，OGD 表明，显式编码真实感结构可以实现可解释、数据高效且具有泛化能力的零样本 sim2real 迁移。

## Abstract
Bridging the simulation-to-reality (sim2real) gap remains challenging as labelled real-world data is scarce. Existing diffusion-based approaches rely on unstructured prompts or statistical alignment, which do not capture the structured factors that make images look real. We introduce Ontology- Guided Diffusion (OGD), a neuro-symbolic zero-shot sim2real image translation framework that represents realism as structured knowledge. OGD decomposes realism into an ontology of interpretable traits -- such as lighting and material properties -- and encodes their relationships in a knowledge graph. From a synthetic image, OGD infers trait activations and uses a graph neural network to produce a global embedding. In parallel, a symbolic planner uses the ontology traits to compute a consistent sequence of visual edits needed to narrow the realism gap. The graph embedding conditions a pretrained instruction-guided diffusion model via cross-attention, while the planned edits are converted into a structured instruction prompt. Across benchmarks, our graph-based embeddings better distinguish real from synthetic imagery than baselines, and OGD outperforms state-of-the-art diffusion methods in sim2real image translations. Overall, OGD shows that explicitly encoding realism structure enables interpretable, data-efficient, and generalisable zero-shot sim2real transfer.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为**本体引导扩散（Ontology-Guided Diffusion, OGD）**的新型神经符号框架，旨在解决计算机视觉和机器人领域中长期存在的“仿真到现实”（Sim2Real）迁移难题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：仿真环境生成的图像与真实世界图像之间存在显著的“视觉鸿沟”（Sim2Real Gap），导致在模拟器中训练的模型在现实中表现不佳。
*   **研究动机**：
    *   现有的基于扩散模型的图像翻译方法通常依赖于**非结构化**的文本提示（Prompts）或隐式的统计对齐，缺乏对“真实感”构成因素（如光照、材质、传感器伪影）的显式建模。
    *   这些方法往往需要大量成对数据，且生成过程缺乏可解释性和精细控制。
    *   论文提出，通过将真实感分解为结构化的知识（本体），可以实现更高效、可解释且零样本（Zero-shot）的迁移。

### 2. 方法论
OGD 框架将 Sim2Real 转换分解为四个关键阶段：
*   **特征提取与特征预测**：利用预训练且冻结的 CLIP 编码器提取图像特征，通过多个轻量级 MLP 头部预测 N 个预定义的“真实感特征”（Traits，如阴影、色差、镜头畸变等）的概率。
*   **真实感本体与知识图谱（KG）**：
    *   构建一个静态知识图谱，节点为特征，边表示特征间的因果或感知关系（支持或对立）。
    *   使用**图神经网络（GNN）**处理这些特征激活，生成一个捕捉全局真实感一致性的结构化嵌入。
*   **符号规划（PDDL）**：
    *   使用规划领域定义语言（PDDL）将真实感转换建模为一个规划问题。
    *   给定合成图像的特征状态和目标真实状态，**符号规划器（Fast Downward）**计算出一系列逻辑一致的视觉编辑动作（如“增加镜头畸变”、“调整光照梯度”）。
*   **本体引导的扩散模型**：
    *   基于 InstructPix2Pix 架构，将符号规划生成的动作序列转化为结构化文本指令。
    *   将 GNN 生成的图嵌入通过交叉注意力机制注入扩散网络。
    *   引入**真实感对齐损失（KG Alignment Loss）**，确保生成的图像在特征空间中符合目标真实感结构。

### 3. 实验设计
*   **数据集**：
    *   **非成对数据**：仅使用 140 张来自 CARLA、MuJoCo、Isaac Sim 等模拟器和真实世界（COCO）的图像来训练特征分类器和 GNN。
    *   **成对数据**：使用 Virtual KITTI 2 与真实 KITTI 数据集，以及一个自定义的 LEGO 数据集进行扩散模型训练。
*   **Benchmark 与对比方法**：
    *   **真实感分类任务**：对比 CLIP 特征、端到端 CNN、非结构化特征元分类器。
    *   **图像翻译任务**：对比 **InstructPix2Pix**（静态手动提示）和 **ControlNet**（使用深度图作为条件）。
    *   **评价指标**：特征距离（TraitDist）、感知相似度（LPIPS）、结构相似度（SSIM）。

### 4. 资源与算力
*   **论文中未明确说明**具体的 GPU 型号、数量及训练总时长。
*   文中提到使用了 GPT-4o 作为离线自动标注器来生成训练特征预测器所需的伪标签，这部分涉及外部 API 调用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了真实/合成图像的二分类实验，验证了图嵌入的判别力。
    *   在 KITTI 和 LEGO 两个不同领域的场景下进行了 Sim2Real 翻译测试。
    *   **消融实验**：分别验证了“无知识图谱”、“无符号规划”和“无对齐损失”对结果的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从特征表示质量到最终生成图像质量的多个维度。通过消融实验客观地证明了神经符号结合的有效性。

### 6. 主要结论与发现
*   **结构化知识优于统计对齐**：显式建模真实感特征及其相互关系，比单纯依赖扩散模型的隐式学习更能捕捉 Sim2Real 的本质差异。
*   **数据效率极高**：仅需 140 张非成对图像即可学习到鲁棒的真实感特征表示，且在推理阶段支持零样本迁移。
*   **性能领先**：OGD 在保持语义结构（SSIM）的同时，在感知真实感（LPIPS）和特征一致性上均优于 SOTA 扩散模型基准。

### 7. 优点（亮点）
*   **神经符号结合**：首次将 PDDL 符号规划引入扩散模型的图像编辑流程，确保了编辑动作的逻辑一致性。
*   **可解释性**：通过特征概率和规划动作，用户可以清晰地看到模型认为“哪里不真实”以及“如何修改”。
*   **通用性**：本体论基于通用的图形学和感知原理，不依赖于特定的模拟器内部参数。

### 8. 不足与局限
*   **本体依赖**：真实感本体和知识图谱目前是手动定义的，虽然基于文献，但可能无法覆盖所有极端或新兴的视觉现象。
*   **标注风险**：依赖 VLM（如 GPT-4o）生成初始伪标签，若 VLM 存在感知偏差，可能会影响特征预测器的准确性。
*   **实时性**：引入了 GNN 推理和 PDDL 规划，虽然这些模块相对轻量，但在极高帧率要求的实时系统中可能存在延迟瓶颈。

（完）

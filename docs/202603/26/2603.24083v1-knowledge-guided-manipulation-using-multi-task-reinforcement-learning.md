---
title: Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning
title_zh: 基于多任务强化学习的知识引导操纵
authors: "Aditya Narendra, Mukhammadrizo Maribjonov, Dmitry Makarov, Dmitry Yudin, Aleksandr Panov"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24083v1"
tags: ["query:q2"]
score: 9.0
evidence: 基于3D场景图和强化学习的多任务机器人操作
tldr: 本文提出KG-M3PO框架，旨在解决部分可观测环境下的多任务机器人操作难题。该方法通过在线构建3D场景图来整合空间、包含及功能关系，并利用图神经网络将结构化知识与视觉、本体感受及语言信息融合，通过端到端强化学习优化控制策略。研究证明，引入动态维护的世界知识能显著提升机器人在复杂、多变场景中的操作性能与泛化能力。
selection_source: fresh_fetch
motivation: 针对机器人操作中因部分可观测性、遮挡和环境布局变化导致的决策鲁棒性不足问题。
method: 提出一种结合在线3D场景图与多任务强化学习的框架，通过图编码器将动态语义关系直接融入控制回路。
result: 在存在遮挡和干扰的多种任务中，该方法在成功率、采样效率及对新物体的泛化表现上均显著优于现有基准。
conclusion: 结构化的世界知识是实现通用机器人操作的关键归纳偏置，将知识模块纳入强化学习计算图能有效对齐关系表示与控制需求。
---

## 摘要
本文介绍了基于知识图谱的大规模多任务基于模型的策略优化（KG-M3PO），这是一个在部分可观测环境下统一了感知、知识和策略的多任务机器人操纵框架。该方法通过在线 3D 场景图增强了第一视角视觉，将开放词汇检测落地为度量式的关系表示。动态关系机制在每一步更新空间、包含和可供性（affordance）边，并且图神经网络编码器通过强化学习（RL）目标进行端到端训练，使得关系特征直接由控制性能塑造。多种观测模态（视觉、本体感受、语言和基于图的）被编码到一个共享的潜空间中，RL 智能体在此基础上运行以驱动控制循环。策略以轻量级图查询以及视觉和本体感受输入为条件，为决策提供了一个紧凑且具有语义信息的决策状态。在一系列包含遮挡、干扰物和布局变化的操纵任务上的实验表明，该方法相比强基线模型取得了持续的提升：知识条件化智能体实现了更高的成功率、更高的样本效率，以及对新物体和未见场景配置更强的泛化能力。这些结果支持了这样一个前提，即结构化、持续维护的世界知识是实现可扩展、可泛化操纵的强大归纳偏置：当知识模块参与 RL 计算图时，关系表示与控制相对齐，从而在部分可观测性下实现稳健的长程行为。

## Abstract
This paper introduces Knowledge Graph based Massively Multi-task Model-based Policy Optimization (KG-M3PO), a framework for multi-task robotic manipulation in partially observable settings that unifies Perception, Knowledge, and Policy. The method augments egocentric vision with an online 3D scene graph that grounds open-vocabulary detections into a metric, relational representation. A dynamic-relation mechanism updates spatial, containment, and affordance edges at every step, and a graph neural encoder is trained end-to-end through the RL objective so that relational features are shaped directly by control performance. Multiple observation modalities (visual, proprioceptive, linguistic, and graph-based) are encoded into a shared latent space, upon which the RL agent operates to drive the control loop. The policy conditions on lightweight graph queries alongside visual and proprioceptive inputs, yielding a compact, semantically informed state for decision making.   Experiments on a suite of manipulation tasks with occlusions, distractors, and layout shifts demonstrate consistent gains over strong baselines: the knowledge-conditioned agent achieves higher success rates, improved sample efficiency, and stronger generalization to novel objects and unseen scene configurations. These results support the premise that structured, continuously maintained world knowledge is a powerful inductive bias for scalable, generalizable manipulation: when the knowledge module participates in the RL computation graph, relational representations align with control, enabling robust long-horizon behavior under partial observability.

---

## 论文详细总结（自动生成）

以下是对论文《Knowledge-Guided Manipulation Using Multi-Task Reinforcement Learning》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人在非结构化日常环境中进行操纵时，面临严重的**部分可观测性（Partial Observability）**问题。物体常被遮挡、放置在容器内或因先前动作而移动。
*   **研究动机**：
    *   **反应式策略的局限**：纯视觉到动作的反应式策略容易产生状态混淆，且在长程任务中难以进行信用分配。
    *   **传统方法的不足**：传统的基于模型的方法依赖脆弱的手调几何流水线，难以扩展至多任务场景。
    *   **需求**：需要一个能够整合**持久化世界知识**、**多任务泛化能力**以及**端到端学习**的统一框架，以弥合感知与策略之间的鸿沟。

### 2. 方法论
论文提出了 **KG-M3PO**（Knowledge-Guided Massively Multi-task Model-based Policy Optimization）框架，其核心思想是将动态 3D 场景图（3DSG）集成到强化学习的计算图中。
*   **核心思想**：通过在线维护的 3D 场景图提供结构化的语义和空间关系，并将其作为策略的输入条件。
*   **关键技术细节**：
    *   **在线场景图构建**：利用 BBQ 算法从第一视角 RGB-D 流中提取物体边界框和 CLIP 嵌入，构建包含空间（如“在...后面”）、包含（如“在...里面”）和可供性关系的图。
    *   **动态更新机制**：每 10 个步骤刷新受影响的子图，确保在物体移动或环境变化时知识的实时性。
    *   **端到端图编码器**：使用图神经网络（GNN）提取知识向量 $k_t$。关键创新在于 **GNN 的参数直接通过 RL 的损失函数进行梯度回传**，使关系表示直接为控制性能服务。
    *   **多模态融合**：将视觉特征、本体感受状态、语言目标和图知识向量拼接，输入到基于高斯分布的策略头和价值头中。

### 3. 实验设计
*   **实验场景**：基于 NVIDIA Isaac Lab 构建的物理仿真环境，使用 Franka Panda 和 UR5 两种机器人臂。
*   **任务设置**：
    *   **全可观测（FO）**：抓取方块、放置方块、开柜子。
    *   **部分可观测（PO）**：抓取被墙遮挡的物体、从容器中取出物体（需先移开遮挡物）。
*   **对比基准（Baselines）**：
    *   **算法对比**：PPO、SAC、DreamerV3、TD-MPC2、IMPALA 以及基础版 M3PO。
    *   **输入对比**：仅相机输入（Camera-only） vs. 相机+知识图谱（Camera+KG）。
*   **评估指标**：采用归一化评分（0-1000分），锚定随机策略（0分）和专家水平（1000分）。

### 4. 资源与算力
*   **硬件配置**：单台工作站，配备 **NVIDIA RTX 5070 Ti (16 GB VRAM)** 和 64 GB RAM。
*   **并行规模**：利用 Isaac Lab 的平铺渲染 API，在单个进程中并行运行 **1024 个环境**。
*   **训练时长**：
    *   基础 M3PO（仅相机）训练 4 亿步约需 0.61 GPU-天。
    *   KG-M3PO（带知识图谱）训练 4 亿步约需 **0.79 GPU-天**。
    *   虽然 KG 增加了计算开销，但在达到相同性能（如 600 分）时，KG-M3PO 所需的总步数和总时间远少于基线。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 种不同难度的操纵任务，并在两种不同的机器人平台上进行了验证。
*   **消融实验**：专门针对**场景图噪声**进行了鲁棒性分析，测试了边界框中心位移、尺寸缩放和旋转噪声对策略的影响。
*   **充分性评价**：实验设计较为充分且客观。通过多任务联合训练和单任务对比，清晰地展示了知识图谱在不同观测条件下的增益。使用 1024 个并行环境确保了结果的统计显著性。

### 6. 主要结论与发现
*   **知识图谱的必要性**：在部分可观测任务中，仅靠相机的策略几乎无法成功，而 KG-M3PO 能够通过持久化状态解决遮挡问题。
*   **效率提升**：在全可观测任务中，引入结构化知识也能显著提升采样效率和最终成功率。
*   **端到端优势**：将 GNN 纳入 RL 优化循环，使得提取的特征与控制目标高度对齐，优于离线预训练或分离的图特征。
*   **鲁棒性**：策略对中等程度的几何感知噪声具有较强的容忍度，性能下降平缓。

### 7. 优点
*   **架构统一**：成功将高层语义关系（场景图）与底层连续控制（RL）在端到端框架下统一。
*   **动态性强**：在线更新机制解决了静态场景图容易过时的问题，适应动态操纵环境。
*   **泛化潜力**：通过语言目标和开放词汇检测，框架具备处理新物体和复杂指令的潜力。

### 8. 不足与局限
*   **缺乏真机验证**：所有实验均在仿真（Isaac Sim）中完成，尚未在真实物理机器人上验证感知噪声和延迟的影响。
*   **感知依赖**：系统高度依赖底层物体检测器的准确性，若检测器在大规模遮挡下彻底失效，场景图的维护将面临挑战。
*   **计算开销**：虽然采样效率高，但每一步的图更新和 GNN 推理增加了额外的计算负担。
*   **动态物体限制**：对于高速运动或拓扑结构剧烈变化的物体，目前的图更新频率可能不足。

（完）

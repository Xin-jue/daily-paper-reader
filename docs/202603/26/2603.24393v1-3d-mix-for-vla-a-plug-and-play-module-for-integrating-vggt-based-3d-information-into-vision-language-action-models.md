---
title: "3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models"
title_zh: 3D-Mix for VLA：一种将基于 VGGT 的 3D 信息集成到视觉-语言-动作模型中的即插即用模块
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Haishan Liu, Changti Wu, Hang Yuan, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24393v1"
tags: ["query:q10"]
score: 10.0
evidence: 在操作任务的VLA模型中集成3D信息
tldr: 针对视觉-语言-动作（VLA）模型在3D空间感知能力的不足，本文提出了一种名为3D-Mix的即插即用模块。通过对九种集成方案的系统性研究，发现语义调节的门控融合机制能最有效地平衡2D语义与3D几何特征。该模块无需修改现有模型架构即可显著提升多种VLA模型在复杂操作任务中的表现，尤其在跨域测试中表现优异，为增强机器人空间智能提供了有效方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要基于2D数据训练，导致其在机器人操控任务中缺乏足够的3D空间感知和几何理解能力。
method: 提出3D-Mix即插即用模块，通过语义调节的门控融合机制将VGGT提取的3D几何特征自适应地集成到VLA架构中。
result: "在多个基准测试中，3D-Mix在九种模型变体上均实现性能提升，在SIMPLER跨域测试中平均性能增益达到7.0%。"
conclusion: 3D-Mix证明了通过原则性的融合策略集成3D信息可以显著增强VLA系统的空间智能和泛化能力。
---

## 摘要
视觉-语言-动作 (VLA) 模型利用多模态大语言模型 (MLLM) 进行机器人控制，但近期研究表明，由于主要在 2D 数据上进行训练，MLLM 的空间智能有限，导致其在操作任务中的 3D 感知能力不足。虽然最近的方法引入了如 VGGT 等专门的 3D 视觉模型来增强空间理解，但它们采用了多种集成机制且缺乏系统性研究，使得最佳融合策略尚不明确。我们进行了一项全面的初步研究，在标准化基准上比较了九种 VGGT 集成方案，发现语义条件门控融合在所有评估方案中表现最佳，该方案能根据任务上下文自适应地平衡 2D 语义特征和 3D 几何特征。我们提出了 3D-Mix，这是一个即插即用模块，可集成到多种 VLA 架构（如 GR00T 风格和 π 风格）中，且无需修改现有的 MLLM 或动作专家组件。在 SIMPLER 和 LIBERO 基准上针对六个 MLLM 系列（九个模型变体，2B-8B 参数）进行的实验表明，3D-Mix 带来了持续的性能提升，在所有九个 GR00T 风格变体中，其在分布外 (OOD) SIMPLER 基准上的平均提升达 +7.0%，为增强 VLA 系统的空间智能建立了一种原则性方法。

## Abstract
Vision-Language-Action (VLA) models leverage Multimodal Large Language Models (MLLMs) for robotic control, but recent studies reveal that MLLMs exhibit limited spatial intelligence due to training predominantly on 2D data, resulting in inadequate 3D perception for manipulation tasks. While recent approaches incorporate specialized 3D vision models such as VGGT to enhance spatial understanding, they employ diverse integration mechanisms without systematic investigation, leaving the optimal fusion strategy unclear. We conduct a comprehensive pilot study comparing nine VGGT integration schemes on standardized benchmarks and find that semantic-conditioned gated fusion, which adaptively balances 2D semantic and 3D geometric features based on task context, achieved the strongest performance among all nine evaluated fusion schemes in our pilot study. We present 3D-Mix, a plug-and-play module that integrates into diverse VLA architectures (GR00T-style and $π$-style) without modifying existing MLLM or action expert components. Experiments across six MLLM series (nine model variants, 2B--8B parameters) on SIMPLER and LIBERO show that 3D-Mix delivers consistent performance gains, averaging +7.0% on the out-of-domain (OOD) SIMPLER benchmark across all nine GR00T-style variants, establishing a principled approach for enhancing spatial intelligence in VLA systems.

---

## 论文详细总结（自动生成）

### 论文总结：3D-Mix for VLA

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在机器人操控中表现出潜力，但其核心组件多模态大语言模型（MLLM）主要在 2D 图像-文本数据上预训练，缺乏显式的 3D 几何监督。这导致 VLA 模型在处理需要精确空间推理、深度感知和抓取姿态估计的任务时表现不佳。虽然已有研究尝试引入 3D 视觉模型（如 VGGT），但集成策略多种多样且缺乏系统性对比。本论文旨在探索**如何最有效地将 3D 几何信息集成到 VLA 模型中**，并提出了一种通用的增强方案。

#### 2. 方法论：核心思想与技术细节
论文提出了 **3D-Mix**，一个即插即用的模块，其核心思想是**语义调节的自适应门控融合（Semantic-conditioned Adaptive Gating）**。
*   **特征提取与对齐**：使用预训练且冻结的 VGGT 提取 3D 几何特征，通过线性投影将其映射到 MLLM 的隐藏空间。
*   **语义调节门控**：
    1.  从 MLLM 的隐藏状态中提取全局语义摘要（通过均值池化）。
    2.  将全局语义上下文与局部几何特征拼接，通过一个可学习的门控网络（Sigmoid 激活）计算每个位置的权重。
    3.  根据权重动态平衡 2D 语义和 3D 几何特征，生成融合后的几何 Token。
*   **架构集成**：
    *   **GR00T 风格**：在 MLLM 最后一层输出后插入 3D-Mix，将融合特征与 MLLM 特征拼接后送入动作专家（DiT）。
    *   **$\pi$ 风格**：采用层级融合，在 MLLM 的多个中间层分别应用 3D-Mix，实现深度的语义-动作对齐。
*   **即插即用**：该模块无需修改 MLLM 或动作专家的内部结构，仅作为中间桥梁存在。

#### 3. 实验设计
*   **初步研究（Pilot Study）**：对比了 9 种融合方案（包括早期融合、中期注入、动作专家融合、空间强迫等），确定了门控融合的最优性。
*   **数据集与基准**：
    *   **SIMPLER**：一个 Real-to-Sim 的分布外（OOD）基准，测试模型在视觉变化和物体配置改变下的泛化能力。
    *   **LIBERO**：包含四个任务套件（空间、物体、目标、长程），评估域内多任务学习能力。
*   **对比对象**：涵盖了 6 个 MLLM 系列（Qwen2.5-VL, Qwen3-VL, RoboBrain 2.0/2.5, MimoEmbodied, RynnBrain），共 9 个模型变体（参数量 2B-8B）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 × NVIDIA H100 GPU**。
*   **训练配置**：采用 DeepSpeed ZeRO-2 优化器。
*   **时长与规模**：每个模型训练 **60,000 步**，全局 Batch Size 为 128。
*   **推理开销**：VGGT 模型在推理时保持冻结，3D-Mix 模块本身非常轻量。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **广度**：在 9 种不同的 MLLM 后端上验证了有效性，证明了方法的通用性。
*   **深度**：通过初步研究系统性地排除了其他 8 种融合方案，确保了最终选择的科学性。
*   **消融实验**：涵盖了 VGGT 冻结 vs. 微调、3D 信息敏感性测试（替换为噪声或零向量）、以及 $\pi$ 风格中的稀疏层融合深度测试。
*   **客观性**：在 OOD（SIMPLER）和域内（LIBERO）两个维度进行评估，避免了单一场景的偏差。

#### 6. 主要结论与发现
*   **最优策略**：语义调节的门控融合是集成 3D 信息的最佳方式，因为它能根据任务上下文动态调整对几何信息的依赖程度。
*   **性能提升**：3D-Mix 在所有测试的 VLA 架构中均实现了性能增益。在 SIMPLER 跨域基准上，GR00T 风格变体的平均成功率提升了 **7.0%**。
*   **冻结优势**：冻结预训练的 VGGT 效果优于或等同于微调，说明通用的几何特征具有极强的迁移能力。
*   **空间智能**：实验证实 MLLM 的确存在“空间盲点”，而引入显式的 3D 几何特征能有效弥补这一缺陷。

#### 7. 优点
*   **通用性强**：支持多种主流 VLA 架构（模块化 vs. 紧耦合），适配不同规模的 MLLM。
*   **低侵入性**：即插即用设计意味着开发者无需重新训练庞大的 MLLM 基座，降低了部署门槛。
*   **原则性研究**：不仅提出了方法，还通过详尽的对比实验回答了“3D 信息应该在哪里以及如何融合”的根本问题。

#### 8. 不足与局限
*   **推理延迟**：虽然 3D-Mix 模块轻量，但引入额外的 VGGT 编码器会增加整体推理的时间开销和显存占用。
*   **仿真依赖**：主要实验在仿真环境（SIMPLER, LIBERO）中完成，虽然 SIMPLER 具有 Real-to-Sim 特性，但在复杂多变的真实物理世界中的表现仍需进一步验证。
*   **VGGT 依赖**：性能高度依赖于预训练 VGGT 的质量，若 3D 编码器本身存在感知偏差，3D-Mix 的效果可能会受限。

（完）

---
title: "Gym-V: A Unified Vision Environment System for Agentic Vision Research"
title_zh: Gym-V：一个用于智能体视觉研究的统一视觉环境系统
authors: "Fanqing Meng, Lingxiao Du, Jiawei Gu, Jiaqi Liao, Linjie Li, Zijian Wu, Xiangyan Liu, Ziqi Zhao, Mengkang Hu, Yue Zhang, Zichen Liu, Jiaheng Zhang, Michael Qizhe Shieh"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15432v2"
tags: ["query:q11"]
score: 9.0
evidence: 用于智能体训练的程序化生成视觉环境
tldr: Gym-V是一个统一的视觉环境平台，包含10个领域、179个程序化生成的视觉任务，旨在解决视觉智能体缺乏标准化基础设施的问题。研究发现，观察脚手架（如描述和规则）对训练成功的影响超过了强化学习算法的选择。该平台支持受控实验，揭示了跨领域迁移和多轮交互对模型泛化能力的影响，为视觉语言模型（VLM）智能体的训练和评估提供了重要基础。
selection_source: fresh_fetch
motivation: 视觉智能体研究缺乏标准化的基础设施，导致难以进行系统性的学习驱动因素分析和公平比较。
method: 开发了包含179个程序化生成环境的Gym-V系统，支持可控难度和多领域任务，用于视觉强化学习实验。
result: 实验表明观察脚手架比RL算法更关键，且多样化任务训练能显著提升泛化能力，而窄域训练可能导致负迁移。
conclusion: Gym-V为视觉智能体研究提供了便捷的训练和评估基准，有助于加速未来视觉语言模型智能体的发展。
---

## 摘要
随着智能体系统越来越多地依赖于来自可验证奖励的强化学习，标准化的“gym”基础设施已成为快速迭代、可复现性和公平比较的关键。视觉智能体缺乏此类基础设施，限制了对驱动其学习的因素以及当前模型不足之处的系统性研究。我们推出了 Gym-V，这是一个统一的平台，包含分布在 10 个领域、难度可控的 179 个程序化生成的视觉环境，能够实现以前在碎片化工具包中无法进行的受控实验。通过使用该平台，我们发现观察支架（observation scaffolding）对训练成功的决定性作用超过了强化学习算法的选择，其中字幕和游戏规则决定了学习是否能够成功。跨领域迁移实验进一步表明，在多样化的任务类别上进行训练具有广泛的泛化能力，而狭隘的训练可能导致负迁移，且多轮交互会放大所有这些效应。Gym-V 作为一个便捷的训练环境和评估工具包基础发布，旨在加速未来对智能体视觉语言模型（agentic VLMs）的研究。

## Abstract
As agentic systems increasingly rely on reinforcement learning from verifiable rewards, standardized ``gym'' infrastructure has become essential for rapid iteration, reproducibility, and fair comparison. Vision agents lack such infrastructure, limiting systematic study of what drives their learning and where current models fall short. We introduce \textbf{Gym-V}, a unified platform of 179 procedurally generated visual environments across 10 domains with controllable difficulty, enabling controlled experiments that were previously infeasible across fragmented toolkits. Using it, we find that observation scaffolding is more decisive for training success than the choice of RL algorithm, with captions and game rules determining whether learning succeeds at all. Cross-domain transfer experiments further show that training on diverse task categories generalizes broadly while narrow training can cause negative transfer, with multi-turn interaction amplifying all of these effects. Gym-V is released as a convenient foundation for training environments and evaluation toolkits, aiming to accelerate future research on agentic VLMs.

---

## 论文详细总结（自动生成）

### Gym-V：用于智能体视觉研究的统一视觉环境系统论文总结

#### 1. 研究动机与核心问题
*   **研究背景**：当前大语言模型（LLM）智能体已拥有成熟的“Gym”式训练基础设施（如 Reasoning Gym），支持程序化生成、自动验证和统一接口。然而，视觉智能体（Vision Agents）仍缺乏此类标准化平台，导致研究碎片化，难以进行公平比较。
*   **核心问题**：视觉感知是智能体学习的瓶颈，但目前尚不清楚视觉观察的呈现方式（Observation Scaffolding）如何影响强化学习（RL）的效果。
*   **论文目标**：推出 **Gym-V**，一个包含 179 个程序化生成环境的统一平台，旨在标准化视觉智能体的训练与评估，并系统研究驱动视觉智能体学习的关键因素。

#### 2. 方法论与关键技术
*   **核心思想**：通过统一的 Gym 兼容 API（支持 `reset()` 和 `step()`），将单轮视觉推理、多轮交互游戏、离线数据集和生成式任务评估整合在一个框架内。
*   **关键技术细节**：
    *   **程序化生成与自动验证**：所有环境均支持确定性种子生成，并内置规则校验器，确保奖励信号的可验证性。
    *   **可组合包装层（Wrappers）**：将观察设计（如是否提供字幕、规则描述、历史窗口长度）作为实验变量，无需修改环境底层逻辑即可调整任务呈现方式。
    *   **分布式奖励服务（EaaS）**：针对图像生成或编辑等生成式任务，采用“评估即服务”架构，通过 Ray Serve 部署昂贵的奖励模型（如 CLIP、HPSv3），支持跨 GPU 自动批处理。
    *   **多智能体支持**：遵循 Ray RLlib 规范，原生支持单智能体、竞争和协作模式。

#### 3. 实验设计
*   **场景与环境**：涵盖 10 个领域（算法、ARC、认知、几何、图论、逻辑、拼图、游戏、空间、时间），共 179 个环境。
*   **对比模型（Benchmark）**：对 9 个主流视觉语言模型（VLM）进行了零样本（Zero-shot）评估，包括 Gemini-3-Pro、Claude 4.5 Sonnet、GPT-5.2（预估版本）、Qwen3-VL 系列及 Qwen2.5-VL 系列。
*   **强化学习算法对比**：在统一算力预算下对比了 **GRPO**、**GSPO** 和 **SAPO** 三种主流 RL 算法。
*   **消融实验**：
    *   **历史长度**：对比 MDP（无历史）、最近 3 轮和最近 5 轮历史的影响。
    *   **规则注入**：对比 Prompt 中包含与不包含游戏规则的效果。
    *   **视觉脚手架**：对比“仅图像”与“图像 + 字幕”的性能差异。
    *   **跨领域迁移**：通过 6x6（单轮）和 4x4（多轮）迁移矩阵研究泛化性。

#### 4. 资源与算力
*   **基础模型**：训练实验主要使用 **Qwen2.5-VL-7B-Instruct** 作为基座模型。
*   **训练框架**：使用 **verl** 强化学习框架。
*   **算力细节**：文中提到训练迭代中每批次采样 256 个实例/轨迹，单轮任务每个实例生成 8 个 rollout。**但论文未明确给出具体的 GPU 型号、总数量及总训练时长**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 105 个单轮环境和 74 个多轮环境，实验覆盖面极广。
*   **充分性**：论文不仅做了横向的模型对比，还深入进行了 RL 算法性能分析、观察设计的消融研究以及复杂的跨领域迁移矩阵实验。
*   **客观性**：通过统一接口和固定超参数进行“苹果对苹果”的对比，并验证了 Gym-V 评估结果与官方 Benchmark（如 MMMU, GenEval）的高度一致性，实验设计严谨客观。

#### 6. 主要结论与发现
*   **观察脚手架至关重要**：观察设计的选择（如添加字幕、提供规则）对训练成功的影响**超过了 RL 算法的选择**。在某些环境下，没有规则描述会导致学习完全失败。
*   **算法表现具有场景依赖性**：没有一种 RL 算法在所有任务中占优。GSPO 在长时程多轮任务中表现更稳健，而 SAPO 在复杂设置下可能崩溃。
*   **迁移学习的非对称性**：在多样化技能类别（如认知、拼图）上训练具有较好的泛化性；而在狭窄类别（如几何）上训练可能导致负迁移。
*   **多轮交互的放大效应**：多轮交互会放大感知错误和策略漂移，使得脚手架设计和课程选择变得更加关键。

#### 7. 论文优点
*   **高度统一性**：首次将视觉智能体的训练、离线监督和基准评估整合进单一 API。
*   **工具属性强**：提供了难度可控的程序化环境，解决了视觉数据污染和评估饱和问题。
*   **洞察深刻**：通过大量受控实验揭示了“观察设计”在视觉 RL 中的核心地位，为后续研究指明了方向。
*   **架构先进**：EaaS 架构有效解决了生成式任务评估成本高、部署难的问题。

#### 8. 不足与局限
*   **时间维度任务挑战**：在“时间（Temporal）”类别（如复古街机游戏）中，当前所有 VLM 智能体几乎都无法取得进展（奖励接近零），显示出模型在精细时间控制上的巨大鸿沟。
*   **算力成本**：虽然提供了统一接口，但大规模视觉 RL 训练对算力的需求依然极高，普通研究者难以复现全量实验。
*   **规则依赖**：部分实验结果显示模型高度依赖 Prompt 中的规则说明，这可能暗示模型在真正的“自主探索”和“规则发现”能力上仍有欠缺。

（完）

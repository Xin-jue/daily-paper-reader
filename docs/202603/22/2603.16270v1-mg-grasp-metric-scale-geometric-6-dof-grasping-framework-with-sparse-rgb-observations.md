---
title: "MG-Grasp: Metric-Scale Geometric 6-DoF Grasping Framework with Sparse RGB Observations"
title_zh: MG-Grasp：基于稀疏 RGB 观测的度量尺度几何 6-DoF 抓取框架
authors: "Kangxu Wang, Siang Chen, Chenxing Jiang, Shaojie Shen, Yixiang Dai, Guijin Wang"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16270v1"
tags: ["query:q2"]
score: 9.0
evidence: 利用3D基础模型和RGB观测的六自由度机器人抓取框架
tldr: 针对传统RGB-D抓取依赖深度传感器及现有纯RGB方法几何表示不准确的问题，本文提出MG-Grasp框架。该框架利用双视图3D大模型，结合相机内外参，从稀疏RGB图像中重建出具有度量尺度且多视图一致的稠密点云，进而生成稳定的6-DoF抓取位姿。实验证明其在GraspNet-1Billion及真实场景中达到了纯RGB抓取领域的领先水平。
selection_source: fresh_fetch
motivation: 现有的纯RGB 6-DoF抓取方法由于几何表示不精确，难以实现物理上可靠的机器人操作。
method: 利用双视图3D基础模型结合相机参数，从稀疏RGB图像重建度量尺度的稠密点云并生成抓取位姿。
result: 在GraspNet-1Billion数据集和真实世界实验中，该方法在纯RGB 6-DoF抓取任务上取得了当前最先进的性能。
conclusion: MG-Grasp证明了通过高质量几何重建可以显著提升无深度传感器条件下的机器人抓取可靠性。
---

## 摘要
单视图 RGB-D 抓取检测仍然是 6-DoF 机器人抓取系统中的常用选择，这通常需要深度传感器。虽然最近对仅使用 RGB 的 6-DoF 抓取方法进行了研究，但其不准确的几何表示并不直接适用于物理上可靠的机器人操作，从而阻碍了可靠抓取的生成。为了解决这些局限性，我们提出了 MG-Grasp，这是一种新型的无深度 6-DoF 抓取框架，可实现高质量的物体抓取。利用结合相机内参/外参的双视图 3D 基础模型，我们的方法从稀疏 RGB 图像中重建出度量尺度且多视图一致的稠密点云，并生成稳定的 6-DoF 抓取。在 GraspNet-1Billion 数据集和真实世界中的实验表明，MG-Grasp 在基于 RGB 的 6-DoF 抓取方法中达到了最先进（SOTA）的抓取性能。

## Abstract
Single-view RGB-D grasp detection remains a com- mon choice in 6-DoF robotic grasping systems, which typically requires a depth sensor. While RGB-only 6-DoF grasp methods has been studied recently, their inaccurate geometric repre- sentation is not directly suitable for physically reliable robotic manipulation, thereby hindering reliable grasp generation. To address these limitations, we propose MG-Grasp, a novel depth- free 6-DoF grasping framework that achieves high-quality object grasping. Leveraging two-view 3D foundation model with camera intrinsic/extrinsic, our method reconstructs metric- scale and multi-view consistent dense point clouds from sparse RGB images and generates stable 6-DoF grasp. Experiments on GraspNet-1Billion dataset and real world demonstrate that MG-Grasp achieves state-of-the-art (SOTA) grasp performance among RGB-based 6-DoF grasping methods.

---

## 论文详细总结（自动生成）

以下是对论文《MG-Grasp: Metric-Scale Geometric 6-DoF Grasping Framework with Sparse RGB Observations》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统的 6-DoF 机器人抓取高度依赖 RGB-D 传感器（深度相机），但深度传感器成本高、在强光或透明/反光物体上表现差。现有的纯 RGB 抓取方法（如基于 NeRF 或 3D 基础模型的方法）往往存在几何表示不准确、缺乏绝对度量尺度（Metric-Scale）以及多视图不一致的问题，导致物理抓取的成功率较低。
*   **研究动机**：利用现有的 3D 基础模型（如 MASt3R），结合已知的相机位姿，从稀疏的 RGB 图像中重建出具有物理度量意义且多视图一致的高质量点云，从而实现可靠的 6-DoF 抓取。

### 2. 方法论
MG-Grasp 框架主要由两个核心模块组成：

*   **度量尺度深度图生成器 (Metric-Scale Depth Map Generator)**：
    *   **深度聚合与匹配**：利用 MASt3R 模型提取两两视图间的点图（Pointmaps）和特征，通过置信度加权聚合得到初始深度图，并建立像素级的稠密对应关系。
    *   **基于三角测量的尺度恢复**：这是关键步骤。由于基础模型输出的深度是“比例不确定”的，该模块利用已知的相机外参进行三角测量，计算出物理空间中的 3D 点，并将预测深度对齐到这些度量点上，从而获得具有真实物理尺寸的深度图。
*   **多视图引导抓取生成 (Multi-View Guided Grasp Generation)**：
    *   **两阶段多视图精细化**：为了解决视图间的对齐误差，设计了优化流程。**第一阶段（3D 一致性）**：最小化匹配点在 3D 空间中的距离；**第二阶段（2D 重投影一致性）**：通过重投影误差进一步约束几何精度。
    *   **抓取感知引导与生成**：利用 MobileSAM 去除背景，结合置信度过滤噪声点，生成高质量点云。最后将其输入局部抓取模型（LoG），通过最远点采样（FPS）和区域聚合预测 6-DoF 抓取位姿。

### 3. 实验设计
*   **数据集与基准**：
    *   **GraspNet-1Billion**：大规模抓取基准数据集，包含 Seen（已见）、Similar（相似）、Novel（新颖）三个子集，并区分 RealSense 和 Kinect 两种相机数据。
    *   **真实世界实验**：使用 UR5e 机械臂和 Robotiq 2F-85 夹爪，针对 25 种日常物体进行杂乱场景下的抓取测试。
*   **对比方法**：
    *   **RGB-D 方法**（作为上限参考）：GraspNet, TransGrasp, HGGD, FlexLoG 等。
    *   **纯 RGB 方法**：GraspNeRF, VG-Grasp 以及基于不同几何后端（VGGT, MVS-based）的变体。

### 4. 资源与算力
*   **硬件配置**：实验在一台配备 **NVIDIA RTX 4090 GPU** 和 Intel Core i9-13900K CPU 的台式机上运行。
*   **运行效率**：
    *   5 视图输入时，端到端推理延迟为 **2.86 秒**。
    *   4 视图输入时，延迟为 **2.13 秒**。
    *   其中几何处理阶段（匹配与精细化）占总耗时的 77.3%。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 GraspNet 数据集上进行了全面的精度（AP）评估。
    *   **消融实验**：验证了“尺度恢复”和“多视图精细化”两个模块的必要性。
    *   **视图数量分析**：测试了从 2 视图到 9 视图对性能的影响，发现 5-6 视图是性能与效率的最佳平衡点。
    *   **真实世界测试**：进行了 40 次抓取尝试，涵盖了多种杂乱堆叠场景。
*   **评价**：实验设计较为充分，涵盖了仿真数据集对比、消融分析及实机验证，对比基准选择合理，客观展示了纯 RGB 方法与 RGB-D 方法之间的差距正在缩小。

### 6. 主要结论与发现
*   **性能领先**：MG-Grasp 在纯 RGB 抓取领域达到了 SOTA 水平，平均 AP 显著超过了之前的 GraspNeRF 和 VG-Grasp。
*   **几何一致性的重要性**：通过显式的多视图几何优化，可以显著提升抓取位姿的准确性，尤其是在处理局部接触细节时。
*   **透明物体潜力**：在真实世界实验中，该方法对透明物体的抓取成功率达到 55%，显示出在深度相机失效场景下的独特优势。

### 7. 优点（亮点）
*   **无需深度传感器**：降低了硬件成本和部署复杂度。
*   **度量尺度恢复**：解决了纯视觉重建中常见的尺度漂移问题，使生成的抓取位姿具有实际操作意义。
*   **端到端流程**：将 3D 基础模型、几何优化与抓取检测有机结合，推理速度达到了秒级，具备实用价值。

### 8. 不足与局限
*   **实时性尚存差距**：2.86 秒的推理时间虽然在静态场景可行，但对于高速动态抓取或闭环控制仍显不足。
*   **极端材质挑战**：对于高反光（镜面）或极度圆滑的球体，稠密对应关系的提取仍可能出错，导致几何重建扭曲。
*   **计算开销**：两两视图间的约束计算随视图增加呈平方级增长，限制了更多视图的接入。

（完）

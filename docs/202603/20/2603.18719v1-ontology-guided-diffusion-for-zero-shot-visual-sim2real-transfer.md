---
title: Ontology-Guided Diffusion for Zero-Shot Visual Sim2Real Transfer
title_zh: 本体引导的扩散模型用于零样本视觉仿真到现实迁移
authors: "Mohamed Youssef, Mayar Elfares, Anna-Maria Meer, Matteo Bortoletto, Andreas Bulling"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18719v1"
tags: ["query:q8"]
score: 9.0
evidence: 本体引导的扩散模型，用于图像翻译中的零样本视觉仿真到现实迁移
tldr: 本研究针对模拟到现实（sim2real）迁移中真实感难以量化的问题，提出了一种名为本体引导扩散（OGD）的神经符号零样本框架。该方法将“真实感”分解为光照、材质等可解释特征的本体，并利用知识图谱和图神经网络提取全局嵌入，同时结合符号规划器生成结构化指令引导扩散模型。OGD不仅在图像转换质量上超越了现有SOTA方法，还通过显式建模结构化知识提升了模型的可解释性和泛化能力。
selection_source: fresh_fetch
motivation: 现有的扩散模型在sim2real转换中往往依赖非结构化提示，无法有效捕捉构成真实感的结构化关键因素。
method: 提出OGD框架，利用本体论将真实感分解为可解释特征，并通过知识图谱、图神经网络和符号规划器共同指导预训练扩散模型进行图像编辑。
result: 实验表明，OGD在区分真实与合成图像的准确性上优于基准，并在零样本sim2real图像转换任务中取得了领先的性能。
conclusion: 显式编码真实感的结构化知识是实现高效、可解释且泛化性强的零样本sim2real迁移的关键路径。
---

## 摘要
弥合仿真到现实（sim2real）的差距仍然具有挑战性，因为带标签的真实世界数据非常稀缺。现有的基于扩散的方法依赖于非结构化提示或统计对齐，这些方法无法捕捉使图像看起来真实的结构化因素。我们引入了本体引导扩散（OGD），这是一种神经符号零样本 sim2real 图像翻译框架，它将真实感表示为结构化知识。OGD 将真实感分解为可解释特征的本体（例如光照和材料属性），并在知识图中编码它们的关系。从合成图像中，OGD 推断特征激活，并使用图神经网络生成全局嵌入。与此同时，符号规划器利用本体特征计算缩小真实感差距所需的一致视觉编辑序列。图嵌入通过交叉注意力机制调节预训练的指令引导扩散模型，而规划的编辑则被转换为结构化指令提示。在各项基准测试中，我们基于图的嵌入比基准方法能更好地区分真实图像和合成图像，且 OGD 在 sim2real 图像翻译方面优于最先进的扩散方法。总之，OGD 表明，显式编码真实感结构可以实现可解释、数据高效且具有泛化能力的零样本 sim2real 迁移。

## Abstract
Bridging the simulation-to-reality (sim2real) gap remains challenging as labelled real-world data is scarce. Existing diffusion-based approaches rely on unstructured prompts or statistical alignment, which do not capture the structured factors that make images look real. We introduce Ontology- Guided Diffusion (OGD), a neuro-symbolic zero-shot sim2real image translation framework that represents realism as structured knowledge. OGD decomposes realism into an ontology of interpretable traits -- such as lighting and material properties -- and encodes their relationships in a knowledge graph. From a synthetic image, OGD infers trait activations and uses a graph neural network to produce a global embedding. In parallel, a symbolic planner uses the ontology traits to compute a consistent sequence of visual edits needed to narrow the realism gap. The graph embedding conditions a pretrained instruction-guided diffusion model via cross-attention, while the planned edits are converted into a structured instruction prompt. Across benchmarks, our graph-based embeddings better distinguish real from synthetic imagery than baselines, and OGD outperforms state-of-the-art diffusion methods in sim2real image translations. Overall, OGD shows that explicitly encoding realism structure enables interpretable, data-efficient, and generalisable zero-shot sim2real transfer.

---

## 论文详细总结（自动生成）

### 论文总结：Ontology-Guided Diffusion (OGD) for Zero-Shot Visual Sim2Real Transfer

#### 1. 核心问题与整体含义（研究动机和背景）
在机器人和具身智能领域，**仿真到现实（Sim2Real）的迁移差距**是长期存在的挑战。虽然仿真器能生成大量带标签数据，但合成图像往往缺乏真实世界的复杂视觉线索（如光照不规则性、材质缺陷、传感器伪影）。
*   **现有局限**：目前的扩散模型（Diffusion Models）主要依赖非结构化文本提示或隐式统计对齐。这种方式缺乏对“真实感”构成因素的显式建模，导致生成结果不可控、缺乏物理一致性，且往往需要大量配对数据或繁琐的提示工程。
*   **核心目标**：提出一种**神经符号（Neuro-symbolic）框架**，将真实感定义为结构化知识，通过本体论（Ontology）和符号规划引导扩散模型，实现可解释、数据高效且零样本的图像翻译。

#### 2. 方法论：核心思想与技术细节
OGD 框架将 Sim2Real 任务分解为四个关键阶段：

*   **特征表示（Trait-based Representation）**：
    *   定义了一套涵盖光照、阴影、材质、几何、传感器伪影等维度的**真实感本体**。
    *   利用冻结的 CLIP 图像编码器提取特征，通过多头 MLP 分类器预测图像中各项真实感特征的概率 $p_i$。
*   **知识图谱与图神经网络（KG & GNN）**：
    *   构建静态**真实感知识图谱**，编码特征间的因果或感知关系（如“非均匀光照”支持“投射阴影”）。
    *   使用 **GraphSAGE** 传播特征概率，生成结构化嵌入 $G$。训练目标是最小化符号一致性损失 $L_{GNN}$，使嵌入符合本体定义的逻辑。
*   **符号规划（Symbolic Planning）**：
    *   使用 **PDDL（规划域定义语言）**。将合成图像的特征状态设为初始态，目标真实感配置设为目标态。
    *   通过 **Fast Downward 规划器**计算最小且逻辑一致的视觉编辑序列（如“增加镜头畸变”、“调整材质粗糙度”），并将其转化为结构化文本指令。
*   **本体引导扩散模型（OGD Model）**：
    *   基于 **InstructPix2Pix** 架构。
    *   **双重调节**：图嵌入通过交叉注意力（Cross-Attention）注入 UNet；符号规划生成的指令作为文本引导。
    *   **对齐损失**：引入真实感对齐损失 $L_{KG}$，强制生成图像的图嵌入与目标真实感嵌入匹配。

#### 3. 实验设计
*   **数据集**：
    *   **表示学习**：使用 140 张来自 CARLA、MuJoCo、Isaac Sim 等仿真器及 COCO 等真实世界的**非配对**图像。
    *   **图像翻译**：使用 **Virtual KITTI 2**（合成）与 **KITTI**（真实）的配对数据，以及一个包含 113 对图像的 **LEGO** 数据集。
*   **Benchmark 与对比方法**：
    *   **分类任务对比**：CLIP 特征、端到端 CNN、纯特征 MLP。
    *   **翻译任务对比**：**InstructPix2Pix**（使用静态手动提示）、**ControlNet**（使用深度图作为结构引导）。
*   **评估指标**：特征距离（TraitDist）、感知相似度（LPIPS）、结构相似度（SSIM）、分类准确率（Accuracy）及 ROC-AUC。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量、训练总时长或内存消耗。
*   **实现细节**：提到使用了 Adam 优化器、固定学习率，并冻结了 CLIP 编码器以减少计算负担。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了真实感分类实验（验证嵌入质量）。
    *   在两个不同领域（自动驾驶 KITTI 和物体级 LEGO）进行了 Sim2Real 翻译实验。
    *   包含完整的**消融实验**：分别验证了“无知识图谱”、“无符号规划”和“无对齐损失”对结果的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从底层特征判别到高层图像生成的全过程。通过仅用 140 张非配对图像即可训练出高效的分类器，证明了其**数据高效性**。对比实验选择了当前最主流的扩散编辑模型，具有较强的说服力。

#### 6. 主要结论与发现
*   **结构化胜过统计对齐**：显式建模真实感特征间的因果关系，比单纯依赖扩散模型的隐式学习能产生更符合物理逻辑的图像。
*   **判别力强**：基于 GNN 的真实感嵌入在区分真/假图像上的准确率（98.4%）远高于原始 CLIP 特征（59.6%）。
*   **零样本泛化**：OGD 在推理时无需目标域的配对数据，仅凭符号规划即可实现高质量迁移，且在 LPIPS 和 SSIM 指标上均优于 SOTA 基准。

#### 7. 优点（亮点）
*   **神经符号结合**：将硬逻辑（PDDL 规划）与软学习（扩散模型）结合，解决了生成模型“幻觉”和逻辑不一致的问题。
*   **可解释性**：用户可以通过查看 PDDL 生成的编辑序列，明确知道模型为了提升真实感做了哪些具体改动。
*   **极高的数据效率**：在训练特征预测器时仅需极少量标注，且推理过程是零样本的，非常适合数据获取成本高的机器人领域。

#### 8. 不足与局限
*   **本体依赖性**：真实感本体和知识图谱是手动定义的（Static Ontology）。如果某些特定领域的真实感因素未被包含在本体中，模型可能无法有效处理。
*   **VLM 标注风险**：训练特征预测器依赖 GPT-4o 等 VLM 生成伪标签，若 VLM 对某些细微视觉特征判断失误，会引入噪声。
*   **计算复杂度**：虽然推理是零样本的，但在推理过程中增加了特征提取、图传播和符号规划的步骤，可能影响实时性（文中未给出推理延迟数据）。

（完）

---
title: "Ada3Drift: Adaptive Training-Time Drifting for One-Step 3D Visuomotor Robotic Manipulation"
title_zh: Ada3Drift：面向单步 3D 视觉运动机器人操作的自适应训练时漂移
authors: "Chongyang Xu, Yixian Zou, Ziliang Feng, Fanman Meng, Shuaicheng Liu"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11984v1"
tags: ["query:q2"]
score: 9.0
evidence: 单步3D视觉运动机器人操作策略
tldr: 针对扩散模型在机器人视觉运动策略中推理延迟高的问题，本文提出Ada3Drift。该方法通过将迭代优化从推理阶段转移到训练阶段，利用训练时漂移场在单步生成中保留多模态动作分布。结合S形调度损失和多尺度场聚合，Ada3Drift在保持高保真度的同时，将推理速度提升了10倍，在多个仿真和真实任务中达到SOTA性能。
selection_source: fresh_fetch
motivation: 现有的单步生成方法在降低推理延迟时容易导致多模态动作分布坍缩，难以在复杂机器人任务中保持动作的物理可行性。
method: 提出一种训练时漂移场方法，通过吸引专家模式并排斥生成样本，并结合S形调度损失和多尺度场聚合来捕捉不同粒度的动作模式。
result: 在多个仿真基准和真实世界任务中，Ada3Drift在仅需单步推理的情况下，实现了优于扩散模型的效果且推理效率提升了10倍。
conclusion: Ada3Drift证明了通过将计算负担转移至训练阶段的漂移场优化，可以有效解决单步生成中的多模态保真度损失问题。
---

## 摘要
基于扩散的视觉运动策略通过迭代去噪有效地捕获多模态动作分布，但其高推理延迟限制了实时机器人控制。最近的流匹配和基于一致性的方法虽然实现了单步生成，但牺牲了保留不同动作模式的能力，导致多模态行为坍缩为平均的、通常在物理上不可行的轨迹。我们观察到，机器人领域中计算预算的不对称性（离线训练与实时推理）自然地促使我们通过将迭代细化从推理阶段转移到训练阶段，来恢复这种多模态保真度。基于这一见解，我们提出了 Ada3Drift，它学习一个训练时漂移场，该场将预测动作吸引至专家演示模式，同时排斥其他生成的样本，从而能够基于 3D 点云观测实现高保真单步生成（1 NFE）。为了应对少样本机器人场景，Ada3Drift 进一步引入了从粗略分布学习到模式锐化细化的 Sigmoid 调度损失转换，以及能够捕获不同空间粒度动作模式的多尺度场聚合。在三个模拟基准测试（Adroit、Meta-World 和 RoboTwin）以及真实世界机器人操作任务上的实验表明，Ada3Drift 达到了最先进的性能，同时所需的函数评估次数比基于扩散的替代方案少 10 倍。

## Abstract
Diffusion-based visuomotor policies effectively capture multimodal action distributions through iterative denoising, but their high inference latency limits real-time robotic control. Recent flow matching and consistency-based methods achieve single-step generation, yet sacrifice the ability to preserve distinct action modes, collapsing multimodal behaviors into averaged, often physically infeasible trajectories. We observe that the compute budget asymmetry in robotics (offline training vs.\ real-time inference) naturally motivates recovering this multimodal fidelity by shifting iterative refinement from inference time to training time. Building on this insight, we propose Ada3Drift, which learns a training-time drifting field that attracts predicted actions toward expert demonstration modes while repelling them from other generated samples, enabling high-fidelity single-step generation (1 NFE) from 3D point cloud observations. To handle the few-shot robotic regime, Ada3Drift further introduces a sigmoid-scheduled loss transition from coarse distribution learning to mode-sharpening refinement, and multi-scale field aggregation that captures action modes at varying spatial granularities. Experiments on three simulation benchmarks (Adroit, Meta-World, and RoboTwin) and real-world robotic manipulation tasks demonstrate that Ada3Drift achieves state-of-the-art performance while requiring $10\times$ fewer function evaluations than diffusion-based alternatives.

---

## 论文详细总结（自动生成）

以下是对论文《Ada3Drift: Adaptive Training-Time Drifting for One-Step 3D Visuomotor Robotic Manipulation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：基于扩散模型（Diffusion-based）的视觉运动策略虽然能很好地处理多模态动作分布（即同一场景下有多种有效操作路径），但其推理时的多次迭代去噪（通常需 10-100 次函数评估，NFE）导致高延迟，难以满足机器人实时控制（10-50Hz）的需求。
*   **研究动机**：现有的单步生成方法（如流匹配 Flow Matching）在加速的同时往往会产生“模式平均（Mode Averaging）”问题，将多个有效的动作模式坍缩为一个物理上不可行的平均轨迹（例如：避障时左右均可，平均后会撞上障碍物）。
*   **核心见解**：利用机器人系统“计算预算不对称性”（离线训练资源充足，在线推理要求极高），将迭代优化过程从推理阶段转移到训练阶段。

### 2. 方法论
Ada3Drift 的核心思想是学习一个**训练时漂移场（Training-Time Drifting Field）**，在训练过程中显式地引导模型输出。
*   **训练时漂移场**：计算预测动作与专家演示之间的双向亲和力矩阵，产生一个位移向量 $V(\hat{x})$。该向量包含两个作用：
    *   **吸引**：将预测动作拉向最近的专家演示模式。
    *   **排斥**：在生成的样本之间产生排斥力，以维持模式的多样性，防止坍缩。
*   **多尺度场聚合（Multi-scale Aggregation）**：为了适应不同任务的几何特征（有的动作紧凑，有的跨度大），在多个温度参数（$\tau \in \{0.02, 0.05, 0.2\}$）下计算漂移场并聚合，实现自适应的空间粒度捕获。
*   **Sigmoid 调度损失转换**：针对少样本（Few-shot）场景，设计了两阶段训练：
    *   前期：由 MSE 损失主导，学习动作分布的粗略结构。
    *   后期：通过 Sigmoid 函数平滑切换到漂移损失主导，进行模式锐化（Mode Sharpening）。
*   **架构优化**：采用 3D 点云编码器（PointNet）提取特征，并使用无时间步（Timestep-free）的 1D U-Net 生成动作序列，去除了扩散模型中不必要的时间步嵌入，简化了推理流程。

### 3. 实验设计
*   **数据集/场景**：
    *   **Adroit**：涉及 24 自由度灵巧手的复杂操作任务（Hammer, Door, Pen）。
    *   **Meta-World**：包含 34 个不同难度的机械臂操作任务。
    *   **RoboTwin**：包含 8 个需要精确 3D 空间推理的仿真任务。
    *   **真实世界**：在 Agilx Cobot Magic 机器人上进行了 5 项任务（叠盘子、放杯子、关抽屉等）。
*   **对比方法**：
    *   **多步方法**：DP3（10 NFE）、Simple DP3。
    *   **单步方法**：FlowPolicy、MP1、Consistency Policy (CP)、AdaFlow。
    *   **图像基准**：Diffusion Policy (DP)。

### 4. 资源与算力
*   **硬件**：实验明确提到使用单张 **NVIDIA RTX 4090D** GPU 进行训练和推理。
*   **训练细节**：使用 AdamW 优化器，学习率为 $10^{-4}$，Batch Size 为 128。每个任务仅需 10-20 个专家演示视频。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从灵巧手到机械臂、从仿真到真实的广泛场景，总计测试了超过 40 个不同的任务。
*   **消融实验**：对比了“朴素漂移”与“自适应漂移”的效果，验证了多尺度聚合和 Sigmoid 调度的必要性。
*   **客观性**：所有 3D 基准测试均在相同硬件环境下复现，并报告了 3 个随机种子的平均成功率及标准差，实验设计较为公平、充分。

### 6. 主要结论与发现
*   **性能领先**：Ada3Drift 在单步生成方法中达到了 SOTA 性能，平均成功率（79.2%）甚至超过了需要 10 步迭代的 DP3（78.0%）。
*   **推理效率**：实现了约 **233 Hz** 的推理频率，比 DP3 快了 **12.5 倍**，完全满足高频实时控制需求。
*   **模式保持**：定性分析显示，Ada3Drift 生成的轨迹比其他单步方法更果断、平滑，有效解决了模式平均导致的抖动和碰撞问题。

### 7. 优点
*   **计算转移策略**：巧妙地利用离线训练时间换取在线推理速度，符合工业机器人部署的实际逻辑。
*   **自适应性强**：多尺度聚合和 Sigmoid 调度解决了少样本学习中漂移场难以初始化的痛点。
*   **3D 感知集成**：直接在点云表征上操作，比 2D 图像方法在处理遮挡和深度信息时更鲁棒。

### 8. 不足与局限
*   **超参数敏感性**：Sigmoid 切换点（0.7E）在极高难度任务中可能仍需微调以获得最佳效果。
*   **模型复杂度**：虽然是单步生成，但其 U-Net 骨干网络比一些极简的 MLP 方案略重，虽然在 4090 上很快，但在嵌入式边缘设备上的表现未提及。
*   **依赖专家数据**：作为模仿学习方法，其上限受限于专家演示的质量和多样性。

（完）

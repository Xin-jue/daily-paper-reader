---
title: "VolumeDP: Modeling Volumetric Representation for Manipulation Policy Learning"
title_zh: VolumeDP：为操作策略学习建模体素表示
authors: "Tianxing Zhou, Feiyang Xue, Zhangchen Ye, Tianyuan Yuan, Hang Zhao, Tao Jiang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17720v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于操作策略学习的体积表示
tldr: 针对视觉模仿学习中2D图像观测与3D动作输出不匹配导致的建模难题，本文提出VolumeDP架构。该方法通过交叉注意力机制将图像特征提升为3D体素表示，并利用可学习模块筛选任务相关的空间Token，最后通过多Token解码器预测动作。VolumeDP在LIBERO等多个基准测试中刷新了SOTA记录，并在真实世界中展现出极强的空间泛化能力和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的视觉模仿学习方法直接将2D图像映射到3D动作，这种维度不匹配限制了机器人的空间推理能力和鲁棒性。
method: 提出一种基于体素表示的策略架构，通过将图像特征提升至3D空间并筛选关键空间Token，实现高效且保真的动作预测。
result: "在LIBERO仿真基准上达到了88.8%的成功率，比最强基准提升了14.8%，并在真实世界实验中表现出优异的泛化性。"
conclusion: VolumeDP证明了显式3D空间建模在提升机器人操纵策略的精确度、计算效率及环境适应性方面的巨大潜力。
---

## 摘要
模仿学习是机器人操作中的一种重要范式。然而，现有的视觉模仿方法直接将 2D 图像观测映射到 3D 动作输出，造成了 2D-3D 不匹配，从而阻碍了空间推理并降低了鲁棒性。我们提出了 VolumeDP，这是一种通过显式 3D 推理来恢复空间对齐的策略架构。VolumeDP 首先通过交叉注意力机制将图像特征提升为体素表示（Volumetric Representation）。随后，它利用一个可学习模块选择与任务相关的体素，并将其转换为一组紧凑的空间 token，在保留对动作至关重要的几何信息的同时，显著减少了计算量。最后，多 token 解码器以整个 token 集合为条件来预测动作，从而避免了将多个空间 token 压缩为单个描述符的有损聚合。VolumeDP 在 LIBERO 仿真基准测试中实现了 88.8% 的最先进平均成功率，比最强的基准模型提升了 14.8%。在 ManiSkill 和 LIBERO-Plus 基准测试中，它也比之前的方法取得了巨大的性能提升。现实世界实验进一步证明了其更高的成功率，以及对新颖空间布局、相机视角和环境背景的鲁棒泛化能力。代码将开源。

## Abstract
Imitation learning is a prominent paradigm for robotic manipulation. However, existing visual imitation methods map 2D image observations directly to 3D action outputs, imposing a 2D-3D mismatch that hinders spatial reasoning and degrades robustness. We present VolumeDP, a policy architecture that restores spatial alignment by explicitly reasoning in 3D. VolumeDP first lifts image features into a Volumetric Representation via cross-attention. It then selects task-relevant voxels with a learnable module and converts them into a compact set of spatial tokens, markedly reducing computation while preserving action-critical geometry. Finally, a multi-token decoder conditions on the entire token set to predict actions, thereby avoiding lossy aggregation that collapses multiple spatial tokens into a single descriptor. VolumeDP achieves a state-of-the-art average success rate of 88.8% on the LIBERO simulation benchmark, outperforming the strongest baseline by a substantial 14.8% improvement. It also delivers large performance gains over prior methods on the ManiSkill and LIBERO-Plus benchmarks. Real-world experiments further demonstrate higher success rates and robust generalization to novel spatial layouts, camera viewpoints, and environment backgrounds. Code will be released.

---

## 论文详细总结（自动生成）

以下是对论文《VolumeDP: Modeling Volumetric Representation for Manipulation Policy Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉模仿学习（IL）方法大多直接将2D图像观测映射到3D动作输出。这种**2D-3D维度不匹配**限制了机器人的空间推理能力，导致模型在面对视角变化、空间布局调整时鲁棒性较差。
*   **研究背景**：虽然引入深度信息（如点云）可以缓解此问题，但深度传感器成本高且在某些环境下不可靠。因此，如何在仅使用**RGB相机**的前提下，构建显式的3D空间表示并实现高效的动作预测，是本文的研究重点。

### 2. 提出的方法论
VolumeDP 包含三个核心模块，旨在通过显式的3D推理恢复空间对齐：
*   **体素表示模块（Volumetric Representation）**：
    *   首先使用共享骨干网络提取多视角图像特征。
    *   预定义一个覆盖工作空间的3D体素网格，通过相机内外参将体素投影至图像平面。
    *   采用**可变形交叉注意力（Deformable Cross-Attention）**机制，将2D图像特征“提升”为3D体素特征，捕捉精细的几何和语义信息。
*   **空间Token生成模块（Spatial Token Generation）**：
    *   由于3D体素中大部分是空空间，该模块利用文本指令和体素特征生成权重，筛选出与任务最相关的区域（如末端执行器和目标物体）。
    *   将稠密的体素特征压缩为一组紧凑的**空间Token**（如200个），并引入本体感受（末端执行器位置）作为辅助监督信号，加速收敛。
*   **多Token解码器（Multi-Token Decoder）**：
    *   基于扩散策略（Diffusion Policy）框架，采用Transformer架构（DiT-Block）。
    *   解码器直接以全量空间Token、文本Token和本体感受Token为条件进行去噪，避免了将空间信息压缩为单一向量带来的信息损失。

### 3. 实验设计
*   **仿真基准**：
    *   **LIBERO**：包含Spatial、Object、Goal、Long四个子集，共40个任务。
    *   **ManiSkill**：选取了Poke Cube、Insert Peg等5个涉及高精度操作的任务。
    *   **LIBERO-Plus**：专门用于评估分布外（OOD）泛化性，涵盖视角、光照、背景、布局等扰动。
*   **对比方法**：包括经典的BC-Trans、BC-VILT，以及强力的扩散策略基准 **Diffusion Policy (DP)** 和 **DiT-Block Policy**。
*   **真实世界实验**：在Galaxea R1 Lite机器人上进行了拨碗、微波炉操作、开门、拧螺母四项任务，验证其在复杂交互和高精度需求下的表现。

### 4. 资源与算力
*   **论文提及情况**：文中**未明确说明**具体的GPU型号、数量及总训练时长。
*   **相关参数**：图像分辨率为256x256，体素网格大小为40x40x40，扩散过程包含100个去噪步骤。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了三大仿真基准和四项真实世界任务，每项实验均采用三个随机种子取平均值，具有较好的统计学意义。
*   **消融实验**：针对体素表示的必要性、Token生成策略（随机、全量、基于深度图等）、解码器结构（拼接、卷积压缩等）进行了详尽的对比。
*   **公平性**：所有对比方法在相同的输入分辨率、动作序列长度和去噪步数下进行，评估标准统一（成功率 SR），实验设计客观公平。

### 6. 主要结论与发现
*   **性能领先**：VolumeDP在LIBERO上达到88.8%的成功率，比最强基准提升了**14.8%**；在ManiSkill上提升了**19.3%**。
*   **泛化性极强**：在LIBERO-Plus的OOD测试中，VolumeDP在视角变化、背景干扰等维度上显著优于2D方法，平均领先基准**21.3%**。
*   **3D表示的价值**：消融实验证明，显式的3D体素表示是提升空间推理能力的关键，而多Token解码器能更有效地利用这些结构化信息。

### 7. 优点（亮点）
*   **RGB-only实现3D推理**：无需昂贵的深度传感器，仅靠RGB图像即可构建具有空间一致性的表示。
*   **高效的Token化策略**：通过可学习的筛选机制解决了3D数据计算量大的问题，同时保留了关键的几何特征。
*   **强大的鲁棒性**：通过体素化建模，天然具备了一定的视角不变性和环境抗干扰能力。

### 8. 不足与局限
*   **体素范围限制**：体素空间（Workspace Bounds）需要根据任务手动预定义或调整，对于超大范围或移动底座任务的适应性有待验证。
*   **计算开销**：虽然进行了Token压缩，但相比纯2D方法，构建3D体素和执行交叉注意力的计算成本仍然更高。
*   **实时性说明不足**：文中未详细讨论推理延迟，这对于高频闭环控制至关重要。

（完）

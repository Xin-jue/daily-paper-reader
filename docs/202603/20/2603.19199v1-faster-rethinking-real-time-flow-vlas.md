---
title: "FASTER: Rethinking Real-Time Flow VLAs"
title_zh: FASTER：重新思考实时流式 VLA 模型
authors: "Yuxiang Lu, Zhe Liu, Xianzhe Fan, Zhenya Yang, Jinghua Hou, Junyi Li, Kaixin Ding, Hengshuang Zhao"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19199v1"
tags: ["query:q12"]
score: 10.0
evidence: 优化基于流的视觉-语言-动作（VLA）模型的实时延迟
tldr: 针对视觉-语言-动作（VLA）模型在物理世界部署中反应延迟高的问题，本文提出了FASTER框架。通过重新审视动作分块策略中的反应机制，研究发现传统流式VLA的恒定采样调度是延迟瓶颈。FASTER引入了视界感知调度，优先处理近期动作采样，将初始反应的去噪步骤压缩至一步，并结合流式客户端-服务器架构，在保持轨迹质量的同时显著提升了机器人在动态任务（如乒乓球）中的实时响应速度。
selection_source: fresh_fetch
motivation: 现有的异步推理方法侧重于轨迹平滑度，却忽视了流式VLA模型在应对环境变化时因采样步骤过多导致的反应延迟问题。
method: 提出了FASTER框架，通过视界感知调度自适应地优先采样近期动作，并将即时反应的去噪过程压缩为单步执行。
result: 在消费级GPU上显著降低了有效反应延迟，并在高动态的乒乓球等现实任务中实现了前所未有的实时响应能力。
conclusion: FASTER通过优化采样调度和推理管线，成功解决了通用策略在实时机器人控制中的延迟瓶颈，实现了精准且平滑的动作生成。
---

## 摘要
实时执行对于在物理世界中部署视觉-语言-动作（VLA）模型至关重要。现有的异步推理方法主要优化轨迹平滑度，但忽略了对环境变化做出反应的关键延迟。通过重新思考动作分块（action chunking）策略中的反应概念，本文对决定反应时间的因素进行了系统分析。我们表明，反应时间遵循由首个动作时间（TTFA）和执行步长（execution horizon）共同决定的均匀分布。此外，我们揭示了在基于流（flow-based）的 VLA 中采用恒定调度（constant schedule）的标准做法可能效率低下，并迫使系统在任何动作开始前完成所有采样步骤，从而构成了反应延迟的瓶颈。为了克服这一问题，我们提出了用于即时反应的快速动作采样（FASTER）。通过引入视界感知调度（Horizon-Aware Schedule），FASTER 在流采样过程中自适应地优先处理近期动作，将即时反应的去噪过程压缩了十倍（例如在 $π_{0.5}$ 和 X-VLA 中），缩减至单步，同时保持了长时轨迹的质量。结合流式客户端-服务器流水线，FASTER 显著降低了真实机器人上的有效反应延迟，尤其是在部署于消费级 GPU 时。包括高动态乒乓球任务在内的真实世界实验证明，FASTER 为通用策略解锁了前所未有的实时响应能力，能够快速生成准确且平滑的轨迹。

## Abstract
Real-time execution is crucial for deploying Vision-Language-Action (VLA) models in the physical world. Existing asynchronous inference methods primarily optimize trajectory smoothness, but neglect the critical latency in reacting to environmental changes. By rethinking the notion of reaction in action chunking policies, this paper presents a systematic analysis of the factors governing reaction time. We show that reaction time follows a uniform distribution determined jointly by the Time to First Action (TTFA) and the execution horizon. Moreover, we reveal that the standard practice of applying a constant schedule in flow-based VLAs can be inefficient and forces the system to complete all sampling steps before any movement can start, forming the bottleneck in reaction latency. To overcome this issue, we propose Fast Action Sampling for ImmediaTE Reaction (FASTER). By introducing a Horizon-Aware Schedule, FASTER adaptively prioritizes near-term actions during flow sampling, compressing the denoising of the immediate reaction by tenfold (e.g., in $π_{0.5}$ and X-VLA) into a single step, while preserving the quality of long-horizon trajectory. Coupled with a streaming client-server pipeline, FASTER substantially reduces the effective reaction latency on real robots, especially when deployed on consumer-grade GPUs. Real-world experiments, including a highly dynamic table tennis task, prove that FASTER unlocks unprecedented real-time responsiveness for generalist policies, enabling rapid generation of accurate and smooth trajectories.

---

## 论文详细总结（自动生成）

### FASTER: 重新思考实时流式 VLA 模型 —— 论文深度总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在物理世界部署时面临严重的**反应延迟（Reaction Latency）**瓶颈。
*   **研究背景**：目前的 VLA 模型多采用“动作分块（Action Chunking）”和“流匹配（Flow Matching）”技术。虽然异步推理解决了动作间的停顿（平滑度问题），但由于流匹配通常需要多步迭代（如 10 步去噪）才能生成动作，导致机器人从观察到做出第一个动作的时间（TTFA）过长。
*   **研究动机**：作者发现，动作序列中近期的动作比远期的动作更容易预测且路径更直。因此，强制所有动作共享相同的采样步数是不必要的，这构成了实时响应的瓶颈。

#### 2. 论文提出的方法论：FASTER
*   **核心思想**：通过**视界感知调度（Horizon-Aware Schedule, HAS）**，为动作块中不同位置的动作分配不同的采样进度，让即时动作在极少步数（甚至 1 步）内完成去噪，而远期动作继续细化。
*   **关键技术细节**：
    *   **TTFA 指标**：引入“首个动作时间（Time to First Action）”作为衡量反应速度的核心指标。
    *   **HAS 调度公式**：定义每个动作索引 $i$ 的“命中时间（Hit Time）” $u_i$。近期的动作 $u_i$ 较大（接近 1），意味着它们在采样初期就达到去噪终点；远期动作 $u_i$ 较小，需经历完整迭代。
    *   **混合调度微调（Mixed Schedule Fine-tuning）**：在训练时以一定概率 $p$ 混合使用 HAS 和传统的恒定调度，确保模型在不改变架构的情况下适应非均匀采样。
    *   **流式客户端-服务器接口**：服务器每完成一步去噪，若有动作达到终点则立即流式传回客户端执行，无需等待整个动作块生成完毕。
    *   **早停策略（Early Stopping）**：一旦执行视界内的动作全部生成，立即停止后续采样，进一步提高推理频率。

#### 3. 实验设计
*   **数据集与场景**：
    *   **现实世界任务**：高动态的“乒乓球（Table Tennis）”任务（极度依赖反应速度）、“抓取饮料（Pick Beverage）”和“折叠毛巾（Fold Towel）”。
    *   **仿真基准**：LIBERO（考察知识迁移）、CALVIN（考察长程操作）和 Kinetix（考察力控动态任务）。
*   **对比方法（Baselines）**：
    *   同步推理（Sync）。
    *   朴素异步推理（Naive Async）。
    *   SOTA 方法：Training-time RTC（侧重平滑度的动作调节技术）。
*   **硬件平台**：对比了高性能 GPU（RTX 4090）和消费级 GPU（RTX 4060）上的表现。

#### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA A800 GPU** 进行模型微调。
*   **训练时长/规模**：针对 $\pi_{0.5}$ 和 X-VLA 模型，在不同任务上进行了 30k 到 60k 步的微调。
*   **推理算力**：在 RTX 4090 和 RTX 4060 上进行部署测试，验证了在低算力设备上的加速效果。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **现实世界**：乒乓球任务 15 次试验，抓取任务 35 次，折叠任务 10 次。
    *   **仿真**：LIBERO 每个任务 50 次试验（共 40 个任务）；CALVIN 评估了 1000 条指令链。
    *   **消融实验**：针对调度因子 $\alpha$、混合概率 $p$ 进行了详细测试。
*   **充分性评价**：实验设计较为充分，涵盖了从高动态到精细操作的不同维度，且在不同等级的 GPU 上验证了泛化性。通过概率分布模型分析反应时间，比单一均值更客观地反映了物理世界的随机性。

#### 6. 论文的主要结论与发现
*   **加速效果显著**：FASTER 将即时反应的采样压缩了 **10 倍**，在 X-VLA 模型上 TTFA 缩短了约 3 倍。
*   **解锁动态能力**：在 RTX 4060 这种低算力平台上，FASTER 是唯一能让机器人成功回击乒乓球的方法（Sync 和其他 Async 方法因延迟过高而失败）。
*   **性能保持**：在大幅加速的同时，FASTER 在 LIBERO 和 CALVIN 等标准仿真测试中仅有极微小的性能损失，证明了 HAS 调度的有效性。
*   **反应时间分布**：揭示了反应时间并非恒定值，而是受推理延迟和执行频率共同影响的均匀分布。

#### 7. 优点
*   **即插即用（Plug-and-play）**：无需修改 VLA 模型架构，无需额外的训练成本（仅需在标准微调中加入 HAS）。
*   **视角独特**：首次明确区分了“执行平滑度”与“反应灵敏度”，并针对性地解决了后者。
*   **系统级优化**：不仅有算法改进，还配套设计了流式通信和早停机制，形成了完整的实时部署方案。

#### 8. 不足与局限
*   **长程精度权衡**：在极其复杂的长程任务中，过于激进的采样（如 $\alpha$ 过小）可能会导致远期轨迹质量轻微下降。
*   **任务覆盖**：虽然涵盖了乒乓球等动态任务，但对于需要极高精度的微小物体操作，单步采样是否能保持足够的空间精度仍有待更广泛的验证。
*   **依赖微调**：虽然不需要额外架构，但仍需要针对特定任务进行微调以适应 HAS 调度，对于完全零样本（Zero-shot）推理的支持尚不明确。

（完）

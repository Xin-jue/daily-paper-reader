---
title: Sparse Autoencoders Reveal Interpretable and Steerable Features in VLA Models
title_zh: 稀疏自编码器揭示了 VLA 模型中具有可解释性且可引导的特征
authors: "Aiden Swann, Lachlain McGranahan, Hugo Buurmeijer, Monroe Kennedy, Mac Schwager"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19183v1"
tags: ["query:q12"]
score: 9.0
evidence: 机器人操作中VLA模型的可解释性技术
tldr: 本研究利用机械可解释性技术，通过在视觉-语言-动作（VLA）模型的隐藏层激活上训练稀疏自编码器（SAEs），揭示了其内部表征。研究发现，虽然大部分特征对应于特定演示的记忆，但仍存在可解释且可操控的通用运动原语。通过在LIBERO基准上进行干预实验，证明了这些特征能因果性地影响机器人行为，并揭示了训练数据规模对特征泛化性的影响，为提升VLA模型的泛化能力提供了新见解。
selection_source: fresh_fetch
motivation: 旨在探究VLA模型在机器人操控任务中泛化表现不一致的深层原因，区分模型是学习了通用技能还是仅依赖于对训练数据的记忆。
method: 通过在VLA模型隐藏层训练稀疏自编码器（SAEs）提取特征，并结合特征干预实验和量化指标来评估特征的可解释性与泛化性。
result: 发现SAE能提取出可操控的通用运动原语，且证明了在大型多样化数据集上训练或采用知识隔离技术能有效减少模型对特定任务的死记硬背。
conclusion: 该研究首次为VLA模型具备跨任务泛化特征提供了机械论证据，并开源了用于激活收集、SAE训练和特征干预的工具包。
---

## 摘要
视觉-语言-动作（VLA）模型已成为通用机器人操控的一种极具前景的方法。然而，它们的泛化能力并不一致：虽然这些模型在某些设置下表现出色，但经过微调的变体往往在面对新物体、新场景和新指令时失效。我们应用机械可解释性（mechanistic interpretability）技术来更好地理解 VLA 模型的内部工作机制。为了探测内部表示，我们在 VLA 的隐藏层激活上训练了稀疏自编码器（SAE）。SAE 学习一个稀疏字典，其特征作为模型计算的紧凑且可解释的基。我们发现，提取的大多数 SAE 特征对应于特定训练演示中的记忆序列。然而，一些特征对应于可解释、通用且可引导的运动基元和语义属性，为 VLA 的泛化能力提供了一个充满希望的视角。我们提出了一种指标，根据特征是代表可泛化的可迁移基元还是特定情节的记忆来对其进行分类。我们通过在 LIBERO 基准测试上的引导实验验证了这些发现。我们展示了单个 SAE 特征会因果性地影响机器人行为。引导通用特征会诱发出与其语义含义一致的行为，并可应用于跨任务和跨场景。这项工作提供了第一个机械论证据，证明 VLA 可以学习跨任务和场景的可泛化特征。我们观察到，在小型机器人数据集上进行监督微调会不成比例地放大记忆效应。相比之下，在更大、更多样化的数据集（如 DROID）上训练或使用知识隔离会促进更通用特征的产生。我们提供了一个开源代码库和用户友好的界面，用于激活收集、SAE 训练和特征引导。我们的项目页面位于 http://drvla.github.io。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising approach for general-purpose robot manipulation. However, their generalization is inconsistent: while these models can perform impressively in some settings, fine-tuned variants often fail on novel objects, scenes, and instructions. We apply mechanistic interpretability techniques to better understand the inner workings of VLA models. To probe internal representations, we train Sparse Autoencoders (SAEs) on hidden layer activations of the VLA. SAEs learn a sparse dictionary whose features act as a compact, interpretable basis for the model's computation. We find that the large majority of extracted SAE features correspond to memorized sequences from specific training demonstrations. However, some features correspond to interpretable, general, and steerable motion primitives and semantic properties, offering a promising glimpse toward VLA generalizability. We propose a metric to categorize features according to whether they represent generalizable transferable primitives or episode-specific memorization. We validate these findings through steering experiments on the LIBERO benchmark. We show that individual SAE features causally influence robot behavior. Steering general features induces behaviors consistent with their semantic meaning and can be applied across tasks and scenes. This work provides the first mechanistic evidence that VLAs can learn generalizable features across tasks and scenes. We observe that supervised fine-tuning on small robotics datasets disproportionately amplifies memorization. In contrast, training on larger, more diverse datasets (e.g., DROID) or using knowledge insulation promotes more general features. We provide an open-source codebase and user-friendly interface for activation collection, SAE training, and feature steering. Our project page is located at http://drvla.github.io

---

## 论文详细总结（自动生成）

这是一份关于论文《Sparse Autoencoders Reveal Interpretable and Steerable Features in VLA Models》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型虽然在机器人操控中表现出色，但其泛化性极不稳定。研究者试图弄清楚：VLA 模型究竟是学习了通用的运动技能（如“抓取”概念），还是仅仅死记硬背（Memorization）了训练集中的特定动作序列？
*   **研究背景**：目前的 VLA 模型在微调后往往会失去语言遵循能力，且对环境扰动极其敏感。由于缺乏内部机制的理解，开发者难以诊断模型失效的原因。本文引入了**机械可解释性（Mechanistic Interpretability）**工具，试图打开 VLA 的“黑盒”。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：利用**稀疏自编码器（SAE）**将 VLA 模型高维、叠加的神经激活分解为稀疏、具有单一语义的“特征”（Features）。
*   **关键技术细节**：
    *   **SAE 架构**：采用 TopK 架构配合 AuxK 辅助损失函数，有效解决了特征“死亡”（Dead Latents）问题，并能精确控制特征稀疏度。
    *   **激活收集**：在 $\pi_{0.5}$（基于 PaliGemma）和 OpenVLA 模型的残差流（Residual Stream）中提取激活。为了计算效率，主要对每一步的 Token 进行了均值池化处理。
    *   **特征分类指标**：提出了四个量化指标来区分“通用特征”与“记忆特征”：
        1.  **情节覆盖率（Episode Coverage）**：特征在多少比例的训练情节中激活。
        2.  **平均起始计数（Mean Onset Count）**：特征在单个情节中“爆发”激活的次数（通用特征通常呈爆发式）。
        3.  **平均激活强度（Mean Activation Magnitude）**。
        4.  **相对运行长度（Relative Run Length）**：特征激活持续时间占情节总长的比例（记忆特征通常持续激活）。
    *   **特征引导（Steering）**：在推理时，将特定 SAE 特征的解码向量按比例加回模型激活中，从而因果性地改变机器人的行为。

### 3. 实验设计
*   **模型对象**：$\pi_{0.5}$（包含 PaliGemma 视觉语言骨干和动作专家网络）和 OpenVLA（基于 Llama 2）。
*   **数据集与 Benchmark**：
    *   **LIBERO**：模拟环境下的机器人操控基准，用于闭环引导实验。
    *   **DROID**：大规模真实世界机器人数据集，用于验证特征在多样化场景下的泛化性。
*   **对比维度**：对比了监督微调（SFT）与知识隔离（Knowledge Insulation, KI）技术对特征泛化性的影响；对比了不同规模数据集（LIBERO vs. DROID）产生的特征差异。

### 4. 资源与算力
*   **算力平台**：论文提到使用了斯坦福大学的 **Marlowe 集群**（基于 GPU 的计算设备）。
*   **具体细节**：文中未明确给出训练 SAE 所需的具体 GPU 型号数量或总时长，但提供了训练超参数（如 Batch Size 为 4096，训练 100 个 Epoch，SAE 潜在空间维度与输入维度一致）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 120 个随机抽样的特征进行了人工标注和可解释性评估。
    *   在 8 个不同的模型层（PaliGemma 层和动作专家层）上训练了 SAE。
    *   进行了多随机种子的消融实验以验证特征的稳定性。
    *   在 LIBERO 环境中进行了多项闭环引导实验（如引导“预抓取对齐”特征）。
*   **充分性评价**：实验设计较为全面，涵盖了模拟与现实数据集、两种主流 VLA 架构，并结合了定性观察（可视化）与定量分析（分类器指标），实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **记忆 vs. 泛化**：VLA 模型中绝大多数特征（>90%）属于对特定情节的**死记硬背**，但确实存在少数**通用的运动原语**（如抓取、运输、任务完成信号）。
*   **微调的影响**：在小型数据集上进行标准微调会显著放大记忆效应；而使用**知识隔离（KI）**技术或在更大规模的数据集（如 DROID）上训练，能显著提升通用特征的比例。
*   **因果可控性**：通过引导 SAE 特征可以精确控制机器人行为（例如，增强“预抓取”特征会让机器人悬停在物体上方而不执行抓取），证明了这些特征具有功能性意义。

### 7. 优点与亮点
*   **首创性**：这是第一篇为 VLA 模型具备跨任务泛化特征提供机械论证据的论文。
*   **量化工具**：提出了一套无需运行策略回放即可评估特征泛化性的自动化指标。
*   **开源贡献**：提供了名为 **Dr. VLA** 的开源工具包，降低了机器人模型可解释性研究的门槛。

### 8. 不足与局限
*   **引导的不确定性**：并非所有具有清晰语义的特征在引导时都能产生预期的因果效果，这可能与 VLA 内部的非线性交互有关。
*   **Token 处理偏差**：为了节省算力，主要使用了均值池化的 Token，这可能丢失了空间细粒度的信息（尽管附录中尝试了单 Token SAE，但效果尚不成熟）。
*   **硬件验证缺失**：所有的引导实验均在仿真环境（LIBERO）中完成，尚未在真实机器人硬件上验证特征引导的鲁棒性。

（完）

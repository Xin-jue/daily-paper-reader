---
title: "Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance"
title_zh: Fast-dVLA：将离散扩散 VLA 加速至实时性能
authors: "Wenxuan Song, Jiayi Chen, Shuai Chen, Jingbo Wang, Pengxiang Ding, Han Zhao, Yikai Qin, Xinhu Zheng, Donglin Wang, Yan Wang, Haoang Li"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25661v1"
tags: ["query:q12"]
score: 10.0
evidence: 将离散扩散VLA模型加速至实时性能
tldr: 针对预训练VLA模型在标准微调中性能提升有限且计算开销大的挑战，本文提出Fast-dVLA。该方法通过在参数空间解耦辅助任务目标，提取增强通用能力的“能力向量”并将其与预训练参数融合，构建出能力增强的元模型。结合轻量级正交正则化损失，该方法在降低计算开销的同时，实现了与复杂辅助微调相当的性能，有效加速了离散扩散VLA模型的实时应用。
selection_source: fresh_fetch
motivation: 传统的辅助任务微调虽能提升VLA模型性能，但其引入的额外损失函数导致了显著的计算开销，限制了微调效率。
method: 通过在参数空间解耦通用能力增强与任务特定分布拟合，提取能力向量并融合至预训练模型，辅以轻量级正交正则化进行微调。
result: 实验结果显示，该方法在多种机器人任务上均表现出色，在保持高性能的同时显著减少了计算资源消耗。
conclusion: 本研究证明了通过参数空间解耦与模型融合，可以兼顾辅助训练的增强效果与标准微调的简洁性，为高效部署VLA模型提供了新方案。
---

## 摘要
本文提出了一种新颖的方法，旨在解决预训练视觉-语言-动作（VLA）模型在标准监督微调（SFT）过程中往往无法有效提升性能并降低适配成本的挑战。一些带有辅助训练目标的高级微调方法可以提高性能并减少收敛步数，但由于辅助任务带来的额外损失函数计算，它们通常会产生显著的计算开销。为了在保持标准 SFT 简洁性的同时实现辅助训练的增强能力，我们在参数空间内解耦了辅助任务训练的两个目标，即增强通用能力和拟合特定任务的动作分布。为实现这一目标，我们只需使用两种不同的训练策略，使模型在小规模任务集上收敛。由此产生的模型参数差异可以被解释为辅助任务提供的能力向量。随后，这些向量与预训练参数合并，形成一个能力增强的元模型。此外，当标准 SFT 辅以轻量级的正交正则化损失时，合并后的模型在计算开销降低的情况下，达到了与辅助微调基准相当的性能。实验结果表明，该方法在多种机器人任务中均非常有效。项目主页：https://chris1220313648.github.io/Fast-dVLA/

## Abstract
This paper proposes a novel approach to address the challenge that pretrained VLA models often fail to effectively improve performance and reduce adaptation costs during standard supervised finetuning (SFT). Some advanced finetuning methods with auxiliary training objectives can improve performance and reduce the number of convergence steps. However, they typically incur significant computational overhead due to the additional losses from auxiliary tasks. To simultaneously achieve the enhanced capabilities of auxiliary training with the simplicity of standard SFT, we decouple the two objectives of auxiliary task training within the parameter space, namely, enhancing general capabilities and fitting task-specific action distributions. To deliver this goal, we only need to train the model to converge on a small-scale task set using two distinct training strategies. The difference between the resulting model parameters can then be interpreted as capability vectors provided by auxiliary tasks. These vectors are then merged with pretrained parameters to form a capability-enhanced meta model. Moreover, when standard SFT is augmented with a lightweight orthogonal regularization loss, the merged model attains performance comparable to auxiliary finetuned baselines with reduced computational overhead. Experimental results demonstrate that this approach is highly effective across diverse robot tasks. Project page: https://chris1220313648.github.io/Fast-dVLA/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：离散扩散视觉-语言-动作模型（dVLA）虽然在多模态对齐和知识保留方面优于传统的自回归（AR）模型，但其**推理速度极慢**，远低于机器人实时控制所需的 30Hz 频率。
*   **研究动机**：
    *   dVLA 通常采用全双向注意力机制，导致每一轮去噪迭代时 Key-Value (KV) 缓存都会改变，无法像 AR 模型那样复用 KV Cache。
    *   作者观察到，即便使用双向注意力，dVLA 在去噪过程中仍表现出隐式的“从左到右”块状生成倾向。
    *   **目标**：在保留扩散模型并行生成优势的同时，引入块状自回归特性以复用 KV Cache，实现实时推理。

### 2. 方法论
Fast-dVLA 提出了一套结合块扩散（Block Diffusion）与并行解码的框架：
*   **块状注意力（Block-Wise Attention）**：将动作序列划分为多个块（Block）。在块内使用双向注意力，但在块间保持因果性（Causal）。一旦某个块去噪完成，其 KV 状态即固定，后续块可直接复用该缓存。
*   **扩散强制（Diffusion Forcing）**：在训练中为不同位置的块分配单调递增的噪声水平（前块噪声低，后块噪声高）。模型学习在给定前序块部分去噪结果的情况下，并行预测后续块。
*   **异步蒸馏（Asymmetric Distillation）**：为了降低训练成本，不从头训练，而是从预训练的全双向 dVLA（教师）蒸馏到块状注意力模型（学生）。学生模型仅具有受限的因果视野，但需对齐教师模型的全局预测。
*   **流水线并行解码（Pipelined Parallel Decoding）**：推理时设计了“半激活”和“全激活”状态。当一个块的去噪置信度达到阈值时，提前启动下一个块的去噪，实现块间的流水线并行。

### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：评估长程（Long-horizon）语言指令操作。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估任务泛化。
    *   **SimplerEnv**：真实到仿真的评估，使用 WidowX 机器人模型。
    *   **真实世界**：在 AgileX 双臂平台上进行传送带抓取、蔬菜分类和检索任务。
*   **基准模型（Benchmarks）**：
    *   **自回归模型**：OpenVLA, $\pi_0$-FAST。
    *   **扩散/流匹配模型**：$\pi_0$, $\pi_0.5$, DD-VLA, Dream-VLA, UD-VLA。
    *   **加速策略对比**：Fast-dLLM, Block Diffusion。

### 4. 资源与算力
*   **训练细节**：采用 LoRA（Rank=32）进行高效微调。
*   **训练时长**：
    *   Dream-VLA 蒸馏：4k 步（约为原始微调预算的 1/5）。
    *   DD-VLA 蒸馏：4k 步（约为原始预算的 1/8）。
    *   UD-VLA 蒸馏：3k 步。
*   **算力设备**：文中**未明确说明**具体的 GPU 型号和数量，但强调了蒸馏过程比从头训练快 10 倍。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流仿真基准和三类真实世界任务，实验覆盖面广。
*   **消融实验**：针对块大小（Block Size）、激活阈值（$\tau_{add}, \tau_{act}$）、置信度阈值（$\tau_{conf}$）以及蒸馏策略进行了详细消融。
*   **客观性**：对比了当前最先进的 $\pi_0$ 系列模型，并展示了在不同基础架构（Dream-VLA, DD-VLA, UD-VLA）上的通用性，实验设计较为公平、客观。

### 6. 主要结论
*   **速度提升**：相比原始 dVLA，实现了 **2.8x 至 4.1x** 的加速。
*   **实时性**：在真实机器人上达到了 **30Hz** 的执行频率，满足实时控制需求。
*   **性能保持**：在大幅加速的同时，成功率（Success Rate）几乎无损，甚至在某些长程任务中因块状因果约束而略有提升。
*   **训练效率**：异步蒸馏显著降低了适配新任务的计算成本。

### 7. 优点
*   **首创性**：首次将离散扩散 VLA 提升至实时性能水平。
*   **架构优雅**：巧妙地利用了模型隐式的自回归倾向，通过块状注意力平衡了并行性与缓存复用。
*   **通用性强**：该方法是一个通用的加速插件，可应用于多种基于离散扩散的 VLA 架构。
*   **端到端优化**：从训练（蒸馏）到推理（流水线解码）均进行了针对性优化。

### 8. 不足与局限
*   **超参数依赖**：流水线解码中的多个阈值（如置信度阈值）需要针对不同任务进行微调，以平衡速度与精度。
*   **教师模型依赖**：加速效果和最终性能高度依赖于预训练教师模型的质量。
*   **硬件细节缺失**：论文未提供具体的算力消耗数据（如 TFLOPS 或具体显存占用），不便于开发者评估部署成本。
*   **块大小限制**：块大小的选择对性能有影响，通常需要与动作维度对齐，这限制了某些灵活输出场景下的应用。

（完）

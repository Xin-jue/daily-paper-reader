---
title: "LILAC: Language-Conditioned Object-Centric Optical Flow for Open-Loop Trajectory Generation"
title_zh: LILAC：用于开环轨迹生成的语言引导、以物体为中心的光流模型
authors: "Motonari Kambara, Koki Seno, Tomoya Kaichi, Yanan Wang, Komei Sugiura"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25481v1"
tags: ["query:q10"]
score: 10.0
evidence: 基于流的VLA用于机械臂轨迹生成
tldr: LILAC是一个基于光流的视觉-语言-动作模型（VLA），旨在通过自然语言指令生成机器人操作轨迹。它利用语义对齐损失和提示条件跨模态适配器，从单张RGB图像和文本指令中生成以物体为中心的2D光流，并将其转换为6自由度机械臂动作。该方法减少了对特定机器人数据的依赖，在光流质量和实际操作成功率上均优于现有方法。
selection_source: fresh_fetch
motivation: 现有的机器人操作任务在从预操作图像和自然语言指令生成准确的物体轨迹时，面临指令与光流对齐困难的问题。
method: 提出了LILAC模型，通过语义对齐损失强化语言调节，并利用跨模态适配器融合视觉提示与图文特征，生成2D光流并转换为6自由度轨迹。
result: 实验表明，LILAC在多个基准测试中的光流生成质量以及在真实物理操作实验中的任务成功率均显著优于现有方法。
conclusion: LILAC证明了基于光流的开环轨迹生成在处理自由形式指令和跨模态对齐方面的有效性，为机器人操作提供了一种高效的VLA方案。
---

## 摘要
我们利用基于流的轨迹生成来解决语言引导的机器人操纵问题，该方法支持在人类和网络上的物体操纵视频上进行训练，且仅需极少量的具身特定数据。这一任务具有挑战性，因为从操纵前的图像和自然语言指令中生成物体轨迹需要适当的指令-流对齐。为了应对这一挑战，我们提出了基于流的语言指令引导开环动作生成器（LILAC）。这种基于流的视觉-语言-动作模型（VLA）从 RGB 图像和自然语言指令中生成以物体为中心的二维光流，并将该光流转换为 6 自由度（6-DoF）机械臂轨迹。LILAC 包含两个关键组件：语义对齐损失（Semantic Alignment Loss），用于强化语言引导以生成与指令对齐的光流；以及提示词引导的跨模态适配器（Prompt-Conditioned Cross-Modal Adapter），用于将学习到的视觉提示与图像和文本特征对齐，从而为光流生成提供丰富的线索。实验表明，我们的方法在多个基准测试中的生成光流质量均优于现有方法。此外，在使用自由形式指令的物理物体操纵实验中，LILAC 展示了比现有方法更高的任务成功率。项目页面见 https://lilac-75srg.kinsta.page/。

## Abstract
We address language-conditioned robotic manipulation using flow-based trajectory generation, which enables training on human and web videos of object manipulation and requires only minimal embodiment-specific data. This task is challenging, as object trajectory generation from pre-manipulation images and natural language instructions requires appropriate instruction-flow alignment. To tackle this challenge, we propose the flow-based Language Instruction-guided open-Loop ACtion generator (LILAC). This flow-based Vision-Language-Action model (VLA) generates object-centric 2D optical flow from an RGB image and a natural language instruction, and converts the flow into a 6-DoF manipulator trajectory. LILAC incorporates two key components: Semantic Alignment Loss, which strengthens language conditioning to generate instruction-aligned optical flow, and Prompt-Conditioned Cross-Modal Adapter, which aligns learned visual prompts with image and text features to provide rich cues for flow generation. Experimentally, our method outperformed existing approaches in generated flow quality across multiple benchmarks. Furthermore, in physical object manipulation experiments using free-form instructions, LILAC demonstrated a superior task success rate compared to existing methods. The project page is available at https://lilac-75srg.kinsta.page/.

---

## 论文详细总结（自动生成）

### 论文总结：LILAC —— 语言引导的以物体为中心的光流开环轨迹生成模型

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对机器人操纵任务中**视觉-语言-动作模型（VLA）**的泛化性与数据效率问题展开研究。
*   **研究动机**：现有的 VLA 模型（如 OpenVLA）通常需要海量的具身特定数据（机器人演示），且在零样本（Zero-shot）迁移中表现不佳。
*   **核心问题**：如何有效地将高层级的自然语言指令与低层级的密集像素运动（光流）对齐，并利用互联网上丰富的人类操纵视频（非机器人数据）来训练通用的操纵策略。
*   **整体含义**：LILAC 提出了一种开环（Open-loop）轨迹生成框架，通过预测以物体为中心的 2D 光流作为中间表示，降低了对特定机器人数据的依赖，并提高了指令执行的准确性。

#### 2. 方法论：核心思想与关键技术
LILAC 的核心思想是将操纵任务分解为“2D 光流生成”和“动作反标记化（Action De-Tokenizer）”两个阶段。
*   **提示词引导的跨模态适配器（Prompt-Conditioned Multi-Modal Adapter）**：
    *   利用多模态大模型（如 GPT-4o）根据图像和指令生成“视觉提示”（Visual Prompt），即在图像上绘制代表运动趋势的箭头。
    *   该视觉提示作为粗略的运动草图，帮助模型缩小高层语义与低层光流之间的粒度差距。
*   **语义对齐解码器与损失函数**：
    *   采用 Transformer 解码器自回归生成 2D 光流序列。
    *   **语义对齐损失（Semantic Alignment Loss）**：强制光流序列的最后一个 Token（CLS Token）与 CLIP 语言嵌入对齐，确保生成的运动轨迹严格遵循指令语义。
*   **动作反标记化（Action De-Tokenizer）**：
    *   将预测的 2D 光流结合深度图和相机参数，通过刚体变换估计初步轨迹，再利用 Transformer 进行精细化处理，最终输出 6 自由度（6-DoF）机械臂轨迹。

#### 3. 实验设计
*   **数据集与基准测试**：
    *   构建了 **Robot Flow Benchmark**，基于 Fractal 和 BridgeData V2 数据集，利用 CoTracker-3 提取物体中心光流作为真值。
    *   包含 Fractal 子集（约 2.6 万个片段）和 BridgeData V2 子集（约 1 万个片段）。
*   **对比方法**：
    *   光流生成基准：Im2Flow2Act、FLIP。
    *   物理实验基准：Im2Flow2Act、FLIP 以及端到端 VLA 模型 $\pi_0$。
*   **物理实验场景**：使用丰田 HSR 机器人，执行移动放置、移动倒水、抽纸巾、开抽屉等 5 类任务，涉及多种未见过的家庭常见物体。

#### 4. 资源与算力
*   **算力设备**：论文明确指出训练使用了 **NVIDIA H200 GPU**。
*   **训练细节**：文中未详细说明具体的训练时长和 GPU 数量，但提到采用分类损失（Cross-entropy）而非回归损失来加速网络收敛。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量评估**：在两个大规模数据集子集上进行了测试，使用了 ADE（平均距离误差）、AUC 和 P@K 等标准指标。
    *   **消融实验**：针对“语义对齐损失”和“视觉提示”分别做了消融，验证了各组件的必要性。
    *   **物理实验**：共进行了 100 次试验（5 类任务，每类 20 次），并进行了错误类别分析（100 个样本）。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真基准到真实机器人部署的完整链路，对比了当前最先进的流模型和端到端模型，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能领先**：LILAC 在光流生成的准确度（ADE）和成功率（AUC）上显著优于 Im2Flow2Act 和 FLIP。
*   **物理成功率高**：在真实机器人实验中，LILAC 的平均成功率比表现最好的基准方法高出 14 个百分点。
*   **推理效率**：作为开环模型，LILAC 的推理速度比闭环模型 $\pi_0$ 快约 1.4 倍，且能有效避免闭环执行中的误差累积。
*   **视觉提示的作用**：消融实验证明，视觉提示是提升性能最关键的因素，能显著引导模型生成符合逻辑的运动方向。

#### 7. 优点
*   **跨模态对齐创新**：通过引入视觉提示（箭头）和语义对齐损失，巧妙解决了语言指令与密集光流难以直接匹配的问题。
*   **数据效率**：能够利用互联网视频进行预训练，减少了对昂贵的机器人演示数据的需求。
*   **开环鲁棒性**：证明了在特定场景下，高质量的单次轨迹生成（开环）比频繁重规划（闭环）更具效率且误差更小。

#### 8. 不足与局限
*   **静态环境假设**：采用开环生成，无法应对执行过程中环境的动态变化（如物体被突然移走）。
*   **2D 局限性**：仅依赖 2D 光流难以处理复杂的 3D 空间逻辑（如避障或需要深度推理的工具使用）。
*   **提示词依赖**：模型性能高度依赖于前端 MLLM 生成视觉提示的质量，若 MLLM 输出错误的箭头方向，会导致后续动作完全失败。
*   **视觉复杂度瓶颈**：在物体密集或空间关系复杂的场景下，视觉提示的生成仍存在较高错误率。

（完）

---
title: "SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models"
title_zh: SABER：一种针对视觉-语言-动作（VLA）模型的隐蔽代理式黑盒攻击框架
authors: "Xiyang Wu, Guangyao Shi, Qingzi Wang, Zongxia Li, Amrit Singh Bedi, Dinesh Manocha"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24935v1"
tags: ["query:q12"]
score: 9.0
evidence: 针对视觉-语言-动作 (VLA) 模型的对抗性攻击
tldr: 针对视觉-语言-动作（VLA）模型对指令扰动的敏感性，本文提出了SABER框架。这是一种基于智能体的黑盒攻击方法，利用经过GRPO训练的ReAct攻击者，在有限的编辑预算内生成微小且合理的指令修改。SABER能有效诱导机器人任务失败、增加执行时长或违反约束。实验表明，该方法在LIBERO基准测试中显著降低了多种SOTA VLA模型的性能，为机器人基础模型的红队测试提供了高效、可扩展的方案。
selection_source: fresh_fetch
motivation: VLA模型容易受到微小文本扰动的影响，因此需要一种自动化的黑盒攻击工具来系统地评估其在对抗性指令下的鲁棒性。
method: 开发了SABER框架，使用经GRPO训练的ReAct智能体，通过字符、标记和提示词级别的编辑工具生成隐蔽的对抗性指令。
result: "在LIBERO基准上，SABER使任务成功率降低20.6%，执行步数增加55%，且编辑效率显著优于基于GPT的强基准模型。"
conclusion: 研究证明微小的指令修改足以严重干扰机器人行为，且基于智能体的攻击框架为机器人基础模型的红队测试提供了实用且高效的手段。
---

## 摘要
视觉-语言-动作（VLA）模型使机器人能够遵循基于视觉观测的自然语言指令，但指令通道也引入了一个关键漏洞：微小的文本扰动即可改变下游机器人的行为。因此，系统的鲁棒性评估需要一种黑盒攻击者，能够在不同的 VLA 模型中生成极小但有效的指令编辑。为此，我们提出了 SABER，这是一种以智能体为中心的方法，用于在受限的编辑预算下自动生成针对 VLA 模型的基于指令的对抗性攻击。SABER 使用经过 GRPO 训练的 ReAct 攻击者，在受限的编辑预算下，利用字符级、Token 级和提示词级工具生成微小且合理的对抗性指令编辑，从而诱导目标行为退化，包括任务失败、不必要的执行时长增加以及约束违规增加。在涵盖六种最先进 VLA 模型的 LIBERO 基准测试中，SABER 使任务成功率降低了 20.6%，动作序列长度增加了 55%，约束违规增加了 33%，同时与强大的基于 GPT 的基准模型相比，所需的工具调用减少了 21.1%，字符编辑减少了 54.7%。这些结果表明，微小且合理的指令编辑足以显著降低机器人的执行性能，并且这种代理式黑盒流水线为机器人基础模型的红队测试提供了一种实用、可扩展且自适应的方法。

## Abstract
Vision-language-action (VLA) models enable robots to follow natural-language instructions grounded in visual observations, but the instruction channel also introduces a critical vulnerability: small textual perturbations can alter downstream robot behavior. Systematic robustness evaluation therefore requires a black-box attacker that can generate minimal yet effective instruction edits across diverse VLA models. To this end, we present SABER, an agent-centric approach for automatically generating instruction-based adversarial attacks on VLA models under bounded edit budgets. SABER uses a GRPO-trained ReAct attacker to generate small, plausible adversarial instruction edits using character-, token-, and prompt-level tools under a bounded edit budget that induces targeted behavioral degradation, including task failure, unnecessarily long execution, and increased constraint violations. On the LIBERO benchmark across six state-of-the-art VLA models, SABER reduces task success by 20.6%, increases action-sequence length by 55%, and raises constraint violations by 33%, while requiring 21.1% fewer tool calls and 54.7% fewer character edits than strong GPT-based baselines. These results show that small, plausible instruction edits are sufficient to substantially degrade robot execution, and that an agentic black-box pipeline offers a practical, scalable, and adaptive approach for red-teaming robotic foundation models.

---

## 论文详细总结（自动生成）

以下是对论文《SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型通过自然语言指令控制机器人，但这种接口存在严重的对抗性漏洞。微小的文本扰动（如拼写错误或词汇替换）可能导致机器人执行失败、效率低下或违反安全约束。
*   **核心问题**：现有的攻击方法多依赖手动设计或昂贵的 GPT 搜索，缺乏一种针对黑盒 VLA 模型的、自动化且隐蔽（低编辑预算）的通用攻击框架。
*   **研究意义**：开发 SABER 框架旨在为机器人基础模型提供一种实用的红队测试工具，在部署前量化其鲁棒性，并揭示除“任务失败”之外的隐性风险（如动作膨胀和安全违规）。

### 2. 方法论
*   **核心思想**：将对抗性指令生成建模为一个受限预算下的**序列决策问题**，通过一个智能体（Agent）自动组合多种编辑工具来优化攻击效果。
*   **关键技术细节**：
    *   **ReAct 智能体**：攻击者采用“思考-行动”循环，利用 LLM（Qwen2.5-3B）作为大脑进行多轮推理和工具调用。
    *   **两阶段工作流 (FIND → APPLY)**：智能体首先定位（FIND）指令中适合扰动的位置，然后应用（APPLY）具体的编辑操作。
    *   **扰动工具箱**：包含字符级（插入/删除/替换字符）、Token 级（同义词替换/属性交换）和提示词级（注入冗余约束或验证步骤）三种粒度的工具。
    *   **训练算法 (GRPO)**：使用群体相对策略优化（Group Relative Policy Optimization）进行强化学习。该算法无需访问目标 VLA 模型的梯度，仅根据机器人执行的反馈（Rollout）来优化攻击策略。
    *   **两阶段训练流程**：先通过少量 GPT 生成的数据进行冷启动（SFT），再进行大规模的 GRPO 强化学习优化。

### 3. 实验设计
*   **数据集与场景**：使用 **LIBERO** 机器人操纵基准测试集，涵盖 Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程任务）四个子集。
*   **目标模型 (Victim Models)**：针对 6 种最先进的 VLA 模型进行了测试，包括 $\pi_0$、$\pi_{0.5}$、GR00T-N1.5、X-VLA、InternVLA-M1 和 DeepThinkVLA。
*   **对比基准**：与基于 **GPT-5 mini** 的强力攻击者进行对比，评估在相同工具接口下的表现。
*   **评估指标**：
    *   **ASR (攻击成功率)**：诱导任务失败的比率。
    *   **AIR (动作膨胀率)**：执行步数增加的比例。
    *   **CVI (约束违反膨胀率)**：碰撞或违规行为增加的比例。
    *   **隐蔽性指标**：平均工具调用次数和字符编辑距离。

### 4. 资源与算力
*   **模型基础**：攻击智能体基于 **Qwen2.5-3B-Instruct**。
*   **训练技术**：使用了 **LoRA**（低秩自适应）进行高效微调。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或具体的训练时长，但提到了使用 ART 框架进行分布式强化学习训练。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个任务类别上进行了广泛测试，并划分了训练集（每类前 7 个任务）和测试集（每类后 3 个任务），验证了跨任务的泛化性。
*   **消融实验**：专门设计了实验验证“冷启动（SFT）”的必要性，证明了没有 SFT 的 GRPO 训练会导致策略退化。
*   **充分性评价**：实验设计较为充分，涵盖了多种不同架构（如带推理链和不带推理链）的 VLA 模型，证明了攻击的通用性和客观性。

### 6. 主要结论与发现
*   **攻击有效性**：SABER 平均使任务成功率降低 **20.6%**，动作序列长度增加 **55%**，约束违规增加 **33%**。
*   **高效与隐蔽**：相比 GPT-5 mini 基准，SABER 在达到更好攻击效果的同时，减少了 **21.1%** 的工具调用和 **54.7%** 的字符编辑。
*   **策略演化**：通过强化学习，智能体学会了从昂贵的提示词级攻击转向更精准、高杠杆的 Token 级编辑，从而在有限预算内实现最大破坏。

### 7. 优点
*   **黑盒适用性**：完全不需要目标模型的内部参数或梯度，符合实际红队测试场景。
*   **多维度攻击**：不仅关注让机器人“罢工”，还能诱导其“磨洋工”（动作膨胀）或“乱动”（违反约束）。
*   **高效率**：通过 GRPO 优化，实现了比通用大模型更精准、更隐蔽的对抗性编辑。

### 8. 不足与局限
*   **模态局限**：目前仅关注文本指令的扰动，尚未涉及视觉观察（图像/视频）层面的对抗性攻击。
*   **环境限制**：所有实验均在模拟器中完成，尚未在真实物理机器人上验证攻击的迁移效果。
*   **防御缺失**：论文重点在于“如何攻击”，对于如何防御此类代理式攻击（如指令清洗、鲁棒训练）讨论较少。

（完）

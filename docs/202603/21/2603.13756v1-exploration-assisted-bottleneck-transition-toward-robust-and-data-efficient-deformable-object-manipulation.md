---
title: Exploration-assisted Bottleneck Transition Toward Robust and Data-efficient Deformable Object Manipulation
title_zh: 探索辅助的瓶颈状态转换：面向鲁棒且数据高效的可变形物体操作
authors: "Yujiro Onishi, Ryo Takizawa, Yoshiyuki Ohmura, Yasuo Kuniyoshi"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13756v1"
tags: ["query:q2"]
score: 9.0
evidence: 变形物体操作与机器人操作
tldr: 针对变形物体操作（DOM）中模仿学习难以处理分布外（OOD）状态且数据需求量大的问题，本文提出ExBot框架。该框架引入“瓶颈状态”作为任务执行的标准化起点，将OOD恢复简化为向瓶颈状态的转换。通过结合可识别性划分的状态空间和双重动作原语，ExBot在无需精确感知的情况下，利用探索性动作处理严重自遮挡等复杂OOD场景，显著提升了数据效率和鲁棒性。
selection_source: fresh_fetch
motivation: 变形物体具有近乎无限的配置，导致模仿学习在面对分布外状态和感知不确定性（如自遮挡）时表现不佳且数据采集成本极高。
method: 提出ExBot框架，通过定义瓶颈状态简化任务流程，并根据状态可识别性采用双重动作原语进行探索与转换。
result: 在绳索和布料操作的真实实验中，ExBot成功实现了从包含严重自遮挡在内的多种OOD初始状态完成任务。
conclusion: 该研究证明了通过瓶颈状态转换和探索辅助机制，可以有效提升变形物体操作在复杂、不可见状态下的数据效率与鲁棒性。
---

## 摘要
模仿学习在机器人操作中取得了显著成果，但在分布外（OOD）状态下表现不佳。这一局限性在可变形物体操作（DOM）中尤为突出，因为近乎无限的可能构型使得全面的数据收集变得不可行。尽管已有几种方法处理 OOD 状态，但它们通常需要详尽的数据或高精度的感知。由于 DOM 固有的复杂性（包括自遮挡），这些要求往往是不切实际的。为了解决 DOM 中的 OOD 问题，我们提出了一种名为“探索辅助的瓶颈状态转换”（ExBot）的新型框架，该框架通过两个关键优势应对 OOD 挑战。首先，我们引入了瓶颈状态，即作为任务执行起点的标准化构型。这使得 OOD 挑战可以被重新定义为将各种初始状态转换为这些瓶颈状态的问题，从而显著减少了对演示数据的需求。其次，考虑到感知的不完善，我们根据可识别性划分了 OOD 状态空间，并采用了双重动作原语。这种方法使 ExBot 能够在不需要精确感知的情况下操作甚至不可识别的状态。通过将演示集中在瓶颈状态周围，并利用探索来改变感知条件，ExBot 实现了数据效率以及对严重 OOD 场景的鲁棒性。在绳索和布料操作上的真实世界实验表明，该方法能够从包括严重自遮挡在内的各种 OOD 状态中成功完成任务。

## Abstract
Imitation learning has demonstrated impressive results in robotic manipulation but fails under out-of-distribution (OOD) states. This limitation is particularly critical in Deformable Object Manipulation (DOM), where the near-infinite possible configurations render comprehensive data collection infeasible. Although several methods address OOD states, they typically require exhaustive data or highly precise perception. Such requirements are often impractical for DOM owing to its inherent complexities, including self-occlusion. To address the OOD problem in DOM, we propose a novel framework, Exploration-assisted Bottleneck Transition for Deformable Object Manipulation (ExBot), which addresses the OOD challenge through two key advantages. First, we introduce bottleneck states, standardized configurations that serve as starting points for task execution. This enables the reconceptualization of OOD challenges as the problem of transitioning diverse initial states to these bottleneck states, significantly reducing demonstration requirements. Second, to account for imperfect perception, we partition the OOD state space based on recognizability and employ dual action primitives. This approach enables ExBot to manipulate even unrecognizable states without requiring accurate perception. By concentrating demonstrations around bottleneck states and leveraging exploration to alter perceptual conditions, ExBot achieves both data efficiency and robustness to severe OOD scenarios. Real-world experiments on rope and cloth manipulation demonstrate successful task completion from diverse OOD states, including severe self-occlusions.

---

## 论文详细总结（自动生成）

以下是对论文《Exploration-assisted Bottleneck Transition Toward Robust and Data-efficient Deformable Object Manipulation》（ExBot）的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：模仿学习（IL）在处理可变形物体操作（DOM）时，面临**分布外（OOD）状态鲁棒性差**和**数据效率低**的双重挑战。
*   **研究背景**：可变形物体（如绳索、布料）具有近乎无限的构型，且极易发生严重的自遮挡。传统的模仿学习需要海量数据才能覆盖可能的初始状态，而现有的感知算法在物体揉成一团或严重遮挡时往往会失效，导致机器人无法从非标准状态恢复并完成任务。

### 2. 方法论：ExBot 框架
ExBot 的核心思想是将复杂的 OOD 恢复问题分解为“状态识别-探索转换-标准化执行”三个阶段：
*   **瓶颈状态（Bottleneck States, $S_b$）**：预定义一组标准化的中间构型（如两手各执绳索一端）。所有任务演示仅从这些状态开始，从而显著降低数据需求。
*   **状态空间划分与双重动作原语**：
    *   **可识别状态 ($R$)**：感知算法能准确提取关键点。执行**准备动作 ($a_p$)**，将物体从当前状态引导至瓶颈状态。
    *   **不可识别状态 ($\bar{R}$)**：由于遮挡或揉皱导致感知失败。执行**探索动作 ($a_e$)**，通过物理干预（如提起、抖动）改变物体构型，使其进入可识别状态。
*   **关键技术细节**：
    *   **感知模块 (ORM)**：使用基于图的算法（绳索）或关键点检测器（布料）提取抽象表示。
    *   **动作决策策略 (ADP)**：利用**视觉语言模型（VLM，如 GPT-5.2）**作为推理引擎，对比原始图像与感知叠加图，判断当前识别是否准确，并决定执行 $a_p$ 还是 $a_e$。
    *   **行为克隆 (BC) 模型**：采用 ACT (Action Chunking with Transformer) 架构，仅学习从瓶颈状态到任务完成的轨迹。

### 3. 实验设计
*   **实验场景**：
    1.  **绳索打结 (Knot Tying)**：从随机抛掷、折叠的 OOD 状态开始完成打结。
    2.  **布料折叠 (Cloth Folding)**：从揉成一团的 OOD 状态开始完成四层折叠。
*   **基准测试 (Benchmark)**：
    *   **VLM-based ORM**：使用纯视觉模型（如 Qwen2.5-VL）直接进行关键点识别的对比。
    *   **End-to-end BC**：直接从 OOD 状态训练到任务完成的端到端模仿学习模型。
*   **评估指标**：可识别率 (RR)、累积准确率 (CAR)、转换成功率、最终任务成功率。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **模型细节**：提到了使用 GPT-5.2 作为决策推理机，以及 ACT 架构作为底层控制策略。演示数据量较小（绳索 157 次，布料 138 次），体现了该方法对算力和数据的低依赖性。

### 5. 实验数量与充分性
*   **实验规模**：针对每种任务进行了 30 组独立的 OOD 初始状态测试。
*   **充分性评价**：
    *   **客观性**：通过标准化的 OOD 协议（如随机抛掷、揉搓）生成初始状态，具有较高的随机性和挑战性。
    *   **对比公平性**：对比了端到端模型在相同数据量下的表现（结果为 0% 成功率），有力证明了瓶颈状态转换的必要性。
    *   **消融/对比**：对比了不同感知策略（算法 vs VLM）在探索过程中的表现，验证了“感知-动作”协同设计的有效性。

### 6. 主要结论与发现
*   **瓶颈状态是关键**：通过将 OOD 状态引导至瓶颈状态，ExBot 能够以极少量的演示数据（约 150 次）处理极其复杂的初始构型。
*   **探索动作的必要性**：在初始识别率极低（绳索 <15%，布料 0%）的情况下，通过探索动作，最终识别率分别提升至 97% 和 83%。
*   **VLM 的决策能力**：VLM 能够有效判断感知算法的可靠性，从而闭环地引导物体从不可见状态转变为可见状态。

### 7. 优点与亮点
*   **容错性强**：不追求“完美感知”，而是通过物理探索主动改善感知环境，这更符合生物处理复杂物体的方式。
*   **数据高效**：将学习压力从“覆盖全空间”转变为“向中心收敛”，极大地降低了 DOM 任务的标注成本。
*   **模块化设计**：感知、决策、探索和执行模块解耦，易于针对不同物体进行扩展。

### 8. 不足与局限
*   **任务复杂度有限**：目前仅验证了单根绳索和单块布料，尚未涉及更复杂的场景（如打结的绳索解开、多物体交互或 T 恤等复杂服装）。
*   **VLM 判定偏差**：实验中发现 VLM 有时过于保守，会导致不必要的重复探索，或在特定构型（如布料对折）下产生误判。
*   **硬件依赖**：准备动作（$a_p$）对夹具的标定和精度要求较高，抓取失败是导致任务失败的主要原因之一。
*   **实时性挑战**：依赖大语言模型进行推理可能会带来一定的决策延迟。

（完）

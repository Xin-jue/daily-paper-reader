---
title: "Fast-WAM: Do World Action Models Need Test-time Future Imagination?"
title_zh: Fast-WAM：世界动作模型是否需要测试时的未来想象？
authors: "Tianyuan Yuan, Zibin Dong, Yicheng Liu, Hang Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16666v1"
tags: ["query:q4"]
score: 9.0
evidence: 世界动作模型作为具身控制中VLA模型的替代方案
tldr: Fast-WAM 探讨了世界动作模型（WAM）在推理时是否需要显式的未来想象。研究发现，虽然现有 WAM 依赖耗时的视频去噪生成未来图像，但其核心优势其实源于训练阶段的视频建模。Fast-WAM 保留了视频协同训练，但在推理时跳过预测步骤，在 LIBERO 和 RoboTwin 等基准测试中表现优异，且速度提升了 4 倍以上，实现了实时控制。
selection_source: fresh_fetch
motivation: 现有 WAM 采用“先想象再执行”模式导致推理延迟高，研究旨在探究推理时的显式未来生成是否为性能所必需。
method: 提出 Fast-WAM 架构，在训练时保留视频协同训练以增强表示学习，但在推理时直接输出动作而跳过视频生成。
result: Fast-WAM 在仿真和真实任务中达到了 SOTA 水平，延迟仅为 190ms，比传统 WAM 快 4 倍以上。
conclusion: 视频预测在 WAM 中的主要价值在于训练期间提升世界表示能力，而非推理时的显式观测生成。
---

## 摘要
世界动作模型（World Action Models, WAMs）已成为具身控制中视觉-语言-动作（Vision-Language-Action, VLA）模型的一种极具前景的替代方案，因为它们显式地建模了视觉观测在动作作用下如何演化。大多数现有的 WAM 遵循“先想象后执行”的范式，由于迭代视频去噪产生了显著的测试时延迟，但目前尚不清楚显式的未来想象对于强大的动作性能是否真的必要。在本文中，我们探讨了 WAM 是否在测试时需要显式的未来想象，或者其优势是否主要源于训练期间的视频建模。我们通过提出 Fast-WAM 来解耦训练期间的视频建模与推理期间的显式未来生成。Fast-WAM 是一种在训练时保留视频协同训练但在测试时跳过未来预测的 WAM 架构。我们进一步实例化了多个 Fast-WAM 变体，以便对这两个因素进行受控比较。在这些变体中，我们发现 Fast-WAM 与“先想象后执行”变体相比仍具竞争力，而移除视频协同训练会导致更大幅度的性能下降。实验表明，在没有具身预训练的情况下，Fast-WAM 在仿真基准测试（LIBERO 和 RoboTwin）以及现实世界任务中均取得了与最先进方法相当的结果。它的运行延迟为 190 毫秒，实现了实时运行，比现有的“先想象后执行”型 WAM 快 4 倍以上。这些结果表明，WAM 中视频预测的主要价值可能在于提高训练期间的世界表示能力，而非在测试时生成未来观测。项目页面：https://yuantianyuan01.github.io/FastWAM/

## Abstract
World Action Models (WAMs) have emerged as a promising alternative to Vision-Language-Action (VLA) models for embodied control because they explicitly model how visual observations may evolve under action. Most existing WAMs follow an imagine-then-execute paradigm, incurring substantial test-time latency from iterative video denoising, yet it remains unclear whether explicit future imagination is actually necessary for strong action performance. In this paper, we ask whether WAMs need explicit future imagination at test time, or whether their benefit comes primarily from video modeling during training. We disentangle the role of video modeling during training from explicit future generation during inference by proposing \textbf{Fast-WAM}, a WAM architecture that retains video co-training during training but skips future prediction at test time. We further instantiate several Fast-WAM variants to enable a controlled comparison of these two factors. Across these variants, we find that Fast-WAM remains competitive with imagine-then-execute variants, while removing video co-training causes a much larger performance drop. Empirically, Fast-WAM achieves competitive results with state-of-the-art methods both on simulation benchmarks (LIBERO and RoboTwin) and real-world tasks, without embodied pretraining. It runs in real time with 190ms latency, over 4$\times$ faster than existing imagine-then-execute WAMs. These results suggest that the main value of video prediction in WAMs may lie in improving world representations during training rather than generating future observations at test time. Project page: https://yuantianyuan01.github.io/FastWAM/

---

## 论文详细总结（自动生成）

### Fast-WAM 论文深度总结

#### 1. 核心问题与研究动机
*   **研究背景**：世界动作模型（World Action Models, WAMs）通过建模视觉观测随动作的变化（即预测未来视频），在具身智能控制中表现出优于传统视觉-语言-动作（VLA）模型的潜力。
*   **核心问题**：现有的 WAM 通常采用“先想象、后执行”的范式，即在推理时先通过耗时的扩散模型生成未来视频，再根据视频预测动作。这导致了极高的计算延迟。
*   **研究动机**：作者质疑“测试时的显式未来想象”是否真的必要。他们猜测 WAM 的优势可能主要源于**训练阶段的视频建模任务**（提升了特征表示能力），而非**推理阶段生成的图像本身**。

#### 2. 方法论：Fast-WAM 架构
*   **核心思想**：解耦训练时的视频建模目标与推理时的未来生成过程。在训练时保留视频预测作为辅助任务，但在推理时跳过视频生成，直接从潜空间表示中提取动作。
*   **关键技术细节**：
    *   **Mixture-of-Transformer (MoT) 架构**：基于预训练的视频扩散 Transformer（Wan2.2-5B），增加了一个 1B 参数的“动作专家（Action Expert）”DiT。
    *   **结构化注意力掩码（Structured Attention Mask）**：在训练时，动作 Token 可以访问当前帧的视觉 Token，但被禁止访问未来的视频 Token。这确保了动作预测仅依赖于由视频建模任务强化的视觉表示，而非未来图像。
    *   **流匹配（Flow Matching）目标**：对动作预测和视频建模均采用流匹配目标进行联合训练。
*   **算法流程**：
    *   **训练阶段**：输入当前帧、任务指令、未来视频帧（加噪）和动作序列（加噪），通过联合损失函数优化模型。
    *   **推理阶段**：仅输入当前帧和指令，视频 DiT 进行单次前向传播生成潜空间特征，动作专家直接去噪输出动作块，无需迭代生成未来视频。

#### 3. 实验设计
*   **仿真基准测试**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务。
    *   **RoboTwin 2.0**：具有挑战性的双臂协作基准，包含 50 多个任务，涉及重度场景随机化。
*   **现实世界任务**：折叠毛巾（Towel Folding）。这是一个长程、涉及柔性物体动力学的复杂任务。
*   **对比方法**：
    *   **VLA 模型**：OpenVLA, $\pi_0$, $\pi_{0.5}$。
    *   **WAM 模型**：Motus, LingBot-VA。
    *   **受控变体**：Fast-WAM-Joint（联合去噪）、Fast-WAM-IDM（先视频后动作）、Fast-WAM w/o video co-train（移除视频协同训练）。

#### 4. 资源与算力
*   **硬件平台**：所有延迟测试均在单张 **NVIDIA RTX 5090D V2 (32GB)** GPU 上完成。
*   **模型规模**：总参数量约 **6B**（5B 视频底座 + 1B 动作专家）。
*   **训练时长**：LIBERO 训练 20k 步，RoboTwin 和真实世界任务训练 30k 步。文中未明确给出具体的总训练小时数，但提到其具有很强的数据效率。

#### 5. 实验数量与充分性
*   **实验规模**：在仿真环境中进行了数千次评估（LIBERO 2000 次试验，RoboTwin 每任务 100 次试验），并在真实世界中进行了详尽测试。
*   **充分性与公平性**：
    *   作者设计了严谨的**消融实验**，通过控制变量法（保持底座、数据、训练策略一致）对比了三种 WAM 范式。
    *   实验涵盖了单臂、双臂、刚体、柔性体等多种场景，验证了结论的普适性。
    *   对比了有无“具身预训练”的情况，证明了 Fast-WAM 在无预训练下也能达到 SOTA。

#### 6. 主要结论与发现
*   **性能与速度双赢**：Fast-WAM 在性能上与“先想象后执行”的 WAM 相当，但推理延迟仅为 **190ms**，比传统 WAM 快 **4 倍以上**。
*   **核心价值在于训练**：受控对比显示，移除视频协同训练会导致性能大幅下降，而移除推理时的视频生成对性能影响极小。这证明了 **视频预测的主要价值在于训练期间改善世界表示**。
*   **数据效率**：即使没有大规模具身数据预训练，Fast-WAM 也能在复杂任务（如折叠毛巾）中表现出色。

#### 7. 优点与亮点
*   **高效性**：解决了 WAM 难以实时部署的痛点，实现了 5Hz 左右的控制频率。
*   **科学性**：通过解耦实验，清晰地回答了 WAM 领域关于“想象力”必要性的基本科学问题。
*   **通用性**：架构简单且模块化，易于集成到现有的视频生成底座中。

#### 8. 不足与局限
*   **预训练规模**：虽然在无具身预训练下表现良好，但尚未在大规模互联网级具身数据集上验证其扩展性（Scaling）。
*   **自回归限制**：为了受控实验，本文省略了外层的自回归循环（Auto-regressive rollout），主要关注单次动作块的生成。
*   **任务复杂度**：虽然涵盖了柔性物体，但对于需要极高频率反馈或极精细力控的任务，单次前向传播的表示能力是否足够仍需进一步探讨。

（完）

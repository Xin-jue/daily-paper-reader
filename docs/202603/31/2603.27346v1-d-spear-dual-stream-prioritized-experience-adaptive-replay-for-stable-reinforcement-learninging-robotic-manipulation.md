---
title: "D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay for Stable Reinforcement Learninging Robotic Manipulation"
title_zh: D-SPEAR：用于稳定机器人操作强化学习的双流优先经验自适应回放
authors: "Yu Zhang, Karl Mason"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27346v1"
tags: ["query:q2"]
score: 9.0
evidence: 机器人操作的稳定强化学习
tldr: 针对机器人操作任务中强化学习训练不稳定的问题，本文提出D-SPEAR框架。该框架通过解耦Actor和Critic的采样策略，让Critic利用优先经验回放提高学习效率，而Actor则通过低误差样本进行更新以增强策略稳定性。结合自适应锚点机制和Huber损失函数，D-SPEAR在robosuite基准测试中显著提升了SAC和TD3等算法的性能与训练稳定性。
selection_source: fresh_fetch
motivation: 传统的经验回放策略忽略了Actor和Critic对数据需求的不同，导致在复杂机器人任务中容易出现策略震荡和性能崩溃。
method: 提出双流采样框架，Critic采用优先采样，Actor采用低误差样本采样，并引入基于TD误差变异系数的自适应锚点机制。
result: 在Block-Lifting和Door-Opening等挑战性任务中，D-SPEAR在最终性能和训练稳定性上均优于SAC、TD3和DDPG等主流算法。
conclusion: 解耦Actor与Critic的采样流并结合自适应调整机制，是提升离策强化学习在复杂机器人操作中鲁棒性的有效途径。
---

## 摘要
由于具有丰富的接触动力学、长时程以及训练不稳定性，机器人操作对强化学习而言仍具挑战性。尽管 SAC 和 TD3 等离策（off-policy）演员-评论家（actor-critic）算法在仿真中表现良好，但在现实设置中往往会遭遇策略振荡和性能崩溃，部分原因在于经验回放策略忽略了演员和评论家对数据的不同需求。我们提出了 D-SPEAR：双流优先经验自适应回放，这是一种在维持共享经验回放池的同时，解耦演员和评论家采样的回放框架。评论家利用优先回放进行高效的价值学习，而演员则使用低误差转换进行更新，以稳定策略优化。一种自适应锚定机制根据 TD 误差的变异系数平衡均匀采样和优先采样，而基于 Huber 的评论家目标函数进一步提高了在异构奖励尺度下的鲁棒性。我们在 robosuite 基准测试中具有挑战性的机器人操作任务（包括抬块和开门）上评估了 D-SPEAR。结果表明，D-SPEAR 在最终性能和训练稳定性方面均始终优于 SAC、TD3 和 DDPG 等强离策基准算法，消融实验证实了演员端和评论家端回放流的互补作用。

## Abstract
Robotic manipulation remains challenging for reinforcement learning due to contact-rich dynamics, long horizons, and training instability. Although off-policy actor-critic algorithms such as SAC and TD3 perform well in simulation, they often suffer from policy oscillations and performance collapse in realistic settings, partly due to experience replay strategies that ignore the differing data requirements of the actor and the critic. We propose D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay, a replay framework that decouples actor and critic sampling while maintaining a shared replay buffer. The critic leverages prioritized replay for efficient value learning, whereas the actor is updated using low-error transitions to stabilize policy optimization. An adaptive anchor mechanism balances uniform and prioritized sampling based on the coefficient of variation of TD errors, and a Huber-based critic objective further improves robustness under heterogeneous reward scales. We evaluate D-SPEAR on challenging robotic manipulation tasks from the robosuite benchmark, including Block-Lifting and Door-Opening. Results demonstrate that D-SPEAR consistently outperforms strong off-policy baselines, including SAC, TD3, and DDPG, in both final performance and training stability, with ablation studies confirming the complementary roles of the actorside and critic-side replay streams.

---

## 论文详细总结（自动生成）

这是一份关于论文《D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay for Stable Reinforcement Learning in Robotic Manipulation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在机器人操作任务中，强化学习（RL）常因接触动力学复杂、任务时程长而导致训练不稳定（如策略震荡或性能崩溃）。
*   **研究动机**：作者发现传统的经验回放（ER）策略（如 PER）对 Actor 和 Critic 采用相同的采样策略。然而，两者对数据的需求存在**本质矛盾**：Critic 需要高 TD 误差的样本来纠正价值估计，而 Actor 若使用这些高误差（即不准确）的样本进行更新，会引入高方差和偏差的梯度，导致训练不稳定。

### 2. 方法论
D-SPEAR 框架的核心思想是**解耦 Actor 和 Critic 的采样流**，主要包含以下三个关键技术：
*   **双流采样（Dual-Stream Replay）**：
    *   **Critic 流**：采用标准优先采样（PER），倾向于高 TD 误差样本，以加速价值函数收敛。
    *   **Actor 流**：采用反向优先采样，倾向于低 TD 误差样本，确保策略更新基于相对准确的价值估计，从而增强稳定性。
*   **自适应锚点机制（Adaptive Anchor Mechanism）**：
    *   在每个 Batch 中保留一定比例的均匀采样样本（锚点集），以维持数据分布覆盖。
    *   **动态调整**：根据 TD 误差的变异系数（CV）自动调整锚点比例 $\lambda$。当 TD 误差分布差异大时，增加优先采样比例；当分布均匀时，增加均匀采样比例。
*   **鲁棒 Critic 目标函数**：
    *   将标准 SAC 中的均方误差（MSE）替换为 **Huber 损失**，以降低对异常值和异构奖励尺度的敏感性。

### 3. 实验设计
*   **实验场景**：基于 `robosuite` 仿真基准测试，使用了 7 自由度的 Franka Panda 机器人。
*   **具体任务**：
    1.  **Lift（抬块）**：抓取并提升物体至指定高度。
    2.  **Door（开门）**：通过与把手交互打开铰链门（涉及复杂的接触动力学）。
*   **对比基准（Baselines）**：对比了三种主流的离策算法：**SAC**（软演员-评论家）、**TD3**（双延迟深度确定性策略梯度）和 **DDPG**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练参数**：每个任务训练 250,000 个环境步（500 个 Episode），控制频率为 20Hz。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对两个核心任务进行了完整的性能对比。
    *   每组实验均使用了 **5 个不同的随机种子**，并绘制了带有标准差阴影的训练曲线。
    *   **消融实验**：分别验证了“无双流结构”、“无 Actor 低误差流”和“无 Critic 高误差流”的情况。
*   **充分性评价**：实验设计较为客观，通过多种子实验和消融实验证明了各组件的有效性。虽然任务数量（2个）不算多，但涵盖了机器人操作中典型的抓取和接触交互挑战。

### 6. 主要结论与发现
*   **性能提升**：D-SPEAR 在最终回报（Return）上显著优于 SAC、TD3 和 DDPG。在 Lift 任务中，其最终得分几乎是 SAC 的两倍。
*   **稳定性增强**：相比于基准算法在训练后期经常出现的性能震荡或崩溃，D-SPEAR 的学习曲线更加平滑且单调上升。
*   **互补性**：消融实验证实，Actor 端和 Critic 端的差异化采样策略具有互补作用，缺一不可。

### 7. 优点
*   **理论直观**：敏锐捕捉到了 Actor-Critic 架构中采样需求的矛盾，并给出了简洁的解耦方案。
*   **自适应性**：引入 CV 动态调整锚点比例，减少了人工调参的工作量，增强了算法在不同阶段的灵活性。
*   **即插即用**：该框架可以很容易地集成到现有的各种离策 Actor-Critic 算法中。

### 8. 不足与局限
*   **实验覆盖面有限**：仅在 `robosuite` 的两个任务上进行了测试，未在更复杂的长程任务（如多阶段装配）或视觉输入（Vision-based）任务中验证。
*   **缺乏真实机器人验证**：所有实验均在仿真环境中完成，未探讨 Sim-to-Real（仿真到现实）的迁移效果。
*   **计算开销**：虽然解耦采样逻辑简单，但在大型回放池中频繁计算 CV 和维护两种优先分布可能会带来额外的计算开销，文中未对此进行量化分析。

（完）

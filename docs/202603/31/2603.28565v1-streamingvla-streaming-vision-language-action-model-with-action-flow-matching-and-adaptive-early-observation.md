---
title: "StreamingVLA: Streaming Vision-Language-Action Model with Action Flow Matching and Adaptive Early Observation"
title_zh: StreamingVLA：结合动作流匹配与自适应早期观测的流式视觉-语言-动作模型
authors: "Yiran Shi, Dongqi Guo, Tianchen Zhao, Feng Gao, Liangzhi Shi, Chao Yu, ZhiJian Mo, Qihua Xiao, XiaoShuai Peng, Qingmin Liao, Yu Wang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28565v1"
tags: ["query:q12"]
score: 9.0
evidence: 具有动作流匹配的流式VLA模型
tldr: 针对视觉-语言-动作（VLA）模型在边缘设备部署时因顺序执行导致的延迟和停顿问题，本文提出了StreamingVLA。该模型通过引入动作流匹配（Action Flow Matching）和自适应早期观测机制，实现了观测、生成与执行阶段的异步并行化。在保持性能的同时，StreamingVLA显著提升了执行流畅度，实现了2.4倍的延迟加速和6.5倍的停顿减少，为实时机器人控制提供了高效方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型由于观测、生成和执行阶段必须顺序串行，导致在资源受限平台上存在高延迟和频繁停顿的效率瓶颈。
method: 提出动作流匹配以重叠生成与执行阶段，并设计动作显著性感知的自适应观测机制以重叠执行与观测阶段，实现异步并行化。
result: 在不牺牲性能的情况下，StreamingVLA实现了2.4倍的延迟加速，并将执行过程中的停顿次数减少了6.5倍。
conclusion: StreamingVLA通过流式处理架构有效提升了VLA模型的推理效率和执行流畅度，具有重要的实际部署价值。
---

## 摘要
视觉-语言-动作（VLA）模型在自然语言驱动的感知与控制方面展现了卓越的性能。然而，VLA 模型的高计算成本带来了显著的效率挑战，特别是在实际部署中资源受限的边缘平台。由于 VLA 的不同阶段（观测、动作生成和执行）必须顺序进行，并等待前一阶段的完成，系统面临频繁的停顿和高延迟。为了解决这一问题，我们进行了系统分析以识别快速且流畅生成的挑战，并提出以“流式”方式使 VLA 具备跨阶段异步并行的能力。首先，我们消除了对动作分块（action chunking）的依赖，并采用了动作流匹配（action flow matching），该方法学习动作流的轨迹，而非对分块动作进行去噪，从而使动作生成与执行的延迟相互重叠。其次，我们设计了一种动作显著性感知的自适应观测机制，从而使执行与观测的延迟相互重叠。在不牺牲性能的情况下，StreamingVLA 实现了显著的加速并提高了执行的流畅度。它实现了 2.4 倍的延迟加速，并将执行停顿减少了 6.5 倍。

## Abstract
Vision-language-action (VLA) models have demonstrated exceptional performance in natural language-driven perception and control. However, the high computational cost of VLA models poses significant efficiency challenges, particularly for resource-constrained edge platforms in real-world deployments. However, since different stages of VLA (observation, action generation and execution) must proceed sequentially, and wait for the completion of the preceding stage, the system suffers from frequent halting and high latency. To address this, We conduct a systematic analysis to identify the challenges for fast and fluent generation, and propose enabling VLAs with the ability to asynchronously parallelize across VLA stages in a "streaming" manner. First, we eliminate the reliance on action chunking and adopt action flow matching, which learns the trajectory of action flows rather than denoising chunk-wise actions. It overlaps the latency of action generation and execution. Second, we design an action saliency-aware adaptive observation mechanism, thereby overlapping the latency of execution and observation. Without sacrificing performance, StreamingVLA achieves substantial speedup and improves the fluency of execution. It achieves a 2.4 $\times$ latency speedup and reduces execution halting by 6.5 $\times$.

---

## 论文详细总结（自动生成）

### StreamingVLA 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在机器人控制中表现出色，但在实际部署（尤其是边缘设备）时面临严重的**效率瓶颈**。现有的 VLA 模型通常采用“动作分块（Action Chunking）”技术，但其工作流仍是**顺序串行**的：观测（VLM 处理） $\rightarrow$ 动作生成（扩散模型去噪） $\rightarrow$ 动作执行。这种串行模式导致两个主要问题：
*   **高延迟（High Latency）：** 每一阶段都必须等待前一阶段完成。
*   **执行停顿（Execution Halting）：** 在两组动作执行之间，机器人会因为等待模型推理而产生明显的停顿（Stuttering），导致动作不连贯。
本文的研究动机是实现 VLA 各阶段的**异步并行化**，即“流式（Streaming）”执行，以提升速度和流畅度。

#### 2. 方法论：核心思想与关键技术
论文提出了 **StreamingVLA** 框架，通过两个核心技术实现阶段间的重叠（Overlap）：

*   **基于状态的动作流匹配（Action Flow Matching, AFM）：**
    *   **核心思想：** 放弃传统的动作分块去噪（Diffusion），改为学习动作流的轨迹。它将动作建模为对“物理状态”的更新，而不是预测绝对值。
    *   **技术细节：** 引入“动作空间状态（Action-space State）”，通过累加历史动作来维持状态一致性。为了适配复杂的控制器（如 OSC_POSE），论文扩展了公式以确保动作空间与物理空间的对齐。
    *   **架构调整：** 修改了模型中的归一化层（Normalization），移除偏移项并共享统计数据，以保持动作与状态之间的“可加性”（$x_t + a_t = x_{t+1}$）。这使得模型可以逐个生成动作并立即执行，从而重叠了生成与执行阶段。

*   **动作显著性感知的自适应早期观测（Adaptive Early Observation, AEO）：**
    *   **核心思想：** 在当前动作序列执行完之前，提前启动下一轮的 VLM 观测。
    *   **关键技术：** 引入一个轻量级的**动作显著性预测器**。该预测器评估剩余动作对环境改变的程度（显著性）。如果显著性低（环境变化小），则提前观测；如果显著性高（如正在开门），则延迟观测以避免感知偏差。这重叠了执行与观测阶段。

#### 3. 实验设计
*   **基础模型：** 以 $\pi_{0.5}$-Libero 为基准模型。
*   **数据集/场景：** 使用 **LIBERO** 仿真基准测试，包含四个任务套件：Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程），共 40 个不同任务。
*   **对比方法：** 
    *   原始 $\pi_{0.5}$（不同动作步长 $h=5, 10$）。
    *   RTC（实时分块）、SmolVLA、VLASH（通过微调缓解早期观测偏差）、Temporal Ensembling（时间集成）。
*   **评估指标：** 成功率（Success Rate）、单步动作耗时（$T_{action}$）、停顿间隙（$T_{halt}$）。

#### 4. 资源与算力
*   **训练算力：** 使用了 **4 台 NVIDIA A800-SXM4-80GB GPU**。
*   **训练时长：** 主模型训练 60,000 次迭代，耗时约 **10-20 GPU 小时**；自适应预测器训练耗时约 3-4 GPU 小时。
*   **推理算力：** 评估在单张 A800 GPU 上完成。
*   **实机部署：** 在 Franka Panda 机器人臂上进行了真实场景验证。

#### 5. 实验数量与充分性
*   **实验规模：** 每个任务进行 50 次 Rollout（总计数千次实验），覆盖了仿真和真实世界。
*   **消融实验：** 针对动作流匹配（AFM）、归一化修改（NM）、状态对齐（SA）以及不同早期观测策略（Naive vs. Random vs. Adaptive）做了详细对比。
*   **充分性评价：** 实验设计非常充分且客观。通过对比多种 SOTA 异步推理方法，证明了该方法在不牺牲成功率的前提下，显著优于现有技术的加速效果。

#### 6. 主要结论与发现
*   **性能保持：** StreamingVLA 在 LIBERO 上的平均成功率为 94.9%，与基准模型（95.1%）几乎持平。
*   **显著加速：** 实现了 **2.4 倍** 的端到端延迟加速。
*   **流畅度提升：** 将执行过程中的停顿时间减少了 **6.5 倍**（从约 232ms 降至 36ms），实现了肉眼可见的流畅控制。
*   **自适应的重要性：** 简单的早期观测会导致成功率大幅下降（从 97% 降至 86%），而自适应机制能有效识别关键动作，保护模型性能。

#### 7. 优点与亮点
*   **系统性分析：** 论文首先对 VLA 运行时间进行了量化分解，明确了优化目标（$T_{action}$ 和 $T_{halt}$）。
*   **异步并行：** 突破了 VLA 串行执行的固有模式，首次在两个维度（生成-执行、执行-观测）同时实现重叠。
*   **物理一致性：** 通过状态建模和架构微调，解决了大规模 VLA 模型在流式生成时的数值稳定性问题。
*   **实战价值：** 提供了算法伪代码，并在真实机器人上验证了有效性，具有很强的工程落地意义。

#### 8. 不足与局限
*   **架构依赖：** 该方法主要针对类似 $\pi_0$ 这种“VLM + 扩散/流匹配专家”结构的模型，对于纯自回归（Token-based）的 VLA 模型适配性未做深入探讨。
*   **预测器开销：** 虽然预测器很轻量（占推理时间 5%），但在极度资源受限的嵌入式芯片上，这部分额外计算仍需考量。
*   **极端场景：** 在环境变化极快、每个动作都具有高显著性的任务中，自适应机制可能会退化回串行模式，加速效果会打折扣。

（完）

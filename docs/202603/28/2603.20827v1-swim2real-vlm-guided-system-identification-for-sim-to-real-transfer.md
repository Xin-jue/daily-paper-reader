---
title: "Swim2Real: VLM-Guided System Identification for Sim-to-Real Transfer"
title_zh: Swim2Real：基于视觉语言模型（VLM）引导的系统辨识，用于从仿真到现实的迁移
authors: "Kevin Qiu, Kyle Walker, Mike Y. Michelis, Marek Cygan, Josie Hughes"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20827v1"
tags: ["query:q8"]
score: 9.0
evidence: VLM引导的系统辨识用于从仿真到现实的迁移
tldr: 针对软体水下机器人仿真参数标定困难的问题，本文提出 Swim2Real 框架。该方法利用视觉语言模型（VLM）对比真实与仿真视频，自动提出并优化 16 个模拟器参数，无需人工设计搜索阶段。通过引入回溯线搜索机制，显著提高了参数更新的成功率。实验证明，该方法大幅降低了虚实速度误差，并成功实现了强化学习策略从仿真到物理实体的零样本迁移，为水下机器人自动化调优提供了新路径。
selection_source: fresh_fetch
motivation: 软体水下机器人由于复杂的流固耦合和非线性动力学，导致其仿真参数标定过程极其复杂且难以通过传统方法自动化完成。
method: 通过 VLM 对比真实与仿真视频并提出参数更新建议，配合回溯线搜索优化步长，实现 16 个模拟器参数的同步自动校准。
result: "Swim2Real 将速度误差降低了 43%，且其训练的强化学习策略在物理实体上的游泳距离比 BayesOpt 和 CMA-ES 方法分别高出 12% 和 90%。"
conclusion: VLM 引导的系统辨识能够有效弥合水下机器人的虚实差距，实现无需人工干预的自动化模拟器调优与零样本策略迁移。
---

## 摘要
我们提出了 Swim2Real，这是一个利用视觉语言模型（VLM）反馈从游泳视频中校准 16 参数鱼形机器人仿真器的流水线，无需人工设计的搜索阶段。校准软体水下机器人具有极大的挑战性，因为非线性流固耦合使得参数空间变得混乱，简化的流体模型引入了持续存在的仿真与现实差距（sim-to-real gap），且受控的水下实验难以复现。该平台之前的研究需要三个手动定制的阶段来处理这种复杂性。VLM 通过对比仿真视频和真实视频来提出参数更新建议。随后，回溯线搜索（backtracking line search）验证每个步长，通过恢复方向正确但幅度过大的提议，将接受率从 14% 提高到 42%，增加了两倍。Swim2Real 同时校准所有 16 个参数，在所有电机频率下最紧密地匹配真实鱼的速度（平均绝对误差 MAE = 7.4 mm/s，比次优方法低 43%），且在五次运行中均无离群种子。训练策略生成的电机指令以 50 Hz 的频率迁移到实体鱼上，完成了从游泳视频到现实世界部署的流水线。下游强化学习（RL）策略的游泳距离比基于贝叶斯优化（BayesOpt）校准的仿真器高出 12%，比 CMA-ES 高出 90%。这些结果表明，基于 VLM 引导的校准可以直接从视频中缩小水下机器人的仿真与现实差距，实现强化学习向物理游泳者的零样本迁移，而无需手动进行系统辨识，这是迈向水下机器人自动化、通用仿真器调优的一步。

## Abstract
We present Swim2Real, a pipeline that calibrates a 16-parameter robotic fish simulator from swimming videos using vision-language model (VLM) feedback, requiring no hand-designed search stages. Calibrating soft aquatic robots is particularly challenging because nonlinear fluid-structure coupling makes the parameter landscape chaotic, simplified fluid models introduce a persistent sim-to-real gap, and controlled aquatic experiments are difficult to reproduce. Prior work on this platform required three manually tailored stages to handle this complexity. The VLM compares simulated and real videos and proposes parameter updates. A backtracking line search then validates each step size, tripling the accept rate from 14% to 42% by recovering proposals where the direction is correct but the magnitude is too large. Swim2Real calibrates all 16 parameters simultaneously, most closely matching real fish velocities across all motor frequencies (MAE = 7.4 mm/s, 43% lower than the next-best method), with zero outlier seeds across five runs. Motor commands from the trained policy transfer to the physical fish at 50 Hz, completing the pipeline from swimming video to real-world deployment. Downstream RL policies swim 12% farther than those from BayesOpt-calibrated simulators and 90% farther than CMA-ES. These results demonstrate that VLM-guided calibration can close the sim-to-real gap for aquatic robots directly from video, enabling zero-shot RL transfer to physical swimmers without manual system identification, a step toward automated, general-purpose simulator tuning for underwater robotics.

---

## 论文详细总结（自动生成）

### Swim2Real: 基于 VLM 引导的系统辨识与仿真到现实迁移总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何自动化地校准软体水下机器人的仿真参数，以实现高精度的“仿真到现实”（Sim-to-Real）策略迁移。
*   **研究背景与动机**：
    *   软体水下机器人具有复杂的非线性流固耦合动力学，传统解析模型难以精确描述。
    *   现有的系统辨识方法（如贝叶斯优化或进化策略）通常将模拟器视为黑盒，仅依赖标量误差信号，忽略了视频中丰富的物理结构信息。
    *   以往针对该平台的校准需要人工将其分解为多个阶段（如先调刚度再调流体系数），过程繁琐且难以泛化。

#### 2. 方法论：核心思想与技术细节
Swim2Real 提出了一种由视觉语言模型（VLM）引导的自动化校准流水线：
*   **VLM 物理推理**：使用 Gemini 2.5 Pro 作为核心推理引擎。系统向 VLM 提供仿真与真实视频的叠加对比图、数值误差数据（标记点位置、速度）以及参数语义说明。VLM 识别物理差异（如“尾部摆动过大”），并基于物理直觉提出 16 个参数（流体系数、电机几何、各关节刚度与阻尼）的更新建议。
*   **回溯线搜索（Backtracking Line Search）**：由于 VLM 虽能找准方向但难以精确量化步长，系统引入了线搜索机制。若 VLM 提出的全步长更新未能降低误差，则按几何比例（$\beta=0.5$）缩减步长并重新评估。
*   **算法流程**：
    1.  初始化参数并运行仿真。
    2.  渲染仿真与真实视频的对比叠加层。
    3.  VLM 分析视频和历史记录，提出新的参数矢量。
    4.  通过线搜索验证步长，接受首个能降低 L2 标记点误差的参数。
    5.  重复迭代直至达到评估预算（40 次）。

#### 3. 实验设计
*   **实验平台**：一个长 0.6 米、重 1.5 公斤的腱驱动鱼形机器人，在 2m x 3m 的水池中进行测试。
*   **数据集/场景**：在 8 个不同的电机频率（0.5Hz - 2.25Hz）下收集真实游泳视频，跟踪鱼身上的 9 个标记点。
*   **对比基准（Benchmarks）**：
    *   **BayesOpt**：基于高斯过程代理模型的贝叶斯优化。
    *   **CMA-ES**：协方差矩阵自适应进化策略。
    *   **Random Search**：随机搜索。
*   **消融实验**：包括“无步长搜索”（类似 Vid2Sid 方法）、“更换 VLM 模型（Gemini 3.1）”以及“单次 VLM 提议（热启动）”。

#### 4. 资源与算力
*   **硬件环境**：仿真在 AMD Ryzen 7 PRO 7840U CPU 的单核上运行。
*   **耗时统计**：
    *   单次 8 频率仿真评估约需 13 秒。
    *   单次 VLM 调用（含视频渲染、上传、推理、解析）约需 42 秒。
    *   Swim2Real 总运行时间约为 19 分钟（其中仿真 8 分钟，VLM 11 分钟），而基准方法约为 6 分钟。
*   **算力说明**：论文未提及使用了多高性能的 GPU，主要算力消耗在于 VLM 的 API 调用和 CPU 上的 MuJoCo 仿真。

#### 5. 实验数量与充分性
*   **实验规模**：所有方法均在 5 个随机种子下运行，每个种子限制 40 次仿真评估预算。
*   **充分性评价**：
    *   实验涵盖了参数校准精度、样本效率、下游强化学习（RL）任务性能以及物理实体部署。
    *   通过 5 个种子的标准差展示了方法的稳定性（Swim2Real 无离群值，而 CMA-ES 在 16 维空间下表现极不稳定）。
    *   对比了不同频率下的速度误差，验证了模型在目标函数之外的泛化能力。实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **精度领先**：Swim2Real 的速度匹配误差（MAE）为 7.4 mm/s，比次优方法（BayesOpt）低 43%。
*   **可靠性高**：在 16 维高维搜索中，Swim2Real 的 5 个种子结果高度一致（误差波动仅 3mm），而进化策略等方法容易崩溃。
*   **线搜索的必要性**：引入线搜索将 VLM 提议的接受率从 14% 提升至 42%，有效解决了 VLM 步长估计不准的问题。
*   **下游任务增益**：在校准后的仿真器中训练的 RL 策略，在真实迁移测试中游泳距离比 BayesOpt 提升了 12%，比 CMA-ES 提升了 90%。

#### 7. 优点与亮点
*   **端到端自动化**：无需人工干预即可同时校准 16 个跨领域的物理参数，打破了以往需要分阶段手动调优的限制。
*   **可解释性**：VLM 提供的推理过程（如识别出“高频锁定”现象）具有物理意义，这是标量优化器无法提供的。
*   **零样本迁移**：成功实现了从纯视频输入到物理机器人电机指令部署的闭环。

#### 8. 不足与局限
*   **误差下限限制**：由于模拟器将连续弯曲的尾部离散化为 5 个刚性关节，存在约 50mm 的固有建模误差底线。
*   **部署偏差**：物理部署采用开环控制，无法纠正硬件本身存在的转向偏差（如腱绳摩擦导致的左偏），导致真实轨迹与仿真存在弧度差异。
*   **成本问题**：VLM API 的调用增加了运行时间和经济成本，且对闭源模型（Gemini）有一定依赖。
*   **参数耦合**：VLM 有时会混淆流体阻力与关节刚度的影响，导致在某些回合中调整方向错误。

（完）

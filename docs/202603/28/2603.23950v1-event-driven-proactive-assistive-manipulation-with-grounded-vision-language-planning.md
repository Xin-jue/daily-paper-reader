---
title: Event-Driven Proactive Assistive Manipulation with Grounded Vision-Language Planning
title_zh: 基于具身视觉语言规划的事件驱动主动辅助操作
authors: "Fengkai Liu, Hao Su, Haozhuang Chi, Rui Geng, Congzhi Ren, Xuqing Liu, Yucheng Xu, Yuichi Ohsita, Liyun Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23950v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于辅助操作的落地视觉语言规划
tldr: 本研究提出了一种从请求驱动转向事件驱动的主动协作机器人框架。传统协作依赖用户指令，而该框架通过视觉语言规划，根据人类操作引起的空间状态转换（前后快照）来推断任务目标。系统包含事件监控器和规划器，能自动识别干预时机并生成动作序列。实验证明，该方法在桌面数字积木协作任务中表现出色，能有效提高主动完成率，并在不可解场景中合理等待，显著提升了人机协作的流畅性。
selection_source: fresh_fetch
motivation: 传统的协作机器人通常依赖显式指令，而人类伙伴能通过观察环境状态变化主动提供帮助。
method: 提出一种事件驱动框架，通过监控交互进度并提取状态转换前后的快照，利用视觉语言规划器推断目标并生成可执行动作。
result: 在真实桌面协作实验中，该框架在可解场景中提升了主动完成率，并在不可解场景中表现出更合理的等待行为。
conclusion: 基于状态变化证据的事件驱动主动协作模式，能显著增强机器人理解任务意图的能力和协作的智能性。
---

## 摘要
协作操作中的辅助通常由用户指令发起，这使得高层推理呈现为请求驱动模式。然而，在流畅的人类团队合作中，合作伙伴往往根据观察到的动作结果推断后续的有益步骤，而非被动等待指令。受此启发，我们提出从请求驱动辅助向事件驱动主动辅助的转变，即机器人动作由人机交互引起的工作空间状态转换触发，而非由用户提供的任务指令触发。为此，我们提出了一个事件驱动框架，通过事件监测器跟踪交互进度，并在事件完成后提取表征状态转换的稳定前后快照。基于这些稳定快照，规划器分析隐含的状态转换以推断任务级目标并决定是否进行干预；若需干预，则生成一系列辅助动作序列。为了确保输出的可执行性与可验证性，我们将动作限制在一组动作原语内，并通过整数 ID 引用目标对象。我们在真实的桌面数字积木协作任务上对该框架进行了评估，结果表明，显式的前后状态变化证据提升了机器人在可解场景中的主动完成能力，并在不可解场景中实现了合理的等待。

## Abstract
Assistance in collaborative manipulation is often initiated by user instructions, making high-level reasoning request-driven. In fluent human teamwork, however, partners often infer the next helpful step from the observed outcome of an action rather than waiting for instructions. Motivated by this, we introduce a shift from request-driven assistance to event-driven proactive assistance, where robot actions are initiated by workspace state transitions induced by human--object interactions rather than user-provided task instructions. To this end, we propose an event-driven framework that tracks interaction progress with an event monitor and, upon event completion, extracts stabilized pre/post snapshots that characterize the resulting state transition. Given the stabilized snapshots, the planner analyzes the implied state transition to infer a task-level goal and decide whether to intervene; if so, it generates a sequence of assistive actions. To make outputs executable and verifiable, we restrict actions to a set of action primitives and reference objects via integer IDs. We evaluate the framework on a real tabletop number-block collaboration task, demonstrating that explicit pre/post state-change evidence improves proactive completion on solvable scenes and appropriate waiting on unsolvable ones.

---

## 论文详细总结（自动生成）

### 论文总结：基于具身视觉语言规划的事件驱动主动辅助操作

#### 1. 核心问题与研究动机
*   **核心问题**：传统的人机协作机器人通常是“请求驱动型”（Request-Driven），即机器人必须在接收到显式指令（如语音或文字）后才开始行动。这限制了协作的流畅性。
*   **研究动机**：在人类团队合作中，伙伴往往能通过观察环境状态的变化（事件结果）主动推断下一步需求。本论文旨在实现从“请求驱动”向“事件驱动”（Event-Driven）的范式转变，让机器人能够根据人类操作引起的环境状态转换（State Transitions）主动发起辅助，而无需等待指令。

#### 2. 方法论
该框架结合了云端视觉语言模型（VLM）规划与本地具身执行，核心流程如下：
*   **事件监控与快照提取**：利用本地状态机监控工作空间。通过光流（Optical Flow）检测人类交互的开始与结束。在交互前提取“预事件快照”（$I^-$），在交互结束且场景稳定后提取“后事件快照”（$I^+$）。
*   **本地对象映射（Local Object Map）**：使用 FastSAM 对后事件快照进行实例分割，为每个物体分配唯一的整数 ID，并记录其几何中心和抓取位姿。
*   **云端 VLM 规划**：将 $(I^-, I^+)$ 快照对发送至云端 VLM（如 Qwen2VL）。模型通过对比前后状态差异推断人类意图（如：人类摆放了“1+2=”，意图是完成等式），并输出结构化的 JSON 动作序列（如 `pick(ID)`, `place(ID, relation)`）。
*   **ID 锚定与执行验证**：本地控制器将 VLM 输出的符号化 ID 映射回具体的度量坐标，执行动作后通过视觉反馈验证空间关系是否符合预期。

#### 3. 实验设计
*   **实验场景**：真实的桌面数字积木协作任务。人类摆放算式（加减乘除），机器人负责补全结果。
*   **场景分类**：
    *   **可解场景（Solvable）**：备选块中存在正确答案，机器人应主动放置。
    *   **不可解场景（Unsolvable）**：备选块中无正确答案，机器人应选择“等待（Wait）”。
*   **对比基准（Baselines）**：
    *   **Post-only**：仅提供交互后的单张快照。
    *   **Always-on**：无事件触发，周期性地对视频流进行采样推理。
    *   **Request-driven**：传统的指令驱动模式，提供显式的任务指令。

#### 4. 资源与算力
*   **本地端**：使用 Yahboom JetCobot 平台，搭载 **NVIDIA Jetson Orin 8GB** 处理感知与执行。
*   **云端规划**：VLM 推理运行在配备 **NVIDIA RTX 6000 Ada GPU** 的服务器上。
*   **模型**：使用了 **Qwen2VL-Instruct-32B** 作为核心规划器。
*   **训练时长**：文中未提及模型训练时长，主要利用了预训练 VLM 的零样本（Zero-shot）推理能力。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 **40 组** 真实机器人实验（20 组可解，20 组不可解）。
*   **充分性评价**：实验涵盖了多种算术逻辑和干扰情况，并对失败模式进行了详细的分类分析（如推理失败、执行失败、识别错误等）。虽然 40 组实验在统计学上规模不算巨大，但作为真实机器人系统的验证，其对比实验设计合理，能够客观反映“前后状态对比”对意图推断的重要性。

#### 6. 主要结论与发现
*   **事件驱动的优越性**：相比于单帧观察（Post-only），提供“前后对比”证据能显著提升机器人对人类意图的理解准确率。
*   **决策鲁棒性**：该框架在可解场景中达到了 **1.00 的推理成功率**；在不可解场景中，其表现也优于基准方法，能更准确地选择“等待”而非盲目干预。
*   **效率提升**：事件驱动模式避免了“Always-on”模式下的冗余计算，每次成功协作仅需一次 VLM 调用。

#### 7. 优点与亮点
*   **范式创新**：明确提出了从请求驱动到事件驱动的转变，更符合自然的人机协作逻辑。
*   **ID 锚定机制**：通过整数 ID 桥接了 VLM 的符号化输出与机器人的度量空间执行，有效解决了生成式模型输出坐标不准的问题。
*   **闭环验证**：引入了执行后的结果验证环节，增强了系统的可靠性。

#### 8. 不足与局限
*   **任务复杂度有限**：目前仅在结构化的数字积木任务上验证，对于更复杂、非结构化的家庭或工业场景（如重叠物体、动态环境）的泛化能力有待观察。
*   **延迟问题**：依赖云端大模型推理，虽然减少了本地算力需求，但在实时性要求极高的场景下可能存在网络延迟风险。
*   **样本偏差**：实验场景相对单一，且未涉及多个人类同时操作或人类中途修改意图的复杂交互情况。

（完）

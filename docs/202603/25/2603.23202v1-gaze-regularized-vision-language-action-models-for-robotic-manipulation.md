---
title: Gaze-Regularized Vision-Language-Action Models for Robotic Manipulation
title_zh: 用于机器人操作的注视正则化视觉-语言-动作模型
authors: "Anupam Pani, Yanchao Yang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23202v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于机械臂操作的视线正则化VLA模型
tldr: 本研究针对视觉-语言-动作（VLA）模型在精细机器人操作任务中缺乏主动视觉注意力分配的问题，提出了一种注视正则化训练框架。该方法通过KL散度将人类注视热图转化为补丁级分布，以此约束Transformer的内部注意力，使模型在不改变架构或增加推理开销的情况下，学习到人类的感知偏置。实验表明，该方法显著提升了操作任务的成功率、收敛速度和鲁棒性，同时增强了模型的可解释性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理精细操作时缺乏有效的视觉注意力分配机制，而人类注视数据蕴含了丰富的任务意图和规划信息。
method: 提出一种注视正则化框架，通过KL散度将人类注视模式引入Transformer的注意力机制，引导模型关注任务相关的关键特征。
result: "在机器人操作基准测试中实现了4-12%的性能提升，并表现出更快的训练收敛速度以及对光照和噪声的更强鲁棒性。"
conclusion: 引入人类感知先验能显著加速机器人学习过程，在提升任务性能的同时增强了系统的可解释性和部署效率。
---

## 摘要
尽管视觉-语言-动作（VLA）模型取得了长足进步，但由于当前模型缺乏主动视觉注意力分配机制，机器人在处理细粒度操作任务时仍显吃力。人类的注视自然地编码了意图、规划和执行模式，为引导机器人感知提供了强大的监督信号。我们提出了一种注视正则化训练框架，在不改变模型架构或增加推理开销的情况下，将 VLA 模型的内部注意力与人类视觉模式对齐。该方法将时间聚合的注视热图转换为补丁级（patch-level）分布，并通过 KL 散度对 Transformer 的注意力进行正则化，在保持部署效率的同时，建立了对任务相关特征的归纳偏置。当集成到现有的 VLA 架构中时，我们的方法在各项操作基准测试中实现了 4-12% 的性能提升。注视正则化模型能以更少的训练步数达到同等性能，并在光照变化和传感器噪声下保持鲁棒性。除了性能指标外，学习到的注意力模式还产生了反映人类策略的可解释可视化结果，增强了对机器人系统的信任。此外，该框架不需要眼动追踪设备，可直接应用于现有数据集。这些结果表明，人类感知先验可以显著加速机器人学习，同时提高任务性能和系统可解释性。

## Abstract
Despite advances in Vision-Language-Action (VLA) models, robotic manipulation struggles with fine-grained tasks because current models lack mechanisms for active visual attention allocation. Human gaze naturally encodes intent, planning, and execution patterns -- offering a powerful supervisory signal for guiding robot perception. We introduce a gaze-regularized training framework that aligns VLA models' internal attention with human visual patterns without architectural modifications or inference-time overhead. Our method transforms temporally aggregated gaze heatmaps into patch-level distributions and regularizes the transformer's attention through KL divergence, creating an inductive bias toward task-relevant features while preserving deployment efficiency. When integrated into existing VLA architectures, our approach yields 4-12% improvements across manipulation benchmarks. The gaze-regularized models reach equivalent performance with fewer training steps and maintain robustness under lighting variations and sensor noise. Beyond performance metrics, the learned attention patterns produce interpretable visualizations that mirror human strategies, enhancing trust in robotic systems. Moreover, our framework requires no eye-tracking equipment and applies directly to existing datasets. These results demonstrate that human perceptual priors can significantly accelerate robot learning while improving both task performance and system interpretability.

---

## 论文详细总结（自动生成）

### 论文总结：用于机器人操作的注视正则化视觉-语言-动作模型 (Gaze-Regularized VLA)

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA 等）在处理精细机器人操作时存在“被动感知”的局限性。这些模型通常将所有视觉信息视为整体处理，试图同时学习“看哪里”和“如何做”，导致训练效率低、收敛慢，且在复杂环境下缺乏鲁棒性。
相比之下，人类在操作时会通过**主动视觉注意力（注视）**来分配感知资源，注视点预示了任务意图和动作规划。本论文的核心动机是利用人类注视模式作为监督信号，引导 VLA 模型的内部注意力机制，使其从被动观察者转变为主动感知者，从而提升操作精度和学习效率。

#### 2. 论文提出的方法论
论文提出了一种**注视正则化（Gaze-Regularization）训练框架**，其核心思想是在训练阶段引入人类注视先验，而不改变模型架构，且推理时不增加额外开销。

*   **核心思想**：通过最小化模型内部注意力分布与人类注视分布之间的 KL 散度，将人类的感知偏置注入模型。
*   **关键技术细节**：
    1.  **注视先验生成**：由于现有机器人数据集缺乏眼动数据，研究者使用预训练的注视预测模型（如 GLC 网络）在视频序列上生成合成注视热图。
    2.  **时间聚合**：为了捕捉人类注视的预测性和稳定性，对连续帧的热图进行加权时间平均，生成平滑的注视先验。
    3.  **补丁级分布转换**：将连续的热图投影到与 Transformer 视觉 Token 相同的网格上（如 16x16），转化为离散概率分布 $G_{i,t}$。
    4.  **注意力正则化**：提取 Transformer 最后一层视觉-语言交叉注意力的分布 $S_{i,t}$，计算其与注视分布的 KL 散度损失：$L_{gaze} = \lambda \cdot D_{KL}(G_{i,t} \| S_{i,t})$。
    5.  **总损失函数**：$L_{total} = L_{action} + L_{gaze}$，其中 $L_{action}$ 是标准的动作预测损失（如流匹配损失）。

#### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **LIBERO Suite**：包括 LIBERO-Spatial（空间位置敏感任务）、LIBERO-Object（细粒度物体识别）、LIBERO-Goal（顺序推理）和 LIBERO-10（多任务泛化）。
    *   **ALOHA-Sim**：用于验证跨域泛化能力（如 Transfer Cube, Peg Insertion）。
    *   **真实世界实验**：在 Aloha 机器人上进行了抓取放置、多杯子连续操作等任务。
*   **对比方法**：
    *   **Baseline**：未加正则化的 Pi-0 和 OpenVLA 模型。
    *   **变体对比**：使用不同注视预测模型（如 DeepGaze）、均匀分布注视、以及带有视觉扰动（光照、噪声）的版本。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   文中提到了训练步数（如 10k, 20k, 30k steps），并指出注视正则化模型在 20k 步时即可达到基准模型 30k 步以上的性能，强调了其**样本效率**。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个 LIBERO 子集、ALOHA 仿真环境以及真实机器人部署，实验组数较多。
*   **消融实验**：非常充分。研究者测试了正则化系数 $\lambda$ 的敏感性、不同注视生成算法的影响、语言指令扰动、相机视角缺失以及视觉退化（光照和噪声）下的表现。
*   **客观性**：通过在两种不同的 VLA 架构（Pi-0 和 OpenVLA）上验证，证明了该方法的通用性和客观性。

#### 6. 主要结论与发现
*   **性能提升**：在 LIBERO 各项任务中，成功率普遍提升了 4-12%。例如在 LIBERO-Spatial 上从 85.9% 提升至 95.5%。
*   **收敛加速**：正则化模型在训练早期（10k-20k 步）就表现出显著优势，证明人类先验能加速策略学习。
*   **鲁棒性增强**：在光照变化和传感器噪声下，注视正则化模型表现出更强的稳定性，因为它学会了关注语义稳定的特征（如物体边界）。
*   **可解释性**：可视化结果显示，正则化后的模型注意力更集中于任务相关区域（如目标物体和容器），而非背景杂质，这增强了系统的可信度。

#### 7. 优点
*   **即插即用（Modular）**：无需修改模型架构，可直接应用于现有的 Transformer 类 VLA 模型。
*   **推理零开销**：注视引导仅发生在训练阶段，推理时不需要眼动仪或注视预测模型，保持了实时性。
*   **无需真实眼动数据**：通过合成注视解决了机器人领域缺乏大规模人类注视标注的问题。
*   **显著提升鲁棒性**：通过引导注意力，模型自然地过滤了环境中的视觉干扰。

#### 8. 不足与局限
*   **合成注视的偏差**：虽然 GLC 模型表现良好，但合成注视只是人类真实注视的近似，可能无法完全捕捉到专家操作时的微妙心理偏置。
*   **对预训练模型的依赖**：正则化效果受限于注视预测模型的质量，若预测模型在某些极端场景下失效，可能会引入错误的归纳偏置。
*   **任务覆盖范围**：虽然涵盖了多种操作，但对于需要极高动态响应或极复杂长程规划的任务，注视正则化的贡献程度仍需进一步验证。
*   **算力细节缺失**：缺乏具体的训练资源消耗报告，不便于其他研究者评估复现成本。

（完）

---
title: "Rewarding DINO: Predicting Dense Rewards with Vision Foundation Models"
title_zh: Rewarding DINO：利用视觉基础模型预测稠密奖励
authors: "Pierre Krack, Tobias Jülg, Wolfram Burgard, Florian Walter"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16978v1"
tags: ["query:profile-1"]
score: 9.0
evidence: 利用基础模型进行机器人操作的稠密奖励预测
tldr: Rewarding DINO 是一种基于视觉基础模型的语言条件奖励建模方法，旨在解决机器人操作中稠密奖励函数设计难的问题。该方法通过排序损失在 Meta-World+ 任务上训练，能够从图像中推断任务进度而非仅仅模仿专家轨迹。其模型轻量且具有良好的泛化能力，在仿真和真实世界中均表现出色，可直接用于强化学习算法。
selection_source: fresh_fetch
motivation: 传统的稠密奖励设计依赖仿真特权信息，而基于演示的奖励模型往往受限于特定轨迹且泛化性差。
method: 提出 Rewarding DINO，利用视觉基础模型和排序损失函数，通过语言指令引导学习通用的任务进度评估模型。
result: 在 Meta-World+ 任务中实现了高准确率和强相关性，并成功泛化至未见过的仿真场景及真实世界环境。
conclusion: 该研究证明了视觉基础模型可有效学习任务语义并提供可靠的稠密奖励，为无特权信息的机器人学习提供了新途径。
---

## 摘要
在机器人操作中，设计良好的稠密奖励函数不仅能指示任务是否完成，还能编码过程中的进度。通常，设计稠密奖励具有挑战性，且往往需要访问仅在仿真中可用、而在现实世界实验中无法获取的特权状态信息。这使得从摄像头图像推断任务状态信息的奖励预测模型变得极具吸引力。一种常见的方法是基于视觉相似性或序列帧排序，从专家演示中预测奖励。然而，这会使生成的奖励函数偏向于特定的解决方案，并导致其在演示未覆盖的状态下定义不明。在这项工作中，我们介绍了 Rewarding DINO，这是一种语言条件化的奖励建模方法，它学习的是实际的奖励函数而非特定的轨迹。该模型紧凑的尺寸使其能够以较低的计算开销直接替代解析奖励函数。我们使用基于排名的损失函数，在从 24 个 Meta-World+ 任务中采样的数据上训练模型，并评估了成对准确率、排名相关性和校准度。Rewarding DINO 在训练集任务中取得了具有竞争力的性能，并能泛化到仿真和现实世界中的新设置，表明其学习到了任务语义。我们还使用现成的强化学习算法测试了该模型，以解决 Meta-World+ 训练集中的任务。

## Abstract
Well-designed dense reward functions in robot manipulation not only indicate whether a task is completed but also encode progress along the way. Generally, designing dense rewards is challenging and usually requires access to privileged state information available only in simulation, not in real-world experiments. This makes reward prediction models that infer task state information from camera images attractive. A common approach is to predict rewards from expert demonstrations based on visual similarity or sequential frame ordering. However, this biases the resulting reward function towards a specific solution and leaves it undefined in states not covered by the demonstrations. In this work, we introduce Rewarding DINO, a method for language-conditioned reward modeling that learns actual reward functions rather than specific trajectories. The model's compact size allows it to serve as a direct replacement for analytical reward functions with comparatively low computational overhead. We train our model on data sampled from 24 Meta-World+ tasks using a rank-based loss and evaluate pairwise accuracy, rank correlation, and calibration. Rewarding DINO achieves competitive performance in tasks from the training set and generalizes to new settings in simulation and the real world, indicating that it learns task semantics. We also test the model with off-the-shelf reinforcement learning algorithms to solve tasks from our Meta-World+ training set.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rewarding DINO: Predicting Dense Rewards with Vision Foundation Models》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在没有仿真环境“特权状态信息”（如物体精确坐标）的情况下，仅通过视觉图像和语言指令，为机器人操作任务生成高质量、通用的**稠密奖励函数**（Dense Reward Functions）。
*   **研究动机**：
    *   **奖励工程难**：手动设计稠密奖励既费时又依赖仿真环境的内部信息，难以直接应用于现实世界。
    *   **现有方法局限**：目前的视觉奖励模型（如基于演示的方法）往往强行让机器人模仿特定轨迹，导致模型在未见过的状态下失效，且无法处理从零开始的强化学习（RL）。
    *   **基础模型潜力**：预训练的视觉基础模型（如 DINO）已具备丰富的语义和空间特征，理论上可以转化为任务进度的指示器。

### 2. 方法论：核心思想与技术细节
Rewarding DINO 的核心是将奖励预测建模为一个**语言条件下的监督学习问题**，通过比较状态对的优劣来学习奖励函数。
*   **核心架构**：
    *   **视觉编码器**：采用冻结的 **DINOv3 (ViT-S/16)**，利用其强大的稠密特征表示能力。
    *   **语言编码器**：采用冻结的 **SBERT (all-MiniLM-L6-v2)**，将自然语言指令转化为目标嵌入。
    *   **特征融合**：使用 **FiLM (特征线性调制)** 层将语言嵌入注入到视觉特征中，相比交叉注意力机制更轻量高效。
    *   **多视角输入**：同时接收机械臂腕部相机和侧面相机的图像，以减少遮挡带来的歧义。
*   **关键算法与公式**：
    *   **成对排序损失 (Pairwise Ranking Loss)**：不直接回归绝对奖励值，而是学习状态间的偏好关系。
    *   **损失函数**：$\ell(\Delta s) = \log(1 + \exp(-y \Delta s / \tau))$，其中 $y$ 是真实奖励的排序标签，$\Delta s$ 是模型预测的分数差，$\tau$ 是温度参数。
    *   **数据采样策略**：结合专家轨迹和随机轨迹，通过“坐标+奖励”双重分桶（Binning）进行去重，确保数据集覆盖了从初始状态到完成状态的整个空间。

### 3. 实验设计
*   **数据集与场景**：
    *   **Meta-World+**：使用了 24 个任务（如开门、拨动开关、按按钮等），包含语言变体。
    *   **仿真 Pick-Cube**：用于模拟实验室真实环境的抓取任务。
    *   **真实世界实验**：在 Franka Research 3 机器人上进行实机测试，验证模拟到现实（Sim-to-Real）的迁移能力。
*   **基准 (Benchmark) 与对比**：
    *   对比了**解析奖励（Ground Truth）**、**稀疏奖励**以及基于势能的奖励塑造（PBRS）方法。
    *   评估指标包括：成对准确率（Pairwise Accuracy）、肯德尔秩相关系数（Kendall’s tau）和期望校准误差（ECE）。

### 4. 资源与算力
*   **算力支持**：文中提到使用了埃尔朗根国家高性能计算中心（NHR@FAU）的资源。
*   **具体配置**：
    *   **未明确说明**具体的 GPU 型号和数量。
    *   **训练细节**：训练了 3000 个 epoch，每个 epoch 包含 100,000 个样本对，Batch Size 为 512。
    *   **模型规模**：视觉骨干网络为 21M 参数的轻量级 ViT，奖励预测头是一个小型 MLP，推理开销极低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖 24 个不同的操作任务。
    *   数据集包含约 17 万个去重后的步骤和 81 万张图像。
    *   进行了强化学习闭环训练实验（PPO 算法）。
*   **充分性评价**：实验设计较为充分。作者不仅验证了模型在训练集上的表现，还专门设计了**任务泛化**（如只学过“开抽屉”去预测“关抽屉”）和**跨模态泛化**（Sim-to-Real）实验。通过校准曲线（Reliability Diagram）验证了概率解释的有效性，体现了严谨的科学态度。

### 6. 主要结论与发现
*   **视觉基础模型有效性**：预训练的 DINO 特征足以在不微调骨干网络的情况下，通过极小的预测头恢复出精确的稠密奖励信号。
*   **排序学习优于回归**：采用成对排序损失能更好地处理不同任务间奖励量级的差异。
*   **支持从零训练**：该模型预测的奖励可以作为势能函数（PBRS），引导 PPO 算法在多个任务中从随机策略进化到成功，性能接近使用仿真特权信息的解析奖励。
*   **泛化能力**：模型对语言指令的同义词表述具有鲁棒性，并能在一定程度上理解任务的反向语义。

### 7. 优点与亮点
*   **轻量化与实时性**：模型体积小，可作为解析奖励函数的“掉入式”（Drop-in）替代品，计算开销低。
*   **理论完备性**：引入了基于势能的奖励塑造（PBRS）理论，保证了在理想情况下添加预测奖励不会改变最优策略。
*   **数据效率**：通过分桶去重技术，在相对较小的数据规模下实现了较强的泛化性能。

### 8. 不足与局限
*   **非马尔可夫风险**：由于图像仅是部分观测（Partial Observation），模型可能给视觉相似但物理状态不同的帧分配相同奖励，这在理论上违反了 RL 的马尔可夫性质。
*   **成功检测缺失**：模型擅长预测进度（连续值），但在判断任务是否彻底完成（离散成功检测）方面仍有提升空间。
*   **任务泛化跨度有限**：虽然在相似任务间有泛化，但对于完全未见过的物体类别或大幅度的视觉背景改变，其表现仍有待验证。

（完）

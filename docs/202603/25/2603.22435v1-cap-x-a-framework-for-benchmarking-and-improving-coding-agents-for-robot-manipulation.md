---
title: "CaP-X: A Framework for Benchmarking and Improving Coding Agents for Robot Manipulation"
title_zh: CaP-X：一个用于基准测试和提升机器人操纵编程智能体的框架
authors: "Max Fu, Justin Yu, Karim El-Refai, Ethan Kou, Haoru Xue, Huang Huang, Wenli Xiao, Guanzhi Wang, Fei-Fei Li, Guanya Shi, Jiajun Wu, Shankar Sastry, Yuke Zhu, Ken Goldberg, Linxi \"Jim\" Fan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22435v1"
tags: ["query:q14"]
score: 9.0
evidence: 用于基准测试和改进机器人操作代码智能体的框架
tldr: 本研究针对机器人操作中的“代码即策略”（CaP）方法，提出了CaP-X开源框架。该框架包含交互式环境CaP-Gym和评估基准CaP-Bench，用于系统研究代码代理。通过对12个前沿模型的评估，发现其性能高度依赖人工设计的抽象，但可通过增加测试时计算（如多轮交互、反馈、技能合成）来弥补。基于此，研究者开发了免训练的CaP-Agent0和支持强化学习的CaP-RL，在仿真和真实机器人上均实现了高可靠性。
selection_source: fresh_fetch
motivation: 旨在系统性地评估和提升代码即策略（Code-as-Policy）代理在机器人操作任务中的自主控制能力与鲁棒性。
method: 提出了包含交互环境、评估基准、免训练代理框架及强化学习方案的CaP-X综合平台。
result: 实验发现模型性能受限于人工先验，但通过多轮交互、执行反馈和自动技能合成等推理策略可显著提升低级原语下的表现。
conclusion: CaP-X为具身智能代码代理的研究提供了标准化平台，证明了结合推理优化与强化学习能有效提升机器人任务的成功率。
---

## 摘要
“代码即策略”（Code-as-Policy）探讨了可执行代码如何补充数据密集型的视觉-语言-动作（VLA）方法，但其作为具身操纵自主控制器的有效性仍未得到充分探索。我们提出了 CaP-X，这是一个用于系统研究机器人操纵中“代码即策略”智能体的开源框架。其核心是 CaP-Gym，一个智能体通过合成并执行组合了感知与控制原语的程序来控制机器人的交互式环境。在此基础上，CaP-Bench 在不同的抽象级别、交互程度和感知落地方面评估了前沿的语言及视觉-语言模型。通过对 12 个模型的评估，CaP-Bench 揭示了一个一致的趋势：性能随人工设计的抽象而提升，但随着这些先验知识的移除而下降，暴露了对设计者脚手架的依赖。同时，我们观察到这种差距可以通过扩展智能体测试时计算来弥补——通过多轮交互、结构化执行反馈、视觉差异化、自动技能合成和集成推理——即使智能体在底层原语上运行，也能显著提高鲁棒性。这些发现使我们推导出 CaP-Agent0，这是一个无需训练的框架，在仿真和真实具身环境下的多项操纵任务中达到了人类水平的可靠性。我们进一步引入了 CaP-RL，表明具有可验证奖励的强化学习可以提高成功率，并能以极小的差距实现从仿真到现实的迁移。总之，CaP-X 为推进具身编程智能体提供了一个规范的开源平台。

## Abstract
"Code-as-Policy" considers how executable code can complement data-intensive Vision-Language-Action (VLA) methods, yet their effectiveness as autonomous controllers for embodied manipulation remains underexplored. We present CaP-X, an open-access framework for systematically studying Code-as-Policy agents in robot manipulation. At its core is CaP-Gym, an interactive environment in which agents control robots by synthesizing and executing programs that compose perception and control primitives. Building on this foundation, CaP-Bench evaluates frontier language and vision-language models across varying levels of abstraction, interaction, and perceptual grounding. Across 12 models, CaP-Bench reveals a consistent trend: performance improves with human-crafted abstractions but degrades as these priors are removed, exposing a dependence on designer scaffolding. At the same time, we observe that this gap can be mitigated through scaling agentic test-time computation--through multi-turn interaction, structured execution feedback, visual differencing, automatic skill synthesis, and ensembled reasoning--substantially improves robustness even when agents operate over low-level primitives. These findings allow us to derive CaP-Agent0, a training-free framework that recovers human-level reliability on several manipulation tasks in simulation and on real embodiments. We further introduce CaP-RL, showing reinforcement learning with verifiable rewards improves success rates and transfers from sim2real with minimal gap. Together, CaP-X provides a principled, open-access platform for advancing embodied coding agents.

---

## 论文详细总结（自动生成）

### CaP-X：机器人操纵编程智能体基准测试与提升框架总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的机器人控制依赖专家编写的复杂程序，而新兴的视觉-语言-动作（VLA）模型虽然强大但缺乏可解释性且泛化性受限。**“代码即策略”（Code-as-Policy, CaP）**试图利用大语言模型（LLM）生成可执行代码来控制机器人。然而，现有研究大多依赖人类预设的高级原语（如“堆叠物体”），这掩盖了智能体真实的推理能力。
**研究动机：** 系统性地探究编程智能体在不同抽象级别、交互模式和感知反馈下的表现，并研究如何通过增加“测试时计算”（Test-time Computation）来减少对人工设计原语的依赖。

#### 2. 论文提出的方法论
论文提出了 **CaP-X** 框架，包含以下核心组件：
*   **CaP-Gym：** 一个基于 Gymnasium 的交互式环境，将物理仿真器（或真实机器人）与 Python 代码执行器（REPL）绑定。智能体通过调用感知（如 SAM3, Molmo 2）和控制（如 PyRoki 逆运动学）原语来完成任务。
*   **CaP-Bench：** 一个多维度的基准测试，将评估分为 8 个层级（S1-S4 单轮，M1-M4 多轮），涵盖从特权状态到原始 RGB-D 输入、从高级宏指令到低级原子原语的各种场景。
*   **CaP-Agent0：** 一个免训练的智能体框架，核心技术包括：
    *   **视觉差异模块（VDM）：** 将视觉观察转化为描述场景变化的结构化文本。
    *   **自动技能合成：** 从成功的执行轨迹中自动提取并固化通用的低级逻辑函数，形成技能库。
    *   **并行推理：** 同时采样多个模型的生成结果并进行集成（Ensemble）。
*   **CaP-RL：** 引入可验证奖励的强化学习（RLVR），使用 **GRPO（组相对策略优化）** 算法对代码生成模型进行后训练，提升其逻辑严密性和策略鲁棒性。

#### 3. 实验设计
*   **数据集与场景：** 集成了 187 个任务，来源于 RoboSuite、LIBERO-PRO 和 BEHAVIOR 仿真器，涵盖桌面操纵、双臂协作和移动操纵。
*   **基准测试（Benchmark）：** 重点评估了 7 个核心任务（如方块提升、堆叠、溢出擦除、插销等）。
*   **对比方法：** 
    *   **模型：** 评估了 12 个前沿模型，包括 GPT-5.2、GPT-o1、Gemini-3-Pro、Claude 4.5、DeepSeek-V3.1、Qwen-235B 等。
    *   **基线：** 与人类专家编写的代码、OpenVLA、$\pi_0$ 和 $\pi_{0.5}$ 等先进 VLA 模型进行对比。

#### 4. 资源与算力
*   **算力说明：** 论文未详细列出所有实验的总 GPU 小时数。
*   **训练细节：** 在 CaP-RL 部分，提到对 Qwen2.5-Coder-7B 进行后训练，每个任务进行 50 次迭代。
*   **推理：** 所有的感知原语（SAM3, Molmo 等）作为独立服务运行，支持高吞吐量并行评估。

#### 5. 实验数量与充分性
*   **实验规模：** 每个任务层级进行 **100 次独立试验**，这在机器人领域属于较大规模的统计评估。
*   **充分性：** 实验涵盖了从仿真到现实（Sim-to-Real）的迁移，并在 Franka Panda 和 AgiBot G1 机器人上进行了零样本（Zero-shot）验证。
*   **消融实验：** 针对 CaP-Agent0 的各个组件（VDM、技能库、并行查询）做了详细的消融分析，证明了各部分的增益。

#### 6. 主要结论与发现
*   **抽象依赖性：** 模型性能随原语抽象级别的降低而显著下降，说明现有 CaP 方法高度依赖人工设计的“脚手架”。
*   **反馈的重要性：** 多轮交互和结构化文本反馈（VDM）比直接输入原始图像（M2 层级）更能提升代码生成的成功率。
*   **测试时计算的威力：** 通过多轮调试、技能合成和集成推理，智能体在低级原语上的表现可以达到甚至超过人类专家水平。
*   **RL 增益：** 强化学习能显著减少代码编译错误，并让模型学会更复杂的几何推理（如动态计算堆叠高度）。

#### 7. 优点（亮点）
*   **系统性强：** 首次系统地解构了代码智能体在机器人控制中的能力边界。
*   **开源贡献：** 提供了统一的 CaP-Gym 接口，降低了开发具身编程智能体的门槛。
*   **技能进化：** 自动合成技能库的思想实现了从“人工设计 API”向“智能体自发构建 API”的转变。
*   **Sim-to-Real 鸿沟小：** 由于代码操作的是抽象 API 而非原始像素，CaP-RL 表现出极强的跨环境迁移能力。

#### 8. 不足与局限
*   **接触密集型任务挑战：** 对于需要高频视觉伺服或复杂力反馈的任务（如精细插入、倾倒液体），纯编程控制仍显脆弱。
*   **感知模块依赖：** 智能体的表现高度受限于底层感知原语（如 SAM3）的准确性，感知噪声是主要的失败诱因。
*   **实时性：** 多轮交互和调用大型 VLM 进行视觉差异分析会带来显著的推理延迟，难以处理高速动态场景。

（完）

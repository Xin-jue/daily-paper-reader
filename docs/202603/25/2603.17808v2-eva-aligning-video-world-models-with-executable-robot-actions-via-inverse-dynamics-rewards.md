---
title: "EVA: Aligning Video World Models with Executable Robot Actions via Inverse Dynamics Rewards"
title_zh: EVA：通过逆动力学奖励将视频世界模型与可执行机器人动作对齐
authors: "Ruixiang Wang, Qingming Liu, Yueci Deng, Guiliang Liu, Zhen Liu, Kui Jia"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17808v2"
tags: ["query:q13"]
score: 9.0
evidence: 将视频世界模型与可执行机器人动作对齐以进行任务规划
tldr: 针对视频世界模型在机器人任务中生成的视频虽视觉连贯但物理不可执行（可执行性差距）的问题，本文提出了EVA框架。该框架利用逆动力学模型（IDM）作为奖励模型，通过强化学习对视频生成模型进行后训练对齐。EVA通过评估生成视频所诱导的动作序列的平滑度及是否违反约束，显著减少了物理伪影，提升了机器人在仿真和真实环境中的任务成功率。
selection_source: fresh_fetch
motivation: 现有的视频世界模型生成的视频往往缺乏物理一致性，导致逆动力学模型解码出的机器人动作不稳定或不可执行。
method: 提出EVA框架，将逆动力学模型转化为奖励模型，通过强化学习优化视频生成模型，使其生成的视频更符合物理规律和机器人运动约束。
result: 在RoboTwin基准测试和真实双臂机器人实验中，EVA有效减少了生成视频中的物理伪影，并显著提高了下游任务的执行成功率。
conclusion: 通过将可执行性约束引入视频生成模型的对齐过程，可以有效弥合视觉生成与物理控制之间的差距，增强世界模型的实用性。
---

## 摘要
视频生成模型越来越多地被用作机器人的世界模型。在这种模式下，模型根据当前观测和任务指令生成未来的视觉推演（rollout），并由逆动力学模型（IDM）将生成的帧转换为可执行的机器人动作。然而，目前的视频世界模型缺乏显式的可执行性约束。因此，视觉上连贯的推演可能仍然违反刚体和运动学一致性，在由 IDM 解码时产生不稳定或不可行的控制指令。我们将这种视觉生成与物理可执行控制之间的不匹配称为“可执行性差距”（executability gap）。虽然在推理阶段可以通过拒绝采样等技术缓解这一差距，但由于视频生成的成本极高，此类方法效率较低。在本文中，我们将可执行性差距作为训练信号，并引入了可执行视频对齐（Executable Video Alignment, EVA），这是一种用于对齐视频世界模型的强化学习后训练框架。EVA 在真实机器人轨迹上训练逆动力学模型，并将其重新用作奖励模型，通过生成的视频所诱导的动作序列来评估视频，在鼓励以速度、加速度和加加速度（jerk）衡量的平滑运动的同时，惩罚违反具身约束的动作。重要的是，即使生成的视频包含严重的视觉伪影，该奖励仍然具有信息量，因为此类伪影通常会转化为不稳定或超出范围的动作。在 RoboTwin 基准测试和真实双臂机器人上的实验表明，EVA 减少了生成推演中特定于具身的伪影，并提高了下游任务执行的成功率。

## Abstract
Video generative models are increasingly used as world models for robotics, where a model generates a future visual rollout conditioned on the current observation and task instruction, and an inverse dynamics model (IDM) converts the generated frames into executable robot actions. However, current video world models lack explicit executability constraints. As a result, visually coherent rollouts may still violate rigid-body and kinematic consistency, producing unstable or infeasible control commands when decoded by an IDM. We refer to this mismatch between visual generation and physically executable control as the executability gap. While this gap can be mitigated at inference time using techniques such as rejection sampling, such approaches are inefficient due to the high cost of video generation. In this paper, we leverage the executability gap as a training signal and introduce Executable Video Alignment (EVA), a reinforcement-learning post-training framework for aligning video world models. EVA trains an inverse dynamics model on real robot trajectories and repurposes it as a reward model that evaluates generated videos through the action sequences they induce, encouraging smooth motions measured by velocity, acceleration, and jerk while penalizing actions that violate embodiment constraints. Importantly, the reward remains informative even when generated videos contain severe visual artifacts, since such artifacts typically translate into unstable or out-of-bound actions. Experiments on the RoboTwin benchmark and a real bimanual robot show that EVA reduces embodiment-specific artifacts in generated rollouts and improves downstream task execution success.

---

## 论文详细总结（自动生成）

### 论文总结：EVA - 通过逆动力学奖励将视频世界模型与可执行机器人动作对齐

#### 1. 核心问题与研究动机
*   **核心问题**：现有的机器人视频世界模型（Video World Models）虽然能生成视觉上连贯的视频，但往往忽略了物理上的**可执行性（Executability）**。
*   **研究背景**：在“视频生成+逆动力学模型（IDM）”的解耦架构中，视频模型负责规划视觉轨迹，IDM 负责将视频转为动作。然而，生成的视频常出现机械臂变形、关节突变或违反运动学约束的现象，导致 IDM 解码出抖动、越界或不可行的控制指令。
*   **核心概念**：论文定义了**“可执行性差距”（Executability Gap）**，即视觉生成的流畅性与物理执行的可行性之间的不匹配。

#### 2. 方法论（EVA 框架）
*   **核心思想**：将逆动力学模型（IDM）从单纯的解码器转变为**奖励模型**，通过强化学习（RL）对视频生成模型进行后训练对齐，使其生成的视频在动作空间内满足物理约束。
*   **关键技术细节**：
    *   **逆动力学模型 (IDM)**：在真实机器人数据上训练，输入连续视频帧，输出对应的机器人动作。采用空间 Softmax 层提取特征，以提高对生成视频中伪影的鲁棒性。
    *   **基于 IDM 的可执行性奖励**：
        1.  **平滑度惩罚**：利用有限差分计算动作序列的速度、加速度和加加速度（Jerk），通过 Huber 损失函数惩罚剧烈波动。
        2.  **边界惩罚**：惩罚超出机器人具身物理极限（如关节限位、最大速度）的动作。
        3.  **奖励函数**：将上述惩罚项映射为有界的奖励信号 $R(V)$。
    *   **策略优化 (GRPO)**：采用组相对策略优化（Group Relative Policy Optimization），针对流匹配（Flow-matching）模型构建随机过程，通过采样多组视频并计算相对优势（Advantage）来更新视频生成模型的 LoRA 参数。

#### 3. 实验设计
*   **实验场景与基准**：
    *   **仿真环境**：使用 **RoboTwin 2.0** 基准测试，包含 21 个双臂操作任务（如开笔记本、按订书机、转开关等）。
    *   **真实世界**：使用 Agilex CobotMagic 双臂机器人平台，涵盖 5 个训练任务和 5 个分布外（OOD）任务（如倒水、擦托盘、折衣服）。
*   **对比方法**：
    *   **模仿学习/VLA 模型**：ACT, Diffusion Policy (DP), RDT, $\pi_0$。
    *   **视频世界模型**：Vidar, GE-Act, 以及未经过 RL 对齐的基准模型 EVA (w/o RL)。
*   **评估指标**：任务成功率、人类评估的视觉质量（运动学合理性、交互合理性、指令遵循度）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：
    *   **仿真环境**：GRPO 训练约需 **6 天**（136 个优化步骤，每步 4 个内部 epoch）。
    *   **真实环境**：GRPO 训练约需 **3 天**（46 个优化步骤）。
*   **模型规模**：基于 Wan2.1-14B 视频生成模型，使用 LoRA（Rank 32）进行微调。

#### 5. 实验数量与充分性
*   **实验规模**：在仿真中测试了 21 个任务，每个任务 20 次试验；在真实世界中测试了 10 个任务，每个任务 20 次试验。
*   **充分性分析**：
    *   **消融实验**：验证了 IDM 架构中空间 Softmax 的必要性。
    *   **奖励有效性分析**：通过实验证明了奖励分数与视频视觉质量及下游执行成功率之间存在正相关关系。
    *   **多样性**：涵盖了从简单放置到复杂接触、从刚体到柔性物体的多种任务，实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **弥合差距**：EVA 显著减少了生成视频中的机械臂变形和运动不连续性，将运动学合理性评分提升了约 20.9%。
*   **性能提升**：在 RoboTwin 仿真中，EVA 的平均成功率（52.6%）优于所有对比的 VLA 和视频模型；在真实世界 OOD 任务中，EVA 表现出极强的泛化能力（成功率 60%），远超传统模仿学习方法。
*   **奖励鲁棒性**：即使视频存在视觉瑕疵，IDM 提取的动作特征仍能提供有效的训练信号，引导模型向物理可执行的方向优化。

#### 7. 优点与亮点
*   **创新视角**：首次提出将 IDM 作为奖励模型来解决视频世界模型的可执行性问题，而非仅仅关注视觉保真度。
*   **密集奖励**：相比于稀疏的任务成功奖励，基于动作序列的平滑度和约束奖励提供了更密集的监督信号。
*   **解耦优势**：保留了视频模型强大的互联网知识（泛化性），同时通过后训练引入了特定具身的物理约束。

#### 8. 不足与局限
*   **动力学缺失**：目前的奖励主要关注运动学（位置、速度），未显式建模力、摩擦力或力矩，对于高精度接触任务可能不足。
*   **计算开销**：基于扩散/流匹配的视频生成推理成本高，难以实现高频闭环控制（目前采用退避视界执行）。
*   **奖励作弊（Reward Hacking）**：论文提到过度优化可能导致模型产生“静止不动”或“肢体拉长”等异常行为以获取高分，需依赖早停策略。

（完）

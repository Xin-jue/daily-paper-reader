---
title: "SOFTMAP: Sim2Real Soft Robot Forward Modeling via Topological Mesh Alignment and Physics Prior"
title_zh: SOFTMAP：基于拓扑网格对齐与物理先验的虚实迁移软体机器人正向建模
authors: "Ziyong Ma, Uksang Yoo, Jonathan Francis, Weiming Zhi, Jeffrey Ichnowski, Jean Oh"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19384v1"
tags: ["query:q8"]
score: 9.0
evidence: 软体机器人操作器的仿真到现实前向建模
tldr: 针对软体机器人因材料非线性和制造差异导致的建模难题，本文提出SOFTMAP框架。该框架通过ARAP拓扑对齐将仿真与真实点云统一，结合仿真预训练的MLP模型、真实数据驱动的残差修正网络及线性驱动校准层，实现了腱驱动软体手指的高精度实时3D正向建模，显著提升了形状预测精度与远程操作表现。
selection_source: fresh_fetch
motivation: 软体机器人由于复杂的非线性材料特性和制造变异性，难以通过低维驱动指令实现精确且实时的3D正向建模。
method: 结合ARAP拓扑对齐、仿真预训练模型、基于真实观测的残差修正网络以及线性驱动校准层，构建了一个Sim2Real学习框架。
result: "在物理硬件上达到了3.786mm的形状预测精度和30 FPS的推理速度，并将远程操作任务成功率提升了36.5%。"
conclusion: SOFTMAP为软体操纵器的3D正向建模与控制提供了一种数据高效且高精度的解决方案。
---

## 摘要
尽管软体机器人操作臂相比刚性机器人具有显著优势，包括固有的顺应性、安全的人机交互以及适应复杂几何形状的能力，但由于迟滞等非线性材料现象和制造差异，基于低维驱动指令的精确正向建模仍然是一个开放性挑战。我们提出了 SOFTMAP，这是一个用于腱驱动软体手指操作臂实时三维正向建模的虚实迁移（sim-to-real）学习框架。SOFTMAP 结合了四个组件：（1）基于尽可能刚性（ARAP）的拓扑对齐，将仿真和真实的点云投影到一个共享的、拓扑一致的顶点空间；（2）一个在仿真数据上预训练的轻量级 MLP 正向模型，用于将伺服指令映射到完整的三维手指几何形状；（3）一个在少量真实观测数据上训练的残差修正网络，用于预测每个顶点的位移场，以补偿虚实差异；以及（4）一个闭式线性驱动校准层，实现了 30 FPS 的实时推理。我们在仿真和物理硬件上对 SOFTMAP 进行了评估，实现了最先进的形状预测精度，仿真中的 Chamfer 距离为 0.389 mm，硬件上为 3.786 mm，在多条目标路径上实现了毫米级的手指尖轨迹跟踪，且远程操作任务成功率比基准线提高了 36.5%。我们的结果表明，SOFTMAP 为软体操作臂的三维正向建模和控制提供了一种高效利用数据的方法。

## Abstract
While soft robot manipulators offer compelling advantages over rigid counterparts, including inherent compliance, safe human-robot interaction, and the ability to conform to complex geometries, accurate forward modeling from low-dimensional actuation commands remains an open challenge due to nonlinear material phenomena such as hysteresis and manufacturing variability. We present SOFTMAP, a sim-to-real learning framework for real-time 3D forward modeling of tendon-actuated soft finger manipulators. SOFTMAP combines four components: (1) As-Rigid-As-Possible (ARAP)-based topological alignment that projects simulated and real point clouds into a shared, topologically consistent vertex space; (2) a lightweight MLP forward model pretrained on simulation data to map servo commands to full 3D finger geometry; (3) a residual correction network trained on a small set of real observations to predict per-vertex displacement fields that compensate for sim-to-real discrepancies; and (4) a closed-form linear actuation calibration layer enabling real-time inference at 30 FPS. We evaluate SOFTMAP on both simulated and physical hardware, achieving state-of-the-art shape prediction accuracy with a Chamfer distance of 0.389 mm in simulation and 3.786 mm on hardware, millimeter-level fingertip trajectory tracking across multiple target paths, and a 36.5% improvement in teleoperation task success over the baseline. Our results show that SOFTMAP provides a data-efficient approach for 3D forward modeling and control of soft manipulators.

---

## 论文详细总结（自动生成）

### SOFTMAP 论文结构化深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
软体机器人操作器（如软体手指）具有高顺应性和安全性，但其**精确的正向建模（Forward Modeling）**一直是难题。核心挑战在于：
*   **复杂的非线性特性**：软体材料存在迟滞（Hysteresis）、蠕变和制造变异性，难以用简单的解析公式描述。
*   **虚实鸿沟（Sim-to-Real Gap）**：物理仿真（如有限元分析）虽能生成大量数据，但往往忽略了现实中的物理细节；而纯数据驱动方法则需要昂贵的真实硬件标注数据。
*   **实时性要求**：闭环控制需要模型在保持高精度的同时实现快速推理。

**SOFTMAP** 的研究动机是结合物理仿真的先验知识与少量真实观测数据，构建一个既准确又实时的 3D 正向模型。

#### 2. 方法论：核心思想与技术细节
SOFTMAP 提出了一个四阶段的虚实迁移学习框架：
*   **ARAP 拓扑对齐**：利用“尽可能刚性”（As-Rigid-As-Possible, ARAP）变形算法，将仿真生成的网格和真实扫描的点云投影到一个共享的、拓扑一致的顶点空间。这解决了不同来源数据在采样密度和顶点对应关系上的不一致问题。
*   **仿真预训练（物理先验）**：在 SOFA 仿真框架生成的清洁数据上预训练一个轻量级 MLP（FingerMLP）。该模型学习从 2D 伺服指令到 548 个顶点 3D 坐标的映射。
*   **残差修正网络**：在少量真实观测数据上训练一个微型 MLP，预测每个顶点的位移场（Displacement Field）。通过 `真实形状 = 仿真预测 + 残差修正` 的方式，补偿未建模的物理特性。
*   **线性驱动校准层**：通过闭式线性映射对齐伺服电机的编码器刻度与仿真驱动空间，确保模型在真实硬件上的输入一致性，并支持 **30 FPS** 的实时推理。

#### 3. 实验设计
*   **实验对象**：腱驱动（Tendon-actuated）软体手指（基于 MOE 开源平台）。
*   **数据集/场景**：
    *   **仿真**：SOFA 框架生成的超弹性材料变形数据。
    *   **现实**：通过双相机多视图重建（VGGT + SAM 算法）获取的 3D 点云。
*   **Benchmark 与对比方法**：
    *   **形状预测对比**：对比了 DeepSoRo（之前的 SOTA 方法）、线性模型、XGBoost 以及基于 Laplacian 编码的模型。
    *   **下游任务**：轨迹跟踪（绘制字母 S、H、O、E）和远程操作（推挤 T 形块、立方体、三角棱柱）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练总时长。
*   **性能表现**：文中提到该框架在推理阶段可达到 **30 FPS**，证明了其轻量化 MLP 架构在实时控制中的可行性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   **形状预测**：在仿真和真实环境下分别进行了定量评估（Chamfer 距离和顶点误差）。
    *   **消融实验**：对比了“仅仿真模型”与“带残差修正模型”的表现，验证了残差模块减少了 33.4% 的误差。
    *   **应用实验**：包含 4 种轨迹跟踪任务和 3 种不同几何形状物体的远程操作推挤实验。
*   **充分性评价**：实验设计较为全面，涵盖了从底层几何精度到高层任务表现的完整链路。对比实验选择了领域内具有代表性的 DeepSoRo，且包含了消融分析，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **高精度**：SOFTMAP 在真实硬件上的形状预测 Chamfer 距离仅为 **3.786 mm**，显著优于 DeepSoRo（6.386 mm）。
*   **虚实迁移有效性**：通过少量真实数据修正，模型精度大幅提升，证明了“物理先验 + 残差修正”是解决虚实鸿沟的高效方案。
*   **控制性能提升**：在远程操作任务中，SOFTMAP 相比基准线在 IoU（交并比）指标上提升了 **36.5%**，尤其在复杂几何形状（如 T 形块）的推挤中表现优异。

#### 7. 优点（亮点）
*   **数据高效性**：不需要海量的真实硬件数据，通过 ARAP 对齐充分利用了廉价的仿真数据。
*   **拓扑一致性**：引入 ARAP 编码解决了点云无序性问题，使得跨域监督学习成为可能。
*   **实时性与实用性**：30 FPS 的推理速度使其能够直接应用于闭环控制和实时远程操作。

#### 8. 不足与局限
*   **形态固定性**：模型目前针对特定形状的软体手指，如果硬件结构发生改变（如手指长度、腱位置变化），需要重新收集数据并训练残差网络。
*   **未考虑接触力**：模型主要关注几何形状的正向预测，尚未整合接触力反馈，这在复杂的交互任务中可能限制其鲁棒性。
*   **环境依赖**：真实数据的获取依赖于多相机重建系统，在遮挡严重或光照极端的环境下，数据对齐的质量可能会下降。

（完）

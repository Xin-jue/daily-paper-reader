---
title: "Speedup Patch: Learning a Plug-and-Play Policy to Accelerate Embodied Manipulation"
title_zh: Speedup Patch：学习一种用于加速具身操控的即插即用策略
authors: "Zhichao Wu, Junyin Ye, Zhilong Zhang, Yihao Sun, Haoxin Lin, Jiaheng Luo, Haoxiang Ren, Lei Yuan, Yang Yu"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20658v1"
tags: ["query:q7"]
score: 9.0
evidence: 具身操作基础模型的加速框架
tldr: 针对具身智能策略因模仿人类演示而导致执行速度缓慢的问题，本文提出Speedup Patch (SuP)，一种轻量级、策略无关且即插即用的加速框架。SuP通过外部调度器自适应下采样动作块以消除冗余，并利用世界模型作为虚拟评估器，在离线强化学习框架下优化效率与性能的平衡。实验证明，SuP在保持原始成功率的同时，为多种策略实现了约1.8倍的执行加速。
selection_source: fresh_fetch
motivation: 现有的具身策略受限于人类演示的缓慢节奏，且现有加速方法通常需要重新训练或昂贵的在线交互，难以扩展。
method: 将加速问题建模为受限马尔可夫决策过程，利用世界模型预测状态偏差作为安全约束，通过离线强化学习训练调度器进行动作下采样。
result: 在Libero、Bigym及真实世界任务中，SuP在不降低成功率的前提下，为多种策略带来了平均1.8倍的执行加速。
conclusion: SuP证明了通过轻量级的即插即用模块，可以在不重新训练基础模型的情况下显著提升具身操作的执行效率。
---

## 摘要
尽管当前的具身策略展现出卓越的操控技能，但由于继承了人类演示中缓慢的节奏，其执行速度仍然不尽如人意。现有的加速方法通常需要重新训练策略或进行昂贵的在线交互，这限制了它们在大规模基础模型上的可扩展性。在本文中，我们提出了 Speedup Patch (SuP)，这是一个轻量级且与策略无关的框架，仅使用离线数据即可实现即插即用的加速。SuP 引入了一个外部调度器，该调度器能够自适应地对具身策略提供的动作块进行下采样，以消除冗余。具体而言，我们将调度器的优化形式化为一个受限马尔可夫决策过程 (CMDP)，旨在不损害任务性能的前提下最大化效率。由于在离线设置中无法直接进行成功率评估，SuP 引入了基于世界模型的状态偏差作为代理指标，以实施安全约束。通过利用学习到的世界模型作为虚拟评估器来预测反事实轨迹，可以通过离线强化学习对调度器进行优化。在仿真基准测试（Libero、Bigym）和真实世界任务上的实验结果验证了，SuP 在保持原始成功率的同时，为各种策略实现了 1.8 倍的整体执行加速。

## Abstract
While current embodied policies exhibit remarkable manipulation skills, their execution remains unsatisfactorily slow as they inherit the tardy pacing of human demonstrations. Existing acceleration methods typically require policy retraining or costly online interactions, limiting their scalability for large-scale foundation models. In this paper, we propose Speedup Patch (SuP), a lightweight, policy-agnostic framework that enables plug-and-play acceleration using solely offline data. SuP introduces an external scheduler that adaptively downsamples action chunks provided by embodied policies to eliminate redundancies. Specifically, we formalize the optimization of our scheduler as a Constrained Markov Decision Process (CMDP) aimed at maximizing efficiency without compromising task performance. Since direct success evaluation is infeasible in offline settings, SuP introduces World Model based state deviation as a surrogate metric to enforce safety constraints. By leveraging a learned world model as a virtual evaluator to predict counterfactual trajectories, the scheduler can be optimized via offline reinforcement learning. Empirical results on simulation benchmarks (Libero, Bigym) and real-world tasks validate that SuP achieves an overall 1.8x execution speedup for diverse policies while maintaining their original success rates.

---

## 论文详细总结（自动生成）

以下是对论文《Speedup Patch: Learning a Plug-and-Play Policy to Accelerate Embodied Manipulation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的具身智能（Embodied AI）策略虽然具备出色的操控技能，但执行速度普遍缓慢。这主要是因为这些策略通常通过模仿学习（Imitation Learning）训练，继承了人类演示数据中冗余、迟缓的节奏。
*   **核心问题**：现有的加速方法（如重新训练策略或在线微调）成本极高，且难以扩展到参数量巨大的视觉-语言-动作（VLA）基础模型上。
*   **研究目标**：开发一种**轻量级、策略无关且即插即用**的加速框架，仅利用离线数据即可提升现有具身策略的执行效率，而无需重新训练原始模型。

### 2. 方法论
*   **核心思想**：引入一个外部调度器策略（Scheduler Policy），作为原始策略的“补丁（Patch）”。该调度器根据当前状态自适应地决定对原始动作块（Action Chunk）进行多大程度的下采样（Downsampling），从而跳过冗余步骤。
*   **关键技术细节**：
    *   **受限马尔可夫决策过程 (CMDP)**：将加速问题建模为 CMDP，目标是最大化执行速度（奖励 $r=k$），同时满足性能不下降的约束。
    *   **循环世界模型 (RWM/ADM)**：为了在离线状态下评估加速后的风险，训练了一个轻量级的循环世界模型。它不重建高维图像，而是直接预测机器人状态（如末端执行器位姿），用于模拟反事实的加速轨迹。
    *   **状态偏差作为代理约束**：定义加速轨迹与原始轨迹之间的末端执行器（EEF）距离偏差为安全指标。如果偏差超过阈值 $\epsilon$，则视为违规。
    *   **离线强化学习 (IQL)**：利用世界模型生成的合成数据，通过隐式 Q 学习（Implicit Q-Learning）优化调度器，使其在保证安全（低偏差）的前提下选择最大的下采样率。
*   **算法流程**：1) 在离线数据集上训练世界模型；2) 利用世界模型对不同下采样率进行评估，生成合成的 CMDP 数据集；3) 使用 IQL 训练调度器。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **仿真环境**：使用了 **BiGym**（20 个机器人厨卫操控任务，涉及双臂和全身控制）和 **LIBERO**（4 个任务套件共 40 个任务，侧重 VLA 模型的指令遵循）。
    *   **真实世界**：在双臂机器人平台（类似 Aloha）上进行了“整理桌面”、“折叠毛巾”和“堆叠盘子”三个复杂任务。
*   **对比方法**：
    *   **Vanilla Downsample (-ds*)**：固定倍率的静态下采样。
    *   **DemoSpeedup**：通过熵引导的演示加速并重新训练原始策略的方法。
*   **基础策略 (Base Policies)**：涵盖了 ACT、Diffusion Policy (DP)、$\pi_{0.5}$ (VLA) 和 VLA-Adapter 等多种主流架构。

### 4. 资源与算力
*   **模型规模**：调度器非常轻量，仅有 **5.12M** 个可训练参数。
*   **训练时长**：在 LIBERO 任务中，SuP 的训练仅需约 **2 小时**（相比之下，DemoSpeedup 需要 20 小时且需微调 4B 参数的模型）。
*   **推理开销**：调度器的推理延迟仅为 **1ms**，相对于基础模型（如 $\pi_{0.5}$ 的 50ms）几乎可以忽略不计（约占 2%）。
*   **算力设备**：文中未明确标注具体的 GPU 型号和数量，但强调了其低参数量和短训练时间的特性。

### 5. 实验数量与充分性
*   **实验规模**：在仿真中测试了 60 个不同的任务，在真实世界中进行了 3 个任务的实测，每个任务包含多次重复实验（如 LIBERO 每套件 500 次测试）。
*   **消融实验**：对比了基于 ADM 的世界模型与普通 MLP 的差异、IQL 与贪婪 MPC 策略的效果，以及偏差阈值 $\epsilon$ 的敏感度分析。
*   **充分性评价**：实验设计非常充分且客观。它不仅验证了不同架构的通用性，还通过可视化展示了调度器在“接近物体（需精准）”时减速、在“大幅度移动（冗余高）”时加速的智能行为。

### 6. 主要结论与发现
*   **显著加速**：SuP 在仿真和现实中实现了平均 **1.55x 至 2.17x** 的执行加速（综合约 1.8x）。
*   **性能保持**：在大幅提升速度的同时，SuP 能够维持甚至在某些任务中略微提升原始策略的成功率。
*   **架构通用性**：证明了该方法对从轻量级 ACT 到大规模 VLA 模型均有效，具有极强的普适性。

### 7. 优点
*   **即插即用**：无需触动或重新训练庞大的基础模型，部署成本极低。
*   **纯离线学习**：不需要昂贵的在线交互或环境模拟器，直接利用现有演示数据即可训练。
*   **安全性高**：通过世界模型预测偏差，有效地在加速与任务成功率之间取得了平衡。
*   **解释性强**：调度器能识别任务的关键阶段，实现非匀速的动态加速。

### 8. 不足与局限
*   **对世界模型的依赖**：加速效果和安全性高度依赖于世界模型的预测精度。如果世界模型在某些分布外状态下失效，可能导致错误的加速决策。
*   **下采样机制限制**：目前主要基于整数倍的动作块下采样，未来可探索更精细的非整数或连续时间尺度的加速机制。
*   **夹持器补偿**：文中提到下采样会导致夹持器动作位移不足，目前需采用启发式的补偿方法（如加倍动作幅值），这可能需要针对不同硬件进行手动微调。

（完）

---
title: "VLA-IAP: Training-Free Visual Token Pruning via Interaction Alignment for Vision-Language-Action Models"
title_zh: VLA-IAP：通过交互对齐实现视觉-语言-动作模型的免训练视觉 Token 剪枝
authors: "Jintao Cheng, Haozhe Wang, Weibin Li, Gang Wang, Yipu Zhang, Xiaoyu Tang, Jin Wu, Xieyuanli Chen, Yunhui Liu, Wei Zhang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22991v1"
tags: ["query:q12"]
score: 9.0
evidence: 操作任务中VLA模型的视觉标记剪枝
tldr: 针对视觉-语言-动作（VLA）模型在具身智能任务中推理成本高的问题，本文提出了一种无需训练的视觉标记剪枝方法 VLA-IAP。该方法摒弃了传统的语义显著性准则，转向“交互优先”范式，通过几何先验保留关键结构锚点，并结合动态调度策略根据语义-动作对齐调整剪枝强度。实验证明，该方法在保持与原始模型相当性能的同时，显著提升了推理速度，并在多种仿真和真实机器人平台上展现了极强的泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉剪枝方法忽略了物理交互这一VLA任务的核心属性，导致在任务早期容易误删对操作至关重要的结构化区域。
method: 提出一种无需训练的交互对齐剪枝方法，利用几何先验保留结构锚点，并根据语义-动作对齐程度动态调整剪枝强度。
result: "在LIBERO基准测试中实现了97.8%的成功率和高达1.54倍的加速，性能与未剪枝的骨干模型相当。"
conclusion: VLA-IAP通过显式的交互优先范式，在不牺牲性能的前提下有效降低了VLA模型的推理开销，具有很强的实用价值和泛化性。
---

## 摘要
视觉-语言-动作（VLA）模型迅速推动了具身智能的发展，使机器人能够执行复杂的指令驱动任务。然而，随着模型容量和视觉上下文长度的增加，VLA 系统的推理成本成为在资源受限平台上进行实际部署的主要瓶颈。现有的视觉 Token 剪枝方法主要依赖于语义显著性或简单的时序线索，忽视了连续的物理交互，而这是 VLA 任务的一个基本属性。因此，当前的方法往往会剪掉视觉上稀疏但对支持操作至关重要的结构性区域，导致任务早期阶段的行为不稳定。为了克服这一问题，我们提出向显式的“交互优先”范式转变。我们提出的免训练方法 VLA-IAP（交互对齐剪枝）引入了一种几何先验机制来保留结构锚点，并采用一种根据语义-运动对齐情况调整剪枝强度的动态调度策略。这实现了从保守到激进的过渡，确保了早期不确定阶段的鲁棒性以及交互锁定后的效率。大量实验表明，VLA-IAP 在 LIBERO 基准测试中实现了 97.8% 的成功率和 1.25 倍的加速，并且在保持与未剪枝骨干网络相当的性能下，加速比最高可达 1.54 倍。此外，该方法在多种模型架构、三个不同的仿真环境以及真实机器人平台上均表现出优越且一致的性能，验证了其强大的泛化能力和实际应用价值。我们的项目网站是：https://chengjt1999.github.io/VLA-IAP.github.io/。

## Abstract
Vision-Language-Action (VLA) models have rapidly advanced embodied intelligence, enabling robots to execute complex, instruction-driven tasks. However, as model capacity and visual context length grow, the inference cost of VLA systems becomes a major bottleneck for real-world deployment on resource-constrained platforms. Existing visual token pruning methods mainly rely on semantic saliency or simple temporal cues, overlooking the continuous physical interaction, a fundamental property of VLA tasks. Consequently, current approaches often prune visually sparse yet structurally critical regions that support manipulation, leading to unstable behavior during early task phases. To overcome this, we propose a shift toward an explicit Interaction-First paradigm. Our proposed \textbf{training-free} method, VLA-IAP (Interaction-Aligned Pruning), introduces a geometric prior mechanism to preserve structural anchors and a dynamic scheduling strategy that adapts pruning intensity based on semantic-motion alignment. This enables a conservative-to-aggressive transition, ensuring robustness during early uncertainty and efficiency once interaction is locked. Extensive experiments show that VLA-IAP achieves a \textbf{97.8\% success rate} with a \textbf{$1.25\times$ speedup} on the LIBERO benchmark, and up to \textbf{$1.54\times$ speedup} while maintaining performance \textbf{comparable to the unpruned backbone}. Moreover, the method demonstrates superior and consistent performance across multiple model architectures and three different simulation environments, as well as a real robot platform, validating its strong generalization capability and practical applicability. Our project website is: \href{https://chengjt1999.github.io/VLA-IAP.github.io/}{VLA-IAP.com}.

---

## 论文详细总结（自动生成）

### 论文总结：VLA-IAP —— 基于交互对齐的免训练视觉 Token 剪枝方法

#### 1. 论文的核心问题与整体含义
*   **研究背景**：视觉-语言-动作（VLA）模型虽然在具身智能领域表现出色，但其推理延迟极高（通常低于 5Hz），难以满足实时闭环控制的需求。
*   **核心问题**：现有的视觉 Token 剪枝方法多遵循“感知优先”范式，依赖语义显著性或简单时序线索。这导致模型容易误删视觉上不显著但对物理操作至关重要的区域（如透明杯边缘、平滑把手），在任务早期阶段引发行为不稳定。
*   **整体含义**：本文提出从“感知优先”转向“交互优先”范式，通过引入几何先验和动态对齐策略，在大幅降低计算开销的同时，确保机器人操作的鲁棒性。

#### 2. 论文提出的方法论
VLA-IAP 是一种**免训练（Training-free）**的剪枝框架，核心包含三个模块：
*   **几何先验机制（Geometric Prior）**：利用 Sobel 算子提取图像的边缘梯度，将其转化为 Token 级别的结构得分。这确保了即使某些区域语义特征较弱，只要具备关键的物理轮廓（结构锚点），就会被保留。
*   **语义-运动对齐模块（Semantic-Motion Alignment）**：
    *   **语义先验**：计算视觉 Token 与文本指令的互相关得分。
    *   **运动先验**：通过计算相邻帧特征的**二阶时间差分**（近似加速度）来过滤相机平移等线性噪声，并结合形态学闭运算和高斯平滑确保运动区域的连通性。
*   **交互对齐动态策略**：计算语义掩码与运动掩码的交并比（IoU）。
    *   **保守模式（低 IoU）**：任务早期或不确定阶段，采用“双弱排除”策略，仅剔除纯背景，保留更多潜在目标。
    *   **激进模式（高 IoU）**：当操作意图与物理运动对齐时，收缩语义关注点并结合运动区域，实施高比例剪枝以提升效率。

#### 3. 实验设计
*   **仿真基准测试**：
    *   **LIBERO**：评估终身学习和任务泛化能力（空间、对象、目标、长程任务）。
    *   **VLABench**：评估复杂逻辑推理和精细操作。
    *   **CALVIN**：评估长程语言条件下的策略学习。
*   **对比方法**：包括 FastV、SparseVLM、DivPrune、VLA-Cache、SpecPrune-VLA、EfficientVLA 等前沿剪枝方法。
*   **骨干模型**：验证了在 OpenVLA-OFT、DreamVLA、$\pi_0$ 和 $\pi_{0.5}$ 等多种主流架构上的有效性。
*   **真实世界实验**：在自研的双臂机器人平台上进行了简单任务、长程任务和双臂协同任务的测试。

#### 4. 资源与算力
*   **硬件环境**：实验运行在配备 **NVIDIA A100 GPU** 的 Linux 工作站上。
*   **算力需求**：由于该方法是**免训练**的，不需要额外的训练时长。在推理阶段，它显著降低了显存占用（如在 $\pi_0$ 上从 7.11GB 降至 6.88GB）和 CUDA 推理耗时（从 98ms 降至约 65ms）。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了三大仿真基准的数十个子任务，并进行了详尽的消融实验（验证几何先验、IoU 阈值、边缘权重等的影响）。
*   **客观性与公平性**：在相同的 Token 保留率（70%、50%、30%）下与多种基准方法进行对比，并在不同架构（Transformer-based, Diffusion-based）上验证，实验设计全面且对比客观。

#### 6. 论文的主要结论与发现
*   **性能领先**：在 LIBERO 上实现了 97.8% 的成功率，同时获得 1.25x 加速；在 30% Token 保留率下，加速比达 1.54x 且性能无损。
*   **鲁棒性强**：在极端压缩（仅保留 30% Token）时，传统方法在 VLABench 上几乎崩溃（成功率接近 0%），而 VLA-IAP 仍能保持 17.1% - 33.3% 的成功率。
*   **交互优先的必要性**：实验证明，显式保留几何结构锚点能有效修正预训练模型在复杂操作任务中的语义偏差。

#### 7. 优点
*   **范式创新**：首次提出“交互优先”的剪枝理念，更符合具身智能的物理本质。
*   **即插即用**：无需重新训练或微调，可直接应用于现有的各种 VLA 模型。
*   **效率与精度平衡**：通过动态模式切换，解决了“剪枝过度导致失败”与“剪枝不足导致延迟”之间的矛盾。
*   **真实场景验证**：不仅限于仿真，在真实双臂机器人上也验证了其加速效果和成功率提升。

#### 8. 不足与局限
*   **超参数依赖**：IoU 切换阈值、边缘权重等参数可能需要针对不同环境进行微调以达到最优效果。
*   **极端环境限制**：在极度缺乏纹理或光照剧烈变化的场景下，基于 Sobel 的几何先验和基于特征差分的运动先验可能会受到一定干扰。
*   **任务覆盖**：虽然涵盖了多种操作任务，但对于涉及极高速运动或极微小物体（超出 Patch 分辨率）的任务，剪枝策略的精细度仍有提升空间。

（完）

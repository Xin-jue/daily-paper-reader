---
title: "DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion"
title_zh: DiffusionAnything：用于统一导航与抓取前运动的端到端上下文扩散学习
authors: "Iana Zhura, Yara Mahmoud, Jeffrin Sam, Hung Khang Nguyen, Didar Seyidov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26322v1"
tags: ["query:q10"]
score: 9.0
evidence: 用于导航和抓取前操作的扩散策略
tldr: 本研究提出 DiffusionAnything，一种统一的端到端图像空间扩散策略，旨在解决机器人导航与抓取前运动规划中的效率与泛化难题。该模型仅需极少自监督数据，通过多尺度 FiLM 调节和轨迹对齐深度预测，实现了从米级导航到厘米级操作的跨尺度任务处理。其轻量化设计支持纯 RGB 输入和实时部署，在未知场景中展现出卓越的零样本泛化能力。
selection_source: fresh_fetch
motivation: 针对现有视觉动作模型计算资源消耗大、在未知场景中零样本泛化能力差以及导航与操作任务难以统一的问题。
method: 采用多尺度 FiLM 调节、轨迹对齐深度预测以及基于 AnyTraverse 的自监督注意力机制，构建了一个轻量级的端到端扩散策略模型。
result: 该模型仅需 2.0 GB 显存即可实现 10 Hz 的实时推理，且在仅使用 RGB 输入的情况下展现出强大的跨场景零样本泛化性能。
conclusion: DiffusionAnything 证明了利用轻量级扩散模型统一处理多尺度机器人任务的有效性，为机载实时运动规划提供了高效的解决方案。
---

## 摘要
直接从视觉高效预测运动规划仍然是机器人领域的一个基本挑战，通常规划需要明确的目标说明和特定任务的设计。最近的视觉-语言-动作（VLA）模型直接从视觉输入推断动作，但需要巨大的计算资源和大量的训练数据，且在面对新场景时无法实现零样本泛化。我们提出了一种统一的图像空间扩散策略，通过多尺度特征调制，仅需每个任务 5 分钟的自监督数据，即可处理米级导航和厘米级操作。该框架由三项关键创新驱动：(1) 基于任务模式、深度尺度和空间注意力的多尺度 FiLM 条件化，使单一模型能够实现适应任务的行为；(2) 轨迹对齐的深度预测将度量 3D 推理集中在生成的路径点上；(3) 来自 AnyTraverse 的自监督注意力实现了无需视觉语言模型和深度传感器的目标导向推理。该模型纯粹基于 RGB 输入运行（2.0 GB 内存，10 Hz），在实现对新场景的鲁棒零样本泛化的同时，仍适用于机载部署。

## Abstract
Efficiently predicting motion plans directly from vision remains a fundamental challenge in robotics, where planning typically requires explicit goal specification and task-specific design. Recent vision-language-action (VLA) models infer actions directly from visual input but demand massive computational resources, extensive training data, and fail zero-shot in novel scenes. We present a unified image-space diffusion policy handling both meter-scale navigation and centimeter-scale manipulation via multi-scale feature modulation, with only 5 minutes of self-supervised data per task. Three key innovations drive the framework: (1) Multi-scale FiLM conditioning on task mode, depth scale, and spatial attention enables task-appropriate behavior in a single model; (2) trajectory-aligned depth prediction focuses metric 3D reasoning along generated waypoints; (3) self-supervised attention from AnyTraverse enables goal-directed inference without vision-language models and depth sensors. Operating purely from RGB input (2.0 GB memory, 10 Hz), the model achieves robust zero-shot generalization to novel scenes while remaining suitable for onboard deployment.

---

## 论文详细总结（自动生成）

以下是对论文《DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在单一、轻量级的端到端模型中，统一处理机器人**米级（meter-scale）的导航**与**厘米级（centimeter-scale）的抓取前（pre-grasp）运动规划**。
*   **研究动机**：
    *   **传统架构缺陷**：导航与操作通常被视为独立领域，导致级联架构存在误差累积、高延迟和模式切换生硬的问题。
    *   **现有VLA模型局限**：如RT-2、OpenVLA等视觉-语言-动作模型虽然泛化性强，但计算成本极高（参数量巨大）、推理延迟高，且在未知场景中的零样本（zero-shot）表现往往不佳。
    *   **空间推理需求**：语言调节对于本质上是空间或几何的任务（如精确避障和接近物体）显得过于繁琐。

### 2. 方法论
DiffusionAnything 采用基于图像空间的扩散策略，核心思想是通过**上下文感知（Context-aware）**的特征调制来适配不同尺度的任务。
*   **多尺度 FiLM 调节**：引入特征线性调制（FiLM），根据任务模式（导航/抓取）、深度尺度（米/厘米）和空间注意力（地面/物体）对 UNet 编码器的多尺度特征进行干预。
*   **轨迹对齐的深度推理**：模型不预测全量深度图，而是仅在生成的轨迹路径点上预测深度。这种设计降低了计算开销，同时保证了规划所需的度量几何精度。
*   **自监督注意力预测**：利用 AnyTraverse 框架生成的注意力图作为监督信号，使模型能自主识别任务目标（如导航时的可通行区域或操作时的目标物体），无需依赖重型视觉语言模型（VLM）。
*   **损失函数**：总损失由扩散去噪损失（$L_{diff}$）、深度预测损失（$L_{depth}$）、可通行性重建损失（$L_{trav}$）和注意力损失（$L_{attn}$）加权组成。

### 3. 实验设计
*   **硬件平台**：Unitree G1 人形机器人，配备 RGB 摄像头。
*   **实验场景**：包括室内走廊（导航）和带有多种物体的桌面（抓取前规划）。
*   **任务类型**：自主探索导航、目标导向导航、抓取前运动规划。
*   **对比基准（Benchmarks）**：
    *   **导航**：对比了 **NoMaD**（基于大规模机器人数据集训练的视觉导航策略）。
    *   **操作**：对比了 NVIDIA 的 **GR00T**（n1.5 和 n1.6 版本）在人形机器人上的表现。
*   **评估指标**：成功率（SR）、避障间距（Clearance）、目标距离误差（Dist）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **训练开销**：
    *   **主模型**：使用 20 分钟视频数据，训练 50 个 epoch，耗时约 **2 小时**。
    *   **新任务适配**：仅需 5 分钟新数据，微调 10 个 epoch，耗时 **15-20 分钟**。
*   **推理性能**：推理时仅占用 **2.0 GB 显存**，运行频率为 **10 Hz**，完全满足机载实时部署需求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三种主要操作模式，并在多个新场景中进行了零样本泛化测试。
*   **充分性评价**：
    *   **对比实验**：在导航任务上与 SOTA 模型 NoMaD 进行了量化对比；在操作任务上与 GR00T 进行了定性与定量对比。
    *   **消融/验证**：通过可视化注意力图和轨迹精度（4.71cm vs 29.68cm），验证了深度尺度调节的有效性。
    *   **客观性**：实验在真实物理机器人（Unitree G1）上完成，而非仅限于仿真，增强了结果的说服力。

### 6. 主要结论与发现
*   **跨尺度统一**：单一模型通过上下文调节，成功实现了从米级避障到厘米级精确接近的平滑切换。
*   **零样本泛化**：在完全未见过的场景中，导航成功率达到 100%，抓取前规划成功率达到 70.6%（且 100% 无碰撞）。
*   **精度提升**：通过深度尺度调节，抓取任务的精度比导航任务提升了约 6 倍（误差从分米级降至厘米级）。
*   **超越 VLA**：相比于 GR00T 等重型模型，该方法在未知场景下的泛化能力更强，且计算资源消耗极低。

### 7. 优点与亮点
*   **极低的数据依赖**：每个任务仅需 5 分钟自监督数据即可完成适配，远低于传统模仿学习或 VLA 模型的需求。
*   **轻量化与实时性**：2GB 显存占用和 10Hz 的速度使其非常适合部署在算力受限的移动机器人平台上。
*   **几何与语义结合**：通过轨迹对齐深度和空间注意力，巧妙地将 3D 几何推理融入到 2D 图像空间规划中。

### 8. 不足与局限
*   **监督源依赖**：模型性能高度依赖于 AnyTraverse 生成的伪标签质量，若 AnyTraverse 在极端环境下失效，模型表现可能受限。
*   **任务范围限制**：目前仅聚焦于“抓取前（Pre-grasp）”的运动规划，尚未涉及实际抓取时的力反馈控制或复杂的双臂协同。
*   **传感器单一**：纯 RGB 输入在极端光照或缺乏纹理的场景下可能存在深度估计不准的风险。

（完）

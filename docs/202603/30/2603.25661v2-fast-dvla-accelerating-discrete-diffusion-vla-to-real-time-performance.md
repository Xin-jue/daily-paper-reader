---
title: "Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance"
title_zh: Fast-dVLA：将离散扩散 VLA 加速至实时性能
authors: "Wenxuan Song, Jiayi Chen, Shuai Chen, Jingbo Wang, Pengxiang Ding, Han Zhao, Yikai Qin, Xinhu Zheng, Donglin Wang, Yan Wang, Haoang Li"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25661v2"
tags: ["query:q12"]
score: 10.0
evidence: 将离散扩散 VLA 加速至实时性能
tldr: 针对预训练VLA模型在标准微调中性能提升有限且计算开销大的挑战，本文提出Fast-dVLA。该方法通过在参数空间解耦辅助任务目标，提取增强通用能力的“能力向量”并与预训练参数融合构建元模型。结合轻量级正交正则化损失，在降低开销的同时实现了高性能，显著提升了机器人任务的适应效率。
selection_source: fresh_fetch
motivation: 现有的辅助任务微调虽能提升VLA模型性能，但其带来的额外计算开销限制了实时性与微调效率。
method: 在参数空间解耦通用能力增强与特定任务拟合目标，通过融合提取的能力向量构建元模型，并配合轻量级正交正则化进行微调。
result: 实验证明该方法在多种机器人任务中达到了与辅助微调基准相当的性能，且显著降低了计算开销。
conclusion: 本研究通过参数空间解耦与融合，为高效、低成本地提升VLA模型在多样化机器人任务中的表现提供了新路径。
---

## 摘要
本文提出了一种新颖的方法，旨在解决预训练视觉-语言-动作（VLA）模型在标准监督微调（SFT）过程中往往无法有效提升性能并降低适配成本的挑战。一些带有辅助训练目标的高级微调方法可以提高性能并减少收敛步数，但由于辅助任务带来的额外损失函数计算，它们通常会产生显著的计算开销。为了在保持标准 SFT 简洁性的同时实现辅助训练的增强能力，我们在参数空间内解耦了辅助任务训练的两个目标，即增强通用能力和拟合特定任务的动作分布。为实现这一目标，我们只需使用两种不同的训练策略，使模型在小规模任务集上收敛。由此产生的模型参数之间的差异可以被解释为辅助任务提供的能力向量。随后，这些向量与预训练参数合并，形成一个能力增强的元模型。此外，当标准 SFT 辅以轻量级的正交正则化损失时，合并后的模型在计算开销降低的情况下，达到了与辅助微调基准相当的性能。实验结果表明，该方法在多种机器人任务中均非常有效。项目主页：https://chris1220313648.github.io/Fast-dVLA/

## Abstract
This paper proposes a novel approach to address the challenge that pretrained VLA models often fail to effectively improve performance and reduce adaptation costs during standard supervised finetuning (SFT). Some advanced finetuning methods with auxiliary training objectives can improve performance and reduce the number of convergence steps. However, they typically incur significant computational overhead due to the additional losses from auxiliary tasks. To simultaneously achieve the enhanced capabilities of auxiliary training with the simplicity of standard SFT, we decouple the two objectives of auxiliary task training within the parameter space, namely, enhancing general capabilities and fitting task-specific action distributions. To deliver this goal, we only need to train the model to converge on a small-scale task set using two distinct training strategies. The difference between the resulting model parameters can then be interpreted as capability vectors provided by auxiliary tasks. These vectors are then merged with pretrained parameters to form a capability-enhanced meta model. Moreover, when standard SFT is augmented with a lightweight orthogonal regularization loss, the merged model attains performance comparable to auxiliary finetuned baselines with reduced computational overhead. Experimental results demonstrate that this approach is highly effective across diverse robot tasks. Project page: https://chris1220313648.github.io/Fast-dVLA/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-dVLA: Accelerating Discrete Diffusion VLA to Real-Time Performance》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：离散扩散视觉-语言-动作模型（dVLA）虽然在多模态对齐和知识保留方面优于传统的流匹配（Flow-matching）架构，但其**推理速度极慢**，远低于物理机器人实时控制所需的 30Hz 频率。
*   **研究背景**：dVLA 通常采用全双向注意力机制，这导致每一轮去噪迭代时 Key-Value (KV) 状态都会改变，无法像自回归（AR）模型那样利用 KV Cache 加速。
*   **关键观察**：作者发现即便使用双向注意力，dVLA 在去噪过程中仍表现出隐式的“从左到右”块状自回归趋势。

### 2. 方法论
Fast-dVLA 提出了一套结合块状扩散与并行解码的加速方案：
*   **块状注意力（Block-wise Attention）**：将动作序列划分为块，限制注意力仅作用于前缀块和当前块。一旦某个块解码完成，其 KV 状态即固定，从而实现 **KV Cache 的跨迭代复用**。
*   **扩散强迫（Diffusion Forcing）**：为不同动作块分配单调递增的噪声水平（前块噪声低，后块噪声高），允许模型在精细化前序块的同时，并行预测后续块。
*   **异步蒸馏（Asymmetric Distillation）**：为了高效训练，将具有双向注意力的预训练 dVLA 作为教师模型，通过 KL 散度损失引导具有块状注意力的学生模型进行对齐。这种方式比从头训练快 10 倍。
*   **流水线并行解码（Pipelined Parallel Decoding）**：设计了“半激活”和“全激活”状态切换机制，根据前序块的完成比例动态引入新块的并行去噪，平衡了推理速度与动作准确性。

### 3. 实验设计
*   **模拟环境**：
    *   **CALVIN**：评估长程操作任务（ABCD→D 序列）。
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，评估泛化与复杂技能。
    *   **SimplerEnv**：评估真实到模拟（Real-to-Sim）的迁移能力。
*   **基准模型（Baselines）**：
    *   **基础 dVLA**：Dream-VLA、DD-VLA、UD-VLA。
    *   **加速方案对比**：Fast-dLLM、Block Diffusion。
    *   **SOTA VLA**：$\pi_0$、$\pi_{0.5}$、OpenVLA、GR00T-N1 等。
*   **真实世界实验**：在 AgileX 双臂平台上执行传送带抓取、蔬菜分类与放置任务。

### 4. 资源与算力
*   **训练细节**：使用了 LoRA（Rank=32）进行高效微调。
*   **训练时长**：异步蒸馏仅需 2000-4000 步即可收敛（约为标准微调步数的 1/5 到 1/10）。
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量（如 A100 数量），但强调了其方法在计算开销上的显著降低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流模拟基准测试和 3 类真实世界任务。
*   **消融实验**：针对块大小（Block Size）、置信度阈值（$\tau_{conf}$）、激活阈值（$\tau_{add}, \tau_{act}$）以及解码策略进行了详尽测试。
*   **充分性评价**：实验设计较为全面，跨越了不同的 dVLA 架构（如统一模型 UD-VLA 和纯动作模型 Dream-VLA），对比了多种加速范式，结果具有较强的说服力和客观性。

### 6. 主要结论
*   **显著加速**：Fast-dVLA 在保持甚至略微提升成功率的前提下，实现了 **2.8倍至4.1倍** 的推理加速。
*   **实时性能**：在真实机器人上达到了 **30Hz** 的稳定执行频率，满足了实时控制需求。
*   **训练高效**：通过异步蒸馏，大幅缩短了模型适配新任务的训练时间。
*   **性能领先**：在 SimplerEnv 等基准上，其成功率超过了 $\pi_0$ 和 GR00T-N1 等强力竞争对手。

### 7. 优点与亮点
*   **洞察深刻**：揭示了扩散模型中隐式的自回归特性，并据此设计了块状注意力机制。
*   **架构通用**：该加速方案不依赖特定模型，可广泛应用于各类基于离散扩散的 VLA 模型。
*   **平衡性好**：通过流水线解码巧妙解决了 KV Cache 复用与并行化之间的矛盾。

### 8. 不足与局限
*   **超参数依赖**：流水线解码中的多个阈值（$\tau_{add}, \tau_{act}, \tau_{conf}$）可能需要针对不同任务进行微调以获得最优平衡。
*   **蒸馏依赖**：性能高度依赖于教师模型的质量，若教师模型本身存在偏差，学生模型难以通过该框架自我修正。
*   **任务复杂度**：虽然在真实世界中进行了测试，但任务场景相对受控，在极高动态或极不确定环境下的鲁棒性仍待进一步验证。

（完）

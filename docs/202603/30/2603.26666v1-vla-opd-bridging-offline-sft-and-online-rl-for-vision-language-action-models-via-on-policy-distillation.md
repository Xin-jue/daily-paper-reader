---
title: "VLA-OPD: Bridging Offline SFT and Online RL for Vision-Language-Action Models via On-Policy Distillation"
title_zh: VLA-OPD：通过同策略蒸馏桥接视觉-语言-动作模型的离线监督微调与在线强化学习
authors: "Zhide Zhong, Haodong Yan, Junfeng Li, Junjie He, Tianran Zhang, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26666v1"
tags: ["query:q10"]
score: 10.0
evidence: 针对操作任务的VLA模型在线策略蒸馏
tldr: 针对视觉-语言-动作（VLA）模型在部署时面临的离线微调分布偏移和在线强化学习效率低下的问题，本文提出了VLA-OPD框架。该框架通过在线策略蒸馏，利用专家教师对学生自生成轨迹进行密集的Token级监督。采用Reverse-KL目标函数，在纠正策略偏差的同时保留预训练能力，显著提升了模型的鲁棒性与采样效率，有效解决了后训练中的性能退化问题。
selection_source: fresh_fetch
motivation: 旨在解决离线SFT导致的分布偏移与遗忘问题，以及在线RL在稀疏奖励下的低采样效率难题。
method: 通过在线策略蒸馏（VLA-OPD），利用专家教师对学生轨迹进行Token级监督，并采用Reverse-KL目标函数实现稳定的策略对齐。
result: 在LIBERO和RoboTwin2.0基准上，该方法在采样效率、鲁棒性以及缓解灾难性遗忘方面均显著优于传统的SFT和RL方法。
conclusion: VLA-OPD成功结合了SFT的高效性与RL的鲁棒性，为VLA模型的在线优化提供了一种兼顾性能与稳定性的新途径。
---

## 摘要
尽管预训练的视觉-语言-动作（VLA）模型在机器人操控中表现出令人印象深刻的泛化能力，但后期训练对于确保部署期间的可靠性能仍然至关重要。然而，标准的离线监督微调（SFT）面临分布偏移和预训练能力灾难性遗忘的问题，而在线强化学习（RL）则受困于稀疏奖励和低样本效率。在本文中，我们提出了同策略 VLA 蒸馏（VLA-OPD），这是一个结合了 SFT 的效率与 RL 的鲁棒性的框架。VLA-OPD 不依赖稀疏的环境奖励，而是利用专家教师在学生自生成的轨迹上提供密集的标记级（token-level）监督。这使得模型能够在策略诱导的状态下进行主动纠错，同时通过温和的对齐保留预训练的通用能力。至关重要的是，我们通过逆向 KL（Reverse-KL）目标函数构建了 VLA-OPD。不同于会导致模式覆盖（mode-covering）熵爆炸的标准正向 KL，或导致过早熵崩溃的硬交叉熵（Hard-CE），我们有界的模式寻求（mode-seeking）目标通过过滤教师的认知不确定性并保持动作多样性，确保了稳定的策略学习。在 LIBERO 和 RoboTwin2.0 基准测试上的实验表明，VLA-OPD 相比 RL 显著提高了样本效率，相比 SFT 显著提高了鲁棒性，同时有效缓解了后期训练过程中的灾难性遗忘。

## Abstract
Although pre-trained Vision-Language-Action (VLA) models exhibit impressive generalization in robotic manipulation, post-training remains crucial to ensure reliable performance during deployment. However, standard offline Supervised Fine-Tuning (SFT) suffers from distribution shifts and catastrophic forgetting of pre-trained capabilities, while online Reinforcement Learning (RL) struggles with sparse rewards and poor sample efficiency. In this paper, we propose On-Policy VLA Distillation (VLA-OPD), a framework bridging the efficiency of SFT with the robustness of RL. Instead of relying on sparse environmental rewards, VLA-OPD leverages an expert teacher to provide dense, token-level supervision on the student's self-generated trajectories. This enables active error correction on policy-induced states while preserving pre-trained general capabilities through gentle alignment. Crucially, we formulate VLA-OPD via a Reverse-KL objective. Unlike standard Forward-KL that induces mode-covering entropy explosion, or Hard-CE that causes premature entropy collapse, our bounded mode-seeking objective ensures stable policy learning by filtering out the teacher's epistemic uncertainty while maintaining action diversity. Experiments on LIBERO and RoboTwin2.0 benchmarks demonstrate that VLA-OPD significantly improves sample efficiency over RL and robustness over SFT, while effectively mitigating catastrophic forgetting during post-training.

---

## 论文详细总结（自动生成）

以下是对论文《VLA-OPD: Bridging Offline SFT and Online RL for Vision-Language-Action Models via On-Policy Distillation》的结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：视觉-语言-动作（VLA）模型虽然具备强大的泛化能力，但在特定任务部署时仍需“后训练”（Post-training）。
*   **现有范式的局限**：
    *   **离线监督微调（SFT/行为克隆）**：受限于“离线”性质，容易产生分布偏移（一步错步步错）和对预训练能力的灾难性遗忘。
    *   **在线强化学习（RL）**：虽然能通过环境交互纠错，但面临奖励稀疏、采样效率极低和优化不稳定的挑战。
*   **核心目标**：提出一种名为 **VLA-OPD** 的框架，旨在结合 SFT 的高效率与 RL 的高鲁棒性，解决 VLA 模型在后训练中的性能退化问题。

### 2. 方法论
*   **核心思想**：通过**同策略蒸馏（On-Policy Distillation）**，利用一个冻结的专家教师模型，在学生模型自生成的轨迹上提供密集的、Token 级别的监督信号。
*   **关键技术流程**：
    1.  **阶段 1：同策略采样**。学生模型在环境中执行任务，收集包含其自身错误偏离的轨迹。
    2.  **阶段 2：教师标注**。对于学生访问的每个状态，查询专家教师的动作概率分布（Logits），提供即时的纠错指导。
    3.  **阶段 3：模式寻求优化**。使用**逆向 KL 散度（Reverse-KL）**作为目标函数进行策略梯度更新。
*   **关键公式与逻辑**：
    *   定义内在奖励 $r_t = -(\log \pi_{\theta}(a_t|s_t) - \log \pi_{tea}(a_t|s_t))$。
    *   **Reverse-KL 的优势**：具有“模式寻求”（Mode-seeking）特性。相比正向 KL（会导致熵爆炸、模型犹豫）和硬交叉熵（会导致熵崩溃、失去多样性），Reverse-KL 能过滤教师的不确定性，同时保留必要的动作多样性，确保训练稳定。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：单臂操作基准，包含 Spatial、Object、Goal、Long 四个子集。
    *   **RoboTwin2.0**：复杂的双臂协作任务（如双手拿瓶子、叠碗等）。
*   **Benchmark 与对比方法**：
    *   **基准**：OpenVLA-OFT（学生初始化）、SimpleVLA-RL（教师参考）。
    *   **对比方法**：离线 SFT（Octo, OpenVLA, Nora）、在线 RL（基于 GRPO 算法）。
*   **实验设置**：针对数据极度稀缺（1 条演示）和复杂形态（双臂）两种挑战场景进行了验证。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及具体的训练总时长。
*   **超参数细节**：提到了 Batch Size 为 64 或 32，组采样大小（Group Size）$G$ 在 2 到 8 之间。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的 4 个子集和 RoboTwin2.0 的 4 个复杂任务上进行了全面评估。
    *   进行了**灾难性遗忘分析**，对比了模型在已见任务和未见任务上的表现。
    *   **消融实验**：深入对比了 Reverse-KL、Forward-KL 和 Hard-CE 三种对齐目标的熵变化与成功率曲线；测试了不同组采样大小 $G$ 对性能的影响。
*   **充分性评价**：实验设计较为客观且充分，涵盖了从单臂到双臂、从数据稀缺到复杂任务的多个维度，验证了方法在效率、鲁棒性和知识保留方面的平衡。

### 6. 主要结论与发现
*   **高效率**：在 LIBERO-Long 任务中，VLA-OPD 达到相同性能的速度比纯在线 RL 快 **3 倍**。
*   **强性能**：仅用 1 条演示轨迹进行蒸馏，其效果即可媲美甚至超过使用 50 条演示训练的离线模型。
*   **缓解遗忘**：相比离线 SFT，VLA-OPD 能显著更好地保留预训练模型的通用能力（在未见任务上性能下降极小）。
*   **稳定性**：Reverse-KL 是防止策略熵爆炸或崩溃的关键，是实现稳定同策略蒸馏的最优目标。

### 7. 优点与亮点
*   **理论深刻**：对不同 KL 散度方向在机器人动作预测中的影响做了深入的理论分析和实验验证。
*   **架构优雅**：通过将 RL 的优势转化为密集监督问题，避开了稀疏奖励带来的信用分配难题。
*   **实用性强**：为 VLA 模型的持续进化提供了一个低成本、高效率的路径，尤其是在已有专家模型可用的情况下。

### 8. 不足与局限
*   **教师依赖**：该框架的前提是存在一个高性能的专家教师模型（虽然文中提到可以通过 API 或单任务专家获得）。
*   **算力成本细节缺失**：缺乏具体的训练资源消耗数据，不便于其他研究者评估复现成本。
*   **真机验证缺失**：实验全部在仿真环境（Simulation）中完成，尚未展示在真实物理机器人上的部署效果和跨域鸿沟处理能力。

（完）

---
title: "MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation"
title_zh: MMaDA-VLA：具有统一多模态指令与生成的扩散视觉-语言-动作大模型
authors: "Yang Liu, Pengxiang Ding, Tengyue Jiang, Xudong Wang, Wenxuan Song, Minghui Lin, Han Zhao, Hongyin Zhang, Zifeng Zhuang, Wei Zhao, Siteng Huang, Jinkui Shi, Donglin Wang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25406v2"
tags: ["query:q12"]
score: 10.0
evidence: 用于操作的原生预训练大规模扩散VLA模型
tldr: 针对现有视觉-语言-动作（VLA）模型在长程任务中存在的时间不一致性和环境动态捕捉能力不足等问题，本文提出了MMaDA-VLA。这是一种原生预训练的大型离散扩散VLA模型，通过将语言、图像和机器人控制嵌入统一的离散标记空间，利用掩码标记去噪技术并行生成未来目标观测和动作块。该模型在无需额外世界模型的情况下实现了动作与视觉预测的对齐，在LIBERO和CALVIN等基准测试中达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理长程任务时面临架构开销大、时间不一致以及缺乏环境动态建模机制等挑战。
method: 提出一种原生的离散扩散框架，将多模态数据统一到离散标记空间，通过掩码去噪并行生成未来视觉目标和动作序列。
result: "在LIBERO基准测试中达到98.0%的平均成功率，在CALVIN测试中达到4.78的平均长度，均创下SOTA记录。"
conclusion: MMaDA-VLA通过统一多模态理解与生成，有效提升了机器人操作的连贯性与对环境动态的感知能力。
---

## 摘要
视觉-语言-动作（VLA）模型旨在根据视觉观测和自然语言指令控制机器人进行操作。然而，现有的分层和自回归范式通常会引入架构开销，面临时间不一致性和长时程误差累积的问题，并且缺乏在不增加额外模块的情况下捕获环境动力学的机制。为此，我们提出了 MMaDA-VLA，这是一个完全原生的预训练扩散 VLA 大模型，它在单一框架中统一了多模态理解与生成。我们的核心思想是一种原生的离散扩散公式，它将语言、图像和连续机器人控制嵌入到一个离散的 token 空间中，并使用掩码 token 去噪训练单个骨干网络，以并行地联合生成未来的目标观测和动作块（action chunk）。迭代去噪实现了全局、无序的细化，在不使用辅助世界模型的情况下，将动作锚定在预测的未来视觉结果中，从而提高了长时程一致性。在仿真基准测试和真实世界任务中的实验表明，该模型达到了最先进的性能，在 LIBERO 上实现了 98.0% 的平均成功率，在 CALVIN 上实现了 4.78 的平均长度。

## Abstract
Vision-Language-Action (VLA) models aim to control robots for manipulation from visual observations and natural-language instructions. However, existing hierarchical and autoregressive paradigms often introduce architectural overhead, suffer from temporal inconsistency and long-horizon error accumulation, and lack a mechanism to capture environment dynamics without extra modules. To this end, we present MMaDA-VLA, a fully native pre-trained large diffusion VLA model that unifies multi-modal understanding and generation in a single framework. Our key idea is a native discrete diffusion formulation that embeds language, images, and continuous robot controls into one discrete token space and trains a single backbone with masked token denoising to jointly generate a future goal observation and an action chunk in parallel. Iterative denoising enables global, order-free refinement, improving long-horizon consistency while grounding actions in predicted future visual outcomes without auxiliary world models. Experiments across simulation benchmarks and real-world tasks show state-of-the-art performance, achieving 98.0% average success on LIBERO and 4.78 average length on CALVIN.

---

## 论文详细总结（自动生成）

以下是对论文《MMaDA-VLA: Large Diffusion Vision-Language-Action Model with Unified Multi-Modal Instruction and Generation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：现有的视觉-语言-动作（VLA）模型主要分为两类：一是分层架构（VLM + 策略头），这增加了架构复杂性并可能导致信息损失；二是端到端自回归模型，其在生成动作序列时往往缺乏时间一致性，且容易产生长程误差累积。
*   **核心问题**：现有模型大多缺乏显式的环境动力学建模机制（即预测未来视觉状态），且在处理动作这种本质上无序的维度时，强制采用自回归顺序会引入不必要的偏差。
*   **整体含义**：本文提出了 **MMaDA-VLA**，这是一个原生的、预训练的离散扩散 VLA 大模型。它通过将语言、图像和动作统一到同一个离散 token 空间，实现了多模态理解与生成的单一框架集成，旨在提升机器人操作的精度、长程一致性和环境感知能力。

### 2. 方法论
*   **核心思想**：利用**离散掩码扩散（Discrete Masked Diffusion）**机制，在单个骨干网络中并行生成未来的目标观测图像（Goal Observation）和动作块（Action Chunk）。
*   **关键技术细节**：
    *   **统一 Token 化**：使用 LLaDA 分词器处理文本，MAGVIT-v2 处理图像，并将连续动作的每个维度离散化为 256 个 bin，使所有模态共享一个词表。
    *   **混合注意力机制（Hybrid Attention）**：在模态内部采用双向全注意力（Bidirectional Full Attention）以实现全局信息交换；在模态之间采用因果注意力（Causal Attention）以确保信息流向。
    *   **并行生成与迭代去噪**：模型不按顺序生成动作，而是通过多次迭代（Denoising Steps）同时细化目标图像和动作序列。这种“无序”生成更符合动作维度的物理特性。
    *   **训练目标**：采用掩码 token 预测损失函数，随机掩盖生成部分的 token，训练模型根据上下文进行重建。
    *   **推理加速**：引入了**训练无关的 KV 缓存（KV Cache）**框架，通过选择性刷新策略减少重复计算，满足实时控制需求。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真基准**：**LIBERO**（评估空间、对象、目标和长程任务的泛化性）和 **CALVIN**（评估长程语言条件操作，采用 ABC→D 泛化设置）。
    *   **真实世界**：使用 AgileX Piper 机械臂，涵盖简单取放、精密堆叠、复杂储物（开抽屉）和长程整理（整理餐具）四类任务。
*   **对比方法**：对比了包括 Diffusion Policy、Octo、OpenVLA、π0、UniVLA、DreamVLA 等在内的多种 SOTA 模型，涵盖了离散动作、连续动作以及带有世界模型属性的 VLA 变体。

### 4. 资源与算力
*   **模型规模**：基于 80 亿参数（8B）的 MMaDA-Base 骨干网络。
*   **算力投入**：使用了 **8 台 Nvidia H800 GPU 节点**（每台配备 80GB 显存）。
*   **训练时长**：预训练阶段耗时约 **30 小时**。
*   **数据规模**：预训练使用了包含 DROID、BC-Z 等在内的 **6100 万步** 机器人操作数据。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖了 LIBERO 的 4 个子集和 CALVIN 的 1000 次 rollout 评估。
    *   消融实验针对世界模型模块、并行去噪机制、注意力类型及预训练效果进行了 5 组对比。
    *   真实世界实验每类任务进行 30 次测试。
*   **充分性评价**：实验设计非常充分且具有说服力。通过跨模拟器和真实环境的对比，验证了模型在多任务泛化和长程任务中的稳定性。消融实验清晰地展示了“并行去噪”和“目标图像预测”对性能的贡献，实验过程客观公平。

### 6. 主要结论与发现
*   **性能领先**：MMaDA-VLA 在 LIBERO 上达到 98.0% 的平均成功率，在 CALVIN ABC→D 任务中达到 4.78 的平均完成长度，均刷新了纪录。
*   **动力学建模的重要性**：预测未来目标图像（世界模型属性）能显著提升动作生成的质量，使模型具备“预见性”。
*   **并行优于串行**：并行生成目标图像和动作比先生成图像再生成动作的效果更好，因为模型可以在去噪过程中利用中间特征进行相互对齐。
*   **预训练红利**：大规模跨具身（Cross-embodiment）预训练显著增强了模型的泛化能力和操作精度。

### 7. 优点
*   **架构优雅**：无需额外的策略头或独立的世界模型模块，在单一 Transformer 框架内完成了理解与生成。
*   **时间一致性强**：通过扩散过程的全局细化，有效缓解了自回归模型常见的误差累积问题。
*   **推理高效**：通过 KV 缓存技术解决了扩散模型推理慢的痛点，使其能够应用于闭环控制。
*   **原生性**：从预训练阶段就引入扩散机制，避免了从自回归模型微调带来的训练-推理不一致问题。

### 8. 不足与局限
*   **视觉保真度有限**：生成的未来目标图像在精细细节（如夹爪的精确位置、复杂物体的纹理）上仍存在模糊，虽然足以辅助决策，但视觉质量有待提升。
*   **推理开销**：尽管有缓存加速，但多次迭代去噪的计算成本仍高于单次前向传播的自回归模型。
*   **数据依赖**：模型性能高度依赖于大规模、高质量的多模态机器人数据集，对于数据稀缺的特定领域可能难以直接应用。

（完）

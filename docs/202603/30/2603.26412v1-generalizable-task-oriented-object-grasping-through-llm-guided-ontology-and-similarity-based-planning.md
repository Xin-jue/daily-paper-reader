---
title: Generalizable task-oriented object grasping through LLM-guided ontology and similarity-based planning
title_zh: 通过大语言模型引导的本体和基于相似性规划的可泛化面向任务物体抓取
authors: "Hao Chen, Takuya Kiyokawa, Weiwei Wan, Kensuke Harada"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26412v1"
tags: ["query:q2"]
score: 9.0
evidence: 面向任务的物体抓取与操作
tldr: 针对任务导向型抓取（TOG）中语义识别不稳定和泛化性差的问题，本文提出一种以几何为中心的策略。通过大语言模型构建物体-部件-任务本体，结合基于采样的几何分析识别功能部件，并利用相似性匹配框架进行模仿抓取规划。该方法不依赖视觉语义特征，显著提升了在未知物体和任务上的泛化能力与抓取精度。
selection_source: fresh_fetch
motivation: 现有的基于视觉语言模型的任务导向抓取方法在部件识别和抓取推理上存在不稳定性，难以泛化到多样化的物体和任务。
method: 提出一种结合LLM引导的本体库、基于采样的几何分析以及相似性匹配模仿规划的几何中心化策略。
result: 实验证明该方法在功能部件选择、识别及抓取生成方面具有高精度，并能有效扩展至新类别物体。
conclusion: 该研究通过几何分析与知识引导相结合，为实现更具泛化性和鲁棒性的任务导向型机器人抓取提供了有效方案。
---

## 摘要
面向任务的抓取（TOG）比简单的物体抓取更具挑战性，因为它需要精确识别物体部件并仔细选择抓取区域，以确保有效且稳健的操作。虽然最近的方法通过训练大规模视觉语言模型，将部件级物体分割与任务感知抓取规划相结合，但它们在部件识别和抓取推理方面的不稳定性限制了其在不同物体和任务中的泛化能力。为了解决这一问题，我们提出了一种新颖的、以几何为中心的可泛化 TOG 策略，该策略不依赖于视觉识别的语义特征，从而有效克服了基于模型的方法对视角的敏感性。我们的主要建议包括：1）一种基于大语言模型（LLM）构建的“物体-部件-任务”本体，用于根据直观的人类指令选择功能部件；2）一种基于采样的几何分析方法，通过结合多种点分布和距离度量，从观测到的点云中识别所选物体部件；3）一种用于模仿抓取规划的相似性匹配框架，利用具有预先存在的分割和抓取知识的相似已知物体作为参考，指导未知目标的规划。我们通过真实世界实验验证了该方法在功能部件选择、识别和抓取生成方面的高准确性。此外，我们通过扩展现有的本体知识，展示了该方法对新类别物体的泛化能力，体现了其对广泛物体和任务的适应性。

## Abstract
Task-oriented grasping (TOG) is more challenging than simple object grasping because it requires precise identification of object parts and careful selection of grasping areas to ensure effective and robust manipulation. While recent approaches have trained large-scale vision-language models to integrate part-level object segmentation with task-aware grasp planning, their instability in part recognition and grasp inference limits their ability to generalize across diverse objects and tasks. To address this issue, we introduce a novel, geometry-centric strategy for more generalizable TOG that does not rely on semantic features from visual recognition, effectively overcoming the viewpoint sensitivity of model-based approaches. Our main proposals include: 1) an object-part-task ontology for functional part selection based on intuitive human commands, constructed using a Large Language Model (LLM); 2) a sampling-based geometric analysis method for identifying the selected object part from observed point clouds, incorporating multiple point distribution and distance metrics; and 3) a similarity matching framework for imitative grasp planning, utilizing similar known objects with pre-existing segmentation and grasping knowledge as references to guide the planning for unknown targets. We validate the high accuracy of our approach in functional part selection, identification, and grasp generation through real-world experiments. Additionally, we demonstrate the method's generalization capabilities to novel-category objects by extending existing ontological knowledge, showcasing its adaptability to a broad range of objects and tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Generalizable task-oriented object grasping through LLM-guided ontology and similarity-based planning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**任务导向型抓取（Task-Oriented Grasping, TOG）**。与传统的“捡起并放置”不同，TOG 要求机器人根据具体任务（如“倒水”或“递剪刀”）识别物体的特定功能部件（如杯柄或刀刃），并选择合适的抓取位姿。
*   **研究动机**：现有的视觉语言模型（VLM）在部件识别和抓取推理上存在不稳定性，且对观察视角高度敏感，难以泛化到未见过的物体类别。
*   **核心目标**：提出一种以几何为中心的策略，通过大语言模型（LLM）的语义推理能力与基于几何相似性的规划相结合，实现鲁棒且可泛化的任务导向抓取。

### 2. 方法论
该方法不依赖视觉识别的语义特征，而是采用“语义引导+几何匹配”的流程：
*   **LLM 引导的物体-部件-任务本体**：
    *   构建离线本体库（定义物体、部件层级及任务约束）。
    *   利用 GPT-4o 作为在线推理引擎，通过优化的 Prompt（分步推理和答案模板）将人类自然语言指令映射为具体的功能部件名称。
*   **基于采样的几何部件识别**：
    *   使用 SAM 获取物体掩码并投影为点云。
    *   引入**多指标相似性评估**：结合主成分分析（PCA）分布差异、点对点距离（PPD）和中心对中心距离（CCD），在观测点云中通过采样和聚类寻找与模板部件最相似的点簇。
*   **任务导向抓取规划与知识迁移**：
    *   **局部到全局点云配准**：先对齐功能部件，再通过旋转优化对齐整个物体，从而获取变换矩阵。
    *   **模仿抓取规划**：将预存的模板抓取位姿迁移至目标物体。
    *   **稳定性感知位置调整**：通过局部包围盒（LBB）估计，对抓取中心进行微调，确保抓取点位于物体几何中心以提升稳定性。

### 3. 实验设计
实验在真实世界环境下进行，使用 UR5e 机械臂、Robotiq 2F-140 夹爪和 RealSense D435 相机。
*   **测试对象**：涵盖马克杯、瓶子、剪刀等常见类别，以及雨伞、牙膏、螺丝刀等新类别物体。
*   **对比方法（Benchmarks）**：
    *   **部件识别（PRA）**：对比 VLPart（基于视觉语言模型的分割）。
    *   **抓取选择（GSA）**：对比 ShapeGrasp（基于几何分解）和 GraspGPT（基于端到端学习）。
    *   **抓取成功率（GSR）**：对比 GraspNet 和 HGGD（通用抓取检测基准）。
*   **消融实验**：验证了“局部到全局配准”和“位置调整策略”对成功率的影响。

### 4. 资源与算力
*   **硬件配置**：计算在配备 **AMD Ryzen 7 5800H CPU** 和 **NVIDIA GeForce RTX 3060 GPU** 的笔记本电脑上完成。
*   **训练时长**：该方法属于**推理驱动型**而非大规模训练型。LLM 调用为在线 API，几何匹配和配准过程利用了并行计算。文中提到 512 次旋转尝试的优化过程耗时较短，匹配过程在模板数量为 3-5 个时，运行时间在 0.6s-0.9s 左右。

### 5. 实验数量与充分性
*   **实验规模**：
    *   部件识别实验：45 次尝试。
    *   抓取选择实验：90 次尝试。
    *   抓取成功率实验：90 次尝试。
    *   泛化性实验：针对 6 种完全不同的新类别物体进行了 30 组测试。
*   **充分性与客观性**：实验设计较为全面，涵盖了准确率、成功率、鲁棒性（遮挡、噪声、缩放）以及泛化性等多个维度。对比方法选择了当前领域内具有代表性的 SOTA 模型，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **几何中心策略的优越性**：相比于纯视觉语义模型（如 VLPart），基于几何相似性的匹配在不同视角下表现更稳定，部件识别准确率（PRA）提升至 88.9%。
*   **LLM 推理的可靠性**：通过 Prompt 优化，LLM 在指令解析和部件关联上的准确率达到了 100%。
*   **泛化能力强**：即使只有少量模板（每个类别约 3 个），系统也能通过本体知识的扩展（如将“雨伞”映射为“马克杯柄”结构）成功处理新类别物体。
*   **抓取稳定性**：提出的稳定性调整策略显著降低了操作过程中的失败率（如倒水时的泼溅）。

### 7. 优点（亮点）
*   **解耦设计**：将复杂的任务推理（语言）、部件识别（几何）和抓取检测（动作）解耦，提升了系统的可解释性和调试效率。
*   **视角无关性**：通过 3D 几何匹配克服了 2D 视觉模型在不同角度下的识别失效问题。
*   **低数据依赖**：不需要大规模标注数据集进行重头训练，仅需少量 CAD 模板即可实现跨类别泛化。

### 8. 不足与局限
*   **极端环境限制**：在物体高度透明、环境光照极强或场景极度拥挤导致几何特征严重缺失时，方法可能失效。
*   **完全新颖物体的挑战**：如果目标物体在语义和几何上与现有本体库没有任何相似之处，系统将无法处理。
*   **后续操作缺失**：目前研究重点在于“抓取”动作，尚未深入探讨抓取后的复杂连续操作任务（如切菜的具体动作控制）。

（完）

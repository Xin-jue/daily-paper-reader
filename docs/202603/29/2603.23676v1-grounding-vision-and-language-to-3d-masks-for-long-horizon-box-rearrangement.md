---
title: Grounding Vision and Language to 3D Masks for Long-Horizon Box Rearrangement
title_zh: 将视觉和语言对齐到 3D 掩码以实现长程箱体重排
authors: "Ashish Malik, Caleb Lowe, Aayam Shrestha, Stefan Lee, Fuxin Li, Alan Fern"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23676v1"
tags: ["query:q10"]
score: 9.0
evidence: 将视觉和语言落地到长程重排任务中
tldr: "针对长程3D物体重新排列任务，现有方法在处理复杂几何和多目标时存在局限。本文提出RAMP-3D，通过将视觉语言模型扩展到3D掩码预测，将规划任务转化为序列化的“抓取对象”和“放置区域”掩码预测。该方法在包含多达30个箱子的仓库环境中表现优异，成功率达79.5%，为长程规划提供了一种基于掩码的反应式策略新思路。"
selection_source: fresh_fetch
motivation: 传统的符号规划器和2D视觉语言模型在处理具有复杂3D几何约束和大量物体的长程重排任务时表现不佳。
method: 提出RAMP-3D模型，通过预测成对的3D掩码（抓取目标和放置区域）来反应式地生成多步取放动作。
result: "在11种仓库任务变体中实现了79.5%的成功率，显著优于基于2D视觉语言模型的基准方法。"
conclusion: 基于3D掩码的反应式策略是替代传统符号规划流水线、实现长程3D任务规划的一种极具潜力的方法。
---

## 摘要
我们研究了在 3D 环境中，仅利用视觉观测从不完全指定的自然语言目标出发进行长程规划，重点关注多步 3D 箱体重排任务。现有方法通常依赖于符号规划器（其状态和目标的关联对齐较为脆弱），或者依赖于从 2D 视觉语言模型 (VLM) 直接生成动作序列。这两种方法在处理大量物体、丰富的 3D 几何形状以及隐式语义约束的推理时都面临困难。3D VLM 的最新进展展示了自然语言指代物与 3D 分割掩码之间的强对齐能力，这暗示了更通用规划能力的潜力。我们扩展了现有的 3D 对齐模型，并提出了反应式动作掩码规划器 (RAMP-3D)，该规划器将长程规划建模为成对 3D 掩码的序列反应式预测：一个“哪个物体”掩码指示抓取对象，一个“哪个目标区域”掩码指定放置位置。由此产生的系统处理 RGB-D 观测和自然语言任务规范，以反应式地生成用于 3D 箱体重排的多步取放动作。我们在包含 1-30 个箱体和多种自然语言约束的仓库式环境中，针对 11 种任务变体进行了实验。RAMP-3D 在长程重排任务上实现了 79.5% 的成功率，并显著优于基于 2D VLM 的基准模型，证明了基于掩码的反应式策略是长程规划中符号流水线的一个极具前景的替代方案。

## Abstract
We study long-horizon planning in 3D environments from under-specified natural-language goals using only visual observations, focusing on multi-step 3D box rearrangement tasks. Existing approaches typically rely on symbolic planners with brittle relational grounding of states and goals, or on direct action-sequence generation from 2D vision-language models (VLMs). Both approaches struggle with reasoning over many objects, rich 3D geometry, and implicit semantic constraints. Recent advances in 3D VLMs demonstrate strong grounding of natural-language referents to 3D segmentation masks, suggesting the potential for more general planning capabilities. We extend existing 3D grounding models and propose Reactive Action Mask Planner (RAMP-3D), which formulates long-horizon planning as sequential reactive prediction of paired 3D masks: a "which-object" mask indicating what to pick and a "which-target-region" mask specifying where to place it. The resulting system processes RGB-D observations and natural-language task specifications to reactively generate multi-step pick-and-place actions for 3D box rearrangement. We conduct experiments across 11 task variants in warehouse-style environments with 1-30 boxes and diverse natural-language constraints. RAMP-3D achieves 79.5% success rate on long-horizon rearrangement tasks and significantly outperforms 2D VLM-based baselines, establishing mask-based reactive policies as a promising alternative to symbolic pipelines for long-horizon planning.

---

## 论文详细总结（自动生成）

这是一份关于论文《Grounding Vision and Language to 3D Masks for Long-Horizon Box Rearrangement》的结构化深度分析总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在复杂的 3D 仓库环境中，仅根据模糊的自然语言指令（如“按颜色分组，黄色优先”）和原始视觉观测，实现长程（Multi-step）的箱体重新排列规划。
*   **研究动机**：
    *   **传统符号规划器**：依赖手工设计的谓词和状态表示，在处理噪声感知和复杂的 3D 几何约束时非常脆弱。
    *   **2D 视觉语言模型 (VLM)**：虽然具备常识推理能力，但缺乏对 3D 空间、遮挡和体积关系的理解，难以处理精确的 3D 放置任务。
    *   **研究目标**：利用 3D VLM 强大的“语言-3D 掩码”对齐能力，直接将规划任务转化为 3D 空间中的掩码选择问题，从而避开显式的符号层。

### 2. 方法论：RAMP-3D
*   **核心思想**：将长程规划建模为序列化的反应式预测。在每一步，模型输出两个 3D 掩码：**“抓取哪个物体” (Which-object)** 和 **“放置到哪个区域” (Which-target-region)**。
*   **关键技术细节**：
    *   **基础架构**：基于 UniVLG（一种 3D 视觉语言对齐模型）。输入多视图 RGB-D 图像，通过 Swin Transformer 提取特征并投影到 3D 空间进行体素化融合。
    *   **配对一致性模块 (Pair-contrastive features)**：为了确保抓取物体与放置目标匹配，引入了学习到的查询投影（$q_i$ 为抓取嵌入，$k_j$ 为放置嵌入），通过计算兼容性得分 $s_{ij} = \langle q_i, k_j \rangle$ 来联合选择动作对。
    *   **终止预测头 (Done Head)**：一个轻量级的 MLP，用于预测任务是否完成。
    *   **掩码空间动作表示**：动作直接在 3D 点云掩码上定义，通过将预测掩码映射回物体 ID 或几何坐标来执行动作。
*   **算法流程**：接收 RGB-D 和指令 -> 生成 3D 视觉 Token -> Transformer 解码器结合文本查询 -> 输出成对掩码和终止概率 -> 执行取放动作 -> 刷新观测并循环。

### 3. 实验设计
*   **场景与数据集**：在 IsaacSim 中构建了一个 12m x 12m 的仓库环境，包含多达 30 个箱子、托盘、货架和干扰物。
*   **任务变体**：设计了 **11 种任务类型**，涵盖高度限制、颜色优先级、货架/托盘优先级、空间排序（左右）、堆叠限制和可访问性约束。
*   **Benchmark 与对比方法**：
    *   **2D-pointer (基准)**：结合了 GPT-5.1（高层规划）和 Molmo-72B（2D 指向模型）。GPT 根据图像描述动作，Molmo 在俯视图中指向目标点。
    *   **评估模式**：分为“对齐目标模式 (Snap-to-target)”（理想化放置）和“自由形式模式 (Free-form)”（完全依赖模型预测坐标）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练总时长。
*   **相关信息**：提到使用了 `gpt-oss-120b` 进行语言增强，并获得了 NVIDIA 学术资助，暗示训练过程涉及大规模 GPU 集群。

### 5. 实验数量与充分性
*   **实验规模**：
    *   训练集包含约 9.5k 个场景样本，测试集包含 2.2k 个独立样本。
    *   针对 11 种任务变体分别进行了单步准确率和长程成功率的评估。
    *   进行了**消融实验**，验证了辅助任务（如识别空位、已放置箱子等）和语言泛化性的影响。
*   **充分性评价**：实验设计较为充分，涵盖了从简单到极高复杂度（30 个物体）的场景。对比基准 2D-pointer 选用了当前最强的模型组合，体现了对比的公平性。

### 6. 主要结论与发现
*   **性能优异**：RAMP-3D 在长程重排任务中达到了 **79.5%** 的总成功率，远超 2D 基准模型（单步有效性仅 41.4%）。
*   **3D 优势**：直接在 3D 掩码空间操作能更好地处理几何约束，尤其是在处理货架（多层结构）时，2D 模型几乎无法胜任。
*   **复杂度敏感**：随着箱子数量增加（21-30 个），成功率有所下降，主要原因是长程操作中的误差累积。
*   **语言泛化**：通过 LLM 增强，模型对未见过的指令模板表现出良好的鲁棒性。

### 7. 优点与亮点
*   **创新的动作空间**：将规划问题简化为 3D 掩码预测，实现了感知与规划的深度融合，无需复杂的符号转换。
*   **端到端 3D 推理**：能够直接从原始点云中理解“堆叠高度”、“可访问性”等抽象 3D 概念。
*   **配对机制**：引入配对对比损失，解决了多目标场景下抓取与放置目标的匹配歧义问题。

### 8. 不足与局限
*   **反应式规划限制**：模型本质上是反应式的（Reactive），缺乏前瞻性搜索（Lookahead Search）。在处理需要复杂预操作（如为了拿底层箱子必须先移开顶层箱子）的任务时可能失败。
*   **Sim-to-Real 差距**：实验完全在仿真环境（IsaacSim）中进行，未在真实机器人硬件上验证，真实环境中的光照、深度噪声和物理接触可能影响掩码精度。
*   **语义多样性有限**：虽然箱子数量多，但物体种类（箱子、托盘、货架）相对单一，尚未扩展到更通用的家庭或非结构化环境。

（完）

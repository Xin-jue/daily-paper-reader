---
title: "SLAT-Phys: Fast Material Property Field Prediction from Structured 3D Latents"
title_zh: SLAT-Phys：基于结构化3D潜变量的快速材料属性场预测
authors: "Rocktim Jyoti Das, Dinesh Manocha"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23973v1"
tags: ["query:q6"]
score: 9.0
evidence: 用于数字孪生生成的材料属性预测
tldr: SLAT-Phys 是一种从单张 RGB 图像快速预测 3D 资产空间变化物理属性（杨氏模量、密度、泊松比）的方法。它利用预训练 3D 生成模型的结构化潜空间特征，通过轻量级解码器直接估计属性场，无需显式 3D 重建或体素化。该方法在保持高精度的同时，将计算速度提升了 120 倍，每物体仅需 9.9 秒，显著优化了物理仿真和机器人领域的资产处理流程。
selection_source: fresh_fetch
motivation: 现有的视觉物理属性估计方法计算成本高、速度慢，且通常依赖复杂的 3D 重建或体素化预处理。
method: 利用预训练 3D 生成模型中蕴含几何和语义先验的结构化潜特征，结合轻量级神经解码器直接预测连续的物理参数场。
result: 在 NVIDIA RTX A5000 上处理单个物体仅需 9.9 秒，比现有方法快 120 倍，且在参数预测精度上具有竞争力。
conclusion: SLAT-Phys 证明了利用生成模型的潜空间特征可以实现高效且准确的单图物理属性估计，为实时物理仿真提供了新途径。
---

## 摘要
估计3D资产的材料属性场对于基于物理的模拟、机器人技术和数字孪生生成至关重要。现有的基于视觉的方法要么成本高昂且速度缓慢，要么依赖于3D信息。我们提出了 SLAT-Phys，这是一种端到端的方法，可以直接从单张 RGB 图像预测 3D 资产的空间变化材料属性场，而无需显式的 3D 重建。我们的方法利用了来自预训练 3D 资产生成模型的空间组织潜特征，该模型编码了丰富的几何和语义先验，并训练了一个轻量级神经解码器来估计杨氏模量、密度和泊松比。潜表示中关于物体几何和外观的粗略体积布局及语义线索实现了准确的材料估计。我们的实验表明，与现有方法相比，我们的方法在预测连续材料参数方面具有竞争力的准确性，同时显著减少了计算时间。特别是，SLAT-Phys 在 NVIDIA RTXA5000 GPU 上处理每个物体仅需 9.9 秒，并避免了重建和体素化预处理。与之前的方法相比，这实现了 120 倍的加速，并能够从单张图像中更快地估计材料属性。

## Abstract
Estimating the material property field of 3D assets is critical for physics-based simulation, robotics, and digital twin generation. Existing vision-based approaches are either too expensive and slow or rely on 3D information. We present SLAT-Phys, an end-to-end method that predicts spatially varying material property fields of 3D assets directly from a single RGB image without explicit 3D reconstruction. Our approach leverages spatially organised latent features from a pretrained 3D asset generation model that encodes rich geometry and semantic prior, and trains a lightweight neural decoder to estimate Young's modulus, density, and Poisson's ratio. The coarse volumetric layout and semantic cues of the latent representation about object geometry and appearance enable accurate material estimation. Our experiments demonstrate that our method provides competitive accuracy in predicting continuous material parameters when compared against prior approaches, while significantly reducing computation time. In particular, SLAT-Phys requires only 9.9 seconds per object on an NVIDIA RTXA5000 GPU and avoids reconstruction and voxelization preprocessing. This results in 120x speedup compared to prior methods and enables faster material property estimation from a single image.

---

## 论文详细总结（自动生成）

以下是对论文《SLAT-Phys: Fast Material Property Field Prediction from Structured 3D Latents》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从单张 RGB 图像中快速且准确地预测 3D 物体空间变化的物理属性场（如杨氏模量 $E$、密度 $\rho$ 和泊松比 $\nu$）。
*   **研究动机**：
    *   **现有方法局限性**：目前的视觉物理属性估计方法要么依赖昂贵的微分物理模拟优化（耗时数小时），要么需要复杂的 3D 重建（如 NeRF 或 Gaussian Splatting）和多视图特征聚合（耗时约 20 分钟），难以满足机器人实时交互或大规模数据集处理的需求。
    *   **潜在机遇**：大规模 3D 生成模型（如 TRELLIS）学习到的“结构化潜变量”（SLAT）已经包含了丰富的几何和语义先验，可能隐含了物理推理所需的线索。

### 2. 方法论
*   **核心思想**：跳过显式的 3D 重建步骤，直接利用预训练 3D 生成模型的结构化潜空间特征，通过轻量级解码器回归物理参数。
*   **关键技术细节**：
    1.  **特征提取**：使用冻结的 TRELLIS 编码器将单图转换为稀疏的结构化潜变量（SLAT），每个占据的体素包含坐标和 8 维潜特征。
    2.  **物理解码器**：采用轻量级的稀疏 Swin-style Transformer 架构。它在局部体素邻域内进行窗口自注意力计算，捕捉空间依赖性。
    3.  **对齐机制**：由于生成模型与标注数据的坐标系可能不一致，使用迭代最近点（ICP）算法将物理标注体素网格与 SLAT 坐标系进行刚性对齐。
    4.  **多任务学习**：模型同时预测连续的物理参数（回归头）和离散的材料类别（分类头）。
    5.  **下游仿真**：预测的参数通过最近邻插值传递给从同一 SLAT 解码出的 3D 高斯点云，随后输入物质点法（MPM）求解器进行物理仿真。

### 3. 实验设计
*   **数据集**：使用 **PixieVerse** 数据集，包含 1,624 个高质量 3D 资产，涵盖 10 个语义类别（如植物、玩具、运动器材、颗粒材料等）。
*   **Benchmark 与对比方法**：
    *   **NeRF2Physics**：基于大语言模型（LLM）提议和 CLIP 特征相似度传播的方法。
    *   **Pixie**：目前最先进的基于前馈网络预测体积材料场的方法（但需要显式重建）。
*   **评估指标**：归一化空间下的均方误差（MSE）以及材料分类准确率。

### 4. 资源与算力
*   **硬件环境**：所有实验和推理均在单张 **NVIDIA RTX A5000 GPU** 上完成。
*   **推理耗时**：处理单个物体仅需 **9.9 秒**（包含图像预处理、特征提取和物理预测）。
*   **训练细节**：使用 AdamW 优化器，学习率为 1e-4，采用余弦退火调度和混合精度训练。文中未明确给出总训练时长，但提到消融实验中模型在 2,000 步内即可收敛。

### 5. 实验数量与充分性
*   **实验规模**：在 PixieVerse 的 10 个类别上进行了全面测试。
*   **消融实验**：对比了不同容量的解码器架构（Small, Medium, Large），验证了模型参数量增加对预测精度的提升作用。
*   **定性实验**：展示了雪人破碎、花朵随风摆动、橡胶鸭坠落等多种物理仿真场景。
*   **充分性评价**：实验设计较为客观，通过与现有 SOTA 方法在相同硬件和数据集上的端到端耗时对比，有力证明了其速度优势。

### 6. 主要结论与发现
*   **速度飞跃**：SLAT-Phys 实现了比 Pixie 快 **128 倍**、比 NeRF2Physics 快 **121 倍** 的推理速度。
*   **精度持平**：在大幅提速的同时，其物理参数预测的准确性与需要复杂重建的方法（Pixie）相当，远超基于 LLM 的方法。
*   **表征能力**：证明了生成模型的结构化潜变量不仅是几何先验，还编码了足以支持物理推理的语义信号。

### 7. 优点
*   **极高效率**：摆脱了体素化、多视图渲染和显式重建的负担，使单图物理属性估计达到近实时水平。
*   **端到端一致性**：几何重建（3DGS）与物理预测共享同一套潜变量表征，保证了数字孪生生成的内在一致性。
*   **轻量化**：解码器架构简洁，易于集成到现有的机器人或仿真管线中。

### 8. 不足与局限
*   **数据偏差风险**：训练依赖的 PixieVerse 标注是由视觉语言模型（VLM）生成的，可能存在标注噪声或不符合真实物理规律的情况。
*   **模态单一**：仅依赖视觉输入。对于某些视觉特征不明显的材料（如重量相似但硬度不同的塑料），仅靠图像可能难以完美区分，未来可能需要引入触觉或音频模态。
*   **泛化限制**：虽然在 10 类物体上表现良好，但对于超出训练集分布的极端异形物体或复合材料，其预测能力尚待验证。

（完）

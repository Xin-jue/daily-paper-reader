---
title: "FlowScene: Style-Consistent Indoor Scene Generation with Multimodal Graph Rectified Flow"
title_zh: FlowScene：基于多模态图修正流的风格一致室内场景生成
authors: "Zhifei Yang, Guangyao Zhai, Keyang Lu, YuYang Yin, Chao Zhang, Zhen Xiao, Jieyi Long, Nassir Navab, Yikai Wang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19598v1"
tags: ["query:q3"]
score: 9.0
evidence: 用于室内场景生成的三分支场景生成模型
tldr: 本研究针对室内场景生成中真实感与控制力难以兼得的问题，提出了FlowScene模型。该模型采用多模态图作为条件，通过三分支架构协同生成场景布局、物体形状和纹理。其核心是紧耦合的修正流（Rectified Flow）模型，能在生成过程中交换物体信息，实现跨图的协作推理。FlowScene不仅实现了对物体属性的精细控制，还确保了场景整体在结构和外观上的风格一致性，在生成质量和用户偏好上表现卓越。
selection_source: fresh_fetch
motivation: 现有场景生成方法在物体级精细控制、场景整体风格一致性以及高保真纹理表现力方面存在局限。
method: 提出一种基于多模态图的三分支生成模型，利用紧耦合的修正流机制在布局、形状和纹理生成过程中进行协作推理。
result: 实验结果显示，FlowScene在生成真实感、风格一致性以及与人类偏好对齐度方面均显著优于现有的语言和图驱动基准模型。
conclusion: 该研究通过多模态图修正流框架，成功实现了具备高度风格一致性和精细物体控制能力的室内场景生成。
---

## 摘要
场景生成具有广泛的工业应用，要求具备高真实感以及对几何和外观的精确控制。语言驱动的检索方法通过大型物体数据库构建合理的场景，但忽略了物体层面的控制，且往往无法保证场景层面的风格一致性。基于图的表述通过显式建模关系，提供了更高的物体可控性并有助于实现整体一致性，但现有方法难以生成高保真的纹理结果，从而限制了其实用性。我们提出了 FlowScene，这是一种以多模态图为条件的场景生成模型，采用三分支结构协同生成场景布局、物体形状和物体纹理。其核心是一个紧耦合的修正流（rectified flow）模型，在生成过程中交换物体信息，从而实现跨图的协同推理。这实现了对物体形状、纹理和关系的细粒度控制，同时在结构和外观上强制执行场景层面的风格一致性。大量实验表明，FlowScene 在生成真实感、风格一致性以及与人类偏好的一致性方面，均优于基于语言条件和基于图条件的基准模型。

## Abstract
Scene generation has extensive industrial applications, demanding both high realism and precise control over geometry and appearance. Language-driven retrieval methods compose plausible scenes from a large object database, but overlook object-level control and often fail to enforce scene-level style coherence. Graph-based formulations offer higher controllability over objects and inform holistic consistency by explicitly modeling relations, yet existing methods struggle to produce high-fidelity textured results, thereby limiting their practical utility. We present FlowScene, a tri-branch scene generative model conditioned on multimodal graphs that collaboratively generates scene layouts, object shapes, and object textures. At its core lies a tight-coupled rectified flow model that exchanges object information during generation, enabling collaborative reasoning across the graph. This enables fine-grained control of objects' shapes, textures, and relations while enforcing scene-level style coherence across structure and appearance. Extensive experiments show that FlowScene outperforms both language-conditioned and graph-conditioned baselines in terms of generation realism, style consistency, and alignment with human preferences.

---

## 论文详细总结（自动生成）

这是一份关于论文《FlowScene: Style-Consistent Indoor Scene Generation with Multimodal Graph Rectified Flow》的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在生成 3D 室内场景时，同时实现**高保真度**、**细粒度物体控制**以及**场景级的风格一致性**。
*   **研究动机**：
    *   **语言驱动方法**（如 Holodeck）虽方便，但缺乏对单个物体的精确控制，且难以保证物体间风格（如材质、几何特征）的统一。
    *   **图驱动方法**（如 CommonScenes）通过显式建模关系提高了可控性，但现有方法通常无法端到端生成高质量纹理，导致结果真实感不足。
    *   **风格失调**：在缺乏视觉参考时，模型难以根据场景上下文自动推断并匹配其他物体的风格。

### 2. 论文提出的方法论
*   **核心思想**：提出一种基于**多模态图（Multimodal Graph）**驱动的**三分支修正流（Tri-branch Rectified Flow）**模型，通过在生成过程中进行物体间的信息交换，确保布局、形状和纹理的协同一致。
*   **关键技术细节**：
    *   **多模态图表示**：图节点可融合文本描述、参考图像或两者结合；边编码空间（如“在左侧”）和语义（如“同风格”）关系。
    *   **多模态图修正流（MGRF）**：相比传统的扩散模型，修正流采用确定性 ODE 采样，路径更直，训练更稳定且推理更快。
    *   **三分支架构**：
        1.  **布局分支**：生成物体的 3D 边界框（位置、大小、旋转）。
        2.  **形状分支**：利用 VQ-VAE 潜空间生成物体的体素化几何结构。
        3.  **纹理分支**：在生成的几何基础上，生成结构化潜码并解码为高分辨率纹理。
    *   **信息交换单元（InfoExchangeUnit）**：在每个去噪步骤中，利用 **Triplet-GCN** 沿着图的边传递节点特征。这意味着一个物体的生成会实时参考邻近物体的状态，从而强制执行风格一致性。

### 3. 实验设计
*   **数据集**：主要使用 **3D-FRONT** 及其增强版 **SG-FRONT**（包含约 4.5 万个物体实例和 15 种关系类型）。
*   **场景类型**：涵盖卧室（Bedroom）、客厅（Living Room）和餐厅（Dining Room）。
*   **对比基准（Baselines）**：
    *   **免训练/语言驱动类**：Holodeck, LayoutVLM。
    *   **图驱动生成类**：CommonScenes, EchoScene, MMGDreamer。
*   **评估指标**：
    *   **真实感**：FID, FID-CLIP, KID。
    *   **物体质量**：MMD, COV, 1-NNA。
    *   **可控性与一致性**：CLIPScore, FPVScore（多视角感知评分），以及人类偏好调查。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 (80GB)** GPU 进行实验。
*   **训练细节**：
    *   三个分支独立训练，每个分支优化约 **8.2 万步**。
    *   Batch Size 为 196，使用 AdamW 优化器。
*   **推理效率**：采样步数 $K=25$。在生成布局和形状时，比 MMGDreamer 快 **84.93%**（仅需 6.83 秒）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（表 1-4）和定性视觉展示。
*   **消融实验**：非常充分，分别验证了：
    *   三个信息交换单元（LEU, SEU, TEU）对结果的影响。
    *   修正流（Flow）对比扩散模型（Diffusion）的优越性。
    *   不同模态输入比例（视觉 vs 文本）的鲁棒性。
    *   关系掩码比例对生成质量的影响。
*   **客观性**：引入了 25 人的感知研究（Perceptual Study），从 5 个维度进行主观评分，确保了评价的全面性。

### 6. 论文的主要结论与发现
*   **性能领先**：FlowScene 在场景真实感、物体几何精度和风格一致性上均显著优于现有 SOTA 方法。
*   **高效性**：修正流架构在保持高质量的同时，大幅缩短了推理时间。
*   **风格迁移能力**：即使某些节点只有文本描述，模型也能通过图关系从有图像参考的节点中“学习”并应用相似的风格。
*   **多模态灵活性**：模型能无缝处理纯文本、纯图像或混合输入，适应不同的用户交互场景（如 GUI 选择或自然语言描述）。

### 7. 优点（亮点）
*   **端到端纹理生成**：突破了以往图驱动方法只能检索或生成无纹理几何的限制。
*   **紧耦合一致性**：通过在去噪迭代中加入 GCN 信息交换，从底层机制上解决了物体间“各长各的”不协调问题。
*   **交互性强**：提供了语言驱动和 GUI 驱动两种应用接口示例，展示了极高的实用价值。

### 8. 不足与局限
*   **数据集依赖**：目前仅在合成数据集（3D-FRONT）上验证，在真实世界复杂场景或室外场景的泛化能力有待探索。
*   **上游误差传递**：场景生成高度依赖于多模态图的质量。如果前端 LLM/VLM 解析出的图结构有误（如关系缺失），可能导致物体穿插或布局不合理。
*   **视角偏差**：若输入的参考图像视角极度受限，生成的 3D 形状可能存在背面畸变。

（完）

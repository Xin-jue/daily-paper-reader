---
title: "VAMPO: Policy Optimization for Improving Visual Dynamics in Video Action Models"
title_zh: VAMPO：通过策略优化提升视频动作模型中的视觉动力学
authors: "Zirui Ge, Pengxiang Ding, Baohua Yin, Qishen Wang, Zhiyong Xie, Yemin Wang, Jinbo Wang, Hengtao Li, Runze Suo, Wenxuan Song, Han Zhao, Shangke Lyu, Zhaoxin Fan, Haoang Li, Ran Cheng, Cheng Chi, Huibin Ge, Yaozhi Luo, Donglin Wang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19370v1"
tags: ["query:q4"]
score: 9.0
evidence: 改进视觉语言动作系统中的视觉动力学
tldr: 针对视频动作模型在机器人控制中存在的视觉动力学精度不足问题（如物体位姿、接触时机等误差），本文提出了VAMPO框架。该方法将扩散模型的去噪过程建模为序列决策过程，通过策略优化（结合GRPO和欧拉混合采样器）直接优化潜空间中的视觉动力学奖励。实验证明，VAMPO显著提升了视频预测的准确性，进而增强了下游动作生成的成功率和泛化能力。
selection_source: fresh_fetch
motivation: 现有的基于扩散的视频预测模型采用似然代理目标函数，导致其在处理对操作至关重要的精确视觉动力学（如空间关系和接触时机）时存在偏差。
method: 将多步去噪过程视为序列决策任务，引入欧拉混合采样器以降低策略梯度估计的方差，并结合GRPO算法和非对抗性奖励进行策略优化。
result: 在模拟和真实世界的多种操作任务中，VAMPO有效改善了任务相关的视觉动力学，提升了下游动作生成的质量和泛化性能。
conclusion: 通过策略优化直接对齐视觉动力学，是提升视频动作模型在复杂机器人操作任务中表现的一种有效且实用的后训练方法。
---

## 摘要
视频动作模型是视觉-语言-动作（VLA）系统极具吸引力的基础，因为它们可以从大规模视频数据中学习视觉动力学，并将这些知识迁移到下游机器人控制中。然而，目前的基于扩散的视频预测器通常采用似然代理目标进行训练，这虽然鼓励了全局合理的预测，但并未显式优化对操作任务至关重要的精确视觉动力学。这种目标不匹配往往会导致物体位姿、空间关系和接触时机方面的细微误差，并可能被下游策略放大。我们提出了 VAMPO，这是一个通过策略优化直接改进视频动作模型中视觉动力学的后训练框架。我们的核心思想是将多步去噪建模为一个序列决策过程，并利用在潜空间中定义的专家视觉动力学奖励来优化去噪策略。为了使该优化过程切实可行，我们引入了一种欧拉混合采样器，它仅在第一个去噪步骤中注入随机性，从而在保持后续去噪轨迹连贯性的同时，实现了可处理的低方差策略梯度估计。我们进一步将该设计与 GRPO 和可验证的非对抗性奖励相结合。在多种模拟和现实世界的操纵任务中，VAMPO 显著改善了与任务相关的视觉动力学，从而实现了更优的下游动作生成和更强的泛化能力。项目主页：https://vampo-robot.github.io/VAMPO/。

## Abstract
Video action models are an appealing foundation for Vision--Language--Action systems because they can learn visual dynamics from large-scale video data and transfer this knowledge to downstream robot control. Yet current diffusion-based video predictors are trained with likelihood-surrogate objectives, which encourage globally plausible predictions without explicitly optimizing the precision-critical visual dynamics needed for manipulation. This objective mismatch often leads to subtle errors in object pose, spatial relations, and contact timing that can be amplified by downstream policies. We propose VAMPO, a post-training framework that directly improves visual dynamics in video action models through policy optimization. Our key idea is to formulate multi-step denoising as a sequential decision process and optimize the denoising policy with rewards defined over expert visual dynamics in latent space. To make this optimization practical, we introduce an Euler Hybrid sampler that injects stochasticity only at the first denoising step, enabling tractable low-variance policy-gradient estimation while preserving the coherence of the remaining denoising trajectory. We further combine this design with GRPO and a verifiable non-adversarial reward. Across diverse simulated and real-world manipulation tasks, VAMPO improves task-relevant visual dynamics, leading to better downstream action generation and stronger generalization. The homepage is https://vampo-robot.github.io/VAMPO/.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：VAMPO

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对**视频动作模型（Video Action Models, VAM）**在机器人操纵任务中的精度瓶颈提出了解决方案。
*   **核心矛盾**：现有的基于扩散模型的视频预测器主要采用“似然代理目标”（如 ELBO）进行训练。这种目标函数倾向于生成“全局看起来合理”的图像，但缺乏对**精度敏感的视觉动力学**（如物体的微小位姿变化、空间相对关系、接触瞬间的时机）的显式优化。
*   **后果**：预测模型在潜空间或像素空间产生的细微误差会被下游的动作生成模型（AGM）放大，导致机器人在决策边界处执行错误动作，最终引发任务失败。
*   **研究意义**：通过后训练（Post-training）阶段直接优化视觉动力学的准确性，弥合生成目标与控制需求之间的鸿沟。

#### 2. 方法论：核心思想与关键技术
VAMPO 将多步去噪过程重新表述为一个**序列决策过程（MDP）**，并利用强化学习进行优化。
*   **核心思想**：将扩散模型的每一次去噪迭代视为一个动作，通过最大化终端奖励（预测结果与专家视频潜空间的相似度）来微调模型。
*   **关键技术细节**：
    *   **欧拉混合采样器（Euler Hybrid Sampler）**：为了解决策略梯度估计的高方差和长程信用分配难题，该采样器仅在**第一步**去噪时注入随机性（SDE 风格），产生高斯转移概率以便计算策略梯度；剩余步骤则保持确定性（ODE 风格），确保轨迹的连贯性。
    *   **GRPO 优化算法**：采用组相对策略优化（Group Relative Policy Optimization），通过同一条件下的多组采样进行优势估计（Advantage Estimation），无需额外的价值函数网络（Critic），提高了训练稳定性。
    *   **可验证奖励函数**：在潜空间（Latent Space）中定义奖励，结合了 L1 距离和余弦相似度，直接衡量预测动力学与专家演示（Ground Truth）的一致性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **模拟环境**：使用 **CALVIN** 及其扩展版 **L-CALVIN**（长程任务，多达10个步骤）。遵循 ABC→D 协议，即在 ABC 环境训练，在视觉分布不同的 D 环境测试泛化性。
    *   **真实世界**：在 **Agibot Genie 01**（双臂机器人）、Flexiv 和 VidowX 平台上进行验证，涵盖抓取、放置、双臂协作等任务。
*   **对比方法（Baselines）**：
    *   **VLM-based VLA**：OpenVLA, $\pi_0$, $\pi_{0.5}$。
    *   **VPM-based VLA**：UniPi, SuSIE, CLOVER（像素级）；GR-1, VidMan, Seer, VPP, Tri-VLA（潜空间级）。

#### 4. 资源与算力
论文明确列出了实验所用的硬件资源：
*   **VPM 后训练**：使用 **64 台 NVIDIA H20 GPU**，训练约 1.5k 步。
*   **AGM 训练**：使用 **8 台 NVIDIA H20 GPU**，训练约 10 个 epoch。
*   **推理与评估**：在 **NVIDIA RTX 5880 GPU** 上执行。
*   **数据规模**：CALVIN 数据集包含 18,033 条轨迹，后训练阶段使用了约 12.9 万个视频样本。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从模拟器到三种不同真实机器人平台的迁移实验，实验设计较为全面。
*   **消融实验**：针对**后训练步数、优化算法（DDPO vs. GRPO）、采样策略（全 SDE vs. 混合采样）、奖励类型（像素级 vs. 潜空间级）**做了四组详细的消融对比。
*   **客观性**：通过测量 Jacobian 矩阵的有效秩（Effective Rank）来量化“视觉-动作耦合”程度，从理论层面解释了性能提升的原因，增强了实验的说服力。

#### 6. 主要结论与发现
*   **视觉动力学与性能正相关**：优化后的 VPM 能够生成更准确的物体位姿和接触预测，直接提升了下游 AGM 的动作成功率。
*   **长程任务优势明显**：在 L-CALVIN（10步连续任务）中，VAMPO 的提升幅度远超短程任务，证明了精确动力学对减少复合误差的重要性。
*   **泛化能力增强**：即使在未见过的环境（CALVIN D）和真实世界复杂背景下，VAMPO 依然表现出极强的鲁棒性。
*   **视觉-动作耦合加深**：SVD 分析显示，优化后的模型在生成动作时会参考更多独立的视觉特征维度。

#### 7. 优点（亮点）
*   **即插即用**：作为一种后训练框架，VAMPO 不需要修改原有的模型架构，即可提升现有视频动作模型的性能。
*   **采样器创新**：Euler Hybrid Sampler 巧妙地平衡了强化学习所需的探索性（随机性）与扩散模型生成的稳定性（确定性）。
*   **效率与精度平衡**：在潜空间进行奖励计算和优化，避开了高维像素生成的冗余计算，同时保留了关键的几何和物理特征。

#### 8. 不足与局限
*   **训练稳定性**：尽管使用了 GRPO，但实验观察到训练过程中仍存在一定的性能波动（RL 算法的固有特性）。
*   **依赖专家数据**：奖励函数高度依赖于高质量的专家视频演示，在缺乏专家轨迹的开放域场景下，如何定义有效的奖励仍是挑战。
*   **实时性挑战**：虽然潜空间操作较快，但多步去噪过程在极高性能要求的实时控制（如 >50Hz）中可能仍存在延迟压力。

（完）

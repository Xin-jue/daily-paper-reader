---
title: "AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models"
title_zh: AcceRL：一种面向视觉-语言-动作模型的分布式异步强化学习与世界模型框架
authors: "Chengxuan Lu, Shukuan Wang, Yanjie Li, Wei Liu, Shiji Jin, Fuyuan Qian, Peiming Li, Baigui Sun, Yang Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18464v2"
tags: ["query:q10"]
score: 10.0
evidence: 用于视觉-语言-动作模型的分布式强化学习和世界模型框架
tldr: 针对大规模视觉-语言-动作（VLA）模型在强化学习中面临的计算效率低和数据获取难等挑战，本文提出了AcceRL框架。该框架采用完全异步且解耦的设计，通过物理隔离训练、推理和采样过程消除同步障碍，并首次将可训练的世界模型集成到分布式异步流水线中以生成虚拟经验。实验证明，AcceRL在LIBERO基准上达到了SOTA水平，展现出超线性的吞吐量扩展能力和极高的采样效率。
selection_source: fresh_fetch
motivation: 大规模VLA模型在强化学习训练中存在计算效率低下和数据获取困难的瓶颈。
method: 提出一种完全异步解耦的框架，物理隔离训练、推理与采样，并集成可插拔的世界模型生成虚拟经验。
result: 在LIBERO基准测试中取得SOTA性能，实现超线性吞吐量扩展及卓越的硬件利用率。
conclusion: AcceRL为大规模VLA模型的高效分布式强化学习训练提供了一种极具扩展性和样本效率的解决方案。
---

## 摘要
大规模视觉-语言-动作（VLA）模型的强化学习（RL）在计算效率和数据获取方面面临着重大挑战。我们提出了 AcceRL，这是一个完全异步且解耦的强化学习框架，旨在通过物理隔离训练、推理和采样（rollouts）过程来消除同步障碍。至关重要的是，AcceRL 首次将即插即用、可训练的世界模型集成到分布式异步强化学习流水线中，以生成虚拟经验。在 LIBERO 基准测试上的实验表明，AcceRL 达到了当前最先进（SOTA）的性能。在系统层面，它在吞吐量上表现出超线性扩展和高效的硬件利用率。在算法层面，世界模型增强变体在复杂控制任务中提供了前所未有的样本效率和稳健的训练稳定性。代码已在 https://github.com/distanceLu/AcceRL 公开。

## Abstract
Reinforcement learning (RL) for large-scale Vision-Language-Action (VLA) models faces significant challenges in computational efficiency and data acquisition. We propose AcceRL, a fully asynchronous and decoupled RL framework designed to eliminate synchronization barriers by physically isolating training, inference, and rollouts. Crucially, AcceRL is the first to integrate a plug-and-play, trainable world model into a distributed asynchronous RL pipeline to generate virtual experiences. Experiments on the LIBERO~\cite{liu2023libero} benchmark demonstrate that AcceRL achieves state-of-the-art (SOTA) performance. Systematically, it exhibits super-linear scaling in throughput and highly efficient hardware utilization. Algorithmically, the world-model-augmented variant delivers unprecedented sample efficiency and robust training stability in complex control tasks. Code is publicly available at https://github.com/distanceLu/AcceRL.

---

## 论文详细总结（自动生成）

这是一份关于论文《AcceRL: A Distributed Asynchronous Reinforcement Learning and World Model Framework for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大规模视觉-语言-动作（VLA）模型在强化学习（RL）训练中面临严重的**计算效率低下**和**数据获取困难**。
*   **研究背景**：传统的同步RL框架（如PPO）存在“长尾效应”，即高性能GPU必须等待缓慢的物理模拟器完成采样，导致硬件利用率极低。此外，VLA模型参数量巨大（如7B），对数据的渴求与模拟器极慢的运行速度之间存在巨大矛盾。
*   **整体含义**：本文提出了AcceRL框架，通过物理隔离训练、推理和采样过程，并引入可训练的世界模型（World Model），旨在打破物理模拟的时间限制，实现高效、可扩展的具身智能模型训练。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用**完全异步且解耦**的架构。将系统分为训练池（Trainer Pool）、推理池（Inference Pool）和采样池（Rollout Pool），消除同步屏障。
*   **关键技术细节**：
    *   **双层异步机制**：宏观上解耦训练与采样（非阻塞循环流水线）；微观上解耦环境交互与模型推理（推理即服务，采用动态窗口批处理）。
    *   **世界模型集成**：首次在异步流水线中加入基于扩散模型（DIAMOND）的世界模型，实现“在想象中学习”。通过少量真实数据微调世界模型，生成高保真虚拟经验。
    *   **策略滞后处理**：针对异步带来的策略过时问题，提出了**价值重计算（Value Re-computation）**、全局优势归一化以及**GIPO算法**（用高斯信任权重代替硬截断的PPO），以稳定收敛。
    *   **VLA优化**：包括词表瘦身（只保留动作Token）、动作感知注意力池化价值头、以及Token级别的策略优化。

### 3. 实验设计
*   **数据集/场景**：使用了 **LIBERO** 具身智能基准测试。
*   **子任务（Benchmark）**：涵盖了 LIBERO-Spatial（空间）、LIBERO-Object（物体）、LIBERO-Goal（目标）和 LIBERO-Long（长程任务）。
*   **对比方法**：
    *   **OpenVLA-OFT**：基于监督学习的微调基准。
    *   **RLinf-VLA** 与 **SimpleVLA-RL**：近期最先进的VLA强化学习框架。

### 4. 资源与算力
*   **GPU型号**：实验中明确提到了使用 **NVIDIA H200** GPU。
*   **算力规模**：
    *   训练器扩展性测试中使用了 **1至7块 GPU**。
    *   采样端扩展到了 **64个 Rollout Workers**。
*   **训练时长**：文中未明确给出总训练小时数，但强调了吞吐量（SPS）和收敛步数（如在400次更新内收敛）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了LIBERO的四大类任务，并进行了详细的扩展性分析（Throughput Scaling）。
*   **消融实验**：针对“价值重计算机制”和“GIPO算法”进行了专门的对比实验，验证了这些组件对异步训练稳定性的贡献。
*   **充分性评价**：实验设计较为充分，不仅有任务成功率的横向对比，还有系统层面的吞吐量纵向扩展测试。通过对比模型在有无世界模型下的表现，客观证明了世界模型对样本效率的巨大提升（200倍）。

### 6. 主要结论与发现
*   **性能领先**：AcceRL在LIBERO所有类别中均达到SOTA，尤其在长程任务（Long）中表现出极强的鲁棒性（成功率99.1%）。
*   **超线性扩展**：得益于ZeRO-2优化和解耦设计，训练吞吐量随GPU数量增加呈现超线性增长，硬件利用率保持在94%以上。
*   **样本效率飞跃**：集成世界模型后，仅需1万次真实环境交互即可达到传统方法需数百万次才能达到的性能，效率提升200倍。
*   **算法稳定性**：GIPO算法在处理异步带来的过时数据时，比标准PPO表现出更好的收敛平滑度和更高的性能上限。

### 7. 优点（亮点）
*   **架构创新**：彻底解决了分布式RL中的“掉队者”问题（Straggler Effect），实现了推理与交互的极致解耦。
*   **即插即用**：世界模型模块化设计，可根据需要开启“想象训练”模式。
*   **系统级优化**：针对VLA模型特有的显存压力和推理延迟做了深度定制（如词表裁剪和异步预取）。

### 8. 不足与局限
*   **模型支持范围**：论文提到目前该框架尚未支持超大规模语言模型（LLM）的全量对齐训练（Post-training），主要聚焦于VLA的动作微调。
*   **世界模型开销**：虽然减少了环境交互，但训练和运行基于扩散模型的世界模型本身需要额外的计算资源。
*   **应用限制**：实验主要在模拟环境（MuJoCo）中进行，尚未展示在真实物理机器人上的部署表现及跨域泛化能力。

（完）

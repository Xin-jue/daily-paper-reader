---
title: "StageCraft: Execution Aware Mitigation of Distractor and Obstruction Failures in VLA Models"
title_zh: StageCraft：VLA 模型中干扰物与障碍物导致失败的执行感知缓解方案
authors: "Kartikay Milind Pangaonkar, Prabin Rath, Omkar Patil, Nakul Gopalan"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20659v1"
tags: ["query:q10"]
score: 10.0
evidence: 减轻机器人工作空间任务中视觉-语言-动作 (VLA) 模型的故障
tldr: "针对视觉语言动作模型（VLA）在面对干扰物和物理障碍时容易失败的问题，本文提出了 StageCraft。这是一种无需训练的即插即用方法，利用大语言模型（VLM）的上下文推理能力，通过分析策略执行视频和成功标签，识别并移除初始环境中的干扰因素。实验表明，该方法在多种真实任务中将 VLA 性能提升了 40%，且能根据策略强度自动调整干预程度。"
selection_source: fresh_fetch
motivation: 现有的 VLA 模型在存在干扰物或物理障碍的复杂环境中容易失效，且传统的微调方法在处理未见过的干扰场景时效果有限。
method: 提出一种名为 StageCraft 的免训练方法，利用 VLM 对策略执行视频进行推理，在任务开始前识别并清理可能导致失败的干扰物或障碍。
result: "在真实世界任务中实现了 40% 的绝对性能提升，并在仿真实验中证明了其干预程度能随策略强度自适应调整。"
conclusion: StageCraft 证明了利用互联网规模预训练的 VLM 进行环境预处理是提升机器人策略鲁棒性的一种高效且通用的途径。
---

## 摘要
在文本和图像数据上进行的大规模预训练，结合多样化的机器人演示，已帮助视觉-语言-动作模型（VLA）泛化到新的任务、物体和场景。然而，当机器人工作空间中存在执行时的阻碍（如干扰物和物理障碍）时，这些模型仍然容易发生故障。现有的策略改进方法通过微调基础 VLA 来提高泛化能力，但在未见过的干扰物设置中仍然表现不佳。为了解决这一问题，我们研究了是否可以利用互联网规模预训练的大型视觉语言模型（VLM）来对这些阻碍进行推理并缓解策略失败。为此，我们提出了 StageCraft，这是一种无需训练的方法，通过使用基于 VLM 的上下文推理来操纵环境的初始状态，从而提高预训练 VLA 策略的性能。StageCraft 以策略展开视频和成功标签作为输入，并利用 VLM 的推理能力来推断初始状态中哪些物体需要被处理，以避免预期的执行失败。StageCraft 是一个可扩展的即插即用模块，不会对底层策略引入额外的约束，且仅需少量的策略展开即可工作。我们评估了结合 StageCraft 的最先进 VLA 模型的性能，结果显示在涉及多种干扰物和障碍物的三个真实世界任务领域中，性能绝对提升了 40%。我们在 RLBench 中的仿真实验表明，StageCraft 会根据底层策略的强度调整其干预程度，并随着上下文样本的增加而提高性能。StageCraft 的实际效果视频可在 https://stagecraft-decorator.github.io/stagecraft/ 查看。

## Abstract
Large scale pre-training on text and image data along with diverse robot demonstrations has helped Vision Language Action models (VLAs) to generalize to novel tasks, objects and scenes. However, these models are still susceptible to failure in the presence of execution-time impediments such as distractors and physical obstructions in the robot's workspace. Existing policy improvement methods finetune base VLAs to improve generalization, yet they still struggle in unseen distractor settings. To address this problem, we investigate whether internet-scale pretraining of large vision-language models (VLMs) can be leveraged to reason about these impediments and mitigate policy failures. To this end, we propose StageCraft, a training-free approach to improve pretrained VLA policy performance by manipulating the environment's initial state using VLM-based in-context reasoning. StageCraft takes policy rollout videos and success labels as input and leverages VLM's reasoning ability to infer which objects in the initial state need to be manipulated to avoid anticipated execution failures. StageCraft is an extensible plug-and-play module that does not introduce additional constraints on the underlying policy, and only requires a few policy rollouts to work. We evaluate performance of state-of-the-art VLA models with StageCraft and show an absolute 40% performance improvement across three real world task domains involving diverse distractors and obstructions. Our simulation experiments in RLBench empirically show that StageCraft tailors its extent of intervention based on the strength of the underlying policy and improves its performance with more in-context samples. Videos of StageCraft in effect can be found at https://stagecraft-decorator.github.io/stagecraft/ .

---

## 论文详细总结（自动生成）

### StageCraft：VLA 模型中干扰物与障碍物导致失败的执行感知缓解方案

#### 1. 论文的核心问题与整体含义
*   **研究动机**：尽管视觉-语言-动作模型（VLA）在泛化性上取得了长足进步，但在面对真实世界中未见过的干扰物（Distractors）或物理障碍时，其性能会大幅下降。
*   **核心问题**：现有的策略改进方法（如微调或强化学习）成本高昂，且容易导致“灾难性遗忘”。论文探讨了是否可以模仿人类“在执行任务前先清理环境”的行为，利用大视觉语言模型（VLM）的推理能力，在不改变底层策略参数的情况下，通过预先干预环境状态来提高成功率。

#### 2. 论文提出的方法论
*   **核心思想**：StageCraft 是一种**免训练（Training-free）**、**即插即用**的模块。它通过分析少量的策略执行历史（视频和成功/失败标签），利用 VLM 的上下文推理（In-context Reasoning）能力，识别出导致失败的干扰物，并在正式执行任务前指挥机器人将其移除。
*   **关键技术细节**：
    *   **对象集创建（Object-Set Creation）**：记录不同物体组合下的策略表现，形成一个关于“物体集合-成功率”的经验库。
    *   **对象集转换（Object-Set Transition）**：当面对新场景时，VLM 会对比当前物体与经验库，推断出能保证最高成功率的最小物体子集。
    *   **执行流程**：
        1.  **推理**：VLM（如 Gemini 1.5 Pro）根据历史记录确定需要移除的物体。
        2.  **感知**：使用 SAM3 模型根据 VLM 提供的描述进行物体检测和分割。
        3.  **动作**：通过逆运动学（IK）控制机器人执行简单的“拾取-放置”动作，将干扰物移至回收箱。
        4.  **任务执行**：在清理后的环境中运行原始 VLA 策略。

#### 3. 实验设计
*   **实验场景**：
    *   **真实世界**：使用 Franka FR3 机器人，设置了三个任务：叠杯子（Stack Cups）、摆盘（Setup Plate）、球入碗（Block in Bowl）。
    *   **仿真环境**：在 RLBench 中进行“抓取红杯子”任务，用于深入分析策略强度与干预程度的关系。
*   **Benchmark 与对比方法**：
    *   **基础模型**：Pi0.5 和 SmolVLA（均经过特定任务微调）。
    *   **对比设置**：
        1.  **Base**：无干扰物的理想环境。
        2.  **Distractor**：加入 1-5 个干扰物但不进行干预。
        3.  **StageCraft**：加入干扰物并使用本方法进行干预。
*   **消融实验**：对比了不同 VLM 模型（Gemini 1.5 Pro, Gemini 1.0 Pro, GPT-4o）的推理效果，以及是否有“对象集策略”对结果的影响。

#### 4. 资源与算力
*   **训练算力**：论文未详细列出具体的 GPU 型号和训练时长。由于 StageCraft 本身是免训练的，其核心开销在于 VLM 的 API 调用和基础 VLA 模型（Pi0.5/SmolVLA）的推理。
*   **微调说明**：提到使用了 LeRobot 库对基础模型进行全参数微调，这通常需要消费级或企业级 GPU（如 A100/H100），但文中未给出具体配置。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   真实世界中每个任务评估了 15 次测试，结合了 10 次上下文示例。
    *   仿真实验中进行了 100 次评估，涵盖了不同数量的干扰物（0, 1, 2, 3 个）。
*   **充分性评价**：实验设计较为全面，涵盖了真实机器人、仿真环境、多种 VLA 基础模型以及多种 VLM 推理引擎。通过对比不同强度的策略（$\pi_{weak}$ vs $\pi_{strong}$），客观地证明了该方法的自适应性。

#### 6. 论文的主要结论与发现
*   **性能提升**：StageCraft 在三个真实任务中使 VLA 模型的成功率平均绝对提升了 **40%**。
*   **自适应干预**：实验发现 StageCraft 能根据底层策略的“强弱”自动调整干预力度——策略越弱，移除的物体越多；策略越强，干预越少。
*   **推理重要性**：简单的 VLM 提示词（Prompting）效果不佳，必须结合论文提出的“对象集转换策略”才能实现稳定且准确的环境清理。

#### 7. 优点
*   **无需训练**：不需要为每种新干扰物重新训练模型，极大地降低了部署成本。
*   **通用性强**：作为“装饰器（Decorator）”模式，可以适配任何现有的 VLA 策略。
*   **人类启发式逻辑**：通过“先准备再行动”的逻辑，有效解决了 VLA 模型视觉分布偏移的问题。

#### 8. 不足与局限
*   **VLM 依赖性**：性能高度依赖于底层 VLM 的推理能力和上下文窗口长度（视频 Token 较多时可能受限）。
*   **物体假设**：目前仅适用于可以被离散化为“物体集”的环境，对于不可移动的障碍或非物体类的干扰（如光照变化）无法处理。
*   **执行效率**：预先清理环境会增加总体的任务完成时间，在对时间敏感的应用中可能存在局限。

（完）

---
title: "Fast-WAM: Do World Action Models Need Test-time Future Imagination?"
title_zh: Fast-WAM：世界动作模型在测试时需要未来想象吗？
authors: "Tianyuan Yuan, Zibin Dong, Yicheng Liu, Hang Zhao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16666v2"
tags: ["query:q12"]
score: 9.0
evidence: 世界动作模型 (WAM) 作为视觉-语言-动作 (VLA) 模型的替代方案
tldr: 本研究针对世界动作模型（WAM）推理延迟高的问题，探讨了测试时显式未来想象的必要性。作者提出了 Fast-WAM，该模型在训练时保留视频协同训练以学习强大的世界表示，但在推理时跳过耗时的视频生成步骤。实验表明，Fast-WAM 在多个仿真和真实任务中达到了 SOTA 水平，且推理速度提升了 4 倍以上，证明了视频建模的价值主要在于训练阶段的表示增强。
selection_source: fresh_fetch
motivation: 现有 WAM 模型依赖推理时的视频去噪生成，导致严重的测试延迟，限制了其在实时具身控制中的应用。
method: 提出 Fast-WAM 架构，通过在训练中解耦视频建模与动作生成，实现推理时无需显式想象的快速动作输出。
result: Fast-WAM 在 LIBERO 和 RoboTwin 等基准测试中表现与传统 WAM 相当，且推理延迟降至 190ms。
conclusion: 视频预测对 WAM 的贡献主要源于训练时的表示学习，而非测试时的显式未来预测。
---

## 摘要
世界动作模型 (WAMs) 已成为具身控制中视觉-语言-动作 (VLA) 模型的一种极具前景的替代方案，因为它们显式地建模了视觉观测在动作作用下如何演化。大多数现有的 WAM 遵循“先想象后执行”的范式，由于迭代视频去噪产生了显著的测试时延迟，然而目前尚不清楚显式的未来想象对于强大的动作性能是否真的必要。在本文中，我们探讨了 WAM 是否在测试时需要显式的未来想象，或者其优势是否主要源于训练期间的视频建模。通过提出 Fast-WAM，我们将训练期间的视频建模作用与推理期间的显式未来生成解耦。Fast-WAM 是一种在训练时保留视频协同训练但在测试时跳过未来预测的 WAM 架构。我们进一步实例化了多个 Fast-WAM 变体，以便对这两个因素进行受控比较。在这些变体中，我们发现 Fast-WAM 与“先想象后执行”变体相比仍具竞争力，而移除视频协同训练则会导致更大幅度的性能下降。实验表明，在无需具身预训练的情况下，Fast-WAM 在仿真基准测试（LIBERO 和 RoboTwin）以及现实世界任务中均取得了与最先进方法相当的结果。其运行延迟为 190 毫秒，实现了实时运行，比现有的“先想象后执行”型 WAM 快 4 倍以上。这些结果表明，WAM 中视频预测的主要价值可能在于提升训练期间的世界表示，而非在测试时生成未来观测。项目主页：https://yuantianyuan01.github.io/FastWAM/

## Abstract
World Action Models (WAMs) have emerged as a promising alternative to Vision-Language-Action (VLA) models for embodied control because they explicitly model how visual observations may evolve under action. Most existing WAMs follow an imagine-then-execute paradigm, incurring substantial test-time latency from iterative video denoising, yet it remains unclear whether explicit future imagination is actually necessary for strong action performance. In this paper, we ask whether WAMs need explicit future imagination at test time, or whether their benefit comes primarily from video modeling during training. We disentangle the role of video modeling during training from explicit future generation during inference by proposing \textbf{Fast-WAM}, a WAM architecture that retains video co-training during training but skips future prediction at test time. We further instantiate several Fast-WAM variants to enable a controlled comparison of these two factors. Across these variants, we find that Fast-WAM remains competitive with imagine-then-execute variants, while removing video co-training causes a much larger performance drop. Empirically, Fast-WAM achieves competitive results with state-of-the-art methods both on simulation benchmarks (LIBERO and RoboTwin) and real-world tasks, without embodied pretraining. It runs in real time with 190ms latency, over 4$\times$ faster than existing imagine-then-execute WAMs. These results suggest that the main value of video prediction in WAMs may lie in improving world representations during training rather than generating future observations at test time. Project page: https://yuantianyuan01.github.io/FastWAM/

---

## 论文详细总结（自动生成）

以下是对论文《Fast-WAM: Do World Action Models Need Test-time Future Imagination?》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：世界动作模型（WAMs）在测试（推理）阶段是否真的需要显式地生成“未来想象”视频？
*   **研究动机**：
    *   现有的 WAM 通常遵循“先想象后执行”范式，通过迭代去噪生成未来视频来辅助动作预测，但这导致了极高的推理延迟（通常需数秒）。
    *   作者怀疑 WAM 的性能提升主要源于训练时的视频建模任务（提供了更强的物理先验和表示），而非推理时的显式视频合成。
    *   旨在通过解耦“训练时的视频建模”与“推理时的未来生成”，开发一种既快又强的具身智能模型。

### 2. 方法论
*   **核心思想**：提出 **Fast-WAM** 架构。在训练阶段，利用视频预测任务作为辅助目标来塑造强大的世界表示；在推理阶段，跳过耗时的视频去噪过程，直接从视频主干网络的潜在表示中提取特征进行动作预测。
*   **关键技术细节**：
    *   **架构设计**：采用混合 Transformer（MoT）架构，包含一个预训练的视频 DiT（基于 Wan2.2-5B）和一个动作专家 DiT（1B）。
    *   **结构化注意力掩码**：设计了特殊的掩码，确保在训练时动作分支可以访问当前帧特征，但不能“偷看”未来的视频噪声标记，从而实现训练与推理的一致性。
    *   **训练目标**：采用联合流匹配（Flow Matching）目标函数：$L = L_{act} + \lambda L_{vid}$。其中 $L_{vid}$ 负责学习视频动态，$L_{act}$ 负责预测动作序列。
    *   **推理流程**：推理时完全移除未来视频分支。原始图像通过 VAE 编码后，经视频 DiT 进行单次前向传播提取特征，随后动作专家 DiT 直接输出动作块（Action Chunk）。

### 3. 实验设计
*   **实验场景与基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务，评估模型的泛化与长程操作能力。
    *   **RoboTwin 2.0**：具有挑战性的双臂协作基准，包含 50 多个任务，涉及重度场景随机化。
    *   **现实世界任务**：折叠毛巾（Towel Folding），这是一个涉及可变形物体、长路径规划的复杂任务。
*   **对比方法**：
    *   **SOTA 基准**：OpenVLA、$\pi_0$、$\pi_{0.5}$、Motus、LingBot-VA。
    *   **受控变体（消融）**：
        1.  **Fast-WAM-Joint**：模拟联合去噪范式。
        2.  **Fast-WAM-IDM**：模拟“先生成视频再预测动作”范式。
        3.  **Fast-WAM w.o. video co-train**：移除训练时的视频建模目标。

### 4. 资源与算力
*   **硬件环境**：推理延迟在单张 **NVIDIA RTX 5090D V2 (32GB)** GPU 上测量。
*   **模型规模**：总参数量约 **6B**（5B 视频主干 + 1B 动作专家）。
*   **训练细节**：在 LIBERO 上训练 20k 步，在 RoboTwin 和现实任务上训练 30k 步。使用 AdamW 优化器，学习率为 $1 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 90 多个任务，LIBERO 进行了 2000 次试验，RoboTwin 每个任务进行 100 次试验。
    *   现实世界评估不仅测量成功率，还测量了平均完成时间。
*   **充分性评价**：实验设计非常充分且具有高度的科学严谨性。通过构建三个受控变体，成功孤立了“训练目标”和“推理想象”两个变量，有力地支持了论文的核心论点。

### 6. 主要结论与发现
*   **视频建模的价值在于训练**：实验表明，移除视频协同训练会导致性能大幅下降（如 RoboTwin 从 91.8% 降至 83.8%），而移除推理时的显式想象对性能影响极小。
*   **高效性**：Fast-WAM 的推理延迟仅为 **190ms**，比传统的“先想象后执行”模型快 **4 倍以上**，能够满足实时控制需求。
*   **数据效率**：在没有具身预训练的情况下，Fast-WAM 依然能达到或超过许多经过大规模预训练的 VLA 和 WAM 模型。

### 7. 优点
*   **深刻的洞察力**：挑战了 WAM 领域“必须显式生成视频”的直觉，为构建高效具身模型提供了新路径。
*   **架构优雅**：通过 MoT 和注意力掩码巧妙地实现了训练时的多任务学习与推理时的高效解耦。
*   **实时性强**：解决了 WAM 落地现实机器人最核心的延迟瓶颈。

### 8. 不足与局限
*   **长程预测限制**：本研究主要关注单次动作块（Action Chunk）的生成，未深入探讨在需要极长程自回归预测的任务中，显式想象是否会展现出更大的优势。
*   **预训练依赖**：虽然不需要具身预训练，但模型高度依赖于强大的开源视频生成模型（如 Wan2.2）作为初始化，这可能限制了其在完全不同分布数据上的表现。
*   **算力门槛**：尽管推理快，但 6B 参数量的模型在边缘设备上的部署仍有一定挑战。

（完）

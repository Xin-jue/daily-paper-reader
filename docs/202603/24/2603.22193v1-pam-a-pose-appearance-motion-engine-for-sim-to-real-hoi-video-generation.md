---
title: "PAM: A Pose-Appearance-Motion Engine for Sim-to-Real HOI Video Generation"
title_zh: PAM：一种用于 Sim-to-Real 手物交互视频生成的姿态-外观-运动引擎
authors: "Mingju Gao, Kaisen Yang, Huan-ang Gao, Bohan Li, Ao Ding, Wenyi Li, Yangcheng Yu, Jinkun Liu, Shaocong Xu, Yike Niu, Haohan Chi, Hao Chen, Hao Tang, Li Yi, Hao Zhao"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22193v1"
tags: ["query:q9"]
score: 9.0
evidence: 手物交互的虚实迁移视频生成
tldr: 本研究针对手物交互（HOI）视频生成中姿态、外观与运动研究割裂的问题，提出了PAM引擎。PAM是一个统一的框架，能够通过整合深度、分割和关键点等多种条件，实现可控的高质量HOI视频生成。实验证明，PAM在DexYCB和OAKINK2数据集上均取得了领先的生成质量，且生成的合成数据能显著提升下游手部姿态估计任务的性能，实现了从模拟到现实的有效迁移。
selection_source: fresh_fetch
motivation: 现有的HOI生成研究在姿态合成、单图生成和视频生成之间存在断层，缺乏一个能统一处理姿态、外观和运动的框架。
method: 提出了PAM引擎，通过结合深度图、分割掩码和关键点等多种输入条件，实现可控且连贯的HOI视频生成。
result: 在DexYCB数据集上，PAM在视频质量（FVD 29.13）和姿态准确度上均优于现有基准，并能通过合成数据增强提升下游任务表现。
conclusion: PAM成功统一了HOI视频生成的多个维度，为具身智能和AR/VR领域提供了一个强大的模拟到现实的生成工具。
---

## 摘要
手物交互（HOI）的重建与合成正成为具身智能（Embodied AI）和增强现实/虚拟现实（AR/VR）的核心。然而，尽管取得了快速进展，现有的 HOI 生成研究仍分散在三个互不相干的路径中：(1) 仅姿态合成，即预测 MANO 轨迹而不生成像素；(2) 单图 HOI 生成，即从掩码或 2D 线索中生成外观，但缺乏动力学特性；(3) 视频生成方法，这类方法需要完整的姿态序列和真实的第一帧作为输入，阻碍了真正的 Sim-to-Real 部署。受 Joo 等人（2018）理念的启发，我们认为 HOI 生成需要一个统一的引擎，将姿态、外观和运动整合在一个连贯的框架内。因此，我们提出了 PAM：一种用于可控 HOI 视频生成的姿态-外观-运动引擎。我们引擎的性能通过以下实验得到了验证：(1) 在 DexYCB 数据集上，我们获得了 29.13 的 FVD（相比之下 InterDyn 为 38.83）和 19.37 mm 的 MPJPE（相比之下 CosHand 为 30.05 mm），同时生成的视频分辨率为 480x720，高于基准模型的 256x256 和 256x384；(2) 在 OAKINK2 数据集上，我们的全多条件模型将 FVD 从 68.76 优化至 46.31；(3) 对 DexYCB 输入条件的消融实验表明，结合深度、分割和关键点始终能产生最佳结果；(4) 在使用 SimpleHand 的下游手部姿态估计任务中，通过 3,400 个合成视频（20.7 万帧）进行数据增强，使得仅使用 50% 真实数据加合成数据训练的模型，即可达到 100% 真实数据基准的水平。

## Abstract
Hand-object interaction (HOI) reconstruction and synthesis are becoming central to embodied AI and AR/VR. Yet, despite rapid progress, existing HOI generation research remains fragmented across three disjoint tracks: (1) pose-only synthesis that predicts MANO trajectories without producing pixels; (2) single-image HOI generation that hallucinates appearance from masks or 2D cues but lacks dynamics; and (3) video generation methods that require both the entire pose sequence and the ground-truth first frame as inputs, preventing true sim-to-real deployment. Inspired by the philosophy of Joo et al. (2018), we think that HOI generation requires a unified engine that brings together pose, appearance, and motion within one coherent framework. Thus we introduce PAM: a Pose-Appearance-Motion Engine for controllable HOI video generation. The performance of our engine is validated by: (1) On DexYCB, we obtain an FVD of 29.13 (vs. 38.83 for InterDyn), and MPJPE of 19.37 mm (vs. 30.05 mm for CosHand), while generating higher-resolution 480x720 videos compared to 256x256 and 256x384 baselines. (2) On OAKINK2, our full multi-condition model improves FVD from 68.76 to 46.31. (3) An ablation over input conditions on DexYCB shows that combining depth, segmentation, and keypoints consistently yields the best results. (4) For a downstream hand pose estimation task using SimpleHand, augmenting training with 3,400 synthetic videos (207k frames) allows a model trained on only 50% of the real data plus our synthetic data to match the 100% real baseline.

---

## 论文详细总结（自动生成）

以下是对论文《PAM: A Pose–Appearance–Motion Engine for Sim-to-Real HOI Video Generation》的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：现有的手物交互（HOI）生成研究存在“碎片化”问题，主要分为三类：仅姿态合成（无像素）、单图生成（无动力学）和视频生成（依赖真实第一帧）。
*   **研究动机**：在具身智能（Embodied AI）中，从模拟器（Sim）迁移到现实（Real）的关键在于生成高质量、符合物理规律且外观真实的视频。现有方法因依赖真实世界的第一帧或缺乏连贯的运动控制，无法直接用于纯模拟数据的视频化。
*   **整体含义**：提出一个统一的引擎 PAM，仅需初始/目标姿态和物体几何，即可生成高质量的 HOI 视频，填补了模拟姿态与真实视频之间的鸿沟。

### 2. 方法论：PAM 引擎架构
PAM 将生成过程解耦为三个核心阶段：
*   **阶段 I：姿态生成（Pose Generation）**：利用预训练模型（如 GraspXL）根据初始和目标姿态，在模拟器中插值生成符合物理逻辑的 MANO 手部轨迹和物体运动序列。
*   **阶段 II：外观生成（Appearance Generation）**：使用可控图像扩散模型（基于 Flux）生成视频的第一帧。
    *   **关键技术**：引入“桥接条件”，结合深度图（几何）、语义掩码（语义）和手部关键点（精细姿态），通过 ControlNet 注入模型，确保生成图像的几何准确性和外观多样性。
*   **阶段 III：运动生成（Motion Generation）**：使用可控视频扩散模型（基于 CogVideoX）将第一帧动画化。
    *   **技术细节**：视频模型同样受深度、掩码和关键点序列的约束，确保生成的视频在时间上连贯且严格遵循阶段 I 定义的交互轨迹。

### 3. 实验设计
*   **数据集**：
    *   **DexYCB**：标准手抓取物体基准。
    *   **OAKINK2**：复杂的双向手物交互数据集。
*   **对比基准（Benchmarks）**：
    *   **CosHand**、**InterDyn**、**ManiVideo** 等 SOTA 方法。
*   **评估指标**：
    *   视频质量：FVD（Fréchet Video Distance）。
    *   姿态准确度：MPJPE（平均关节位置误差）。
    *   运动忠实度：MF（Motion Fidelity，通过点追踪评估）。
    *   图像质量：SSIM、LPIPS、PSNR。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H800 GPU**。
*   **训练细节**：采用 AdamW 优化器和 DeepSpeed 训练架构，训练步数为 8,000 步。
*   **推理耗时**：在单张 H20 GPU 上，生成 40 帧视频的总耗时约为 **301.1 秒**（姿态 19.3s，外观 36.1s，运动 245.7s）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型数据集上进行了详尽的定量对比。
    *   **消融实验**：针对输入条件（深度/掩码/关键点）、掩码概率、手部表示方式（关键点 vs 投影）、骨干网络（SVD vs CogVideoX）等进行了多组对比。
    *   **下游任务验证**：生成了 3,400 个视频（20.7 万帧）用于数据增强，验证其对 SimpleHand 模型性能的提升。
*   **评价**：实验设计非常充分且客观。通过下游任务的性能提升（Data Efficiency）有力证明了合成数据的实用价值，而非仅仅停留在视觉指标上。

### 6. 主要结论与发现
*   **性能领先**：在 DexYCB 上，PAM 的 FVD 达到 29.13（优于 InterDyn 的 38.83），MPJPE 达到 19.37mm（优于 CosHand 的 30.05mm）。
*   **分辨率突破**：能够生成 **480x720** 的高分辨率视频，远超基准方法的 256x256。
*   **数据增强价值**：实验发现，使用 **50% 真实数据 + PAM 合成数据** 训练的模型，性能即可追平 **100% 真实数据** 训练的基准模型。
*   **多条件必要性**：消融实验证明，深度、掩码和关键点三者结合能显著降低姿态误差并提升视觉质量。

### 7. 优点与亮点
*   **统一框架**：首次将姿态、外观和运动整合，摆脱了对真实世界第一帧的依赖，实现了真正的 Sim-to-Real。
*   **高保真度**：通过多模态条件控制，解决了扩散模型在生成精细手部结构（如手指数量、关节位置）时的常见畸变问题。
*   **实用性强**：生成的视频不仅好看，而且具有极高的几何准确性，能直接用于训练下游视觉算法。

### 8. 不足与局限
*   **误差传递**：阶段 III 的质量高度依赖阶段 II 的初始帧，若第一帧纹理较差，会导致视频闪烁；同时阶段 I 的物理错误（如穿模）会遗传给最终视频。
*   **计算开销**：由于采用了大型扩散模型（Flux/CogVideoX），推理时间较长（约 5 分钟生成一个短视频），难以满足实时生成需求。
*   **交互复杂度**：虽然在双向交互上有所尝试，但对于极其复杂的长程操作或多物体交互的泛化能力仍有待进一步验证。

（完）

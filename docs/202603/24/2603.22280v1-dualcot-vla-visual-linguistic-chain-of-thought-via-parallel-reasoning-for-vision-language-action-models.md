---
title: "DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models"
title_zh: DualCoT-VLA：通过并行推理实现视觉-语言-动作模型的视觉-语言思维链
authors: "Zhide Zhong, Junfeng Li, Junjie He, Haodong Yan, Xin Gong, Guanyi Zhao, Yingjie Cai, Jiantao Gao, Xu Yan, Bingbing Liu, Yingcong Chen, Liuqing Yang, Haoang Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22280v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于复杂机器人操作任务的VLA模型并行推理思维链
tldr: 针对视觉-语言-动作（VLA）模型在复杂任务中逻辑规划不足、空间感知粗糙以及自回归推理延迟高的问题，本文提出DualCoT-VLA。该方法通过集成视觉思维链（底层空间理解）与语言思维链（高层任务规划）实现全面的多模态推理。同时，引入并行推理机制，利用可学习查询令牌将传统的逐步自回归解码转变为单步前向推理，显著降低了推理延迟。实验表明，该模型在LIBERO、RoboCasa等基准测试及实物机器人上均取得领先性能。
selection_source: fresh_fetch
motivation: 现有VLA模型难以同时兼顾高层逻辑规划与底层空间细节，且自回归推理机制存在高延迟和误差累积问题。
method: 提出DualCoT-VLA，结合视觉与语言双重思维链，并利用可学习查询令牌实现单步并行推理。
result: 在LIBERO、RoboCasa GR1基准测试及真实世界机器人实验中均实现了最先进的性能表现。
conclusion: 双模态并行思维链推理有效提升了VLA模型的感知精度、规划能力及推理效率。
---

## 摘要
视觉-语言-动作（VLA）模型将视觉观测和语言指令直接映射为机器人动作。虽然标准 VLA 模型在简单任务中表现出色，但在处理需要逻辑规划的复杂多步任务，以及需要细粒度空间感知的精确操作时往往面临困难。最近的研究引入了思维链（CoT）推理，赋予 VLA 模型“先思后行”的能力。然而，目前的基于 CoT 的 VLA 模型面临两个关键局限性：1) 由于依赖孤立的单模态 CoT，无法同时捕捉底层视觉细节和高层逻辑规划；2) 逐步自回归解码导致的高推理延迟和复合误差。为了解决这些局限性，我们提出了 DualCoT-VLA，这是一种具有并行推理机制的 VLA 模型视觉-语言 CoT 方法。为了实现全面的多模态推理，我们的方法集成了用于底层空间理解的视觉 CoT 和用于高层任务规划的语言 CoT。此外，为了克服延迟瓶颈，我们引入了一种并行 CoT 机制，该机制结合了两组可学习的查询标记（query tokens），将自回归推理转变为单步前向推理。大量实验表明，我们的 DualCoT-VLA 在 LIBERO 和 RoboCasa GR1 基准测试以及真实世界平台中均达到了最先进的性能。

## Abstract
Vision-Language-Action (VLA) models map visual observations and language instructions directly to robotic actions. While effective for simple tasks, standard VLA models often struggle with complex, multi-step tasks requiring logical planning, as well as precise manipulations demanding fine-grained spatial perception. Recent efforts have incorporated Chain-of-Thought (CoT) reasoning to endow VLA models with a ``thinking before acting'' capability. However, current CoT-based VLA models face two critical limitations: 1) an inability to simultaneously capture low-level visual details and high-level logical planning due to their reliance on isolated, single-modal CoT; 2) high inference latency with compounding errors caused by step-by-step autoregressive decoding. To address these limitations, we propose DualCoT-VLA, a visual-linguistic CoT method for VLA models with a parallel reasoning mechanism. To achieve comprehensive multi-modal reasoning, our method integrates a visual CoT for low-level spatial understanding and a linguistic CoT for high-level task planning. Furthermore, to overcome the latency bottleneck, we introduce a parallel CoT mechanism that incorporates two sets of learnable query tokens, shifting autoregressive reasoning to single-step forward reasoning. Extensive experiments demonstrate that our DualCoT-VLA achieves state-of-the-art performance on the LIBERO and RoboCasa GR1 benchmarks, as well as in real-world platforms.

---

## 论文详细总结（自动生成）

这是一份关于论文《DualCoT-VLA: Visual-Linguistic Chain of Thought via Parallel Reasoning for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在处理复杂多步任务时，往往缺乏高层逻辑规划能力；而在处理精确操作时，又缺乏细粒度的空间感知。
*   **研究背景**：虽然近期有研究引入思维链（CoT）来增强 VLA 的“思考”能力，但存在两大局限：
    1.  **模态孤立**：现有的 CoT 通常仅限于单一模态（纯文本或纯视觉），难以同时兼顾逻辑规划和空间细节。
    2.  **推理延迟高**：大多数 CoT 依赖自回归（AR）逐个生成 Token，导致严重的推理延迟和误差累积，难以满足机器人实时控制的需求。

### 2. 提出的方法论：DualCoT-VLA
*   **核心思想**：提出一种**并行隐式双模态思维链**机制。通过在连续潜空间中同时进行视觉和语言推理，并将自回归过程转化为单步前向推理。
*   **关键技术细节**：
    *   **并行推理机制**：引入两组可学习的查询令牌（Query Tokens）——视觉 CoT 令牌（$M=16$）和语言 CoT 令牌（$N=4$）。这些令牌与图像、指令一起输入 VLM 主干网络，在一次前向传播中生成推理特征。
    *   **视觉 CoT（几何蒸馏）**：利用冻结的视觉基础模型（Depth Anything 3）作为教师，通过交叉注意力投影器，将 VLM 的视觉查询状态与密集的 3D 空间特征对齐，赋予模型底层空间感知。
    *   **语言 CoT（步骤级监督）**：利用冻结的轻量级 LLM（如 Qwen3-0.6B）作为解码器，监督语言查询令牌生成人类可读的任务规划文本，确保模型内部化了高层逻辑。
    *   **动作生成**：将推理增强后的隐藏状态输入到基于流匹配（Flow-Matching）的扩散 Transformer（DiT）动作头中，预测连续动作。
    *   **推理优化**：推理时丢弃辅助的教师模块，仅靠 VLM 主干和动作头实现高速预测。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个任务套件，评估长程操作能力。
    *   **RoboCasa GR1**：涉及 29 自由度灵巧手的复杂桌面任务，评估细粒度感知。
    *   **真实世界**：基于 AgileX Cobot（双臂机器人）进行面包放置、方块排序、水果收集等任务。
*   **对比方法**：
    *   **非 CoT 模型**：OpenVLA, $\pi_0$, PD-VLA, GR00T 系列。
    *   **自回归 CoT 模型**：CoT-VLA, ThinkAct, DeepThinkVLA, LaRA-VLA 等。

### 4. 资源与算力
*   **算力设备**：论文提到所有训练均在 **NVIDIA H100 GPU** 上进行。
*   **具体细节**：文中未明确给出具体的 GPU 数量、总训练时长或能耗数据，但提供了超参数设置（如 LIBERO 学习率为 2.5e-5，Batch Size 为 48）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 上进行了 500 个回合的评估；在 RoboCasa 上进行了 24 个任务、每个任务 50 次尝试。
    *   真实世界实验包含 3 种难度等级，每个任务 25 次试验。
*   **充分性与公平性**：实验涵盖了从仿真到现实、从简单到复杂的多种场景。通过消融实验分别验证了视觉 CoT 和语言 CoT 的贡献，并进行了详细的推理延迟对比分析。实验设计较为客观，对比了当前最前沿的多种基准模型。

### 6. 主要结论与发现
*   **性能领先**：DualCoT-VLA 在 LIBERO（平均成功率 98.8%）和 RoboCasa（55.1%）上均刷新了 SOTA 记录。
*   **效率极高**：相比自回归 CoT 模型（延迟约 3178ms），DualCoT-VLA 的总推理延迟仅为 **83.2ms**，仅比非 CoT 模型略高，完全满足实时控制需求。
*   **模态互补**：视觉 CoT 显著提升了空间任务（Spatial）的成功率，而语言 CoT 则对长程任务（Long）至关重要。

### 7. 优点
*   **并行化创新**：成功解决了 CoT 推理速度慢的顽疾，将 $O(N)$ 的时间复杂度降至 $O(1)$。
*   **双模态融合**：通过隐式表征同时捕捉了“在哪里操作（空间）”和“下一步做什么（逻辑）”。
*   **可解释性**：虽然是隐式推理，但通过轻量级探针和解码器，可以将潜空间状态还原为深度图和文本，证明了模型确实在“思考”。

### 8. 不足与局限
*   **依赖教师模型**：训练过程高度依赖高质量的视觉基础模型（DA3）和语言模型（Qwen）提供监督信号。
*   **数据标注成本**：语言 CoT 的训练需要详细的任务步骤标注，虽然文中使用了自动生成技术，但在极端复杂任务下可能存在标注质量瓶颈。
*   **硬件门槛**：尽管推理快，但 VLM 主干网络（4B 参数）和 DiT 结构对嵌入式部署的显存仍有一定要求。

（完）

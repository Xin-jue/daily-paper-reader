---
title: "VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models"
title_zh: VP-VLA：视觉提示作为视觉-语言-动作模型的接口
authors: "Zixuan Wang, Yuxin Chen, Yuqi Liu, Jinhui Ye, Pengguang Chen, Changsheng Lu, Shu Liu, Jiaya Jia"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22003v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于机器人控制的视觉-语言-动作（VLA）模型双系统框架
tldr: 针对传统VLA模型在空间精度和鲁棒性上的不足，本文提出VP-VLA框架。该框架采用双系统架构，通过视觉提示接口解耦高层规划与底层执行。System 2规划器将指令分解并生成视觉锚点（如十字准星和边界框），System 1控制器则依据这些提示生成精确动作。实验证明，该方法在多个基准测试中显著提升了任务成功率，为提升机器人操作的精准度提供了新思路。
selection_source: fresh_fetch
motivation: 传统的端到端VLA模型因黑盒映射机制，在处理复杂指令时往往缺乏空间精度和对分布外场景的鲁棒性。
method: 提出一种双系统框架，利用视觉提示（如边界框和准星）作为接口，将高层任务规划与底层动作控制进行解耦。
result: "在Robocasa和SimplerEnv基准测试中，VP-VLA的成功率分别提升了5%和8.3%，表现优于QwenOFT和GR00T-N1.6等基准模型。"
conclusion: 通过引入结构化视觉提示接口，可以有效增强VLA模型的空间感知能力和复杂任务的执行可靠性。
---

## 摘要
视觉-语言-动作 (VLA) 模型通常将视觉观测和语言指令直接映射为机器人控制信号。这种“黑盒”映射迫使单次前向传播同时处理指令解释、空间定位和底层控制，往往导致空间精度较差，且在分布外 (out-of-distribution) 场景中的鲁棒性有限。为了解决这些局限性，我们提出了 VP-VLA，这是一个通过结构化视觉提示接口将高层推理与底层执行解耦的双系统框架。具体而言，“系统 2 规划器” (System 2 Planner) 将复杂指令分解为子任务，并识别相关的目标物体和目标位置。这些空间锚点随后作为结构化视觉提示（如十字准星和边界框）直接叠加在视觉观测上。在这些提示的引导下，并通过训练过程中一种新颖的辅助视觉定位目标进行增强，“系统 1 控制器” (System 1 Controller) 能够可靠地生成精确的底层执行动作。在 Robocasa-GR1-Tabletop 基准测试和 SimplerEnv 仿真中的实验表明，VP-VLA 将成功率分别提高了 5% 和 8.3%，超越了包括 QwenOFT 和 GR00T-N1.6 在内的竞争基线模型。

## Abstract
Vision-Language-Action (VLA) models typically map visual observations and linguistic instructions directly to robotic control signals. This "black-box" mapping forces a single forward pass to simultaneously handle instruction interpretation, spatial grounding, and low-level control, often leading to poor spatial precision and limited robustness in out-of-distribution scenarios. To address these limitations, we propose VP-VLA, a dual-system framework that decouples high-level reasoning and low-level execution via a structured visual prompting interface. Specifically, a "System 2 Planner" decomposes complex instructions into sub-tasks and identifies relevant target objects and goal locations. These spatial anchors are then overlaid directly onto visual observations as structured visual prompts, such as crosshairs and bounding boxes. Guided by these prompts and enhanced by a novel auxiliary visual grounding objective during training, a "System 1 Controller" reliably generates precise low-level execution motions. Experiments on the Robocasa-GR1-Tabletop benchmark and SimplerEnv simulation demonstrate that VP-VLA improves success rates by 5% and 8.3%, surpassing competitive baselines including QwenOFT and GR00T-N1.6.

---

## 论文详细总结（自动生成）

以下是对论文《VP-VLA: Visual Prompting as an Interface for Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的视觉-语言-动作（VLA）模型通常采用“黑盒”式的端到端映射，将视觉观测和语言指令直接转换为控制信号。这种方式迫使模型在单次前向传播中同时处理语义理解、空间定位和底层控制，导致其在空间精度上表现欠缺，且在面对分布外（OOD）场景（如新物体、新位置）时鲁棒性极差。
*   **核心问题**：如何解耦高层推理与底层执行，以提升机器人操作的精准度和泛化能力？

### 2. 方法论
VP-VLA 提出了一个受人类认知启发（System 1 & System 2）的**双系统框架**：
*   **System 2 规划器（高层推理）**：
    *   **任务分解**：利用预训练 VLM（如 Qwen3-VL）将复杂指令分解为原子子任务（如“抓取瓶子”、“放入箱子”）。
    *   **事件驱动机制**：规划器并非每帧运行，而是由物理状态变化（如夹爪开合）触发，降低计算开销。
    *   **视觉提示生成**：识别目标物体和位置，利用分割模型（SAM3）在原始图像上叠加**结构化视觉提示**（抓取目标用“十字准星”，放置区域用“边界框”）。
*   **System 1 控制器（底层执行）**：
    *   **视觉提示接口**：控制器同时接收原始图像和带有视觉提示的图像，将任务从“理解意图”简化为“视觉追踪”。
    *   **辅助定位目标（Auxiliary Grounding Objective）**：在训练中引入辅助任务，要求模型预测提示点的离散化坐标（JSON 格式），通过交叉熵损失强化模型对视觉提示的感知。
    *   **动作生成**：基于 VLM 主干和动作解码器生成连续控制信号。

### 3. 实验设计
*   **仿真基准测试**：
    *   **Robocasa-GR1-Tabletop**：包含 24 个复杂的厨房操作任务，涉及多步交互。
    *   **SimplerEnv**：基于 BridgeDataV2 和 Fractal 数据集，评估在不同环境下的泛化性。
*   **对比方法**：包括 QwenOFT、GR00T-N1.5/N1.6、$\pi_0$、$\pi_{0.5}$、OpenVLA、Octo 等最先进的 VLA 模型。
*   **真实世界场景**：
    *   **垃圾分类**：评估在杂乱环境下的语义识别与放置精度。
    *   **属性引用抓取**：根据颜色指令抓取特定物体，测试属性绑定能力。
    *   **空间坐标放置**：在蛋托网格中按指令位置放置，测试精细空间推理。

### 4. 资源与算力
*   **算力配置**：实验使用了 **8 张 GPU** 进行训练（具体型号未标明，但通常为 A100 或 H100 级别）。
*   **训练细节**：在 SimplerEnv 任务中微调了 70k 步，Batch Size 为 32（每张卡）或总计 256。
*   **模型规模**：高层规划器和底层控制器均基于 Qwen3-VL-4B-Instruct。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验中每个任务进行 50 次独立试验以确保统计显著性。
*   **消融实验**：针对辅助定位损失、视觉提示形状（点 vs 十字）、提示叠加方式、任务分解必要性等进行了详细对比。
*   **充分性评价**：实验设计非常充分，涵盖了从仿真到现实、从分布内到分布外的多维度评估。通过对比多种 SOTA 模型，客观地证明了双系统解耦的优越性。

### 6. 主要结论与发现
*   **性能提升**：在 Robocasa 上平均成功率提升 **5%**，在 SimplerEnv 上提升 **8.3%**，刷新了多个基准的 SOTA 记录。
*   **泛化能力**：VP-VLA 在面对从未见过的物体（OOD Object）和位置（OOD Position）时表现出极强的鲁棒性，性能下降远小于基准模型。
*   **空间精度**：视觉提示显著增强了模型对目标物体的定位精度，尤其是在需要精细操作的任务中。

### 7. 优点
*   **架构优雅**：通过视觉提示这一“显式接口”实现了高底层解耦，符合认知科学逻辑。
*   **即插即用**：视觉提示可以作为一种通用的增强手段，应用于不同的 VLA 主干网络。
*   **训练高效**：引入辅助定位损失（Grounding Loss）有效解决了模型忽略视觉提示的问题，使训练更具方向性。

### 8. 不足与局限
*   **依赖性风险**：系统的性能高度依赖于 System 2 规划器和分割模型（SAM3）的准确性。如果高层规划出错或分割偏移，底层执行将无法纠正。
*   **推理延迟**：虽然采用了事件驱动机制，但调用大型 VLM 进行规划和 SAM 进行分割仍会引入额外的推理延迟，可能影响极高频的实时控制。
*   **数据标注成本**：虽然文中提到使用了规则和自动标注，但获取高质量的子任务分解和对应的视觉掩码在复杂现实场景中仍具挑战。

（完）

---
title: "Altered Thoughts, Altered Actions: Probing Chain-of-Thought Vulnerabilities in VLA Robotic Manipulation"
title_zh: 改变的思维，改变的行动：探究 VLA 机器人操控中的思维链漏洞
authors: "Tuan Duong Trinh, Naveed Akhtar, Basim Azam"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12717v1"
tags: ["query:q10"]
score: 9.0
evidence: 探究VLA机器人操作中的思维链推理
tldr: 本研究探讨了视觉-语言-动作（VLA）模型中思维链（CoT）推理的脆弱性。通过对40个LIBERO任务进行七种不同类型的文本干扰实验，研究者发现动作解码器高度依赖推理轨迹中的实体引用（如物体名称），而对逻辑顺序、空间方向甚至推理的合理性并不敏感。这一发现揭示了推理增强型机器人模型中一个独特且隐蔽的攻击向量，即仅通过篡改内部推理过程即可显著降低任务成功率，为提升VLA模型的鲁棒性提供了重要见解。
selection_source: fresh_fetch
motivation: 旨在研究VLA模型中连接推理模块与动作解码器的内部思维链文本通道是否真实影响动作执行及其存在的安全风险。
method: 提出了一种包含盲噪声、机械语义和LLM自适应攻击的分类法，通过在不改变输入的前提下篡改中间推理轨迹来评估模型性能。
result: 实验发现替换物体名称使成功率大幅下降，而句子重排、空间反转或由大模型生成的错误计划对性能几乎没有影响。
conclusion: 动作解码器主要依赖实体引用的完整性而非推理的逻辑结构，确立了内部推理轨迹是推理增强型模型特有的隐蔽威胁向量。
---

## 摘要
最近的视觉-语言-动作（VLA）模型越来越多地采用思维链（CoT）推理，在解码运动指令之前生成自然语言计划。推理模块与动作解码器之间的这一内部文本通道尚未受到对抗性审查。我们提出疑问：动作解码器实际上依赖于该中间计划的哪些属性？在保持所有输入不变的情况下，仅对推理轨迹进行针对性破坏是否会降低机器人的物理任务性能？我们设计了一个包含七种文本破坏方式的分类体系，分为三个攻击者层级（盲噪声、机械语义和 LLM 自适应），并将其应用于 40 个 LIBERO 桌面操控任务中的最先进推理 VLA 模型。我们的结果揭示了一种显著的不对称性：替换推理轨迹中的物体名称使总成功率降低了 8.3 个百分点（pp）——在目标条件任务中达到 -19.3 pp，在单个任务中达到 -45 pp——而句子重排、空间方向反转、Token 噪声，甚至是由 70B 参数的 LLM 构建的看似合理但错误的计划，其影响都微乎其微（在 ±4 pp 以内）。这种不对称性表明，动作解码器依赖于实体引用的完整性，而非推理质量或顺序结构。值得注意的是，复杂的基于 LLM 的攻击者表现不如简单的机械式物体名称替换，因为保持合理性会无意中保留解码器所需的实体落地结构。使用非推理 VLA 进行的跨架构对照实验证实，该漏洞仅存在于推理增强模型中，而指令级攻击会降低两种架构的性能——这证明了内部推理轨迹是一个独特且隐蔽的威胁向量，对输入验证防御是不可见的。

## Abstract
Recent Vision-Language-Action (VLA) models increasingly adopt chain-of-thought (CoT) reasoning, generating a natural-language plan before decoding motor commands. This internal text channel between the reasoning module and the action decoder has received no adversarial scrutiny. We ask: which properties of this intermediate plan does the action decoder actually rely on, and can targeted corruption of the reasoning trace alone -- with all inputs left intact -- degrade a robot's physical task performance? We design a taxonomy of seven text corruptions organized into three attacker tiers (blind noise, mechanical-semantic, and LLM-adaptive) and apply them to a state-of-the-art reasoning VLA across 40 LIBERO tabletop manipulation tasks. Our results reveal a striking asymmetry: substituting object names in the reasoning trace reduces overall success rate by 8.3~percentage points (pp) -- reaching $-$19.3~pp on goal-conditioned tasks and $-$45~pp on individual tasks -- whereas sentence reordering, spatial-direction reversal, token noise, and even a 70B-parameter LLM crafting plausible-but-wrong plans all have negligible impact (within $\pm$4~pp). This asymmetry indicates that the action decoder depends on entity-reference integrity rather than reasoning quality or sequential structure. Notably, a sophisticated LLM-based attacker underperforms simple mechanical object-name substitution, because preserving plausibility inadvertently retains the entity-grounding structure the decoder needs. A cross-architecture control using a non-reasoning VLA confirms the vulnerability is exclusive to reasoning-augmented models, while instruction-level attacks degrade both architectures -- establishing that the internal reasoning trace is a distinct and stealthy threat vector invisible to input-validation defenses.

---

## 论文详细总结（自动生成）

### 论文总结：改变的思维，改变的行动——探究 VLA 机器人操控中的思维链漏洞

#### 1. 核心问题与整体含义
随着视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA 等）的发展，引入**思维链（Chain-of-Thought, CoT）**推理已成为趋势。模型在执行动作前先生成一段自然语言计划（如“我要拿起酒瓶并放在架子上”）。
*   **研究动机**：这种“先思后行”的模式在推理模块与动作解码器之间建立了一个内部文本通道。作者提出一个关键安全疑问：动作解码器究竟依赖该计划的哪些属性？如果攻击者在不触动视觉输入和原始指令的前提下，仅篡改中间的推理轨迹，是否能诱导机器人发生物理行为错误？
*   **背景**：现有的 CoT 攻击多集中在 LLM 的文本输出（如绕过安全审查），而本论文首次探讨了其在**具身智能（Embodied AI）**领域导致的物理任务失败风险。

#### 2. 方法论
作者提出了一种针对推理轨迹的**拦截与篡改攻击模型**，并设计了包含七种破坏方式的分类体系，分为三个层级：
*   **核心思想**：通过系统性地破坏 CoT 的不同语言属性（顺序、实体、空间词、噪声等），作为一种“探针”来诊断动作解码器的因果敏感性。
*   **攻击层级（Taxonomy）**：
    1.  **Tier 1: 盲噪声（Blind Noise）**：包括随机替换 50% 的 Token（Random Tokens）和使用填充词替换（Padding），测试模型对非结构化噪声的容忍度。
    2.  **Tier 2: 机械语义（Mechanical-Semantic）**：
        *   **句子重排（Shuffled）**：打乱推理步骤的顺序。
        *   **实体交换（Entity Swap）**：将 CoT 中的物体名称替换为场景中不存在的其他物体（如“酒瓶”变“布丁”）。
        *   **否定翻转（Negation Flip）**：将空间方向词反转（如“左”变“右”）。
    3.  **Tier 3: LLM 自适应（LLM-Adaptive）**：使用 Llama-3.1-70B 编写看似合理但逻辑错误的推理计划。
*   **注入机制**：在推理生成后、动作解码前拦截文本，修改后再喂给解码器。

#### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 桌面操控基准测试，涵盖四个子集：LIBERO-Object（简单抓取）、Spatial（空间推理）、Goal（多样化目标）、Long（长程任务），共 40 个任务。
*   **目标模型**：**DeepThinkVLA**（基于 PaliGemma，具有 2.9B 参数的推理 VLA，在 LIBERO 上有 95.4% 的基准成功率）。
*   **对比/对照方法**：
    *   **OpenVLA-OFT**：非推理 VLA 模型，用于验证漏洞是否为推理架构特有（双重分离实验）。
    *   **指令级攻击**：对比直接修改任务指令（Input-level）与修改 CoT（Internal-level）的效果差异。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA H100 GPU** 上运行。
*   **算力消耗**：整个实验活动包含 **180 个作业**，每个作业运行时间约为 **2.3 小时**。
*   **模型调用**：LLM 自适应攻击部分使用了 vLLM 部署的 Llama-3.1-70B-Instruct。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务评估 20 个 Episode，跨越 3 个随机种子。总计每个实验条件下进行 **2,400 个 Episode**（40 任务 × 20 次 × 3 种子）。
*   **充分性分析**：
    *   实验设计非常充分，包含了**剂量反应分析（Graded corruption）**，测试了不同比例（25% 到 100%）的破坏强度。
    *   采用了统计学检验（配对 t 检验、Cohen’s d 效应量、Wilcoxon 符号秩检验）来确保结果的客观性。
    *   通过跨架构对照（推理 vs 非推理）和跨表面对照（指令 vs CoT），有效地隔离了变量，证明了结论的可靠性。

#### 6. 主要结论与发现
*   **选择性因果敏感性**：动作解码器对 **实体引用（Entity Grounding）** 表现出极高的依赖。仅交换物体名称就导致成功率下降 8.3 pp（在目标任务中下降 19.3 pp，单项最高下降 45 pp）。
*   **逻辑结构不敏感**：令人惊讶的是，句子顺序打乱、空间词翻转、甚至 LLM 编造的错误计划对成功率的影响几乎可以忽略不计（±4 pp 以内）。
*   **能力反转（Capability Inversion）**：复杂的 LLM 攻击者表现不如简单的机械式实体替换。原因是 LLM 为了保持“合理性”，无意中保留了模型所需的实体结构。
*   **隐蔽性威胁**：虽然指令级攻击威力更大，但 CoT 攻击在输入端（图像和指令）是完全干净的，这使其能绕过现有的输入验证防御系统。

#### 7. 优点
*   **视角独特**：首次揭示了 VLA 模型内部推理通道的脆弱性，将 CoT 安全研究从文本生成扩展到了物理执行。
*   **诊断深入**：不仅证明了攻击有效，还通过实验拆解了模型到底在“听”CoT 的什么（即实体名而非逻辑）。
*   **防御启示**：提出了基于实体一致性检查的轻量级防御思路，实验证明该方法能以极低误报率拦截 100% 的实体交换攻击。

#### 8. 不足与局限
*   **模型覆盖有限**：主要针对 DeepThinkVLA 进行了深入研究。虽然该架构具有代表性，但结论是否能完美推广到所有双系统 VLA（如 NVIDIA GR00T）仍需更多验证。
*   **任务场景局限**：实验集中在 LIBERO 桌面操控任务，尚未在更复杂的工业环境或移动机器人任务中测试。
*   **攻击优化空间**：研究侧重于诊断性破坏，而非寻找“最优”的对抗样本，实际攻击者的破坏力可能比文中展示的更高。

（完）

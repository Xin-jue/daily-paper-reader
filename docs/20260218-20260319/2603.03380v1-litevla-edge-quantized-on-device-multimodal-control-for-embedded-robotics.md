---
title: "LiteVLA-Edge: Quantized On-Device Multimodal Control for Embedded Robotics"
title_zh: LiteVLA-Edge：面向嵌入式机器人的量化端侧多模态控制
authors: "Justin Williams, Kishor Datta Gupta, Roy George, Mrinmoy Sarkar"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03380v1"
tags: ["query:q12"]
score: 9.0
evidence: 用于嵌入式机器人的量化端侧 VLA 流水线
tldr: LiteVLA-Edge 针对嵌入式机器人部署难题，提出了一种在 Jetson Orin 硬件上实现全本地推理的视觉-语言-动作（VLA）模型流水线。该方法通过 FP32 监督微调、4位 GGUF 量化以及 llama.cpp GPU 加速推理，实现了约 6.6Hz 的端到端低延迟。该研究为在嵌入式设备上运行紧凑型多模态控制模型提供了实用的系统路径，并集成了 ROS 2 环境，为未来机器人任务评估奠定了基础。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型因计算需求高和推理延迟大，难以在资源受限的嵌入式机器人硬件上直接部署。
method: 结合了 FP32 图像到动作的监督微调、4位 GGUF 后训练量化，并利用 llama.cpp 在 Jetson Orin 上进行 GPU 加速推理。
result: 在 ROS 2 集成环境下实现了 150.5 毫秒（约 6.6Hz）的平均端到端延迟，支持完全离线运行。
conclusion: 该研究证明了在嵌入式硬件上执行反应式语言条件控制的可行性，并为端侧 VLA 模型提供了可复现的基准。
---

## 摘要
视觉-语言-动作 (VLA) 模型为感知、语言调节和动作生成提供了一个统一框架，但由于计算需求和推理延迟，许多现有系统仍难以部署在嵌入式机器人环境中。在本文中，我们提出了 LiteVLA-Edge，这是一个面向部署的 VLA 流水线，旨在 Jetson Orin 级硬件上实现完全端侧推理。我们的方法结合了 FP32 精度下的监督式图像到动作微调、训练后 4 位 GGUF 量化，以及通过 llama.cpp 运行时实现的 GPU 加速推理。在我们的部署配置下，LiteVLA-Edge 在完全离线运行于集成 ROS 2 的感知-推理-动作流水线中时，实现了 150.5 ms（约 6.6 Hz）的平均端到端延迟。我们的贡献并非引入新的策略目标，而是为在嵌入式硬件上本地执行紧凑型多模态控制模型提供了一条实际的系统路径，同时保留了感知、推理和执行之间的模块化接口。这些结果确立了反应式语言调节控制的时间可行性，并为未来机器人端侧 VLA 的任务级评估提供了一个可复现的基准。

## Abstract
Vision-Language-Action (VLA) models provide a unified framework for perception, language conditioning, and action generation, but many existing systems remain difficult to deploy in embedded robotic settings because of their computational requirements and inference latency. In this paper, we present LiteVLA-Edge, a deployment-oriented VLA pipeline for fully on-device inference on Jetson Orin-class hardware. Our approach combines supervised image-to-action fine-tuning in FP32 with post-training 4-bit GGUF quantization and GPU-accelerated inference through the \texttt{llama.cpp} runtime. Under our deployment configuration, LiteVLA-Edge achieves a mean end-to-end latency of 150.5\,ms (approximately 6.6\,Hz) while operating entirely offline within a ROS~2-integrated perception--reasoning--action pipeline. Rather than introducing a new policy objective, our contribution is a practical systems path for executing compact multimodal control models locally on embedded hardware while preserving modular interfaces between perception, reasoning, and actuation. These results establish timing feasibility for reactive language-conditioned control and provide a reproducible baseline for future task-level evaluation of on-device VLAs in robotics.

---

## 论文详细总结（自动生成）

### LiteVLA-Edge：面向嵌入式机器人的量化端侧多模态控制总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 RT-2、OpenVLA）虽然泛化能力强，但参数量巨大（通常 >7B），依赖云端计算或高性能桌面 GPU，无法在功耗受限、低延迟要求高或无网络（GPS-denied）的嵌入式机器人环境（如 Jetson Orin）中运行。
*   **研究背景**：早期的 LiteVLA 虽实现了端侧运行，但推理延迟达数秒，只能进行异步开环执行。本文旨在通过量化和优化推理流水线，将 VLA 从“慢速推理”提升至“实时闭环控制”水平。

#### 2. 方法论
*   **核心思想**：构建一个从感知到动作的模块化流水线，利用紧凑型多模态骨干网络，通过监督微调和 4 位量化，在嵌入式 GPU 上实现高频推理。
*   **关键技术细节**：
    *   **骨干网络**：采用蒸馏版的 **SmolVLM-256M**（参数量仅 2.56 亿）。
    *   **微调策略**：在 FP32 精度下使用 **LoRA**（秩 r=8，缩放因子 α=8）进行图像到动作的监督微调（SFT），以保持动作生成的精确度。
    *   **模型压缩**：采用 **GGUF 格式**进行训练后 **4 位量化（Q4_K_M）**，显著降低内存占用和总线延迟。
    *   **推理加速**：利用 **llama.cpp** 库及其 CUDA 后端，将 42 层 Transformer 全部卸载到 Jetson AGX Orin 的 GPU 上。
    *   **系统集成**：通过 **ROS 2** 节点发布 `geometry_msgs/Twist` 速度指令，实现 6.6 Hz 的推理频率与 100 Hz 的底层控制器心跳同步。

#### 3. 实验设计
*   **硬件环境**：主要在 **NVIDIA Jetson AGX Orin (64GB)** 上进行评估，对比了 Raspberry Pi 4 等低端平台。
*   **对比方法**：
    *   **通用 VLM**：Moondream2, TinyLLaVA, PaliGemma, Qwen2-VL（这些模型仅具推理能力，无直接动作输出）。
    *   **VLA 系统**：OpenVLA（7B，需 RTX 4090）、EdgeVLA（~1B，需 A100 或高端边缘 GPU）、Efficient VLA。
*   **Benchmark**：主要测量端到端推理延迟、推理频率（Hz）以及动作生成的稳定性（抖动 σ）。

#### 4. 资源与算力
*   **推理算力**：使用 **NVIDIA Jetson AGX Orin**，运行在 40W 功率模式下。
*   **训练算力**：文中提到在 FP32 下进行 LoRA 微调，但**未明确说明**具体使用的训练 GPU 型号（如 A100 或 H100）及总训练时长。
*   **配置参数**：上下文窗口 $n_{ctx} = 512$，最大输出 12 个 token。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 **300 次**连续推理运行以测量延迟分布。
*   **充分性评价**：
    *   **系统性能评估充分**：对延迟、标准差、最小/最大耗时进行了详尽统计，证明了系统的确定性。
    *   **任务评估相对薄弱**：实验侧重于“部署可行性”和“时延分析”，缺乏在多种复杂物理机器人任务（如不同抓取场景、长程导航）中的成功率对比。
    *   **客观性**：作者明确指出其贡献在于“系统路径”而非“新策略算法”，评价较为客观。

#### 6. 主要结论与发现
*   **性能突破**：LiteVLA-Edge 实现了 **150.5 ms** 的平均端到端延迟（约 **6.6 Hz**），相比之前基准提升了约 220%。
*   **闭环可行性**：6.6 Hz 的频率达到了反应式视觉伺服控制的门槛，允许机器人在动态环境中实时修正轨迹。
*   **稳定性**：系统抖动极低（σ < 0.2 ms），确保了 ROS 2 控制心跳的确定性，避免了高延迟 AI 控制器常见的危险振荡。

#### 7. 优点
*   **实用性强**：提供了一套完整的、基于开源工具（llama.cpp, ROS 2）的嵌入式部署方案，易于复现。
*   **平衡性佳**：在极小的参数量（256M）下保留了多模态推理能力，同时满足了实时控制的频率要求。
*   **模块化设计**：解耦了高层语义推理与底层物理执行，支持安全覆盖（Safety Overrides）。

#### 8. 不足与局限
*   **任务泛化性验证不足**：论文未提供在大规模机器人数据集（如 OXE）上的具体任务成功率指标，主要关注时延。
*   **量化损失风险**：虽然文中称动作稳定，但 4 位量化在极端精确操作任务中可能引入的数值漂移（Action Drift）仍需更广泛的验证。
*   **硬件依赖**：虽然使用了 GGUF 格式，但高性能表现高度依赖 Jetson Orin 的统一内存架构和 CUDA 优化，在非 GPU 嵌入式平台上可能退化。

（完）

---
title: "SimRecon: SimReady Compositional Scene Reconstruction from Real Videos"
title_zh: SimRecon：基于真实视频的仿真就绪组合式场景重建
authors: "Chong Xia, Kai Zhu, Zizhuo Wang, Fangfu Liu, Zhizheng Zhang, Yueqi Duan"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02133v2"
tags: ["query:q11"]
score: 9.0
evidence: 从真实视频中进行组合式场景重建以用于仿真和交互
tldr: SimRecon 提出了一种从真实视频中进行“感知-生成-仿真”的组合式场景重建框架。针对传统方法在复杂场景中视觉保真度低和物理不合理的问题，该方法通过主动视点优化获取最佳投影图像以辅助单物体补全，并利用场景图合成器在仿真器中指导场景构建。实验证明，该方法在 ScanNet 数据集上显著优于现有技术，实现了可直接用于仿真的高质量物体中心化场景表示。
selection_source: fresh_fetch
motivation: 传统的组合式场景重建方法在处理真实世界复杂场景时，往往面临视觉保真度不足和物理布局不合理的问题。
method: 提出包含感知、生成、仿真三个阶段的框架，并引入主动视点优化和场景图合成器两个关键模块来衔接各阶段。
result: 在 ScanNet 数据集上的实验结果表明，SimRecon 在重建质量和物理一致性方面均优于当前最先进的方法。
conclusion: SimRecon 有效实现了从真实视频到可交互仿真场景的自动化转换，为机器人仿真和场景理解提供了新思路。
---

## 摘要
组合式场景重建旨在从真实世界视频中创建以物体为中心的表示，而非整体场景，这天然适用于仿真和交互。传统的组合式重建方法主要强调视觉外观，在真实世界场景中的泛化能力有限。在本文中，我们提出了 SimRecon，这是一个实现了针对杂乱场景重建的“感知-生成-仿真”流水线的框架，该框架首先从视频输入中进行场景级语义重建，然后执行单物体生成，最后在仿真器中组装这些资产。然而，简单地结合这三个阶段会导致生成资产的视觉失真以及最终场景在物理上的不合理，这一问题在复杂场景中尤为严重。因此，我们进一步提出了三个阶段之间的两个衔接模块来解决这一问题。具体而言，针对从感知到生成的过渡（这对视觉保真度至关重要），我们引入了主动视点优化（Active Viewpoint Optimization），它在三维空间中主动搜索以获取最佳投影图像，作为单物体补全的条件。此外，针对从生成到仿真的过渡（这对物理合理性至关重要），我们提出了场景图合成器（Scene Graph Synthesizer），它引导在三维仿真器中从零开始构建，镜像了真实世界的原生构建原理。在 ScanNet 数据集上的广泛实验验证了我们的方法优于以往的最先进方法。

## Abstract
Compositional scene reconstruction seeks to create object-centric representations rather than holistic scenes from real-world videos, which is natively applicable for simulation and interaction. Conventional compositional reconstruction approaches primarily emphasize on visual appearance and show limited generalization ability to real-world scenarios. In this paper, we propose SimRecon, a framework that realizes a "Perception-Generation-Simulation" pipeline towards cluttered scene reconstruction, which first conducts scene-level semantic reconstruction from video input, then performs single-object generation, and finally assembles these assets in the simulator. However, naively combining these three stages leads to visual infidelity of generated assets and physical implausibility of the final scene, a problem particularly severe for complex scenes. Thus, we further propose two bridging modules between the three stages to address this problem. To be specific, for the transition from Perception to Generation, critical for visual fidelity, we introduce Active Viewpoint Optimization, which actively searches in 3D space to acquire optimal projected images as conditions for single-object completion. Moreover, for the transition from Generation to Simulation, essential for physical plausibility, we propose a Scene Graph Synthesizer, which guides the construction from scratch in 3D simulators, mirroring the native, constructive principle of the real world. Extensive experiments on the ScanNet dataset validate our method's superior performance over previous state-of-the-art approaches.

---

## 论文详细总结（自动生成）

以下是对论文《SimRecon: SimReady Compositional Scene Reconstruction from Real Videos》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从杂乱的真实世界视频中，自动重建出既具有高视觉保真度、又符合物理规律且可直接用于仿真交互（Sim-Ready）的组合式 3D 场景。
*   **研究动机**：
    *   **整体重建的局限**：传统的 3D 重建（如 3DGS）将场景视为整体，缺乏物体边界和完整几何，无法进行物理交互。
    *   **现有组合式方法的不足**：现有的物体中心化重建方法在处理真实场景时，常因视角选择不当导致生成物体畸变，且物体间缺乏物理约束，导致在仿真器中出现“物体漂浮”或“穿模”等物理不合理现象。
    *   **Real-to-Sim 鸿沟**：从感知到生成、再到仿真的过程缺乏有效的衔接机制。

### 2. 方法论
SimRecon 提出了一个“感知-生成-仿真”的流水线，核心在于两个关键的衔接模块：
*   **主动视点优化 (Active Viewpoint Optimization, AVO)**：
    *   **核心思想**：利用信息论方法，在 3D 空间中主动搜索能提供最大信息增益的视角，作为物体生成的输入条件。
    *   **技术细节**：将信息增益建模为 3D 高斯渲染过程中的累积不透明度（Opacity）。通过最大化渲染图像的物体像素透明度总和，并引入深度正则化项（防止视点过近），利用梯度下降优化相机位姿。
    *   **迭代扩展**：采用多轮优化并对已观察区域进行衰减，从而获取多个互补的最优视角。
*   **场景图合成器 (Scene Graph Synthesizer, SGS)**：
    *   **核心思想**：通过视觉语言模型（VLM）推断物体间的支撑和附着关系，构建全局一致的场景图。
    *   **技术细节**：采用“分区推断+在线合并”策略。先将场景划分为多个区域并获取局部场景图，再通过广度优先搜索（BFS）合并至全局图。
    *   **冲突解决**：若新推断的关系与已有路径冲突，则针对冲突物体重新采样视角并由 VLM 裁决。
*   **分层物理组装**：根据场景图的层级结构，在仿真器（如 Isaac Sim）中按顺序放置物体，并激活短暂的物理模拟让物体自然沉降，确保物理稳定性。

### 3. 实验设计
*   **数据集**：使用真实的 **ScanNet** 数据集中的 20 个复杂室内场景。
*   **输入**：仅使用原始 RGB 视频，不依赖深度、法线或预标注的语义信息。
*   **对比方法 (Baselines)**：
    *   **单视图方法**：Gen3DSR, SceneGen。
    *   **场景级方法**：DPRecon, InstaScene。
    *   **仿真器对比**：MetaScenes（基于检索和 MCMC 优化的方法）。
*   **评估指标**：
    *   **几何质量**：倒角距离 (CD)、F-Score、法线一致性 (NC)。
    *   **渲染质量**：PSNR、SSIM、LPIPS（有参考）以及 MUSIQ（无参考）。
    *   **效率**：推理时间。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX A6000** GPU。
*   **耗时**：
    *   主动视点优化（AVO）：每个物体约需 **30 秒**。
    *   全流程耗时：约 **21 分钟**（显著快于 DPRecon 的 10 小时以上，略慢于 InstaScene 但质量更高）。

### 5. 实验数量与充分性
*   **实验规模**：在 20 个真实场景上进行了定量评估，涵盖了多种家具和杂乱布局。
*   **消融实验**：针对 AVO（验证 2D 可见性 vs 3D 信息增益、深度约束的重要性）和 SGS（验证全局推断 vs 分区合并、冲突解决机制）分别做了详细的对比实验。
*   **充分性评价**：实验设计较为充分，不仅对比了视觉重建指标，还通过仿真器可视化验证了物理合理性。对比基线涵盖了最新的 SOTA 方法，评估维度全面（几何、渲染、物理、效率）。

### 6. 主要结论与发现
*   **AVO 的有效性**：相比启发式的视角选择，AVO 能显著减少生成物体的几何畸变，尤其是在物体被严重遮挡的情况下。
*   **SGS 的必要性**：通过场景图引导的组装比简单的位置对齐或后处理优化更能保证场景的物理稳定性，避免了物体漂浮。
*   **性能领先**：SimRecon 在几何重建精度（F-Score 提升约 13%）和渲染保真度上均优于现有方法，且具备良好的推理效率。

### 7. 优点
*   **系统性强**：首次完整实现了从原始视频到物理仿真环境的自动化闭环。
*   **衔接巧妙**：AVO 解决了感知与生成间的“视角质量”问题，SGS 解决了生成与仿真间的“物理逻辑”问题。
*   **泛化性好**：不依赖特定类别的 CAD 模型检索，而是通过生成模型处理任意物体。

### 8. 不足与局限
*   **依赖基础模型**：系统的上限受限于底层 3D 生成模型（如 Rodin）和 VLM（如 Qwen2.5-VL）的能力。
*   **动态场景限制**：目前主要针对静态室内场景，尚未讨论动态物体或光影剧烈变化的场景。
*   **物理属性推断**：虽然提到了质量、材质等物理属性，但文中对这些属性的精确推断方法描述较少，主要依赖 VLM 的常识推理。

（完）

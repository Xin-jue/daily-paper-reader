---
title: "LiLo-VLA: Compositional Long-Horizon Manipulation via Linked Object-Centric Policies"
title_zh: LiLo-VLA：通过链接的以对象为中心的策略实现组合式长程操纵
authors: "Yue Yang, Shuo Cheng, Yu Fang, Homanga Bharadhwaj, Mingyu Ding, Gedas Bertasius, Daniel Szafir"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21531v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于长程操作任务的VLA框架
tldr: 针对长程操作任务中视觉语言动作模型（VLA）难以处理组合复杂性和级联错误的问题，本文提出LiLo-VLA框架。该框架通过解耦全局运输与局部交互，利用以物体为中心的VLA策略实现零样本泛化。通过动态重规划和技能复用，LiLo-VLA在仿真和真实世界长程任务中表现优异，显著提升了任务成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在处理涉及多个运动结构变化的长程任务时，容易受到环境干扰并产生级联故障。
method: 提出一种模块化框架，将任务分解为负责全局移动的到达模块和负责局部物体交互的以物体为中心的VLA模块。
result: "在仿真基准测试中达到69%的成功率，远超Pi0.5等模型，并在真实世界任务中实现85%的成功率。"
conclusion: LiLo-VLA通过模块化解耦和物体中心化策略，有效解决了长程操作中的泛化与鲁棒性难题。
---

## 摘要
通用机器人必须掌握长程操纵，这被定义为在非结构化环境中涉及多次运动学结构变化（例如，连接或分离物体）的任务。虽然视觉-语言-动作（VLA）模型具有掌握多种原子技能的潜力，但它们在对这些技能进行排序时面临组合复杂性的挑战，并且由于环境敏感性，容易出现级联故障。为了应对这些挑战，我们提出了 LiLo-VLA（Linked Local VLA），这是一个模块化框架，能够在从未经过训练的情况下，对新的长程任务实现零样本泛化。我们的方法将运输与交互解耦：到达模块（Reaching Module）处理全局运动，而交互模块（Interaction Module）采用以对象为中心的 VLA 来处理孤立的感兴趣对象，从而确保对无关视觉特征的鲁棒性以及对空间配置的不变性。至关重要的是，这种模块化通过动态重规划和技能重用促进了鲁棒的故障恢复，有效地减轻了端到端方法中常见的级联错误。我们引入了一个包含 21 个任务的仿真基准，由两个具有挑战性的套件组成：LIBERO-Long++ 和 Ultra-Long。在这些仿真中，LiLo-VLA 实现了 69% 的平均成功率，分别比 Pi0.5 和 OpenVLA-OFT 高出 41% 和 67%。此外，在 8 个长程任务的真实世界评估中，平均成功率达到了 85%。项目页面：https://yy-gx.github.io/LiLo-VLA/。

## Abstract
General-purpose robots must master long-horizon manipulation, defined as tasks involving multiple kinematic structure changes (e.g., attaching or detaching objects) in unstructured environments. While Vision-Language-Action (VLA) models offer the potential to master diverse atomic skills, they struggle with the combinatorial complexity of sequencing them and are prone to cascading failures due to environmental sensitivity. To address these challenges, we propose LiLo-VLA (Linked Local VLA), a modular framework capable of zero-shot generalization to novel long-horizon tasks without ever being trained on them. Our approach decouples transport from interaction: a Reaching Module handles global motion, while an Interaction Module employs an object-centric VLA to process isolated objects of interest, ensuring robustness against irrelevant visual features and invariance to spatial configurations. Crucially, this modularity facilitates robust failure recovery through dynamic replanning and skill reuse, effectively mitigating the cascading errors common in end-to-end approaches. We introduce a 21-task simulation benchmark consisting of two challenging suites: LIBERO-Long++ and Ultra-Long. In these simulations, LiLo-VLA achieves a 69% average success rate, outperforming Pi0.5 by 41% and OpenVLA-OFT by 67%. Furthermore, real-world evaluations across 8 long-horizon tasks demonstrate an average success rate of 85%. Project page: https://yy-gx.github.io/LiLo-VLA/.

---

## 论文详细总结（自动生成）

以下是对论文《LiLo-VLA: Compositional Long-Horizon Manipulation via Linked Object-Centric Policies》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在处理**长程操纵（Long-Horizon Manipulation）**任务时面临两大瓶颈：
    1.  **缺乏组合泛化能力**：难以在未见过的任务序列中灵活组合原子技能，通常需要大量特定任务的演示数据。
    2.  **级联失效（Cascading Failures）**：VLA 模型容易过拟合视觉特征或空间配置，一旦序列中某个环节出错，会导致整个任务失败。
*   **研究背景**：长程任务涉及多次运动结构变化（如抓取、放置、倾倒）。现有方法要么是端到端训练（数据效率低），要么是分层规划（依赖人工设计的奖励或目标），难以兼顾鲁棒性与泛化性。

### 2. 方法论：LiLo-VLA 框架
LiLo-VLA 的核心思想是将**全局运输（Transport）**与**局部交互（Interaction）**解耦，通过模块化设计实现零样本组合泛化。

*   **到达模块（Reaching Module）**：
    *   使用经典的运动规划器（如 MPLib）处理全局运动，确保无碰撞地将机械臂末端送达目标物体附近的“接近位姿”（Approach Pose）。
    *   **扰动策略**：在训练数据生成阶段，对起始位姿引入随机扰动，使下游 VLA 策略对运动规划和感知的误差具有鲁棒性。
*   **交互模块（Interaction Module）**：
    *   **以物体为中心的 VLA**：仅使用腕部摄像头（Wrist-view）观测，消除全局环境干扰。
    *   **视觉掩码增强**：在训练和推理时对非目标物体进行随机擦除或掩码处理，强制模型关注当前操作对象，解决观测空间偏移（OSS）问题。
*   **闭环执行与故障恢复算法**：
    *   系统按顺序执行符号动作序列。
    *   **恢复机制**：每步执行后进行几何验证。若失败且未持物（如抓取失败），则原地重试；若失败且已丢弃物体（如放置失败），则回溯至上一个“抓取”动作重新开始。

### 3. 实验设计
*   **仿真场景**：
    *   **LIBERO-Long++**：在原始基准上增加视觉干扰物，测试视觉鲁棒性。
    *   **Ultra-Long**：包含 3 个超长任务（最高达 16 个步骤），测试时间扩展性。
    *   **对比方法**：Pi0.5（流匹配 VLA）、OpenVLA-OFT（优化后的 OpenVLA）。
*   **真实世界**：
    *   使用 Franka Emika Panda 机器人，涵盖 8 个长程任务（最高 8 步）。
    *   测试维度包括标准配置、多样化布局（视觉干扰）和改变序列顺序（组合泛化）。

### 4. 资源与算力
*   **硬件设备**：
    *   **推理端**：远程集群节点，配备 **NVIDIA RTX A6000 GPU**。
    *   **控制/感知端**：本地工作站，配备 **NVIDIA RTX 4080 GPU**。
    *   **机器人**：Franka Emika Panda，配备 Robotiq 2F-85 夹爪及 ZED 系列摄像头。
*   **训练细节**：
    *   仿真模型（OpenVLA-OFT）训练了 100,000 步。
    *   真实世界模型（Pi0.5）训练了 30,000 步。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验涵盖 21 个任务配置，每个配置进行 10 次试验；真实世界实验涵盖 8 个任务，每个配置 5 次试验。
*   **消融实验**：详细对比了“无到达模块”、“无视觉掩码”、“无恢复机制”以及不同摄像头配置（腕部 vs 第三视角）的影响。
*   **评估客观性**：通过引入“变体序列”（Variant Sequences）强制测试零样本组合能力，而非简单的轨迹记忆，实验设计较为严谨且公平。

### 6. 主要结论与发现
*   **性能领先**：在仿真中，LiLo-VLA 达到 69% 的平均成功率，远超 Pi0.5 (28%) 和 OpenVLA-OFT (2%)。
*   **组合泛化**：LiLo-VLA 能够直接执行从未训练过的长程序列，而基准模型在序列顺序改变时性能往往会崩溃。
*   **鲁棒性**：通过以物体为中心的视觉处理，模型对背景杂乱和布局变化的抵抗力显著增强。
*   **恢复价值**：闭环恢复机制是实现超长程（16步）任务的关键，移除该模块会导致成功率从 44% 降至 0%。

### 7. 优点（亮点）
*   **解耦设计**：巧妙结合了经典运动规划的确定性与 VLA 的语义操作能力，降低了学习难度。
*   **零样本能力**：无需长程演示数据，仅靠原子技能即可组合出复杂行为，极大地提高了数据效率。
*   **视觉鲁棒性**：通过腕部视角和随机掩码技术，有效解决了长程任务中常见的视觉过拟合问题。

### 8. 不足与局限
*   **感知依赖**：系统高度依赖外部感知模型（如 FoundationPose）提供物体位姿，在处理透明或严重遮挡物体时可能失效。
*   **技能上限**：整体性能受限于底层 VLA 骨干网络对原子技能的掌握熟练度。
*   **验证成本**：真实世界中的成功验证目前仍部分依赖人工或启发式几何规则，缺乏完全自主的语义级故障理解。

（完）

---
title: "PVI: Plug-in Visual Injection for Vision-Language-Action Models"
title_zh: PVI：视觉-语言-动作模型的插件式视觉注入
authors: "Zezhou Zhang, Songxin Zhang, Xiao Xiong, Junjie Zhang, Zejian Xie, Jingyi Xi, Zunyao Mao, Zan Mao, Zhixin Mai, Zhuoyang Song, Jiaxing Zhang"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12772v1"
tags: ["query:q12"]
score: 10.0
evidence: 用于VLA架构的插件模块，注入时空和几何线索
tldr: 本研究针对视觉-语言-动作（VLA）模型在处理细粒度几何线索和时序信息方面的不足，提出了插件式视觉注入（PVI）模块。PVI是一种轻量级、与编码器无关的模块，通过零初始化的残差路径将辅助视觉特征注入预训练的动作专家模型中，仅需单阶段微调即可保持预训练行为。实验表明，PVI能显著提升策略性能，且时序视频特征在多阶段任务中优于静态图像特征，并在真实机器人双手织物折叠任务中验证了其有效性。
selection_source: fresh_fetch
motivation: 现有的VLA模型往往会削弱细粒度的几何特征且缺乏显式的时序证据，而现有的视觉注入方法通常需要复杂的架构修改或仅限于静态特征。
method: 提出了一种名为PVI的轻量级、编码器无关模块，通过零初始化残差路径将辅助视觉表示（如V-JEPA2时序特征）注入预训练的动作专家中。
result: PVI在多项任务中一致优于基础策略，且实验证明时序视频特征在需要状态跟踪和协调的多阶段任务中比强静态图像特征更有效。
conclusion: PVI提供了一种高效且灵活的方式来增强VLA模型的视觉感知能力，在模拟环境和真实长程双手操作任务中均表现出极高的实用价值。
---

## 摘要
将预训练的视觉语言模型（VLM）与流匹配动作专家相结合的视觉-语言-动作（VLA）架构已成为语言条件操控的一种强大范式。然而，针对语义抽象进行优化且通常以静态视觉观测为条件的 VLM，往往会削弱细粒度的几何线索，并且通常缺乏为动作专家提供的显式时间证据。先前的工作通过注入辅助视觉特征来缓解这一问题，但现有方法要么侧重于静态空间表示，要么需要对架构进行重大修改以适应时间输入，导致时间信息未得到充分探索。我们提出了插件式视觉注入（PVI），这是一种轻量级、与编码器无关的模块，它附加到预训练的动作专家上，并通过零初始化的残差路径注入辅助视觉表示，仅需单阶段微调即可保留预训练行为。使用 PVI，我们在基础策略和一系列具有竞争力的替代注入策略上获得了一致的提升。我们的对照研究表明，时间视频特征（V-JEPA2）优于强大的静态图像特征（DINOv2），在需要状态跟踪和协调的多阶段任务中提升最为显著。在长时程双臂折叠衣服上的真实机器人实验进一步证明了 PVI 在模拟环境之外的实用性。

## Abstract
VLA architectures that pair a pretrained VLM with a flow-matching action expert have emerged as a strong paradigm for language-conditioned manipulation. Yet the VLM, optimized for semantic abstraction and typically conditioned on static visual observations, tends to attenuate fine-grained geometric cues and often lacks explicit temporal evidence for the action expert. Prior work mitigates this by injecting auxiliary visual features, but existing approaches either focus on static spatial representations or require substantial architectural modifications to accommodate temporal inputs, leaving temporal information underexplored. We propose Plug-in Visual Injection (PVI), a lightweight, encoder-agnostic module that attaches to a pretrained action expert and injects auxiliary visual representations via zero-initialized residual pathways, preserving pretrained behavior with only single-stage fine-tuning. Using PVI, we obtain consistent gains over the base policy and a range of competitive alternative injection strategies, and our controlled study shows that temporal video features (V-JEPA2) outperform strong static image features (DINOv2), with the largest gains on multi-phase tasks requiring state tracking and coordination. Real-robot experiments on long-horizon bimanual cloth folding further demonstrate the practicality of PVI beyond simulation.

---

## 论文详细总结（自动生成）

### PVI: 视觉-语言-动作模型的插件式视觉注入 论文总结

#### 1. 核心问题与研究动机
现有的视觉-语言-动作（VLA）模型通常采用“预训练 VLM + 扩散/流匹配动作专家”的架构。然而，这种设计存在两个主要瓶颈：
*   **语义与几何的鸿沟**：VLM 骨干网络主要针对高层语义抽象进行优化，往往会过滤掉对精细操控至关重要的几何细节（如边缘、接触面）。
*   **时序信息的缺失**：VLM 通常基于静态图像观测，缺乏动作专家所需的动态时序证据（如运动趋势、状态转换）。
虽然先前有研究尝试注入辅助特征，但大多局限于静态表示，或需要对预训练架构进行大规模修改。本论文旨在提出一种**轻量级、非侵入式且支持时序信息**的注入方案。

#### 2. 方法论：插件式视觉注入 (PVI)
PVI 的核心思想是在不改变预训练 VLA 骨干网络的情况下，通过一个并行的“副本分支”将辅助视觉特征注入动作专家。
*   **核心架构**：
    *   **辅助编码器（Encoder-agnostic）**：可以使用任何预训练模型（如时序视频编码器 V-JEPA2 或静态图像编码器 DINOv2）提取特征。
    *   **可训练副本分支**：实例化一个与主动作专家（DiT）架构相同的副本分支，并用预训练权重初始化。该分支接收辅助视觉特征作为条件输入。
    *   **层级残差注入**：副本分支的每一层隐藏状态通过**零初始化（Zero-initialized）**的线性层映射，并以残差形式加到主 DiT 的对应层中。
*   **训练策略**：冻结 VLM 和主 DiT 骨干，仅训练注入路径（投影层、副本分支、注入层）。由于采用零初始化，训练初期模型行为与原始预训练模型完全一致，确保了训练的稳定性。

#### 3. 实验设计
*   **基础模型与场景**：以 GR00T N1.5 为基座模型。
*   **仿真环境**：使用 **RoboTwin 2.0** 仿真器，涵盖 20 到 50 种不同的双手操作任务（如调整瓶子、敲击闹钟、堆叠碗碟等）。
*   **对比方法**：
    *   **Concat**：输入层特征拼接。
    *   **ControlNet-style**：借鉴图像生成的控制网络结构。
    *   **ReferenceNet-style**：参考网络结构。
    *   **ControlVLA-style**：双重交叉注意力注入。
*   **真实机器人实验**：在 Airbot 双臂平台上进行长时程（8 个阶段）的双手织物折叠任务。

#### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA H20Z GPU**。
*   **训练规模**：
    *   标准任务：4 张 H20Z，训练 20k 步。
    *   长时程/多任务：8 张 H20Z，训练 60k 步或约 60 个 Epoch。
*   **超参数**：学习率统一为 $1 \times 10^{-4}$，Batch Size 为 32 或 64。

#### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度对比**：对比了 5 种不同的注入架构策略。
*   **表示研究**：系统对比了静态特征（DINOv2）、时序特征（V-JEPA2）及其组合的效果。
*   **规模化验证**：不仅在 20 个独立任务上测试，还进行了 20 任务和 50 任务的混合多任务训练。
*   **消融实验**：对时序上下文长度（帧数）、投影层冻结、零初始化等关键变量进行了消融分析。
*   **公平性**：所有对比方法均在相同的预训练权重、数据量和训练预算下进行。

#### 6 主要结论与发现
*   **注入机制至关重要**：PVI 显著优于 Concat 和 ControlNet 等变体，将平均成功率从 35.7% 提升至 59.7%（+24.0%）。
*   **时序特征优于静态特征**：在 PVI 框架下，注入 V-JEPA2 视频特征的效果一致优于 DINOv2 静态特征，尤其是在需要状态跟踪和双臂协调的任务中。
*   **行为保持**：零初始化和残差设计允许模型在保留预训练知识的同时，平滑地整合新视觉线索。
*   **现实迁移**：PVI 成功驱动了复杂的真实双手折叠任务，证明了其在处理可变形物体和长程任务中的实用性。

#### 7. 优点（亮点）
*   **非侵入式设计**：无需重新训练庞大的 VLM 骨干，对现有 VLA 模型具有极强的适配性。
*   **编码器无关性**：提供了一个通用的接口，可以根据任务需求灵活更换视觉前端（图像、视频、点云等）。
*   **单阶段微调**：简化了训练流程，且由于副本分支的存在，训练非常稳定。
*   **时序增强**：填补了现有 VLA 模型在动态感知方面的短板。

#### 8. 不足与局限
*   **推理开销**：虽然训练是轻量级的，但推理时需要额外运行一个辅助编码器和副本分支，会增加一定的计算延迟和显存占用。
*   **数据依赖**：性能提升仍依赖于高质量的动作演示数据进行微调，并非完全的零样本增强。
*   **参数量增加**：虽然冻结了大部分参数，但总参数量（Total Parameters）由于引入了辅助编码器和副本分支而显著增加。

（完）

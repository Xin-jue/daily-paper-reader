---
title: "AnoleVLA: Lightweight Vision-Language-Action Model with Deep State Space Models for Mobile Manipulation"
title_zh: AnoleVLA：基于深度状态空间模型的轻量级视觉-语言-动作模型，用于移动操作
authors: "Yusuke Takagi, Motonari Kambara, Daichi Yashima, Koki Seno, Kento Tokura, Komei Sugiura"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15046v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于语言引导机器人操作的轻量级VLA模型
tldr: "针对语言引导的机器人操作任务，传统基于 Transformer 的视觉-语言-动作模型（VLA）因计算成本高而难以在资源受限环境下部署。本文提出 AnoleVLA，一种基于深度状态空间模型（SSM）的轻量化 VLA。该模型利用 SSM 高效的序列建模能力处理多模态输入并生成动作轨迹。实验表明，AnoleVLA 在真实场景中的成功率比大型 VLA 高出 21%，且推理速度提升了三倍，为移动机器人提供了高效且通用的解决方案。"
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作模型（VLA）由于 Transformer 架构的高计算开销，难以在资源受限的移动机器人平台上实时部署。
method: 提出 AnoleVLA 模型，采用深度状态空间模型（SSM）替代传统 Transformer 来高效处理多模态序列并生成机器人动作轨迹。
result: 在真实世界测试中，AnoleVLA 的任务成功率比主流大型 VLA 高出 21 个百分点，同时推理速度快了约三倍。
conclusion: 该研究证明了基于 SSM 的轻量化架构在保持高性能的同时，能显著提升移动机器人操作的计算效率和任务泛化能力。
---

## 摘要
在本研究中，我们解决了语言引导的机器人操作问题，即机器人需要根据视觉观察和自然语言指令操作各种物体。这一任务对于在人类环境中运行的服务机器人至关重要，且要求具备安全性、效率和任务级的通用性。尽管视觉-语言-动作模型（VLA）在该任务中表现出强大的性能，但由于标准 Transformer 骨干网络的计算成本，它们在资源受限环境中的部署仍然具有挑战性。为了克服这一限制，我们提出了 AnoleVLA，这是一种轻量级 VLA，它使用深度状态空间模型来高效处理多模态序列。该模型利用其轻量且快速的序列状态建模来处理视觉和文本输入，从而使机器人能够高效地生成轨迹。我们在仿真和物理实验中评估了所提方法。值得注意的是，在真实世界的评估中，AnoleVLA 在任务成功率上比代表性的大规模 VLA 高出 21 个百分点，同时推理速度快了约三倍。

## Abstract
In this study, we address the problem of language-guided robotic manipulation, where a robot is required to manipulate a wide range of objects based on visual observations and natural language instructions. This task is essential for service robots that operate in human environments, and requires safety, efficiency, and task-level generality. Although Vision-Language-Action models (VLAs) have demonstrated strong performance for this task, their deployment in resource-constrained environments remains challenging because of the computational cost of standard transformer backbones. To overcome this limitation, we propose AnoleVLA, a lightweight VLA that uses a deep state space model to process multimodal sequences efficiently. The model leverages its lightweight and fast sequential state modeling to process visual and textual inputs, which allows the robot to generate trajectories efficiently. We evaluated the proposed method in both simulation and physical experiments. Notably, in real-world evaluations, AnoleVLA outperformed a representative large-scale VLA by 21 points for the task success rate while achieving an inference speed approximately three times faster.

---

## 论文详细总结（自动生成）

以下是对论文《AnoleVLA: Lightweight Vision-Language-Action Model with Deep State Space Models for Mobile Manipulation》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视觉-语言-动作模型（VLA）虽然在机器人操作任务中表现出色，但大多基于 Transformer 架构。Transformer 的自注意力机制随序列长度呈平方级计算复杂度（$O(L^2)$），导致在资源受限的移动机器人平台上推理延迟高、显存占用大，难以实现实时、长上下文的多模态处理。
*   **研究背景**：服务机器人需要在人类环境中根据自然语言指令执行复杂的物体操作（如“把苹果放进红碗里”）。这要求模型既要具备强大的多模态理解能力，又要满足移动平台的轻量化和高频率控制需求。

### 2. 方法论
*   **核心思想**：引入**深度状态空间模型（Deep SSM，具体为 Mamba）**替代 Transformer 作为 VLA 的骨干网络，利用其线性计算复杂度（$O(L)$）实现高效的序列建模。
*   **关键技术细节**：
    *   **多模态输入编码**：输入包括当前图像（使用 SigLIP 2 编码）、机器人本体状态（Proprioception）、状态增量（State Delta）以及自然语言指令。所有输入被投影到统一的潜在空间并拼接成序列。
    *   **Mamba 骨干网络**：采用 370M 参数的 Mamba 块堆叠而成，通过其选择性扫描机制动态处理多模态信息，并维持一个循环状态来整合长时上下文。
    *   **连续动作生成**：不同于将动作离散化为 Token 的主流做法，AnoleVLA 直接从骨干网络的最终隐藏状态通过线性层预测连续的动作块（Action Chunk，长度 $H=50$）。
    *   **两阶段训练策略**：
        *   **第一阶段**：使用速度损失（Velocity Loss, $L_1$）训练模型重建参考轨迹。
        *   **第二阶段**：引入加速度损失（Acceleration Loss），监督动作的时间差分，以提升轨迹的平滑度和执行稳定性。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：使用 Meta-World 基准测试，包含 50 种不同的物体操作任务（如开门、移动物体等）。
    *   **物理实验**：使用丰田 HSR 移动机器人，在模拟家庭环境（WRS2020RS 标准）中执行移动、抓取、开/关抽屉、按压泵瓶等 5 类任务。
*   **对比方法（Baselines）**：
    *   大规模模型：$\pi_{0.5}$ (3.0B 参数)。
    *   同规模轻量化模型：SmolVLA (0.45B)、VLA-Adapter (0.5B)、TinyVLA (0.42B)。
*   **评估指标**：任务成功率（Success Rate）和推理速度（Inference Speed, ms/chunk）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA GeForce RTX 4090 (24GB)** GPU 和 Intel Core i9-14900F 处理器。
*   **训练时长**：总训练步数为 400k 步，耗时约 **20 小时**。
*   **模型规格**：参数量约为 **467M**，计算量为 91 GFLOPs。
*   **推理性能**：推理速度为 **216 ms/chunk**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真：使用了 Meta-World 的 2500 个情节进行训练和验证。
    *   物理：收集了 250 个真实世界情节，每类任务进行 20 次测试。
*   **消融实验**：针对“加速度损失”进行了对比实验，证明两阶段训练能提升约 4.7% 的成功率。
*   **充分性评价**：实验设计较为全面，涵盖了从仿真到现实的迁移，并进行了详细的错误分析（归纳为位置识别错误、抓取点预测错误等）。对比实验选择了多个最新的轻量化 VLA，具有较强的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：在 Meta-World 仿真中，AnoleVLA 平均成功率达 67.85%，远超 $\pi_{0.5}$ (43.8%) 和 SmolVLA (57.33%)。
*   **真实世界优势**：在物理实验中，AnoleVLA 成功率比 $\pi_{0.5}$ 高出 **21 个百分点**，且推理速度快了约 **3 倍**。
*   **平滑性提升**：加速度损失的引入显著减少了机器人执行动作时的抖动，使其在开抽屉等接触密集型任务中表现更稳健。
*   **样本效率**：即使在每项任务仅有 50 个演示样本的小规模数据集上，AnoleVLA 也能实现有效的任务习得，表现出比大型模型更好的微调适应性。

### 7. 优点
*   **架构创新**：率先在 VLA 领域验证了 SSM（Mamba）替代 Transformer 的可行性，解决了长序列处理的效率瓶颈。
*   **高效推理**：在保持高性能的同时，显著降低了计算开销，非常适合部署在移动机器人等边缘设备上。
*   **控制平滑**：通过两阶段训练和连续动作空间设计，解决了传统 VLA 动作不连贯、抖动的问题。

### 8. 不足与局限
*   **空间推理能力有限**：错误分析显示，最主要的失败原因是“位置识别错误”，表明模型在从图像中提取精确空间坐标方面仍有提升空间。
*   **应用限制**：目前主要针对短时域的动作块生成，对于极长时程的任务规划能力尚待进一步验证。
*   **泛化风险**：虽然在 50 种任务上表现良好，但在面对完全未见过的物体或极端光照环境时的鲁棒性仍需更多实验支撑。

（完）

---
title: Learning Surgical Robotic Manipulation with 3D Spatial Priors
title_zh: 利用3D空间先验学习手术机器人操控
authors: "Yu Sheng, Lidian Wang, Xiaomeng Chu, Jiajun Deng, Min Cheng, Yanyong Zhang, Bei Hua, Houqiang Li, Jianmin Ji"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03798v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于手术机械臂操作的端到端视觉运动策略
tldr: 本研究针对手术机器人操作中3D空间感知不足的问题，提出了Spatial Surgical Transformer (SST) 端到端策略。通过构建包含3万对立体图像的大规模数据集Surgical3D，研究者微调了几何Transformer以提取3D潜表征，并利用多级空间特征连接器将其与机器人动作空间对齐。实验证明，该方法在打结和离体器官解剖等复杂任务中达到了SOTA性能，显著提升了手术机器人的空间泛化能力，为临床部署提供了新方案。
selection_source: fresh_fetch
motivation: 现有的手术机器人操作方法存在多阶段误差累积或因增加腕部摄像头而导致临床实用性差的问题。
method: 提出SST模型，利用新构建的Surgical3D数据集微调几何Transformer提取3D特征，并通过轻量级连接器实现端到端动作映射。
result: 在打结和器官解剖等真实机器人实验中，SST展现了卓越的性能和强大的空间泛化能力，优于现有方法。
conclusion: 该研究通过引入3D空间先验，为实现临床可用的端到端手术机器人操作迈出了重要一步。
---

## 摘要
实现3D空间感知对于需要精确且精细操作的手术机器人操控至关重要。现有方法要么在操控前显式重建手术场景，要么通过增加腕部摄像头来补充默认的立体内窥镜，从而增强多视图特征。然而，这两种范式都存在显著局限性：前者由于其多阶段性质，容易导致误差累积并阻碍端到端优化；而后者在临床实践中很少被采用，因为腕部摄像头可能会干扰手术机器人臂的运动。在这项工作中，我们提出了空间手术Transformer（SST），这是一种端到端的视觉运动策略，通过直接挖掘嵌入在内窥镜图像中的3D空间线索，赋予手术机器人3D空间感知能力。首先，我们构建了Surgical3D，这是一个包含3万对具有精确3D几何信息的写实立体内窥镜图像对的大规模数据集，解决了手术场景中3D数据稀缺的问题。基于Surgical3D，我们微调了一个强大的几何Transformer，以从立体内窥镜图像中提取鲁棒的3D潜表征。随后，这些表征通过轻量级的多级空间特征连接器（MSFC）与机器人的动作空间无缝对齐，且全部在以内窥镜为中心的坐标系内完成。广泛的真实机器人实验表明，SST在打结和离体器官解剖等复杂手术任务上实现了最先进的性能和强大的空间泛化能力，标志着向实际临床部署迈出了重要一步。数据集和代码将会开源。

## Abstract
Achieving 3D spatial awareness is crucial for surgical robotic manipulation, where precise and delicate operations are required. Existing methods either explicitly reconstruct the surgical scene prior to manipulation, or enhance multi-view features by adding wrist-mounted cameras to supplement the default stereo endoscopes. However, both paradigms suffer from notable limitations: the former easily leads to error accumulation and prevents end-to-end optimization due to its multi-stage nature, while the latter is rarely adopted in clinical practice since wrist-mounted cameras can interfere with the motion of surgical robot arms. In this work, we introduce the Spatial Surgical Transformer (SST), an end-to-end visuomotor policy that empowers surgical robots with 3D spatial awareness by directly exploring 3D spatial cues embedded in endoscopic images. First, we build Surgical3D, a large-scale photorealistic dataset containing 30K stereo endoscopic image pairs with accurate 3D geometry, addressing the scarcity of 3D data in surgical scenes. Based on Surgical3D, we finetune a powerful geometric transformer to extract robust 3D latent representations from stereo endoscopes images. These representations are then seamlessly aligned with the robot's action space via a lightweight multi-level spatial feature connector (MSFC), all within an endoscope-centric coordinate frame. Extensive real-robot experiments demonstrate that SST achieves state-of-the-art performance and strong spatial generalization on complex surgical tasks such as knot tying and ex-vivo organ dissection, representing a significant step toward practical clinical deployment. The dataset and code will be released.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Spatial Surgical Transformer (SST)** 的端到端视觉运动策略，旨在提升手术机器人在复杂临床环境中的 3D 空间感知与操作精度。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：手术机器人操作需要毫米级的精度，但现有的视觉学习方法在 3D 空间感知上存在缺陷。
*   **研究背景**：
    *   **显式重建法**：先重建 3D 场景再操作，容易产生多阶段误差累积，且无法进行端到端优化。
    *   **多视角增强法**：通过在机械臂末端增加腕部摄像头来补充视角。但在临床实践中，手术器械需通过狭窄的穿刺器（Trocar），腕部摄像头会干扰运动，极具局限性。
*   **研究动机**：开发一种仅依赖标准立体内窥镜图像、具备 3D 空间先验、且能端到端优化的手术操控策略。

### 2. 方法论
SST 的核心思想是将预训练的几何模型转化为手术领域的空间特征提取器，并将其与动作空间对齐。
*   **Surgical3D 数据集构建**：利用 NVIDIA Omniverse 构建了包含 3 万对写实立体图像的大规模数据集，提供精确的深度图和点云标注。通过对真实手术视频生成伪标签，缓解了“仿真到现实”的领域差距。
*   **几何 Transformer 微调**：采用 **MASt3R** 作为原型，在 Surgical3D 上进行微调。该模型通过回归损失（Regression Loss）和置信度感知损失（Confidence-aware Loss）学习预测密集的 3D 点图，从而提取鲁棒的 3D 潜表征。
*   **多级空间特征连接器 (MSFC)**：
    *   从几何 Transformer 的不同层提取特征：底层捕捉细粒度局部细节，高层捕捉全局上下文。
    *   通过轻量级 MLP 将这些多级特征对齐并融合到策略解码器的动作特征空间中。
*   **以内窥镜为中心的动作空间**：由于手术机器人运动学不精确，SST 采用相对位姿表示（SE(3) 变换），并将所有动作转换到内窥镜坐标系下，确保感知与动作的坐标一致性。

### 3. 实验设计
*   **实验场景**：在 **Torin 手术机器人**上进行了三类真实任务评估：
    1.  **Peg Pickup（套圈拾取）**：在具有不规则几何形状的硅胶肝脏模型上进行。
    2.  **Knot Tying（打结）**：双臂协作，涉及柔性缝合线的复杂操作。
    3.  **Ex-vivo Gallbladder Dissection（离体胆囊解剖）**：在真实猪器官上进行抓取和切割动作。
*   **对比方法 (Benchmarks)**：
    *   **SRT**：带腕部摄像头的 SOTA 手术机器人 Transformer。
    *   **ACT**：动作分块 Transformer（通用机器人学习基准）。
    *   **Diffusion Policy (DP)**：扩散策略。

### 4. 资源与算力
*   **算力设备**：论文提到在 **NVIDIA RTX-4090 GPU** 上进行推理测试。
*   **推理延迟**：SST（基于 MASt3R）的推理延迟为 **56.2ms**（约 18Hz），而基于更重型的 VGGT 模型则需 140.4ms。
*   **训练细节**：所有模型均训练了 **100 个 epoch**。具体的 GPU 训练数量和总时长未在文中明确说明。

### 5. 实验数量与充分性
*   **实验规模**：每个任务收集了 **180-200 条**演示轨迹。
*   **评估标准**：每个任务进行 10 次独立测试，记录成功率。
*   **消融实验**：针对“是否在 Surgical3D 上微调”、“连接器设计（LFC vs MSC vs MSFC）”、“模型效率”进行了详细对比。
*   **充分性评价**：实验覆盖了从硅胶模型到真实离体器官的跨度，对比了当前主流的端到端策略，实验设计较为客观、公平，验证了 3D 先验在提升泛化能力方面的关键作用。

### 6. 主要结论与发现
*   **性能领先**：SST 在不使用腕部摄像头的情况下，性能达到了与使用腕部摄像头的 SRT 相当甚至更优的水平。
*   **空间泛化强**：在 Peg Pickup 任务中，当目标物体放置在训练分布之外的区域时，SST 表现出极强的空间适应性，而 ACT 等方法则完全失效。
*   **3D 先验的必要性**：消融实验证明，未经手术领域微调的几何模型无法提供有效的空间线索，导致策略在复杂任务中失败。

### 7. 优点与亮点
*   **临床实用性**：摆脱了对腕部摄像头的依赖，完全符合现有手术室的硬件配置。
*   **端到端架构**：避免了显式 3D 重建带来的误差累积，同时保留了 3D 几何的解释性。
*   **高质量数据集**：填补了手术领域大规模 3D 标注数据的空白。

### 8. 不足与局限
*   **动态环境挑战**：虽然在离体器官上表现良好，但在具有大量出血、烟雾或剧烈形变的真实在体（In-vivo）手术环境中的鲁棒性仍需进一步验证。
*   **实时性瓶颈**：虽然 18Hz 的频率能满足基本需求，但对于极高频的精细操作，推理延迟仍有优化空间。
*   **任务覆盖**：目前仅验证了抓取、打结和简单切割，尚未涉及缝合（Suturing）等更长程、更高难度的手术步骤。

（完）

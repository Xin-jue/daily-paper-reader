---
title: "Self-Correcting VLA: Online Action Refinement via Sparse World Imagination"
title_zh: 自纠错 VLA：通过稀疏世界想象实现的在线动作细化
authors: "Chenyv Liu, Wentao Tan, Lei Zhu, Fengling Li, Jingjing Li, Guoli Yang, Heng Tao Shen"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21633v1"
tags: ["query:q12"]
score: 10.0
evidence: 具有稀疏世界想象能力的自纠正 VLA 模型用于动作优化
tldr: 针对视觉-语言-动作（VLA）模型在物理动力学理解和自我改进机制上的不足，本文提出SC-VLA。该方法通过引入稀疏世界想象，利用辅助预测头预测任务进度和未来趋势来约束策略，并结合在线动作细化模块根据预测状态调整轨迹。实验证明，SC-VLA在模拟和真实机器人任务中均达到SOTA，显著提升了成功率并减少了执行步数，实现了高效的自我改进。
selection_source: fresh_fetch
motivation: 现有的VLA模型过度依赖统计先验而缺乏对物理动力学的理解，且现有的世界模型范式缺乏显式的自我改进机制。
method: 提出SC-VLA框架，通过辅助预测头实现稀疏世界想象，并利用在线动作细化模块根据预测的未来状态重塑奖励以调整动作轨迹。
result: "在机器人操作任务中，SC-VLA比最优基准模型成功率高出9%，执行步数减少16%，且在真实世界实验中获得14%的性能提升。"
conclusion: SC-VLA通过内在引导的稀疏想象和在线细化机制，有效增强了VLA模型的物理感知能力和任务执行效率。
---

## 摘要
标准的视觉-语言-动作 (VLA) 模型依赖于拟合统计数据先验，这限制了它们对底层物理动力学的鲁棒理解。强化学习通过探索增强了物理落地，但通常依赖于与智能体内部状态隔离的外部奖励信号。世界动作模型已成为一种极具前景的范式，它整合了想象与控制以实现预测性规划。然而，它们依赖于隐式上下文建模，缺乏显式的自我改进机制。为了解决这些问题，我们提出了自纠错 VLA (SC-VLA)，它通过稀疏想象内在引导动作细化，从而实现自我改进。我们首先通过集成辅助预测头来设计稀疏世界想象，以预测当前任务进度和未来轨迹趋势，从而约束策略编码短期物理演化。随后，我们引入了在线动作细化模块来重塑依赖于进度的稠密奖励，并根据预测的稀疏未来状态调整轨迹方向。在模拟基准测试和真实世界设置中具有挑战性的机器人操纵任务评估表明，SC-VLA 达到了最先进的性能，与表现最佳的基准模型相比，其任务吞吐量最高，步数减少了 16%，成功率提高了 9%，在真实世界实验中也获得了 14% 的提升。代码可在 https://github.com/Kisaragi0/SC-VLA 获取。

## Abstract
Standard vision-language-action (VLA) models rely on fitting statistical data priors, limiting their robust understanding of underlying physical dynamics. Reinforcement learning enhances physical grounding through exploration yet typically relies on external reward signals that remain isolated from the agent's internal states. World action models have emerged as a promising paradigm that integrates imagination and control to enable predictive planning. However, they rely on implicit context modeling, lacking explicit mechanisms for self-improvement. To solve these problems, we propose Self-Correcting VLA (SC-VLA), which achieve self-improvement by intrinsically guiding action refinement through sparse imagination. We first design sparse world imagination by integrating auxiliary predictive heads to forecast current task progress and future trajectory trends, thereby constraining the policy to encode short-term physical evolution. Then we introduce the online action refinement module to reshape progress-dependent dense rewards, adjusting trajectory orientation based on the predicted sparse future states. Evaluations on challenging robot manipulation tasks from simulation benchmarks and real-world settings demonstrate that SC-VLA achieve state-of-the-art performance, yielding the highest task throughput with 16% fewer steps and a 9% higher success rate than the best-performing baselines, alongside a 14% gain in real-world experiments. Code is available at https://github.com/Kisaragi0/SC-VLA.

---

## 论文详细总结（自动生成）

这是一份关于论文《Self-Correcting VLA: Online Action Refinement via Sparse World Imagination》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型主要面临三大挑战：
*   **统计先验依赖**：模型倾向于模仿训练数据的统计模式，而非真正理解物理动力学。
*   **外部奖励脱节**：强化学习（RL）微调通常依赖外部定义的奖励信号，这些信号往往难以设计且与智能体的内部状态隔离。
*   **缺乏自我改进机制**：现有的世界模型虽能预测未来，但缺乏显式机制利用预测结果来实时纠正和优化动作。

本文提出 **SC-VLA** 框架，旨在通过“稀疏世界想象”产生内生奖励，引导模型进行在线动作细化，实现物理感知的自我改进。

### 2. 方法论
SC-VLA 采用两阶段架构，结合了离线动作生成与在线残差细化：

*   **核心思想**：利用模型内部生成的“未来想象”（任务进度和状态变化）作为约束和奖励来源，通过残差强化学习（Residual RL）对基础策略进行微调。
*   **关键技术细节**：
    1.  **稀疏世界想象 (SPI)**：在基于 DiT 的 VLA 模型中间层加入辅助预测头，预测当前任务进度 $p_t$ 和短期物理状态增量 $\Delta s_t$（包括末端执行器的位置、旋转和夹爪状态）。
    2.  **在线动作细化 (OAR)**：
        *   **残差策略**：最终动作 $a_t = a_{base} + \lambda a_{res}$。其中 $a_{base}$ 来自冻结的基础模型，$a_{res}$ 由轻量级 MLP（通过 SAC 算法训练）生成。
        *   **内生奖励函数**：构建引导奖励 $r_{guide}$，衡量实际位移方向与预测位移方向的一致性。
        *   **动态权重调度**：最终奖励 $r_{final} = \eta(\hat{p}_t) \cdot w_{guide} \cdot r_{guide} + r_{env} - c$。随着任务进度 $\hat{p}_t$ 增加，逐渐减小预测引导的权重，让位给真实的物理反馈。

### 3. 实验设计
*   **场景与数据集**：
    *   **仿真环境**：使用 ManiSkill3 平台，涵盖四个高精度接触任务：`StackCube`（叠方块）、`PlaceSphere`（放球）、`LiftPegUpright`（扶正销钉）、`PegInsertion`（插销钉）。
    *   **真实世界**：使用 ARX5 机械臂执行叠方块、放球、插销钉和推方块任务。
*   **Benchmark 与对比方法**：
    *   **对比模型**：Diffusion Policy (DP)、ACT、$\pi_0$（物理智能基础模型）、GR00T N1.5（本文的基础模型版本）。
    *   **评估指标**：任务成功率、任务吞吐量（完成任务所需的平均步数）。

### 4. 资源与算力
论文明确列出了实验所用的硬件资源：
*   **基础模型训练**：使用 1 张 NVIDIA L40 GPU，训练 50,000 次迭代。
*   **基准模型训练**：DP 和 ACT 使用 1 张 NVIDIA RTX 5090；$\pi_0$ 使用 1 张 NVIDIA RTX PRO 6000。
*   **RL 细化阶段**：针对不同任务，训练步数从 50 万步（简单任务）到 300 万步（复杂插拔任务）不等。

### 5. 实验数量与充分性
*   **实验规模**：每个仿真任务进行 50 次独立评估，真机任务进行 20 次试验。
*   **消融实验**：非常充分。分别验证了“状态预测”、“进度预测”、“想象奖励”以及“动态权重”对性能的影响。
*   **公平性**：所有对比方法均使用相同的 100 条演示数据进行训练，评估协议一致，确保了对比的客观性。

### 6. 主要结论与发现
*   **性能领先**：SC-VLA 在所有任务中均达到 SOTA。在最难的插销钉任务中，成功率比基础模型提升了 16%。
*   **高效执行**：SC-VLA 的执行效率最高，平均步数比 $\pi_0$ 减少了 43%，比 DP 减少了 8%，证明了“纠错机制”能显著减少无效动作。
*   **内生奖励有效性**：实验证明，即使没有复杂的外部稠密奖励，仅靠模型自身的“想象”也能引导 RL 跨越稀疏奖励的冷启动难题。

### 7. 优点（亮点）
*   **内生性**：奖励信号源于模型内部对物理演化的预测，减少了对人工设计奖励函数的依赖。
*   **稀疏性优势**：相比于预测高维像素流的世界模型，预测稀疏的状态增量计算开销更小，更易于在线部署。
*   **稳定性**：通过残差结构和动态权重，既保留了预训练模型的大规模先验知识，又赋予了其处理分布外（OOD）扰动的灵活性。

### 8. 不足与局限
*   **参数敏感性**：残差缩放因子 $\lambda$ 和 RL 训练参数在不同任务间存在差异（如表 7 所示），这意味着针对新任务可能仍需要一定的超参数调优。
*   **预测精度依赖**：如果基础模型的“想象”能力极差（预测完全错误），可能会对残差学习产生负面引导，尽管动态权重缓解了这一问题。
*   **应用限制**：目前主要集中在机械臂操纵任务，尚未在更复杂的移动操纵或多机器人协作场景中验证。

（完）

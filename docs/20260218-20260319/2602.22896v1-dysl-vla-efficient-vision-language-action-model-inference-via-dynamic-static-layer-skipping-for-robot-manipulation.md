---
title: "DySL-VLA: Efficient Vision-Language-Action Model Inference via Dynamic-Static Layer-Skipping for Robot Manipulation"
title_zh: DySL-VLA：通过动静态层跳过实现机器人操纵的高效视觉-语言-动作模型推理
authors: "Zebin Yang, Yijiahao Qi, Tong Xie, Bo Yu, Shaoshan Liu, Meng Li"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22896v1"
tags: ["query:q12"]
score: 10.0
evidence: 通过层跳减实现高效的VLA操作推理
tldr: 针对视觉-语言-动作（VLA）模型在机器人任务中计算开销大的问题，本文提出DySL-VLA框架。该方法基于动作重要性差异，将模型层分为核心层和可跳过层，并引入先验-后验跳层指导机制。通过两阶段知识蒸馏训练，在保持精度的同时显著提升了推理速度并减少了参数量，为实时机器人操作提供了高效解决方案。
selection_source: fresh_fetch
motivation: VLA模型计算成本高，难以满足机器人实时操作的需求，且不同动作步骤对精度的要求存在差异。
method: 提出动态-静态跳层框架，将层分为信息层和增量层，并利用先验-后验指导机制及两阶段知识蒸馏进行优化。
result: "在Calvin数据集上，相比Deer-VLA成功长度提升2.1%，可训练参数减少85.7倍，且在同等精度下比RoboFlamingo快3.75倍。"
conclusion: DySL-VLA通过动态跳层策略有效平衡了VLA模型的推理效率与任务性能，是实现高效机器人操控的有力工具。
---

## 摘要
视觉-语言-动作（VLA）模型通过融合语言模型的推理能力和视觉模型的 3D 理解能力，在机器人操纵等任务中取得了显著成功。然而，其高昂的计算成本仍然是需要实时性能的实际应用中的主要障碍。我们观察到，任务中的动作具有不同程度的重要性：关键步骤需要高精度，而次要步骤可以容忍更大的方差。利用这一见解，我们提出了 DySL-VLA，这是一个通过根据每个动作的重要性动态跳过 VLA 层来解决计算成本的新型框架。DySL-VLA 将其层分为两类：始终执行的信息层（informative layers）和可以有选择地跳过的增量层（incremental layers）。为了在不牺牲准确性的情况下智能地跳过层，我们发明了一种先验-后验跳过引导机制，用于确定何时启动层跳过。我们还提出了一种跳过感知的两阶段知识蒸馏算法，以高效地将标准 VLA 训练为 DySL-VLA。我们的实验表明，在 Calvin 数据集上，DySL-VLA 的成功长度比 Deer-VLA 提高了 2.1%，同时可训练参数减少了 85.7 倍，并且在等精度下相对于 RoboFlamingo 基准实现了 3.75 倍的加速。我们的代码可在 https://github.com/PKU-SEC-Lab/DYSL_VLA 获取。

## Abstract
Vision-Language-Action (VLA) models have shown remarkable success in robotic tasks like manipulation by fusing a language model's reasoning with a vision model's 3D understanding. However, their high computational cost remains a major obstacle for real-world applications that require real-time performance. We observe that the actions within a task have varying levels of importance: critical steps demand high precision, while less important ones can tolerate more variance. Leveraging this insight, we propose DySL-VLA, a novel framework that addresses computational cost by dynamically skipping VLA layers based on each action's importance. DySL-VLA categorizes its layers into two types: informative layers, which are consistently executed, and incremental layers, which can be selectively skipped. To intelligently skip layers without sacrificing accuracy, we invent a prior-post skipping guidance mechanism to determine when to initiate layer-skipping. We also propose a skip-aware two-stage knowledge distillation algorithm to efficiently train a standard VLA into a DySL-VLA. Our experiments indicate that DySL-VLA achieves 2.1% improvement in success length over Deer-VLA on the Calvin dataset, while simultaneously reducing trainable parameters by a factor of 85.7 and providing a 3.75x speedup relative to the RoboFlamingo baseline at iso-accuracy. Our code is available on https://github.com/PKU-SEC-Lab/DYSL_VLA.

---

## 论文详细总结（自动生成）

### DySL-VLA：通过动静态层跳过实现机器人操纵的高效推理总结

#### 1. 核心问题与整体含义
*   **研究动机**：视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然在机器人操纵中表现出色，但其巨大的计算开销导致推理延迟高（仅 1-5 Hz），难以满足机器人实时物理交互（通常需 20-50 Hz）的需求。
*   **核心观察**：论文指出，机器人任务中的不同动作重要性不同（如“抓取”比“接近”更关键），且 VLA 模型内部不同层对预测的贡献也不均等。
*   **研究目标**：在不显著损失精度的前提下，通过动态跳过不必要的模型层来加速 VLA 推理。

#### 2. 方法论
DySL-VLA 框架包含三个核心组件：
*   **动态-静态层跳过（Dynamic-Static Layer-Skipping）**：
    *   **静态层（信息层）**：通过余弦相似度分析，识别出显著改变激活分布的关键层，这些层在推理中始终保留。
    *   **动态层（增量层）**：信息量较少的层，可根据需要跳过。
    *   **适配器（Adapters）**：在跳过动态层时，使用轻量级适配器来总结被跳过层的信息并拟合激活值。
*   **先验-后验跳过指导（Prior-Post Skipping Guidance）**：
    *   **先验预测**：基于轨迹连续性（$C_t$）动态移动“允许跳过点”。当检测到动作不连续（暗示进入关键步骤）时，强制保留更多层。
    *   **后验验证**：若跳过层后的预测导致连续性骤降，则重新进行全量推理以确保关键动作的准确性。
*   **跳过感知的两阶段知识蒸馏**：
    *   **第一阶段**：仅训练适配器，使其具备总结动态层信息的能力。
    *   **第二阶段**：联合训练跳过控制器（决定是否跳过）和适配器，引入任务损失和跳过惩罚项，优化推理效率。

#### 3. 实验设计
*   **数据集与场景**：
    *   **CALVIN**：用于评估长程操纵任务的成功长度（0-5）。
    *   **LIBERO**：评估在不同子任务（空间、物体、目标等）下的成功率（SR）。
*   **基准模型（Backbones）**：RoboFlamingo (3B/9B) 和 OpenVLA-oft (7B)。
*   **对比方法**：包括原始模型、DeeR-VLA（早停机制）、FlexiDepth（自适应跳层）、以及传统的 HULC 和 SPIL 等方法。

#### 4. 资源与算力
*   **硬件平台**：训练使用 RTX 4090（Calvin）和 A6000（LIBERO）；部署测试使用了嵌入式平台 **NVIDIA Jetson Orin**。
*   **训练开销**：
    *   在 Calvin 数据集上，DySL-VLA 仅需 **37 GPU 小时**（相比 DeeR-VLA 的 112 小时减少了约 67%）。
    *   训练步数减少了 13.7 倍，可训练参数量减少了 85.7 倍（仅 14M-226M 参数）。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3B 到 9B 不同参数规模的模型，并在两个主流机器人基准数据集上进行了详尽测试。
*   **消融实验**：针对先验预测、后验验证、两阶段蒸馏、静态层比例、轨迹长度 $k$ 等关键变量进行了多组消融实验。
*   **评价**：实验设计较为全面，不仅对比了算法性能，还深入分析了在边缘计算设备（Jetson Orin）上的实际推理延迟，具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升**：在 Calvin 数据集上，DySL-VLA 的成功长度比 DeeR-VLA 提升了 2.1%。
*   **显著加速**：相比 RoboFlamingo 基准，实现了 **3.75 倍** 的推理加速；在 Jetson Orin 上，配合动作分块（Action Chunking）技术，控制频率可达 **23.2 Hz**。
*   **高效训练**：通过冻结 LLM 主干仅训练适配器和控制器，极大地降低了微调成本，且避免了破坏预训练模型的泛化能力。

#### 7. 优点
*   **动静结合**：不同于盲目跳层或简单早停，该方法识别并保护了“信息层”，平衡了效率与精度。
*   **动作感知**：引入轨迹连续性作为跳层依据，捕捉到了机器人操作中“关键步骤需高计算量”的本质特征。
*   **部署友好**：不需要专门的算子优化（如量化或剪枝所需的内核），易于在标准硬件上部署。

#### 8. 不足与局限
*   **后验验证开销**：虽然“后验验证”能保证精度，但在关键动作发生时会触发二次推理，可能导致瞬时的延迟波动。
*   **静态层选择**：静态层的识别依赖于预先的离线分析（余弦相似度），对于完全不同分布的新任务，可能需要重新评估静态层的位置。
*   **应用限制**：主要针对 Transformer 架构的 LLM 主干进行优化，对于视觉编码器部分的计算开销优化较少。

（完）

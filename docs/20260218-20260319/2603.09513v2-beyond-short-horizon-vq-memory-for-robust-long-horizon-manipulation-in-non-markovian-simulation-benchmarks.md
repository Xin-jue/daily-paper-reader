---
title: "Beyond Short-Horizon: VQ-Memory for Robust Long-Horizon Manipulation in Non-Markovian Simulation Benchmarks"
title_zh: 超越短时程：用于非马尔可夫模拟基准中鲁棒长时程操纵的 VQ-Memory
authors: "Honghui Wang, Zhi Jing, Jicong Ao, Shiji Song, Xuelong Li, Gao Huang, Chenjia Bai"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09513v2"
tags: ["query:q14"]
score: 9.0
evidence: 用于关节操作基准测试的LLM辅助仿真框架
tldr: 针对现有机器人仿真基准任务过于简单且缺乏非马尔可夫特性的问题，本文提出了RuleSafe基准，包含多种复杂解锁机制的长程任务。同时，提出VQ-Memory方法，通过VQ-VAE将历史本体感知状态编码为离散潜在令牌，为视觉-语言-动作（VLA）模型提供鲁棒的时间线索。实验证明该方法显著提升了长程规划能力、泛化性及操作效率。
selection_source: fresh_fetch
motivation: 现有机器人仿真基准多为简单的短程任务，难以模拟现实世界中复杂的非马尔可夫特性和长程操作需求。
method: 提出了RuleSafe基准和VQ-Memory表示法，后者利用向量量化变分自编码器将过去状态压缩为离散令牌以提供紧凑的时间记忆。
result: 在多种SOTA VLA模型和扩散策略上的实验表明，VQ-Memory增强了模型在长程任务中的规划能力、泛化性能和计算效率。
conclusion: VQ-Memory为处理非马尔可夫长程操作任务提供了一种轻量且鲁棒的记忆机制，有效提升了复杂环境下的机器人操作表现。
---

## 摘要
收集真实机器人数据的高昂成本使得机器人模拟成为评估和数据生成的扩展平台。然而，大多数现有基准集中在简单的操纵任务（如取放），未能捕捉到现实世界任务的非马尔可夫特性以及关节物体交互的复杂性。为了解决这一局限性，我们提出了 RuleSafe，这是一个基于可扩展的大语言模型（LLM）辅助模拟框架构建的新型关节操纵基准。RuleSafe 的特点是具有多种解锁机制的保险箱，如钥匙锁、密码锁和逻辑锁，这些机制需要不同的多阶段推理和操纵策略。这些由 LLM 生成的规则产生了非马尔可夫和长时程任务，需要时间建模和基于记忆的推理。我们进一步提出了 VQ-Memory，这是一种紧凑且结构化的时间表示，它使用向量量化变分自编码器（VQ-VAE）将过去的本体感受状态编码为离散的潜变量标记。这种表示在过滤低级噪声的同时保留了高级任务阶段的上下文，提供了与现有视觉-语言-动作模型（VLA）兼容的轻量级且鲁棒的时间线索。在最先进的 VLA 模型和扩散策略上进行的大量实验表明，VQ-Memory 持续改进了长时程规划，增强了对未知配置的泛化能力，并以更低的计算成本实现了更高效的操纵。项目页面：vqmemory.github.io

## Abstract
The high cost of collecting real-robot data has made robotic simulation a scalable platform for both evaluation and data generation. Yet most existing benchmarks concentrate on simple manipulation tasks such as pick-and-place, failing to capture the non-Markovian characteristics of real-world tasks and the complexity of articulated object interactions. To address this limitation, we present RuleSafe, a new articulated manipulation benchmark built upon a scalable LLM-aided simulation framework. RuleSafe features safes with diverse unlocking mechanisms, such as key locks, password locks, and logic locks, which require different multi-stage reasoning and manipulation strategies. These LLM-generated rules produce non-Markovian and long-horizon tasks that require temporal modeling and memory-based reasoning. We further propose VQ-Memory, a compact and structured temporal representation that uses vector-quantized variational autoencoders (VQ-VAEs) to encode past proprioceptive states into discrete latent tokens. This representation filters low-level noise while preserving high-level task-phase context, providing lightweight yet robust temporal cues that are compatible with existing Vision-Language-Action models (VLA). Extensive experiments on state-of-the-art VLA models and diffusion policies show that VQ-Memory consistently improves long-horizon planning, enhances generalization to unseen configurations, and enables more efficient manipulation with reduced computational cost. Project page: vqmemory.github.io

---

## 论文详细总结（自动生成）

这是一份关于论文《Beyond Short-Horizon: VQ-Memory for Robust Long-Horizon Manipulation in Non-Markovian Simulation Benchmarks》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的机器人操纵基准测试（如取放任务）大多过于简单，属于短时程（Short-horizon）任务，且通常满足马尔可夫假设（即当前观察足以决定下一步动作）。然而，现实世界中的复杂任务（如解锁保险箱）具有**非马尔可夫特性**，需要机器人具备时间记忆来推断当前的任务阶段（Task-phase）。
*   **核心挑战**：关节物体（如带锁的门、抽屉）的交互逻辑复杂，手动设计这些长程任务的规则和轨迹成本极高，且现有的视觉-语言-动作（VLA）模型在处理长程依赖时存在计算开销大或易受噪声干扰的问题。

### 2. 方法论
论文提出了两个核心贡献：**RuleSafe 基准**和 **VQ-Memory 表示法**。
*   **RuleSafe 基准**：
    *   利用 LLM 辅助生成复杂的解锁规则（钥匙锁、密码锁、逻辑锁）。
    *   引入**部件阶段（Part-Phase）**和**任务阶段（Task-Phase）**作为潜在变量，定义多步依赖关系。
    *   通过空间标注和任务分解，自动生成高质量的专家演示数据。
*   **VQ-Memory 核心思想**：
    *   **离散化编码**：使用向量量化变分自编码器（VQ-VAE）将连续的机器人本体感知状态（关节序列）编码为离散的潜在标记（Tokens）。
    *   **损失函数**：包含重构损失（Reconstruction Loss）和承诺损失（Commitment Loss），确保 Codebook 捕捉到核心运动原语。
    *   **后处理聚类**：在 VQ-VAE 训练后，对 Codebook 进行 K-means 聚类，进一步压缩冗余信息，提取高层语义模式（如“正在旋转拨盘”），从而过滤低级噪声。
    *   **模型集成**：将这些离散 Token 作为辅助输入（类似于语言 Token）喂给 VLA 模型（如 $\pi_0$、RDT 等）。

### 3. 实验设计
*   **数据集与场景**：RuleSafe 基准包含 20 种复杂的解锁规则和 10 种不同几何形状的保险箱。
*   **对比方法**：
    *   **基础模型**：DP3（基于点云的扩散策略）、RDT（扩散 Transformer）、CogACT、$\pi_0$（流匹配模型）。
    *   **对比配置**：无记忆模型、原始关节序列记忆（Raw memory）、VQ-Memory。
*   **评估指标**：成功率（SR）和过程得分（PS，衡量完成任务步骤的百分比）。

### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A100 GPU**。
*   **训练时长**：
    *   DP3：训练 200k 步（Batch size 256）。
    *   RDT/CogACT/$\pi_0$：从预训练权重微调 30k 步（Batch size 8-32）。
    *   VQ-VAE：作为预处理模块单独训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了单任务（针对特定复杂规则）和多任务（20 个任务同时训练）设置。
*   **消融实验**：详细探讨了聚类中心数量（K=2 到 256）和记忆长度（20 到 60 个 Token）对性能的影响。
*   **客观性**：实验在多种不同架构（扩散模型、Transformer、流匹配）上验证了 VQ-Memory 的有效性，证明了该方法的**模型无关性（Model-agnostic）**，对比实验设计较为公平。

### 6. 主要结论与发现
*   **性能提升**：在多任务设置下，$\pi_0$ 模型集成 VQ-Memory 后，平均成功率从 **25.0% 提升至 56.3%**，过程得分提升了 27.7%。
*   **抗噪与泛化**：相比于直接使用原始关节序列作为记忆，VQ-Memory 通过离散化和聚类有效防止了模型对特定轨迹的过拟合，增强了在未见过的保险箱配置上的泛化能力。
*   **阶段识别**：实验证明 VQ-Memory 能够帮助模型在视觉特征相似的情况下（如手靠近把手时），准确判断当前是该“旋转”还是“拉动”。

### 7. 优点
*   **创新性**：将 VQ-VAE 用于本体感知状态的压缩记忆，而非仅仅用于动作分词，为非马尔可夫任务提供了一种轻量级解决方案。
*   **基准价值**：RuleSafe 填补了关节物体长程操纵基准的空白，且 LLM 辅助生成的方式具有良好的可扩展性。
*   **效率**：相比于存储历史视频帧，离散的关节 Token 极大地降低了 VLA 模型的计算负担。

### 8. 不足与局限
*   **底层执行瓶颈**：虽然 VQ-Memory 提升了高层规划，但整体成功率（56.3%）仍受限于底层操作精度（如手指对齐、打滑等），这可能需要更多的演示数据或更高频率的控制。
*   **仿真局限**：目前所有实验均在 SAPIEN 仿真环境中完成，尚未在真实机器人上验证其 Sim-to-Real 的迁移效果。
*   **数据依赖**：VQ-VAE 的 Codebook 质量高度依赖于专家演示数据的覆盖范围。

（完）

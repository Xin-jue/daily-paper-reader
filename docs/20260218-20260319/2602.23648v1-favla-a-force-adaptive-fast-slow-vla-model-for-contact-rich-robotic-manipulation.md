---
title: "FAVLA: A Force-Adaptive Fast-Slow VLA model for Contact-Rich Robotic Manipulation"
title_zh: FAVLA：一种用于接触密集型机器人操控的力自适应快慢 VLA 模型
authors: "Yao Li, Peiyuan Tang, Wuyang Zhang, Chengyang Zhu, Yifan Duan, Weikai Shi, Xiaodong Zhang, Zijiang Yang, Jianmin Ji, Yanyong Zhang"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.23648v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于接触密集型机器人操作的VLA模型
tldr: FAVLA 针对接触密集型机器人任务，提出了一种力自适应的快慢 VLA 模型。它解决了传统模型统一采样频率导致的力反馈延迟问题，通过将慢速感知规划（VLM）与快速力觉控制（AE）解耦，并引入力适配器和自适应频率调度，实现了对力变化的快速响应，显著提升了操作成功率和反应速度。
selection_source: fresh_fetch
motivation: 现有 VLA 模型通常采用统一采样频率，无法有效利用高频力反馈信号，导致在接触密集型任务中对力突变的响应存在延迟。
method: 提出快慢解耦架构，由低频 VLM 进行感知规划并预测力变化，由高频 AE 结合实时力序列生成动作，并利用力适配器和自适应频率调度优化控制。
result: 实验表明，FAVLA 在接触密集型任务中显著优于基线模型，具有更强的反应能力和更高的成功率，且能以更小的接触力完成任务。
conclusion: 通过解耦感知与控制并引入自适应高频力反馈，FAVLA 有效提升了机器人在复杂接触环境下的操作性能。
---

## 摘要
力/力矩反馈可以显著提升视觉-语言-动作（VLA）模型在接触密集型操控任务中的表现，但现有的大多数方法都在单一运行频率下融合所有模态。这种设计忽略了真实机器人传感器采样率不匹配的问题，迫使对反应式校正所需的高频接触线索进行降采样。结合常见的 VLM-动作专家（AE）流水线（该流水线在昂贵的 VLM 更新之间主要以开环方式执行动作块），统一频率融合通常会导致对冲击、粘滑现象和力峰值的响应延迟。我们提出了 FAVLA，一种力自适应的快慢 VLA 模型，它将慢速感知规划与快速接触感知控制解耦。FAVLA 以固定的低频运行慢速 VLM，以编码模态产生潜在表示并预测近期的力变化。随后，快速 AE 以可变的高频执行，根据最新的力序列数据生成反应式动作。我们还引入了一个力适配器，将高频力特征注入多个 AE 层，并根据 VLM 预测的力变化自适应地调度 AE 的执行频率。在接触密集型任务上的大量实验表明，FAVLA 显著优于基线模型，实现了卓越的反应能力和成功率，尤其是在操控过程中接触力较小的情况下。

## Abstract
Force/torque feedback can substantially improve Vision-Language-Action (VLA) models on contact-rich manipulation, but most existing approaches fuse all modalities at a single operating frequency. This design ignores the mismatched sampling rates of real robot sensors, forcing downsampling of the high-frequency contact cues needed for reactive correction. Combined with common VLM-action-expert (AE) pipelines that execute action chunks largely open loop between expensive VLM updates, unified-frequency fusion often yields delayed responses to impacts, stick-slip, and force spikes. We propose FAVLA, a force-adaptive fast-slow VLA that decouples slow perception planning from fast contact-aware control. FAVLA runs a slow VLM at a fixed low frequency to encode modalities to produce latent representations and to predict near-future force variation. A fast AE then executes at a variable high frequency, conditioning on the latest force sequence data to generate reactive actions. We further introduce a force adapter that injects high-frequency force features into multiple AE layers, and adaptively schedules the AE's execution frequency based on the VLM's predicted force variation. Extensive experiments on contact-rich tasks demonstrate that FAVLA significantly outperforms baselines, achieving superior reactivity and success rates, especially with a smaller contact force during manipulation.

---

## 论文详细总结（自动生成）

以下是对论文《FAVLA: A Force-Adaptive Fast-Slow VLA model for Contact-Rich Robotic Manipulation》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在处理“接触密集型”（Contact-Rich）任务（如精密组装、插拔）时表现不佳。
*   **研究动机**：
    *   **频率不匹配**：视觉传感器通常为低频（15-30Hz），而力/力矩传感器为高频（100-1000Hz）。现有模型通常将所有模态降采样至统一低频，导致丢失了应对冲击、粘滑和力突变的关键高频信号。
    *   **开环执行风险**：主流 VLM-动作专家（AE）架构在昂贵的 VLM 更新间隔内通常以开环方式执行动作块，无法对执行过程中的力变化做出即时反应，容易导致操作力过大或损坏工件。

### 2. 方法论
FAVLA 提出了一种**力自适应的快慢解耦架构**，核心思想是将“慢速语义推理”与“快速反应控制”分离：
*   **慢速 VLM 主干（Slow VLM Backbone）**：
    *   以固定的低频运行，处理视觉、语言和**历史力序列**。
    *   生成视觉-语言-力（VLF）上下文表示（KV Cache），供快速模块复用。
    *   **力方差预测头**：预测未来短时间内的力波动情况，作为调整执行频率的依据。
*   **快速动作专家（Fast Action Expert, AE）**：
    *   以可变的高频运行，基于最新的机器人状态和**实时力序列**生成动作。
    *   **力适配器（Force Adapter）**：通过交叉注意力机制，将高频力特征直接注入 AE 的多个 Transformer 层，而非简单的 Token 拼接，增强了模型对力信号的敏感度。
*   **力自适应推理策略**：
    *   根据 VLM 预测的力方差动态调整 AE 的执行频率。在自由空间运动时降低频率以节省算力，在即将接触或接触过程中提高频率以实现精密控制。
    *   使用**一致性动作集成**（固定噪声采样 + 时间集成）来确保高频执行下的动作平滑性。

### 3. 实验设计
*   **实验场景**：在真实机器人上设置了四个具有挑战性的接触密集型任务：
    1.  **USB 插入**：毫米级精度要求。
    2.  **齿轮组装**：需通过力反馈感知不可见的轴孔对齐。
    3.  **纸箱翻转**：动态接触，需防止滑落或压坏纸箱。
    4.  **白板擦拭**：需维持稳定的接触力。
*   **对比基线（Benchmarks）**：
    *   **$\pi_0$**：原始视觉-语言流匹配模型。
    *   **$\pi_0$ + Force**：简单拼接力 Token 的版本。
    *   **TA-VLA**：引入扭矩感知辅助任务的模型。
    *   **ForceVLA**：使用混合专家（MoE）架构处理力信息的模型。
*   **评估指标**：任务成功率、平均峰值接触力（衡量安全性/柔顺性）。

### 4. 资源与算力
*   **硬件平台**：Monte 机器人（双臂 7-DoF X-ARM），配备 6 轴力/力矩传感器。
*   **算力消耗**：
    *   使用单张 **NVIDIA A100 (80GB)** GPU 进行训练。
    *   每个任务的特定模型训练时长约为 **6 小时**。
    *   训练迭代次数为 30,000 次，Batch Size 为 8。
*   **数据规模**：共收集 260 条演示轨迹（约 1.84 小时数据），涵盖 4 个任务。

### 5. 实验数量与充分性
*   **实验规模**：每个任务进行了 10 到 30 次不等的独立测试（总计约 85 次真实机器人测试）。
*   **消融实验**：
    *   验证了力注入 AE、力方差预测和自适应频率策略的各自贡献。
    *   对比了固定频率（n=1, 2, 4）与自适应频率的效果。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还深入分析了接触力大小和频率预测的可视化，证明了方法的有效性。但在跨场景泛化（如完全未见过的任务类型）方面的实验相对较少。

### 6. 主要结论与发现
*   **性能领先**：FAVLA 实现了 **80.8%** 的平均成功率，比最强基线（ForceVLA）高出 **13.8%**，比纯视觉基线高出 **38%**。
*   **更安全的操作**：在齿轮组装等任务中，FAVLA 的峰值接触力显著低于基线模型（例如从 12.0N 降至 7.7N），证明了其具备更好的柔顺控制能力。
*   **自适应频率的优越性**：动态调整频率比固定高频效果更好，因为它在非接触阶段减少了不必要的动作块切换，保证了轨迹的时间连续性。

### 7. 优点
*   **架构创新**：巧妙地解决了 VLA 模型中传感器采样率不匹配的痛点，通过快慢解耦实现了真正的闭环力反馈控制。
*   **实用性强**：力自适应策略在保证精度的同时兼顾了计算效率，对工业精密组装具有很强的参考价值。
*   **鲁棒性**：可视化实验表明，模型能准确预测接触发生的时机并提前提高采样频率。

### 8. 不足与局限
*   **数据依赖**：虽然使用了预训练权重，但仍需针对每个任务进行微调（Fine-tuning），尚未实现完全通用的零样本（Zero-shot）接触控制。
*   **语义误判风险**：在失败案例分析中提到，模型有时会将“未对齐产生的阻力”误判为“插入完成的阻力”，导致过早松开夹持器，说明力信号的语义理解仍有提升空间。
*   **任务复杂度限制**：目前的任务仍属于相对短程的操作，对于需要极长时间跨度且伴随复杂力变化的连续任务，其稳定性有待验证。

（完）

---
title: "Notes-to-Self: Scratchpad Augmented VLAs for Memory Dependent Manipulation Tasks"
title_zh: Notes-to-Self：用于记忆依赖型操纵任务的草稿本增强型 VLA
authors: "Sanjay Haresh, Daniel Dijkman, Apratim Bhattacharyya, Roland Memisevic"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21013v1"
tags: ["query:q10"]
score: 10.0
evidence: 增强草稿本的VLA用于操作任务
tldr: 针对现有视觉-语言-动作（VLA）模型在处理非马尔可夫长程任务时缺乏记忆能力的问题，本文提出了“Notes-to-Self”方法。通过引入语言草稿本（scratchpad），赋予模型空间和时间记忆，使其能够记录物体位置并追踪任务进度。实验证明，该方法在ClevrSkills、MemoryBench及真实场景任务中显著提升了模型的泛化性能和长程操作能力。
selection_source: fresh_fetch
motivation: 现有的VLA模型大多是“无状态”的，难以应对需要记忆物体位置或任务进度的非马尔可夫长程操作任务。
method: 引入语言草稿本机制，让模型在输出动作前先生成描述任务状态、物体位置和子目标的文本，从而实现空间与时间记忆。
result: 在ClevrSkills、MemoryBench和真实世界拾取任务中，该方法显著提高了非循环和循环模型在记忆依赖型任务上的表现。
conclusion: 通过语言草稿本增强VLA模型的记忆能力，是解决复杂、长程机器人操控任务的一种有效且具有泛化性的方案。
---

## 摘要
许多灵巧操纵任务本质上是非马尔可夫的，然而在近期兴起的视觉-语言-动作（VLA）范式中，这一事实却鲜少受到关注。尽管现有的 VLA 成功地将互联网规模的语义理解引入了机器人领域，但它们主要是“无状态”的，在处理依赖记忆的长程任务时面临挑战。在这项工作中，我们探索了一种通过引入语言草稿本（language scratchpad）来赋予 VLA 空间和时间记忆的方法。草稿本使得记忆特定任务信息（如物体位置）成为可能，并允许模型跟踪计划以及向该计划中的子目标推进。我们在 ClevrSkills 环境中的一部分记忆依赖型任务、MemoryBench 以及一个具有挑战性的真实世界拾取与放置任务上评估了该方法。结果表明，引入语言草稿本显著提高了非循环和循环模型在这些任务上的泛化能力。

## Abstract
Many dexterous manipulation tasks are non-markovian in nature, yet little attention has been paid to this fact in the recent upsurge of the vision-language-action (VLA) paradigm. Although they are successful in bringing internet-scale semantic understanding to robotics, existing VLAs are primarily "stateless" and struggle with memory-dependent long horizon tasks. In this work, we explore a way to impart both spatial and temporal memory to a VLA by incorporating a language scratchpad. The scratchpad makes it possible to memorize task-specific information, such as object positions, and it allows the model to keep track of a plan and progress towards subgoals within that plan. We evaluate this approach on a split of memory-dependent tasks from the ClevrSkills environment, on MemoryBench, as well as on a challenging real-world pick-and-place task. We show that incorporating a language scratchpad significantly improves generalization on these tasks for both non-recurrent and recurrent models.

---

## 论文详细总结（自动生成）

以下是对论文《Notes-to-Self: Scratchpad Augmented VLAs for Memory Dependent Manipulation Tasks》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然具备强大的语义理解能力，但大多是“无状态”的（Stateless），即仅根据当前图像和指令预测动作。
*   **核心问题**：这种马尔可夫假设（当前状态足以决定下一动作）在处理需要记忆的长程任务时会失效。例如，“交换两个物体”需要记住物体的初始位置（空间记忆）以及当前进行到了哪一步（时间记忆）。
*   **整体含义**：本文旨在通过引入“语言草稿本”（Language Scratchpad）机制，赋予 VLA 模型显式的空间和时间记忆能力，使其能够处理非马尔可夫性质的复杂操纵任务。

### 2. 论文提出的方法论
*   **核心思想**：利用大模型（VLM）天生的语言生成能力，在执行动作前先生成一段“想法”（Thought），并将其存储在外部草稿本中。这个草稿本会作为后续所有步骤的输入上下文，形成一个不断演化的语义记忆痕迹。
*   **关键技术细节**：
    *   **草稿本结构**：包含三个部分：**Grounding**（记录初始物体和机械臂位置，提供空间记忆）、**Plan**（分解子任务步骤）和 **Act**（追踪已完成的子任务，提供时间记忆）。
    *   **更新逻辑**：模型在完成一个子任务时会预测一个特殊的 `<done>` 标记，触发草稿本的更新，将新的描述加入上下文。
    *   **模型适配**：该方法同时应用于**非循环模型（T-VLA）**（基于 PaliGemma-2）和**循环模型（R-VLA）**（基于 Mamba）。对于 R-VLA，草稿本与动作序列交织在一起进行自回归训练。

### 3. 实验设计
*   **数据集与场景**：
    1.  **ClevrSkills-Mem**：作者基于 ClevrSkills 提出的新基准，包含 5 个记忆依赖型任务（如“触摸-重置-抓取”、“交换物体位置”等）。
    2.  **MemoryBench**：现有的机器人记忆基准，重点测试“放回方块”任务。
    3.  **真实世界任务**：使用 UFACTORY xArm 6 机械臂执行“抓取-放置-还原”任务。
*   **对比方法**：
    *   **Baseline**：不带草稿本的 T-VLA（Transformer 架构）和 R-VLA（循环架构）。
    *   **SOTA 对比**：在 MemoryBench 上对比了 SAM2Act、RVT-2 等专门针对记忆设计的模型。

### 4. 资源与算力
*   **算力设备**：实验使用了 **4 台 Nvidia A100 GPU**。
*   **训练细节**：
    *   **T-VLA**：基于 PaliGemma-2 (3B)，进行全参数微调，学习率为 $2 \times 10^{-5}$。
    *   **R-VLA**：基于 Mamba (130M)，配合 ViT 图像骨干网络。
    *   **真实世界**：对 OpenVLA (7B) 进行 LoRA 微调（Rank=32）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ClevrSkills-Mem 的 5 个任务上各进行了 50 次 Rollout 测试。
    *   在 MemoryBench 上使用了 100 条轨迹训练，25 条测试。
    *   真实世界任务收集了 200 条演示轨迹，进行了 20 次随机位置的实机测试。
*   **充分性评价**：实验覆盖了仿真与现实、非循环与循环架构，并针对空间和时间记忆分别设计了任务，实验设计较为客观、公平，能够有力支撑其结论。

### 6. 论文的主要结论与发现
*   **显著提升**：引入草稿本后，非循环模型（T-VLA）在 ClevrSkills 任务上的平均成功率提升了约 **48%**；循环模型（R-VLA）也获得了约 **11%** 的提升。
*   **超越固有记忆**：即使是自带隐式记忆的循环模型（R-VLA），在长程任务中仍会遗忘初始状态，而显式的语言草稿本能有效弥补这一缺陷。
*   **泛化能力**：在真实世界中，草稿本增强的模型成功率达到 65%，而原始 VLA 模型完全无法完成任务（0%），证明了该方法在处理时间歧义任务时的有效性。

### 7. 优点
*   **简单且通用**：不需要修改模型底层架构，只需改变输入输出格式，即可适配各种主流 VLA。
*   **语义可解释性**：草稿本内容是自然语言，人类可以直接观察模型的“思考过程”和计划进度。
*   **灵活的记忆表征**：语言可以同时编码空间坐标（如物体位置）和抽象逻辑（如任务进度）。

### 8. 不足与局限
*   **低级时间记忆失效**：在需要极精细、连续时间追踪的任务（如“旋转物体并还原”）中，语言草稿本提升有限，因为这类任务难以用离散的语言标签描述。
*   **推理开销**：由于需要生成额外的文本 Token，推理时的延迟会有所增加。
*   **数据依赖**：草稿本的训练需要带有子任务标注和物体位置信息的轨迹数据，这在某些大规模无标注数据集中难以获得。

（完）

---
title: "NS-VLA: Towards Neuro-Symbolic Vision-Language-Action Models"
title_zh: NS-VLA：迈向神经符号视觉-语言-动作模型
authors: "Ziyue Zhu, Shangyang Wu, Shuai Zhao, Zhiqiu Zhao, Shengjie Li, Yi Wang, Fang Li, Haoran Luo"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09542v1"
tags: ["query:q12"]
score: 10.0
evidence: 用于机器人操作的神经符号 VLA 架构
tldr: 针对视觉-语言-动作（VLA）模型在可重用原语学习、数据依赖及探索能力方面的局限，本文提出NS-VLA框架。该框架结合神经符号学习与在线强化学习，通过符号编码器提取结构化原语，利用符号求解器进行高效动作序列化，并借助强化学习优化生成。实验证明，NS-VLA在单样本训练、零样本泛化及数据效率方面均优于现有方法，显著提升了机器人操作的灵活性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的VLA模型难以学习可重用的动作原语，且过度依赖大规模数据，限制了其在演示之外的探索能力。
method: 提出一种神经符号框架，利用符号编码器提取结构化特征，结合符号求解器进行动作规划，并通过在线强化学习优化策略。
result: 实验表明NS-VLA在单样本学习、抗干扰能力和零样本泛化性上均优于此前方法，并具有极高的数据效率。
conclusion: NS-VLA通过融合神经符号推理与强化学习，为实现高效率、强泛化能力的机器人视觉-语言-动作建模提供了新方案。
---

## 摘要
视觉-语言-动作（VLA）模型旨在将指令落地于视觉语境中，并为机器人操纵生成动作序列。尽管近期取得了进展，但 VLA 模型在学习相关且可重用的原语、减少对大规模数据和复杂架构的依赖，以及实现超越演示的探索方面仍面临挑战。为了应对这些挑战，我们提出了一种通过在线强化学习（RL）实现的创新神经符号视觉-语言-动作（NS-VLA）框架。该框架引入了一个符号编码器来嵌入视觉和语言特征并提取结构化原语，利用符号求解器进行高效的数据动作排序，并利用在线强化学习通过广泛探索来优化生成。在机器人操纵基准测试上的实验表明，NS-VLA 在单样本训练和数据扰动设置下均优于以往的方法，同时展现出卓越的零样本泛化能力、高数据效率和扩展的探索空间。我们的代码已开源。

## Abstract
Vision-Language-Action (VLA) models are formulated to ground instructions in visual context and generate action sequences for robotic manipulation. Despite recent progress, VLA models still face challenges in learning related and reusable primitives, reducing reliance on large-scale data and complex architectures, and enabling exploration beyond demonstrations. To address these challenges, we propose a novel Neuro-Symbolic Vision-Language-Action (NS-VLA) framework via online reinforcement learning (RL). It introduces a symbolic encoder to embedding vision and language features and extract structured primitives, utilizes a symbolic solver for data-efficient action sequencing, and leverages online RL to optimize generation via expansive exploration. Experiments on robotic manipulation benchmarks demonstrate that NS-VLA outperforms previous methods in both one-shot training and data-perturbed settings, while simultaneously exhibiting superior zero-shot generalizability, high data efficiency and expanded exploration space. Our code is available.

---

## 论文详细总结（自动生成）

### 论文总结：NS-VLA（神经符号视觉-语言-动作模型）

#### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型主要采用端到端的深度学习架构，虽然在模仿学习上取得了进展，但存在三大瓶颈：
    1.  **缺乏结构感知**：模型直接从图像/指令映射到动作，无法捕捉机器人操作中可重用的“原语”（Primitives）逻辑。
    2.  **数据依赖性强**：依赖大规模演示数据和复杂架构，在小样本（如 1-shot）下表现极差。
    3.  **探索能力受限**：主要局限于模仿专家演示，难以在环境中进行自主探索。
*   **核心目标**：提出一种**神经符号（Neuro-Symbolic）**框架，通过将连续的神经感知与离散的符号推理相结合，并引入在线强化学习，提升模型的数据效率、泛化能力和探索空间。

#### 2. 方法论
NS-VLA 框架由三个紧密耦合的部分组成：
*   **神经符号编码器（Neuro-Symbolic Encoder）**：
    *   利用预训练 VLM 提取视觉-语言特征。
    *   **计划生成**：将自然语言指令分解为结构化的符号原语序列（如 `pick(white_mug)` -> `place_on(plate)`）。
    *   **符号分类器**：预测当前正在执行的原语，并引入**单调约束（Monotone Constraint）**，确保任务进度只增不减，消除动作切换时的闪烁（Flicker）现象。
*   **符号求解器（Symbolic Solver）**：
    *   **视觉令牌稀疏化（Visual Token Sparsification）**：通过查询驱动的 Top-K 注意力机制，仅保留与当前原语相关的视觉特征，大幅降低计算冗余。
    *   **动作生成器**：使用因果 Transformer 将稀疏视觉特征、原语嵌入和本体状态映射为**动作块（Action Chunks）**，实现高效的实时控制。
*   **在线强化学习优化（Online RL）**：
    *   采用 **GRPO（组相对策略优化）** 算法。
    *   **奖励设计**：结合了任务成功奖励、原语里程碑奖励和基于潜在空间原型的进度塑造奖励（Progress Shaping）。
    *   **KL 锚定**：在强化学习过程中引入 KL 散度约束，防止策略偏离行为克隆（BC）的先验太远，确保训练稳定性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：用于评估多任务操作能力。
    *   **LIBERO-Plus**：引入光照、纹理、布局等 7 种环境扰动，测试鲁棒性。
    *   **CALVIN**：评估长程（Long-horizon）任务的连续完成能力。
*   **Benchmark 设置**：
    *   **全量数据训练**：使用全部演示数据。
    *   **1-Shot 训练**：每个任务仅提供一个演示轨迹，极具挑战性。
    *   **零样本泛化**：在 CALVIN ABC 拆分上训练，在 D 拆分上直接测试。
*   **对比方法**：包括 OpenVLA、$\pi_0$、UniVLA、VLA-Adapter、ACT、Diffusion Policy 等主流 VLA 模型。

#### 4. 资源与算力
*   **模型规模**：实验中测试了基于 Qwen3-VL 的 2B、4B 和 8B 不同参数规模的骨干网络。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到其符号求解器和视觉稀疏化设计显著提升了推理效率和训练收敛速度。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流基准数据集，进行了数百次评估迭代。
*   **消融实验**：非常充分。分别验证了符号分类器（P.C.）、视觉提取器（V.E.）、动作生成器（A.G.）和强化学习（R.L.）的必要性。
*   **客观性**：通过引入环境扰动（LIBERO-Plus）和极低数据量（1-shot）等极端设置，客观地展示了该方法在鲁棒性和效率上的优势，而非单纯堆砌参数量。

#### 6. 论文的主要结论与发现
*   **数据效率极高**：在 1-shot 设置下，NS-VLA 的成功率远超基准模型，且性能下降幅度最小。
*   **鲁棒性强**：在面对视觉噪声和空间布局变化时，由于其“原语-动作”的解耦设计，模型能更准确地锁定目标物体。
*   **泛化能力卓越**：在 CALVIN 零样本测试中，5 连任务成功率比现有 SOTA 提升了 11.2%。
*   **探索空间大**：在线 RL 使得模型生成的轨迹覆盖范围比端到端方法更广，具备更强的环境适应性。

#### 7. 优点
*   **架构创新**：成功将神经符号推理引入 VLA，解决了端到端模型“黑盒”不可解释且难以泛化的问题。
*   **计算高效**：视觉稀疏化和动作块生成技术有效降低了 VLM 处理高频控制信号的算力负担。
*   **训练稳定**：通过单调约束和 KL 锚定，解决了强化学习在机器人操作中奖励稀疏、训练易崩溃的难题。

#### 8. 不足与局限
*   **原语依赖**：目前仍依赖预定义的符号原语库，对于完全未见过的全新动作类型，可能需要手动扩展原语定义。
*   **Sim-to-Real 跨度**：实验主要在仿真环境中完成，虽然考虑了扰动，但在真实物理世界中的摩擦力、碰撞等复杂动力学表现仍待验证。
*   **标注成本**：虽然训练数据量减少了，但初始阶段可能需要对演示数据进行原语级别的自动或半自动标注。

（完）

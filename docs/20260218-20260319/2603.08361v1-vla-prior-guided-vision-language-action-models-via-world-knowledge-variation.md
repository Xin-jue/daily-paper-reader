---
title: "$Δ$VLA: Prior-Guided Vision-Language-Action Models via World Knowledge Variation"
title_zh: $Δ$VLA：基于世界知识变化的先验引导视觉-语言-动作模型
authors: "Yijie Zhu, Jie He, Rui Shao, Kaishen Yuan, Tao Tan, Xiaochen Yuan, Zitong Yu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08361v1"
tags: ["query:q10"]
score: 10.0
evidence: 用于机器人操作的先验引导 VLA 模型
tldr: 本研究针对现有视觉-语言-动作（VLA）模型仅预测绝对未来状态而忽视演变过程的问题，提出了$Δ$VLA框架。该框架通过建模相对于当前世界知识先验的变化量（Variation）来指导动作生成。核心贡献包括：利用PWKE提取关键空间与语义先验，通过LWVQ在离散潜空间中量化知识演变，以及设计CV-Atten减少表示干扰。实验证明，$Δ$VLA在模拟和真实机器人任务中均取得SOTA性能，且具备更高的效率。
selection_source: fresh_fetch
motivation: 现有模型主要预测绝对的未来视觉状态，缺乏对指导动作生成的底层世界知识演变过程的深入推理。
method: 提出一种基于先验引导的框架，通过提取世界知识先验并利用量化潜空间建模知识的变化量（Δ）来生成动作。
result: 在多个模拟基准和真实世界机器人任务中实现了最先进的性能，同时显著提高了计算效率。
conclusion: 建模世界知识的变化量而非绝对状态，是提升VLA模型感知、推理与控制集成能力的有效途径。
---

## 摘要
最近的视觉-语言-动作（VLA）模型通过统一感知、推理和控制，显著提升了机器人操作水平。为了实现这种集成，近期的研究多采用预测范式，通过建模未来的视觉状态或世界知识来引导动作生成。然而，这些模型侧重于预测结果，而非推理变化的底层过程，而后者对于确定如何行动至关重要。为此，我们提出了 $Δ$VLA，这是一个先验引导的框架，它通过建模相对于显式当前世界知识先验的世界知识变化

## Abstract
Recent vision-language-action (VLA) models have significantly advanced robotic manipulation by unifying perception, reasoning, and control. To achieve such integration, recent studies adopt a predictive paradigm that models future visual states or world knowledge to guide action generation. However, these models emphasize forecasting outcomes rather than reasoning about the underlying process of change, which is essential for determining how to act. To address this, we propose $Δ$VLA, a prior-guided framework that models world-knowledge variations relative to an explicit current-world knowledge prior for action generation, rather than regressing absolute future world states. Specifically, 1) to construct the current world knowledge prior, we propose the Prior-Guided WorldKnowledge Extractor (PWKE). It extracts manipulable regions, spatial relations, and semantic cues from the visual input, guided by auxiliary heads and prior pseudo labels, thus reducing redundancy. 2) Building upon this, to represent how world knowledge evolves under actions, we introduce the Latent World Variation Quantization (LWVQ). It learns a discrete latent space via a VQ-VAE objective to encode world knowledge variations, shifting prediction from full modalities to compact latent. 3)Moreover, to mitigate interference during variation modeling, we design the Conditional Variation Attention (CV-Atten), whichpromotes disentangled learning and preserves the independence of knowledge representations. Extensive experiments on both simulated benchmarks and real-world robotic tasks demonstrate $Δ$VLA achieves state-of-the-art performance while improving efficiency. Code and real-world execution videos are available at https://github.com/JiuTian-VL/DeltaVLA.

---

## 论文详细总结（自动生成）

这篇论文提出了 **$\Delta$VLA**，一种新型的视觉-语言-动作（VLA）模型框架，旨在通过建模“世界知识的变化量”而非“绝对未来状态”来提升机器人的操作能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的预测型 VLA 模型通常试图预测未来的图像或世界状态（如语义、几何信息）。作者认为这种“绝对状态预测”存在两个缺陷：
    1.  **缺乏因果锚点**：如果不显式建模当前状态，模型难以区分哪些部分应该改变，哪些应该保持不变，导致推理不切实际。
    2.  **连续变化量不稳定**：直接预测连续的差异值（Delta）受场景和指令影响极大，难以作为稳定的策略输入。
*   **研究背景**：动作的质量本质上是由它引起的**变化**决定的。因此，模型应该学习“世界如何随动作演变”，而不是仅仅预测“未来长什么样”。

### 2. 方法论：核心思想与关键技术
$\Delta$VLA 放弃了回归绝对未来状态的范式，转而采用**先验引导的变化建模**。其核心流程如下：
*   **PWKE（先验引导世界知识提取器）**：
    *   利用 SigLIP（语义强）和 DINOv2（几何强）提取特征。
    *   通过辅助头（Auxiliary Heads）和伪标签（来自 CoTracker、Depth-Anything v2、SAM）显式监督模型提取：**可操作区域、深度线索和语义信息**。这为模型提供了当前的“世界知识先验”。
*   **LWVQ（潜层世界变化量量化）**：
    *   采用 VQ-VAE 结构，在无监督的情况下学习一个离散的潜空间（Codebook）。
    *   它将连续的世界知识变化压缩为离散的“变化量标记（Variation Tokens）”，将复杂的模态预测简化为紧凑的潜空间推理。
*   **CV-Atten（条件变化注意力机制）**：
    *   在 LLM 主干网络中设计了结构化掩码，确保每个变化量标记仅关注其对应的先验模态（如深度变化只看深度先验），从而减少跨模态干扰，实现解耦学习。
*   **动作生成**：模型根据“当前先验 + 任务指令 + 预测的变化量标记”并行解码生成动作序列。

### 3. 实验设计
*   **模拟基准**：
    *   **LIBERO**：包含 Spatial、Object、Goal、Long 四个子集，共 40 个任务，侧重语言理解和长程操作。
    *   **RoboTwin 2.0**：包含 8 个双臂协作任务，具有高度的领域随机化。
*   **真实世界场景**：在 Galaxea R1 Lite 和 AgileX Cobot Magic 两个平台上执行了四个长程任务（抽屉操作、鞋子对齐、折叠 T 恤、水果盘点）。
*   **对比方法**：包括通用 VLA（OpenVLA, $\pi_0$）、微调变体（OpenVLA-OFT）以及预测型 VLA（CoT-VLA, Seer, DreamVLA 等）。

### 4. 资源与算力
*   **训练算力**：使用了 **8 张 NVIDIA A800 (80GB) GPU**。
*   **推理算力**：真实世界部署在单张 **RTX 5090 (32GB) GPU** 上。
*   **训练时长**：在 LIBERO 上训练约 60K 步，每 10K 步的训练成本约为 **4.9 小时**（显著低于 OpenVLA 的 11.7 小时）。

### 5. 实验数量与充分性
*   **实验规模**：模拟实验涵盖了数百次测试（LIBERO 每任务 50 次，RoboTwin 每任务 100 次）；真实世界实验每任务 25 次。
*   **消融实验**：非常详尽，涵盖了模块有效性（PWKE/LWVQ/CV-Atten）、Token 数量、伪标签噪声敏感度、变化量表示形式（连续 vs 离散）等多个维度。
*   **评价**：实验设计客观公平，通过跨平台、跨任务的对比，充分验证了模型在性能和效率上的双重优势。

### 6. 主要结论与发现
*   **性能领先**：$\Delta$VLA 在 LIBERO 平均成功率达到 **97.8%**，在 RoboTwin 2.0 达到 **80.4%**，均刷新了 SOTA 记录。
*   **效率极高**：推理延迟仅 **0.105s**，吞吐量达 **76.2 Hz**，训练成本比同类模型降低了约 60%。
*   **鲁棒性强**：即使在伪标签存在 30% 噪声的情况下，模型性能依然稳定，证明了其对感知误差的容忍度。
*   **长程任务优势**：通过建模变化量，模型能更好地处理多阶段任务（如折叠衣服），减少了误差累积。

### 7. 优点与亮点
*   **范式创新**：将“预测未来”转变为“预测变化”，为 VLA 模型提供了一个更具因果性和物理意义的推理方向。
*   **离散化接口**：LWVQ 引入的离散变化量标记，解决了连续 Delta 难以学习的问题，使策略调节更加稳定。
*   **计算高效**：通过 PWKE 过滤冗余视觉信息，大幅提升了训练和推理速度，适合实时控制。

### 8. 不足与局限
*   **依赖预训练模型**：PWKE 的效果依赖于 Depth-Anything、SAM 等离线模型生成的伪标签，虽然实验证明了其鲁棒性，但在极端未见场景下，伪标签的质量仍可能成为瓶颈。
*   **多阶段训练**：模型训练分为 PWKE 训练、LWVQ 预训练和最终 VLA 训练三个阶段，相比端到端训练，流程较为复杂。
*   **应用限制**：目前主要针对机械臂抓取和放置类任务，对于更复杂的柔性物体交互或极高精度的装配任务，其离散化表示的粒度是否足够仍需进一步验证。

（完）

---
title: "From Passive Observer to Active Critic: Reinforcement Learning Elicits Process Reasoning for Robotic Manipulation"
title_zh: 从被动观察者到主动评估者：强化学习激发机器人操控的过程推理
authors: "Yibin Liu, Yaxing Lyu, Daqi Gao, Zhixuan Liang, Weiliang Tang, Shilong Mu, Xiaokang Yang, Yao Mu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15600v1"
tags: ["query:q10"]
score: 9.0
evidence: 用于长程机器人操作任务的视频多模态大模型
tldr: 针对长程机器人操作中过程监督的挑战，本文提出 PRIMO R1 框架，通过强化学习将视频大模型从被动观察者转变为主动评价者。该方法利用基于结果的 RL 激励模型生成思维链进行进度评估，并采用结构化时空输入。实验表明，该 7B 模型在精度上显著超越 72B 模型，并在故障检测任务中刷新了 SOTA 记录。
selection_source: fresh_fetch
motivation: 现有的视频多模态大模型多采用监督微调，仅能识别事件而缺乏针对任务目标的评估能力，难以胜任长程机器人操作的监督任务。
method: 提出 PRIMO R1 框架，利用基于结果的强化学习诱导模型生成过程推理思维链，并结合初始与当前状态的结构化时空输入进行进度估计。
result: "该 7B 模型在推理误差上比基准降低了 50%，在 RoboFail 故障检测基准上达到 67.0% 的准确率，超越了 OpenAI o1 等闭源大模型。"
conclusion: 通过强化学习激发模型的过程推理能力，可以显著提升视频大模型在机器人复杂任务监控和故障检测中的表现与泛化能力。
---

## 摘要
准确的过程监督仍然是长程机器人操控面临的关键挑战。一个主要的瓶颈在于，目前主要在监督微调（SFT）范式下训练的视频多模态大语言模型（MLLM）仅作为被动的“观察者”，识别正在发生的事件，而非根据最终任务目标评估当前状态。在本文中，我们介绍了 PRIMO R1（过程推理诱导监测），这是一个 7B 规模的框架，旨在将视频 MLLM 转化为主动的“评估者”。我们利用基于结果的强化学习来激励生成显式的思维链（Chain-of-Thought），以进行进度估计。此外，我们的架构通过在初始状态和当前状态图像之间显式锚定视频序列，构建了结构化的时间输入。在所提出的 PRIMO 数据集和基准测试的支持下，在多种域内环境和域外真实世界人形机器人场景中进行的广泛实验表明，PRIMO R1 达到了最先进的性能。在定量分析上，我们的 7B 模型在专门推理基准上的平均绝对误差降低了 50%，与 72B 规模的通用 MLLM 相比，表现出显著的相对准确率提升。此外，PRIMO R1 在困难的故障检测任务上表现出强大的零样本泛化能力。我们在 RoboFail 基准测试上以 67.0% 的准确率确立了最先进的性能，超过了 OpenAI o1 等闭源模型 6.0%。

## Abstract
Accurate process supervision remains a critical challenge for long-horizon robotic manipulation. A primary bottleneck is that current video MLLMs, trained primarily under a Supervised Fine-Tuning (SFT) paradigm, function as passive "Observers" that recognize ongoing events rather than evaluating the current state relative to the final task goal. In this paper, we introduce PRIMO R1 (Process Reasoning Induced Monitoring), a 7B framework that transforms video MLLMs into active "Critics". We leverage outcome-based Reinforcement Learning to incentivize explicit Chain-of-Thought generation for progress estimation. Furthermore, our architecture constructs a structured temporal input by explicitly anchoring the video sequence between initial and current state images. Supported by the proposed PRIMO Dataset and Benchmark, extensive experiments across diverse in-domain environments and out-of-domain real-world humanoid scenarios demonstrate that PRIMO R1 achieves state-of-the-art performance. Quantitatively, our 7B model achieves a 50% reduction in the mean absolute error of specialized reasoning baselines, demonstrating significant relative accuracy improvements over 72B-scale general MLLMs. Furthermore, PRIMO R1 exhibits strong zero-shot generalization on difficult failure detection tasks. We establish state-of-the-art performance on RoboFail benchmark with 67.0% accuracy, surpassing closed-source models like OpenAI o1 by 6.0%.

---

## 论文详细总结（自动生成）

以下是对论文《From Passive Observer to Active Critic: Reinforcement Learning Elicits Process Reasoning for Robotic Manipulation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对**长程机器人操控（Long-horizon Manipulation）中的过程监督**难题展开研究。
*   **研究动机**：现有的视频多模态大模型（Video MLLMs）大多采用监督微调（SFT）范式，这使它们更像被动的“观察者”（Observer），擅长描述视频内容，但在定量评估任务进度、识别细微执行错误方面表现较差。
*   **核心挑战**：模型往往无法将连续的视觉轨迹与离散的逻辑成功条件对齐，导致它们在面对未见过的物体或失败尝试时，容易给出错误的进度评分（例如，仅仅因为动作看起来像成功就给出高分）。
*   **整体含义**：提出 **PRIMO R1** 框架，旨在将模型转变为主动的“评估者”（Critic），通过强化学习（RL）激发其生成显式的思维链（CoT）推理，从而实现高精度的任务进度估计和故障检测。

### 2. 方法论：核心思想与关键技术
核心思想是利用**基于结果的强化学习（Outcome-based RL）**来诱导模型产生结构化的过程推理。

*   **结构化时空输入**：
    *   不同于仅输入视频，该模型显式地将输入锚定为：**初始状态图像 ($I_{init}$)** + **过程视频序列 ($V_{seq}$)** + **当前状态图像 ($I_{curr}$)**。
    *   这种“边界锚定”策略为模型提供了清晰的视觉基准，将模糊的时间感知转化为结构化的状态对齐验证。
*   **基于 GRPO 的强化学习**：
    *   采用**组相对策略优化（Group Relative Policy Optimization, GRPO）**。该算法通过采样一组输出并计算相对得分来估计优势（Advantage），无需额外的价值网络（Critic Network），显著降低了显存开销。
*   **奖励函数设计**：
    *   **格式奖励 ($r_{fmt}$)**：强制模型遵循 `<think>`（包含规划、观察、推理三部分）和 `<answer>` 的结构。
    *   **准确度奖励 ($r_{acc}$)**：采用有界线性衰减函数，预测值与真实进度越接近，奖励越高。
*   **推理流程**：模型生成 CoT 时遵循“规划（分解步骤）→ 观察（识别视觉证据）→ 推理（对齐步骤与证据）”的逻辑，最后输出 0-100 的进度值。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：构建了 PRIMO Dataset，包含 116k SFT 数据和 182k RL 数据，涵盖 AgiBot（真实世界）、BEHAVIOR-1k 和 RoboTwin（高保真仿真）。
    *   **评估基准（PRIMO Bench）**：分为域内（ID）同任务测试、域外（OOD）跨任务测试，以及跨环境测试（使用 Leju KUAVO-MY 人形机器人在工厂、酒店等真实场景采集的数据）。
    *   **故障检测基准**：在 **RoboFail** 上进行零样本泛化评估。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, OpenAI o1, Gemini 2.0 Flash, Claude-3.5-Haiku。
    *   **开源通用/视频模型**：Qwen2.5-VL (7B/72B), InternVL 2.5 (8B), Cosmos-Reasoning (7B/56B), Video R1 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 (80GB) GPU**。
*   **训练配置**：
    *   **SFT 阶段**：学习率 1e-6，Batch Size 为 64，训练 1 个 Epoch。
    *   **RL 阶段**：学习率 1e-6，组采样大小 (G) 为 8，每步 Rollout 32 次，训练 1 个 Epoch。
    *   **输入规格**：训练时视频限制为 16 帧，推理时增加到 32 帧以提升精度。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个主要环境（AgiBot, Behavior, RoboTwin, Real Humanoid）上进行了详尽测试，涵盖了从原子动作到长程复合任务的多种复杂度。
*   **消融实验**：
    *   验证了**输入模态**的必要性（证明了 $I_{init} + V_{seq} + I_{curr}$ 的组合优于单一视频输入）。
    *   验证了 **SFT 与 RL 的协同效应**（证明了 RL 显著提升了 OOD 泛化能力）。
*   **充分性评价**：实验设计非常充分且具有挑战性，特别是引入了真实世界人形机器人的 OOD 测试，证明了模型不仅在仿真中有效，在非结构化的现实环境中也具备鲁棒性。

### 6. 主要结论与发现
*   **性能跨越**：7B 规模的 PRIMO R1 在进度估计精度上超越了 72B 的 Qwen2.5-VL，平均绝对误差（MAE）降低了约 50%。
*   **故障检测能力**：在 RoboFail 基准上达到 67.0% 的准确率，超过了 OpenAI o1 (61.0%)，证明了**优化连续进度推理能本质上增强离散故障检测的表征能力**。
*   **RL 的价值**：RL 不仅提升了准确率，更重要的是通过诱导 CoT 减少了模型在任务末期（80%-100% 阶段）的幻觉。
*   **Sim-to-Real**：模型在未见过的真实人形机器人场景中保持了 72.32% 的高准确率，展现了极强的迁移能力。

### 7. 优点（亮点）
*   **范式转变**：成功将视频 MLLM 从简单的描述者提升为具备逻辑一致性的评价者。
*   **结构化输入设计**：通过显式锚定初始和当前状态，解决了视频模型在长序列中空间细节稀释的问题。
*   **高效的 RL 方案**：利用 GRPO 实现了在 7B 模型上的高效推理诱导，且推理过程具有高度的可解释性（通过 Planning-Observation-Reasoning 三段式 CoT）。

### 8. 不足与局限
*   **推理延迟**：由于生成了详尽的思维链（平均约 360 个 token），推理延迟（约 0.62s）虽然优于部分推理模型，但对于需要极高频率反馈的闭环控制任务可能仍显稍慢。
*   **帧数限制**：受限于当前 MLLM 的上下文窗口和计算成本，输入视频被压缩至 16-32 帧，这可能导致在极长程任务中丢失关键的瞬时动作细节。
*   **偏差风险**：模型生成的规划步骤（Planning）依赖于其内在的常识，如果任务极其冷门或违反直觉，模型可能会生成错误的参考步骤。

（完）

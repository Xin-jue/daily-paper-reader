---
title: Mean-Flow based One-Step Vision-Language-Action
title_zh: 基于均值流的一步式视觉-语言-动作模型
authors: "Yang Chen, Xiaoguang Ma, Bin Zhao"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01469v1"
tags: ["query:q12"]
score: 9.0
evidence: 用于灵巧机器人操作的一步动作生成VLA
tldr: 针对基于FlowMatching的视觉-语言-动作（VLA）模型在机器人操作中因迭代采样导致生成延迟高的问题，本文提出了一种基于均值流（Mean-Flow）的一步生成VLA框架。该方法通过解决动作生成过程中的噪声诱导问题，消除了传统Flow-Matching的一致性约束，实现了高效的一步动作生成。实验表明，该方法在保持性能的同时，生成速度显著优于SmolVLA和扩散策略，为高效机器人操控提供了新方案。
selection_source: fresh_fetch
motivation: 现有的FlowMatching VLA模型因迭代采样过程导致生成延迟过高，限制了其在实时机器人任务中的应用。
method: 提出均值流（Mean-Flow）方法，通过解决动作生成过程中的噪声诱导问题并消除一致性约束，实现了一步式的动作生成。
result: 在真实机器人实验中，该方法的生成速度分别比SmolVLA和扩散策略快8.7倍和83.9倍。
conclusion: 该研究证明了均值流一步生成框架在提升VLA模型效率方面的巨大潜力，是高效机器人操控的理想骨干网络。
---

## 摘要
基于流匹配（FlowMatching）的视觉-语言-动作（VLA）框架的最新进展在生成高频动作块方面展现出显著优势，特别是在高灵巧性的机器人操纵任务中。尽管取得了这些显著成就，但其实际应用仍受限于较长的生成延迟，这源于其固有的迭代采样需求和架构限制。为了解决这一关键瓶颈，我们提出了一种基于均值流（Mean-Flow）的一步式 VLA 方法。具体而言，我们解决了动作生成过程中由噪声引起的问题，从而消除了传统流匹配方法固有的连贯性约束。这显著提高了生成效率，并实现了一步式动作生成。真实世界的机器人实验表明，所提出的基于均值流的一步式 VLA 的生成速度分别是 SmolVLA 和扩散策略（Diffusion Policy）的 8.7 倍和 83.9 倍。这些结果阐明了其作为基于 VLA 的机器人操纵的高效骨干网络的巨大潜力。

## Abstract
Recent advances in FlowMatching-based Vision-Language-Action (VLA) frameworks have demonstrated remarkable advantages in generating high-frequency action chunks, particularly for highly dexterous robotic manipulation tasks. Despite these notable achievements, their practical applications are constrained by prolonged generation latency, which stems from inherent iterative sampling requirements and architectural limitations. To address this critical bottleneck, we propose a Mean-Flow based One-Step VLA approach. Specifically, we resolve the noise-induced issues in the action generation process, thereby eliminating the consistency constraints inherent to conventional Flow-Matching methods. This significantly enhances generation efficiency and enables one-step action generation. Real-world robotic experiments show that the generation speed of the proposed Mean-Flow based One-Step VLA is 8.7 times and 83.9 times faster than that of SmolVLA and Diffusion Policy, respectively. These results elucidate its great potential as a high-efficiency backbone for VLA-based robotic manipulation.

---

## 论文详细总结（自动生成）

以下是对论文《Mean-Flow based One-Step Vision-Language-Action》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在生成高频、连续的机器人动作块时，面临**生成延迟高**的瓶颈。
*   **研究背景**：
    *   **Transformer-based VLA**（如 RT-1/2）存在动作离散化伪影和不连贯问题。
    *   **Diffusion-based VLA**（如 Octo）通过去噪过程生成平滑动作，但需要多次迭代采样，实时性差。
    *   **FlowMatching-based VLA**（如 $\pi_0$）虽然比扩散模型快，但仍依赖多步欧拉积分。减少推理步数（NFE）会导致严重的偏置和轨迹畸变。
*   **动机**：提出一种无需蒸馏或复杂一致性约束，即可实现**一步生成（One-Step）**的高效 VLA 框架。

### 2. 核心方法论
*   **核心思想**：将传统的“学习瞬时向量场（Instantaneous Vector Field）”转变为“学习**均值去噪向量场（Mean Denoising Vector Field）**”。
*   **关键技术细节**：
    *   **均值流恒等式（MeanFlow Identity）**：定义均值向量场 $u$ 为时间区间 $[r, t]$ 内瞬时向量场 $v$ 的积分平均。通过数学推导，建立 $u$ 与 $v$ 及其雅可比向量积（JVP）的关系。
    *   **训练目标**：通过最小化预测均值场与目标均值场之间的 $L_2$ 差异进行训练。使用 `stop-gradient` 操作处理 JVP 项，避免昂贵的高阶梯度计算。
    *   **一步生成逻辑**：在推理时，模型直接预测从噪声分布到数据分布的全局平均变换，公式为 $A_0 = A_1 - u_\theta(A_1, 0, 1)$，从而实现单次前向传播完成动作生成。
    *   **架构集成**：采用 SmolVLM-2 作为视觉语言骨干网络，结合基于 Transformer 的动作专家（Action Expert）来预测均值向量场。

### 3. 实验设计
*   **实验场景**：使用国产 SO-101 六自由度机械臂进行真实世界实验。
*   **任务设置**：
    1.  **Pick-place（抓取投放）**：抓取红块放入盒子。
    2.  **Stacking（堆叠）**：将橙色玩具堆叠在色块上。
    3.  **Sorting（排序）**：长时程任务，将红/绿方块分别放入左右盒子。
*   **数据集**：每个任务收集 100 个演示视频，共 300 条轨迹。
*   **对比方法（Benchmark）**：
    *   **SmolVLA**：基于流匹配的轻量化 VLA。
    *   **Diffusion Policy**：基于扩散过程的视觉运动策略。

### 4. 资源与算力
*   **训练设备**：使用单张 **NVIDIA RTX 4090** GPU。
*   **推理设备**：笔记本电脑（Intel i5-10200H + **NVIDIA GTX 1650** GPU）。
*   **训练时长**：参数敏感性实验训练 60,000 步；多任务消融实验训练 200,000 步。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **参数敏感性分析**：针对 Flow Ratio（流比例）、Loss Metric（损失函数 Gamma 值）、NFE（推理步数）和 Action Chunk Size（动作块大小）进行了详细测试。
    *   **消融实验**：在三个真实任务上进行了多轮独立测试（每组 10 轮，每轮 10-20 次尝试）。
*   **充分性评价**：实验设计较为全面，涵盖了从超参数调优到端到端任务成功率的对比。通过真实机器人实验验证了模型在低算力设备（GTX 1650）上的部署能力，具有较强的说服力。

### 6. 主要结论与发现
*   **速度优势**：Mean-Flow VLA 的生成速度比 SmolVLA 快 **8.7 倍**，比 Diffusion Policy 快 **83.9 倍**。
*   **性能平衡**：在 NFE=1（一步生成）时，模型能保持约 78% 的平均成功率；当 NFE 增加到 5 时，成功率进一步提升，达到了效率与精度的平衡。
*   **关键超参数**：
    *   **Flow Ratio=0.2** 效果最好，说明同时学习瞬时场和均值场有助于稳定轨迹。
    *   **Adaptive Loss ($\gamma=0.5$)** 显著优于标准 $L_2$ 损失，能有效抑制多模态数据中的离群值干扰。
    *   **Action Chunk Size=20** 是最佳平衡点，兼顾了动作连贯性与环境反馈频率。

### 7. 优点
*   **极高效率**：真正实现了一步式动作生成，极大地降低了机器人交互的推理延迟。
*   **训练简洁**：不需要预训练、蒸馏或复杂的连贯性启发式算法，数学框架严谨且易于实现。
*   **部署友好**：在消费级显卡上即可实现高频实时控制，降低了 VLA 模型的应用门槛。

### 8. 不足与局限
*   **高精度任务挑战**：在 Stacking（堆叠）等需要极高空间精度的任务中，一步生成的成功率（64%）明显低于多步迭代的 SmolVLA（81.5%），说明一步生成在处理精细修正时仍有提升空间。
*   **泛化性验证有限**：实验主要集中在固定场景的三个任务上，尚未在大规模未见场景或更复杂的灵巧手操作中进行广泛验证。
*   **数据依赖**：模型性能高度依赖于高质量的专家演示数据，对于数据噪声的鲁棒性有待进一步研究。

（完）

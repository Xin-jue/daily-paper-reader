---
title: "HSC-VLA: Hierarchical Scene-Clearing for Robust Bimanual Manipulation in Dense Clutter"
title_zh: HSC-VLA：用于稠密杂乱环境下鲁棒双臂操作的分层场景清理框架
authors: "Zhen Liu, Xinyu Ning, Zhe Hu, XinXin Xie, Yitong Liu, Zhongzhu Pu"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07484v1"
tags: ["query:q10"]
score: 10.0
evidence: 针对稠密杂乱环境下的双臂操作任务的HSC-VLA框架
tldr: HSC-VLA 针对高密度杂乱环境下的双臂操作难题，提出了一种分层架构。该架构将高层视觉语义推理与底层高频运动执行解耦，通过“大脑”模块生成任务相关的场景掩码来过滤干扰物，并由基于扩散策略的“小脑”模块执行操作。实验表明，该方法在超市货架等复杂场景中显著提升了任务成功率和长程操作的鲁棒性，有效克服了端到端模型的表征瓶颈。
selection_source: fresh_fetch
motivation: 现有的视觉-语言-动作模型在密集杂乱环境中容易受到无关视觉干扰的影响，导致指令遵循失败和长程任务性能下降。
method: 提出分层框架 HSC-VLA，利用高层“大脑”生成任务特定的场景掩码以过滤干扰，并结合底层扩散策略“小脑”进行双臂操作。
result: "在密集杂乱的超市货架实验中，HSC-VLA 达到了 86.7% 的成功率，比最强的基准模型高出 52.4%，并在长程任务中表现优异。"
conclusion: 通过显式的场景清理抽象和分层解耦，HSC-VLA 有效解决了复杂环境下的视觉表征瓶颈，显著增强了机器人操作的鲁棒性。
---

## 摘要
现代视觉-语言-动作（VLA）模型在稠密操作环境中经常面临严重的指令遵循失败问题。在这些环境中，与任务无关的视觉杂物会分散注意力，破坏定位（grounding），并显著降低复杂长程场景下的性能。为了克服单一端到端架构的表示瓶颈，我们提出了 HSC-VLA，这是一个分层框架，通过显式的场景清理抽象，将高层视觉语义推理与低层高频感觉运动执行解耦。HSC-VLA 采用高层“大脑”（Brain）来分解长程任务，并生成特定任务的场景掩码，在抑制干扰物的同时保留与任务相关的几何信息。过滤后的观测结果随后被传递给低层“小脑”（Cerebellum），这是一个基于扩散的策略，仅利用掩码过滤后的视觉和本体感受来执行双臂操作。在稠密杂乱的超市货架上进行的广泛实验表明，HSC-VLA 在高密度杂乱环境下实现了 86.7% 的综合成功率，比表现最好的单一基准模型（π0-Full FT，34.3%）高出 52.4%。HSC-VLA 还表现出强大的长程任务性能，在杂物分类和补货任务中分别达到 72% 和 66% 的成功率，证明了其在复杂杂乱操作中强大的鲁棒性和有效的故障恢复能力。

## Abstract
Modern Vision--Language--Action models often suffer from critical instruction-following failures in high-density manipulation environments, where task-irrelevant visual clutter dilutes attention, corrupts grounding, and substantially degrades performance in complex long-horizon scenarios. To overcome the representation bottleneck of monolithic end-to-end architectures, we propose HSC-VLA, a hierarchical framework that decouples high-level visual-semantic reasoning from low-level, high-frequency sensorimotor execution through an explicit scene-clearing abstraction. HSC-VLA employs a high-level Brain to decompose long-horizon tasks and to generate task-specific scene masks that preserve task-relevant geometry while suppressing distractors. The filtered observations are then passed to a low-level Cerebellum, a diffusion-based policy that performs bimanual manipulation using only mask-filtered vision and proprioception. Extensive experiments in densely cluttered supermarket shelves demonstrate that HSC-VLA achieves 86.7\% aggregate success under high-density clutter, surpassing the best monolithic baseline ($π_0$-Full FT at 34.3\%) by 52.4\%. HSC-VLA also exhibits strong long-horizon performance, reaching 72\% on clutter sorting and 66\% on restocking, demonstrating strong robustness and effective failure recovery in complex cluttered manipulation.

---

## 论文详细总结（自动生成）

以下是对论文《HSC-VLA: Hierarchical Scene-Clearing for Robust Bimanual Manipulation in Dense Clutter》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的单体式（Monolithic）视觉-语言-动作（VLA）模型在**高密度杂乱环境**（如超市货架）中表现不佳。
*   **研究背景**：
    *   **表征瓶颈**：在密集场景下，大量无关的视觉干扰（遮挡、反光、无关物体）会稀释模型的注意力，导致目标定位失败。
    *   **因果困惑**：单体模型难以同时兼顾高层语义推理（长程规划）与低层高频运动控制，容易陷入无效循环或遗忘目标。
    *   **分布偏移**：环境稍有变化，端到端模型往往因缺乏显式的几何抽象而崩溃。

### 2. 方法论
HSC-VLA 提出了一个**分层架构**，将任务解耦为高层规划和低层执行：
*   **高层“大脑”（Brain）**：
    *   使用预训练的视觉语言模型（如 Qwen-v1-235B）作为语义规划器。
    *   **任务分解**：将长程指令分解为子目标序列。
    *   **场景简化**：识别干扰物并生成边界框（Bounding Boxes），确定哪些区域应被忽略。
*   **显式场景清理（Scene-Clearing Abstraction）**：
    *   利用 SAM 或 Cutie 等分割模型将边界框转化为像素级掩码（Mask）。
    *   通过掩码过滤原始图像，仅保留与任务相关的几何结构，抑制背景噪声。
*   **低层“小脑”（Cerebellum）**：
    *   采用**基于扩散（Diffusion-based）**的策略。
    *   输入：掩码过滤后的图像 + 机器人本体感受 + 子目标嵌入。
    *   输出：动作块（Action Chunking），实现平滑的双臂协调控制。
*   **感知-动作一致性协议**：确保离线训练和在线推理使用完全相同的视觉预处理流程，减少分布差异。

### 3. 实验设计
*   **实验场景**：
    *   **真实环境**：使用 Inspire-Omni O1 双臂机器人，在模拟超市货架（每层 5-20 个物体）进行测试。
    *   **仿真环境**：在 RoboTwin 2.0 物理引擎中进行原子技能基准测试。
*   **任务设置**：
    *   **工具箱任务**：精准抓取、目标放置、双臂协作。
    *   **长程任务**：杂物分类（需移动干扰物）、补货（识别空位并放置）。
*   **对比基准（Baselines）**：
    *   模仿学习方法：ACT、DP、DP3。
    *   主流 VLA 模型：RDT-1B、$\pi_0$（包括 LoRA 微调和全参数微调版本）。

### 4. 资源与算力
*   **训练细节**：使用 AdamW 优化器，Batch Size 为 32，训练 50k 个梯度步数。学习率采用线性预热（1k 步）加余弦退火策略。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号和数量。但考虑到使用了 235B 参数规模的 VLM 作为高层规划器，且涉及扩散策略的训练，推测需要高性能计算集群（如多张 A100 或 H100）。

### 5. 实验数量与充分性
*   **数据规模**：采集了 2100 条真实世界的专家轨迹（通过动觉教导获得）。
*   **实验组数**：
    *   在不同杂乱密度（低/高）下进行了数百次测试（SR@300, SR@200 等）。
    *   针对场景简化策略进行了消融实验（无掩码 vs 静态掩码 vs 动态清理）。
*   **充分性评价**：实验设计较为充分，涵盖了从单步技能到复杂长程任务的完整评估，且在真实机器人上进行了大量验证，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能飞跃**：在高密度杂乱环境下，HSC-VLA 的综合成功率达到 **86.7%**，远超表现最好的单体基准模型 $\pi_0$-Full FT（34.3%），提升幅度达 **52.4%**。
*   **长程鲁棒性**：在补货任务中，HSC-VLA 成功率达到 66%，而基准模型仅为 14%，证明了分层架构在减少误差累积方面的优势。
*   **掩码的作用**：消融实验证明，**动态更新掩码**对于长程任务至关重要，因为它能随环境变化实时调整关注区域。

### 7. 优点
*   **解耦思想明确**：通过显式的“场景清理”层，有效解决了 VLA 模型在复杂视觉环境下的注意力稀释问题。
*   **感知一致性**：强调训练与推理阶段视觉处理的严格对齐，增强了模型的零样本鲁棒性。
*   **双臂协作能力**：在需要高精度协调的复杂任务（如双臂搬运）中表现出极强的稳定性。

### 8. 不足与局限
*   **依赖性风险**：系统性能高度依赖于高层 VLM 定位干扰物的准确性以及分割模型的质量，一旦掩码生成错误，底层执行将失效。
*   **计算延迟**：动态掩码更新和高层 VLM 推理引入了额外的计算开销，可能限制机器人在极高频交互场景下的表现。
*   **技能泛化受限**：目前的“工具库”由预定义的原始策略组成，对于需要全新运动模式或极复杂接触动力学的任务，泛化能力尚存疑问。

（完）

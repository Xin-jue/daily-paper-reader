---
title: "ExpertGen: Scalable Sim-to-Real Expert Policy Learning from Imperfect Behavior Priors"
title_zh: ExpertGen：基于不完美行为先验的可扩展仿真到现实专家策略学习
authors: "Zifan Xu, Ran Gong, Maria Vittoria Minniti, Ahmet Salih Gundogdu, Eric Rosen, Kausik Sivakumar, Riedana Yan, Zixing Wang, Di Deng, Peter Stone, Xiaohan Zhang, Karl Schmeckpeper"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15956v1"
tags: ["query:q8"]
score: 10.0
evidence: 用于跨仿真到现实迁移的自动化专家策略学习
tldr: ExpertGen 提出了一种从不完美行为先验中学习专家策略的框架，旨在解决高质量机器人数据获取成本高的问题。该方法首先利用扩散模型学习不完美演示的先验，随后通过强化学习优化初始噪声，在保持策略冻结的情况下提升任务成功率。这种方法不仅能有效利用稀疏奖励，还能确保行为符合人类流形。实验证明其在工业装配和长程操作任务中表现优异，并成功实现了从仿真到真实环境的迁移。
selection_source: fresh_fetch
motivation: 获取大规模高质量的人类演示数据成本极高，限制了通用机器人策略的学习。
method: 利用扩散模型学习不完美先验，并通过强化学习在冻结策略的前提下优化初始噪声，实现高效且安全的策略提升。
result: "在工业装配和长程操作任务中分别达到 90.5% 和 85% 的成功率，显著优于现有基准方法。"
conclusion: ExpertGen 证明了通过结合不完美先验与噪声优化，可以规模化地生成高质量专家策略并成功应用于真实机器人。
---

## 摘要
学习具有泛化性和鲁棒性的行为克隆策略需要大量高质量的机器人数据。虽然人类演示（例如通过远程操作）是专家行为的标准来源，但在现实世界中大规模获取此类数据的成本极高。本文介绍了 ExpertGen，这是一个在仿真中自动进行专家策略学习的框架，旨在实现可扩展的仿真到现实（sim-to-real）迁移。ExpertGen 首先使用在不完美演示上训练的扩散策略（diffusion policy）来初始化行为先验，这些演示可以由大语言模型合成或由人类提供。然后，通过优化扩散模型的初始噪声并保持原始策略冻结，利用强化学习引导该先验实现高任务成功率。通过保持预训练的扩散策略冻结，ExpertGen 将探索正则化在安全且类人的行为流形内，同时仅需稀疏奖励即可实现有效学习。在具有挑战性的操作基准测试上的实证评估表明，ExpertGen 在无需奖励工程的情况下，能够可靠地生成高质量的专家策略。在工业装配任务中，ExpertGen 实现了 90.5% 的总成功率，而在长程操作任务中，它达到了 85% 的总成功率，优于所有基准方法。所生成的策略表现出灵巧的控制能力，并在各种初始配置和失败状态下保持鲁棒。为了验证仿真到现实的迁移，学习到的基于状态的专家策略通过 DAgger 进一步蒸馏为视觉运动策略，并成功部署在真实的机器人硬件上。

## Abstract
Learning generalizable and robust behavior cloning policies requires large volumes of high-quality robotics data. While human demonstrations (e.g., through teleoperation) serve as the standard source for expert behaviors, acquiring such data at scale in the real world is prohibitively expensive. This paper introduces ExpertGen, a framework that automates expert policy learning in simulation to enable scalable sim-to-real transfer. ExpertGen first initializes a behavior prior using a diffusion policy trained on imperfect demonstrations, which may be synthesized by large language models or provided by humans. Reinforcement learning is then used to steer this prior toward high task success by optimizing the diffusion model's initial noise while keep original policy frozen. By keeping the pretrained diffusion policy frozen, ExpertGen regularizes exploration to remain within safe, human-like behavior manifolds, while also enabling effective learning with only sparse rewards. Empirical evaluations on challenging manipulation benchmarks demonstrate that ExpertGen reliably produces high-quality expert policies with no reward engineering. On industrial assembly tasks, ExpertGen achieves a 90.5% overall success rate, while on long-horizon manipulation tasks it attains 85% overall success, outperforming all baseline methods. The resulting policies exhibit dexterous control and remain robust across diverse initial configurations and failure states. To validate sim-to-real transfer, the learned state-based expert policies are further distilled into visuomotor policies via DAgger and successfully deployed on real robotic hardware.

---

## 论文详细总结（自动生成）

以下是对论文《ExpertGen: Scalable Sim-to-Real Expert Policy Learning from Imperfect Behavior Priors》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
机器人领域目前面临的主要瓶颈是**高质量专家数据的稀缺性**。虽然视觉-语言-动作（VLA）模型在语义理解上表现出色，但缺乏大规模、多样化且鲁棒的物理执行数据。
*   **研究动机**：获取真实世界的人类演示（如远程操作）成本极高且难以规模化；而传统的仿真强化学习（RL）虽然可扩展，但通常依赖于极其复杂的**奖励工程（Reward Engineering）**，且生成的动作往往不自然、难以迁移至现实。
*   **核心目标**：开发一个名为 **ExpertGen** 的框架，能够从“不完美”的初始行为先验（如 LLM 生成的简单脚本或少量人类演示）出发，在仿真中自动精炼出高质量、可直接用于 Sim-to-Real 迁移的专家策略。

### 2. 方法论
ExpertGen 采用三阶段流程，核心思想是**在保持预训练行为流形不变的前提下，通过优化扩散模型的初始噪声来提升性能**。

*   **阶段一：生成式行为先验建模**：
    *   使用不完美的演示数据（脚本或少量人类数据）训练一个基于状态的**扩散策略（Diffusion Policy）**。该策略捕捉了基本的动作结构，但可能在复杂环境下失败。
*   **阶段二：专家策略获取（核心技术：DSRL + FastTD3）**：
    *   **扩散引导强化学习（DSRL）**：在强化学习训练过程中，保持扩散模型的权重 $\epsilon_\theta$ **冻结**，仅通过一个轻量级的 RL 适配器来优化扩散过程的**初始高斯噪声**。
    *   **优势**：由于扩散模型权重不动，生成的动作始终保持在“类人”或“符合物理直觉”的数据流形内，避免了传统 RL 常见的抖动。
    *   **算法实现**：采用 **FastTD3**（一种针对大规模并行仿真优化的 off-policy 算法），仅利用**稀疏的任务成功奖励**（0/1 信号）进行训练，无需人工设计复杂的中间奖励。
*   **阶段三：视觉运动策略蒸馏（DAgger）**：
    *   将基于状态（Privileged State）的专家策略作为教师，通过 **DAgger** 算法将知识蒸馏到基于视觉（图像或点云）的学生策略中，并加入大量的视觉领域随机化，以实现零样本（Zero-shot）仿真到现实的迁移。

### 3. 实验设计
*   **实验场景与基准**：
    *   **ANY TASK**：包含 8 个桌面操作任务（如抬起、推动物体、堆叠、开抽屉等），涉及长程操作和接触密集型任务。
    *   **AutoMate**：包含 9 个高精度工业装配任务（如各种形状的插拔），具有极小的几何公差。
*   **对比方法**：
    *   **Scripted Policy**：LLM 生成的原始脚本。
    *   **Diffusion Policy (BC)**：仅使用行为克隆。
    *   **Residual RL**：在固定先验上叠加残差动作。
    *   **SMP (Score-Matching Motion Priors)**：利用扩散模型作为隐式奖励。
    *   **ExpertGen-PPO**：将 FastTD3 替换为 PPO 的消融版本。

### 4. 资源与算力
*   **硬件环境**：
    *   **GPU**：NVIDIA RTX 6000 Ada。
    *   **CPU**：AMD Ryzen Threadripper PRO 5955WX。
*   **仿真平台**：使用 **IsaacLab** 进行大规模 GPU 并行仿真。
*   **训练细节**：文中提到在并行仿真中进行训练，但未明确给出具体的总训练时长（小时数）。

### 5. 实验数量与充分性
*   **实验规模**：每个任务在评估时进行了 1024 次（ANY TASK）或 256 次（AutoMate）随机初始化测试，并提供了 95% 的置信区间。
*   **消融实验**：
    *   测试了不同数量的初始演示（50 到 1000 条）对性能的影响。
    *   对比了 DAgger 与普通 BC 在蒸馏阶段的效率。
    *   验证了使用人类演示作为先验与脚本作为先验的差异。
*   **真实世界验证**：在 Franka 机器人上进行了多项任务的实物部署，验证了点云和 RGB 两种输入模式。
*   **评价**：实验设计全面，涵盖了成功率、动作平滑度（Jerk）、轨迹相似度（DTW）等多个维度，对比基准涵盖了当前主流的离线转在线 RL 方法，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能卓越**：ExpertGen 在工业装配任务中达到 **90.5%** 的成功率，在长程操作任务中达到 **85%**，显著超过所有基准方法。
*   **无需奖励工程**：仅靠稀疏奖励即可在并行仿真中高效收敛，解决了 RL 规模化应用的最大痛点。
*   **鲁棒性强**：生成的策略在面对外部扰动（如随机开启夹爪、施加外力）时表现出极强的恢复能力，而纯扩散策略在扰动下几乎完全失效。
*   **平滑度高**：相比残差 RL，ExpertGen 生成的动作更平滑（Jerk 值更低），更接近人类演示的流形。

### 7. 优点与亮点
*   **创新的噪声优化机制**：通过优化扩散初始噪声而非策略权重，完美平衡了“探索新行为”与“保持既有流形”之间的矛盾。
*   **端到端自动化**：从 LLM 生成脚本到最终视觉策略部署，全流程高度自动化，极具工业应用潜力。
*   **Sim-to-Real 成功率高**：通过 DAgger 蒸馏和视觉随机化，实现了在真实世界中对复杂、接触密集型任务的稳健执行。

### 8. 不足与局限
*   **先验依赖性**：ExpertGen 的性能仍受限于初始演示的覆盖范围。如果初始先验完全没有包含某种必要的技能（例如物体翻转），RL 很难在冻结权重的约束下凭空创造出全新的动作类别。
*   **计算开销**：虽然 FastTD3 提高了效率，但扩散模型的多步去噪推理在强化学习的 Rollout 阶段仍然比普通神经网络策略更耗费计算资源。
*   **应用限制**：目前主要集中在桌面操作，对于全身协调（如足式机器人）或更广阔场景的适用性尚待验证。

（完）

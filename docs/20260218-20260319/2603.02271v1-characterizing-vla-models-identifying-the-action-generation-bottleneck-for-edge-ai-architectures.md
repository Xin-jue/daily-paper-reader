---
title: "Characterizing VLA Models: Identifying the Action Generation Bottleneck for Edge AI Architectures"
title_zh: 表征 VLA 模型：识别边缘 AI 架构的动作生成瓶颈
authors: "Manoj Vishwanathan, Suvinay Subramanian, Anand Raghunathan"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.02271v1"
tags: ["query:q12"]
score: 10.0
evidence: 表征 VLA 模型性能并识别动作生成瓶颈
tldr: "本研究针对具身智能中的视觉-语言-动作（VLA）模型在边缘端的部署挑战进行了深入分析。通过在Nvidia Jetson Orin和Thor平台上测试MolmoAct-7B模型，研究揭示了动作生成阶段是主要的性能瓶颈，占据了高达75%的端到端延迟。文章进一步通过建模预测了扩展至千亿参数模型所需的硬件规格，并提出高带宽内存（HBM）和存内计算（PIM）是解决边缘端实时性问题的关键技术路径。"
selection_source: fresh_fetch
motivation: 具身智能机器人要求极低的响应延迟，使得大规模VLA模型在边缘硬件上的实时部署成为亟待解决的难题。
method: 在Nvidia Jetson系列平台上对MolmoAct-7B模型进行性能表征，并利用分析建模与仿真技术预测未来硬件需求。
result: "识别出受内存带宽限制的动作生成阶段贡献了高达75%的端到端延迟，是系统的主要执行瓶颈。"
conclusion: 未来边缘AI架构需引入高带宽内存或存内计算技术，以支持百亿级参数规模VLA模型的实时推理。
---

## 摘要
视觉-语言-动作（VLA）模型是一类新兴的工作负载，对于边缘侧的机器人技术和具身智能（Embodied AI）至关重要。随着这些模型规模的扩大，它们展现出显著的能力提升，但必须在本地部署以满足实时应用严格的延迟要求。本文表征了 VLA 模型在两代边缘硬件（即 Nvidia Jetson Orin 和 Thor 平台）上的性能。通过使用先进的 VLA 模型 MolmoAct-7B，我们识别出了一个主要的执行瓶颈：高达 75% 的端到端延迟消耗在受内存限制的动作生成阶段。通过分析建模和仿真，我们预测了扩展到 100B 参数模型的硬件需求。我们还探讨了高带宽内存技术和存内计算（PIM）的影响，将其视为具身智能边缘系统中有前景的未来发展路径。

## Abstract
Vision-Language-Action (VLA) models are an emerging class of workloads critical for robotics and embodied AI at the edge. As these models scale, they demonstrate significant capability gains, yet they must be deployed locally to meet the strict latency requirements of real-time applications. This paper characterizes VLA performance on two generations of edge hardware, viz. the Nvidia Jetson Orin and Thor platforms. Using MolmoAct-7B, a state-of-the-art VLA model, we identify a primary execution bottleneck: up to 75% of end-to-end latency is consumed by the memory-bound action-generation phase. Through analytical modeling and simulations, we project the hardware requirements for scaling to 100B parameter models. We also explore the impact of high-bandwidth memory technologies and processing-in-memory (PIM) as promising future pathways in edge systems for embodied AI.

---

## 论文详细总结（自动生成）

### 论文总结：表征 VLA 模型：识别边缘 AI 架构的动作生成瓶颈

#### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型在具身智能（机器人）领域表现出强大的泛化能力，但其规模（10B-100B 参数）与边缘端实时性需求（10-20Hz 控制频率）之间存在巨大的性能鸿沟。
*   **研究动机**：为了实现真正的通用自主系统，模型必须在本地运行以满足低延迟要求。然而，现有的边缘加速器在处理 VLA 模型特有的、受内存带宽限制的自回归（Autoregressive）动作生成任务时显得力不从心。本文旨在量化这一瓶颈并探索未来的硬件改进路径。

#### 2. 方法论
*   **核心思想**：采用“实测表征 + 仿真预测”的混合方法。首先在现有边缘平台上建立性能基准，然后利用高保真模拟器预测更大规模模型在未来硬件架构上的表现。
*   **关键技术细节**：
    *   **VLA 架构分解**：将模型分为三个子系统：视觉编码器（感知）、生成引擎（基于 Transformer 解码器的推理）和动作变换器（将内部表示转化为电机指令）。
    *   **硬件分析建模**：利用内部 XPU 模拟器，对 Streaming Multiprocessors (SMs)、平铺策略（Tiling）以及矩阵引擎的非对称带宽特性进行微架构级建模。
    *   **算子级优化模拟**：通过 Roofline 模型计算算子性能，并模拟跨算子的预取（Prefetching）优化，以准确评估内存受限操作的停顿情况。

#### 3. 实验设计
*   **测试模型**：MolmoAct-7B（最先进的 VLA 模型）。
*   **硬件基准（Benchmark）**：
    *   **Nvidia Jetson AGX Orin (64GB)**：代表当前主流边缘平台。
    *   **Nvidia Jetson Thor (128GB)**：代表下一代边缘平台。
*   **对比场景**：
    *   对比了不同内存技术：LPDDR5、LPDDR5X、GDDR7 以及存内计算（PIM）。
    *   对比了不同模型规模：从 7B 扩展到 100B 参数。
*   **评估指标**：端到端延迟（ms）、控制频率（Hz）。

#### 4. 资源与算力
*   **硬件型号**：实验直接使用了 Nvidia Jetson Orin（203 GB/s 带宽，100 TFLOPS BF16）和 Jetson Thor（273 GB/s 带宽，500 TFLOPS BF16）。
*   **训练/推理说明**：本文侧重于**推理性能表征**，未提及模型训练的具体算力消耗。推理测试使用了 PyTorch 运行时和 NVIDIA Nsight Compute 工具进行内核级追踪。

#### 5. 实验数量与充分性
*   **实验规模**：论文展示了在两代真实硬件上的详细性能分解，并针对 5 种假设的硬件变体（如 Thor+PIM, Orin+GDDR7 等）进行了仿真实验。
*   **充分性评价**：作为一篇 3 页的短论文（Short Paper），其实验聚焦于识别瓶颈。虽然模型种类单一（仅 MolmoAct），但通过仿真覆盖了从 7B 到 100B 的参数跨度，对于揭示硬件瓶颈具有较强的代表性和客观性。

#### 6. 主要结论与发现
*   **生成阶段是核心瓶颈**：动作生成（自回归解码）占据了端到端延迟的 **75%**。
*   **内存带宽受限**：尽管 Thor 的算力是 Orin 的 5 倍，但由于内存带宽仅提升了 1.3 倍，端到端性能仅提升了 1.4 倍，证实了系统严重受限于内存带宽而非计算能力。
*   **实时性差距巨大**：当前边缘硬件的延迟比实时要求（10Hz）高出 **200-300 倍**。
*   **未来路径**：即使采用 GDDR7 或 PIM 技术，对于 100B 规模的模型，单纯靠硬件升级仍难以达到 10Hz 的目标，需要算法与系统的协同设计。

#### 7. 优点
*   **瓶颈定位精准**：明确指出 VLA 模型在边缘端的瓶颈在于“内存受限的动作生成”，而非视觉感知。
*   **前瞻性强**：通过高保真仿真（精度达 70%-90%）预测了 100B 参数模型的硬件需求，为未来边缘芯片设计提供了量化依据。
*   **技术路径清晰**：探讨了 PIM 等新兴存储技术对具身智能的潜在贡献。

#### 8. 不足与局限
*   **模型多样性不足**：仅表征了 MolmoAct 一种模型架构，未探讨不同 Tokenization 策略（如连续动作空间 vs 离散 Token）对瓶颈的影响。
*   **软件优化探讨较少**：主要关注硬件规格，对模型压缩（量化、剪枝）或投机采样（Speculative Decoding）等软件侧优化手段提及较少。
*   **应用场景受限**：未考虑多摄像头输入或复杂传感器融合带来的额外计算开销。

（完）

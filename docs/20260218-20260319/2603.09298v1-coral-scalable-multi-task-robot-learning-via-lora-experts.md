---
title: "CORAL: Scalable Multi-Task Robot Learning via LoRA Experts"
title_zh: CORAL：通过 LoRA 专家实现的可扩展多任务机器人学习
authors: "Yuankai Luo, Woping Chen, Tong Liang, Zhenguo Li"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09298v1"
tags: ["query:q12"]
score: 9.0
evidence: 使用LoRA专家的多任务VLA模型微调
tldr: 针对视觉-语言-动作（VLA）模型在多任务学习中存在的任务干扰和存储开销大的问题，本文提出了CORAL框架。该框架冻结预训练骨干网络，为每个任务分配独立的轻量化LoRA专家，并通过动态推理引擎进行路由。这种参数隔离机制有效消除了负迁移和灾难性遗忘，在真实机器人和仿真基准测试中均显著优于联合训练，为可扩展的终身机器人学习提供了实用方案。
selection_source: fresh_fetch
motivation: 传统多任务微调面临任务间的梯度冲突（负迁移）以及为每个任务维护完整模型带来的巨大存储压力。
method: 采用冻结的VLA骨干网络配合任务特定的LoRA专家，并通过CORAL管理器根据指令动态切换专家。
result: 在Galaxea R1机器人及多个仿真基准上，CORAL克服了指令歧义，性能大幅超越了传统的联合训练方法。
conclusion: CORAL通过参数隔离实现了无干扰的多任务学习和持续的任务扩展，是实现大规模机器人终身学习的有效途径。
---

## 摘要
在现实世界的机器人中部署视觉-语言-动作（VLA）模型揭示了一个核心的多任务学习挑战：如何协调多任务机器人学习中的任务干扰。当多个任务在单一阶段进行联合微调时，来自不同任务的梯度可能会发生冲突，导致负迁移并降低每个任务的性能。然而，为每个任务维护一个单独的完整检查点通常在存储和部署上是不可行的。为了解决这一困境，我们提出了 CORAL，这是一个与主干网络和具身形式无关的框架，旨在减轻多任务干扰，同时保持对连续新任务流的自然扩展性。CORAL 冻结单个预训练的 VLA 主干网络，并为每个任务附加一个轻量级的低秩自适应（LoRA）专家；在运行时，动态推理引擎（CORAL 管理器）将语言指令路由到相应的专家，并实时切换专家，且具有零推理开销。这种严格的参数隔离避免了复杂的门控网络，并从结构上防止了参数级的跨任务干扰；作为一项附加功能，它还允许按顺序引入新任务，而不会因灾难性遗忘导致参数覆盖。我们在现实世界的 Galaxea R1 双臂移动协作机器人以及三个仿真基准测试（LIBERO、WidowX、Google Robot）上验证了 CORAL。结果表明，CORAL 克服了细粒度的指令歧义，且性能显著优于联合训练，为终身多任务机器人学习提供了一个实用且可扩展的系统。网站：https://frontierrobo.github.io/CORAL

## Abstract
Deploying Vision-Language-Action (VLA) models in real-world robotics exposes a core multi-task learning challenge: reconciling task interference in multi-task robotic learning. When multiple tasks are jointly fine-tuned in a single stage, gradients from different tasks can conflict, causing negative transfer and reducing per-task performance. Yet maintaining a separate full checkpoint per task is often storage- and deployment-prohibitive. To address this dilemma, we present CORAL, a backbone- and embodiment-agnostic framework designed primarily to mitigate multi-task interference while remaining naturally extensible to a continuous stream of new tasks. CORAL freezes a single pre-trained VLA backbone and attaches one lightweight Low-Rank Adaptation (LoRA) expert per task; at runtime, a dynamic inference engine (the CORAL Manager) routes language instructions to the appropriate expert and swaps experts on the fly with zero inference overhead. This strict parameter isolation avoids complex gating networks and prevents parameter-level cross-task interference by construction; as an added capability, it also enables sequentially introducing new tasks without parameter overwriting caused by catastrophic forgetting. We validate CORAL on a real-world Galaxea R1 dual-arm mobile manipulator and three simulation benchmarks (LIBERO, WidowX, Google Robot), where CORAL overcomes fine-grained instructional ambiguity and substantially outperforms joint training, yielding a practical and scalable system for lifelong multi-task robot learning. Website: https://frontierrobo.github.io/CORAL

---

## 论文详细总结（自动生成）

### CORAL: 通过 LoRA 专家实现的可扩展多任务机器人学习

#### 1. 核心问题与整体含义（研究动机和背景）
在将视觉-语言-动作（VLA）模型部署于现实机器人时，面临三大核心挑战：
*   **多任务干扰（负迁移）：** 联合微调多个任务时，不同任务的梯度冲突会导致性能下降，尤其是指令存在细微歧义时。
*   **存储开销：** 为每个任务保存完整的模型参数在边缘设备上不可行（模型通常很大）。
*   **灾难性遗忘：** 顺序学习新任务会覆盖旧知识。
**CORAL** 的提出旨在通过“参数隔离”机制，在保持基础模型泛化能力的同时，实现高效、可扩展且无干扰的多任务学习。

#### 2. 方法论
CORAL 采用两阶段学习范式，且与具体的主干网络和机器人形式无关：
*   **具身感知通用预训练：** 首先在多样化数据集上训练/微调 VLA 基础模型（$\theta_{base}$），使其掌握基本的控制、运动学和环境结构，随后永久冻结该模型。
*   **轻量化任务特定 LoRA 专家：** 为每个任务训练独立的 LoRA 适配器（$\theta_k$）。LoRA 模块被注入到 VLM 编码器的注意力层和动作头中。
*   **确定性路由：** 不同于传统 Mixture-of-Experts (MoE) 需要复杂的学习门控网络，CORAL 利用机器人指令本身具有的任务明确性，直接根据语言指令索引对应的专家。
*   **CORAL 管理器（动态部署）：** 在推理时，管理器根据指令动态加载、合并（Merge）或卸载（Un-merge）LoRA 权重。由于 LoRA 权重可以直接合并进基础模型，推理时具有**零额外计算开销（Zero Inference Overhead）**。

#### 3. 实验设计
*   **仿真基准测试：**
    *   **LIBERO：** 包含 40 个任务（Spatial, Object, Goal, Long 四个子集）。
    *   **WidowX (Simpler-Bridge)：** 评估真实到仿真的迁移能力。
    *   **Google Robot (Simpler-Fractal)：** 测试在不同变体下的鲁棒性。
*   **真实世界评估：**
    *   使用 **Galaxea R1** 双臂移动协作机器人。
    *   测试了 8 种复杂的跨场景任务（如折叠 T 恤、插吸管、倒垃圾等）。
    *   测试了新能力获取（开门、按电梯按钮），涉及 8 种变体。
*   **对比方法：** 包括联合全量微调（Joint Full Fine-Tuning）、独立全量微调、以及多种 SOTA VLA 模型（如 SimVLA, $\pi_{0.5}$, RT-2-X, X-VLA 等）。

#### 4. 资源与算力
*   **模型规模：** 实验使用了 0.8B (SimVLA) 和 3B ($\pi_{0.5}$) 参数规模的基础模型。
*   **存储效率：** 一个秩为 16 的 LoRA 专家仅占约 **26 MB**，相比全量模型（约 3GB）压缩了约 100 倍。
*   **训练时长：** 每个专家仅需训练 1 到 5 个 Epoch（或约 50 步优化），极其高效。
*   **推理延迟：** 专家切换过程在单 GPU 上可在 **100ms** 内完成。
*   **算力细节：** 文中未明确给出具体的 GPU 型号和总训练机时，但强调了其在消费级硬件和边缘设备上的可行性。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从仿真到真实的完整闭环，仿真实验涉及 40+ 任务，真实机器人实验包含 50 次重复试验以确保统计显著性。
*   **充分性：** 实验不仅对比了成功率，还深入分析了存储开销、切换延迟以及灾难性遗忘问题。
*   **客观性：** 通过在不同主干网络（SimVLA 和 $\pi_{0.5}$）上应用 CORAL，证明了该框架的通用性和鲁棒性。

#### 6. 主要结论与发现
*   **性能超越：** CORAL 在 LIBERO 等基准上刷新了记录（平均成功率 99.3%），显著优于联合训练。
*   **解决干扰：** 实验证明联合微调会导致性能崩溃（成功率从 90%+ 降至 24.5%），而 CORAL 通过参数隔离完美避开了这一问题。
*   **零遗忘：** 由于专家参数完全独立，顺序学习新任务对旧任务的成功率影响为零。
*   **高效部署：** 可以在单个模型的存储空间内存储数百个任务专家。

#### 7. 优点
*   **架构优雅：** 利用语言指令作为天然的路由索引，规避了 MoE 门控网络训练难、易失效的问题。
*   **部署友好：** 零推理开销和极小的存储占用，非常适合资源受限的移动机器人。
*   **灵活性高：** 支持终身学习，可以随时添加新任务专家而无需重新训练整个系统。

#### 8. 不足与局限
*   **指令依赖：** 路由高度依赖语言指令的准确性，如果指令模糊或无法映射到已知专家，系统可能失效。
*   **表示层限制：** 虽然参数隔离解决了梯度冲突，但所有专家共享同一个冻结的骨干网络，如果基础模型的特征提取能力不足，LoRA 专家的上限会受限。
*   **知识共享：** 目前每个专家是完全独立的，缺乏在相似任务之间共享知识的层级结构（作者将其列为未来工作）。

（完）

---
title: "Pri4R: Learning World Dynamics for Vision-Language-Action Models with Privileged 4D Representation"
title_zh: Pri4R：利用特权 4D 表示学习视觉-语言-动作模型的世界动力学
authors: "Jisoo Kim, Jungbin Cho, Sanghyeok Chu, Ananya Bal, Jinhyung Kim, Gunhee Lee, Sihaeng Lee, Seung Hwan Kim, Bohyung Han, Hyunmin Lee, Laszlo A. Jeni, Seungryong Kim"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.01549v1"
tags: ["query:q12"]
score: 9.0
evidence: 通过特权4D表示赋予VLA模型世界动力学理解
tldr: Pri4R 旨在解决现有视觉-语言-动作（VLA）模型在物理交互中缺乏时空动力学理解的问题。该方法在训练阶段引入特权 4D 信息，通过轻量级点追踪头预测 3D 轨迹，使模型在共享表示空间中学习场景几何演变。推理时无需额外输入或计算开销，显著提升了机器人在复杂操纵任务中的物理感知能力和控制精度。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型虽然语义理解能力强，但往往难以捕捉物理交互中的时空动力学，导致在复杂操纵任务中表现不佳。
method: 在训练中增加一个轻量级点追踪头，利用 VLA 特征预测未来 3D 点轨迹，从而将 4D 动力学知识隐式注入模型表示。
result: "在 LIBERO-Long 和 RoboCasa 仿真及真实世界任务中分别实现了 10% 和 40% 的性能提升，且推理阶段无额外开销。"
conclusion: 3D 点轨迹预测是学习动作-世界动力学的有效监督目标，Pri4R 为增强 VLA 模型的物理感知提供了一种简单高效的方案。
---

## 摘要
人类不仅学习自己的身体如何移动，还学习周围世界如何响应其动作。相比之下，虽然最近的视觉-语言-动作（VLA）模型表现出令人印象深刻的语义理解能力，但它们往往无法捕捉主导物理交互的时空动力学。在本文中，我们介绍了 Pri4R，这是一种简单而有效的方法，通过在训练期间利用特权 4D 信息，赋予 VLA 模型对世界动力学的隐式理解。具体而言，Pri4R 为 VLA 增加了一个轻量级的点轨迹预测头，用于预测 3D 点轨迹。通过将 VLA 特征注入该预测头以共同预测未来的 3D 轨迹，模型学会在其共享表示空间中融入演化的场景几何，从而为精确控制提供更具物理感知能力的上下文。由于其架构简单，Pri4R 仅需极小改动即可与主流 VLA 设计模式兼容。在推理过程中，我们使用原始 VLA 架构且不作任何更改来运行模型；Pri4R 不会增加额外的输入、输出或计算开销。在模拟和真实世界的评估中，Pri4R 显著提高了在具有挑战性的操纵任务上的性能，包括在 LIBERO-Long 上获得 +10% 的提升，在 RoboCasa 上获得 +40% 的提升。我们进一步证明了 3D 点轨迹预测是学习动作-世界动力学的有效监督目标，并通过广泛的消融实验验证了我们的设计选择。

## Abstract
Humans learn not only how their bodies move, but also how the surrounding world responds to their actions. In contrast, while recent Vision-Language-Action (VLA) models exhibit impressive semantic understanding, they often fail to capture the spatiotemporal dynamics governing physical interaction. In this paper, we introduce Pri4R, a simple yet effective approach that endows VLA models with an implicit understanding of world dynamics by leveraging privileged 4D information during training. Specifically, Pri4R augments VLAs with a lightweight point track head that predicts 3D point tracks. By injecting VLA features into this head to jointly predict future 3D trajectories, the model learns to incorporate evolving scene geometry within its shared representation space, enabling more physically aware context for precise control. Due to its architectural simplicity, Pri4R is compatible with dominant VLA design patterns with minimal changes. During inference, we run the model using the original VLA architecture unchanged; Pri4R adds no extra inputs, outputs, or computational overhead. Across simulation and real-world evaluations, Pri4R significantly improves performance on challenging manipulation tasks, including a +10% gain on LIBERO-Long and a +40% gain on RoboCasa. We further show that 3D point track prediction is an effective supervision target for learning action-world dynamics, and validate our design choices through extensive ablations.

---

## 论文详细总结（自动生成）

### 论文总结：Pri4R —— 利用特权 4D 表示学习视觉-语言-动作模型的世界动力学

#### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 OpenVLA）主要依赖模仿学习，通过图像和指令预测动作。然而，这些模型往往只学习了“如何移动”（动作模仿），而缺乏对“动作后世界如何响应”的物理规律理解（即世界动力学）。这导致模型在处理复杂的物理交互（如避障、操作铰链物体）时表现脆弱。
**Pri4R** 的核心动机是：通过在训练阶段引入**特权 4D 信息**（即随时间演变的 3D 几何结构），赋予 VLA 模型隐式的物理感知能力，使其理解动作与场景几何演变之间的因果关系，且在推理时不增加任何额外负担。

#### 2. 方法论
*   **核心思想**：采用“特权学习”（Privileged Learning）范式。在训练时，利用 3D 点轨迹（Point Tracks）作为辅助监督信号；在推理时，移除辅助模块，保持原始 VLA 架构不变。
*   **关键技术细节**：
    *   **点轨迹预测头（Point Track Head）**：由两个轻量级 MLP 组成。
        *   **Point MLP**：将当前时刻的 3D 点集 $P_t$ 编码为点特征。
        *   **Fusion MLP**：将 VLA 主干网络的内部多模态嵌入（zt）与点特征融合，预测未来 $H$ 步的 3D 位移 $\Delta P_{t:t+H}$。
    *   **架构兼容性**：设计了两种接口。对于 **OpenVLA-OFT**，直接提取动作查询标记的隐藏状态；对于 **$\pi$ 系列**，引入一个轻量级的 Transformer 嵌入模块来提取特征。
    *   **损失函数**：总损失 $L = L_{act} + \omega_{pt} L_{pt}$，其中 $L_{pt}$ 是预测位移与真实位移之间的 $\ell_1$ 损失。
*   **算法流程**：训练时，模型同时预测机器人动作和场景中关键点的未来轨迹。这种联合优化迫使 VLA 的共享表示空间编码物理世界的演变规律。

#### 3. 实验设计
*   **数据集与场景**：
    *   **仿真环境**：使用 **LIBERO**（包含 Spatial, Object, Goal, Long 四个子集）和 **RoboCasa**（包含 24 种复杂的厨房操纵任务）。
    *   **真实世界**：使用 OMY-F3M 单臂机器人，设计了四类任务：跨越障碍物抓取、放入指定箱子、抓取最远物体、抓取移动物体。
*   **Benchmark 与对比方法**：
    *   对比了 Diffusion Policy, Octo, DiT Policy, OpenVLA, OpenVLA-OFT, $\pi_0, \pi_{0.5}$ 等最先进的模型。
    *   在仿真中，以原始 VLA 模型作为 Baseline，观察加入 Pri4R 后的增益。

#### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA H200 GPU**（具体数量为 4 到 8 块，视任务而定）。
*   **训练时长**：
    *   OpenVLA-OFT 在 LIBERO 上训练 90k 步，在 RoboCasa 上训练 120k 步。
    *   $\pi$ 系列在两个基准上均训练 30k 步。
    *   **效率提升**：论文指出 Pri4R 虽然初期学习较慢，但最终达到基准峰值性能的速度比 Baseline 快 **2.7 倍**，节省了约 8 个 H200 GPU-days。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的仿真测试（LIBERO 500 次试验，RoboCasa 24 个任务组）和真实世界测试（126 次试验）。
*   **消融实验**：非常充分。涵盖了：
    *   **监督信号对比**：对比了 3D 点轨迹、2D 点轨迹、目标点集、深度图预测，证明 3D 轨迹最有效。
    *   **追踪对象对比**：对比了仅追踪机器人、仅追踪场景、以及两者同时追踪（Pri4R 采用）。
    *   **架构组件**：验证了点集输入方式、嵌入模块设计、预测头架构（MLP vs Transformer）的影响。
*   **客观性**：通过在不同架构（主干中心型 vs 专家型）上验证，证明了方法的通用性和公平性。

#### 6. 主要结论与发现
*   **性能飞跃**：Pri4R 在 LIBERO-Long 上提升了 **10%** 的成功率，在 RoboCasa 上提升了高达 **40%**。
*   **物理感知**：3D 点轨迹预测能显著增强模型对空间深度、物体接触和运动趋势的理解。
*   **零开销**：该方法在推理阶段完全不改变原始模型的计算流，实现了“免费”的性能提升。
*   **收敛加速**：引入几何监督有助于模型更快地找到最优策略。

#### 7. 优点
*   **简洁高效**：不改变推理架构，解决了 VLA 模型推理延迟大的痛点。
*   **监督信号精准**：相比于密集的视频预测或抽象的语言描述，3D 点轨迹在时空上既密集又稀疏（关注关键点），与机器人动作空间高度对齐。
*   **通用性强**：无缝集成到现有的主流 VLA 框架中。

#### 8. 不足与局限
*   **数据依赖**：在真实世界中，Pri4R 依赖于现成的 3D 点追踪模型（如 SpatialTracker）来生成伪标签，标注质量会影响训练效果。
*   **静态推理**：虽然训练时学习了动力学，但推理时由于没有 3D 输入，模型仍可能在极端分布外（OOD）场景下因缺乏实时几何反馈而失败。
*   **预训练缺失**：目前仅在微调阶段验证，尚未在大规模预训练阶段（如 Embodiment X）进行验证。

（完）

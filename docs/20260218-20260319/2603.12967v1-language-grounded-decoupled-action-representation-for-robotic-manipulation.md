---
title: Language-Grounded Decoupled Action Representation for Robotic Manipulation
title_zh: 机器人操作中基于语言引导的解耦动作表示
authors: "Wuding Weng, Tongshu Wu, Liucheng Chen, Siyu Xie, Zheng Wang, Xing Xu, Jingkuan Song, Heng Tao Shen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12967v1"
tags: ["query:q10"]
score: 9.0
evidence: 机器人操作的语言对齐动作表示
tldr: 针对机器人操作中高层语义理解与底层动作控制之间的异构性挑战，本文提出LaDA框架。该框架利用自然语言作为语义桥梁，将动作解耦为平移、旋转和夹爪控制三个可解释原语。通过引入语义引导的软标签对比学习和自适应加权策略，LaDA增强了动作的泛化性与运动一致性。实验证明其在模拟基准和真实场景中均表现优异，能有效处理未见或语义相关的任务。
selection_source: fresh_fetch
motivation: 旨在解决机器人操作中高层视觉语言理解与底层动作控制不匹配，导致在处理新任务或语义相关任务时鲁棒性不足的问题。
method: 提出LaDA框架，将动作解耦为三种可解释原语，并结合语义引导的软标签对比学习与自适应加权策略进行训练。
result: 在LIBERO、MimicGen基准测试及真实世界演示中，LaDA展现出强大的性能，并能有效泛化至未见过的相关任务。
conclusion: 通过引入细粒度的解耦动作表示和语义对齐机制，LaDA显著提升了机器人操作任务的泛化能力和动作准确性。
---

## 摘要
高层视觉语言理解与底层动作控制之间的异构性仍然是机器人操作中的一个根本挑战。尽管最近的方法在特定任务的动作对齐方面取得了进展，但它们在为新任务或语义相关任务生成鲁棒且准确的动作时往往面临困难。为此，我们提出了基于语言引导的解耦动作表示（LaDA）框架，该框架利用自然语言作为语义桥梁来连接感知与控制。LaDA 引入了一个由三个可解释动作原语（平移、旋转和夹持器控制）组成的细粒度中间层，为底层动作提供了显式的语义结构。它进一步采用了一种语义引导的软标签对比学习目标，以对齐跨任务的相似动作原语，从而增强泛化能力和运动一致性。受课程学习启发，一种自适应权重策略动态平衡了对比目标和模仿目标，以实现稳定且有效的训练。在模拟基准测试（LIBERO 和 MimicGen）以及真实世界演示中的广泛实验验证了 LaDA 取得了强大的性能，并能有效地泛化到未知或相关的任务。

## Abstract
The heterogeneity between high-level vision-language understanding and low-level action control remains a fundamental challenge in robotic manipulation. Although recent methods have advanced task-specific action alignment, they often struggle to generate robust and accurate actions for novel or semantically related tasks. To address this, we propose the Language-Grounded Decoupled Action Representation (LaDA) framework, which leverages natural language as a semantic bridge to connect perception and control. LaDA introduces a fine-grained intermediate layer of three interpretable action primitives--translation, rotation, and gripper control--providing explicit semantic structure for low-level actions. It further employs a semantic-guided soft-label contrastive learning objective to align similar action primitives across tasks, enhancing generalization and motion consistency. An adaptive weighting strategy, inspired by curriculum learning, dynamically balances contrastive and imitation objectives for stable and effective training. Extensive experiments on simulated benchmarks (LIBERO and MimicGen) and real-world demonstrations validate that LaDA achieves strong performance and generalizes effectively to unseen or related tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Language-Grounded Decoupled Action Representation for Robotic Manipulation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器人操作中“高层视觉语言理解”与“底层动作控制”之间的异构性（Gap）。
*   **研究动机**：
    *   现有的视觉-语言-动作（VLA）模型通常将感知与控制紧密耦合，缺乏可解释性，且难以在不同任务间复用运动知识。
    *   潜空间动作学习（Latent Action Learning）缺乏显式语义，导致跨任务迁移能力差。
    *   现有的语言条件策略往往使用粗粒度的离散原语（如“向前移动”），无法捕捉精确的运动参数（如位移量、旋转轴）。
*   **目标**：构建一个既有语义基础又能在任务间迁移的动作表示框架。

### 2. 方法论
LaDA 框架的核心思想是利用**自然语言作为语义桥梁**，将连续动作解耦并对齐。
*   **动作解耦（Action Decomposition）**：将 7 自由度的末端执行器动作分解为三个可解释的语言引导原语：
    1.  **平移原语**（如“沿 X 轴移动 0.5 米”）。
    2.  **旋转原语**（如“绕 Z 轴旋转 90 度”）。
    3.  **夹爪原语**（“张开”或“闭合”）。
*   **语义引导的软标签对比学习（SCL）**：
    *   **软标签相似度矩阵 ($S$)**：不使用传统的 0/1 硬标签，而是根据动作原语在平移、旋转和夹爪维度上的匹配程度计算连续的亲和力权重。
    *   **双路径对齐**：同时进行“动作-动作对齐”和“动作-原语描述对齐”，确保动作嵌入空间既反映物理运动又符合语言语义。
*   **自适应损失加权（Adaptive Weighting）**：受课程学习启发，利用移动平均（MA）估计对比损失和模仿损失的量级，动态调整两者权重，以平衡粗粒度的行为监督与细粒度的语义对齐。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Open X-Embodiment (OXE) 数据集的子集（约 2250 万帧图像）。
    *   **模拟环境**：LIBERO（包含 Spatial, Object, Goal, Long 四个套件，共 40 个任务）和 MimicGen（9 个接触密集型任务）。
    *   **真实世界**：Franka Emika Panda 机器人，执行抓取并放置方块的任务。
*   **对比方法**：包括 OpenVLA, Octo, Diffusion Policy, CLIP-RT, Phoenix, UniACT, LAPA 等 15 种以上的先进 VLA 模型。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量及训练总时长。
*   **模型规模**：提到 LaDA 的参数量约为 0.6B，相比 1.3B 的 CLIP-RT 更加轻量化，且在性能上持平或更优。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的 40 个任务上进行了广泛测试，每个任务进行 50 次随机试验。
    *   在 MimicGen 的 9 个复杂任务上进行了对比。
    *   进行了**泛化性实验**：测试了未见过的指令（Cross-task）和语义相似但目标改变的任务（Similar-task）。
    *   **消融实验**：验证了软标签对比学习（SCL）和自适应加权（AW）的有效性。
    *   **可视化**：通过 t-SNE 展示了动作嵌入空间的聚类效果。
*   **充分性评价**：实验设计非常充分，涵盖了从短时到长时任务、从模拟到现实、从多任务学习到零样本泛化的多个维度，对比基线全面，结果具有客观性。

### 6. 主要结论与发现
*   **性能领先**：LaDA 在 LIBERO 上的平均成功率达到 93.6%，在 MimicGen 上达到 67%，均显著优于现有基线。
*   **泛化能力强**：通过将动作分解为语言原语，模型能够更好地理解任务间的共享运动模式，从而在处理新指令时表现出更强的鲁棒性。
*   **语义对齐的价值**：软标签对比学习比硬标签更能捕捉动作之间的细微联系，有助于形成结构化的动作嵌入空间。

### 7. 优点
*   **可解释性**：将黑盒动作转化为可理解的平移、旋转、夹爪原语。
*   **数据效率**：通过语义桥梁减少了冗余学习，在较小参数规模下实现了高性能。
*   **自适应优化**：动态平衡不同损失函数，解决了多目标优化中的收敛不稳定问题。

### 8. 不足与局限
*   **语言依赖**：模型性能高度依赖于自动生成的动作语言描述的质量，若描述存在噪声可能影响对齐效果。
*   **离散化风险**：虽然微调阶段使用了连续回归，但预训练阶段的动作离散化分箱（Binning）可能会损失一定的运动精度。
*   **场景复杂度**：真实世界实验相对简单（单臂抓取），尚未在双臂协作或更复杂的非结构化环境中进行大规模验证。

（完）

---
title: "Joint-Aligned Latent Action: Towards Scalable VLA Pretraining in the Wild"
title_zh: 联合对齐潜动作：迈向野外环境下可扩展的 VLA 预训练
authors: "Hao Luo, Ye Wang, Wanpeng Zhang, Haoqi Yuan, Yicheng Feng, Haiweng Xu, Sipeng Zheng, Zongqing Lu"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.21736v1"
tags: ["query:q10"]
score: 10.0
evidence: 利用异构人类数据进行操作任务的VLA预训练
tldr: 针对视觉-语言-动作模型（VLA）缺乏大规模多样化机器人数据的问题，本文提出JALA预训练框架。该框架通过学习联合对齐的潜动作（Latent Action），绕过复杂的视觉动态重建，将预测动作嵌入与逆动力学及真实动作对齐。利用包含750万视频的UniHand-Mix数据集进行大规模预训练，JALA在模拟和真实环境的机器人操控任务中均表现出显著的性能提升，为利用人类视频数据进行VLA预训练提供了可扩展的新路径。
selection_source: fresh_fetch
motivation: 现有的VLA模型受限于高质量机器人数据的匮乏，而海量的人类操作视频因缺乏精确标签或难以处理而未被充分利用。
method: 提出JALA框架，通过学习与逆动力学和真实动作共同对齐的预测性动作嵌入，构建一个感知转换且以行为为中心的潜空间。
result: 在UniHand-Mix大规模数据集上预训练后，模型生成的动作更真实，并显著提升了下游机器人操控任务的成功率。
conclusion: 联合对齐的潜动作为从异构人类数据中进行可扩展的VLA预训练提供了一种有效方案。
---

## 摘要
尽管取得了进展，视觉-语言-动作模型（VLA）仍受限于大规模、多样化机器人数据的匮乏。虽然人类操作视频提供了一个丰富的替代方案，但现有方法被迫在规模较小、标注精确的数据集与规模庞大但手部追踪标签不可靠的野外素材之间做出选择。我们提出了 JALA，一个学习联合对齐潜动作（Jointly-Aligned Latent Actions）的预训练框架。JALA 绕过了完整的视觉动力学重建，转而学习一种与逆动力学和真实动作均对齐的预测性动作嵌入。这产生了一个感知状态转移、以行为为中心的潜空间，用于从异构的人类数据中学习。我们利用 UniHand-Mix 扩展了这一方法，这是一个包含 750 万个视频片段（超过 2000 小时）、融合了实验室和野外素材的视频语料库。实验表明，JALA 在受控和无约束场景下都能生成更真实的手部动作，显著提升了仿真和真实世界任务中下游机器人操作的性能。这些结果表明，联合对齐的潜动作为利用人类数据进行 VLA 预训练提供了一条可扩展的途径。

## Abstract
Despite progress, Vision-Language-Action models (VLAs) are limited by a scarcity of large-scale, diverse robot data. While human manipulation videos offer a rich alternative, existing methods are forced to choose between small, precisely-labeled datasets and vast in-the-wild footage with unreliable hand tracking labels. We present JALA, a pretraining framework that learns Jointly-Aligned Latent Actions. JALA bypasses full visual dynamic reconstruction, instead learns a predictive action embedding aligned with both inverse dynamics and real actions. This yields a transition-aware, behavior-centric latent space for learning from heterogeneous human data. We scale this approach with UniHand-Mix, a 7.5M video corpus (>2,000 hours) blending laboratory and in-the-wild footage. Experiments demonstrate that JALA generates more realistic hand motions in both controlled and unconstrained scenarios, significantly improving downstream robot manipulation performance in both simulation and real-world tasks. These results indicate that jointly-aligned latent actions offer a scalable pathway for VLA pretraining from human data.

---

## 论文详细总结（自动生成）

### 论文结构化总结：JALA (Joint-Aligned Latent Action)

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作模型（VLA）的发展受限于大规模、多样化机器人数据的匮乏。虽然人类操作视频（Human Videos）是极佳的替代数据源，但面临**质量与多样性的权衡**：实验室采集的数据虽有精确的3D手部追踪标签，但场景受限；野外视频（In-the-wild）虽然多样性极高，但缺乏可靠的动作标签。
现有的利用野外视频的方法（如基于视觉重建的动力学模型）在处理精细的手部操作时，往往因为难以准确重建复杂的视觉细节而引入噪声。本文的核心动机是：**如何绕过复杂的像素级重建，直接从异构的人类视频中提取可扩展的、具有物理意义的动作先验？**

#### 2. 方法论：核心思想与关键技术
JALA 提出了一种**联合对齐潜动作（Jointly-Aligned Latent Action）**的框架，其核心思想是将 VLA 的内部预测嵌入与通过逆动力学提取的潜动作进行对齐。

*   **核心流程：**
    *   **潜动作提取（LAP）：** 使用潜动作感知器（Latent Action Perceiver, LAP）作为逆动力学模型（IDM），输入动作块的起始帧和结束帧，生成潜动作向量 $z$。
    *   **预测嵌入生成（VLA）：** VLA 模型根据上下文生成中间隐藏状态，作为预测嵌入 $h$。
    *   **联合对齐（Joint Alignment）：** 通过损失函数 $L_{Align} = \sum \|h - z\|_1$ 将两者对齐。
    *   **有监督学习（MCP）：** 对于有标签的数据，使用掩码块预测（Masked Chunk Prediction, MCP）任务，让模型预测真实的动作 Token。
*   **关键技术细节：**
    *   **双感知器架构：** 引入 LAP 和 LSP（潜状态感知器），LSP 负责将初始帧上下文注入潜空间。
    *   **解耦 EMA 更新：** 为了稳定训练，LAP 和 LSP 共享参数，但采用非对称的指数移动平均（EMA）更新，防止潜空间坍缩。
    *   **混合掩码方案：** 在预训练中结合了随机掩码和全掩码（针对无标签视频），使模型能同时处理有标签和无标签数据。

#### 3. 实验设计
*   **数据集：** 构建了 **UniHand-Mix**，包含 750 万个样本（>2000 小时）。
    *   **实验室子集（5M）：** 包含精确的 MANO 手部姿态标注。
    *   **野外子集（2.5M）：** 来自 Ego4D，经过手部活动验证和指令生成，大部分无动作标签。
*   **Benchmark 与场景：**
    *   **手部动作生成：** 在 Lab 和 Wild 两个划分集上评估生成动作的准确性。
    *   **仿真环境：** LIBERO（4个任务集）、RoboCasa（厨房任务）、GR1 Tabletop（桌面任务）。
    *   **真实世界：** 使用 Franka 机械臂 + Inspire 灵巧手，执行“放三个物体”、“擦白板”、“浇花”等复杂多步任务。
*   **对比方法：** 包括 Being-H0、LAPA（基于重建的 SOTA）、GR00T N1.5、OpenVLA、UniVLA 等。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA A800 (80GB) GPU**。
*   **预训练时长：** 在 7.5M 数据集上训练 1 个 Epoch，耗时约 **68 小时**。
*   **微调时长：** 
    *   LIBERO：约 8 小时（30k steps）。
    *   RoboCasa：约 16 小时（60k steps）。
    *   真实世界任务：约 8 小时。

#### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **多基准测试：** 涵盖了从动作预测到仿真控制，再到真实机器人部署的全流程。
*   **消融实验：** 详细探讨了野外视频比例（0% 到 100% 的扩展性）、隐藏层选择（第 14/19/24/28 层）、是否有对齐损失、是否有解耦 EMA 更新等。
*   **公平性：** 通过 retrain LAPA（命名为 LAPA†）在相同骨干网络和数据下进行对比，证明了性能提升源于算法设计而非单纯的数据或模型规模。

#### 6. 主要结论与发现
*   **扩展性：** 增加野外无标签视频的比例能持续提升下游任务的成功率，证明了 JALA 框架的扩展潜力。
*   **性能领先：** 在 LIBERO 单视图测试中，JALA 在 3B 以下参数模型中达到 SOTA，甚至接近某些使用了大规模机器人数据的更大模型。
*   **鲁棒性：** 在真实世界任务中，JALA 在面对未见过的视觉背景（如更换桌布颜色）时表现出极强的泛化能力，远超基准模型。
*   **效率：** 相比基于重建的方法，JALA 训练速度更快（节省约 20% 时间），且效果更好。

#### 7. 优点（亮点）
*   **范式创新：** 抛弃了高成本的像素重建，改用预测嵌入与潜动作对齐，解决了野外视频利用率低的难题。
*   **数据贡献：** 提供了大规模的混合数据集 UniHand-Mix，为后续研究提供了参考。
*   **灵巧手适配：** 实验证明了从人类手部视频学习的先验能很好地迁移到具有相似形态的灵巧手机器人上。

#### 8. 不足与局限
*   **依赖视觉编码器：** 潜动作的质量仍受限于预训练视觉编码器（如 DINOv3 或 V-JEPA2）提取特征的能力。
*   **手部检测依赖：** 在野外视频处理中，仍需依赖现有的手部检测和姿态估计工具进行初步过滤，这些工具的误差可能传播。
*   **任务复杂度：** 虽然在多步任务上表现良好，但对于需要极高精度力控或触觉反馈的任务，纯视觉预训练的局限性尚待观察。

（完）

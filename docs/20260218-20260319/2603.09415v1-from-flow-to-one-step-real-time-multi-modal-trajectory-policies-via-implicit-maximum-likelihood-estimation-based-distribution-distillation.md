---
title: "From Flow to One Step: Real-Time Multi-Modal Trajectory Policies via Implicit Maximum Likelihood Estimation-based Distribution Distillation"
title_zh: 从流到单步：通过基于隐式极大似然估计的分布蒸馏实现实时多模态轨迹策略
authors: "Ju Dong, Liding Zhang, Lei Zhang, Yu Fu, Kaixin Bai, Zoltan-Csaba Marton, Zhenshan Bing, Zhaopeng Chen, Alois Christian Knoll, Jianwei Zhang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09415v1"
tags: ["query:q2"]
score: 9.0
evidence: 用于机器人操作的实时多模态轨迹策略
tldr: 针对基于扩散和流匹配的生成式策略在机器人操作中推理延迟高的问题，本文提出一种基于隐式极大似然估计（IMLE）的分布蒸馏框架。该方法将条件流匹配（CFM）专家模型蒸馏为单步执行的学生模型，通过双向倒角距离目标函数保持多模态动作分布，并结合多模态感知编码器，实现了高频实时闭环控制和动态环境下的鲁棒重规划。
selection_source: fresh_fetch
motivation: 现有的生成式策略因迭代积分导致高延迟，而单步加速方法常面临模式崩溃，难以维持多模态演示的连贯性。
method: 利用IMLE将CFM专家模型蒸馏至单步学生模型，结合双向倒角距离优化分布覆盖度，并集成多模态几何感知编码器。
result: 该框架在单次前向传播中保留了专家的多模态分布，显著提升了控制频率并增强了对动态干扰的鲁棒性。
conclusion: 本研究通过分布蒸馏实现了高效实时的多模态轨迹策略，为高性能机器人闭环控制提供了可行方案。
---

## 摘要
基于扩散（diffusion）和流匹配（flow matching）的生成式策略通过建模多模态人类演示，在机器人操作中取得了优异的性能。然而，它们对迭代常微分方程（ODE）积分的依赖引入了显著的延迟，限制了高频闭环控制。最近的单步加速方法虽然减轻了这种开销，但往往会出现分布崩溃，产生无法执行连贯操作策略的平均轨迹。我们提出了一个框架，通过隐式极大似然估计（IMLE）将条件流匹配（CFM）专家模型蒸馏为快速的单步学生模型。双向 Chamfer 距离提供了一个集合级目标，同时提升了模态覆盖度和保真度，使得在单次前向传递中保留教师模型的多模态动作分布成为可能。统一的感知编码器进一步将多视图 RGB、深度、点云和本体感受整合到几何感知表示中。由此产生的高频控制支持实时收缩时界重规划（receding-horizon re-planning），并增强了在动态扰动下的鲁棒性。

## Abstract
Generative policies based on diffusion and flow matching achieve strong performance in robotic manipulation by modeling multi-modal human demonstrations. However, their reliance on iterative Ordinary Differential Equation (ODE) integration introduces substantial latency, limiting high-frequency closed-loop control. Recent single-step acceleration methods alleviate this overhead but often exhibit distributional collapse, producing averaged trajectories that fail to execute coherent manipulation strategies. We propose a framework that distills a Conditional Flow Matching (CFM) expert into a fast single-step student via Implicit Maximum Likelihood Estimation (IMLE). A bi-directional Chamfer distance provides a set-level objective that promotes both mode coverage and fidelity, enabling preservation of the teacher multi-modal action distribution in a single forward pass. A unified perception encoder further integrates multi-view RGB, depth, point clouds, and proprioception into a geometry-aware representation. The resulting high-frequency control supports real-time receding-horizon re-planning and improved robustness under dynamic disturbances.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **CFM-Student** 的实时多模态机器人轨迹策略框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持生成式模型（如扩散模型、流匹配模型）处理**多模态动作分布**（即完成同一任务有多种可行路径）能力的同时，实现**高频实时闭环控制**？
*   **研究背景**：
    *   现有的扩散（Diffusion）和流匹配（Flow Matching）策略虽然表现优异，但依赖多步迭代推理，导致延迟高（通常仅 2-3Hz），难以应对动态环境。
    *   现有的单步加速技术（如一致性蒸馏）在处理复杂多模态分布时容易出现“模式崩溃”（Mode Collapse），即输出多种可能路径的平均值，导致生成的轨迹在物理上不可行。

### 2. 方法论
该论文提出了一种“从流到单步”（Flow to One Step）的分布级蒸馏框架：
*   **核心思想**：先训练一个强大的**条件流匹配（CFM）**专家模型作为教师，然后利用**隐式极大似然估计（IMLE）**将其分布知识蒸馏到一个单步执行的学生模型中。
*   **关键技术细节**：
    *   **CFM 教师模型**：在数据空间建模轨迹分布，引入时间相关的掩码机制（Time-dependent masking）以防止模型学习到捷径，确保生成高质量的多模态监督数据。
    *   **IMLE 蒸馏目标**：不使用传统的 MSE 或 KL 散度（这些会导致均值化），而是采用**双向倒角距离（Bi-directional Chamfer Distance）**作为集合损失函数。
        *   **模式覆盖（Mode Covering）**：确保学生生成的候选轨迹能覆盖教师的所有模式。
        *   **模式寻求（Mode Seeking）**：确保学生生成的轨迹都落在教师定义的合法行为流形内。
    *   **多模态感知编码器**：集成 RGB、深度图、点云和本体感受数据。通过**双向跨模态注意力**建立语义与几何的对应关系，并使用**门控融合（Gated Fusion）**动态调整各模态权重。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：RLBench 基准测试，包含 8 个代表性任务（如开门、插充电器、放书等）。
    *   **现实世界**：使用 Franka Emika Panda 机械臂，执行 5 个任务，包括动态物体存放、厨房清理及动态柜子开启等。
*   **对比方法**：
    *   迭代模型：Diffusion Policy、DP3、PointFlowMatch、FlowPolicy（对比其多步与单步变体）。
    *   专用单步模型：Consistency Policy（一致性策略）、IMLE Policy。

### 4. 资源与算力
*   **推理算力**：在现实部署中使用了 **NVIDIA RTX 3070 GPU**。
*   **训练细节**：
    *   教师模型训练 2000 个 epoch。
    *   学生模型在教师生成的离线数据集上训练 1500 个 epoch。
    *   每个观测点由教师生成 $K=16$ 条候选轨迹作为监督信号。
*   **数据量**：仿真中每项任务 100 个演示；现实中每项任务 300 个远程操作演示。

### 5. 实验数量与充分性
*   **实验规模**：仿真实验涵盖 8 个任务，每个任务评估 300 个回合，取 3 次独立运行的平均值；现实实验涵盖 5 个任务，每个任务进行 30 次测试。
*   **消融实验**：针对感知模态（RGB/深度/点云）、蒸馏候选数量 $K$、预测时界 $T$ 进行了详细消融。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还重点对比了推理频率（Hz），并深入分析了单步模型常见的失败模式（模式崩溃 vs. 接触不稳定），论证具有客观性。

### 6. 主要结论与发现
*   **速度飞跃**：学生模型在现实世界中达到了 **125.0 Hz** 的推理频率，比教师模型（2.9 Hz）快了约 **43 倍**。
*   **性能保持**：在 RLBench 中，单步学生模型保持了教师模型约 93% 的性能（68.6% vs 74.1%），远超其他单步基准（如 Consistency Policy 仅 16.3%）。
*   **动态鲁棒性**：由于高频闭环控制能力，学生模型在动态任务（如抓取移动物体）中表现优于教师模型，因为教师模型因延迟过高无法及时响应环境变化。

### 7. 优点与亮点
*   **解决模式崩溃**：通过 IMLE 和双向倒角距离，成功在单步模型中保留了复杂的多模态分布特征。
*   **感知增强**：统一的几何感知编码器有效融合了 2D 语义和 3D 结构信息，提升了策略在复杂环境下的稳定性。
*   **实时重规划**：极高的推理速度支持了实时收缩时界重规划（Receding-horizon re-planning），显著增强了抗干扰能力。

### 8. 不足与局限
*   **性能差距**：尽管表现优异，但单步学生与多步教师之间仍存在约 5%-8% 的成功率差距，反映了蒸馏过程中的信息损失。
*   **训练成本**：需要先训练一个昂贵的教师模型并生成大规模离线轨迹集，增加了训练管线的复杂性。
*   **应用限制**：虽然处理了动态干扰，但在极端复杂的接触力学任务中，失败模式仍集中在碰撞和抓取不稳定上，表明底层控制或感知精细度仍有提升空间。

（完）

---
title: "Act-Observe-Rewrite: Multimodal Coding Agents as In-Context Policy Learners for Robot Manipulation"
title_zh: Act-Observe-Rewrite：作为机器人操控上下文内策略学习者的多模态编程智能体
authors: Vaishak Kumar
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.04466v1"
tags: ["query:q10"]
score: 9.0
evidence: 用于机器人操作策略的多模态语言模型
tldr: 本研究提出Act-Observe-Rewrite (AOR)框架，探索多模态大模型在无需梯度更新、专家演示或奖励工程的情况下，如何通过反思失败学习机器人操作。AOR将底层电机控制代码作为推理单元，使LLM能根据视觉观察和任务结果，在尝试间隙重写Python控制器代码。该方法利用代码的可解释性实现上下文策略学习，在多项robosuite任务中取得了高成功率，证明了LLM作为机器人策略学习器的潜力。
selection_source: fresh_fetch
motivation: 探索机器人能否在缺乏演示、梯度更新和复杂奖励函数的情况下，仅通过对自身失败的推理来学习复杂的物理操作任务。
method: 提出AOR框架，利用LLM根据视觉反馈和执行结果，在试验间隙不断重写和优化底层的Python控制器代码。
result: 在三项robosuite机器人操作任务中，该智能体在无演示和无奖励工程的前提下实现了极高的任务成功率。
conclusion: 将可解释的代码作为策略表示，使LLM能够通过上下文学习诊断并修复系统性故障，为机器人策略学习提供了新范式。
---

## 摘要
多模态语言模型能否在没有梯度更新、演示或奖励工程的情况下，通过对其自身失败的推理来学习操控物理对象？我们认为答案是肯定的，并精确地描述了其成立的条件。我们提出了 Act-Observe-Rewrite (AOR) 框架。在该框架中，大语言模型 (LLM) 智能体在视觉观察和结构化回合结果的指导下，通过在尝试之间合成全新的可执行 Python 控制器代码来改进机器人操控策略。与以往将 LLM 锚定在预定义技能库或使用代码生成进行单次规划合成的工作不同，AOR 将完整的底层电机控制实现作为 LLM 推理的单元，使智能体不仅能改变机器人的动作内容，还能改变其动作方式。核心观点是，将可解释代码作为策略表示，创造了一种与不透明神经策略在性质上完全不同的上下文内学习：智能体可以诊断系统性失败并重写其原因。我们在三个 robosuite 操控任务中验证了这一点，并报告了令人鼓舞的结果：智能体在没有演示、奖励工程或梯度更新的情况下实现了极高的成功率。

## Abstract
Can a multimodal language model learn to manipulate physical objects by reasoning about its own failures-without gradient updates, demonstrations, or reward engineering? We argue the answer is yes, under conditions we characterise precisely. We present Act-Observe-Rewrite (AOR), a framework in which an LLM agent improves a robot manipulation policy by synthesising entirely new executable Python controller code between trials, guided by visual observations and structured episode outcomes. Unlike prior work that grounds LLMs in pre-defined skill libraries or uses code generation for one-shot plan synthesis, AOR makes the full low-level motor control implementation the unit of LLM reasoning, enabling the agent to change not just what the robot does, but how it does it. The central claim is that interpretable code as the policy representation creates a qualitatively different kind of in-context learning from opaque neural policies: the agent can diagnose systematic failures and rewrite their causes. We validate this across three robosuite manipulation tasks and report promising results, with the agent achieving high success rates without demonstrations, reward engineering, or gradient updates.

---

## 论文详细总结（自动生成）

这是一份关于论文《Act-Observe-Rewrite: Multimodal Coding Agents as In-Context Policy Learners for Robot Manipulation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：多模态大语言模型（LLM）能否在**没有梯度更新、没有专家演示、没有复杂奖励工程**的情况下，仅通过对自身失败的推理来学习复杂的物理操作任务？
*   **研究动机**：
    *   **现有模型局限**：当前的视觉-语言-动作（VLA）模型虽然泛化性强，但部署失败时难以诊断和微调，且预训练成本极高。
    *   **范式转变**：借鉴 LLM 在编程和决策任务中的“自我反思”（Reflexion）能力，探索一种“不是训练策略，而是编写并重写策略”的新范式。
    *   **代码的优势**：相比于不透明的神经网络权重，可执行的 Python 代码具有高度的可解释性，允许 LLM 诊断并修复底层的逻辑错误（如坐标系转换错误）。

### 2. 方法论：Act-Observe-Rewrite (AOR) 框架
AOR 采用了一种**双时间尺度循环**架构：
*   **快循环（执行层）**：在回合内运行。机器人根据当前的 Python 控制器类（包含视觉处理和控制逻辑），以控制频率执行动作。
*   **慢循环（推理层）**：在回合间运行。多模态 LLM 充当“编程智能体”。
*   **核心流程**：
    1.  **Act（行动）**：执行当前控制器，记录动作、相位日志和奖励。
    2.  **Observe（观察）**：收集回合结束后的结构化数据（如最小距离、是否振荡）和关键帧图像（RGB-D）。
    3.  **Rewrite（重写）**：LLM 分析失败原因（是视觉偏差、逻辑错误还是参数问题？），然后**重新编写整个 Python 控制器类**。
*   **关键技术细节**：
    *   **代码作为策略单元**：LLM 不仅调整参数，还改变算法架构（如增加重试机制、修改几何计算公式）。
    *   **安全机制**：包含编译沙箱（验证代码合法性）、动作裁剪（限制输出范围）和回退机制（失败时恢复上一版控制器）。

### 3. 实验设计
*   **实验平台**：使用 **robosuite** 仿真平台（基于 MuJoCo 物理引擎）。
*   **任务设置**：
    1.  **Lift（提升）**：抓取红色方块。用于验证视觉校准和快速收敛。
    2.  **PickPlaceCan（拾放易拉罐）**：将可乐罐放入箱子。测试颜色识别和复杂环境下的感知鲁棒性。
    3.  **Stack（堆叠）**：将红方块叠在绿方块上。高难度任务，要求 2cm 以内的精度。
*   **对比基准（Benchmark）**：
    *   对比了 **Claude Code**（基于 Claude 3.5 Sonnet 系列）与 **OpenAI Codex**（GPT-4 级别）。
    *   结果显示 Codex 在相同预算下无法完成任务，凸显了底层编程智能体能力的重要性。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或数量。
*   **训练成本**：由于该方法属于**上下文内学习（In-Context Learning）**，**不需要任何模型训练或梯度更新**。主要的计算开销在于 LLM 的推理 API 调用以及仿真环境的运行。
*   **推理工具**：使用了 Anthropic 的 Claude Code 智能体进行多模态分析和代码合成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对三个任务进行了多次迭代（Lift 3次迭代，PickPlaceCan 2次，Stack 达20次）。
    *   对成功后的控制器进行了随机初始化的重复实验（如 20/20 次重复以验证鲁棒性）。
*   **充分性与客观性**：
    *   实验设计较为客观，作者诚实地报告了 Stack 任务中 9% 的失败率，并详细分析了智能体陷入局部最优的原因。
    *   通过消融式的诊断过程，展示了 LLM 如何一步步发现并修复视觉管线中的系统性 Bug（如 OpenGL 与 OpenCV 坐标系的冲突）。

### 6. 主要结论与发现
*   **代码表示的优越性**：代码合成允许 LLM 修复神经网络无法触及的“硬伤”（如 back-projection 公式中的正负号错误）。
*   **自主调试能力**：AOR 能够自主发现仿真环境或传感器协议中未记录的 Bug，这种深度诊断能力是传统强化学习或模仿学习不具备的。
*   **性能表现**：在完全没有演示的情况下，Lift 和 PickPlaceCan 达到 100% 成功率，Stack 达到 91%。

### 7. 优点与亮点
*   **零样本学习**：无需收集数千个演示视频或进行昂贵的强化学习训练。
*   **高度可解释**：生成的策略是人类可读的 Python 代码，便于审计和手动微调。
*   **闭环反馈**：通过视觉关键帧与代码逻辑的关联推理，实现了真正的“反思式”改进，而非盲目的参数搜索。

### 8. 不足与局限
*   **仅限于仿真**：尚未在真实机器人上验证，现实世界的噪声和光照变化可能对 HSV 分割等简单视觉方案提出挑战。
*   **搜索效率与局部最优**：在 Stack 任务中，智能体虽然诊断出了“夹爪碰撞”问题，但在 20 次迭代内未能想出避障策略，显示出 LLM 在复杂物理策略搜索上的局限。
*   **顺序迭代慢**：目前是单线尝试，如果能并行测试多个候选控制器，效率会更高。
*   **依赖模型能力**：系统的上限完全取决于底层 LLM 对几何、物理和编程的推理水平。

（完）

---
title: "SmoothVLA: Aligning Vision-Language-Action Models with Physical Constraints via Intrinsic Smoothness Optimization"
title_zh: SmoothVLA：通过内在平滑度优化实现视觉-语言-动作模型与物理约束的对齐
authors: "Jiashun Li, Xiaoyu Shi, Hong Xie, Mingsheng Shang, Yun Lu"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13925v1"
tags: ["query:q12"]
score: 10.0
evidence: 使VLA模型符合机器人操作的物理约束
tldr: SmoothVLA是一个针对机器人操作的视觉-语言-动作（VLA）模型微调框架。针对强化学习微调中常见的轨迹抖动和违反物理约束问题，该研究提出了一种结合任务奖励与轨迹加加速度（Jerk）惩罚的内在平滑度优化方法。通过GRPO算法，SmoothVLA在提升任务成功率的同时，显著增强了动作的平滑性与泛化能力，为对齐物理世界约束提供了新思路。
selection_source: fresh_fetch
motivation: 现有的VLA模型在强化学习微调过程中，往往会产生剧烈抖动且违反物理约束的动作轨迹。
method: 提出一种基于物理信息的混合奖励函数，结合任务稀疏奖励与基于轨迹加加速度的内在平滑奖励，并利用GRPO算法进行优化。
result: "在LIBERO基准测试中，SmoothVLA在平滑度上比标准强化学习提升了13.8%，且在多任务泛化性上显著优于监督微调。"
conclusion: 通过内在奖励优化平滑度，可以有效实现VLA模型与物理世界约束的对齐，提升机器人控制的稳定性和可行性。
---

## 摘要
视觉-语言-动作（VLA）模型已成为机器人操控的一种强大范式。然而，现有的后训练方法在稳定性和探索之间面临困境：有监督微调（SFT）受限于演示质量且缺乏泛化能力，而强化学习（RL）虽然提高了探索能力，但往往会产生不稳定、抖动的轨迹，从而违反物理约束。为了弥补这一差距，我们提出了 SmoothVLA，这是一种新型的强化学习微调框架，能够协同优化任务性能和运动平滑度。其技术核心是一个受物理启发的混合奖励函数，该函数将二值稀疏任务奖励与源自轨迹加加速度（jerk）的连续稠密项相结合。至关重要的是，这种奖励是内在的，直接从策略展开（rollouts）中计算得出，无需外部环境反馈或繁琐的奖励工程。利用群体相对策略优化（GRPO），SmoothVLA 将轨迹平滑度确立为显式的优化先验，引导模型实现物理可行且稳定的控制。在 LIBERO 基准测试上的广泛实验表明，SmoothVLA 在平滑度上比标准 RL 提高了 13.8%，并且在多种任务的泛化性上显著超过了 SFT。我们的工作为通过内在奖励优化使 VLA 模型与物理世界约束对齐提供了一种可扩展的方法。

## Abstract
Vision-Language-Action (VLA) models have emerged as a powerful paradigm for robotic manipulation. However, existing post-training methods face a dilemma between stability and exploration: Supervised Fine-Tuning (SFT) is constrained by demonstration quality and lacks generalization, whereas Reinforcement Learning (RL) improves exploration but often induces erratic, jittery trajectories that violate physical constraints. To bridge this gap, we propose SmoothVLA, a novel reinforcement learning fine-tuning framework that synergistically optimizes task performance and motion smoothness. The technical core is a physics-informed hybrid reward function that integrates binary sparse task rewards with a continuous dense term derived from trajectory jerk. Crucially, this reward is intrinsic, that computing directly from policy rollouts, without requiring extrinsic environment feedback or laborious reward engineering. Leveraging the Group Relative Policy Optimization (GRPO), SmoothVLA establishes trajectory smoothness as an explicit optimization prior, guiding the model toward physically feasible and stable control. Extensive experiments on the LIBERO benchmark demonstrate that SmoothVLA outperforms standard RL by 13.8\% in smoothness and significantly surpasses SFT in generalization across diverse tasks. Our work offers a scalable approach to aligning VLA models with physical-world constraints through intrinsic reward optimization.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SmoothVLA**，一个旨在解决视觉-语言-动作（VLA）模型在强化学习微调过程中产生的“动作抖动”问题的框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 VLA 模型在从模拟环境转向物理世界时，面临“探索与稳定性”的悖论。
    *   **SFT（有监督微调）**：受限于演示数据的质量和多样性，泛化能力差。
    *   **RL（强化学习）**：虽然增强了探索能力，但由于奖励稀疏和随机搜索，往往产生高频振荡、不连续的轨迹（即“抖动”）。
*   **研究动机**：在物理世界中，轨迹平滑度（Smoothness）不仅是美学要求，更是功能性要求。剧烈抖动会导致机械磨损、控制失效及安全性降低。论文旨在通过引入物理约束，使 VLA 模型生成的动作既能完成任务又符合物理规律。

### 2. 方法论
*   **核心思想**：将物理学中的**加加速度（Jerk，加速度对时间的导数）**作为内在奖励项，引入强化学习的优化目标中，引导模型在成功完成任务的前提下，选择最平滑的动作路径。
*   **关键技术细节**：
    *   **物理信息混合奖励函数**：设计了一个公式 $R(\tau) = I_{success} \cdot (1 - \lambda \cdot \text{mean}(\text{Jerk}^2))$。其中 $I_{success}$ 是二值任务成功奖励，后半部分是基于轨迹 Jerk 的惩罚项。
    *   **内在奖励优化**：该奖励是“内在”的，直接从策略生成的动作序列和本体感受反馈中计算，无需外部传感器或复杂的环境建模。
    *   **GRPO 算法**：采用群体相对策略优化（Group Relative Policy Optimization）。通过对同一指令采样一组轨迹，并在组内进行奖励归一化，促使模型在多个成功的尝试中“卷”出最平滑的那一个。
    *   **微分运动学**：通过雅可比矩阵及其高阶导数，将关节空间的运动映射到操作空间，精确计算末端执行器的 Jerk。

### 3. 实验设计
*   **基准测试（Benchmark）**：
    *   **LIBERO**：包含 Goal、Spatial、Object、Long 四个子集，涵盖多种语言引导的机器人操控任务。
    *   **LIBERO-Plus**：用于评估鲁棒性，包含语言指令改写、光照变化、背景纹理变化和物体布局改变等扰动。
*   **对比方法**：
    *   **OpenVLA-SFT**：纯监督微调基准。
    *   **OpenVLA-RL / Octo**：标准的强化学习微调方法。
    *   **DPO / GRPO 变体**：对比了引入平滑奖励前后的不同偏好优化算法。

### 4. 资源与算力
*   **模型架构**：以 **OpenVLA** 为骨干网络，使用 **LoRA** 进行高效微调。
*   **优化器**：AdamW。
*   **超参数**：SFT 阶段学习率 $4 \times 10^{-5}$，RL 阶段 $2 \times 10^{-5}$，Batch Size 为 16，平滑权重 $\lambda = 0.2$。
*   **算力说明**：文中提到所有实验在相同的硬件环境下进行以确保公平，但**未明确给出具体的 GPU 型号、数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 LIBERO 的 5 个任务套件上进行了大规模测试。
    *   针对 5 种环境扰动（语言、光照、背景、布局等）进行了鲁棒性验证。
    *   **消融实验**：对比了二值奖励（$R_{binary}$）、随机奖励（$R_{random}$）和物理平滑奖励（$R_{smooth}$）的效果。
*   **充分性评价**：实验设计较为充分。每个设置独立运行 5 次并取平均值，具有统计学意义。通过定性（轨迹可视化）和定量（Jerk 指标）双重分析，客观地证明了平滑度与成功率的正相关性。

### 6. 主要结论与发现
*   **平滑度显著提升**：相比标准 RL 方法，SmoothVLA 的轨迹平滑度提升了 **13.8%**。
*   **泛化与鲁棒性增强**：在面对分布外（OOD）的扰动（如光照或布局改变）时，SmoothVLA 的成功率显著高于 SFT（平均高出 24.2%）和标准 RL。
*   **性能平衡**：证明了优化平滑度不仅不会抑制探索，反而能通过减少无效的剧烈动作来提高任务执行的稳定性，从而提升最终成功率。

### 7. 优点（亮点）
*   **物理对齐**：首次在 VLA 模型微调中正式定义并解决了“探索-稳定性悖论”。
*   **无需外部反馈**：利用 Jerk 作为内在奖励，实现了反馈自主性，降低了对环境建模的依赖。
*   **通用性强**：作为一种后训练协议，它可以作为插件应用于不同的 VLA 架构和 RL 算法（如 DPO、GRPO）。

### 8. 不足与局限
*   **模拟器依赖**：实验主要在 LIBERO 模拟环境中完成，虽然考虑了物理约束，但在真实物理机器人上的部署表现（如应对真实电机的非线性特性）仍需进一步验证。
*   **超参数敏感性**：平滑权重 $\lambda$ 目前是固定值（0.2），对于不同动力学特性的机器人或不同精细度的任务，可能需要动态调整。
*   **安全性覆盖有限**：目前主要关注运动平滑度，尚未涉及碰撞规避、接触力控制等更复杂的物理安全约束。

（完）

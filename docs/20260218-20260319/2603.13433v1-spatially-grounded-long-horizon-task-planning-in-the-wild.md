---
title: Spatially Grounded Long-Horizon Task Planning in the Wild
title_zh: 真实场景下的空间定位长程任务规划
authors: "Sehun Jung, HyunJee Song, Dong-Hee Kim, Reuben Tan, Jianfeng Gao, Yong Jae Lee, Donghyun Kim"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13433v1"
tags: ["query:q14"]
score: 9.0
evidence: 针对机械臂操作中空间对齐长程动作规划的基准测试
tldr: 针对当前视觉语言模型在机器人长程任务规划中缺乏空间可执行性评估的问题，本文提出了GroundedPlanBench基准测试，用于同时评估子动作规划与空间动作定位。此外，开发了V2GP自动化数据生成框架，利用真实机器人视频提升模型的空间感知规划能力，并通过实验证明了该方法在提升规划准确性和空间定位方面的有效性。
selection_source: fresh_fetch
motivation: 现有的机器人任务规划基准缺乏对空间可执行性的评估，导致模型生成的指令难以在真实物理空间中准确执行。
method: 提出了GroundedPlanBench基准和V2GP框架，通过从机器人视频中自动提取数据来增强模型的分层子动作规划与空间定位能力。
result: 实验表明当前VLM在空间感知的长程规划上存在瓶颈，而V2GP显著提升了模型在基准测试和真实机器人实验中的表现。
conclusion: 该研究通过引入空间定位评估和自动化训练框架，为实现可直接用于机器人操作的空间感知规划迈出了重要一步。
---

## 摘要
机器人操作的最新进展越来越多地利用视觉语言模型（VLM）进行高层推理，例如将任务指令分解为以自然语言表达的序列动作计划，以指导下游的底层电机执行。然而，当前的基准测试并未评估这些计划是否在空间上可执行，特别是在指定机器人执行计划时应进行交互的确切空间位置方面，这限制了对真实世界操作能力的评估。为了弥补这一差距，我们定义了一项名为定位规划（grounded planning）的新任务，并引入了 GroundedPlanBench，这是一个新策划的用于真实场景下空间定位长程动作规划的基准测试。GroundedPlanBench 联合评估了分层子动作规划和空间动作定位（即在何处执行动作），从而能够系统地评估生成的子动作对于机器人操作是否在空间上可执行。我们进一步引入了视频到空间定位规划（V2GP），这是一个利用真实世界机器人视频演示来改进空间定位长程规划的自动化数据生成框架。我们的评估表明，空间定位的长程规划仍然是当前 VLM 的主要瓶颈。实验结果证明，V2GP 为提高动作规划和空间定位性能提供了一种有效的方法，该方法在我们的基准测试以及真实世界的机器人操作实验中均得到了验证，推动了空间可执行规划的进展。

## Abstract
Recent advances in robot manipulation increasingly leverage Vision-Language Models (VLMs) for high-level reasoning, such as decomposing task instructions into sequential action plans expressed in natural language that guide downstream low-level motor execution. However, current benchmarks do not assess whether these plans are spatially executable, particularly in specifying the exact spatial locations where the robot should interact to execute the plan, limiting evaluation of real-world manipulation capability. To bridge this gap, we define a novel task of grounded planning and introduce GroundedPlanBench, a newly curated benchmark for spatially grounded long-horizon action planning in the wild. GroundedPlanBench jointly evaluates hierarchical sub-action planning and spatial action grounding (where to act), enabling systematic assessment of whether generated sub-actions are spatially executable for robot manipulation. We further introduce Video-to-Spatially Grounded Planning (V2GP), an automated data generation framework that leverages real-world robot video demonstrations to improve spatially grounded long-horizon planning. Our evaluations reveal that spatially grounded long-horizon planning remains a major bottleneck for current VLMs. Our results demonstrate that V2GP provides a promising approach for improving both action planning and spatial grounding performance, validated on our benchmark as well as through real-world robot manipulation experiments, advancing progress toward spatially actionable planning.

---

## 论文详细总结（自动生成）

以下是对论文《Spatially Grounded Long-Horizon Task Planning in the Wild》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视觉语言模型（VLM）在机器人任务规划中虽能生成自然语言步骤，但往往缺乏**空间可执行性**。即模型知道“做什么”，但不知道“在哪里做”，导致生成的计划在物理空间中存在歧义或幻觉，难以直接指导底层电机执行。
*   **研究背景**：现有的基准测试多基于模拟器，缺乏真实世界多样性，且大多只评估抽象规划或单步空间推理，未能将长程（Long-Horizon）任务分解与精确的空间坐标定位结合起来。

### 2. 论文提出的方法论
论文提出了两个核心贡献：**GroundedPlanBench**（基准测试）和 **V2GP**（训练框架）。

*   **GroundedPlanBench**：
    *   定义了“定位规划”（Grounded Planning）任务：输入图像和高层指令，输出带空间坐标（边界框 bbox 和点 point）的子动作序列。
    *   涵盖了 `open`, `grasp`, `place`, `close` 四种原子动作。
*   **V2GP (Video-to-Spatially Grounded Planning) 框架**：
    1.  **时间子动作分解**：利用机器人夹持器（gripper）的开合信号，将长视频自动分割成多个操作片段。
    2.  **交互物体识别**：调用 VLM 分析每个片段，提取被操作物体的详细视觉描述和语义类别。
    3.  **动作空间定位**：使用 SAM3 模型，结合物体描述，在视频中跟踪并提取抓取（bbox）和放置（point）的精确 2D 坐标。
    4.  **空间定位任务规划生成**：将上述信息整合，并利用大模型（如 Gemini）将显式指令改写为隐式/抽象指令，构建包含 43K 样本的训练集，用于微调 VLM（如 Qwen3-VL）。

### 3. 实验设计
*   **数据集与场景**：基于 DROID 真实世界机器人数据集，涵盖 564 个场景和 86 个任务。
*   **Benchmark 构成**：GroundedPlanBench 包含 308 个真实视频，衍生出 1009 个评估回合，分为短程（1-4 步）、中程（5-8 步）和长程（9-26 步）任务，并区分显式和隐式指令。
*   **对比方法**：
    *   **端到端模型**：Gemini-3-Flash, Gemini-2.5-Flash, Qwen3-VL, InternVL3.5。
    *   **解耦方法**：GPT-5.2/Qwen3 + 专门的定位模型（如 SAM3, Embodied-R1）。
*   **评估指标**：动作召回率（ARR）和任务成功率（TSR，要求全序列动作及空间定位均正确）。

### 4. 资源与算力
*   **模型微调**：使用了 **LoRA (Low-Rank Adaptation)** 技术对 Qwen3-VL-4B 和 32B 版本进行微调。
*   **算力细节**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。但提到训练集规模为 43,000 个样本。

### 5. 实验数量与充分性
*   **实验规模**：进行了 1000+ 次评估回合，涵盖了不同长度的动作序列和指令类型。
*   **消融与对比**：对比了闭源最强模型与开源模型，并对比了“端到端生成”与“规划+定位解耦”两种范式。
*   **真实机器人验证**：在 Franka Research 3 机器人上进行了 10 项真实任务测试，验证了生成计划的可执行性。
*   **充分性评价**：实验设计较为全面，通过多维度（时长、指令抽象度）的对比，客观揭示了当前 VLM 在长程空间规划上的短板。

### 6. 论文的主要结论与发现
*   **性能瓶颈**：长程规划和隐式指令仍然是当前 VLM 的主要挑战，即使是强力模型（如 Gemini-3-Flash）在长程任务上的成功率也显著下降。
*   **空间定位缺失**：传统的“解耦式”方法（先规划文字再定位）容易因文字描述歧义导致定位失败。
*   **V2GP 的有效性**：通过 V2GP 框架微调后的 Qwen3-VL-4B 在短程显式任务上 TSR 从 39.5% 提升至 58.2%，甚至超过了未微调的 32B 模型。
*   **开源潜力**：经过针对性数据增强，中等规模的开源模型可以在特定机器人规划任务上逼近闭源顶尖模型的表现。

### 7. 优点（亮点）
*   **自动化数据闭环**：V2GP 实现了从原始机器人演示视频到“指令-动作-坐标”训练数据的自动化转换，极大降低了标注成本。
*   **基准测试的现实意义**：GroundedPlanBench 填补了真实场景下“空间定位+长程规划”联合评估的空白。
*   **软硬件结合**：不仅有离线指标，还通过实机实验证明了 2D 空间输出可以有效转化为 3D 物理动作。

### 8. 不足与局限
*   **信号噪声敏感性**：子动作分解高度依赖夹持器的信号，若信号有噪声或操作失败（如掉落），会导致数据标注错误。
*   **幻觉风险**：数据生成过程中依赖其他基础模型（如 SAM3 或 Gemini）进行标注，可能引入“模型标注模型”带来的累积幻觉。
*   **长程数据稀缺**：尽管有自动化框架，但高质量、极长程（20 步以上）的真实机器人演示数据在原始数据集中仍然相对较少。
*   **动态环境限制**：目前的规划主要基于初始帧图像，对于执行过程中环境发生剧烈变化的动态场景处理能力有限。

（完）

---
title: "DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference"
title_zh: DepthCache：用于视觉-语言-动作模型推理的深度引导免训练视觉 Token 合并
authors: "Yuquan Li, Lianjie Ma, Han Ding, Lijun Zhu"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10469v1"
tags: ["query:q12"]
score: 10.0
evidence: 机器人操作中的视觉-语言-动作 (VLA) 模型推理优化
tldr: DepthCache是一个无需训练的视觉Token合并框架，旨在加速视觉-语言-动作（VLA）模型的推理。针对现有方法在空间推理上的不足，它利用深度信息作为结构先验，对近景工作区和远景背景实施差异化压缩，并结合时间冗余和运动自适应策略优化计算。在多个VLA模型上实现了1.28倍的加速，且成功率几乎无损，显著提升了机器人闭环控制的响应速度。
selection_source: fresh_fetch
motivation: 现有的视觉Token压缩方法因均匀处理Token而破坏了机器人控制所需的空间推理能力，导致推理延迟高且性能下降。
method: 提出利用深度信息进行空间差异化合并，并结合跨帧分布合并与运动自适应流水线来减少冗余计算。
result: 在LIBERO基准测试中，DepthCache在保持成功率几乎不变的情况下实现了1.28倍的推理加速，优于现有的剪枝和合并基准。
conclusion: DepthCache提供了一种通用且高效的VLA推理加速方案，在不改变模型结构的前提下显著提升了机器人的实时操作性能。
---

## 摘要
视觉-语言-动作（VLA）模型实现了通用的机器人操控，但面临高推理延迟的问题。这一瓶颈源于大型语言骨干网络处理的海量视觉 Token。现有方法通常采用均匀的 Token 剪枝或合并，这会损害机器人控制中至关重要的空间推理能力。我们提出了 DepthCache，这是一个利用深度信息作为结构先验进行视觉 Token 压缩的免训练框架。它根据深度将观测结果划分为不同区域，并应用空间差异化的合并比例，在压缩远景背景的同时保留近场工作空间。为了利用时间冗余，DepthCache 将合并过程分散到连续帧中，在减少每步计算量的同时确保表示的一致性。此外，运动自适应流水线根据末端执行器的动力学进一步优化了辅助视角的压缩。该框架无需修改模型，可推广至多种 VLA 架构。在 LIBERO 基准测试中，DepthCache 在三种 VLA 模型（pi_0.5、OpenVLA、GR00T）上实现了高达 1.28 倍的推理加速，且平均成功率下降不到 1%，而剪枝和合并基准方法在同等压缩率下会导致 4-24% 的性能下降。在物理机械臂上的真实世界实验表明，DepthCache 在延迟敏感场景中实现了更快的任务吞吐量和更灵敏的闭环控制。

## Abstract
Vision-Language-Action (VLA) models enable generalist robotic manipulation but suffer from high inference latency. This bottleneck stems from the massive number of visual tokens processed by large language backbones. Existing methods either prune or merge tokens uniformly, degrading the spatial reasoning essential for robotic control. We present DepthCache, a training-free framework that leverages depth as a structural prior for visual token compression. It partitions observations into depth-based regions and applies spatially differentiated merge ratios, preserving the near-field workspace while compressing the distant background. To exploit temporal redundancy, DepthCache distributes the merging process across consecutive frames, ensuring consistent representations while reducing per-step computation. A motion-adaptive pipeline further optimizes auxiliary view compression based on end-effector dynamics. The framework requires no model modification, generalizing across diverse VLA architectures. On the LIBERO benchmark, DepthCache achieves up to 1.28x inference speedup with less than 1% average success rate degradation across three VLA models (pi_0.5, OpenVLA, GR00T), whereas pruning and merging baselines incur 4--24% degradation at comparable compression. Real-world experiments on a physical manipulator demonstrate that DepthCache enables faster task throughput and more responsive closed-loop control in latency-sensitive scenarios.

---

## 论文详细总结（自动生成）

以下是对论文《DepthCache: Depth-Guided Training-Free Visual Token Merging for Vision-Language-Action Model Inference》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型在机器人操控中表现优异，但由于需要处理海量的视觉 Token（每个视角数百个），导致推理延迟极高，难以满足实时闭环控制的需求。
*   **研究动机**：
    *   **现有方法的缺陷**：现有的 Token 剪枝（Pruning）会破坏空间连续性，导致精细操作失败；而现有的 Token 合并（Merging）通常采用均匀比例，忽略了工作区（近景）与背景（远景）的重要性差异。
    *   **生物学启发**：人类视觉在抓取时会集中注意力于目标区域（高分辨率），而对周边区域进行压缩（低分辨率）但并不丢弃。
    *   **核心思路**：利用机器人场景中天然存在的**深度信息**作为结构先验，实现空间差异化、时间连续且无需训练的 Token 压缩。

### 2. 方法论
DepthCache 是一个免训练的框架，包含两个针对不同视角的流水线：
*   **主视角流水线（第三人称视角）**：
    *   **双重保护机制**：在初始化阶段，结合 LLM 的**跨模态注意力**（语义保护）和**深度梯度边缘检测**（几何保护），识别并锁定任务关键 Token 不参与压缩。
    *   **基于深度的区域划分**：利用 K-Means 对非保护 Token 按深度聚类，根据距离远近分配差异化合并率（远景压缩狠，近景保留多）。
    *   **渐进式 Token 合并**：为了避免动作突变，将合并过程分散到连续的 $W$ 帧中逐步完成，确保视觉特征转换的平滑性。
    *   **动态重置**：监测深度变化，若场景发生显著变动（如物体被移动），则触发重新初始化。
*   **辅助视角流水线（腕部摄像头）**：
    *   **运动自适应状态机**：根据机械臂末端执行器的动力学状态切换模式。在快速移动（Transit）时大幅压缩，在精细操作（Fine Manipulation）阶段恢复全分辨率。
*   **通用性**：该方法运行在视觉编码器之外，无需修改模型权重，适用于任何接受视觉 Token 输入的 VLA 架构。

### 3. 实验设计
*   **数据集与基准**：
    *   **仿真环境**：使用 **LIBERO** 基准测试，涵盖 Spatial、Object、Goal、Long 四个套件，共 40 个任务。
    *   **真实世界**：使用 PIPER 6 自由度机械臂，执行抓取放置、叠方块、开抽屉等任务。
*   **对比模型**：
    *   **VLA 骨干网络**：$\pi_{0.5}$ (3.3B)、OpenVLA (7B)、GR00T (2.2B)。
    *   **对比方法**：FastV（剪枝）、SP-VLA（动作感知剪枝）、ToSA（带空间意识的均匀合并）。
*   **评估指标**：成功率（SR）、推理延迟（ms）、加速比（Speedup）、Token 保留率（$\rho$）。

### 4. 资源与算力
*   **硬件环境**：所有实验（仿真与实物）均在单张 **NVIDIA RTX 4090 GPU (24GB)** 上运行。
*   **训练时长**：由于是 **Training-free（免训练）** 方法，该框架不需要任何训练时间，直接在推理阶段插入使用。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验涵盖 40 个任务，每个任务评估 100 个回合，总计 4000 次测试，确保了统计意义。
    *   跨越了三种完全不同的 VLA 架构，验证了方法的通用性。
    *   进行了详细的**消融实验**（针对深度划分、双重保护、渐进合并等 5 个组件）和**参数敏感性分析**。
*   **客观性**：对比了当前最先进的剪枝和合并基准，实验设计考虑了时间连续性和空间完整性，评价维度全面。

### 6. 主要结论与发现
*   **性能保持**：在三种模型上实现了 **1.07x - 1.28x** 的推理加速，而平均成功率下降**不到 1%**。
*   **优于基准**：相比之下，剪枝方法（如 FastV）在同等压缩下会导致 4%-24% 的性能崩溃，证明了“合并”优于“剪枝”。
*   **实时性优势**：在真实世界中，DepthCache 显著缩短了任务完成总时间（提升约 22.7%），并增强了机器人在受到外部扰动时的恢复能力（响应更快）。

### 7. 优点与亮点
*   **免训练与即插即用**：无需昂贵的微调过程，极易部署到现有 VLA 模型中。
*   **结构先验的巧妙利用**：首次将深度信息从“感知输入”转变为“推理优化先验”，解决了均匀压缩破坏空间逻辑的问题。
*   **时间平滑性**：通过渐进式合并解决了 Token 减少带来的动作抖动问题，这在机器人控制中至关重要。

### 8. 不足与局限
*   **加速上限受限**：根据阿姆达尔定律（Amdahl's Law），该方法仅优化了视觉 Token 数量，无法加速 LLM 骨干网络本身的计算或动作解码阶段（如扩散模型的去噪过程）。
*   **依赖深度传感器**：虽然 RGB-D 摄像头很常见，但在仅有 RGB 输入的极端简陋硬件上无法直接应用。
*   **场景局限**：目前的评估主要集中在桌面操控任务，对于更广阔的户外场景或高速动态场景的适用性仍需进一步验证。

（完）

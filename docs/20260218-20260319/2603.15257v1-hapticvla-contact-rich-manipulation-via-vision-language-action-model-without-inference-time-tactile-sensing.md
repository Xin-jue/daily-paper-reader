---
title: "HapticVLA: Contact-Rich Manipulation via Vision-Language-Action Model without Inference-Time Tactile Sensing"
title_zh: HapticVLA：无需推理时触觉感知的视觉-语言-动作模型高接触操作
authors: "Konstantin Gubernatorov, Mikhail Sannikov, Ilya Mikhalchuk, Egor Kuznetsov, Makar Artemov, Ogunwoye Faith Ouwatobi, Marcelino Fernando, Artem Asanov, Ziang Guo, Dzmitry Tsetserukou"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15257v1"
tags: ["query:q12"]
score: 10.0
evidence: 用于高频接触操作任务的HapticVLA架构
tldr: HapticVLA 提出了一种无需推理时触觉传感器的视觉-语言-动作模型，用于处理富接触操作任务。该方法通过两阶段训练：首先利用安全感知奖励加权流匹配（SA-RWFM）训练具备触觉意识的专家模型，随后通过触觉蒸馏（TD）将该能力迁移至常规 VLA，使其能仅凭视觉和状态预测触觉特征。实验表明，该模型在真实场景中表现优异，超越了直接使用触觉反馈的基准模型。
selection_source: fresh_fetch
motivation: 触觉传感器硬件成本高且难以在不同机器人平台间复用，限制了具备触觉感知能力的 VLA 模型的广泛应用。
method: 采用两阶段法，先通过包含触觉奖励的流匹配训练专家模型，再将其触觉推理能力蒸馏到仅依赖视觉和状态输入的 VLA 模型中。
result: "在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，显著优于包括推理时带有直接触觉反馈在内的各类基准模型。"
conclusion: 该研究证明了可以通过离线学习和蒸馏技术，在不依赖实时触觉硬件的情况下，赋予机器人模型复杂的富接触操作和安全感知能力。
---

## 摘要
触觉感知是视觉-语言-动作（VLA）架构的一项关键能力，因为它能在高接触任务中实现灵巧且安全的操作。然而，对专用触觉硬件的依赖增加了成本，并降低了在不同机器人平台上的可复现性。我们认为，触觉感知操作可以进行离线学习，并在推理时无需直接触觉反馈即可部署。为此，我们提出了 HapticVLA，它包含两个紧密耦合的阶段：安全感知奖励加权流匹配（SA-RWFM）和触觉蒸馏（TD）。SA-RWFM 训练了一个流匹配动作专家，该专家结合了预计算的安全感知触觉奖励，对过大的抓取力和次优的抓取轨迹进行惩罚。TD 进一步将这种触觉感知能力转移到常规 VLA 中：我们从 SA-RWFM 教师模型中蒸馏出一个紧凑的触觉 token，并训练一个学生 VLA 从视觉和状态模态中预测该 token，从而在推理时无需车载触觉传感器即可生成触觉感知动作。这种设计在 VLA 中保留了高接触触觉感知推理，同时在部署期间消除了对车载触觉传感器的需求。在真实世界实验中，HapticVLA 达到了 86.7% 的平均成功率，始终优于基准 VLA——包括在推理期间提供直接触觉反馈的版本。

## Abstract
Tactile sensing is a crucial capability for Vision-Language-Action (VLA) architectures, as it enables dexterous and safe manipulation in contact-rich tasks. However, reliance on dedicated tactile hardware increases cost and reduces reproducibility across robotic platforms. We argue that tactile-aware manipulation can be learned offline and deployed without direct haptic feedback at inference. To this end, we present HapticVLA, which proceeds in two tightly coupled stages: Safety-Aware Reward-Weighted Flow Matching (SA-RWFM) and Tactile Distillation (TD). SA-RWFM trains a flow-matching action expert that incorporates precomputed, safety-aware tactile rewards penalizing excessive grasping force and suboptimal grasping trajectories. TD further transfers this tactile-aware capability into a conventional VLA: we distill a compact tactile token from the SA-RWFM teacher and train a student VLA to predict that token from vision and state modalities, enabling tactile-aware action generation at inference without requiring on-board tactile sensors. This design preserves contact-rich tactile-aware reasoning within VLA while removing the need for on-board tactile sensors during deployment. On real-world experiments, HapticVLA achieves a mean success rate of 86.7%, consistently outperforming baseline VLAs - including versions provided with direct tactile feedback during inference.

---

## 论文详细总结（自动生成）

### HapticVLA 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖实时触觉传感器硬件的情况下，赋予视觉-语言-动作（VLA）模型处理“高接触（Contact-Rich）”任务（如抓取易碎品）的能力？
*   **研究动机**：触觉感知对安全操作至关重要，但专用触觉硬件成本高、跨平台复用性差。本文旨在通过“离线学习触觉知识，在线仅凭视觉推理”的思路，解决 VLA 模型在处理软性或易碎物体时缺乏力觉感知、容易损坏物体的问题。

#### 2. 方法论：核心思想与关键技术
HapticVLA 采用两阶段训练框架：
*   **阶段一：安全感知奖励加权流匹配（SA-RWFM）**
    *   **触觉奖励计算**：利用离线采集的触觉图（Tactile Maps），计算平均力、峰值压力、压力集中度及滑动检测。定义安全奖励函数，对过大抓取力、非对称受力及滑动进行惩罚。
    *   **专家模型训练**：在流匹配（Flow Matching）目标函数中引入奖励权重。通过“优势得分（Advantage-like score）”对高质量样本进行加权，训练出一个具备触觉意识的动作专家（Teacher）。
    *   **锚点正则化**：引入正则化项约束参数漂移，防止生成式策略在加权训练中发生模式崩溃。
*   **阶段二：触觉蒸馏（TD）**
    *   **离线目标生成**：使用训练好的专家模型在全量数据集上生成“带触觉意识”的动作预测作为软目标。
    *   **学生模型初始化**：将专家模型的视觉和语言权重迁移至学生模型，但移除触觉输入层。
    *   **混合目标训练**：学生模型通过混合真实演示（Ground-truth）和专家预测（Teacher predictions）进行训练，从而在仅输入视觉和机器人状态的情况下，学习到隐含的控力逻辑。

#### 3. 实验设计
*   **硬件平台**：双臂 LeRobot SO-101 机器人。右臂配备高密度触觉阵列（每指 100 个触点）。
*   **实验任务**：
    1.  **果酱罐放置**：处理塑料罐的形变。
    2.  **华夫饼放置**：极易碎，需精确控力。
    3.  **鸡蛋放置**：最高难度，需精准调节抓取宽度和力度。
*   **数据集**：包含 310 个真实世界回合（含成功与故意损坏物体的失败样本）以及 1000 个数字孪生（Isaac Sim）生成的仿真样本。
*   **对比基准（Benchmarks）**：SmolVLA（基础模型）、X-VLA、VLA-0。

#### 4. 资源与算力
*   **推理端**：部署在 **NVIDIA Jetson Orin NX (16GB)** 边缘计算设备上。
*   **训练端**：论文未明确说明训练所使用的具体 GPU 型号、数量及总训练时长。但提到使用了异步推理优化以适应边缘端部署。

#### 5. 实验数量与充分性
*   **实验规模**：每个任务进行了 20 次独立测试，总计 60 次真实世界评估。
*   **消融实验**：针对“有无触觉蒸馏（TD）”和“同步/异步推理”进行了对比实验。
*   **充分性评价**：实验设计较为客观，涵盖了从易变形到极易碎的不同梯度任务。通过对比包含实时触觉反馈的基准模型，证明了蒸馏方法的优越性。然而，样本总量（310个真实回合）在深度学习领域仍属于较小规模。

#### 6. 主要结论与发现
*   **性能卓越**：HapticVLA 达到了 **86.7%** 的平均成功率，远超基础 SmolVLA（仅 41.7%）。
*   **无需传感器推理**：通过蒸馏，模型在没有触觉传感器的情况下，在鸡蛋抓取任务中比基础模型提升了 **45%** 的成功率。
*   **超越实时反馈**：实验发现，经过蒸馏的学生模型有时甚至优于直接输入原始触觉信号的模型，这可能是因为蒸馏过程过滤了触觉信号中的噪声。

#### 7. 优点与亮点
*   **硬件解耦**：最大的亮点在于实现了“触觉能力的软件化”，降低了高精度操作对昂贵硬件的依赖。
*   **安全感知**：通过 SA-RWFM 显式引入安全奖励，使 VLA 模型具备了初步的“物理常识”（如知道物体会碎）。
*   **部署友好**：模型轻量化（基于 SmolVLA），且无需在部署现场安装复杂的触觉采集系统。

#### 8. 不足与局限
*   **任务泛化性**：实验仅限于三个特定的拾取任务，尚未验证在更复杂的长程逻辑任务中的表现。
*   **数据依赖**：虽然推理不需要传感器，但训练阶段仍需高质量的触觉标注数据，数据采集成本依然存在。
*   **算力细节缺失**：未披露训练资源消耗，难以评估该方案的训练成本。
*   **时间对齐风险**：消融实验显示异步推理可能导致性能下降，说明模型对视觉-动作的时间延迟较为敏感。

（完）

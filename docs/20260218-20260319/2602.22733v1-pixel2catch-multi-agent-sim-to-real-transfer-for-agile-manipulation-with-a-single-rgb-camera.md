---
title: "Pixel2Catch: Multi-Agent Sim-to-Real Transfer for Agile Manipulation with a Single RGB Camera"
title_zh: Pixel2Catch：利用单 RGB 摄像头进行敏捷操作的多智能体虚实迁移
authors: "Seongyong Kim, Junhyeon Cho, Kang-Won Lee, Soo-Chul Lim"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.22733v1"
tags: ["query:q8"]
score: 9.0
evidence: 敏捷操作的虚实迁移
tldr: 本研究提出Pixel2Catch框架，旨在利用单目RGB摄像头实现机器人对抛掷物体的敏捷抓取。该方法不依赖显式的3D位置估计，而是通过像素级视觉信息捕捉物体的位置和尺度变化。为解决高自由度机械臂与多指手系统的学习稳定性问题，设计了异构多智能体强化学习框架，将臂与手定义为具有独立角色和奖励的智能体。实验证明该策略能成功从仿真环境迁移至现实世界，实现了高效的敏捷操纵。
selection_source: fresh_fetch
motivation: 旨在克服传统3D位置估计的复杂性，利用单目RGB图像的像素级视觉线索实现快速且准确的物体抓取。
method: 采用异构多智能体强化学习框架，将机械臂和多指手视为独立智能体，并基于角色分配特定的观测和奖励进行协同训练。
result: 成功在仿真中训练出抓取策略，并顺利迁移至真实世界，实现了对动态抛掷物体的稳定捕捉。
conclusion: 像素级视觉感知与多智能体协同控制相结合，为高自由度机器人系统的敏捷操纵提供了一种有效的Sim-to-Real解决方案。
---

## 摘要
为了接住抛出的物体，机器人必须能够感知物体的运动并及时生成控制动作。本研究并非显式估计物体的三维位置，而是专注于一种新颖的方法，即利用从单张 RGB 图像中提取的像素级视觉信息来识别物体运动。这些视觉线索捕捉了物体位置和比例的变化，使策略能够对物体的运动进行推理。此外，为了在由配备多指手的机械臂组成的高自由度（high-DoF）系统中实现稳定学习，我们设计了一个异构多智能体强化学习框架，将机械臂和手定义为具有不同角色的独立智能体。每个智能体都使用特定角色的观测和奖励进行协作训练，所学策略已成功从仿真环境迁移到现实世界。

## Abstract
To catch a thrown object, a robot must be able to perceive the object's motion and generate control actions in a timely manner. Rather than explicitly estimating the object's 3D position, this work focuses on a novel approach that recognizes object motion using pixel-level visual information extracted from a single RGB image. Such visual cues capture changes in the object's position and scale, allowing the policy to reason about the object's motion. Furthermore, to achieve stable learning in a high-DoF system composed of a robot arm equipped with a multi-fingered hand, we design a heterogeneous multi-agent reinforcement learning framework that defines the arm and hand as independent agents with distinct roles. Each agent is trained cooperatively using role-specific observations and rewards, and the learned policies are successfully transferred from simulation to the real world.

---

## 论文详细总结（自动生成）

### 论文总结：Pixel2Catch - 基于单 RGB 摄像头的多智能体敏捷抓取虚实迁移

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖昂贵的运动捕捉系统或不稳定的 3D 深度信息的情况下，仅利用单个 RGB 摄像头实现机器人对高速抛掷物体的敏捷抓取。
*   **研究动机**：
    *   **人类视觉启发**：人类接球并不依赖精确的 3D 坐标计算，而是通过观察物体在视野中位置和大小的相对变化来判断运动趋势。
    *   **克服 Sim-to-Real 鸿沟**：传统的 3D 位置估计在现实世界中受传感器噪声影响大，导致仿真训练的策略难以直接迁移。
    *   **高自由度控制难题**：机械臂结合多指手（如 Allegro Hand）具有极高的自由度，单一智能体强化学习（RL）往往难以收敛。

#### 2. 方法论
*   **核心思想**：将抓取任务分解为“视觉特征提取”与“异构多智能体协同控制”两个部分。
*   **关键技术细节**：
    *   **像素级视觉特征 ($z_{pixel}$)**：利用 **SAM 2 (Segment Anything Model 2)** 进行鲁棒的目标分割。提取物体在图像空间中的中心坐标 $(c_x, c_y)$、宽度 $w$ 和高度 $h$，以及它们在连续帧间的变化量。这些特征隐含了物体的横向移动和距离远近（缩放）。
    *   **异构多智能体强化学习 (MARL)**：将机器人系统拆分为两个独立智能体：
        *   **手臂智能体 (Arm Agent)**：负责将末端执行器移动到物体轨迹附近。
        *   **手部智能体 (Hand Agent)**：负责在物体接近时形成稳定的抓取姿态。
    *   **算法流程**：采用 **MAPPO (Multi-Agent PPO)** 框架。在训练阶段使用集中式评论家（Centralized Value Network）获取全局状态（包括特权 3D 信息），在执行阶段各智能体仅根据各自的观测和像素特征独立决策。
    *   **虚实迁移技术**：通过系统辨识（System Identification）优化仿真中的动力学参数（如摩擦力、阻尼），并结合领域随机化（Domain Randomization）增强策略的鲁棒性。

#### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：基于 NVIDIA Isaac Lab，模拟物体随机抛掷轨迹。
    *   **现实环境**：UR5e 机械臂 + Allegro 多指手 + 单个 RealSense D435 摄像头（仅使用 RGB 流）。
*   **数据集/物体**：使用了立方体、L 型块、三角形块等不同几何形状的物体，分为训练集（Seen）和测试集（Unseen）。
*   **对比方法 (Benchmarks)**：
    1.  **w/o-PF**：无像素特征，仅提供初始位置。
    2.  **S-A RL**：单智能体强化学习（臂手统一控制）。
    3.  **Only-Center**：仅使用中心点坐标特征。
    4.  **Only-WH**：仅使用宽高（尺度）特征。

#### 4. 资源与算力
*   **硬件支持**：训练使用了 **2 台 NVIDIA RTX A6000 GPU**。
*   **训练规模**：在 512 个并行仿真环境中进行训练。
*   **控制频率**：策略运行频率为 30 Hz，物理仿真频率为 120 Hz。
*   **说明**：文中未明确给出总训练时长（小时数），但提到了训练步数（约 $10^8$ 数量级）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真中对每种策略进行了 1000 次试验（100 个环境 $\times$ 10 次）。
    *   现实世界中，对每种物体进行了 30 次实际抛掷测试。
*   **充分性评价**：实验设计较为充分。通过消融实验验证了视觉特征中“中心点”与“宽高”的各自贡献，并通过对比单/多智能体架构证明了任务分解的有效性。实验涵盖了 Seen 和 Unseen 物体，体现了泛化性。

#### 6. 主要结论与发现
*   **像素特征的有效性**：仅靠图像空间的中心位移和尺度变化，足以让机器人推断出物体的 3D 运动趋势，无需显式深度估计。
*   **多智能体优势**：异构多智能体框架（MARL）在处理高自由度系统时显著优于单智能体（S-A RL），因为它降低了单个策略的学习难度，减少了奖励函数的冲突。
*   **Sim-to-Real 成功率**：该方法在现实世界中实现了约 **70% 的追踪率**和 **50% 左右的成功抓取率**，且无需任何现实数据微调。

#### 7. 优点
*   **低成本感知**：仅需普通 RGB 相机，降低了系统复杂度和硬件成本。
*   **鲁棒性强**：利用 SAM 2 提取特征，对光照变化和背景干扰具有较强的抵抗力。
*   **架构优雅**：将复杂的臂手协同问题转化为异构多智能体协作，提供了一种处理高自由度机器人控制的新思路。

#### 8. 不足与局限
*   **视角依赖**：摄像头位置相对固定（位于机器人后上方），如果物体运动超出视野或被遮挡，策略可能失效。
*   **任务单一**：目前仅针对单臂抓取，对于超大物体或需要双臂协作的场景尚未覆盖。
*   **成功率仍有提升空间**：现实世界中 50% 的抓取成功率虽然在动态任务中已属不易，但对比人类表现仍有差距，主要受限于现实中物体碰撞的复杂动力学。

（完）

---
title: "DAM-VLA: A Dynamic Action Model-Based Vision-Language-Action Framework for Robot Manipulation"
title_zh: DAM-VLA：一种基于动态动作模型的机器人操纵视觉-语言-动作框架
authors: "Xiongfeng Peng, Jiaqian Yu, Dingzhe Li, Yixiang Jin, Lu Xu, Yamin Mao, Chao Zhang, Weiming Li, Sujin Jang, Dongwook Lee, Daehyun Ji"
date: 2026-03-01
pdf: "https://arxiv.org/pdf/2603.00926v1"
tags: ["query:q12"]
score: 10.0
evidence: 集成VLM推理与扩散动作模型的VLA框架
tldr: DAM-VLA是一个基于动态动作模型的视觉-语言-动作框架，旨在解决机器人操作中通用任务适应性与精细操作精度之间的矛盾。它将预训练VLM的推理能力与专门用于机械臂和夹持器控制的扩散动作模型相结合，通过动作路由、特征融合和双尺度动作加权机制，实现了从粗略移动到精确操作的无缝衔接。在模拟和真实环境的复杂任务中表现优异。
selection_source: fresh_fetch
motivation: 现有的VLA框架在处理需要高精度的复杂操作任务时，难以兼顾通用任务的适应性与动作的精确性。
method: 该框架集成了VLM推理与扩散动作模型，并引入动作路由机制、动态特征融合以及双尺度动作加权来协调机械臂与夹持器的控制。
result: 在SIMPLER、FurnitureBench及真实世界评估中，DAM-VLA在长程和接触密集型任务上的成功率均超过了现有的先进方法。
conclusion: 通过将高层认知推理与底层动态动作模型相结合，DAM-VLA显著增强了机器人在动态环境下的泛化能力和操作精度。
---

## 摘要
在仓库、医院和家庭等动态环境中，机器人必须在粗略运动和精确操纵之间无缝切换，以完成复杂任务。然而，目前的视觉-语言-动作（VLA）框架大多改编自预训练的视觉-语言模型（VLM），往往难以在通用任务适应性与复杂操纵所需的专门精度之间取得平衡。为了应对这一挑战，我们提出了 DAM-VLA，一种基于动态动作模型的 VLA 框架。DAM-VLA 将 VLM 推理与专门用于机械臂和夹持器控制的基于扩散的动作模型相结合。具体而言，它引入了：(i) 一种动作路由机制，利用特定任务的视觉和语言线索来选择合适的动作模型（如机械臂移动或夹持器操纵）；(ii) 一种动态动作模型，融合了高层 VLM 认知与底层视觉特征以预测动作；以及 (iii) 一种双尺度动作加权机制，实现了机械臂移动模型与夹持器操纵模型之间的动态协调。在广泛的评估中，DAM-VLA 在模拟环境（SIMPLER、FurnitureBench）和真实场景中均取得了优于现有最先进 VLA 方法的成功率，展示了从标准取放任务到具有挑战性的长时程及接触密集型任务的稳健泛化能力。

## Abstract
In dynamic environments such as warehouses, hospitals, and homes, robots must seamlessly transition between gross motion and precise manipulations to complete complex tasks. However, current Vision-Language-Action (VLA) frameworks, largely adapted from pre-trained Vision-Language Models (VLMs), often struggle to reconcile general task adaptability with the specialized precision required for intricate manipulation. To address this challenge, we propose DAM-VLA, a dynamic action model-based VLA framework. DAM-VLA integrates VLM reasoning with diffusion-based action models specialized for arm and gripper control. Specifically, it introduces (i) an action routing mechanism, using task-specific visual and linguistic cues to select appropriate action models (e.g., arm movement or gripper manipulation), (ii) a dynamic action model that fuses high-level VLM cognition with low-level visual features to predict actions, and (iii) a dual-scale action weighting mechanism that enables dynamic coordination between the arm-movement and gripper-manipulation models. Across extensive evaluations, DAM-VLA achieves superior success rates compared to state-of-the-art VLA methods in simulated (SIMPLER, FurnitureBench) and real-world settings, showing robust generalization from standard pick-and-place to demanding long-horizon and contact-rich tasks.

---

## 论文详细总结（自动生成）

以下是对论文 **《DAM-VLA: A Dynamic Action Model-Based Vision-Language-Action Framework for Robot Manipulation》** 的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）虽然具备强大的泛化能力，但在处理需要高精度的复杂操纵任务时，往往难以兼顾“通用任务适应性”与“动作精确性”。
*   **研究背景**：作者发现机械臂的“大范围移动（Arm Movement）”与“精细夹持操纵（Gripper Manipulation）”在路径约束、视觉关注点及数据分布上存在显著差异。现有的单一动作头模型难以同时优化这两种截然不同的运动模式。

### 2. 方法论
DAM-VLA 框架通过集成预训练 VLM 的推理能力与专门的扩散动作模型（Diffusion Action Models），实现了动态的动作生成。其核心组件包括：
*   **动作路由机制（Action Routing）**：利用 VLM 的推理潜变量（Reasoning Latent），通过一个分类头预测当前应执行“移动”还是“操纵”。该路由受夹持器状态变化的监督。
*   **动态动作模型（Dynamic Action Model）**：
    *   采用双头扩散模型结构。
    *   **机械臂移动模型**：结合 VLM 的认知特征与视觉全局特征（Class Token），负责大范围路径规划。
    *   **夹持器操纵模型**：结合 VLM 特征与视觉局部特征（Register Token），负责精细的触碰和抓取。
*   **双尺度动作加权（Dual-Scale Action Weighting）**：
    *   **轨迹级权重**：在夹持状态切换点附近使用非对称高斯分布，强化关键操纵阶段的监督。
    *   **动作块级权重**：对预测序列进行指数衰减加权，优先保证近期动作的准确性。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Open X-Embodiment 数据集中的 Fractal 和 BridgeDataV2 子集。
    *   **微调**：FurnitureBench（500条专家轨迹）、真实世界数据（50条演示轨迹）。
*   **Benchmark 与场景**：
    *   **SIMPLER 仿真**：评估 Google Robot 和 WidowX 机器人在视觉匹配（VM）和变体聚合（VA，含光照/背景变化）下的表现。
    *   **FurnitureBench**：评估长时程、接触密集型的“单腿组装”任务。
    *   **真实世界**：使用 Franka 机器人进行取放杯子的任务（含分布内与分布外测试）。
*   **对比方法**：RT-1, RT-2-X, OpenVLA, CogACT, $\pi_0$, SpatialVLA 等最先进模型。

### 4. 资源与算力
*   **硬件配置**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练时长**：
    *   **预训练**：在 8 台 H100 上运行约 **2 天**，Batch Size 为 256。
    *   **微调**：采用相同的硬件配置和超参数进行特定任务的学习。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 SIMPLER 仿真中涵盖了 2 种机器人、8 类任务、数百次测试。
    *   在 FurnitureBench 中进行了 50 次随机初始位置的试验。
    *   在真实世界中进行了 80 次试验（涵盖 16 种不同条件）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了成功率，还通过消融实验验证了视觉 Token、双尺度权重、双动作头等每个组件的有效性。引入 VA（变体聚合）设置有效证明了模型在动态环境下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：DAM-VLA 在 SIMPLER 仿真中的平均成功率显著超过 CogACT 和 OpenVLA（如在 Google Robot VA 设置下达到 81%，远超 CogACT 的 62%）。
*   **长时程任务优势**：在复杂的家具组装任务中，DAM-VLA 在“拧螺丝”等接触密集型步骤表现出更高的精度。
*   **泛化能力**：在真实世界的分布外（OOD）测试中，DAM-VLA 保持了 82.2% 的成功率，证明了其对未见背景和物体的强大适应力。

### 7. 优点（亮点）
*   **解耦设计**：巧妙地将移动与操纵解耦，并利用 VLM 的推理能力进行动态路由，解决了单一模型“样样通样样松”的问题。
*   **视觉特征精细化**：针对不同动作类型分别输入全局（Class Token）和局部（Register Token）视觉特征，符合机器人学的直觉。
*   **加权机制创新**：双尺度加权机制有效解决了扩散模型在时序预测上的不确定性问题。

### 8. 不足与局限
*   **任务多样性限制**：虽然涉及了组装任务，但实验核心仍集中在取放（Pick-and-Place）类动作，尚未广泛扩展到擦拭、折叠等更多样化的任务族。
*   **精细协调仍有提升空间**：在某些特定任务（如“绿块叠黄块”）中，虽然性能有所提升，但相比其他任务，增幅较小，说明极高精度的微调协调仍是挑战。
*   **路由扩展性**：目前仅在两个动作模型间路由，未来若要处理更复杂的多种工具切换，路由信号的复杂度和准确性将面临更大考验。

（完）

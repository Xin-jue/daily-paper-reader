---
title: "RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks"
title_zh: RoboClaw：一种用于可扩展长程机器人任务的智能体框架
authors: "Ruiying Li, Yunlang Zhou, YuYao Zhu, Kylin Chen, Jingyuan Wang, Sukai Wang, Kongtao Hu, Minhui Yu, Bowen Jiang, Zhan Su, Jiayao Ma, Xin He, Yongjian Shen, Yang Yang, Guanghui Ren, Maoqing Yao, Wenhao Wang, Yao Mu"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11558v2"
tags: ["query:q10"]
score: 10.0
evidence: 用于长程机器人操作和数据采集的VLA系统
tldr: RoboClaw是一个统一的数据采集、策略学习与任务执行的智能体框架，旨在解决长程机器人任务中数据采集难和多策略执行脆弱的问题。它引入了“纠缠动作对（EAP）”机制，通过前向操作与逆向恢复动作的耦合实现自主复位和持续数据采集。该框架利用视觉语言模型（VLM）进行高层推理和策略调度，显著降低了人工干预，提升了长程任务的成功率和系统的可扩展性。
selection_source: fresh_fetch
motivation: 现有的机器人学习流程在数据采集、策略学习和部署上相互分离，导致对人工环境重置的高度依赖以及长程任务执行的脆弱性。
method: 提出RoboClaw框架，通过纠缠动作对（EAP）实现自主复位的数据采集，并利用VLM驱动的控制器进行高层推理与动态策略编排。
result: "在真实世界任务中，该方法比基准方法在长程任务成功率上提升了25%，同时减少了53.7%的人工投入时间。"
conclusion: RoboClaw通过统一采集与执行阶段的上下文语义，有效提升了机器人系统的自主性、稳定性和长程任务处理能力。
---

## 摘要
视觉-语言-动作（VLA）系统在语言驱动的机器人操控方面展现出强大潜力。然而，将其扩展到长程任务仍然具有挑战性。现有的流程通常将数据采集、策略学习和部署分开，导致对人工环境重置的严重依赖以及多策略执行的脆弱性。我们提出了 RoboClaw，这是一个将数据采集、策略学习和任务执行统一在单个 VLM 驱动控制器下的智能体机器人框架。在策略层面，RoboClaw 引入了纠缠动作对（EAP），将前向操控行为与逆向恢复动作耦合，形成用于自主数据采集的自重置循环。该机制能够以极少的人工干预实现持续的同策略（on-policy）数据获取和迭代策略优化。在部署期间，同一个智能体执行高层推理，并动态编排已学习的策略原语以完成长程任务。通过在采集和执行过程中保持一致的上下文语义，RoboClaw 减少了两个阶段之间的不匹配，并提高了多策略的鲁棒性。在真实世界操控任务中的实验表明，与传统的开环流程相比，该框架提高了稳定性和可扩展性，同时显著减少了机器人整个生命周期中的人力投入，在长程任务上的成功率比基准方法提高了 25%，并减少了 53.7% 的人力时间投入。

## Abstract
Vision-Language-Action (VLA) systems have shown strong potential for language-driven robotic manipulation. However, scaling them to long-horizon tasks remains challenging. Existing pipelines typically separate data collection, policy learning, and deployment, resulting in heavy reliance on manual environment resets and brittle multi-policy execution. We present RoboClaw, an agentic robotics framework that unifies data collection, policy learning, and task execution under a single VLM-driven controller. At the policy level, RoboClaw introduces Entangled Action Pairs (EAP), which couple forward manipulation behaviors with inverse recovery actions to form self-resetting loops for autonomous data collection. This mechanism enables continuous on-policy data acquisition and iterative policy refinement with minimal human intervention. During deployment, the same agent performs high-level reasoning and dynamically orchestrates learned policy primitives to accomplish long-horizon tasks. By maintaining consistent contextual semantics across collection and execution, RoboClaw reduces mismatch between the two phases and improves multi-policy robustness. Experiments in real-world manipulation tasks demonstrate improved stability and scalability compared to conventional open-loop pipelines, while significantly reducing human effort throughout the robot lifecycle, achieving a 25% improvement in success rate over baseline methods on long-horizon tasks and reducing human time investment by 53.7%.

---

## 论文详细总结（自动生成）

这是一份关于论文《RoboClaw: An Agentic Framework for Scalable Long-Horizon Robotic Tasks》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何解决视觉-语言-动作（VLA）系统在处理**长程（Long-Horizon）机器人任务**时的可扩展性难题。
*   **研究背景**：
    *   **数据采集成本高**：传统流程依赖人工演示和频繁的手动环境重置。
    *   **执行脆弱性**：数据采集、策略学习和部署阶段相互分离，导致语义理解不一致和分布偏移（Mismatch），微小误差在长程任务中会迅速累积导致失败。
    *   **缺乏自主性**：现有系统在遇到错误时往往无法自主恢复，高度依赖人工干预。

### 2. 方法论
RoboClaw 提出了一个统一的智能体框架，将 VLM（视觉语言模型）作为“元控制器”，协调数据采集与任务执行。
*   **核心思想**：通过一个闭环的智能体架构，统一感知、推理和动作，使机器人能够像人类一样通过“思考-尝试-纠错”来完成复杂任务。
*   **关键技术细节**：
    *   **纠缠动作对（Entangled Action Pairs, EAP）**：为每个前向操作策略（如“放入抽屉”）配对一个逆向恢复策略（如“从抽屉取出”），形成自重置循环，实现无需人工干预的持续在线数据采集。
    *   **三层抽象架构**：
        1.  **策略层（Policies）**：基于 $\pi_{0.5}$ VLA 模型生成底层电机动作。
        2.  **工具层（Tools）**：通过模型上下文协议（MCP）提供接口（如启动策略、环境摘要、请求人工）。
        3.  **技能层（Skills）**：编排工具以完成特定程序。
    *   **结构化记忆机制**：包含角色身份（Role）、任务级记忆（Task-level, 记录进度）和工作记忆（Working, 记录短期上下文），支持 VLM 进行思维链（CoT）推理。

### 3. 实验设计
*   **实验场景**：在真实世界中设计了四个典型场景：卧室梳妆台、厨房架子、书房书桌、便利店货架。
*   **具体任务**：包括放置润肤露、放置妆前乳（含关抽屉）、插入口红（高精度）、纸巾擦拭（持续接触）。
*   **对比基准（Benchmark）**：
    *   **手动采集基准**：完全依赖人工演示和重置。
    *   **端到端 VLA 基准**：不使用 RoboClaw 框架，直接运行训练好的策略。
    *   **理论预期成功率**：将四个子任务独立成功率相乘得出的预期值。

### 4. 资源与算力
*   **硬件平台**：灵动（Agibot）G01 双臂移动操作机器人，配备 OmniPicker 夹爪。
*   **模型微调**：使用 LoRA 技术对 $\pi_{0.5}$ 进行微调。
*   **训练参数**：bfloat16 精度，Batch size 为 16，训练 10k 步，学习率 $2.5 \times 10^{-5}$。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但提到了推理步数为 3 步，并使用了梯度检查点（Gradient Checkpointing）技术。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 个子任务，每个任务进行了 50 次测试，并观察了 5 个迭代周期的性能提升。
    *   长程任务（梳妆台整理）进行了 20 次完整试验。
*   **充分性评价**：实验覆盖了从单点技能到复杂长程链条的完整流程。通过对比不同迭代次数下的成功率，客观展示了“在线采集-迭代优化”的效果。实验设计较为公平，对比了纯人工成本和纯模型执行的差异。

### 6. 主要结论与发现
*   **效率提升**：RoboClaw 将人工时间投入减少了 **53.7%**，人工干预频率降低了约 8 倍。
*   **性能增强**：在长程任务中，RoboClaw 的成功率比基准方法提高了 **25%**。
*   **闭环学习**：通过 EAP 机制，策略成功率随迭代显著提升（如口红插入从 2/50 提升至 23/50）。
*   **故障处理**：系统能区分“非降级失败”（可直接重试）和“降级失败”（需调用恢复策略），展现了初步的自愈能力。

### 7. 优点与亮点
*   **统一性**：打破了数据采集和部署的壁垒，使机器人能在执行任务的同时积累经验。
*   **自重置机制**：EAP 的设计非常巧妙，解决了真实世界机器人学习中最头疼的环境重置问题。
*   **语义一致性**：由于采集和执行共用同一个 VLM 控制器，减少了任务理解上的偏差。

### 8. 不足与局限
*   **延迟问题**：依赖云端大模型（VLM）进行推理可能带来较高的延迟，影响实时响应。
*   **逆向动作假设**：EAP 依赖于任务是“可逆”的，对于某些不可逆任务（如切菜、焊接）或极其复杂的动态环境，构建逆向恢复策略可能非常困难。
*   **硬件依赖**：实验主要在特定型号机器人上完成，其跨平台的泛化能力尚待验证。
*   **安全风险**：虽然有“呼叫人工”接口，但在完全自主采集过程中，若 VLA 策略发生严重碰撞，系统保护机制的描述较少。

（完）
